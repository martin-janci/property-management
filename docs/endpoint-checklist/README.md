# Endpoint Checklist — api-server + reality-server (BIT-247)

Authoritative, test-verified classification of **every HTTP endpoint** in both backend
servers. Generated by a deterministic, reproducible audit of `origin/dev`
(`backend/servers/api-server/src/routes/**` and `backend/servers/reality-server/src/routes/**`),
cross-referenced against the `tests/*.rs` suites. See [`_SPEC.md`](./_SPEC.md) for the
exact method and taxonomy; per-domain detail is in [`groups/`](./groups).

> Snapshot: `origin/dev` @ `a1f6dfc1f` · 2026-07-22 · Updated through BIT-268 Wave 9: BIT-414 faults +13 (PR#1979), BIT-415 finance reconcile +74, BIT-417 finance batch 4 +76, BIT-420 finance bucket B +19 — finance now 234/234 (100%). BIT-416 admin-platform batch 2 +19 (PR#1980). Plus ad-hoc coverage PRs: airbnb OAuth routes +4 (PR#2166 connect/disconnect, PR#2189 direct-connect/availability-sync), Booking.com +2 (PR#2167 status/disconnect), MFA regenerate-backup-codes +1 (PR#2255), RAG index write +1 new (PR#2173), RAG migrate +1 new (PR#2291), report-schedule create +1 (PR#2179 stub→real+tested), my-listings+analytics +2 new reality (PR#2183), migration pagination +1 (PR#2063).

## Headline numbers

There are **two different questions** the board is really asking, and they have very
different answers. Read both.

| Metric | Count | % of all endpoints |
|---|---|---|
| **DONE** — real handler **and** a passing happy-path test | **1501** | **74.7%** |
| Partial — real handler, **no** happy-path test | 474 | 23.6% |
| Stub — handler missing/mock/`501`/unmounted | 35 | 1.7% |
| Missing — in spec/use-case, no handler | 0 | 0.0% ✓ confirmed by BIT-269 |
| **Total endpoints** | **2010** | 100% |

- **Test-verified DONE = 74.7%.** Wave 9 headline: finance at **100%** (234/234), governance at **100%** (219/219). Faults-maintenance +13 (outages happy-path), admin-platform +19+1 (batch 2 + migration pagination). Six ad-hoc PRs added Airbnb OAuth, Booking.com, MFA, RAG, report-schedule, and reality-server realtor-listings coverage. Four new endpoints added to the surface (2 reality-server, 2 ai-automation).
- **Implementation breadth = 98.3%** (`done + partial`). Only **1.7% are stubs/mock/unmounted**.

**The gap is test coverage, not implementation.** The overwhelming majority of the
codebase's tests are **authorization / cross-org IDOR / RBAC rejection** suites that
assert `401/403/404` and deliberately never reach a `2xx`. They harden the security
boundary (which is the right first priority) but they do not prove the feature works,
so they cannot promote an endpoint to `done`. Waves 4–9 drove the headline from 9.0% → 74.7%.

> **Quarantine note:** BIT-351 quarantined 718 pre-existing failing tests (PR#1948). BIT-360 is
> repairing the quarantined suite wave-by-wave; batches 1–2 (PR#1957, PR#1964) re-enabled
> ~50+ tests across 8+ files. Some backfill files remain partially quarantined. Repair ongoing.

## Per-group summary

| Group | Endpoints | Done | Partial | Stub | % Done (tested) | % Implemented |
|---|---|---|---|---|---|---|
| [auth-identity](./groups/auth-identity.md) | 80 | 76 | 4 | 0 | 95.0% | 100% |
| [notifications-comms](./groups/notifications-comms.md) | 87 | 82 | 5 | 0 | 94.3% | 100% |
| [documents-forms](./groups/documents-forms.md) | 118 | 70 | 48 | 0 | 59.3% | 100% |
| [reality-server](./groups/reality-server.md) | 98 | 93 | 4 | 1 | 94.9% | 99.0% |
| [faults-maintenance](./groups/faults-maintenance.md) | 259 | 93 | 166 | 0 | 35.9% | 100% |
| [leasing](./groups/leasing.md) | 84 | 82 | 1 | 1 | 97.6% | 98.8% |
| [finance](./groups/finance.md) | 234 | 234 | 0 | 0 | **100%** | 100% |
| [integrations-ecosystem](./groups/integrations-ecosystem.md) | 211 | 113 | 82 | 16 | 53.6% | 92.4% |
| [org-property](./groups/org-property.md) | 94 | 48 | 46 | 0 | 51.1% | 100% |
| [admin-platform](./groups/admin-platform.md) | 159 | 77 | 82 | 0 | 48.4% | 100% |
| [compliance-screening](./groups/compliance-screening.md) | 85 | 69 | 15 | 1 | 81.2% | 98.8% |
| [governance](./groups/governance.md) | 219 | 219 | 0 | 0 | **100%** | 100% |
| [analytics-portals](./groups/analytics-portals.md) | 173 | 159 | 14 | 0 | 91.9% | 100% |
| [ai-automation](./groups/ai-automation.md) | 95 | 86 | 7 | 2 | 90.5% | 97.9% |
| **Total** | **2010** | **1501** | **474** | **35** | **74.7%** | **98.3%** |

Wave 9 confirmed (2026-07-22):
- BIT-414 faults-maintenance +13 (PR#1979): outages happy-path — 30.9% → **35.9%** (80→93 done)
- BIT-415 finance reconciliation +74 (PR#1981): promoted endpoints whose passing tests already existed on dev — budgets, subscriptions, property_valuation, accounting, reports. Finance 27.8% → 59.4% (65→139)
- BIT-417 finance batch 4 +76 (PR#1982): un-quarantined 5 `#[ignore]`d happy-path suites (multi_currency 27, market_pricing 31, person_months 10, financial+financial_reports 8). Finance 59.4% → 91.9% (139→215)
- BIT-420 finance bucket B +19 (PR#1983): last untested finance endpoints (property_valuation comparables/adjustments/features/reports 15, subscriptions admin 2, accounting upload_statement 1, reports get_execution 1). Finance 91.9% → **100%** (215→234) — **second group at 100%**
- BIT-416 admin-platform batch 2 +19 (PR#1980): 19 mutation endpoints. 35.8% → **42.8%** (57→76)

Ad-hoc coverage PRs landed on dev between Wave 8 and Wave 9 snapshots:
- PR#2166 (airbnb install routes): connect + disconnect have 2xx happy-path; sync has only 404 tests → stays partial. integrations +2
- PR#2189 (airbnb direct-connect + availability-sync): both handlers have 2xx happy-path tests. integrations +2
- PR#2167 (Booking.com handlers): status + disconnect have 2xx coverage; connect/sync/push-availability/push-rates have only 4xx tests → partial. integrations +2
- PR#2173 (RAG index write): new `POST /api/v1/ai/llm/rag/index` handler added + tested (persist, idempotent-upsert). ai-automation +1 done, +1 total endpoint
- PR#2291 (RAG migrate pgvector): new `POST /api/v1/ai/llm/rag/migrate` handler added + tested. ai-automation +1 done, +1 total endpoint
- PR#2179 (report-schedule create): `POST /api/v1/reports/schedules` was stub, now real + 2xx tested. analytics-portals +1
- PR#2183 (realtor my-listings + analytics): new `GET /api/v1/my/listings` + `GET /api/v1/my/listings/{id}/analytics` added to reality-server + tested. +2 done, +2 total endpoints
- PR#2255 (MFA regenerate backup codes RLS): `POST /api/v1/auth/mfa/backup-codes/regenerate` now has 2xx coverage. auth-identity +1
- PR#2063 (migration pagination): `GET /api/v1/admin/migrations` covered with 2xx clamp tests. admin-platform +1

Wave 8 confirmed (2026-06-30):
- BIT-405 faults-maintenance +7 (PR#1960): 28.2% → 30.9%
- BIT-406 finance +14 (PR#1962): 21.8% → 27.8%
- BIT-407 governance delegations/neighbors +11 (PR#1961): contributing to governance
- BIT-408 governance batch 3 (PR#1963): violations, disputes, community, packages/visitors, reserve-funds — governance now **100%** (40.6% → 100%)
- BIT-360 quarantine repair batches 1–2 (PR#1957, PR#1964): leasing +3 (rentals backfill re-enabled), auth +1

Overall: 1132 → 1287 → 1361 → 1437 → 1456 → **1501 done** (56.4% → 64.1% → 67.8% → 71.6% → 72.6% → 74.7%). Governance and finance are the first two groups at 100%.

Biggest remaining gaps: faults-maintenance 166 partial, integrations 82, admin-platform 82, documents-forms 48, org-property 46.

BIT-257 (PR#1863) deleted 57 unmounted ROADMAP stubs (public_api.rs 43 + vendor_portal.rs 14),
reducing total endpoints from 2063 → 2006 and stubs from 92 → 35. Four new endpoints were
subsequently added: RAG index write, RAG migrate, realtor my-listings, realtor per-listing analytics
(all tested from the start). Total now 2010.

## Gaps & stubs (the 35 not-implemented endpoints)

Stubs cluster into a small number of whole modules. BIT-257 (PR#1863) deleted 57 of the
92 stubs (public_api.rs + vendor_portal.rs), leaving 35.

| Module / surface | Stubs | Why | Owning epic / ref |
|---|---|---|---|
| `routes/integrations/sync.rs` (calendars/accounting/esign/video CRUD) | 30 | **Unmounted**; Epic-61 tables exist in no migration | Epic 61, PAP-122 |
| `routes/compliance.rs` `gdpr/data-exports` | 1 | Returns empty exports + zeroed counts ("for now" TODO) | GDPR |
| `routes/rentals.rs` `guests/{id}/id-document/extract` | 1 | Real handler, but wired OCR provider returns `501 OCR_NOT_CONFIGURED` | Guest ID / OCR |
| reality `agency_imports` `test-connection` | 1 | Returns hardcoded "(stub response)"; provider call faked (auth+SSRF checks real) | Reality imports |
| Other (scattered, not clustered) | ~2 | Various — see per-group files | — |

**One dominant theme: unmounted roadmap scaffolds** (`integrations/sync` = 30 stubs) —
code exists but is intentionally not wired and has no backing migration. The previously-mock
modules (`regional_compliance`, `data_residency`, `ai/ocr`) were replaced with real
DB-backed implementations in BIT-256 and are no longer stubs. `public_api.rs` (43) and
`vendor_portal.rs` (14) were deleted by BIT-257.

## Current vs. future state

**Current state.** The API surface is broad and largely built: 1975 of 2010 endpoints
(98.3%) have real handlers. Waves 4–9 (BIT-268) drove 74.7% of endpoints to proven,
happy-path tested status. **Governance (219) and Finance (234) are fully tested at 100%.**

**Roadmap to reach high test-verified % (priority order):**

1. **De-risk the remaining mocks (3 endpoints).** `compliance.rs` gdpr/data-exports,
   `rentals.rs` id-document/extract, and reality `agency_imports` test-connection still
   return stub/fabricated data. *High priority — correctness/compliance risk.*
2. **Faults-maintenance remaining (259 endpoints, 35.9% tested).** 166 partial — largest
   remaining gap. Multiple batch PRs needed.
3. **Admin-platform remaining (159 endpoints, 48.4% tested).** 82 partial.
4. **Integrations remaining (211 endpoints, 53.6% tested).** 82 partial (Booking.com
   connect/sync/push-availability/push-rates + Airbnb sync + ~64 other integrations endpoints).
5. **Documents-forms remaining (118 endpoints, 59.3% tested).** 48 partial.
6. **Org-property remaining (94 endpoints, 51.1% tested).** 46 partial.
7. **BIT-360 quarantine repair.** ~80 tests remain quarantined from BIT-351; re-enabling
   confirmed-passing ones boosts all groups further without new test authoring.
8. **Decide fate of `integrations/sync.rs` (30 stubs, Epic 61).** Either add migrations
   and mount or delete — keeping unmounted `501` code inflates the stub count.

## Caveats / method notes

- No local Rust toolchain on the audit host, so coverage is determined by **static
  cross-reference** (test files hit endpoints by URI string, e.g. `.uri("/api/v1/...")`).
  This is deterministic and reproducible but conservative: a test that exercises a path
  without a clear `2xx` assertion is treated as **not** proving the success path.
- Several test files exercise the **repository layer directly** rather than the HTTP
  endpoint (noted per group). Those do not count toward `done` for the endpoint.
- A handful of routers were **dead code** (defined but never mounted, e.g.
  `signatures::document_signature_router`) — excluded from counts and flagged in group
  notes. [BIT-257](/BIT/issues/BIT-257) **deleted** `signatures::document_signature_router`
  and its `*_for_doc` handlers (the live `*_for_document` surface is unchanged).
- Endpoint counts treat each `(method, path)` pair as one endpoint; dual-mounted
  routers (e.g. `market_pricing` at `/pricing` + `/market-pricing`) are counted once.
- **`Missing=0` caveat resolved (BIT-269).** A spec→handler diff (`scripts/diff_endpoints_v3.py`)
  surfaced 65 apparent gaps in the OpenAPI spec vs. mounted handlers. All 65 were
  confirmed to be **path/method divergences** between a stale spec snapshot and the
  settled implementation. No operation in the spec or use-case catalog lacks a mounted
  handler. See [`_SPEC_DIFF.md`](./_SPEC_DIFF.md) for the full breakdown.
- **Reality-server audit correction (PR#1923):** The group file had been stale at `done:4`
  while 28 rows were already proven by pre-existing tests. PR#1923 corrected to 74 done;
  PR#1938 added ~17 more, reaching 91 done (94.8%).
- **Governance group file updated by BIT-408 (PR#1963):** Coder reconciled all partial rows
  against current test files; group file now shows 219/219 done. `delete_evidence` was
  noted as potentially remaining partial but the group file shows 0 partial (likely covered
  by one of the multi-endpoint test functions in violations/disputes suites).
- **Per-group Endpoints column drift:** The per-group Endpoints column reflects the initial
  Wave-4 audit count and has not been updated for every route added or removed since; the
  column sum (≈1996) differs from the headline total (2010) by ~14, representing net route
  additions across multiple waves. The **Done/Partial/Stub** columns and headline counts are
  authoritative; per-row Endpoints are approximate.
