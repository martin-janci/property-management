# Endpoint Checklist — api-server + reality-server (BIT-247)

Authoritative, test-verified classification of **every HTTP endpoint** in both backend
servers. Generated by a deterministic, reproducible audit of `origin/dev`
(`backend/servers/api-server/src/routes/**` and `backend/servers/reality-server/src/routes/**`),
cross-referenced against the `tests/*.rs` suites. See [`_SPEC.md`](./_SPEC.md) for the
exact method and taxonomy; per-domain detail is in [`groups/`](./groups).

> Snapshot: `origin/dev` @ `a65331b` · 2026-06-27 · 208 route source files.

## Headline numbers

There are **two different questions** the board is really asking, and they have very
different answers. Read both.

| Metric | Count | % of all endpoints |
|---|---|---|
| **DONE** — real handler **and** a passing happy-path test | **118** | **5.7%** |
| Partial — real handler, **no** happy-path test | 1820 | 88.2% |
| Stub — handler missing/mock/`501`/unmounted | 125 | 6.1% |
| Missing — in spec/use-case, no handler | 0 | 0.0% ✓ confirmed by BIT-269 |
| **Total endpoints** | **2063** | 100% |

- **Test-verified DONE = 5.7%.** This is the strict number the ticket defines: an
  endpoint only counts as done if a test exercises its success path. By that bar the
  platform is ~5.7% done.
- **Implementation breadth = 93.9%** (`done + partial`). Almost every endpoint has a
  real, DB/repo-backed handler. Only **6.1% are stubs/mock/unmounted**.

**The gap is test coverage, not implementation.** The overwhelming majority of the
codebase's tests are **authorization / cross-org IDOR / RBAC rejection** suites that
assert `401/403/404` and deliberately never reach a `2xx`. They harden the security
boundary (which is the right first priority) but they do not prove the feature works,
so they cannot promote an endpoint to `done`. Closing the 5.7% → high-90s gap is
primarily a **happy-path integration-test** effort, not a build effort.

## Per-group summary

| Group | Endpoints | Done | Partial | Stub | % Done (tested) | % Implemented |
|---|---|---|---|---|---|---|
| [auth-identity](./groups/auth-identity.md) | 80 | 28 | 38 | 14 | 35.0% | 82.5% |
| [notifications-comms](./groups/notifications-comms.md) | 87 | 23 | 64 | 0 | 26.4% | 100% |
| [documents-forms](./groups/documents-forms.md) | 118 | 15 | 103 | 0 | 12.7% | 100% |
| [reality-server](./groups/reality-server.md) | 96 | 4 | 91 | 1 | 4.2% | 99.0% |
| [faults-maintenance](./groups/faults-maintenance.md) | 273 | 10 | 249 | 14 | 3.7% | 94.9% |
| [leasing](./groups/leasing.md) | 84 | 3 | 80 | 1 | 3.6% | 98.8% |
| [finance](./groups/finance.md) | 234 | 8 | 226 | 0 | 3.4% | 100% |
| [integrations-ecosystem](./groups/integrations-ecosystem.md) | 268 | 9 | 186 | 73 | 3.4% | 72.8% |
| [org-property](./groups/org-property.md) | 94 | 3 | 91 | 0 | 3.2% | 100% |
| [admin-platform](./groups/admin-platform.md) | 159 | 5 | 154 | 0 | 3.1% | 100% |
| [compliance-screening](./groups/compliance-screening.md) | 85 | 2 | 63 | 20 | 2.4% | 76.5% |
| [governance](./groups/governance.md) | 219 | 5 | 214 | 0 | 2.3% | 100% |
| [analytics-portals](./groups/analytics-portals.md) | 173 | 3 | 170 | 0 | 1.7% | 100% |
| [ai-automation](./groups/ai-automation.md) | 93 | 0 | 91 | 2 | 0.0% | 97.8% |
| **Total** | **2063** | **118** | **1820** | **125** | **5.7%** | **93.9%** |

Auth-identity leads on test coverage (35%) — unsurprising, since auth got the earliest
and deepest test investment. AI/automation has **zero** happy-path coverage.

## Gaps & stubs (the 125 not-implemented endpoints)

Stubs cluster into a small number of whole modules. Fixing these modules clears ~98% of
the stub bucket.

| Module / surface | Stubs | Why | Owning epic / ref |
|---|---|---|---|
| `routes/public_api.rs` → `/api/v1/developer/*` | 43 | **Unmounted** roadmap scaffold; handlers return `501` | Epic 69, ROADMAP(PAP-24) |
| `routes/integrations/sync.rs` (calendars/accounting/esign/video CRUD) | 30 | **Unmounted**; Epic-61 tables exist in no migration | Epic 61, PAP-122 |
| `routes/regional_compliance.rs` | 19 | Entire module is **mock** — hardcoded data, ignores `AppState`, no DB | Epic 72 (SK/CZ) |
| `routes/data_residency.rs` | 14 | Entire module is **mock** — returns fabricated regions/audit/PDF | Epic 71 |
| `routes/vendor_portal.rs` | 14 | **Unmounted** (ROADMAP PAP-24); every handler `501`. `vendors/` is the live surface | Epic — vendor portal |
| `routes/ai/ocr.rs` | 2 | `meter-reading` always `501`; `correction` discards payload | Epic 64 (AI/OCR) |
| `routes/compliance.rs` `gdpr/data-exports` | 1 | Returns empty exports + zeroed counts ("for now" TODO) | GDPR |
| `routes/rentals.rs` `guests/{id}/id-document/extract` | 1 | Real handler, but wired OCR provider returns `501 OCR_NOT_CONFIGURED` | Guest ID / OCR |
| reality `agency_imports` `test-connection` | 1 | Returns hardcoded "(stub response)"; provider call faked (auth+SSRF checks real) | Reality imports |

**Two themes:** (1) **unmounted roadmap scaffolds** (`public_api`, `integrations/sync`,
`vendor_portal` = 87 stubs) — code exists but is intentionally not wired and has no
backing migration; (2) **mock modules** (`regional_compliance`, `data_residency` = 33
stubs) — wired and reachable but return fabricated data, which is a **correctness/▲risk
hazard** because callers can't tell mock from real. The mock modules should be
prioritized over the unmounted scaffolds: an unmounted route 404s honestly; a mock route
lies.

## Current vs. future state

**Current state.** The API surface is broad and largely built: 1938 of 2063 endpoints
(93.9%) have real handlers. Security posture is the most-tested dimension. But only 5.2%
of endpoints have a test that proves the feature actually works end-to-end, and two
compliance-adjacent modules return mock data in production paths.

**Roadmap to a high test-verified % (grouped, in priority order):**

1. **De-risk the mocks (33 endpoints).** Replace `regional_compliance.rs` and
   `data_residency.rs` mock handlers with real DB-backed implementations + tests, or
   gate them behind an explicit "not implemented" `501` so they stop returning
   fabricated data. *Highest priority — correctness/compliance risk.*
2. **Happy-path coverage for the high-traffic core (~1000 endpoints).** faults,
   finance, governance, org-property, admin-platform, analytics are ~100%-implemented
   but ~2-4% tested. Add success-path integration tests group-by-group. This is the
   single biggest lever on the headline number and is **test work, not build work**.
   Blocker to retire first: several suites note the auth harness "can't mint a tenant_id
   JWT," which is why so many tests call repos directly instead of issuing HTTP requests
   — fixing the JWT test fixture unblocks happy-path testing across many groups.
3. **Decide the fate of unmounted scaffolds (87 endpoints).** For `public_api` /
   `integrations/sync` / `vendor_portal`: either commit to the epic (add migrations,
   mount, implement, test) or delete the scaffold. Keeping unmounted `501` code inflates
   the surface and confuses audits.
4. **AI/automation (93 endpoints, 0% tested).** Real handlers, no happy-path coverage at
   all — needs a dedicated test pass; finish the two OCR stubs.

## Caveats / method notes

- No local Rust toolchain on the audit host, so coverage is determined by **static
  cross-reference** (test files hit endpoints by URI string, e.g. `.uri("/api/v1/...")`).
  This is deterministic and reproducible but conservative: a test that exercises a path
  without a clear `2xx` assertion is treated as **not** proving the success path.
- Several test files exercise the **repository layer directly** rather than the HTTP
  endpoint (noted per group). Those do not count toward `done` for the endpoint.
- A handful of routers are **dead code** (defined but never mounted, e.g.
  `signatures::document_signature_router`) — excluded from counts and flagged in group
  notes.
- Endpoint counts treat each `(method, path)` pair as one endpoint; dual-mounted
  routers (e.g. `market_pricing` at `/pricing` + `/market-pricing`) are counted once.
- **`Missing=0` caveat resolved (BIT-269).** A spec→handler diff (`scripts/diff_endpoints_v3.py`)
  surfaced 65 apparent gaps in the OpenAPI spec vs. mounted handlers. All 65 were
  confirmed to be **path/method divergences** between a stale spec snapshot and the
  settled implementation (e.g. `/votes` → `/voting`, flat `/units` → nested under
  `/buildings/{id}/units`, `PATCH` → `PUT` throughout, `/reservations` → `/bookings`).
  No operation in the spec or use-case catalog lacks a mounted handler. See
  [`_SPEC_DIFF.md`](./_SPEC_DIFF.md) for the full breakdown.
