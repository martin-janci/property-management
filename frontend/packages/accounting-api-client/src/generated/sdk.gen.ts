// This file is auto-generated by @hey-api/openapi-ts

import type { Client, ClientMeta, Options as Options2, RequestResult, TDataShape } from './client';
import { client } from './client.gen';
import type { AddTagData, AddTagErrors, AddTagResponses, ConfirmTwoFactorData, ConfirmTwoFactorErrors, ConfirmTwoFactorResponses, CreateCompanyData, CreateCompanyErrors, CreateCompanyResponses, CreateContactData, CreateContactErrors, CreateContactResponses, CreateCreditNoteData, CreateCreditNoteErrors, CreateCreditNoteResponses, CreateInvoiceData, CreateInvoiceErrors, CreateInvoiceResponses, CreateItemData, CreateItemErrors, CreateItemResponses, CreateNumberingSeriesData, CreateNumberingSeriesErrors, CreateNumberingSeriesResponses, CreateShareLinkData, CreateShareLinkErrors, CreateShareLinkResponses, DeactivateContactData, DeactivateContactErrors, DeactivateContactResponses, DeactivateMemberData, DeactivateMemberErrors, DeactivateMemberResponses, DeleteInvoiceData, DeleteInvoiceErrors, DeleteInvoiceResponses, DuplicateInvoiceData, DuplicateInvoiceErrors, DuplicateInvoiceResponses, EnrollTwoFactorData, EnrollTwoFactorErrors, EnrollTwoFactorResponses, GetBillingData, GetBillingErrors, GetBillingResponses, GetCompanyData, GetCompanyErrors, GetCompanyResponses, GetCompanySettingsData, GetCompanySettingsErrors, GetCompanySettingsResponses, GetContactData, GetContactErrors, GetContactResponses, GetInvoiceData, GetInvoiceErrors, GetInvoiceResponses, GetItemData, GetItemErrors, GetItemResponses, GrantAccountantAccessData, GrantAccountantAccessErrors, GrantAccountantAccessResponses, InviteUserData, InviteUserErrors, InviteUserResponses, IssueInvoiceData, IssueInvoiceErrors, IssueInvoiceResponses, ListAddressesData, ListAddressesErrors, ListAddressesResponses, ListAuditLogData, ListAuditLogErrors, ListAuditLogResponses, ListBankAccountsData, ListBankAccountsErrors, ListBankAccountsResponses, ListCategoriesData, ListCategoriesErrors, ListCategoriesResponses, ListContactsData, ListContactsErrors, ListContactsResponses, ListInvoiceItemsData, ListInvoiceItemsErrors, ListInvoiceItemsResponses, ListInvoicesData, ListInvoicesErrors, ListInvoicesResponses, ListItemsData, ListItemsErrors, ListItemsResponses, ListLinksData, ListLinksErrors, ListLinksResponses, ListMyCompaniesData, ListMyCompaniesErrors, ListMyCompaniesResponses, ListNumberingSeriesData, ListNumberingSeriesErrors, ListNumberingSeriesResponses, ListPriceLevelsData, ListPriceLevelsErrors, ListPriceLevelsResponses, ListUnitsData, ListUnitsErrors, ListUnitsResponses, ListVatRatesData, ListVatRatesErrors, ListVatRatesResponses, LivenessData, LivenessResponses, MergeContactData, MergeContactErrors, MergeContactResponses, ReadinessData, ReadinessErrors, ReadinessResponses, RevokeAccountantAccessData, RevokeAccountantAccessErrors, RevokeAccountantAccessResponses, RevokeShareLinkData, RevokeShareLinkErrors, RevokeShareLinkResponses, SetExchangeRateData, SetExchangeRateErrors, SetExchangeRateResponses, SetMemberRoleData, SetMemberRoleErrors, SetMemberRoleResponses, SwitchCompanyData, SwitchCompanyErrors, SwitchCompanyResponses, UpdateContactData, UpdateContactErrors, UpdateContactResponses, UpdateInvoiceData, UpdateInvoiceErrors, UpdateInvoiceResponses, UpdateItemData, UpdateItemErrors, UpdateItemResponses, UpsertCompanySettingsData, UpsertCompanySettingsErrors, UpsertCompanySettingsResponses, ViewSharedInvoiceData, ViewSharedInvoiceErrors, ViewSharedInvoiceResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * Grant an accountant ongoing access to the active company (UC-ACC-01.7).
 * Admin-only. The accountant is a cross-company principal; this creates a
 * per-company membership edge with a read+export role (default read-only). The
 * accountant can then switch into this company alongside their other clients.
 */
export const grantAccountantAccess = <ThrowOnError extends boolean = false>(options: Options<GrantAccountantAccessData, ThrowOnError>): RequestResult<GrantAccountantAccessResponses, GrantAccountantAccessErrors, ThrowOnError> => (options.client ?? client).post<GrantAccountantAccessResponses, GrantAccountantAccessErrors, ThrowOnError>({
    url: '/api/v1/accounts/accountant-access',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke an accountant's access to the ACTIVE company only (UC-ACC-01.7).
 * Admin-only. Immediate and isolated: the accountant keeps access to every
 * other company that still grants it.
 */
export const revokeAccountantAccess = <ThrowOnError extends boolean = false>(options: Options<RevokeAccountantAccessData, ThrowOnError>): RequestResult<RevokeAccountantAccessResponses, RevokeAccountantAccessErrors, ThrowOnError> => (options.client ?? client).delete<RevokeAccountantAccessResponses, RevokeAccountantAccessErrors, ThrowOnError>({ url: '/api/v1/accounts/accountant-access/{user_id}', ...options });

/**
 * Billing/plan overview with usage-vs-limits (UC-ACC-01.8). MVP stub: reports
 * the default plan + live usage (companies owned, members in active company)
 * against the gate constants the create/invite handlers enforce, so the
 * displayed limits match the enforced limits.
 */
export const getBilling = <ThrowOnError extends boolean = false>(options?: Options<GetBillingData, ThrowOnError>): RequestResult<GetBillingResponses, GetBillingErrors, ThrowOnError> => (options?.client ?? client).get<GetBillingResponses, GetBillingErrors, ThrowOnError>({ url: '/api/v1/accounts/billing', ...options });

/**
 * List the companies (agendas) the authenticated user may operate
 * (UC-ACC-01.2/.3/.7). Spans every company the principal actively belongs to;
 * a revoked accountant / deactivated user drops off immediately (the repo
 * query requires an active membership). `active_company_id` echoes the tenant
 * the current JWT is scoped to (the principal's "last active company").
 */
export const listMyCompanies = <ThrowOnError extends boolean = false>(options?: Options<ListMyCompaniesData, ThrowOnError>): RequestResult<ListMyCompaniesResponses, ListMyCompaniesErrors, ThrowOnError> => (options?.client ?? client).get<ListMyCompaniesResponses, ListMyCompaniesErrors, ThrowOnError>({ url: '/api/v1/accounts/companies', ...options });

/**
 * Create a new company/agenda and make the caller its Owner/Admin
 * (UC-ACC-01.1/.2). Enforces the plan company-count limit (UC-ACC-01.8) before
 * creation, derives/validates the slug, then provisions org + owner membership
 * in one transaction and writes an audit entry. Returns the new company.
 */
export const createCompany = <ThrowOnError extends boolean = false>(options: Options<CreateCompanyData, ThrowOnError>): RequestResult<CreateCompanyResponses, CreateCompanyErrors, ThrowOnError> => (options.client ?? client).post<CreateCompanyResponses, CreateCompanyErrors, ThrowOnError>({
    url: '/api/v1/accounts/companies',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the profile of a company by id within the principal's scope
 * (UC-ACC-01.3). RLS-scoped: the connection is bound to the active company, so
 * a cross-company id returns 404 (the row is invisible) rather than leaking its
 * existence (UC-ACC-01.2 isolation).
 */
export const getCompany = <ThrowOnError extends boolean = false>(options: Options<GetCompanyData, ThrowOnError>): RequestResult<GetCompanyResponses, GetCompanyErrors, ThrowOnError> => (options.client ?? client).get<GetCompanyResponses, GetCompanyErrors, ThrowOnError>({ url: '/api/v1/accounts/companies/{id}', ...options });

/**
 * Invite a user (by email) into the ACTIVE company with a role
 * (UC-ACC-01.4/.5). Admin-only (manage members). Enforces the per-company seat
 * limit (UC-ACC-01.8), resolves the email to an account, upserts a pending
 * membership, and audits. Re-inviting a previously deactivated user
 * re-activates the invite while preserving their authored documents
 * (UC-ACC-01.6).
 */
export const inviteUser = <ThrowOnError extends boolean = false>(options: Options<InviteUserData, ThrowOnError>): RequestResult<InviteUserResponses, InviteUserErrors, ThrowOnError> => (options.client ?? client).post<InviteUserResponses, InviteUserErrors, ThrowOnError>({
    url: '/api/v1/accounts/members/invite',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Change a member's role within the active company (UC-ACC-01.5). Admin-only.
 */
export const setMemberRole = <ThrowOnError extends boolean = false>(options: Options<SetMemberRoleData, ThrowOnError>): RequestResult<SetMemberRoleResponses, SetMemberRoleErrors, ThrowOnError> => (options.client ?? client).post<SetMemberRoleResponses, SetMemberRoleErrors, ThrowOnError>({
    url: '/api/v1/accounts/members/role',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Deactivate a member of the active company (UC-ACC-01.6). Admin-only. Revokes
 * access immediately (membership flips to 'removed', failing every subsequent
 * request's membership check) while their authored documents remain intact
 * (those reference the user id, not the membership row).
 */
export const deactivateMember = <ThrowOnError extends boolean = false>(options: Options<DeactivateMemberData, ThrowOnError>): RequestResult<DeactivateMemberResponses, DeactivateMemberErrors, ThrowOnError> => (options.client ?? client).delete<DeactivateMemberResponses, DeactivateMemberErrors, ThrowOnError>({ url: '/api/v1/accounts/members/{user_id}', ...options });

/**
 * Confirm the principal may switch to a company (UC-ACC-01.3). The actual
 * active-company selection is reflected in the next JWT/session issued by
 * api-server; this validates access and returns 204 on success, 403 otherwise.
 * This is the cross-company isolation gate (NFR-ACC-01): a switch to a company
 * the principal does not actively belong to is denied.
 */
export const switchCompany = <ThrowOnError extends boolean = false>(options: Options<SwitchCompanyData, ThrowOnError>): RequestResult<SwitchCompanyResponses, SwitchCompanyErrors, ThrowOnError> => (options.client ?? client).post<SwitchCompanyResponses, SwitchCompanyErrors, ThrowOnError>({
    url: '/api/v1/accounts/switch-company',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List categories (UC-ACC-04.4).
 */
export const listCategories = <ThrowOnError extends boolean = false>(options?: Options<ListCategoriesData, ThrowOnError>): RequestResult<ListCategoriesResponses, ListCategoriesErrors, ThrowOnError> => (options?.client ?? client).get<ListCategoriesResponses, ListCategoriesErrors, ThrowOnError>({ url: '/api/v1/catalog/categories', ...options });

/**
 * List catalog items (UC-ACC-04.1). Hides deactivated items from the picker.
 */
export const listItems = <ThrowOnError extends boolean = false>(options?: Options<ListItemsData, ThrowOnError>): RequestResult<ListItemsResponses, ListItemsErrors, ThrowOnError> => (options?.client ?? client).get<ListItemsResponses, ListItemsErrors, ThrowOnError>({ url: '/api/v1/catalog/items', ...options });

/**
 * Create a catalog item (UC-ACC-04.1/.8 inline quick-add). Persisting from a
 * document line creates a FULL, reusable catalog item — the same path as an
 * explicit create.
 */
export const createItem = <ThrowOnError extends boolean = false>(options: Options<CreateItemData, ThrowOnError>): RequestResult<CreateItemResponses, CreateItemErrors, ThrowOnError> => (options.client ?? client).post<CreateItemResponses, CreateItemErrors, ThrowOnError>({
    url: '/api/v1/catalog/items',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get a catalog item (UC-ACC-04.1).
 */
export const getItem = <ThrowOnError extends boolean = false>(options: Options<GetItemData, ThrowOnError>): RequestResult<GetItemResponses, GetItemErrors, ThrowOnError> => (options.client ?? client).get<GetItemResponses, GetItemErrors, ThrowOnError>({ url: '/api/v1/catalog/items/{id}', ...options });

/**
 * Update a catalog item (UC-ACC-04.1). Editing does NOT mutate past documents
 * — those carry their own snapshotted line values.
 */
export const updateItem = <ThrowOnError extends boolean = false>(options: Options<UpdateItemData, ThrowOnError>): RequestResult<UpdateItemResponses, UpdateItemErrors, ThrowOnError> => (options.client ?? client).patch<UpdateItemResponses, UpdateItemErrors, ThrowOnError>({
    url: '/api/v1/catalog/items/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List price levels for an item (UC-ACC-04.2).
 */
export const listPriceLevels = <ThrowOnError extends boolean = false>(options: Options<ListPriceLevelsData, ThrowOnError>): RequestResult<ListPriceLevelsResponses, ListPriceLevelsErrors, ThrowOnError> => (options.client ?? client).get<ListPriceLevelsResponses, ListPriceLevelsErrors, ThrowOnError>({ url: '/api/v1/catalog/items/{id}/price-levels', ...options });

/**
 * List bank accounts (UC-ACC-02.10).
 */
export const listBankAccounts = <ThrowOnError extends boolean = false>(options?: Options<ListBankAccountsData, ThrowOnError>): RequestResult<ListBankAccountsResponses, ListBankAccountsErrors, ThrowOnError> => (options?.client ?? client).get<ListBankAccountsResponses, ListBankAccountsErrors, ThrowOnError>({ url: '/api/v1/config/bank-accounts', ...options });

/**
 * Get the company settings for the active company (UC-ACC-02.1).
 */
export const getCompanySettings = <ThrowOnError extends boolean = false>(options?: Options<GetCompanySettingsData, ThrowOnError>): RequestResult<GetCompanySettingsResponses, GetCompanySettingsErrors, ThrowOnError> => (options?.client ?? client).get<GetCompanySettingsResponses, GetCompanySettingsErrors, ThrowOnError>({ url: '/api/v1/config/company', ...options });

/**
 * Upsert the company settings (UC-ACC-02.1/.2/.11).
 */
export const upsertCompanySettings = <ThrowOnError extends boolean = false>(options: Options<UpsertCompanySettingsData, ThrowOnError>): RequestResult<UpsertCompanySettingsResponses, UpsertCompanySettingsErrors, ThrowOnError> => (options.client ?? client).put<UpsertCompanySettingsResponses, UpsertCompanySettingsErrors, ThrowOnError>({
    url: '/api/v1/config/company',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List numbering series (UC-ACC-02.3).
 */
export const listNumberingSeries = <ThrowOnError extends boolean = false>(options?: Options<ListNumberingSeriesData, ThrowOnError>): RequestResult<ListNumberingSeriesResponses, ListNumberingSeriesErrors, ThrowOnError> => (options?.client ?? client).get<ListNumberingSeriesResponses, ListNumberingSeriesErrors, ThrowOnError>({ url: '/api/v1/config/numbering', ...options });

/**
 * Create a numbering series (UC-ACC-02.3).
 */
export const createNumberingSeries = <ThrowOnError extends boolean = false>(options: Options<CreateNumberingSeriesData, ThrowOnError>): RequestResult<CreateNumberingSeriesResponses, CreateNumberingSeriesErrors, ThrowOnError> => (options.client ?? client).post<CreateNumberingSeriesResponses, CreateNumberingSeriesErrors, ThrowOnError>({
    url: '/api/v1/config/numbering',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List units (UC-ACC-02.8).
 */
export const listUnits = <ThrowOnError extends boolean = false>(options?: Options<ListUnitsData, ThrowOnError>): RequestResult<ListUnitsResponses, ListUnitsErrors, ThrowOnError> => (options?.client ?? client).get<ListUnitsResponses, ListUnitsErrors, ThrowOnError>({ url: '/api/v1/config/units', ...options });

/**
 * List VAT rates (UC-ACC-02.9).
 */
export const listVatRates = <ThrowOnError extends boolean = false>(options?: Options<ListVatRatesData, ThrowOnError>): RequestResult<ListVatRatesResponses, ListVatRatesErrors, ThrowOnError> => (options?.client ?? client).get<ListVatRatesResponses, ListVatRatesErrors, ThrowOnError>({ url: '/api/v1/config/vat-rates', ...options });

/**
 * List contacts for the active company (UC-ACC-03.1). Hides deactivated/merged
 * contacts so pickers only show live parties.
 */
export const listContacts = <ThrowOnError extends boolean = false>(options?: Options<ListContactsData, ThrowOnError>): RequestResult<ListContactsResponses, ListContactsErrors, ThrowOnError> => (options?.client ?? client).get<ListContactsResponses, ListContactsErrors, ThrowOnError>({ url: '/api/v1/contacts', ...options });

/**
 * Create a contact (UC-ACC-03.1). Persists + audits in one transaction.
 */
export const createContact = <ThrowOnError extends boolean = false>(options: Options<CreateContactData, ThrowOnError>): RequestResult<CreateContactResponses, CreateContactErrors, ThrowOnError> => (options.client ?? client).post<CreateContactResponses, CreateContactErrors, ThrowOnError>({
    url: '/api/v1/contacts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Deactivate (soft-delete) a contact (UC-ACC-03.1). Never hard-deletes: the
 * contact is hidden from pickers but its historical documents stay intact.
 */
export const deactivateContact = <ThrowOnError extends boolean = false>(options: Options<DeactivateContactData, ThrowOnError>): RequestResult<DeactivateContactResponses, DeactivateContactErrors, ThrowOnError> => (options.client ?? client).delete<DeactivateContactResponses, DeactivateContactErrors, ThrowOnError>({ url: '/api/v1/contacts/{id}', ...options });

/**
 * Get a single contact (UC-ACC-03.1).
 */
export const getContact = <ThrowOnError extends boolean = false>(options: Options<GetContactData, ThrowOnError>): RequestResult<GetContactResponses, GetContactErrors, ThrowOnError> => (options.client ?? client).get<GetContactResponses, GetContactErrors, ThrowOnError>({ url: '/api/v1/contacts/{id}', ...options });

/**
 * Update a contact (UC-ACC-03.1/.9). Loads the current row, overlays the
 * request fields, persists, and audits — all in one transaction.
 */
export const updateContact = <ThrowOnError extends boolean = false>(options: Options<UpdateContactData, ThrowOnError>): RequestResult<UpdateContactResponses, UpdateContactErrors, ThrowOnError> => (options.client ?? client).patch<UpdateContactResponses, UpdateContactErrors, ThrowOnError>({
    url: '/api/v1/contacts/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List a contact's addresses (UC-ACC-03.4).
 */
export const listAddresses = <ThrowOnError extends boolean = false>(options: Options<ListAddressesData, ThrowOnError>): RequestResult<ListAddressesResponses, ListAddressesErrors, ThrowOnError> => (options.client ?? client).get<ListAddressesResponses, ListAddressesErrors, ThrowOnError>({ url: '/api/v1/contacts/{id}/addresses', ...options });

/**
 * Merge a contact into another (UC-ACC-03.5). Re-points documents/history to
 * the surviving contact transactionally with no data loss, then marks the
 * source merged + inactive. The re-key, the flag, and the audit entry all live
 * in one transaction.
 */
export const mergeContact = <ThrowOnError extends boolean = false>(options: Options<MergeContactData, ThrowOnError>): RequestResult<MergeContactResponses, MergeContactErrors, ThrowOnError> => (options.client ?? client).post<MergeContactResponses, MergeContactErrors, ThrowOnError>({
    url: '/api/v1/contacts/{id}/merge',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List invoices for the active company (UC-ACC-05.1).
 */
export const listInvoices = <ThrowOnError extends boolean = false>(options?: Options<ListInvoicesData, ThrowOnError>): RequestResult<ListInvoicesResponses, ListInvoicesErrors, ThrowOnError> => (options?.client ?? client).get<ListInvoicesResponses, ListInvoicesErrors, ThrowOnError>({ url: '/api/v1/invoices', ...options });

/**
 * Create a draft invoice with line items (UC-ACC-05.1/.2). Money/VAT computed +
 * rounded by `AccountingRepository::create_invoice_rls` (the #1522 invariant:
 * header == Σ rounded line rows), which `accounting_core::money` mirrors.
 */
export const createInvoice = <ThrowOnError extends boolean = false>(options: Options<CreateInvoiceData, ThrowOnError>): RequestResult<CreateInvoiceResponses, CreateInvoiceErrors, ThrowOnError> => (options.client ?? client).post<CreateInvoiceResponses, CreateInvoiceErrors, ThrowOnError>({
    url: '/api/v1/invoices',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a draft invoice (UC-ACC-05.1). Issued invoices cannot be deleted.
 */
export const deleteInvoice = <ThrowOnError extends boolean = false>(options: Options<DeleteInvoiceData, ThrowOnError>): RequestResult<DeleteInvoiceResponses, DeleteInvoiceErrors, ThrowOnError> => (options.client ?? client).delete<DeleteInvoiceResponses, DeleteInvoiceErrors, ThrowOnError>({ url: '/api/v1/invoices/{id}', ...options });

/**
 * Get a single invoice (UC-ACC-05.1).
 */
export const getInvoice = <ThrowOnError extends boolean = false>(options: Options<GetInvoiceData, ThrowOnError>): RequestResult<GetInvoiceResponses, GetInvoiceErrors, ThrowOnError> => (options.client ?? client).get<GetInvoiceResponses, GetInvoiceErrors, ThrowOnError>({ url: '/api/v1/invoices/{id}', ...options });

/**
 * Update a draft invoice (UC-ACC-05.1). Issued invoices are immutable except via
 * a corrective document, so the update is rejected unless status is `draft`.
 */
export const updateInvoice = <ThrowOnError extends boolean = false>(options: Options<UpdateInvoiceData, ThrowOnError>): RequestResult<UpdateInvoiceResponses, UpdateInvoiceErrors, ThrowOnError> => (options.client ?? client).patch<UpdateInvoiceResponses, UpdateInvoiceErrors, ThrowOnError>({
    url: '/api/v1/invoices/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a credit note against an issued invoice (UC-ACC-05.7).
 *
 * The original stays immutable. A new `doc_type='credit_note'` document is
 * inserted with `corrected_invoice_id` set and SIGNED (negative) amounts: full
 * correction (empty `items`) reverses the whole original; partial correction
 * reverses only the supplied lines. VAT is reversed via the negated totals
 * (`vat::reverse_groups` semantics) so the correction lands in the same VAT
 * buckets. A `corrects` document link is recorded (UC-ACC-05.15).
 */
export const createCreditNote = <ThrowOnError extends boolean = false>(options: Options<CreateCreditNoteData, ThrowOnError>): RequestResult<CreateCreditNoteResponses, CreateCreditNoteErrors, ThrowOnError> => (options.client ?? client).post<CreateCreditNoteResponses, CreateCreditNoteErrors, ThrowOnError>({
    url: '/api/v1/invoices/{id}/credit-note',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Duplicate an invoice into a fresh draft (UC-ACC-05.16).
 *
 * Deep-copies the line items; resets dates to today, status to `draft`; carries
 * over NO number, NO payments, NO links and NO doc-type extensions — exactly a
 * fresh draft the user can edit and issue independently.
 */
export const duplicateInvoice = <ThrowOnError extends boolean = false>(options: Options<DuplicateInvoiceData, ThrowOnError>): RequestResult<DuplicateInvoiceResponses, DuplicateInvoiceErrors, ThrowOnError> => (options.client ?? client).post<DuplicateInvoiceResponses, DuplicateInvoiceErrors, ThrowOnError>({ url: '/api/v1/invoices/{id}/duplicate', ...options });

/**
 * Set/override the exchange rate on a foreign-currency invoice (UC-ACC-05.4).
 * Persists the rate AND the base-currency equivalent (`base_currency_amount`)
 * computed from the document total via `money::to_base_currency`, which drives
 * VAT/reporting.
 */
export const setExchangeRate = <ThrowOnError extends boolean = false>(options: Options<SetExchangeRateData, ThrowOnError>): RequestResult<SetExchangeRateResponses, SetExchangeRateErrors, ThrowOnError> => (options.client ?? client).post<SetExchangeRateResponses, SetExchangeRateErrors, ThrowOnError>({
    url: '/api/v1/invoices/{id}/exchange-rate',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Issue a draft: allocate the next gapless number and lock it (UC-ACC-05.1).
 *
 * Issuance preconditions (blocked with 400 otherwise): the draft has a contact
 * and at least one line. Number allocation is atomic + gapless: inside ONE
 * transaction we (1) resolve the doc_type's numbering series for the issue
 * year, (2) `allocate_next_number_rls` (single `UPDATE ... RETURNING`), (3)
 * `numbering::format_number`, (4) `issue_invoice_rls` (status draft→issued),
 * (5) audit. Concurrent issuance therefore can never skip or duplicate a
 * number (legal requirement).
 */
export const issueInvoice = <ThrowOnError extends boolean = false>(options: Options<IssueInvoiceData, ThrowOnError>): RequestResult<IssueInvoiceResponses, IssueInvoiceErrors, ThrowOnError> => (options.client ?? client).post<IssueInvoiceResponses, IssueInvoiceErrors, ThrowOnError>({
    url: '/api/v1/invoices/{id}/issue',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List items for an invoice (UC-ACC-05.2).
 */
export const listInvoiceItems = <ThrowOnError extends boolean = false>(options: Options<ListInvoiceItemsData, ThrowOnError>): RequestResult<ListInvoiceItemsResponses, ListInvoiceItemsErrors, ThrowOnError> => (options.client ?? client).get<ListInvoiceItemsResponses, ListInvoiceItemsErrors, ThrowOnError>({ url: '/api/v1/invoices/{id}/items', ...options });

/**
 * List typed document links from/to an invoice (UC-ACC-05.15).
 */
export const listLinks = <ThrowOnError extends boolean = false>(options: Options<ListLinksData, ThrowOnError>): RequestResult<ListLinksResponses, ListLinksErrors, ThrowOnError> => (options.client ?? client).get<ListLinksResponses, ListLinksErrors, ThrowOnError>({ url: '/api/v1/invoices/{id}/links', ...options });

/**
 * Confirm 2FA with a TOTP code, enabling it (UC-ACC-16.1).
 */
export const confirmTwoFactor = <ThrowOnError extends boolean = false>(options: Options<ConfirmTwoFactorData, ThrowOnError>): RequestResult<ConfirmTwoFactorResponses, ConfirmTwoFactorErrors, ThrowOnError> => (options.client ?? client).post<ConfirmTwoFactorResponses, ConfirmTwoFactorErrors, ThrowOnError>({
    url: '/api/v1/platform/2fa/confirm',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Begin 2FA enrollment: generate a secret + recovery codes (UC-ACC-16.1).
 * Stores the enrollment disabled (`enabled=false`) until confirmed.
 */
export const enrollTwoFactor = <ThrowOnError extends boolean = false>(options?: Options<EnrollTwoFactorData, ThrowOnError>): RequestResult<EnrollTwoFactorResponses, EnrollTwoFactorErrors, ThrowOnError> => (options?.client ?? client).post<EnrollTwoFactorResponses, EnrollTwoFactorErrors, ThrowOnError>({ url: '/api/v1/platform/2fa/enroll', ...options });

/**
 * List the audit log for the active company (UC-ACC-16.7).
 */
export const listAuditLog = <ThrowOnError extends boolean = false>(options?: Options<ListAuditLogData, ThrowOnError>): RequestResult<ListAuditLogResponses, ListAuditLogErrors, ThrowOnError> => (options?.client ?? client).get<ListAuditLogResponses, ListAuditLogErrors, ThrowOnError>({ url: '/api/v1/platform/audit-log', ...options });

/**
 * Create a capability-token share link for an invoice (UC-ACC-05.11).
 */
export const createShareLink = <ThrowOnError extends boolean = false>(options: Options<CreateShareLinkData, ThrowOnError>): RequestResult<CreateShareLinkResponses, CreateShareLinkErrors, ThrowOnError> => (options.client ?? client).post<CreateShareLinkResponses, CreateShareLinkErrors, ThrowOnError>({
    url: '/api/v1/platform/share-links',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke a share link (UC-ACC-05.11).
 */
export const revokeShareLink = <ThrowOnError extends boolean = false>(options: Options<RevokeShareLinkData, ThrowOnError>): RequestResult<RevokeShareLinkResponses, RevokeShareLinkErrors, ThrowOnError> => (options.client ?? client).delete<RevokeShareLinkResponses, RevokeShareLinkErrors, ThrowOnError>({ url: '/api/v1/platform/share-links/{id}', ...options });

/**
 * Add a tag to an entity (UC-ACC-05.14).
 */
export const addTag = <ThrowOnError extends boolean = false>(options: Options<AddTagData, ThrowOnError>): RequestResult<AddTagResponses, AddTagErrors, ThrowOnError> => (options.client ?? client).post<AddTagResponses, AddTagErrors, ThrowOnError>({
    url: '/api/v1/platform/tags',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * PUBLIC read-only view of a shared invoice by token (UC-ACC-05.11). No auth.
 * Returns 404 for a missing/revoked/expired token (non-enumerating).
 */
export const viewSharedInvoice = <ThrowOnError extends boolean = false>(options: Options<ViewSharedInvoiceData, ThrowOnError>): RequestResult<ViewSharedInvoiceResponses, ViewSharedInvoiceErrors, ThrowOnError> => (options.client ?? client).get<ViewSharedInvoiceResponses, ViewSharedInvoiceErrors, ThrowOnError>({ url: '/api/v1/share/{token}', ...options });

/**
 * Liveness probe. No I/O — process-alive only. Docker HEALTHCHECK target.
 */
export const liveness = <ThrowOnError extends boolean = false>(options?: Options<LivenessData, ThrowOnError>): RequestResult<LivenessResponses, unknown, ThrowOnError> => (options?.client ?? client).get<LivenessResponses, unknown, ThrowOnError>({ url: '/health', ...options });

/**
 * Readiness probe (deep check). DB connectivity only; used by operator
 * dashboards, NOT wired to Docker HEALTHCHECK.
 */
export const readiness = <ThrowOnError extends boolean = false>(options?: Options<ReadinessData, ThrowOnError>): RequestResult<ReadinessResponses, ReadinessErrors, ThrowOnError> => (options?.client ?? client).get<ReadinessResponses, ReadinessErrors, ThrowOnError>({ url: '/readiness', ...options });
