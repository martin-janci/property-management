# 2026-06-27

## Fire payload received (mid-run, ~06:35 UTC)
- `buffer-low: claimable=0/72 — coverage.json stale (2026-06-23, all 12 partials resolved/in-flight); request fresh coverage rescan + refill`
- **Routine action:** logged as signal `dispatcher-buffer-low-2026-06-27` (score 0, log-only, candidate vector `dx`).
- **What the routine can NOT do:** trigger a coverage rescan. Per the `ppt-project-management` skill ("Do NOT run the skill's `scan` mode here — the authoritative full rebuild is the on-demand local `/ppt-project-management scan`"), a coverage rescan is local-only. The routine's Phase 1.6 only does upkeep against an existing `coverage.json`; it does not author one.
- **What needs to happen** (out-of-band, by an operator with the local skill on hand):
  1. Run `/ppt-project-management scan` locally to regenerate `.research/management/coverage.json` against current `sprint-status` + merged PRs.
  2. Land the resulting commit on `dev` so the dispatcher can refill its claimable buffer with the new partials.
- **Underlying class** — already a known self-improvement finding: `phase-2-6-buffer-claimable-overstates` (open=8, high=2). The dispatcher's claimable formula treats archive-terminal task_ids as still-claimable; until `gc1-reconcile.sh` / action-list-reconcile auto-close them (incl. `status=failed`), every coverage refresh is masked by the same arithmetic. Fix is two-part and unlanded (see brief's "Self-review findings").

## Since last run
- Merged PRs: 96 (range #1567–#1853)
- Open PRs touched: 31
- New / updated issues: 37 (all `follow-up` / `from-merged-review` label set — none untriaged)
- Commits: 100+ on `dev` (window truncated at 100 entries; deeper history retained)
- Routine lag: 11d 3h since last run (stale_routine_alert) — last_run_iso was 2026-06-16T03:25Z
- Phases that failed: none (Phase 1.5 expert-review + Phase 1.6 PM rotation skipped — see notes below)

## Shipped
- 96 merged PRs span: ~30 feat (Epic 2B notifications, Epic 3 buildings/units, Epic 6 announcements, Epic 11/11.x financial+payments+stripe, Epic 12 meter+OCR, Epic 16 saved-search alerts, ACC accounting-server, UC-05.x messaging N-party, BIT-176/181/182/183/184/185/186/189/191/201/202/208/214/239/244/183), 22 fix (incl. financial rustfmt, RLS baseline + service-role allowlist, dev test regressions, iot dup-channel, mobile typecheck, db dup-migration 00187 + 00192, frontend i18n, manager-role gate for booking conflicts, Stripe currency validation, Booking.com listing-push currency), 1 revert (PR #1713 — see Watch), 12 churn-resolution PRs split fat repository/route files, 7 chore + research-dispatcher housekeeping commits, 9 dependabot.

## Watch
- Reverted: #1713 reverts #1690 (delegations frontend) — intentional retirement per BIT-213, not a bug-revert. Captured as `bug-revert-pr-1713-delegations` (score 3, status=dropped, evidence cites retirement ticket; no investigation owed).
- Stalled review: none — every open PR (31) was touched within the last 4 days; the 7d-idle threshold is not crossed.
- Churn hotspots: skipped — per-commit `numstat` not available in cloud run (no local git checkout-of-dev). Dispatcher already merged 12 explicit `churn-hotspot-*` / `repeated-churn-*` resolution PRs this window, resolving the corresponding open backlog items (see *Backlog deltas — Done*).

## PRs stuck in draft despite approval
- none (skipped — `gh pr list` requires gh CLI auth which is unavailable in this env; mcp__github__list_pull_requests output truncated `reviewDecision` to avoid 200KB payload bloat. Re-checking via the GitHub MCP search would require ~30 per-PR fetches and is deferred to the next non-catch-up run.)

## PRs with verdict=changes, no fix-round progress in 24h
- none (skipped — same reason; planning-branch `assignments.json` fetch via mcp__github__get_file_contents on `ref=planning` was deferred to keep this catch-up brief from spending its token budget on dispatcher coordination state.)

## Code review slice
- Segment reviewed: SKIPPED (catch-up run)
- Reason: 11-day routine lag — the dispatcher's per-PR reviewer ran on each of the 96 merged PRs as they landed; running an additional rotating expert pass on a snapshot of stale code adds little value above the merged review trail and 37 outstanding `from-merged-review` follow-ups already filed as labeled GitHub issues.
- Next segment (will run next routine): `ppt-web-core` (oldest unreviewed at 2026-06-05) — no change to `state.review_cursor` this run.
- Skipped: yes — catch-up run

## Screen-map status
- Total `docs/screens/` files: not recomputed this run (catch-up). Last reported: ppt/ + reality/ populated, mobile/ still empty.
- Drift signals this run: 0 emitted (per-PR file-list fetch over 96 merged PRs would dominate the token budget; deferred to next non-catch-up run). PR #1736 explicitly updated screen-map + sitemap alongside route changes, indicating the implementer-side discipline held.
- Orphan screens this run: 0 (same — heuristic deferred).
- Last `screen-map.yml` CI run on `main`: not queried this run (deferred).

## Backlog deltas
- **New:** [3] `bug-revert-pr-1713-delegations` Delegation frontend revert (PR #1713 reverted PR #1690) — verify intentional retirement (BIT-213). Marked **dropped** at creation: revert is planned retirement per BIT-213, not a bug-revert; no investigation owed.
- **Resolved (open → done)** — 12 churn / code-review items where the dispatcher merged a PR whose title encodes the exact signal id:
  - `churn-hotspot-backend-crates-db-src-repositories-document-rs` ← PR #1683
  - `churn-hotspot-backend-crates-db-src-repositories-sensor-rs` ← PR #1810
  - `churn-hotspot-backend-crates-db-src-repositories-subscription-rs` ← PR #1796
  - `churn-hotspot-backend-crates-db-tests-form-rls-repo-tests-rs` ← PR #1719
  - `churn-hotspot-backend-crates-integrations-src-booking-rs` ← PR #1693
  - `churn-hotspot-backend-servers-api-server-src-routes-aml-dsa-rs` ← PR #1708
  - `churn-hotspot-backend-servers-api-server-src-routes-emergency-rs` ← PR #1798
  - `churn-hotspot-backend-servers-api-server-src-routes-enhanced-tenant-screening-rs` ← PR #1800
  - `churn-hotspot-backend-servers-api-server-src-routes-reserve-funds-rs` ← PR #1816
  - `churn-hotspot-frontend-apps-mobile-app-config-icon-test-ts` ← PR #1718
  - `code-review-mobile-rn-screens-mock-data` ← PR #1681
  - `repeated-churn-backend-servers-api-server-src-routes-forms-rs` ← PR #1700
- **Decayed:** 11 open items (score −1, no new signals in 14 days) — `code-review-mobile-native-kmp-deeplink-token-not-url-decoded`, `code-review-mobile-native-kmp-search-stale-response-race`, `test-gap-screen-map-drift-pr-1085-reality`, `test-gap-screen-map-drift-pr-1100-ppt`, `bug-risky-churn-pr-963-api-main-rs`, `test-gap-hotfix-no-test-pr-959-reality-listings-pagination`, `bug-ios-deeplink-info-plist-missing`, `test-gap-hotfix-no-test-pr-1288-webhook-rls`, `test-gap-hotfix-no-test-pr-1287-rls-llm-sessions`, `test-gap-hotfix-no-test-pr-1289-api-ecosystem`, `test-gap-hotfix-no-test-pr-1292-mfa-rls`.
- **Dropped (decay to 0):** 17 open churn/code-review items at score 1 from 2026-06-06 → 06-10 dropped after 14 days without re-firing — see `backlog.json` evidence lines for `decayed: no new signals in 14 days` markers.

## Plans promoted
- none — catch-up run. The two highest-score open items (`code-review-mobile-native-kmp-*` at 1, decayed from 2) lost the score discipline. No item now sits at score ≥ 3 + open + un-planned. The six existing `ready` plans (mobile-rn report-fault fake submit, reality-web realtor mgmt untranslated, reality-web share-comparison 404, reality-web listing SSR crash, iOS SearchView uncompilable, security-llm-doc-idor) remain queued for the implementer.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist (`screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / `lockfile-drift` / `stalled-review`) this run.

## Self-review findings
- open=8 (high=2, medium=4, low=2); new this run=0; acknowledged=19
- `phase-6-archive-push-198kb-mcp-limit` [recurrence=0] — Phase 6 archive-move produced 198KB assignments-archive.json that `mcp__github__push_files` can't inline (64KB cap) and `git push` is proxy-403'd; archive record wasn't persisted, Merged-total understated by 1. Fix: push via NEW branch + open PR + `mcp__github__merge_pull_request` squash — server-side merge carries any size, bypasses both blockers. Landed PR #1163 (sha 08ab915) this way; T19 now green.
- `phase-2-6-buffer-claimable-overstates` [recurrence=0] — Phase 2.6 buffer reported claimable=54/open=54 but real claimable=0; all 54 already terminal in assignments-archive. Fix (two-part, unlanded): (1) gc1-reconcile.sh / action-list-reconcile must auto-close action-list items whose exact task_id is terminal in active+archive, **including status=failed** (today only merged/done close); (2) Phase 2.6 buffer-guard open_count AND goal-gate GC3 claimable formula must subtract archive task_ids (currently only subtract active ids); self-test T15 encodes the same blind formula — guard needs assertion.

## Open questions
- Should Phase 1.5 / 1.6 be best-effort-skipped on catch-up runs (>3-day lag) by design, or should the routine drop into a sampling mode (1 expert + 1 PM role) instead of skipping entirely? Today skipped — next routine run will pick up the normal rotation.
- The 31 open PRs include 7 dependabot grouped-update PRs (#1836–#1842). No `lockfile-drift` shape detected, but the policy on auto-merging grouped Dependabot PRs after CI green deserves a written-down convention — currently the dispatcher does not pick them up by default.

## Quiet day?
- No — 96 merged PRs, 12 backlog resolutions, 11 decay-bumps + 17 decay-drops, 1 new vector (dropped immediately as intentional). State advanced on all four cursors (PR, issue, commit, seen_signals).
