# 2026-08-25

## Refire — buffer-low (08:50 UTC)

Dispatcher fired the routine again mid-day with payload `buffer-low: claimable=11/72 (only ~5 non-gated; 6 mobile-native parked) — refill planner`. This section captures the incremental delta on top of the 03:20 UTC run above.

- **Refill outcome:** none — 0 new plans promoted. No open backlog item meets the Phase-3 readiness gate (`score >= 3`, or security-fast-track: `vector=security ∧ confidence=high ∧ score >= 2`). Best available candidates: `code-review-mobile-native-kmp-httpclient-no-timeout` (score 2, high, bug — one short of gate) and `code-review-ppt-web-ui-content-mod-overdue-noop` (score 2, high, bug — one short of gate). Neither promotes; they need one more signal (or a manual score bump) before the next run picks them up.
- **Why the buffer is thin:** 6 of the 11 claimable rows are the parked `mobile-native-kmp` cluster (upstream mobile-native gate, per prior brief). The remaining ~5 non-gated rows are what the dispatcher can burn through. No fresh scoreable churn today — the merged PRs since 03:20 were mostly planner/QA-produced follow-ups on already-resolved work, not new-file signals.
- **New activity since 03:20 (window #2839–#2847, 9 PRs):** all merged already; nothing stalled/reverted new. #2847 resolved `code-review-ppt-web-ui-dsa-reports-no-i18n` (marked done); the rest are planner/QA follow-ups (screen-map link update, mobile-rn lint adopt, 3 regression tests, 1 refactor plan). #2846 sits open as a draft (`pm-qa-csv-export-crlf-sanitizer-fuzz`).
- **State advanced:** `last_pr_seen` 2838 → 2847, `last_issue_seen` 2832 → 2847, `last_commit_sha` → 142ae17, `last_run_iso` → 08:50 UTC. `stats.runs` 70 → 71.

Everything below is from the 03:20 run and remains authoritative for the day's shipped/watch/backlog picture; only the deltas above are new.

## Since last run
- Merged PRs: 13 (range #2821–#2838)
- Open PRs touched: 0 (nothing updated since 2026-08-22T03:03:20Z beyond the 13 merged; 13 stalled candidates still open — 10 dependabot / 3 human `feat(acc)` already in `needs-human-judgement`)
- New / updated issues: 6 (all closed follow-ups: #2822/#2823/#2824/#2831/#2832 from post-merge review; #2743 dispatcher-recurred-defects touched by activity, still closed)
- Commits: 86 on `dev` (13 real merge commits + 73 dispatcher chore commits touching only `.research/management/{assignments,action-list}.json`)
- Routine lag: 3d 0h since last run — **lag_warning fired** (>36h threshold); **stale_routine_alert fired** (≥3d — cloud routine may be paused; the dispatcher on the planning branch is still ticking, both continued to produce chore commits, so this appears to be the daily routine specifically missing beats)
- Phases that failed: none — Phase 1 fully ran (PR/issue/commit fetches + churn analysis); Phase 1.5 ran (`ppt-web-ui` segment, 3 findings); Phase 1.6 ran (upkeep mode, `pm-qa` role, coverage epic idx 5); Phase 3 ran (1 new plan promoted, 1 existing plan status corrected); Phase 5 skipped (no allowlisted signal type met the certainty bar this run)

## Shipped
- #2821 `code-review-api-handlers-booking-connect-non-manager-hijack` — added `verify_manager_role_in_org` on `connect_booking`/`disconnect_booking`/`disconnect_airbnb` (install.rs) and `airbnb_oauth_callback` (oauth.rs); includes new IDOR suite `integrations_cross_org_idor_tests.rs` locking the 403 gate — resolves the security fast-track item this brief promoted 2026-08-22.
- #2825 `gh-issue-2824` — i18n VerificationBadge expiry copy + de-duplicated expiry logic; 9 files (6 locale JSON, badge component + i18n test + expiry test).
- #2826 `gh-issue-2823` — per-channel bookkeeping + bounded retry for held-notification drain; adds migration 00234, touches quiet_hours_drain.rs / notification_pipeline.rs (later reworked by #2834).
- #2827 `gh-issue-2822` — neutralize CR/LF in CSV export sanitizer (Closes #2822); helpers.rs + audit.rs.
- #2828 `code-review-ppt-web-ui-facilities-booking-silent-errors` — surface booking fetch/approve/reject/cancel errors in ppt-web (MyBookingsPage.tsx + PendingBookingsPage + test).
- #2829 `code-review-ppt-web-ui-aml-dashboard-prompt-alert-flow` — replaced AML dashboard `window.prompt`/`alert` decision flow with typed in-app dialogs + i18n across 6 locales.
- #2830 `code-review-ppt-web-ui-facilities-booking-hardcoded-i18n` — i18n facilities booking UI strings (BookingList / BookFacility / MyBookings / PendingBookings + 6 locales).
- #2833 `gh-issue-2832` — reset AML EDD/Review dialog state per assessment via per-id `key=` remount (regression-guarded in AmlDashboardPage.test.tsx). Titled `fix(UC-90)` on the merge commit — a follow-up to #2829 that touches the same 2 files → both files now in this run's top-3 churn.
- #2834 `gh-issue-2831` — atomic claim so quiet-hours drain delivers held notifications at-most-once; migration 00235 + dedicated replica-claim test suite. Follow-up to #2826 → `quiet_hours_drain.rs` also in top-3 churn.
- #2835 `code-review-mobile-rn-votedetail-conditional-hooks` — VoteDetailScreen hooks run before returns (Rules-of-Hooks fix).
- #2836 `code-review-mobile-rn-thread-detail-hardcoded-en` — localize ThreadDetailScreen UI (6 locales + 2 tests).
- #2837 `code-review-mobile-rn-voice-cmd-hardcoded-en` — localize voice-assistant confirmations (6 locales + i18n test).
- #2838 `churn-hotspot-voice-webhooks-rs-retry1` — centralize voice OAuth token encryption in `voice_webhooks.rs`; single-file surgical (68+/-27 lines).

## Watch
- Stalled review: none NEW this run — 3 human `feat(acc)` PRs (#2555 UC-ACC-05.17 / #2558 UC-ACC-05.9 / #2559 UC-ACC-05.8) remain in `needs-human-judgement` (all 30d idle now, all already scored). 10 dependabot PRs also stalled (11d – 25d) but excluded from stalled-review noise per convention — they belong to `dep-update-noise` (delta 0).
- Reverted: none.
- Churn hotspots (top-3 this run):
  - `backend/servers/api-server/src/services/quiet_hours_drain.rs` — 242 lines (PRs #2826, #2834 — the same file was fix-then-refixed within the window; two of the merged PRs are follow-ups to each other)
  - `frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.tsx` — 199 lines (PRs #2829, #2833 — same fix-then-fix-again pattern on the AML dashboard)
  - `frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.test.tsx` — 175 lines (PRs #2829, #2833)
  All three are **new** hotspots (`runs_seen=1`). No `repeated-churn` this run.

## PRs stuck in draft despite approval
- none — 0 open PRs match `isDraft==true AND reviewDecision==APPROVED` in this window (the only recent draft-approved PR was #2821 in the 2026-08-22 window, which merged 08:23Z that day).

## PRs with verdict=changes, no fix-round progress in 24h
- none in current window — no active `.research/management/assignments.json` rows sit in `verdict=changes`; the dispatcher chore-commit log shows only `approve` verdicts across the merged batch.

## Code review slice
- Segment reviewed: `ppt-web-ui` (reason: churn-aligned — 2 of the 3 top-churn files are under this segment; also tied oldest-unreviewed at 2026-08-16)
- Experts: frontend + completeness
- Findings: 3 (all `code-review-finding` signals — see *Backlog deltas*)
- Next segment: `mobile-native-kmp` (oldest unreviewed after this run: 2026-08-16 — the only tie left; alphabetical breaks to `mobile-native-kmp` since `ppt-web-ui` advanced today)
- Skipped: no

## Screen-map status
- Total `docs/screens/` files: 153 (ppt=106 + reality=47 + mobile=0 — mobile still not seeded, drift detection intentionally excludes it)
- Drift signals this run: 0 — none of the 13 merged PRs touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`
- Orphan screens this run: 0 — no route basenames changed under the trigger set, so no orphan spot-check work to do
- Last `screen-map.yml` CI run on `main`: success @ 2026-06-04T22:19Z (workflow only fires on relevant docs/frontend paths; the long gap is expected)

## Backlog deltas
- **New:** [3] `code-review-ppt-web-ui-content-mod-unsafe-cast` — ContentModerationPage moderation-action + appeal-decision paths cast `window.prompt` free text through `as '<union>'` straight to the API (same anti-pattern PR #2829 removed from AML)
- **New:** [2] `code-review-ppt-web-ui-content-mod-overdue-noop` — `handleShowOverdue` only resets statusFilter to 'pending' with a self-admitting TODO; "Show Overdue" button is a silent no-op
- **New:** [1] `code-review-ppt-web-ui-dsa-reports-no-i18n` — `DsaReportsPage.tsx` has no useTranslation; every visible string is hardcoded English while sibling compliance pages localize
- **New:** [1] churn-hotspot `backend/servers/api-server/src/services/quiet_hours_drain.rs`
- **New:** [1] churn-hotspot `frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.tsx`
- **New:** [1] churn-hotspot `frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.test.tsx`
- **Bumped:** none this run (all new signals landed as new items — no matching existing rows)
- **Decayed:** `code-review-mobile-native-kmp-cancellation-swallowed` — 2 → 1 (14d without new signal); `code-review-mobile-native-kmp-ssoservice-untested` — 1 → 0 → dropped (decayed to zero)
- **Dropped:** `code-review-mobile-native-kmp-ssoservice-untested` (decay to zero — 14d without a fresh signal on `mobile-native/.../auth/SsoService.kt`)
- **Housekeeping:** `code-review-mobile-native-kmp-inquiries-response-contract` — status open → ready (plan file has existed at `plans/…` since 2026-08-16; prior run wrote the plan but forgot to advance the status)
- **Housekeeping:** `code-review-api-handlers-booking-connect-non-manager-hijack` — plan moved `plans/` → `plans/_archive/` after PR #2821 merged; backlog row already at `status=done` from the prior dispatcher reconcile.

## Plans promoted
- `plans/code-review-ppt-web-ui-content-mod-unsafe-cast.md` — replace ContentModerationPage `window.prompt` + `as '<union>'` casts with typed `<TakeModerationActionDialog>` + `<DecideAppealDialog>` mirroring the AML fix in PR #2829 · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met the allowlist × confidence=high × score≥3 bar this run. Detail: the 3 human stalled-review PRs (#2555/#2558/#2559) are already in `state.seen_signals` (one-shot dedup); dependabot stalls are `dep-update-noise` (delta 0). No `screen-map-drift`, no `screen-map-orphan`, no `fixme-in-merged-code`, no lockfile-drift variant of `dep-update-noise` this window.

## Self-review findings
- none — no `.research/self-improvement/findings.json` file present in the repo

## Open questions
- Cloud routine has now missed **3 days** — this run is a manually- (or externally-) triggered catch-up. Worth checking why the cron isn't firing; the dispatcher on the planning branch continued to tick, so the outage is scoped to the daily routine specifically. Recommend inspecting the deployment-side scheduler; the routine itself has nothing to log beyond `stale_routine_alert`.
- The 3 stalled `feat(acc)` PRs (#2555 / #2558 / #2559) are now 30d idle. Same open question the prior brief carried — blocked on the accounting spec resolution.

## Quiet day?
- **No** — 3-day catch-up: 13 PRs shipped, 6 new backlog vectors created, 1 new plan promoted, 0 auto-fixes. `stats.runs` bumped 69 → 70; `stats.vectors_created` 318 → 324; `stats.plans_created` 27 → 28.
