# 2026-06-16

## Since last run
- Merged PRs: **26** (#1385–#1402, #1416, #1417, #1418, #1423, #1424, #1426, #1430, #1431, #1432, #1439) — span 1385–1439, 23 application-code + 2 dispatcher state + 1 backend feature that **broke dev compile** (#1426 → issue #1437).
- Open PRs touched: 12 newly active (incl. #1435 / #1436 / #1438 — emergency fixes for the #1426 dev-compile break), plus carry-over stale drafts (#1316 @ 3d, #1197 @ 7d, #988 @ 13d).
- New / updated issues: **13 new** (#1403–#1413, #1422, #1437) + 24 updated existing; the open critical-priority signal is **#1437** (dev backend does not compile — `integrations` crate + api-server tests broken by PR #1426, blocks ALL backend CI).
- Commits on `dev`: 55 since `67c24bde` → `4829015b` (25 real-code commits + 30 dispatcher/version-bump commits).
- Routine lag: ~23h since last run — under 36h; no `lag_warning`.
- Phases that failed: **none**.

## Shipped
- **#1397** `fix: harden forms submit/download org scope` — `forms.rs` org-scope hardening + uniform `rls.release().await` on all 12 intermediate-query early-return paths; `form_cross_org_idor_tests.rs` covers cross-org 403/200.
- **#1393 / #1394 / #1395** — pm-qa coverage drops: Booking.com OAuth handler + CSRF + secure-credential-replacement coverage (#1362, #1374); document download/preview presigned-URL minting/expiry + access-gate allow-path tests (#1377); CI-executable coverage for realtime preference-sync publish leg (#1376).
- **#1398 / #1400 / #1416** — atomicity / drift regressions: record_payment non-atomic check-then-insert regression test (#1361); cron-validator drift block (#1368); record_reserve_transaction concurrency + `FOR UPDATE` (#1371).
- **#1399 / #1430** — work-orders service-history cross-org IDOR coverage and explicit org gate (#1372). 403 contract chosen here; #1438 (open) proposes the 404 alternative as defense-in-depth follow-up (BIT-56 RlsConnection conversion).
- **#1417** `pm-qa-vote-partial-cmp-nan-fuzz` — directly closes Phase 1.5 finding `code-review-api-core-vote-partial-cmp-panic` (NaN-weight `partial_cmp().unwrap()` panic on `/votes/{id}/results`). 5,000-iter NaN fuzz, evidence-gone verified by grep.
- **#1418** `fix(frontend) BIT-21` — resolves #1349 (tautological absolute-label test) and #1350 (missing beforeEach import).
- **#1431** `feat(gh-1375)` — pre-push cargo fmt + clippy gate; local hook, not yet a CI gate (Phase 1.6 pm-devops flagged the gap).
- **#1432** — removed stale `ai_chat.rs` + `building_certification.rs` from rls-pool-field-baseline (closes #1367).
- **#1385–#1389 / #1391 / #1401 / #1402** — mobile (gap-85-2 env-setup docs, Airbnb OAuth token-exchange serde tests, iOS xcconfig + per-env build config + Info.plist API_BASE_URL/ENVIRONMENT, FilterSheet Near Me Android/shared, iOS CoreLocation Near Me confirm, navigation state preservation AC-4 + auth-guard AC-5).
- **#1426 (botched)** — `feat(api-server): realtime preference-sync CI coverage + tenant-aware request helper` merged but combined a stale `client()` accessor + misplaced `#[derive(Clone)]` + two corrupted test files. `dev` backend does not compile post-merge. **See #1437 + #1435 / #1436 below.**
- **#1423 / #1424** / **#1439** — dispatcher state PRs + GH #1380 reconciliation.

## Watch
- **🚨 CRITICAL — #1437 (open, [bug])**: `dev` backend does not compile. `cargo check -p api-server --tests` + `cargo fmt --all -- --check` both red. Root cause: PR #1426 left `redis.rs` with a misplaced `#[derive(Clone)]` on `InMemoryBroker` (whose `Mutex` field is not Clone), a stale `client()` accessor on `PubSubService` (no `client` field after the 1376 refactor), and `tests/common/mod.rs` missing the `&` on `'a TestApp`. **Open fix PRs in flight: #1435 (broad fmt+helper repair) and #1436 (alternative + cargo-fmt sweep); #1438 stacked on #1435 (RLS-conversion work).** Every backend PR's CI is red until one of these lands.
- **Reverts:** none this run (the #1426 breakage is being patched forward, not reverted).
- **Churn hotspots this run (top 3):** `backend/servers/api-server/tests/booking_oauth_csrf_tests.rs` (484 lines — NEW from #1393), `…/booking_oauth_routes_tests.rs` (381 NEW), `backend/servers/api-server/src/routes/forms.rs` (334 — #1397 org-scope hardening; runs_seen=2 → repeated-churn fired).
- **Stale drafts / stalled review:** #1197 (7d, draft, no reviewDecision), #1179 (7d, not draft, no reviewDecision — newly emitted as `stalled-review-pr-1179`), #1316 (3d, draft), #988 (13d, epic — already in seen_signals).
- **Closed-not-merged this run:** #1425 (GH #1377 — document presigned-URL tests; functionally superseded by merged #1394 same day — backlog row `closed-not-merged-pr-1425`).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **mobile-rn** (reason: oldest unreviewed at 13d stale; no good churn-aligned pick — backend churn was in `api-handlers`/`api-core`, both reviewed within 6 days).
- Experts: frontend.
- Findings: **3** — see Backlog deltas for `code-review-mobile-rn-report-fault-fake-submit` (high, +3), `code-review-mobile-rn-screens-mock-data` (medium, +2), `code-review-mobile-rn-deeplink-init-unhandled` (medium, +2). One promoted to a plan.
- Next segment: **mobile-native-kmp** (last reviewed 2026-06-06 — oldest after this run).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0 — not yet seeded).
- Drift signals this run: **1** (`screen-map-drift-pr-1418-ppt` — PR #1418 touched `frontend/apps/ppt-web/src/routes/groups/faults.route.test.tsx` under `routes/**` without updating `docs/screens/ppt/*.md`. Heuristic caveat — it's a test-file fix for #1349/#1350, not a route surface change; review-candidate only).
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not fetched this run (no `docs/screens/**` edits to trigger).

## Backlog deltas
- **New:**
  - [3] `code-review-mobile-rn-report-fault-fake-submit` — ReportFaultScreen.tsx handleSubmit() fakes API call (Phase 1.5; promoted to plan).
  - [2] `code-review-mobile-rn-screens-mock-data` — 9 production screens render hardcoded MOCK_* arrays (Phase 1.5).
  - [2] `code-review-mobile-rn-deeplink-init-unhandled` — useDeepLinkRouting onNavigate-identity re-init + void promise no .catch (Phase 1.5).
  - [2] `screen-map-drift-pr-1418-ppt` — PR #1418 routes/** touch w/o screen-doc update (heuristic, test-file fix; review candidate).
  - [1] `churn-hotspot-backend-servers-api-server-tests-booking-oauth-csrf-tests-rs` — 484 lines NEW test file (healthy churn).
  - [1] `churn-hotspot-backend-servers-api-server-tests-booking-oauth-routes-tests-rs` — 381 lines NEW test file (healthy churn).
  - [1] `closed-not-merged-pr-1425` — PR #1425 (GH #1377) closed unmerged; superseded by merged #1394.
  - [1] `dx-stalled-review-pr-1179` — PR #1179 docs(epics) backfill open 7d with no reviewDecision.
- **Bumped:** [1→2] `churn-hotspot-backend-servers-api-server-src-routes-forms-rs` — runs_seen=1→2 across 2026-06-13/2026-06-16 (`repeated-churn` signal stacked +1; original `churn-hotspot` re-emitted with +0).
- **Decayed:** `test-gap-screen-map-drift-pr-922-ppt` — −1 (no signal in 14d, 2→1).
- **Dropped:** `refactor-oauth-integration-tests-hot` — decayed to 0 (was 1, no signal in 14d).
- **Resolved:** `code-review-api-core-vote-partial-cmp-panic` → `done` (PR #1417 merged 2026-06-16; `partial_cmp().unwrap()` grep-gone in vote.rs; new `select_winner()` helper at line 51 + 5,000-iter NaN fuzz).

## Plans promoted
- `plans/code-review-mobile-rn-report-fault-fake-submit.md` — Wire ReportFaultScreen.handleSubmit() to a real `POST /api/v1/faults` mutation; add ReportFaultScreen.test.tsx pinning the network call. Mode: cloud-ok (no device required for the unit-level test). · adversarial pass: passed.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. `screen-map-drift-pr-1418-ppt` is medium-confidence + heuristic-caveat (test file only); `stalled-review-pr-1179` matches comment-only shape but item score is only 1; `fixme-in-merged-code` did not fire; no lockfile-drift dep-update.

## Self-review findings
- `.research/self-improvement/findings.json` absent — none

## Open questions
- **Decision needed on #1437 fix path:** merge #1435 (broad repair) or #1436 (alternative + cargo-fmt sweep)? Both are open against `dev`. #1438 stacks on #1435 (work-orders BIT-56 RLS-connection conversion). The fix-PR sequence affects how quickly the stranded approved PRs (#1392, #1420, #1421) can drain.
- **Stale drafts to call:** #1316 (3d, verify-document-folder-organization-backend-promote), #1197 (7d, OAuth integration tests), #988 (13d, e2e framework epic).
- **Phase 1.6 PM rotation advanced** — pm-devops ran this run (last 2026-05-27 → 20d stale; next: pm-security at index 5). Coverage cursor advanced 11 → 12 (epic re-checked).

## Quiet day?
- **No.** 23 application-code PRs merged, 1 critical pipeline-blocker filed and fix PRs already in flight, 8 new backlog vectors + 1 resolution, 1 plan promoted, 3 code-review findings from Phase 1.5, pm-devops Phase 1.6 (full role analysis after 20d stale + Scrum Master synthesis). `stats.runs` 39 → 40, `vectors_created` 164 → 173, `plans_created` 11 → 12.
