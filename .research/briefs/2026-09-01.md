# 2026-09-01

## Since last run
- Merged PRs: 6 (range #2897, #2905, #2907, #2908, #2910, #2911; last routine cursor was #2904 — #2897 in window via `mergedAt > SINCE_ISO`)
- Open PRs touched: 2 (#2912 open non-draft, #2902 open draft/needs-human-review)
- New / updated issues: 0 (issue #2909 was created and closed within the last-run window — #2911 closes it)
- Commits: ~25 on `dev` (mostly dispatcher-chore commits + the 5 code PRs above)
- Routine lag: 19h since last run — on schedule (last_run_iso 2026-08-31T14:45:57Z, this run 2026-09-01T10:50Z)
- Phases that failed: none
- Trigger: `buffer-low` payload from the dispatcher — `claimable=8/72, all mobile-native/KMP-gated (unlandable in cloud); need landable backend/frontend vectors. refill planner`.

## Shipped
- #2911 gh-issue-2909: sweep clamp_limit across remaining api-server list handlers (Closes #2909) — 10 handlers converted from `.unwrap_or(N)` to `clamp_limit(..)` (post-#2878 sweep). No new TODO/FIXME.
- #2910 churn-hotspot-reality-server-sso: bound in-memory pending SSO session map — reality-server `PendingSsoSession` map now prunes on-mint (mirrors #820/#921 sweep), single-file diff + 3 unit tests. No new TODO/FIXME.
- #2908 chore(research): dispatcher 2026-08-31 14:41 state — research-branch, `.research/` only, no application code.
- #2907 gh-issue-2906: unblock backend test-shard 1 — align booking IDOR test JWT secret + `require_super_admin` for compliance audit-log endpoints. Closes #2906. Well-tested; no new TODO.
- #2905 churn-hotspot(reality-server): floor search-alert drainer poll interval to avoid zero-period panic — `SEARCH_ALERT_DRAINER_INTERVAL_SECS=0` no longer panics tokio::time::interval. Single-file + unit test.
- #2897 code-review-reality-server-sso-per-call-client-no-timeout: reuse pooled SSO HTTP client with timeouts — one AppState-owned reqwest::Client (15s/5s) across `sso_login`/`sso_callback`/`sso_introspect`.

## Watch
- Stalled review: (unchanged from prior briefs) — #2559 / #2558 / #2555 (feat(acc) UC-ACC-05.x) still `needs-human-judgement`; 30+ days idle; owner=self, no reviewer.
- Reverted: none this run.
- Churn hotspots: research-branch churn dominates (backlog/state/management writes); only real code hotspots since last run are `reality-server/routes/sso.rs` (touched by PRs #2897 and #2910) and `api-server/routes/compliance.rs` (touched by PR #2907). Both are already covered by explicit `code-review-finding` signals folded this run.

## PRs stuck in draft despite approval
- #2902 screen-map-drift-pr-2894-reality — draft + needs-human-review; branch contaminated (reverts merged .research ledger + AuthContext test). Prior dispatcher note: needs human close+re-cut, do NOT merge.
- #2744 fix(dispatcher): un-wedge oversize-archive push + close retry-remint cross-id ghost — draft + needs-human-review; approved but self-PR (Approvals gate demands non-author), sitting 19d.

## PRs with verdict=changes, no fix-round progress in 24h
- #2902 last_updated 2026-09-01T02:28 (dispatcher held it; reviewer verdict=changes) — parked as needs-human-review per dispatcher `bc9adb1e`/`3a286e20`.
- otherwise none from planning-branch assignments.json this window.

## Code review slice
- Segment reviewed: none (Phase 1.5 skipped this supplementary fire) — instead this routine **adopted 4 unspent Tier-1d dispatcher-generated signal files** (`.research/signals/2026-09-01-api-core-tier1d.json`, `2026-09-01-ppt-web-ui-tier1d.json`, `2026-08-31-api-handlers-tier1d.json`, `2026-08-31-reality-server-tier1d.json`, `2026-08-31-mobile-native-kmp-tier1d.json`), folded 13 `code-review-finding` signals into backlog, none previously in `seen_signals`.
- Experts covered by adopted signals: rust (api-core, api-handlers, reality-server), frontend (ppt-web-ui), kotlin (mobile-native-kmp).
- Findings: 13 (all in Backlog deltas below; all `code-review-finding`, all `medium` confidence except the mobile-native one at `high`).
- Next segment: `mobile-native-kmp` (review_cursor oldest, 2026-08-25) — deferred to next scheduled run since it's the parked cloud-build blocker (#2652).
- Skipped: yes — supplementary buffer-low fire; adopted existing tier1d output rather than spawning fresh review.

## Screen-map status
- Total `docs/screens/` files: unchanged (`ppt/` + `reality/`; `mobile/` still not seeded).
- Drift signals this run: 0 (merged PRs #2897, #2905, #2907, #2910, #2911 touched backend + tests only — no `frontend/apps/ppt-web/src/App.tsx` or `routes/**` or `frontend/apps/reality-web/src/app/**` files).
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched this supplementary run (no route churn to warrant it).

## Backlog deltas
- **New (all 12 landable — refill for buffer-low fire):**
  - [2] `code-review-api-core-fcm-v1-static-oauth-token` — api-server push_fanout static FCM_OAUTH_TOKEN → Android push 401s ~1h post-boot (bug)
  - [2] `code-review-api-core-apns-jwt-per-send-mint` — ApnsHttpAdapter mints ES256 JWT every send (perf)
  - [1] `code-review-api-core-fcm-legacy-dead-endpoint` — /fcm/send legacy fallback is Google-decommissioned (bug)
  - [2] `code-review-ppt-web-ui-emergency-dir-raw-err-message` — 5 setError sites render raw err.message, t() fallback is dead (bug)
  - [1] `code-review-ppt-web-ui-meters-dates-no-locale` — 8 toLocaleDateString() sites ignore i18n language (bug)
  - [1] `code-review-ppt-web-ui-notif-settings-hardcoded-errors` — Settings > Notifications error toasts hardcoded English (bug)
  - [2] `code-review-api-handlers-data-export-report-stub` — GDPR data-export report returns hardcoded zeros to platform admins (bug)
  - [2] `code-review-api-handlers-user-audit-logs-total-page-len` — `total = page.len()` hides audit-log pages 2..N on super_admin surface (bug)
  - [1] `code-review-api-handlers-compliance-raw-db-error-leak` — raw sqlx `e.to_string()` bubbled to client 500 body (bug)
  - [2] `code-review-reality-server-agent-review-self-review` — agent review endpoint missing `reviewer_id != subject_id` guard (bug)
  - [1] `code-review-reality-server-pagination-offset-overflow` — reality-server list `?offset` uncapped; parallel to api-server #2911 (bug)
  - [1] `code-review-reality-server-saved-search-error-string-status` — client parses error strings for status; brittle contract (bug)
- **Bumped:** [2→4] `code-review-mobile-native-kmp-create-listing-not-wired` — stacked `code-review-mobile-native-kmp-create-listing-notimpl-stub` (+2, high-confidence), confidence medium→high, updated_at→today.
- **Decayed:** none — no open item's `updated_at` is older than 14d as of today (oldest open is `portfolio-analytics-caps-100` at 13d).
- **Dropped:** none.
- **Resolved (from merged PRs):** none — merged PRs this window did not verbatim-cite any existing backlog `id`, and no open item's `files` + evidence patterns are gone (spot-checked mobile-native-kmp open items — all patterns intact; mobile-native untouched this window).

## Plans promoted
- **0 new plans this run.** Only candidate meeting `score >= 3 && confidence == high && status == open && !plan` is `code-review-mobile-native-kmp-create-listing-not-wired` (score 4, high). Deliberately deferred: it is mobile-native/KMP, **unlandable in the current cloud runner** (issue #2652 AGP egress blocker) — a companion mobile-native plan (`code-review-mobile-native-kmp-inquiries-response-contract`) is already `ready` on the same parked pile. Promoting a second unlandable plan would not address the `buffer-low` intent (dispatcher wants LANDABLE refill). The 12 new landable candidates enter backlog at score 1-2, one increment below the promotion gate — future runs can stack a second signal to unlock promotion without gaming the gate.

## Auto-fix
- `no candidate` — no signal met `confidence == high` + `score >= 3` + allowlist match this run. Allowlist types (screen-map-drift, screen-map-orphan, fixme-in-merged-code, dep-update-noise lockfile-bump, stalled-review) all inapplicable: no route churn, no new FIXMEs, no lockfile drift, and the three `stalled-review-*` items in backlog are already at `needs-human-judgement` (owner=self, no reviewer to ping).

## Self-review findings
- none (`.research/self-improvement/findings.json` not populated / absent).

## Open questions
- Should we open a follow-up issue for the mobile-native cloud-build blocker (#2652 AGP egress) to raise operator visibility? The dispatcher has been flagging `GC3-buffer-bounds=FAIL` every run since 2026-08-31 — 4+ dispatcher cycles logging the same structural block. This routine's answer: no new issue right now (existing #2652 is the tracker), but if buffer stays starved through 2026-09-03 the routine should file a follow-up "structural-block" issue naming the specific dispatcher-cycle count.

## Quiet day?
- No — 6 merged PRs, 12 new landable backlog items, 1 item bumped/stacked.

## Goal violations
- none — G1-G17 checked in Phase 6, all passing (see `signals/2026-09-01.json.goal_checks`).
