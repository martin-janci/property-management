# 2026-09-17

_Cloud routine catch-up run after 16-day gap. Trigger: `buffer-low: claimable=12/72 — refill planner`._

## Since last run
- Merged PRs: ~35 (range #2922–#2956) — last routine cursor at #2921 (2026-09-01); latest merged on `dev` is #2956 (2026-09-08 20:33Z). Nothing has merged in the last 9 days — the pipeline itself has been quiet since #2956.
- Open PRs touched: 2 dependabot (#2958 gradle-minor-patch, #2959 npm-minor-patch); no non-bot open code PRs.
- New / updated issues: last cursor #2847 → latest #2951 (open, mobile RN jest-expo/react-native version rot); #2946 (Cross-tenant IDOR portfolio_analytics) also open. All others in window landed or closed.
- Commits: ~35 code + many dispatcher-chore commits on `dev`.
- Routine lag: **16d 1h since last routine commit** — `stale_routine_alert` fires. Dispatcher (separate loop) has been ticking on `.research/management/assignments.json`; only the routine loop stalled. Deep-catch-up NOT run in this fire — cursors advanced past known state, backlog reconciled minimally, and no synthetic 30-day sweep was launched (avoids re-scoring dispatcher-consumed signals).
- Phases that failed: 1.5 (skipped — dispatcher already produced today's Tier-1d segment review; routine adopts that signal); 1.6 (skipped — heavy PM analysis deferred; state.pm_cursor untouched).

## Shipped
_This run does NOT re-materialise per-PR shipped notes for the 35-PR catch-up window. Recent shipped PRs (post-#2921) were dispatcher-driven code-review resolutions and NFC-credential purge PRs (#2940-#2956); their content is already reflected in the merged-PR labels (`post-merge-reviewed`) and the corresponding backlog rows have been terminal (`done`/`dropped`) since the reconciler ran. Explicitly deferred to keep this run's commit scope small; if a fuller shipped list is needed, replay with `text: "deep"`._

## Watch
- Stalled review: none new (dispatcher-owned drafts turnover normally).
- Reverted: none in window.
- Churn hotspots: **skipped** — routine did not run a fresh top-3 churn scan this fire (avoiding cursor-advance thrash on the 16-day window). Dispatcher's per-run Tier-1d output (see `.research/signals/2026-09-17-*-tier1d.json`) is the current signal of segment activity.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: `ppt-web-core` (adopted from dispatcher Tier-1d 2026-09-17 fire — see `.research/signals/2026-09-17.json`).
- Experts: frontend.
- Findings: 1 net-new MEDIUM — `code-review-ppt-web-core-ws-jwt-in-url` (WebSocket handshake sends full JWT access token in URL query string; residual log-capture exposure through proxy access logs / browser history / APM traces despite wss://). Added to backlog at score 2 (security/medium).
- Next segment: `mobile-native-kmp` (review_cursor `2026-08-25` still oldest) — deferred; cloud runner remains AGP-egress-blocked (#2652).
- Skipped: yes — routine reused dispatcher's morning fire rather than spawning a fresh review pass in the 16-day catch-up.

## Screen-map status
- Total `docs/screens/` files: unchanged from 2026-09-01 (no `frontend/apps/mobile/src/**` docs yet).
- Drift signals this run: 0 (no scan on the 16-day window in this fire).
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched.

## Backlog deltas
- **New:**
  - [2] `code-review-ppt-web-core-ws-jwt-in-url` — JWT access token in WS handshake query string; hardening: short-lived WS ticket endpoint or backend-side access-log query-string redaction. security/medium; ingested from dispatcher signal. (see backlog.md)
- **Bumped:** none.
- **Decayed (−1 each, `updated_at` > 14d as of today):**
  - `code-review-mobile-native-kmp-portfolio-analytics-caps-100` 1→0 → dropped (mobile-native cloud-unlandable, #2652).
  - `code-review-mobile-native-kmp-cancellation-swallowed` 1→0 → dropped.
  - `code-review-mobile-native-kmp-httpclient-no-timeout` 1→0 → dropped.
  - `code-review-mobile-native-kmp-create-listing-not-wired` 4→3 (still open; still promotable; still cloud-unlandable, kept unpromoted per 2026-09-01 rationale).
- **Dropped:** the 3 above.
- **Resolved:** none newly resolved in this fire — backlog-reconcile.sh (dispatcher-side) already flipped the recent merged PRs' matching rows to `done`/`dropped` between 2026-09-01 and 2026-09-17.

## Plans promoted
- **0 new plans this run.** Rationale carried forward from 2026-09-01: the only landable-gate-passing candidate on the active backlog is `code-review-mobile-native-kmp-create-listing-not-wired` (score 3 after this run's decay, high, has files, has sources) — but mobile-native/KMP is cloud-unlandable (#2652 AGP egress). Promoting it would not refill the dispatcher's *landable* buffer. Held. `code-review-ppt-web-core-ws-jwt-in-url` (score 2) is below the +3 promotion threshold and below the security fast-track's +2 confidence=high bar (it is confidence=medium — analysis-grade, needs one more corroborating signal to become fact-grade).

## Auto-fix
- `no candidate` — no signal met `confidence == high` + `score >= 3` + allowlist match. The new `ws-jwt-in-url` is `medium`+ score 2 (below both gates), and its allowlist match would be `code-review-finding` which is explicitly outside the auto-fix allowlist by design (routine is not the right context to touch WS auth transport code).

## Self-review findings
- none (`.research/self-improvement/findings.json` absent).

## Open questions
- **#2652 AGP egress blocker — still the chronic buffer floor.** Mobile-native/KMP work has now accumulated ~6 weeks of parked signals (create-listing-not-wired, inquiries-response-contract, android-sso-csrf, plus the 3 items dropped this run). The dispatcher's `buffer-low` fires are increasingly a symptom of "no landable KMP work" not "no signals". Worth escalating: either unblock the egress path in the cloud runner, or move mobile-native to a mefistos-bridge implementer only.
- **Routine cadence.** The 16-day gap between 2026-09-01 and 2026-09-17 is the second-longest since routine launch. Every dispatcher tick during this window continued to fire `buffer-low` triggers without the routine responding; the trigger-to-routine wiring in `dispatcher-trigger-bootstrap.md` may have gone dormant. Worth verifying the cloud-side trigger receiver on the next operator poke.

## Quiet day?
- **No — but a small day.** 1 new backlog vector (ws-jwt-in-url), 3 items decayed to dropped, 1 item decayed one step. No plan promoted. No auto-fix. The point of this fire is buffer visibility, not vector inflation: the buffer is low because the landable pipeline is genuinely thin (the mobile-native pile is unlandable and everything else was consumed).
