# 2026-08-27

## Since last run
- Merged PRs: 9 (range #2848–#2860)
- Open PRs touched: 0 since 2026-08-25T08:50:37Z (10 dependabot stalls unchanged from last run; 3 human `feat(acc)` PRs still `needs-human-judgement`; 1 draft PR #2744 dispatcher fix still parked at `needs-human-review`)
- New / updated issues: 6 total in window (2 still open — #2861/#2862 both `follow-up`+`from-merged-review`; 4 closed — #2853/#2854/#2857/#2859 all resolved-and-closed within the window)
- Commits: many on `dev` (9 real merge commits + a dense stream of dispatcher chore commits touching only `.research/management/{assignments,action-list}.json`; last non-dispatcher commit b82f728 = PR #2860 merge)
- Routine lag: 1d 18h since last run — **lag_warning fired** (>36h threshold, <72h so no stale_routine_alert this run — cloud routine slipped exactly one daily beat, dispatcher on planning branch continued to tick through the gap)
- Phases that failed: `phase-1.5-skipped` (rotating expert review) + `phase-1.6-skipped` (PM/scrum-master rotation) — both intentionally skipped this run for a lean catch-up; the respective cursors (`review_cursor`, `pm_cursor`, `coverage_cursor`) are left at HEAD so next run picks up the segment/role/epic they were on. Not a Phase-1 fetch failure — GitHub REST paths all succeeded via `mcp__github__*`.

## Shipped
- #2848 `churn-hotspot-aml-dashboard-page` — extract AML dashboard sections + data hook: AmlDashboardPage.tsx becomes a thin orchestrator; moves 240 lines to `useAmlDashboard()` and 3 sub-components (`AmlThresholdsSection` / `AmlCountryRisksTable` / `AmlFiltersPanel`). Resolves the churn-hotspot signal `churn-hotspot-…-AmlDashboardPage.tsx` from 2026-08-25 (via extraction rather than surgical fix).
- #2849 `code-review-ppt-web-ui-content-mod-overdue-noop` — wires ContentModerationPage overdue affordance to actually filter cases (client-side, on `age_hours`); resolves the score=2 open item from 2026-08-25.
- #2850 `pm-qa-verification-badge-expiry-i18n-visual-diff` — snapshot VerificationBadge expiry copy across sk/cs/de/en (#2825 follow-up).
- #2851 `pm-qa-voice-oauth-token-encryption-roundtrip-test` — unit + integration tests for centralized voice OAuth token encryption (#2838 follow-up).
- #2852 `churn-hotspot AmlDashboardPage.test.tsx` — de-duplicates decision-flow test helpers (`decisionSelect()`, `reviewNotesInput()`, `reasonInput()`, `submitReview()`, `cancelDialog()`, `eddDialogSubmit()`); resolves the churn-hotspot on the test file.
- #2855 `gh-issue-2854` — replace DsaReportsPage `window.alert` with localized toast feedback (Closes #2854).
- #2856 `gh-issue-2853` — server-side moderation overdue filter: new `overdue: Option<bool>` list param in `ModerationQueueQuery`, mirrored predicate in `ComplianceRepository::list_moderation_cases` (`status IN ('pending','under_review') AND created_at < NOW() - 24h`). Client no longer narrows a truncated page (Closes #2853). No DB test in the merge — that was filed as #2859 and closed 1d later by #2860.
- #2858 `fix(api-server)` **security** — capability-grant email-verification gate now resolves the *strictest* policy across all grantee memberships (was `.first()` on a `list_for_user` that has no `ORDER BY` → non-deterministic fail-open). Adds sqlx integration test seeding lax+strict membership order. Closes #2857.
- #2860 `gh-issue-2859` — DB test for the overdue moderation filter (queue-stats vs list-total invariant) + explicit truncation notice when the overdue page hits `OVERDUE_PAGE_LIMIT=200` while `overdue_count > 200`. Closes #2859.

## Watch
- Stalled review: none NEW this run — 3 human `feat(acc)` PRs (#2555 UC-ACC-05.17 / #2558 UC-ACC-05.9 / #2559 UC-ACC-05.8) still `needs-human-judgement` (now 32d idle). 10 dependabot PRs still stalled (13d–27d) — `dep-update-noise` delta 0.
- Reverted: none this window.
- Churn hotspots (top-4 this run — 2 fresh, 2 repeated):
  - `frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.tsx` — 3 PR touches this window (#2849 client-side overdue, #2856 server-side move, #2860 truncation notice); `runs_seen` 0→1. **FRESH hotspot.**
  - `frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.test.tsx` — 3 PR touches this window; `runs_seen` 0→1. **FRESH hotspot.**
  - `frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.tsx` — 1 PR (#2848); `runs_seen` 1→2 → **repeated-churn**. Reads as planned refactor tail, not instability.
  - `frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.test.tsx` — 2 PRs (#2848 + #2852); `runs_seen` 1→2 → **repeated-churn**. Also refactor tail.
- Open post-merge-review follow-ups (both from-merged-review, both opened this run — not yet triaged into backlog because they're already-labeled GitHub issues, not `untriaged-issue`):
  - **#2861** — PR #2858 capability-grant fix leaves *root-cause* membership non-determinism (list_for_user still lacks ORDER BY), a dead single-org code path, and stale doc. Symptom masked by the strictest-across-orgs fix; the root cause should be closed next.
  - **#2862** — PR #2860 truncation notice fires a false positive at the exact `overdue_count == 200` boundary (`cases.length === OVERDUE_PAGE_LIMIT` is not sufficient — should also require `overdue_count > OVERDUE_PAGE_LIMIT`). Low-severity off-by-one on the affordance the same PR added.

## PRs stuck in draft despite approval
- #2744 — draft `needs-human-review` since 2026-08-13 (14d) — dispatcher-internal fix for oversize-archive push (#1162) + retry-remint cross-id ghost (#2460). Approved+green; blocked on the human-review gate. **Not new**; already surfaced in prior briefs as an operator-side merge.

## PRs with verdict=changes, no fix-round progress in 24h
- none in current window — dispatcher chore-commits across the 9 merges show only `approve` verdicts and same-window `merged-now` transitions.

## Code review slice
- **Skipped this run** (Phase 1.5 not invoked) — token-budget catch-up run; `state.review_cursor` unchanged so the next run picks up exactly where we left off. Next-up on cursor age: `reality-web` (2026-08-18, 9d) is oldest; runner-up churn-aligned candidate would be `api-handlers` (auth_policy.rs security fix in PR #2858 already yielded a follow-up issue #2861, which effectively surfaces one code-review-finding without a segment sweep).

## Screen-map status
- Total `docs/screens/` files: 153 (ppt=106 + reality=47 + mobile=0 — unchanged; mobile intentionally excluded from drift detection).
- Drift signals this run: 0 — none of the 9 merged PRs touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`. All merged UI changes were under `features/compliance/pages/**` and `features/compliance/components/**`.
- Orphan screens this run: 0 — no route basename changes under the trigger set.
- Last `screen-map.yml` CI run on `main`: success @ 2026-06-04T22:19Z (workflow only fires on relevant docs/frontend paths; unchanged from prior brief — no relevant paths touched).

## Backlog deltas
- **New:** [1] `churn-hotspot-frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.tsx` (refactor, high) — see backlog.md
- **New:** [1] `churn-hotspot-frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.test.tsx` (test-gap, high)
- **New:** [1] `repeated-churn-frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.tsx` (refactor, medium)
- **New:** [1] `repeated-churn-frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.test.tsx` (test-gap, medium)
- **Bumped:** none this run
- **Decayed:** none this run — the 3 remaining `open` rows (`code-review-mobile-native-kmp-{portfolio-analytics-caps-100, cancellation-swallowed, httpclient-no-timeout}`) all have `updated_at` within 14d of 2026-08-27 (2026-08-19 / 08-25 / 08-16)
- **Dropped:** none this run
- **Housekeeping:** the two churn-hotspot rows created 2026-08-25 for `AmlDashboardPage.{tsx,test.tsx}` are already at `status=done` (resolved by PRs #2848 / #2852 respectively — the extraction refactor and test helper de-dup); no additional flip needed, they came in done via the reconcile.

## Plans promoted
- **none this run** — the single `ready` row (`code-review-mobile-native-kmp-inquiries-response-contract`, score 3, high) already has a plan at `plans/code-review-mobile-native-kmp-inquiries-response-contract.md` from 2026-08-16, so the mobile-native-kmp gate keeps it parked. No other row hits the readiness gate (`score >= 3`, or security fast-track `vector=security ∧ confidence=high ∧ score >= 2`). Closest sub-threshold candidate remains `code-review-mobile-native-kmp-httpclient-no-timeout` (score 2, high, bug — 1 short of the gate).

## Auto-fix
- `no candidate` — no signal met the allowlist × confidence=high × score≥3 bar this run. Detail: this run's 4 backlog additions are all `churn-hotspot`/`repeated-churn` (analytical types explicitly outside the auto-fix allowlist per Phase 5); the 3 human stalled-review PRs are already in `state.seen_signals` (one-shot dedup); no `screen-map-drift`, no `screen-map-orphan`, no `fixme-in-merged-code`, no lockfile-drift dep-update variant this window.

## Self-review findings
- none — no `.research/self-improvement/findings.json` file present in the repo

## Open questions
- Cloud routine slipped ONE beat (~42h since last run vs the daily cadence). Not stale yet (<72h) but worth watching — the previous run's brief noted 3 consecutive missed days on 2026-08-25 and this catchup extends that pattern. If it re-slips this week, inspect the cloud scheduler beyond the dispatcher (which continued to tick through the gap).
- Follow-up **#2861** (PR #2858 root-cause: `list_for_user` still lacks `ORDER BY`, single-org code path is dead) is a real correctness issue behind the security fix. Suggest ingesting into backlog on next run once labeled beyond `follow-up`, or treating as an implicit `code-review-finding` via a targeted Phase 1.5 sweep on `api-handlers`. Waiting for human triage.
- The 3 stalled `feat(acc)` PRs (#2555 / #2558 / #2559) are now 32d idle. Same open question the prior brief carried — blocked on the accounting spec resolution.

## Quiet day?
- **No** — 9 PRs shipped since last run (including a real security fix — PR #2858), 4 new backlog vectors created, 0 plans promoted, 0 auto-fixes. `stats.runs` bumps 71→72; `stats.vectors_created` 324→328; `stats.plans_created` unchanged at 28; `stats.quiet_days` unchanged at 3.
