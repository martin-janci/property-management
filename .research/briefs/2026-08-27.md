# 2026-08-27

## Since last run
- Merged PRs: 10 (range #2848–#2863)
- Open PRs touched: 1 (#2864 — reviewer approved, CI pending, human-gated merge)
- New / updated issues: 4 (#2853, #2854, #2857, #2859, #2861, #2862 filed and mostly closed via the merges above)
- Commits: 40+ on `dev` (bulk are dispatcher chore commits under `.research/`)
- Routine lag: 1d 21h since last run (over the 36h threshold; `lag-warning-2026-08-27` emitted)
- Phases that failed: none
- Trigger payload: `buffer-low: claimable=6/72 landable=0 (all open backlog is mobile-native/KMP gated; coverage.json missing) — refill planner with landable backend/frontend work` — coverage.json is actually present on disk; mobile-native/KMP gate is real (AGP egress-403 in cloud). Ingested 26 unpromoted tier-1d code-review findings into backlog as landable seed work.

## Shipped
- #2863 gh-issue-2862: suppress overdue truncation notice at exact 200 boundary (moderation queue false-positive fix + boundary regression test).
- #2860 gh-issue-2859: DB test for overdue moderation filter + explicit list truncation notice (IG3 test + capped-page notice for the #2853 server-side filter).
- #2858 fix(api-server) #2857: strictest email-verification policy across all grantee orgs in capability-grant gate (fixes fail-open non-determinism in `AuthPolicyEnforcer::check_capability_grant_for_user`).
- #2856 gh-issue-2853: make moderation overdue filter server-side (drops client-side narrowing capped by `clamp_limit`; adds `overdue=true` query param).
- #2855 gh-issue-2854: replace `DsaReportsPage` `window.alert` with localized toast feedback (last compliance page still on native alert).
- #2852 churn-hotspot `AmlDashboardPage.test.tsx` decision-flow test-helper dedupe.
- #2851 pm-qa voice OAuth token encryption roundtrip tests (unit + suite_8 integration).
- #2850 pm-qa VerificationBadge expiry i18n visual-diff snapshots (sk/cs/de/en).
- #2849 code-review-ppt-web-ui-content-mod-overdue-noop: moderation overdue affordance now filters the queue.
- #2848 churn-hotspot AML dashboard: extract sections + `useAmlDashboard` hook (385 → 157 lines).

## Watch
- Stalled review: PR #2744 (gh-issue-2743 dispatcher archive-push un-wedge) — approved+green but `needs-human-review` label, 14 days in review. Human merge required; dispatcher stays parked on it.
- Reverted: none this window.
- Churn hotspots (Aug 25 → Aug 27):
  - `frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.tsx` — touched by #2848 refactor; `runs_seen=4` (historical).
  - `frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.tsx` — touched by #2849 + #2856 + #2863 in the last 48h; runs_seen bumped.
  - `backend/servers/reality-server/src/routes/moderation.rs` (or successor) — server-side overdue filter added in #2856.
  - `backend/servers/api-server/src/policy/auth_policy.rs` — #2858 fix.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: `none this run` — Phase 1.5 skipped: the dispatcher's Tier-1d rotating generator has run five slices in the last 48h (api-handlers, api-core, reality-server, reality-web, ppt-web-core, mobile-rn) and already produced 26 unpromoted `code-review-finding` signals. Re-running the routine's own slice would compound signal debt without a promotion path.
- Experts: n/a
- Findings: 26 tier-1d findings ingested into backlog this run (see *Backlog deltas*).
- Next segment: `mobile-native-kmp` (oldest unreviewed; but likely to be gated as cloud-unlandable).
- Skipped: yes — reason: unpromoted tier-1d signal debt above ingestion capacity.

## Screen-map status
- Total `docs/screens/` files: existing count (unchanged this run; routine did not enumerate).
- Drift signals this run: 0 (none of the 10 merged PRs touched `apps/ppt-web/src/{App,routes}` or `apps/reality-web/src/app/**` route files without a matching `docs/screens/*` update on inspection).
- Orphan screens this run: 0 (not exhaustively scanned).
- Last `screen-map.yml` CI run on `main`: not checked this run.

## Backlog deltas
- **New (26)**: 26 tier-1d code-review findings ingested (backend/frontend/mobile). Highlights:
  - [3] `code-review-api-handlers-hellosign-unbound-envelope` — HelloSign webhook MACs `event_time+event_type` only; `envelope_id` unsigned → cross-tenant envelope swap. **Promoted this run.**
  - [2] `code-review-api-handlers-esign-webhook-replay` — no timestamp/freshness on any provider.
  - [2] `code-review-api-core-fault-report-csv-injection` — fault-report CSV cells not sanitized (stored-formula-injection).
  - [2] `code-review-api-core-workflow-regex-recompile` — `Regex::new` on every workflow condition evaluation (per-event DoS).
  - [2] `code-review-search-drainer-stub-marks-delivered` — `SearchAlertDrainerWorker` wired with logging stubs; alerts marked delivered without send.
  - [2] `code-review-ppt-web-core-ws-query-invalidation-dead` — `WebSocketWrapper` missing `onEntityEvent`; push invalidation unwired.
  - [2] `code-review-reality-web-realtor-api-missing-bearer-header` — `realtor-api.ts` never sends `Authorization + Bearer`; form-login writes 401.
  - [2] `code-review-reality-web-account-edit-wrong-locale-strings` — Slovak fallback error copy shown to en/cs/de users.
  - [2] `code-review-reality-web-realtor-account-pages-no-i18n` — 11 authenticated realtor/account page shells lack `useTranslations`.
  - [2] `code-review-mobile-native-kmp-account-screen-dead-rows`, `-twofactor-orphan-unwired`, `-profile-edit-drops-fields` — three UX/completeness gaps in KMP shared UI (mobile-native, still cloud-gated).
  - [1] Eight further i18n / minor-severity findings (mobile-rn hardcoded labels, reality-web systemic i18n, workflow_executor NotIn asymmetry, market_pricing DB-error swallow).
- **Bumped**: none this run.
- **Decayed**: none — all 4 open backlog items had `updated_at` within the 14-day decay window.
- **Dropped**: none.
- **Resolved to `done`**: none — the 10 merged PRs do not name any of the 4 open backlog item ids in body/title, and none of the 4 open items' file-cited TODO/FIXME patterns were removed by the merges (all 4 open items are mobile-native/KMP; the merges were backend + ppt-web only).

## Plans promoted
- `plans/code-review-api-handlers-hellosign-unbound-envelope.md` — HelloSign webhook signature omits `envelope_id`; extend the HMAC to the raw body (mirror DocuSign/Adobe) and add a `#[sqlx::test]` cross-tenant envelope-swap regression. Score 3, vector=security, confidence=medium, cloud-ok. adversarial pass: passed (source→hypothesis→approach→test-plan chain traces cleanly; the failing-on-main test uses a real HelloSign signature synthesized under the shared key and asserts 4xx, which will actually fail on `dev` HEAD 6f5e7cf; no placeholder rot).

## Auto-fix
- `no candidate` — no signal meets `confidence=high` + `score>=3` + the Phase-5 allowlist (`screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / lockfile-drift / `stalled-review`). The 26 new tier-1d findings are `confidence=medium`; the routine keeps them on the manual-implementer path via the hellosign plan.

## Self-review findings
- open=35 (high=8, medium=24, low=3); new this run=0; acknowledged=0
- fp-<none> [recurrence=17] — Phase-6 archive-push wedge (assignments-archive.json > 64KiB MCP inline ceiling); fix: adopt PR+API-merge path landed via PR #1163 as primary push mechanism for oversized archives.
- fp-<none> [recurrence=17] — dispatcher hard-codes proxy-403 assumption for `git push`; fix: runtime probe at Phase 1 sets PUSH_METHOD=git on success, bypasses 64KiB size-guard.
- fp-<none> [recurrence=10] — Tier-1d generator manufactures signals faster than planner drains (unpromoted tier1d signals piling in `.research/signals/`); fix: signals→backlog promoter or gate Tier-1d on backlog-avail. **Partly resolved this run** — routine ingested 26 unpromoted tier-1d findings into backlog.
- fp-<none> [recurrence=6] — action-list.json / assignments.json twin-status decoupled; fix: bijection reconcile step + self-test T32.
- fp-<none> [recurrence=3] — Phase-2.6 buffer counts open action-list items already terminal in archive; fix: extend gc1-reconcile + fix GC3 formula to subtract archive ids.
- … and 3 more high-severity open findings (see `.research/self-improvement/findings.json`).

## Open questions
- Should the routine's Phase 1.5 skip logic be codified? Right now the routine has to guess whether the dispatcher's Tier-1d has already saturated the ingestion pipeline; a rule like "skip Phase 1.5 when unconsumed tier-1d signal count > 8" would remove the judgement call. Filing as a self-review finding rather than a plan (routine-prompt change).
- PR #2864 is a self-PR by the dispatcher (approved+CI-pending, human-gated by `needs-human-review`). Should routine surface a Telegram nudge? — currently deferred to Phase 6 delivery digest.

## Quiet day?
- No — 26 new backlog vectors, 1 plan promoted (`code-review-api-handlers-hellosign-unbound-envelope`), 10 merged PRs since last run.
