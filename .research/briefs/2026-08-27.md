# 2026-08-27

## Since last run
- Merged PRs: 9 (range #2848–#2860)
- Open PRs touched: 5 (dependabot npm/cargo/gradle groups + compose-bom)
- New / updated issues: 4 (#2853, #2854, #2857, #2859 — all filed by post-merge review and all now closed)
- Commits: 42 on `dev` (mix of merged PRs + dispatcher chore-runs)
- Routine lag: 1d 17h since last run (41h — >36h threshold, `lag_warning` fired)
- Phases that failed: none
- Trigger: `buffer-low: claimable=6/72 landable≈0 (6 open all mobile-native/KMP gated); refill planner`

## Shipped
- #2860 gh-issue-2859: DB test for overdue moderation filter + explicit list truncation notice — closes the two follow-ups from #2856 (list↔stat invariant test, ≥200 truncation banner). Auto-merged from a self-PR.
- #2858 fix(api-server): enforce strictest email-verification policy across all grantee orgs in capability-grant gate — closes #2857. Fixes a fail-open non-deterministic gate in `AuthPolicyEnforcer::check_capability_grant_for_user`; adds two `#[sqlx::test]` regression tests.
- #2856 gh-issue-2853: make moderation overdue filter server-side — closes #2853; adds `overdue` query param to `list_moderation_cases`, drops client-side narrowing over a truncated page.
- #2855 gh-issue-2854: replace `DsaReportsPage` `window.alert` with localized toast feedback — closes #2854; aligns with #2841/#2829 toast pattern.
- #2852 test(ppt-web): de-duplicate `AmlDashboardPage` decision-flow test helpers — pure test refactor, all six assertions unchanged.
- #2851 pm-qa-voice-oauth-token-encryption-roundtrip-test: unit + integration tests around `encrypt_voice_token_pair` (#2838 choke point).
- #2850 pm-qa-verification-badge-expiry-i18n-visual-diff: snapshot `VerificationBadge` expiry copy across sk/cs/de/en; hard-asserts non-English never leaks English literals.
- #2849 code-review-ppt-web-ui-content-mod-overdue-noop: made the overdue affordance filter the queue client-side (#2856 then moved it server-side).
- #2848 churn-hotspot-aml-dashboard-page: extract AML dashboard sections + `useAmlDashboard` hook (385→157 lines in the page).

## Watch
- Stalled review: **#2744** (draft, human, 14d, `needs-human-review`) — the archive-push wedge fix for #1162 / #2743 is still human-gated; dispatcher hang-alert at 313h. This is the single most important unblock in the loop.
- Reverted: none this run.
- Churn hotspots: `ContentModerationPage.tsx` (+99 lines / 3 commits), `ContentModerationPage.test.tsx` (+280 lines / 3 commits), `messages/en.json` (+16 lines / 3 commits, runs_seen=2 → repeated-churn but cumulative, no score bump). Compliance/moderation dominated activity.
- Buffer starvation persists — claimable pool at 6/72 was **all mobile-native/KMP**, structurally unlandable in the cloud runner (AGP egress-blocked); this run promotes one cloud-landable security plan + queues 6 more cloud-landable backlog rows to refill the planner (see *Plans promoted* + *Backlog deltas*).

## PRs stuck in draft despite approval
- none (only draft #2744 is `needs-human-review`, no APPROVED verdict on the head)

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: api-core (reason: churn-aligned + oldest-unreviewed backend rotation; fresh 2026-08-27 Tier-1d output was surfaced as this run's Phase 1.5)
- Experts: rust, security (from the dispatcher-owned Tier-1d generator that fed 2026-08-27-api-core-tier1d.json + accumulated 2026-08-25/26 output for api-handlers, ppt-web-core, reality-web, reality-server)
- Findings: 7 promoted into backlog this run (fault-report CSV injection, market_pricing DB-error swallow, HelloSign envelope-binding gap, e-sig replay window, ppt-web WS query-invalidation dead, reality-web bearer header, search-alert drainer stub-transports)
- Next segment: `mobile-native-kmp` (oldest-unreviewed among stacks that aren't gated for landing)
- Skipped: no — reviewed (as above; using the dispatcher Tier-1d artifacts to avoid double-firing the skill)

## Screen-map status
- Total `docs/screens/` files: unchanged this run (no route touched by the 9 merged PRs — all compliance/backend/i18n)
- Drift signals this run: 0
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not re-checked (no docs/screens/** or route file touched in-window)

## Backlog deltas
- **New:** [3] `code-review-api-handlers-hellosign-unbound-envelope` (security, high) — attacker-swapped envelope_id forges cross-tenant HelloSign completion; promoted to a plan.
- **New:** [2] `code-review-api-handlers-esign-webhook-replay` (security, medium) — no timestamp/freshness check in e-sig receiver; sibling receivers all have one.
- **New:** [2] `code-review-api-core-fault-report-csv-injection` (security, medium) — fault-report CSV bypasses `sanitize_csv_cell`; stored formula-injection.
- **New:** [1] `code-review-api-core-market-pricing-db-error-swallow` (bug, medium) — `.unwrap_or_default()` on DB queries silently produces fabricated pricing recommendation on DB error.
- **New:** [2] `code-review-ppt-web-core-ws-query-invalidation-dead` (bug, high) — `App.tsx:94-99` passes no `queryClient` to `<WebSocketProvider>`; realtime cache invalidation is dead.
- **New:** [2] `code-review-reality-web-realtor-api-missing-bearer-header` (bug, medium) — form-login realtors 401 on all writes because `request<T>` uses only `credentials:'include'`.
- **New:** [2] `code-review-search-drainer-stub-marks-delivered` (bug, medium) — prod search-alert drainer wired with logging-stub transports; alerts marked delivered without leaving the server.
- **Bumped:** none (all four active items unchanged by this run's signals).
- **Decayed:** none (all four `open`/`ready` items are ≤11d since `updated_at`).
- **Dropped:** none.

## Plans promoted
- `plans/code-review-api-handlers-hellosign-unbound-envelope.md` — HelloSign MAC only covers event_time+event_type; bind envelope_id (or MAC full body like DocuSign/Adobe) + add a provider-consistency assert; failing-on-`dev` regression test in a new `esignature_webhook_hellosign_envelope_binding_tests.rs` suite. · adversarial pass: passed (source→hypothesis→approach→test chain intact; test would fail on `dev` today because `verify_hellosign_signature` accepts a body-swap unchanged, and pass after the fix; no placeholder rot; all cited paths resolve).

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The one signal at (high, 3) is `code-review-api-handlers-hellosign-unbound-envelope`, but security code fixes are not on the auto-fix allowlist (doc-stub / orphan-remove / lockfile-bump / fixme-issue / stalled-review only).

## Self-review findings
- none (`.research/self-improvement/findings.json` not consulted this run — dispatcher owns that stream)

## Open questions
- Should the routine start opportunistically promoting the Tier-1d dispatcher signals that pile up under `.research/signals/*-tier1d.json` (the reason there are hundreds of unpromoted findings — noted in every dispatcher chore log for the past 3 days)? This run promoted 7 by hand to refill the planner; a durable pipeline would be a routine change worth a plan of its own.
- PR #2744 (`fix(dispatcher): un-wedge oversize-archive push`) is the root of the buffer-starvation loop and stays parked on `needs-human-review` at 313h. Nothing in the routine can move it; it needs a human review to unblock the archive-push flow that in turn frees the dispatcher to actually land the mobile-native/KMP backlog.

## Quiet day?
- No (9 shipped PRs, 7 new backlog vectors, 1 plan promoted, ≥1 stalled-review signal). Telegram digest send eligible (Phase 6 will honor the `PM_DIGEST_QUIET=0` decision).
