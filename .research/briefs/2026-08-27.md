# 2026-08-27

## Since last run
- Merged PRs: 11 (range #2848–#2864, all authored by `martin-janci`)
- Open PRs touched: 6 (5 dependabot + 1 draft #2744 needs-human-review)
- New / updated issues: 6 (all opened + closed within window as `follow-up`/`from-merged-review` auto-issues — #2853/#2854/#2857/#2859/#2861/#2862)
- Commits: 55 on `dev` (11 code + 44 dispatcher chores) since last cursor `142ae17`
- Routine lag: ~49h since last run (skipped the 2026-08-26 daily slot) — `lag_warning` emitted; below the 3-day `stale_routine_alert` threshold
- Phases that failed: none

## Shipped
- #2848 `churn-hotspot-aml-dashboard-page` — extract AML dashboard sections + `useAmlDashboard` hook (thin-orchestrator refactor, Story 67.1)
- #2849 `code-review-ppt-web-ui-content-mod-overdue-noop` — client-side overdue filter (initial fix; drew #2853 follow-up)
- #2850 `pm-qa-verification-badge-expiry-i18n-visual-diff` — snapshot VerificationBadge expiry copy across sk/cs/de/en
- #2851 `pm-qa-voice-oauth-token-encryption-roundtrip-test` — unit + integration tests for centralized voice OAuth token encryption (#2838 hardening)
- #2852 `churn-hotspot AmlDashboardPage.test.tsx` — de-duplicate decision-flow test helpers
- #2855 `gh-issue-2854` — replace `DsaReportsPage` `window.alert` with localized toast feedback
- #2856 `gh-issue-2853` — server-side moderation overdue filter (Closes #2853)
- #2858 `fix(api-server) gh-issue-2857` — enforce strictest email-verification policy across all grantee orgs in capability-grant gate
- #2860 `gh-issue-2859` — DB test for overdue moderation filter + explicit list truncation notice
- #2863 `gh-issue-2862` — suppress overdue truncation notice at exact 200 boundary (Closes #2862)
- #2864 `gh-issue-2861` — fix root-cause membership ordering non-determinism + remove dead capability-grant path + refresh stale doc

## Watch
- **Stalled review:** #2555 / #2558 / #2559 (acc UC-ACC-05.17 / 05.9 / 05.8) — now 30d open, still no reviewDecision. All three already in backlog as `needs-human-judgement` — no new signal (dedup: `stalled-review-pr-2555/2558/2559` in `seen_signals`).
- **Draft with human-review label:** #2744 (dispatcher: un-wedge oversize-archive push + close retry-remint cross-id ghost) — 14d in draft, blocks the recurring `mcp-push-cannot-inline-large-archive` + `mcp-primary-push-default-obsolete-direct-git-push-works` self-improvement findings (both r=18). Operator merge would clear two of the top-5 high-severity self-improvement rows.
- **Cascade watch:** the overdue-affordance flow generated four merged PRs + three follow-up issues (#2853→#2859→#2862) in 48h. Each fix drew the next follow-up — a fresh churn-hotspot signal fires on both `ContentModerationPage.tsx` and its test (see Backlog deltas). The flow appears settled after #2863, but this is exactly the pattern that becomes `repeated-churn` next window.
- **Churn hotspots this run:** ContentModerationPage.tsx (121 lines / 4 PRs), ContentModerationPage.test.tsx (332 lines / 4 PRs), auth_policy.rs (191 lines / 2 PRs), voice_webhooks.rs (repeated — runs_seen 3→4, cumulative re-emit only, score_delta=0).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (`.research/management/assignments.json` on this branch has no `verdict=changes` review rows)

## Code review slice
- Segment reviewed: `reality-server` (reason: oldest-unreviewed non-infra-blocked; buffer needs landable Rust work outside mobile-native — see `state.review_cursor` — 2026-08-19 → 2026-08-27)
- Experts: rust
- Findings: 2 (see Backlog deltas for `code-review-reality-server-*` signals)
- Next segment: `reality-web` (oldest unreviewed after this run, `2026-08-18`)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: unchanged (no screen-doc edits in the 11 merged PRs; all touched `frontend/apps/ppt-web/src/features/compliance/pages/**`, which is NOT in the route trigger set — no drift signals apply)
- Drift signals this run: 0 (none of the merged PRs touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (no route-file edits to gate; dispatcher tracks separately)

## Backlog deltas
- **New:** [1] churn-hotspot ContentModerationPage.tsx — 4 PRs in 48h (see Watch); refactor consolidation candidate
- **New:** [1] churn-hotspot ContentModerationPage.test.tsx — ~332 lines of test churn across 4 PRs; suite is rewriting itself
- **New:** [1] churn-hotspot auth_policy.rs — 2 PRs / ~191 lines in 24h (strictest-across-orgs gate + dead-code removal)
- **New:** [2] `code-review-reality-server-imports-total-page-length` — vector=bug, high-confidence: `total = jobs.len() as i64` after LIMIT/OFFSET on both `list_import_jobs` (imports.rs:131) and `list_feeds` (imports.rs:322); fix shape is fully established (mirrors PR #919 for inquiries)
- **New:** [1] `code-review-reality-server-agency-import-history-no-pagination` — vector=bug: `agency_imports.rs:165-183` hard-codes `LIMIT 100`, no offset params, `total` clamped
- **Bumped:** none
- **Decayed:** none (every open item has been touched within the 14-day window)
- **Dropped:** none

## Plans promoted
- none this run (payload flagged `buffer-low: 6/72`; the fresh reality-server findings are the strongest landable candidates but the top-scored one is at score 2 — below the score≥3 promotion gate. Vector `bug` has no fast-track (that's security-only). They enter backlog and will promote on the first re-signal or a paired code-review-finding stacking. Adversarial pass: n/a — no plan written.)

## Auto-fix
- `no candidate` — no signal met the allowlist × confidence=high × score≥3 combination this run. The reality-server pagination findings are high-confidence and mechanically simple (mirror #919), but `code-review-finding` is not on the Phase-5 allowlist by design — those go through manual promotion.

## Self-review findings
- open=35 (high=8, medium=24, low=3); new this run=0; acknowledged=0
- fp-mcp-push-cannot-inline-large-archive [recurrence=18] — Phase 6 archive-move produced a 198KB assignments-archive.json; mcp__github__push cannot inline it; fix: PROVEN WORKAROUND — push dispatcher state commit to a NEW branch
- fp-mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=18] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked; fix: replace hard-coded proxy-403 assumption with runtime probe at Phase 1
- fp-tier1d-signals-accumulate-no-promotion-to-backlog [recurrence=11] — buffer drained (claimable=0/72); fix: add in-repo signals→backlog promoter OR force cross-branch promotion
- fp-stale-in-progress-action-list-decoupled-from-assignments [recurrence=6] — action-list.json holds 31 status=in-progress but assignments.json has fewer; fix: add Phase-2 reconcile step
- fp-remediation-fixes-trapped-in-unmergeable-self-pr [recurrence=3] — PR #2744 carries two recurring-defect fixes but stays in draft; fix: operator un-draft + merge
- … and 3 more (see findings.json)

## Open questions
- Should the routine's Phase-3 promotion gate carve out a "high-confidence bug + fix shape already merged elsewhere" fast-track (analogous to the security fast-track at score≥2), specifically for cases like the reality-server pagination pair where PR #919 is the proven template? The current gate leaves them at score 2 with no promotion path until another signal fires — a mismatch with the payload's `buffer-low` urgency.

## Quiet day?
- No — 11 merged PRs, 5 new backlog vectors, 2 landable Rust findings, 1 cumulative repeated-churn re-emit. `stats.quiet_days` NOT incremented.
