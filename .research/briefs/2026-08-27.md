# 2026-08-27

## Since last run
- Merged PRs: 17 (range #2848–#2864)
- Open PRs touched: 0 (no stalled open PRs in window; 3 human `feat(acc)` PRs #2555/#2558/#2559 remain in `needs-human-judgement` unchanged)
- New / updated issues: 6 (all closed follow-ups from the post-merge review flow: #2853/#2854/#2857/#2859/#2861/#2862)
- Commits: 58 on `dev` (17 real merges + ~41 dispatcher chore commits)
- Routine lag: 2d 12h since last run — **lag_warning fired** (>36h threshold); below the 3d `stale_routine_alert` bar (dispatcher still ticking every ~2h on the `.research/management/` side, but the top-level daily routine missed a beat)
- Phases that failed: none — Phase 1 fully ran (PR + issue + commit fetches via GitHub MCP; `gh` CLI blocked with 403 as expected in this session, MCP tools used instead); Phase 1.5 code-review already staged by dispatcher tier1d runs earlier today (6 files, 13 findings — consumed here); Phase 1.6 skipped this run to save budget (pm_cursor stays at index 4 → `pm-devops`, no coverage_cursor advance — G16 becomes a no-op per spec); Phase 3 promoted 1 landable plan; Phase 5 skipped (no allowlisted signal type met the certainty bar this run — none of the 14 new signals are `screen-map-drift`/`orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile drift, or `stalled-review`).

## Buffer-low context (fire payload)
- Payload: `buffer-low: claimable=6/72 — all remaining backlog is mobile-native/KMP-gated (unlandable in cloud); refill planner with landable vectors`.
- Before this run the open/ready set was `4 items, all mobile-native/KMP` (portfolio-analytics-caps-100, cancellation-swallowed, inquiries-response-contract [ready+plan], httpclient-no-timeout). Zero landable-in-cloud candidates for the dispatcher to claim.
- **Refill outcome:** 13 fresh `code-review-finding` signals ingested from the earlier tier1d review files (all landable — 2 `api-core`, 3 `ppt-web-core`, 1 `reality-server`, 7 `reality-web`); 12 land as new `open` rows at score 1–2 (below the score≥3 promotion gate — they need one more hit or a manual bump), and 1 lands at score 3 and gets promoted today (see *Plans promoted*). Two more items sit at score 2 with high confidence — one more medium+ signal each and they promote next run.
- **Net effect on the landable pool:** was 0 → now 1 ready + 12 open landable rows for the dispatcher to escalate. The mobile-native-gated cluster stays parked; nothing new added to it this run.

## Shipped
- #2848 `churn-hotspot-aml-dashboard-page` — extract AML dashboard sections + data hook.
- #2849 `code-review-ppt-web-ui-content-mod-overdue-noop` — make moderation overdue affordance filter the queue (resolves prior backlog row — already flipped to `done` before this run).
- #2850 `pm-qa-verification-badge-expiry-i18n-visual-diff` — snapshot VerificationBadge expiry copy across sk/cs/de/en (#2825).
- #2851 `pm-qa-voice-oauth-token-encryption-roundtrip-test` — unit + integration tests for centralized voice OAuth token encryption (#2838).
- #2852 `churn-hotspot AmlDashboardPage.test.tsx` — de-duplicate decision-flow test helpers.
- #2855 `gh-issue-2854` — replace DsaReportsPage `window.alert` with localized toast feedback.
- #2856 `gh-issue-2853` — make moderation overdue filter server-side (Closes #2853).
- #2858 `fix(api-server)` — enforce strictest email-verification policy across all grantee orgs in capability-grant gate.
- #2860 `gh-issue-2859` — DB test for overdue moderation filter + explicit list-truncation notice.
- #2863 `gh-issue-2862` — suppress overdue truncation notice at exact 200 boundary.
- #2864 `gh-issue-2861` — fix root-cause membership ordering non-determinism + remove dead capability-grant path + refresh stale doc.
- Plus 6 planner/QA follow-ups (#2839–#2847) already covered in the 2026-08-25 brief.

## Watch
- Stalled review: none NEW this run — 3 human `feat(acc)` PRs (#2555 / #2558 / #2559) remain in `needs-human-judgement` (all 30d+ idle now).
- Reverted: none.
- Churn hotspots: not recomputed this run (Phase 1 restricted to PR/issue/commit list to save budget; the dispatcher's tier1d files already carry a segment-specific view). `hotspot_history` not bumped this run.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none — one active row in `.research/management/assignments.json` (`gh-issue-2743`, PR #2744, status=review, last_updated 2026-08-27T18:29Z — well under the 24h threshold).

## Code review slice
- Segment reviewed: consumed 6 tier1d files staged earlier today by the dispatcher (`api-core`, `ppt-web-core`, `reality-server`, `reality-web` (x3 sub-slices — import, search, generic)). No fresh Phase-1.5 invocation this run.
- Experts: rotating-expert-review (source: `code-review-finding` type)
- Findings: 13 (all `code-review-finding` signals — see *Backlog deltas*)
- Next segment: oldest unreviewed in `state.review_cursor` is `reality-server` (2026-08-19); `api-core` (2026-08-21) next; `ppt-web-core` (2026-08-21) tied.
- Skipped: no

## Screen-map status
- Total `docs/screens/` files: 153 (ppt=106 + reality=47 + mobile=0 — mobile still not seeded)
- Drift signals this run: 0 — none of the 17 merged PRs touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**` (the merged PRs were api-server / features/compliance / DSA / voice / QA-test-only work).
- Orphan screens this run: 0 (no trigger-set route basenames changed)
- Last `screen-map.yml` CI run on `main`: not re-queried this run (workflow only fires on relevant paths).

## Backlog deltas
- **New:** [3] `code-review-ppt-web-core-personmonths-raw-fetch-noauth` — ppt-web person-months.tsx `useBuildingUnits()` bare `fetch()` skips `Authorization/Bearer` header — `/api/v1/units` call fails auth in non-cookie environments (see `plans/code-review-ppt-web-core-personmonths-raw-fetch-noauth.md`).
- **New:** [2] `code-review-api-core-fault-report-csv-injection` — CSV export in `reports/helpers.rs:188` interpolates category/status/priority without a CSV-injection sanitizer.
- **New:** [2] `code-review-ppt-web-core-financial-dash-no-error-state` — `financial.tsx:70-86` destructures only `isLoading`; API errors render blank/partial dashboard silently.
- **New:** [2] `code-review-reality-server-sso-rs-reqwest-no-timeout` — `sso.rs:687` `reqwest::Client::new()` has no `.timeout()`.
- **New:** [2] `code-review-reality-web-feedimport-create-no-error` — `FeedImport.tsx:480-489` awaits `createMutation.mutateAsync` then `onClose()` with no try/catch.
- **New:** [2] `code-review-reality-web-crmconn-create-no-error` — `CrmConnection.tsx:389-404` — same silent-failure anti-pattern as FeedImport.
- **New:** [2] `code-review-reality-web-listings-url-nan-query` — `listings/page.tsx:112-124` bare `Number()` on 5 range URL params — NaN poisons the query.
- **New:** [2] `code-review-reality-web-listings-no-error-state` — `listings/page.tsx:145` destructures only `data/isLoading` from `useListings`; errors render as empty grid.
- **New:** [1] `code-review-api-core-market-pricing-db-error-swallow` — `market_pricing.rs:459` `list_regions(...).unwrap_or_default()` hides transient DB error as empty list.
- **New:** [1] `code-review-ppt-web-core-orgprovider-unmemoized-value` — `OrganizationProvider.tsx:8-12` context value rebuilt on every parent render.
- **New:** [1] `code-review-reality-web-import-delete-toggle-unhandled` — `FeedImport.tsx:154-165` pause/delete `mutateAsync` unhandled.
- **New:** [1] `code-review-reality-web-listing-edit-slovak-i18n` — 930-line `[locale]/account/listings/[id]/edit/page.tsx` hardcodes Slovak, no next-intl import.
- **New:** [1] `code-review-reality-web-account-routes-i18n` — systemic i18n gap across signed-in account/realtor routes.
- **Bumped:** none this run.
- **Decayed:** none — no open item has `updated_at` older than 14 days (oldest `open` is `code-review-mobile-native-kmp-httpclient-no-timeout` at 2026-08-16, only 11 days ago).
- **Dropped:** none.

## Plans promoted
- `plans/code-review-ppt-web-core-personmonths-raw-fetch-noauth.md` — replace hand-rolled `fetch(/api/v1/units)` in `person-months.tsx` with the shared authed `client()` (or the generated `@ppt/api-client` units call) so `Authorization/Bearer` is attached like every other query on the page · adversarial pass: passed (Evidence → Hypothesis → Suggested approach → Test plan chain verified: the `fetch()` at `person-months.tsx:75` is the only bare-fetch call in the file; `lib/api.ts:189-204` demonstrably attaches Bearer; test in `person-months.spec.tsx` will fail on trunk today because the mock is not asserted on and the raw fetch bypasses SDK mocking).

## Auto-fix
- `no candidate` — none of the 14 new signals is on the Phase-5 allowlist (no `screen-map-drift/orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, or `stalled-review`). Score bar (≥3) + confidence=high was met by 0 of the ingested tier1d signals (all medium confidence except two `reality-web` i18n items whose score is 1). Kill switch not set.

## Self-review findings
- none (no `.research/self-improvement/findings.json` change surfaced this run).

## Open questions
- Nothing new needing human judgement. The 3 accounting-domain stalled PRs (#2555/#2558/#2559) remain the standing item.

## Quiet day?
- No — 13 landable-in-cloud vectors added, 1 plan promoted; buffer situation the caller flagged is now materially improved.
