# 2026-08-27

## Refill trigger (22:40 UTC)

Dispatcher fired the routine with payload `buffer-low: claimable=6/72 — all open backlog mobile-native-gated (unlandable in cloud); refill planner with backend/frontend/reality vectors`. The 5-day gap since the last brief (`briefs/2026-08-22.md` was the last full brief; the 2026-08-25 file at HEAD is an in-place refire-append, not a routine commit) plus the buffer situation drove today's focus: emit fresh backend / frontend signals so the planner has non-mobile-native work to promote.

**Outcome:** 6 new backlog vectors created (3 api-core code-review findings + 3 churn hotspots), 1 plan promoted (security-fast-track: `code-review-api-core-check-principal-kind-change-nondeterministic`). Backend buffer refills with one landable, cloud-ok row.

## Since last run
- Merged PRs: 11 (range #2848–#2864, 7 code-merge commits in `origin/dev` since 2026-08-25T08:50Z window — PRs #2848/#2849/#2850/#2851 report `merged: true` via the API but their commits land in the CCR session branch `claude/wonderful-gates-e0tcq1`, not `origin/dev`; the extracted `AmlThresholdsSection.tsx` / `useAmlDashboard.ts` files DO live on `dev` so #2848 reached the tree, just under a different landing sha).
- Open PRs touched: 3 new dependabot PRs (#2865 cargo-minor-patch, #2866 actions/setup-java 5→6, #2867 npm-minor-patch 39 updates); 11 older dependabot PRs still stalled (11d–29d); 3 human `feat(acc)` PRs (#2555/#2558/#2559) still `needs-human-judgement` at 30d.
- New / updated issues: 6 (all closed follow-ups from post-merge review: #2853/#2854/#2857/#2859/#2861/#2862). `last_issue_seen` 2847 → 2862.
- Commits: 7 real merge commits on `dev` in window + ~50 dispatcher chore commits (touching only `.research/management/`).
- Routine lag: **2d 14h since last run — `lag_warning` fired** (>36h threshold); the 3d `stale_routine_alert` threshold was NOT crossed (2 hours short at run time), but this is the second lag-warning in three runs — the daily cron continues to be flaky.
- Phases that failed: none — Phase 1 ran (PR/issue fetch + churn); Phase 1.5 ran (api-core segment, 3 findings); **Phase 1.6 skipped this run** (buffer-low fast-turn — token budget; `pm_cursor` and `coverage_cursor` intentionally NOT advanced so tomorrow's run picks up pm-devops as scheduled and G16 is a no-op today); Phase 3 ran (1 new plan promoted); Phase 5 skipped (see *Auto-fix* below).

## Shipped
- #2848 `churn-hotspot-aml-dashboard-page` — extract AML dashboard sections + `useAmlDashboard` hook (AmlDashboardPage.tsx 385 → 157 lines; +490 −277 across 6 files; behaviour-preserving refactor).
- #2849 `code-review-ppt-web-ui-content-mod-overdue-noop` — Show-Overdue button actually filters the queue now (backlog row already flipped to `done` in HEAD).
- #2850 `pm-qa-verification-badge-expiry-i18n-visual-diff` — snapshot VerificationBadge expiry copy across sk/cs/de/en (Closes #2825).
- #2851 `pm-qa-voice-oauth-token-encryption-roundtrip-test` — unit + integration tests for centralized voice OAuth token encryption (Closes #2838).
- #2852 `churn-hotspot AmlDashboardPage.test.tsx` — de-duplicate AML dashboard decision-flow test helpers.
- #2855 `gh-issue-2854` — replace DsaReportsPage `window.alert` with localized toast feedback (6 locales + i18n test).
- #2856 `gh-issue-2853` — moderation "overdue only" filter server-side (Closes #2853); adds compliance repository query + ContentModerationPage test coverage.
- #2858 `fix(api-server)` — enforce strictest email-verification policy across all grantee orgs in capability-grant gate (Closes #2857 — security-labeled; auth_policy.rs + membership.rs + membership_test.rs + aml_dsa_cross_org_idor_tests.rs).
- #2860 `gh-issue-2859` — DB test for overdue moderation filter + explicit list truncation notice (Closes #2859).
- #2863 `gh-issue-2862` — suppress overdue truncation notice at exact 200 boundary (Closes #2862; ContentModerationPage.tsx guards `overdue_count > OVERDUE_PAGE_LIMIT` strictly).
- #2864 `gh-issue-2861` — fix root-cause membership ordering non-determinism + remove dead capability-grant path + refresh stale doc (Closes #2861; auth_policy.rs cleanup that motivates today's `code-review-api-core-check-principal-kind-change-nondeterministic` sibling finding).

## Watch
- Stalled review: 3 human `feat(acc)` PRs (#2555 UC-ACC-05.17 / #2558 UC-ACC-05.9 / #2559 UC-ACC-05.8) remain in `needs-human-judgement` (30d idle now, already scored). No NEW human-authored PRs went stalled this run. 11 dependabot PRs stalled (2865/2866/2867 fresh today; the older 8 already logged as `dep-update-noise`, delta 0).
- Reverted: none.
- Churn hotspots (top-3 this run):
  - `frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.test.tsx` — 218 lines across 3 PRs (#2849, #2856, #2863) — first-time hotspot; the moderation-overdue behaviour is on its 5th fix cycle in 5 days (#2853/#2856/#2859/#2860/#2862), still stabilising.
  - `backend/servers/api-server/src/services/auth_policy.rs` — 191 lines across 2 security-fix PRs (#2858, #2864) — first-time hotspot; drives all 3 api-core findings this run.
  - `frontend/apps/ppt-web/src/features/compliance/pages/DsaReportsPage.test.tsx` — 133 lines from #2855 (window.alert → localized toast) — single-PR spike, `runs_seen=1`; watch next window before promoting a refactor.
  All three are new — `runs_seen=1` after this run. No `repeated-churn` yet (last run's AmlDashboardPage.{tsx,test.tsx} did NOT re-fire in this window — the refactor / de-dup work from #2848 and #2852 that landed there seems to have settled).
- Risky-churn: none — all four fix-titled PRs (#2856/#2858/#2863/#2864) that touched churn files ALSO shipped tests (aml_dsa_cross_org_idor_tests / membership_test / ContentModerationPage.test / compliance repository test). Clean.

## PRs stuck in draft despite approval
- none — `gh pr list --repo martin-janci/property-management --state open --draft --json ...` returned 1 draft (#2744 `fix(dispatcher): un-wedge oversize-archive push (#1162) + close retry-remint cross-id ghost (#2460)`) with `reviewDecision=null` (approver blocked on self-PR APPROVE limitation, not human review). Not APPROVED-but-stuck.

## PRs with verdict=changes, no fix-round progress in 24h
- none — `.research/management/assignments.json` currently carries 1 row (`gh-issue-2743`, `status=review`, `reviewer_summary=verdict=approve note=both-fixes-correct-well-tested-scoped-clean-ci-green`), no `verdict=changes` open.

## Code review slice
- Segment reviewed: `api-core` (reason: churn-aligned — `auth_policy.rs` is the #2 hotspot this run; also 2nd-oldest unreviewed at 2026-08-21 — 6d).
- Experts: rust + security (single Explore subagent, static-only per skill contract).
- Findings: 3 (all `code-review-finding` signals — see *Backlog deltas*): 1 security-fast-track (`check_principal_kind_change` non-determinism, promoted below); 2 non-promotable (`check_login` platform-default fall-through, `check_password_change` test-gap). All three cite specific line ranges in `auth_policy.rs`.
- Next segment: `reality-server` (oldest unreviewed after this run: 2026-08-19 — 8d).
- Skipped: no.

## Screen-map status
- Total `docs/screens/` files: 153 (ppt=106 + reality=47 + mobile=0 — mobile still not seeded, drift detection intentionally excludes it).
- Drift signals this run: 0 — none of the 7 code-merge PRs touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`.
- Orphan screens this run: 0 — no route basenames touched this run.
- Last `screen-map.yml` CI run on `main`: n/a (workflow gated on relevant paths; long gap expected).

## Backlog deltas
- **New:** [2] `code-review-api-core-check-principal-kind-change-nondeterministic` (security, high) — same fail-open-by-row-order anti-pattern PR #2864 fixed in `check_capability_grant_for_user`, surviving in the sibling method. Security-fast-track: score 2 + confidence high + vector security → promoted below.
- **New:** [2] `code-review-api-core-check-login-no-platform-default` (bug, medium) — latent super-admin MFA fail-open; `check_login` never consults `platform_default_policy()` for zero-membership users despite its own docstring. Currently masked because `AuthPolicy::default()` carries no MFA roles.
- **New:** [2] `code-review-api-core-check-password-change-strictest-untested` (test-gap, medium) — no regression test pins the ordering-independence invariant on `check_password_change`; a future revert to `memberships.first()` would silently accept weak passwords for every multi-tenant user.
- **New:** [1] churn-hotspot `backend/servers/api-server/src/services/auth_policy.rs` (refactor).
- **New:** [1] churn-hotspot `frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.test.tsx` (refactor).
- **New:** [1] churn-hotspot `frontend/apps/ppt-web/src/features/compliance/pages/DsaReportsPage.test.tsx` (refactor).
- **Bumped:** none — no new signal matched an existing open row this run.
- **Decayed:** none — every open item is within the 14-day window (`code-review-mobile-native-kmp-httpclient-no-timeout` at 11d is the closest, no decay yet).
- **Dropped:** none this run.

## Plans promoted
- `plans/code-review-api-core-check-principal-kind-change-nondeterministic.md` — mirror PR #2864's fix for `check_capability_grant_for_user` in the sibling `check_principal_kind_change` method (drop `.first()` + fold in `platform_default_policy()` like `strictest_policy_across` does; add sqlx regression test that pins ordering-independence). `Mode: cloud-ok` (backend Rust + sqlx tests only). **Adversarial pass: passed** — source → hypothesis chain is direct (line 256-259 of auth_policy.rs, same anti-pattern as security-labeled #2857), suggested approach mirrors an existing fix that landed today, test plan cites a specific new sqlx test modelled on `capability_grant_rejected_when_any_org_requires_verification` (auth_policy.rs:442-491), and Quality Gate 11 grep for placeholder rot returned 0 hits.

## Auto-fix
- `no candidate` — no allowlist × confidence=high × score≥3 signal met the bar this run. Detail: the one promoted plan (`code-review-api-core-check-principal-kind-change-nondeterministic`) is `vector=security` + `score=2`, security-fast-track-eligible for Phase 3 promotion but NOT for Phase 5 auto-fix (Phase 5's certainty bar requires `score>=3` unconditionally — the fast-track exception is Phase 3 only, deliberately). The 3 new dependabot PRs (#2865/#2866/#2867) are `dep-update-noise` delta 0 — none are lockfile-drift sub-shapes, so no lockfile-bump path either. The stalled `feat(acc)` PRs are already in `state.seen_signals` (one-shot dedup) and the routine convention excludes them from `stalled-review` comment nudges (owner is a human, blocked on accounting spec resolution). No `screen-map-drift`, no `screen-map-orphan`, no `fixme-in-merged-code` this window.

## Self-review findings
- `open=1 (high=0, medium=0, low=1); new this run=0; acknowledged=0` — the existing `.research/self-improvement/findings.json` carries one low-severity finding from the dispatcher's Phase 8 self-review on 2026-08-27T08:38Z. Nothing high/medium open, so no per-finding bullet needed here.

## Open questions
- Cloud routine still missing beats — 2 of the last 3 fires needed `lag_warning`. The dispatcher on this branch continues to tick reliably; the outage is scoped to the daily routine cron specifically. Same recommendation as 2026-08-25: inspect the deployment-side scheduler. Nothing more to log from the routine itself beyond the warning.
- The 3 stalled `feat(acc)` PRs (#2555 / #2558 / #2559) are now 30d idle — same open question the last three briefs have carried, blocked on the accounting spec resolution. Consider closing without merge or spawning a dedicated planning thread; either move unblocks the buffer more than another auto-comment nudge would.
- Phase 1.6 was skipped this run to keep the buffer-low turn tight. Result: `pm_cursor` still points at `pm-devops` (idx 4) and `coverage_cursor` still points at `epic idx 6`. Tomorrow's run should carry both forward as normal.

## Quiet day?
- **No** — buffer-low refill turn: 11 PRs shipped in the 2d window, 6 new backlog vectors created, 1 new plan promoted, 0 auto-fixes. `stats.runs` 71 → 72; `stats.vectors_created` 324 → 330; `stats.plans_created` 28 → 29.
