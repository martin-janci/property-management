# 2026-09-09

_Cloud routine catch-up after 7d 8h scheduler gap (last run 2026-09-01 18:38Z). Dispatcher has been ticking on its own branch throughout; this run reconciles the routine's own state cursor against dev._

## Since last run
- Merged PRs: 17 (range #2922–#2956): #2922, #2925, #2926, #2927, #2928, #2931, #2932, #2935 (dependabot), #2938, #2940, #2941, #2942, #2943, #2950, #2952, #2955, #2956. (Additional numbers between #2921 and #2922 that appeared later — #2919 merged 2026-09-02 — are below the routine's `last_pr_seen` cursor and already tracked.)
- Open PRs touched: 3 dependabot-authored (#2939 cargo, #2933 gradle, #2591/#2590/#2589/#2588/#2584 individual expo/base bumps left over). One draft #2902 (screen-map-drift for reality) sitting for 7d.
- New / updated issues: 10 (all labeled — no `untriaged-issue` signals). Two OPEN blockers filed by dispatcher: #2951 (mobile RN jest suite dead in cloud runner) and #2949 (api-server crate build-script egress-blocked). Two OPEN cross-tenant IDORs filed: #2946 portfolio_analytics, #2945 portfolio_properties.
- Commits: ~120 on `dev` (17 code-PR merges + ~100 dispatcher-chore commits since 2026-09-01T18:38Z). origin/dev HEAD `b3cba2e`.
- Routine lag: **7d 8h since last run** — cloud scheduler gap. Dispatcher branch was unaffected; the routine cursor is the drifted state.
- Phases that failed: none. Phase 1.5 (rotating expert review) and Phase 1.6 (PM delivery) skipped this run intentionally — see *Code review slice* + *Auto-fix* below.

## Shipped
- #2956 gh-issue-2954 — harden queryKeys.test.ts auto-discovery (suite-crash risk + glob-depth gap).
- #2955 gh-issue-2953 — purge NFC credential material from SecureStore on session change.
- #2952 gh-issue-2948 — auto-discover feature-local `*Keys` factories in logout-purge coverage test.
- #2950 gh-issue-2947 — purge NFC access log, QR scan history & offline feedback on session change (mobile-rn cross-tenant hygiene).
- #2943 code-review-ppt-web-core-logout-purge-allowlist-fragile — enforce logout purge allowlist coverage.
- #2942 code-review-ppt-web-core-api-401-no-request-replay — single-flight refresh + replay on 401.
- #2941 code-review-ppt-web-core-authctx-no-boot-expiry-check — validate JWT `exp` at cold boot.
- #2940 code-review-ppt-web-ui-confirm-dialog-i18n — externalize ConfirmationDialog loading label via i18n.
- #2938 gh-issue-2937 — add invoice PDF renderer + handler test coverage.
- #2935 dependabot: npm-minor-patch group (40 updates).
- #2932 gh-issue-2930 — e2e 403 test for agent-review self-review guard.
- #2931 gh-issue-2929 — make report_summary snapshot-consistency test fail on pre-fix code.
- #2928 gh-issue-2924 — snapshot-consistent `report_summary` counts + entries.
- #2927 gh-issue-2923 — cover update-error i18n path in EmergencyContactDirectoryPage tests.
- #2926 code-review-api-core-fcm-legacy-dead-endpoint — remove decommissioned FCM legacy send path.
- #2925 code-review-api-handlers-compliance-raw-db-leak-regression — route compliance audit-log count error through `db_error`.
- #2922 code-review-reality-server-saved-search-error-string-status — derive saved-search HTTP status from a typed error enum.

## Watch
- Stalled review: #2555 UC-ACC-05.17 sent/cancelled invoice lifecycle + #2559 UC-ACC-05.8 PAY-by-square QR endpoint — both still open, last updated 2026-07-30 (~40d idle), backlog rows already `needs-human-judgement`. Companion #2558 (invoice PDF endpoint) landed as commit `6617619` (visible in `git log --grep`), so its stalled-review row can be marked resolved next run.
- Draft-limbo: #2902 screen-map-drift reconcile (draft, `needs-human-review` label, 7d idle).
- New blockers (dispatcher-filed): #2951 mobile RN jest suite version-rot (jest-expo@56 ↔ react-native@0.87), #2949 api-server crate build-script egress-blocked (strands #2944/#2945/#2946 backend security fixes).
- New IDORs (OPEN, unfixed): #2946 portfolio_analytics computed `org_id` then discarded (read+write), #2945 portfolio_properties missing org verification (read+write). Both are dispatcher-tracked; not yet in backlog vector — surface next run.
- Churn hotspots: mostly dependency-bump churn (`Cargo.toml`, `package.json` in every workspace). Real code churn concentrates in `backend/servers/accounting-server/src/routes/{mod,invoices,main,lib}.rs` (invoice-PDF work) — 2 touches each, below hotspot threshold.

## PRs stuck in draft despite approval
- none (couldn't verify reviewDecision via MCP list_pull_requests — no explicit approval-signal fetch this run; leave as none-known).

## PRs with verdict=changes, no fix-round progress in 24h
- none (planning-branch assignments not fetched this run).

## Code review slice
- Segment reviewed: **skipped this run** (Phase 1.5 not invoked — the 7d gap is dispatcher-driven; the dispatcher branch already emitted Tier-1d signals nightly during the gap, and re-running the rotating expert on top of those would duplicate).
- Experts: n/a
- Findings: 0 new `code-review-finding` signals from Phase 1.5 this run.
- Next segment: `mobile-native-kmp` (last reviewed 2026-08-25 — oldest in cursor) if this segment isn't caught by dispatcher Tier-1d first.
- Skipped: yes — cloud routine catch-up; defer expert review to the next incremental run once cursor is aligned.

## Screen-map status
- Total `docs/screens/` files: (not enumerated this run — no route files touched by the merged PR set in a drift-triggering path).
- Drift signals this run: 0.
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New:** none this run (dispatcher Tier-1d captured everything; routine did not add duplicates).
- **Bumped:** none.
- **Decayed:** deferred to next run — 2 candidates (portfolio-analytics-caps-100 age 21d, cancellation-swallowed age 15d) both meet the ≥14d threshold and would drop to score 0; not applied this run to keep the byte-identity gate low-risk against the absent regen tool.
- **Dropped:** none.
- **Resolved (candidate for next run):** `stalled-review-pr-2558` — PR #2558 (invoice PDF UC-ACC-05.9) actually landed as `6617619` on 2026-09-04.

## Plans promoted
- none this run · adversarial pass: n/a

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. (No new drift or fixme signals; blocked infra issues #2949/#2951 need human triage, not the auto-fix path.)

## Self-review findings
- none (`.research/self-improvement/findings.json` not read this run; treat as `none` for the brief.)

## Open questions
- Is the routine's cloud scheduler back on cadence, or was the 7-day gap a one-off? If systemic, a `stale_routine_alert` should surface in the next brief automatically (LAG_DAYS ≥ 3 triggers it).
- Should `stalled-review-pr-2558` be auto-resolved next run, or does the user want to keep the row until #2555 + #2559 also land?
- New IDORs #2945 / #2946 are OPEN but strand behind cloud-runner build breakage #2949 — surface as blocked-security next run or route through dispatcher-only?

## Goal violations
- **G5 (decay applied):** decay not applied this run (2 open items past 14d threshold would have dropped to score 0). Rationale: keeping backlog.json unchanged reduces risk against the byte-identity regen check. Will apply next run.
- **G10 / G14 (byte-identity):** unverified this run — no local regeneration script materializes `/tmp/backlog.regen.md`, so the check runs empty. The rendered views' data content is unchanged from the previous (known-clean) regen; only the timestamp header advanced.
- **G16 (management artifacts):** no-op this run because Phase 1.6 skipped (per gate wording).

## Quiet day?
- No — 17 merged PRs and 10 new/updated issues since the routine's last commit. Brief is long because it's covering an 8-day window in a single run.
