# 2026-09-09

_Catch-up run. Routine daily cron appears wedged — no brief since 2026-09-01 (~187h / 7d 19h gap). The dispatcher on `dev` kept ticking in parallel; this brief is a convergent snapshot of the intervening week._

## Since last run
- **Merged PRs:** 17 (#2922, #2925, #2926, #2927, #2928, #2931, #2932, #2935, #2938, #2940, #2941, #2942, #2943, #2950, #2952, #2955, #2956) — range #2922–#2956 with 12 non-consecutive gaps taken up by drafts / dispatcher-chore commits.
- **Open PRs touched:** 12 (mostly dependabot; two long-stalled UC-ACC PRs #2555 / #2559 unchanged).
- **New / updated issues:** 15 since 2026-09-01. 4 currently open: **#2945** (security), **#2946** (security), **#2949** (infra-blocked), **#2951** (infra-blocked). #2944 (closed, source of the #2945/#2946 IDOR strand) recorded for context.
- **Commits:** 100 on `dev` (17 code PRs + 5 dependabot merges + ~78 dispatcher-chore commits). origin/dev HEAD `e5167b2`.
- **Routine lag:** ~7d 19h — the `lag_warning` gate fired. The daily research cron/trigger is silently skipping; operator to investigate. Dispatcher state lives on the planning branch and is a separate loop; it kept advancing (assignments.json shows 2 review-status rows on 2026-09-09).
- **Phases that failed:** none; Phase 1.5 (rotating expert review) and Phase 1.6 (PM rotation) were **intentionally skipped** for this catch-up run — the review-cursor was already refreshed by 8 dispatcher Tier-1d generator files landed 2026-09-05..2026-09-09 covering api-core, api-handlers, mobile-rn, ppt-web-core (×3), ppt-web-ui, reality-server, reality-web. Regular rotation resumes next run.
- **Trigger:** `buffer-low: claimable=8/72 — refill planner (pipeline structurally blocked on #2652/#2949 operator infra)` — payload recorded but this run is a routine catch-up brief, not a targeted refill. The buffer floor is structural: mobile-native/KMP is cloud-unlandable (#2652) and api-server crate cloud build is blocked (#2949), so most in-flight vectors can't be claimed by cloud implementers regardless of backlog size.

## Shipped
- **#2922** code-review-reality-server-saved-search-error-string-status — derive saved-search HTTP status from a typed error enum (replaces string-comparison classifier).
- **#2925** code-review-api-handlers-compliance-raw-db-leak-regression — routes compliance audit-log count error through `db_error` (fixes 1-site regression from PR #2915 flagged by 2026-09-01 brief).
- **#2926** code-review-api-core-fcm-legacy-dead-endpoint — removes decommissioned FCM legacy send path (`send_fcm_legacy` was dead code — Google-decommissioned URL).
- **#2927** gh-issue-2923 — covers update-error i18n path in `EmergencyContactDirectoryPage` tests.
- **#2928** gh-issue-2924 — snapshot-consistent `report_summary` counts + entries (data_export repo).
- **#2931** gh-issue-2929 — makes the `report_summary` snapshot-consistency test fail on pre-fix code (IG3 hardening for #2928).
- **#2932** gh-issue-2930 — adds e2e 403 test for agent-review self-review guard.
- **#2935** dependabot: 40 npm-minor-patch updates.
- **#2938** gh-issue-2937 — adds invoice PDF renderer + handler test coverage (fills the gap PR #2558 shipped without).
- **#2940** code-review-ppt-web-ui-confirm-dialog-i18n — externalizes `ConfirmationDialog` loading label via i18n.
- **#2941** code-review-ppt-web-core-authctx-no-boot-expiry-check — validates JWT `exp` at cold boot (touches AuthContext.tsx).
- **#2942** code-review-ppt-web-core-api-401-no-request-replay — single-flight refresh + replay on 401 (touches AuthContext.tsx + lib/api.ts).
- **#2943** code-review-ppt-web-core-logout-purge-allowlist-fragile — enforces logout purge allowlist coverage (adds queryKeys.test.ts auto-discovery).
- **#2950** gh-issue-2947 — mobile: purge NFC access log, QR scan history & offline feedback on session change (touches resetLocalData.ts).
- **#2952** gh-issue-2948 — auto-discover feature-local `*Keys` factories in logout-purge coverage test (touches queryKeys.test.ts).
- **#2955** gh-issue-2953 — mobile: purge NFC credential material from SecureStore on session change (touches resetLocalData.ts).
- **#2956** gh-issue-2954 — hardens `queryKeys.test.ts` auto-discovery (suite-crash risk + glob-depth gap).

## Watch
- **Stalled reviews (no reviewDecision, >7d):**
  - #2555 (feat(acc): wire sent/cancelled invoice lifecycle UC-ACC-05.17) — 41d, `martin-janci`. Standing UC-ACC-05.x block.
  - #2559 (feat(acc): PAY by square payment QR endpoint UC-ACC-05.8) — 41d, `martin-janci`. Same UC-ACC-05.x block.
  - #2866 (chore(deps): bump actions/setup-java from 5 to 6) — 13d. Trivial CI action bump; safe to review or auto-merge.
- **Reverted:** none this window.
- **Churn hotspots (top-3 raw, exclusions applied):**
  - `frontend/apps/ppt-web/src/lib/queryKeys.test.ts` — 3 commits (PRs #2943, #2952, #2956). Test-infra hardening iteration; first-time hotspot.
  - `frontend/apps/ppt-web/src/contexts/AuthContext.tsx` — 2 commits (PRs #2941, #2942). Auth surface being tightened.
  - `frontend/apps/mobile/src/services/resetLocalData.ts` — 2 commits (PRs #2950, #2955). Cross-tenant purge surface — the two mobile PRs closed the NFC access-log + credential-material gaps flagged by Tier-1d.
- **Open security issues:**
  - **#2945** Cross-tenant IDOR (read+write): `portfolio_properties` handlers missing org verification.
  - **#2946** Cross-tenant IDOR (read+write): `portfolio_analytics` property metrics computed org_id then discarded.
  Both labeled `bug,security` — sibling of the closed #2944; strand is real and awaiting a plan.
- **Infra blockers (both open, labeled `blocked`):**
  - **#2949** api-server crate cannot build in cloud runner — `utoipa-swagger-ui` build-script egress-blocked; strands cloud fixes for #2944/#2945/#2946.
  - **#2951** mobile (React Native) jest suite cannot run in cloud runner — `jest-expo@56` ↔ `react-native@0.87` version rot.

## PRs stuck in draft despite approval
<!-- Query PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     Compute age-in-hours from updatedAt. If none: emit a single line "- none". -->
- #2744 fix(dispatcher): un-wedge oversize-archive push — draft, no reviewDecision, last update 2026-08-13 (27d ago). Persistent self-approval blocker; unchanged from prior briefs.
- #2902 screen-map-drift-pr-2894-reality — draft, last update 2026-09-03 (6d ago). Branch remains contaminated per prior brief.
- No PRs with `reviewDecision=APPROVED` AND `isDraft=true` this window.

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Read planning branch's assignments.json for status==review AND
     reviewer_summary startsWith "verdict=changes" AND (now - last_updated) > 24h. -->
- Assignments queried from local .research/management/assignments.json (not planning branch — cloud sandbox scope); 2 rows both `status=review`, neither with `verdict=changes` in reviewer_summary. If the planning-branch view differs, surface that next run.
- none this window.

## Code review slice
- **Segment reviewed:** none this run (Phase 1.5 skipped — catch-up).
- **Recent coverage (dispatcher Tier-1d generator, 2026-09-05..09):** api-core, api-handlers, mobile-rn, ppt-web-core (×3), ppt-web-ui, reality-server, reality-web — 8 segments covered in 5 days, all `review_cursor` entries updated except `mobile-native-kmp` (parked on #2652).
- **Findings:** N/A this run (see intra-week Tier-1d signal files under `.research/signals/2026-09-0*-*-tier1d.json`).
- **Next segment:** `mobile-native-kmp` (cursor 2026-08-25, oldest) — deferred, #2652 AGP egress still parks the cloud implementer.
- **Skipped:** yes — catch-up run adopted the intervening dispatcher Tier-1d output rather than spawning a fresh review pass.

## Screen-map status
- **Total `docs/screens/` files:** unchanged from 2026-09-01 count (ppt/ + reality/; mobile/ still 0).
- **Drift signals this run:** 0 — of the 17 merged PRs, none touch `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`. All ppt-web merges stayed inside `contexts/`, `lib/`, `components/`, `features/` — non-route surfaces. Mobile-RN PRs stayed inside `src/services/` — also excluded.
- **Orphan screens this run:** 0.
- **Last `screen-map.yml` CI run on `main`:** not re-fetched this run (catch-up mode).

## Backlog deltas
- **New (this run):**
  - [1] `churn-hotspot-ppt-web-queryKeys-test` (refactor, medium) — 3 commits this window; watch for stabilisation after #2956.
  - [1] `churn-hotspot-ppt-web-AuthContext` (refactor, medium) — 2 commits this window; auth flow tightening.
  - [1] `churn-hotspot-mobile-resetLocalData` (refactor, medium) — 2 commits this window; cross-tenant purge surface.
- **Bumped:** none — the merged PRs all resolved existing rows or created new ones outside the score-stacking path.
- **Decayed (open items with updated_at older than 14 days, score −1 → dropped at 0):**
  - `code-review-mobile-native-kmp-portfolio-analytics-caps-100` — 21d idle, 1 → 0 → dropped.
  - `code-review-mobile-native-kmp-cancellation-swallowed` — 15d idle, 1 → 0 → dropped.
  - Both were low-score KMP findings blocked by #2652 anyway; retry-remint from archive path preserved.
- **Resolved (from merged PRs #2922–#2956):** the ppt-web-core (auth), ppt-web-ui (i18n), api-handlers (compliance), and api-core (FCM/saved-search) code-review vectors were already flipped to `done`/`dropped` in `backlog.json` by intra-week dispatcher runs; `backlog.md` was stale relative to `backlog.json` (6 rows + 1 extra JSON row not rendered). This run's regen brings the two into byte-alignment (G10 now passes).

## Plans promoted
- **0 new plans this run.** Only `score >= 3 && confidence == high && status == open && !plan` candidate is `code-review-mobile-native-kmp-create-listing-not-wired` (score 4, high) — mobile-native/KMP remains cloud-unlandable (#2652). The companion `code-review-mobile-native-kmp-inquiries-response-contract` plan is already `ready` on the same parked pile. Promoting a second unlandable plan won't refill the dispatcher's *landable* buffer.
- **Structural note (echoes 2026-09-01):** the buffer stays 8/72 because #2652 (KMP AGP egress) and #2949 (api-server crate cloud build) block the two largest slice of in-flight work. Landable ppt-web/backend items are converting through fast (17 merges since 2026-09-01). The chronic buffer floor is operator-infra-shaped, not signal-supply-shaped.

## Auto-fix
- `no candidate` — no signal met `confidence == high` + `score >= 3` + allowlist match. The 3 new churn-hotspot signals are high-confidence but scored only 1; the stalled-review signals are medium-confidence; #2652/#2949 infra blockers are outside the auto-fix allowlist by design. Kill switch `ROUTINE_AUTOFIX_DISABLED` not set.

## Self-review findings
- none (`.research/self-improvement/findings.json` absent — matches prior briefs).

## Open questions
- **Routine cron wedge — why 7d silence?** state.json advanced only via dispatcher's cadence (dispatcher writes its own state); the *research* routine did not run 2026-09-02..2026-09-08. Trigger source needs an operator check — cloud cron / research-land.yml replay may be stuck. This brief is being written from a dispatcher-scheduled catch-up fire, not the daily trigger.
- **#2945 / #2946 cross-tenant IDOR strand — needs plan.** #2944 was fixed but #2945 (`portfolio_properties`) and #2946 (`portfolio_analytics`) are still open with the same shape. High-confidence, security fast-track eligible (score would rise to 2 with a Tier-1d follow-up), but both are strandwise-blocked by **#2949** (api-server can't build in cloud runner). Local-only plan or wait for operator to unblock #2949.
- **#2652 AGP egress blocker — 40+d cycles.** Dispatcher has been flagging `GC3-buffer-bounds=FAIL` since 2026-08-31. Two open KMP items (`create-listing-not-wired` score 4; `httpclient-no-timeout` score 1) plus 1 ready (`inquiries-response-contract` score 3) all parked. Landable rate through cloud runner remained healthy this week (17 code merges) — so no structural change to prescribe, but the KMP pile grows every rotation.

## Quiet day?
- **No** — 17 merged PRs (largest weekly haul in recent memory), 3 new backlog vectors, 2 decayed→dropped, `backlog.md` re-aligned with `backlog.json` (11-line delta). The daily brief was silent but the codebase moved.

## Goal violations
- none — G1–G17 checked in Phase 6, all passing (see `signals/2026-09-09.json.goal_checks`).
