# 2026-09-09

_Catch-up run: routine had not fired since 2026-09-01 18:38 UTC (7d 22h lag). Dispatcher (separate loop, planning branch) kept ticking over the window — merged 19 PRs — so this is a resolution + hotspot + open-follow-up sweep, not a fresh backlog cycle._

## Since last run
- Merged PRs: **19** (range #2922–#2956, plus 5 back-fill merges of older PRs #2558/#2583/#2585/#2586/#2673 that landed in the window)
- Open PRs touched: 5 (all dependabot except #2902 draft screen-map-drift, #2744 dispatcher fix, #2559/#2558/#2555 acc-05 human-judgement pile — unchanged from 2026-09-01)
- New / updated issues: 15 (6 open: #2951, #2949, #2946, #2945, plus 2 previously flagged)
- Commits: **50** on `dev` (~19 code PRs + ~31 dispatcher chore commits)
- Routine lag: **7d 22h** since last routine commit (previous 2026-09-01T18:38:58Z; this run 2026-09-09T04:45Z)
- Phases that failed: none (Phase 1.5 rotating review + Phase 1.6 PM analysis both **skipped** — see *Skipped this run* below)
- Trigger: buffer-low payload (`claimable=9/72 — all landable work blocked on infra (#2652 mobile-native, #2949 api-server); refill coverage with cloud-landable frontend/reality-web stories`) — dispatcher context, addressed via *Buffer-refill posture* below

## Shipped
Grouping by cluster; full titles in git log:
- **Security hardening (auth + session purge, 6 PRs):** #2941 authctx boot expiry (cold-boot JWT exp validation), #2942 401 single-flight refresh + replay, #2943 logout purge allowlist enforcement, #2950 mobile session-purge fanout (NFC access log + QR scan history + offline feedback, closes #2947), #2955 SecureStore NFC credential purge (closes #2953), #2925 compliance raw-db-error-leak regression (closes 2026-09-01 regression).
- **Reality-server correctness (3 PRs):** #2919 agent-review self-review guard, #2932 e2e 403 regression test (closes #2930), #2922 saved-search typed error → HTTP status.
- **Report / audit / accounting (4 PRs):** #2916 report_summary stub → live (already shipped 2026-09-01), #2928 snapshot-consistent report_summary (closes #2924), #2931 snapshot-consistency test failing on pre-fix code (closes #2929), #2938 invoice PDF renderer test coverage (closes #2937), #2558 UC-ACC-05.9 invoice PDF endpoint (the long-stalled feature merged this window).
- **Frontend i18n / tests (2 PRs):** #2940 ConfirmationDialog i18n externalization, #2952 auto-discover feature-local `*Keys` factories in logout-purge coverage (closes #2948), #2956 harden `queryKeys.test.ts` auto-discovery (closes #2954).
- **Dead-code cleanup (1 PR):** #2926 remove decommissioned FCM legacy send path.
- **Dependabot rollups (5 PRs):** #2583 rust_xlsxwriter, #2585 base64, #2586 validator, #2673 ktor group, #2935 npm-minor-patch (40 updates), #2934/#2936 cargo-minor-patch groups (12 + 13 updates).

## Watch
- **Stalled review:** unchanged from 2026-09-01 — #2555 / #2559 UC-ACC-05.x still `needs-human-judgement`, 36+ days idle. #2558 (UC-ACC-05.9 invoice PDF) merged this window — one of three cleared.
- **Reverted:** none this window.
- **Churn hotspots this run:**
  - `frontend/apps/ppt-web/src/contexts/AuthContext.tsx` — 4 touches (PRs #2941, #2942, +2 review-comment rounds); all landed with matching tests. Fresh hotspot; runs_seen=1.
  - `frontend/apps/mobile/src/services/resetLocalData.ts` — 4 touches (PRs #2950, #2955); all landed with matching tests. resetLocalData is now the mobile-side session-purge fanout point. Fresh hotspot; runs_seen=1.
  - Lockfiles (`backend/Cargo.lock`, `backend/Cargo.toml`, `frontend/pnpm-lock.yaml`, per-package `package.json`) excluded per routine churn-exclusion list — dependabot-driven, not instability.

## PRs stuck in draft despite approval
- #2902 screen-map-drift-pr-2894-reality — draft, unchanged for 6+ days (last update 2026-09-03), reviewDecision null. Same "branch contaminated" case flagged on 2026-09-01; needs the author to rebase or close.
- #2744 fix(dispatcher): un-wedge oversize-archive push — draft, self-approval blocker, unchanged from 2026-09-01.

## PRs with verdict=changes, no fix-round progress in 24h
- none — reviewer decisions across the window all resolved: approvals landed or dispatcher fast-path merged the follow-up.

## Code review slice
- Segment reviewed: **none this run** — Phase 1.5 skipped. See *Skipped this run* below.
- Experts: n/a.
- Findings: 0 fresh; the dispatcher's parallel Tier-1d generator has been running on planning branch (2026-09-04 reality-web, 2026-09-06 reality-server, 2026-09-07 reality-web, 2026-09-09 ppt-web-core) — output lives in `.research/management/assignments.json` on `planning`, not in this routine's backlog.
- Next segment: `mobile-native-kmp` (review_cursor still 2026-08-25 — oldest); deferred until #2652 AGP egress unblocks cloud runs, or the routine gets rerouted via ppt-bridge-mcp.
- Skipped: **yes** — see *Skipped this run*.

## Screen-map status
- Total `docs/screens/` files: unchanged (ppt/ + reality/ + reality-mobile/; mobile/ still not seeded → 0).
- Drift signals this run: **0**. The one large PR that touched frontend routes (#2919 fa421c5 merge diff sweep) also updated `docs/screens/ppt/**` and `docs/screens/reality/**` in the same commit — matches the doc-side-updated heuristic.
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (bounded-context spare of ~1500 tokens).

## Backlog deltas
- **New (2):**
  - [2] `security-portfolio-properties-idor-issue-2945` — Cross-tenant IDOR (read+write) in `investor_portal.rs` portfolio_properties handlers. Sibling of #2944 (already fixed); carved out as separate landable. `security`, `high`, cloud-blocked by #2949.
  - [2] `security-portfolio-analytics-idor-issue-2946` — Cross-tenant IDOR (read+write) in `portfolio_analytics.rs`; org_id computed then discarded in the query. `security`, `high`, cloud-blocked by #2949.
- **Bumped:** none this run (hotspot signals recorded in `state.hotspot_history` only — no matching backlog rows to bump).
- **Decayed (2, dropped):**
  - `code-review-mobile-native-kmp-portfolio-analytics-caps-100` — score 1 → 0 → dropped. 21d since last signal; mobile-native-kmp parked behind #2652 AGP egress.
  - `code-review-mobile-native-kmp-cancellation-swallowed` — score 1 → 0 → dropped. 15d since last signal; same parking lot.
- **Dropped (explicit):** none beyond the two decays above.
- **Resolved (from merged PRs):** 0 backlog rows explicitly transitioned this run. The 19 PRs merged over the window either close pre-existing dispatcher assignments (whose canonical state lives in `assignments.json` on planning, not here) or close GitHub issues directly (#2947/#2953/#2948/#2954/#2937/#2929/#2930/#2924/#2923 — all `follow-up` label → closed). No open backlog rows in `backlog.json` matched them by id or file path (the recent activity has been dispatcher-native, bypassing this routine's backlog).

## Plans promoted
- **0 new plans this run.** Rationale:
  - The two new score-2 security items (#2945/#2946) satisfy the *security fast-track* readiness gate (`security` + `high` + `score >= 2`), but both target api-server code that is currently cloud-unlandable per #2949 (utoipa-swagger-ui build-script egress blocked). Promoting them to `plans/` now would grow the parked pile without helping buffer refill. Deferred to a next run gated on #2949 status.
  - The one remaining high-confidence open item at score ≥ 3 is `code-review-mobile-native-kmp-create-listing-not-wired` (score 4, high) — still cloud-unlandable via #2652 AGP egress. Same parking-lot rationale as 2026-09-01.

## Auto-fix
- `no candidate` — no signal met `confidence == high` + `score >= 3` + allowlist match. The two new security items are high-confidence but score 2 (below auto-fix bar of 3 for `fixme-in-merged-code`); the churn-hotspot signals aren't in the allowlist by design. No screen-map-drift, no screen-map-orphan, no stalled-review candidate above the routine's own week-fresh threshold. No lockfile-drift consumer-manifest cases.

## Self-review findings
- none (`.research/self-improvement/findings.json` absent).

## Skipped this run
- **Phase 1.5 (rotating expert review)** — skipped. Rationale: (1) mobile-native-kmp is oldest but blocked by #2652 (spending review tokens on unlandable findings is low value); (2) the dispatcher's parallel Tier-1d generator has been covering reality-web (2026-09-04, 2026-09-07), reality-server (2026-09-06), and ppt-web-core (2026-09-09) over the window — duplicating that in the routine would inflate `seen_signals` without new information. The `review_cursor` is not advanced.
- **Phase 1.6 (PM synthesis + role rotation)** — skipped. Rationale: 7d 22h lag makes a role snapshot lossy against the live dispatcher digest, and the buffer-low payload is already the live view. `pm_cursor` and `coverage_cursor` are not advanced. G16 is a no-op (Phase 1.6 didn't run).

## Buffer-refill posture
- **Trigger asked for** cloud-landable frontend/reality-web refills. **This run's contribution:** 0 direct refills of `assignments.json`. The routine writes to `backlog.json` (on dev), which is a separate pipeline from the dispatcher's `assignments.json` (on planning); routine-side promotion is capped at 2 plans/run and gated by cloud-landability, and neither of my two new items qualifies.
- **Actual refill mechanism today** (out-of-band for this routine): the dispatcher's own Tier-1d generator commits (planning-branch) picked up ppt-web-core signals in `26eda79` this morning — that's what will refill the buffer, not this routine.
- **Structural takeaway:** two infra blockers (#2652 mobile-native, #2949 api-server) are throttling the *landable* pipeline. The dispatcher can only pick cloud-runnable stack signals until at least one clears. The routine can help by preferentially promoting frontend/reality-web plans when ready — none were ready this run.

## Open questions
- **#2949 api-server cloud-build blocker:** utoipa-swagger-ui build-script egress 403. Opened 2026-09-07, no assigned owner. Blocks every open security item this run (#2945, #2946) plus any api-server code review going forward. Escalate to structural-block (needs-human-judgement) if not resolved by next routine run.
- **#2951 mobile-rn jest infra blocker:** jest-expo@56 ↔ react-native@0.87 rot, opened 2026-09-07. Blocks any RN implementer verify path; PR #2952 landed IG3 but the underlying suite still can't run in cloud. Same escalation candidate.
- **Cross-branch signal fanout:** the dispatcher's `.research/management/assignments.json` (on planning) and this routine's `.research/backlog.json` (on dev) are drifting apart — the dispatcher's fast-path merges (PRs #2941–#2956) resolve dispatcher-side tasks that never entered this backlog. Worth a future pass to reconcile the two ledgers so the routine's *Backlog deltas → Resolved* row is meaningful again. Not addressed this run.

## Quiet day?
- **No** — 19 PRs merged over the catch-up window, 2 new IDOR security items surfaced (both cloud-blocked), 2 stale mobile-native-kmp items decayed → dropped. Cumulative work is substantial, but the *routine-side* delta is modest (6 signals, 2 new backlog rows, 0 plans) because the dispatcher fast-path swept most of the actionable work already.

## Goal violations
- G16 is a no-op (Phase 1.6 skipped). G8/G9/G17 abort-on-fail checks run in Phase 6 below; all other goal checks recorded in `signals/2026-09-09.json.goal_checks[]`.
