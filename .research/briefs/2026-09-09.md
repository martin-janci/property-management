# 2026-09-09

## Since last run
- Merged PRs: 17 (range #2922–#2956)
- Open PRs touched: 9 (8 dependabot; 1 auto-fix draft — PR #2902 screen-map-drift, updated 2026-09-03)
- New / updated issues: 17 (13 closed follow-ups; 4 open — 2 security IDORs #2945/#2946, 2 cloud-infra blockers #2949/#2951)
- Commits: 100+ on `dev` (dispatcher + merged PRs)
- Routine lag: **7d 22h** since last run — last_run_iso=2026-09-01T18:38:58Z (cloud scheduler was quiet for ~8 days)
- **Stale-routine alert:** state.json last_run_iso is 8d old; cloud routine cron was likely paused. Dispatcher (separate loop on planning branch) continued ticking — `.research/management/assignments.json` was refreshed daily by the dispatcher.
- Phases that failed: none. Phase 1.5 (rotating expert review) and Phase 1.6 (project management) intentionally deferred this run — see *Code review slice* and *Auto-fix* below.

## Shipped
- #2955 gh-issue-2953 — purge NFC credential material from SecureStore on session change (mobile-rn security follow-up).
- #2956 gh-issue-2954 — harden queryKeys.test.ts auto-discovery (suite-crash risk + glob-depth gap).
- #2950 gh-issue-2947 — purge NFC access log, QR scan history & offline feedback on session change (mobile-rn security).
- #2952 gh-issue-2948 — auto-discover feature-local *Keys factories in logout-purge coverage test (ppt-web).
- #2941 code-review authctx-no-boot-expiry-check — validate JWT exp at cold boot (ppt-web-core auth).
- #2942 code-review api-401-no-request-replay — single-flight refresh + replay on 401 (ppt-web-core api).
- #2943 code-review logout-purge-allowlist-fragile — enforce logout purge allowlist coverage (ppt-web-core).
- #2938 gh-issue-2937 — invoice PDF renderer + handler test coverage (accounting-server).
- #2940 code-review confirm-dialog-i18n — externalize ConfirmationDialog loading label via i18n (ppt-web-ui).
- #2935 dependabot npm-minor-patch — merged (40 updates group).
- #2932 gh-issue-2930 — e2e 403 test for agent-review self-review guard (reality-server).
- #2931 gh-issue-2929 — make report_summary snapshot-consistency test fail on pre-fix code (db).
- #2928 gh-issue-2924 — snapshot-consistent report_summary counts + entries (db).
- #2927 gh-issue-2923 — cover update-error i18n path in EmergencyContactDirectoryPage tests (ppt-web).
- #2925 code-review compliance raw-db-leak-regression — route audit-log count error through db_error (api-handlers).
- #2922 code-review saved-search-error-string-status — derive HTTP status from a typed error enum (reality-server).
- #2926 code-review fcm-legacy-dead-endpoint — remove decommissioned FCM legacy send path (api-core).

## Watch
- Stalled review: none exceeded 7 days no-decision this run. PR #2902 (screen-map-drift auto-fix draft) at 6d — nearing threshold; still marked draft.
- Reverted: none.
- Churn hotspots this run (top-3 after exclusions):
  - `backend/servers/accounting-server/tests/invoice_pdf_tests.rs` (252 lines, runs_seen=1) — new backlog row.
  - `frontend/apps/ppt-web/src/lib/queryKeys.test.ts` (230 lines, runs_seen=1) — new backlog row; three logout-purge coverage PRs.
  - `backend/crates/db/src/repositories/data_export_test.rs` (186 lines, runs_seen=1) — new backlog row.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (Dispatcher assignments.json on the planning branch not fetched this run; if any exist, they are the dispatcher's to work — surface next run.)

## Code review slice
- Segment reviewed: **none (Phase 1.5 deferred)** — reason: 8-day cloud lag catch-up run; token budget prioritized signal observation + backlog integrity + gate compliance over spawning per-segment expert subagents. All 8 segments reviewed within the last 2 weeks (oldest: mobile-native-kmp 2026-08-25, 15d).
- Experts: — (deferred)
- Findings: 0 (see Backlog deltas for `code-review-finding` signals)
- Next segment: **mobile-native-kmp** (oldest unreviewed; churn does not currently point at any live segment — hotspots are in accounting-server + test files not mapped to the 8-segment cursor).
- Skipped: yes — cloud lag catch-up run (single-tick refill).

## Screen-map status
- Total `docs/screens/` files: (unchanged from last run; not enumerated this run — deferred with Phase 1.5)
- Drift signals this run: 0 — no merged PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**` in the 2026-09-01..09-09 window.
- Orphan screens this run: 0 (no route trigger files touched → no delta to spot-check).
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New:** [1] `churn-hotspot-backend/servers/accounting-server/tests/invoice_pdf_tests.rs` — 252 lines this window.
- **New:** [1] `churn-hotspot-frontend/apps/ppt-web/src/lib/queryKeys.test.ts` — 230 lines across #2943/#2952/#2956.
- **New:** [1] `churn-hotspot-backend/crates/db/src/repositories/data_export_test.rs` — 186 lines across #2928/#2931.
- **Bumped:** none this run (no matching item id / title similarity for the signals emitted).
- **Decayed:** `code-review-mobile-native-kmp-portfolio-analytics-caps-100` — 1 → 0, `dropped` (updated_at 2026-08-19 > 14d, no fresh signal).
- **Decayed:** `code-review-mobile-native-kmp-cancellation-swallowed` — 1 → 0, `dropped` (updated_at 2026-08-25 > 14d, no fresh signal).
- **Dropped:** both decayed items above.

## Plans promoted
- none this run.

Rationale: the buffer-low payload ("claimable=9/72, mostly mobile-native/backend gated") signals that the **planner** side of the dispatcher (not the routine's backlog) is starved for cloud-landable work. The routine promotes plans from `.research/backlog.json` — its own supply. Current open/ready rows after decay: 3 items, all `mobile-native-kmp` (cloud-gated per Issue #2652). No cloud-landable candidate reached the readiness bar this run — the fresh signals emitted are all churn-hotspots (score 1, well below the +3 promotion floor) and dep-update-noise (0). The productive refill mechanism for cloud-landable vectors is Phase 1.5 (rotating expert review of `ppt-web-core` / `reality-web` / `reality-server` segments) — deferred this run per catch-up scope; scheduled for the next tick.

## Auto-fix
- `no candidate` — no signal met the certainty bar (confidence=high + backlog score ≥ 3 + allowlist). All fresh signals are either `dep-update-noise` (0) or `churn-hotspot` (1). Kill-switch not set. (`auto_fix_actions=[]` in signals/2026-09-09.json.)

## Self-review findings
- none — `.research/self-improvement/findings.json` not consulted this run.

## Open questions
- **Cloud routine cadence** — the 8-day gap between 2026-09-01 and today suggests the daily cron for this routine (separate from the dispatcher on the planning branch) is missing or throttled. Recommend the operator verify the routine cron is armed and firing daily; the dispatcher's own ticks are not a substitute.
- **Buffer-refill strategy** — the buffer-low payload identifies planner starvation (dispatcher's `.research/management/assignments.json` on the planning branch, not backlog.json here). Rotating expert reviews of `ppt-web-core` and `reality-web` are the most efficient route to new cloud-landable vectors. Should the routine unconditionally run Phase 1.5 on a `ppt-web-core` or `reality-web` segment next tick, overriding the churn-aligned/oldest-unreviewed selection rules while the buffer stays low? Left for operator judgement.

## Quiet day?
- No — 17 merged PRs shipped, 3 new backlog vectors added, 2 items decayed and dropped.
