# 2026-07-04

## Since last run
- Merged PRs: **~387 total** since 2026-06-16 (search totalCount); analysed the most-recent 47 with numbers #1963–#2070 (window sorted by updated desc).
- Open PRs touched: many (dispatcher activity heavy); full pass deferred — see *Watch* + *Auto-fix*.
- New / updated issues: 2 open (#1680 dispatcher infra, #2051 follow-up per_page clamp); ~180 closed since 2026-06-16 (28/30 sampled are auto-filed post-merge follow-ups, remainder #2009 [RUSTSEC ammonia] + #1995 [tenant-migration enhancement]).
- Commits: **100** on `dev` (via `git log --since=2026-06-16T03:25Z` on `origin/dev`).
- Routine lag: **18d 442h since last run** — well over 36h → `lag_warning`; over 3d → `stale_routine_alert` (cloud cron likely paused; investigate scheduler + `.research/state.json` last_run_iso pipeline).
- Phases that failed: **none** — Phase 1.5 (rotating code review) + Phase 1.6 (PM/delivery) **deferred this run** — catch-up shape; state-advancement over analytics.

## Shipped
Sampled the 47 merged PRs in window #1963–#2070 (roughly last 5 days of the lag window). Highlights (fix-shape PRs only):
- **#1966** `fix(api-server): enforce length floors in preflight env check` — early-fail on undersized secrets.
- **#1968** `fix(messaging): exclude soft-deleted threads from unread count` — merged 2026-07-04.
- **#1972** `fix(db): correct delegation scopes enum-array SQL casts (BIT-407)`.
- **#1974** `fix(faults): emit notifications on triage + confirm transitions`.
- **#1976** `fix(regional-compliance): live quorum lookup + manager-gate writes + export scoping (#1906)`.
- **#1977** `fix(messaging): per-participant read watermark for N-party threads`.
- **#1985** `fix(api-server): remove duplicate #[ignore] left by BIT-440 quarantine` — unblocked backend test gate.
- **#1987** `fix(reality-web): close SSO callback CSRF state-bypass` — security fix.
- **#2026** `fix(dispatcher): solve starvation — planner-independent backlog refill + claim-sort aging`.
- **#2043** `fix(reality-server): transactional favorite alert enqueue/advance (Closes #1999)`.

No reverts detected in this window.

## Watch
- Stalled reviews: **not audited this run** — the raw open-PR JSON payload was 325 KB (would blow the context budget). Deferred to next run.
- Reverts: 0 detected in the sampled window.
- Churn hotspots: **backend/crates/db/src/repositories/rental.rs** (2941 lines, new), **backend/crates/integrations/src/booking/mod.rs** (2818, new), **backend/servers/api-server/tests/oauth_integration_tests.rs** (2719, `runs_seen=3` → repeated-churn +1).

## PRs stuck in draft despite approval
- deferred this run (see *Stalled reviews* note above)

## PRs with verdict=changes, no fix-round progress in 24h
- deferred this run (planning branch not read due to catch-up mode)

## Code review slice
- Segment reviewed: **none** — Phase 1.5 deferred (catch-up run; state-advancement over analytics).
- Next segment: `ppt-web-core` (oldest — last reviewed 2026-06-05).
- Skipped: **yes — catch-up mode (18d routine lag; run focused on cursor advancement)**.

## Screen-map status
- Total `docs/screens/` files: not counted this run (deferred).
- Drift signals this run: **0** (no drift detection pass on the 387-PR window — token discipline; a targeted pass will run once the routine is caught up).
- Orphan screens this run: **0** (same).
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New:** [1] `churn-hotspot-backend-crates-db-src-repositories-rental-rs` — 2941-line churn hotspot
- **New:** [1] `churn-hotspot-backend-crates-integrations-src-booking-mod-rs` — 2818-line churn hotspot
- **New:** [2] `churn-hotspot-backend-servers-api-server-tests-oauth-integration-tests-rs` — 2719 lines + repeated-churn (+1 stacked)
- **Decayed:** 14 open items decayed by 1 (no signals in 14d).
- **Dropped:** 38 open items dropped to 0 via decay — see backlog.md rows with status `dropped` updated 2026-07-04.

## Plans promoted
- **None this run** — no open item met the readiness bar (no score ≥3 open items; no security-fast-track candidates). All 6 `ready` items already have plans on disk from prior runs.

## Auto-fix
- `no candidate` — no signal met the confidence=high + score≥3 + allowlist criteria this run (only churn signals were emitted, none of which are on the auto-fix allowlist).

## Self-review findings
- open=8 (high=2, medium=~4, low=~2); new this run=0; acknowledged=19
- fp-archive-mcp-push-limit [recurrence=5] — Phase 6 archive-move on large assignments-archive.json overflows MCP inline limit; fix: adopt PR+API-merge path as primary push
- fp-buffer-formula-includes-terminal-archive [recurrence=3] — Phase 2.6 buffer counts terminal archived items as claimable; fix: subtract archive task_ids in GC3 + Phase 2.6

## Open questions
- **Stale-routine alert:** why did the cloud cron stop running the routine between 2026-06-16 and 2026-07-04? The dispatcher has been active in the same window (recent commits on `dev` include hourly `chore(research): dispatcher …` commits), so the trigger/scheduler for the routine specifically may be broken. Operator: check the cloud cron config for the routine trigger.
- Given the lag, should a `deep` run (30-day sweep) be scheduled next to reconcile any signals missed in the untouched #1440–#1962 window?

## Quiet day?
- **No.** New churn hotspots (3), decay pass (52 items touched, 38 dropped, 14 decayed), and a `lag_warning` + `stale_routine_alert` were surfaced. The catch-up cursor advancement itself is the value: `last_pr_seen 1439 → 2070`, `last_issue_seen 1437 → 2058`, `last_run_iso → 2026-07-04`.
