# 2026-07-04

## Since last run
- Merged PRs: **434** merged since 2026-06-16 (range #988–#2071 — includes long-lived branches finally landing; number range is not a contiguous window). Real product PRs squashed onto current `origin/dev` since the cursor: **51** (of which 22 carry an explicit `(#NNNN)` squash suffix; the remainder are direct-to-`dev` dispatcher/research commits).
- Open PRs touched: **21** (2 drafts open ≥7d — #1797, #1812; the rest are recent `chore(deps)` bumps + 4 fresh implementer PRs from today).
- New / updated issues: **113 new** (only **2 new-open**, 111 already closed; **0 unlabeled-open** — no new triage rows this run).
- Commits: **664** on `dev` reported by the GitHub commits API (includes all commits reachable via merged PR branches). Local `git log --numstat --since` on the current `origin/dev` sees **51** direct commits in the window (dev was force-updated between the two data points).
- Routine lag: **18d 12h** since the previous run (2026-06-16 03:25 UTC) — `lag_warning` + `stale_routine_alert` emitted; cloud router likely paused, investigate before the next scheduled fire.
- Routine-fire text: `buffer-low: claimable=8/72 — refill planner` (dispatcher trigger, not a documented routine trigger per §Special trigger payloads; surfaced as operational note, run proceeded as normal incremental).
- Phases that failed: **none** (Phase 1.5 and Phase 1.6 were **skipped** on purpose — see below — not failed).
- Phases skipped (deliberate): **Phase 1.5** (rotating expert review) + **Phase 1.6** (project management rotation). Reason: 18d catch-up with degraded observation fidelity (agent produced observe.json without per-PR file lists / labels — churn hotspots came from local `git log --numstat` only, screen-map drift / hotfix-no-test / fixme-in-merged-code inputs unavailable). Neither cursor advanced this run.

## Shipped
Full 51-commit list would drown the brief. Highlights on `origin/dev` since 2026-06-16:
- **#1713** `revert(delegations): remove re-added delegation frontend (#1690) — reconcile with retirement (BIT-213)` — retirement-driven revert (feature abandoned, not fix-driven). Emits `revert-pr-1713`, vector `refactor`, +3.
- **#1679 / #1676 / #1677 / #1678** — the four `code-review-reality-web-*` / `code-review-mobile-rn-report-fault-fake-submit` plans from prior runs all landed 2026-06-22 as a batch. Resolutions applied in Phase 2 (see Backlog deltas).
- **#1197** (test-oauth-authorization-server-integration) merged 2026-06-17 — resolves the long-open Epic 6 e2e stub PR.
- **#1179** (docs(epics) catalog backfill for 37 mounted-but-undocumented backend modules) merged 2026-06-16 — the prior brief's `dx-stalled-review-pr-1179` is now `done`.
- Backend regional-compliance feature build-out (~3k lines across `backend/crates/db/src/models/regional_compliance.rs` + `.../routes/regional_compliance.rs`) — surfaces as fresh churn hotspots (see below).
- Faults route stability work in `backend/servers/api-server/src/routes/faults.rs` (~2k lines across 3 commits).

## Watch
- **Stalled drafts (10d, no reviewDecision):** #1797 `fix(api-server): auth on OCR endpoints + manager-gate rental guest PII reads` and #1812 `churn-hotspot-backend-crates-db-src-repositories-reality-portal-rs: split reality-portal repo`. Both emitted as `stalled-review-pr-1797` / `stalled-review-pr-1812`, +1 each.
- **Fresh churn hotspots (not in `hotspot_history`):**
  - `backend/servers/api-server/src/routes/faults.rs` — 3 commits, 1,984 lines.
  - `backend/crates/db/src/models/regional_compliance.rs` — 3 commits, 792 lines.
  - `backend/servers/api-server/src/routes/regional_compliance.rs` — 3 commits, 1,030 lines.
  - All three are new-feature build-out, not stability alarms; scored +1 each for visibility.
- **Reverts:** 1 (#1713 delegation retirement — feature-lifecycle, not correctness).
- **Dep-updates:** 23 chore(deps) merged in-window + several open dependabot PRs today; no lockfile-drift detected via observe agent (needs file-level data — deferred to next run).
- **Force-push on `dev`:** `git fetch` reported `+ 31c688c...7ae37a2 dev -> origin/dev (forced update)`. The last-committed cursor SHA `4829015b` is no longer reachable from `origin/dev`, so `state.last_commit_sha` is being reset to the current `origin/dev` HEAD (`7ae37a29…`). Any commit history under the old lineage is preserved via PR merge records — nothing is lost in this run.

## PRs stuck in draft despite approval
- none  <!-- observe agent could not read reviewDecision on drafts; confidence: medium. Best-effort filter returned 0. -->

## PRs with verdict=changes, no fix-round progress in 24h
- none  <!-- .research/management/assignments.json: 0 rows match status=review AND reviewer_summary startsWith("verdict=changes"). -->

## Code review slice
- Segment reviewed: **none this run** (Phase 1.5 skipped)
- Experts: n/a
- Findings: 0
- Next segment: **api-handlers** (`state.review_cursor.api-handlers = 2026-06-10` — 24d stale, oldest tied with `ppt-web-core` at 29d; either is a valid next pick when Phase 1.5 resumes)
- Skipped: **yes** — reason: 18d catch-up with degraded churn/file-list observation fidelity. Resume next run.

## Screen-map status
- Total `docs/screens/` files: **162** (ppt=100, reality=47, mobile=15 — the `mobile/` product-doc dir has been **seeded since last run** at 15 files; last brief reported 0).
- Drift signals this run: **0** — deliberately skipped. Observe agent's `touches_routes` / `touches_screen_docs` fields were title-heuristic only (no per-PR file access); firing drift signals from that data would produce heavy false positives across 434 PRs. Resume next run once file-list observation is back.
- Orphan screens this run: **0** — same reason.
- Last `screen-map.yml` CI run on `main`: not fetched (no `docs/screens/**` edits confirmed in this run's staged set).
- **Follow-up next run:** the routine spec has a note to re-include `frontend/apps/mobile/src/**` in the trigger set and add `mobile` to the doc-dir brace-expansion once `docs/screens/mobile/` has content. That's now true — 15 files seeded. Amending the routine prompt is out of scope for this catch-up run; flagged so the next full run picks it up (or the operator amends manually).

## Backlog deltas
- **New (6):**
  - [3] `revert-pr-1713` — PR #1713 reverted delegation frontend (retirement-driven, BIT-213). vector=refactor. `files:[]` → **can't promote to a plan** without concrete surface identification.
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-faults-rs` — 3 commits, 1984 lines, fresh hotspot.
  - [1] `churn-hotspot-backend-crates-db-src-models-regional-compliance-rs` — 3 commits, 792 lines.
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-regional-compliance-rs` — 3 commits, 1030 lines.
  - [1] `stalled-review-pr-1797` — draft 10d, OCR auth + rental guest PII gate.
  - [1] `stalled-review-pr-1812` — draft 10d, reality-portal-rs repo split.
- **Bumped:** none this run (all new signals landed on fresh IDs; the previously-decayed `screen-map-drift-pr-1418-ppt` did not receive a fresh signal and continued to decay).
- **Decayed (−1 each, no signal in 14d):** 51 open items (all pre-existing opens at score 1 or 2 aged past the 14d cutoff). Of those, 37 fell to score 0 → `dropped`; 14 stayed open at score 1.
- **Dropped (37 total this run — all previously `open` at score 1 decayed to 0):** the list is enumerable via `jq '[.items[] | select(.status=="dropped" and .updated_at=="2026-07-04")] | map(.id)' .research/backlog.json` — enumerating each here would drown the brief. Notable classes: `refactor-churn-hotspot-*` batch (mobile announcement/document repositories), `test-gap-hotfix-no-test-pr-*` batch (RLS-migration PRs from the May/June window), `triage-*` old issue rows, and single dispatcher/dx items. 14 items decayed 2→1 stayed `open`.
- **Resolved (5) → `done` this run:**
  - `dx-stalled-review-pr-1179` — PR #1179 merged 2026-06-16 (the stalled PR itself landed).
  - `code-review-mobile-rn-report-fault-fake-submit` — PR #1679 merged 2026-06-22; plan moved to `plans/_archive/`.
  - `code-review-reality-web-share-comparison-404` — PR #1676 merged 2026-06-22; plan archived.
  - `code-review-reality-web-realtor-mgmt-untranslated` — PR #1678 merged 2026-06-22; plan archived.
  - `code-review-reality-web-listing-page-ssr-crash` — PR #1677 merged 2026-06-22; plan archived.

## Plans promoted
- **none this run.** `revert-pr-1713` (score 3, the only new open score≥3 item) has `files:[]` — can't satisfy the "≥1 concrete file that resolves on disk" readiness gate. All other new items are score 1. Existing `ready` items (`security-llm-doc-idor`, `bug-ios-searchview-uncompilable`) still have their plans on disk; no re-promotion needed.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. `stalled-review-pr-1797` and `stalled-review-pr-1812` are the only comment-only-shape candidates; both PRs are authored by the operator (martin-janci), so the routine's third-party `Routine ping: … anything blocking?` comment would be talking to the operator about the operator's own drafts — noisy, not actionable. Skipped.

## Self-review findings
- open=8 (high=2, medium=4, low=2); new this run=0; acknowledged=0. Both open highs are dispatcher-side (Phase 6 archive-push + Phase 2.6 buffer-stale) — not routine-side. No new routine-side findings surfaced this run.
- fp-arch-push [recurrence=5] — Phase 6 archive-move produces a 198KB assignments-archive.json that mcp__github__push_files cannot inline; workaround: push to a new branch + open PR via mcp__github__create_pull_request; fix: land the change described in the finding's proposed_fix (dispatcher-side, not routine).
- fp-buffer-stale [recurrence=3] — Phase 2.6 buffer reported claimable=54/open=54 but REAL claimable=0; all 54 items terminal in assignments; fix: extend gc1-reconcile.sh (dispatcher-side, not routine).

## Open questions
- **Why did the cloud routine skip 18 days?** `last_run_iso=2026-06-16T03:25:00Z` → today 2026-07-04. Dispatcher on `planning` kept running (its state files churned normally); the routine's cloud cron is on a different loop. Operator should re-check the routine trigger before the next scheduled fire — if this recurs, the P4 stale_routine_alert becomes worthless.
- **`dev` force-push origin:** who force-pushed and why? The lineage rewrite invalidates the `last_commit_sha` cursor. If this is now a regular event (rebase-and-force flow), the routine should track dev by date not sha.
- **Mobile screen-map dir has 15 files now** — should the routine prompt be amended to un-strikethrough `frontend/apps/mobile/src/**` in the drift trigger set, and add `mobile` to the drift doc-dir?

## Quiet day?
- **No.** 434 merged PRs in the 18d window, 4 previously-promoted plans resolved to `done`, 1 revert, 6 new backlog vectors, 37 decayed to `dropped`, 51 total decays applied. `stats.runs` 40 → 41, `vectors_created` 173 → 179, `plans_created` 12 → 12 (no new plans), `quiet_days` unchanged.

## Goal violations
<!-- Filled after Phase 6 gate check — replace with per-gate report if any of G1-G17 fail. -->
- G1-G17 to be checked in Phase 6.
