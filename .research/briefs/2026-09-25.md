# 2026-09-25

_Incremental run 31h after 2026-09-24T03:19Z (under the 36h `lag_warning` threshold — no lag flag). No application-code churn in the window: all 20 commits on `dev` are `.research/`-only dispatcher heartbeats (0 by the routine itself since yesterday's brief). Phase 1.5 signals adopted from the dispatcher's Tier-1d ppt-web-core review generated earlier today at `signals/2026-09-25-ppt-web-core-tier1d.json` (2 net-new findings); Phase 1.6 ran the rotating pm-security role (66d stale slot refreshed) plus always-on pm-scrum-master. Routine cursor advances to origin/dev HEAD `59cdd7ef6e9c`; `stats.runs` bumps 81→82, `stats.vectors_created` 373→377 (2 code-review + 2 stalled-review)._

## Since last run
- Merged PRs: 0 (last_pr_seen=2956 unchanged — closed range #2963/#2964/#2965 all closed-not-merged dependabot noise on 2026-09-24 10:22).
- Open PRs touched: 9 (2 new IDOR retry drafts #2976/#2977 opened by dispatcher this UTC morning + 7 dependabot updates). None flipped to `merged`.
- New / updated issues: 0 (last_issue_seen=2966 unchanged).
- Commits: 20 on `dev` — all `chore(research): dispatcher …` heartbeats between 2026-09-24 04:33Z and 10:29Z. Zero application-code merges.
- Routine lag: **31h since last run** — under the 36h threshold; no `lag_warning`, no `stale_routine_alert`.
- Phases that failed: none.

## Shipped
- _nothing_ — 0 PRs merged in the 31h window.

## Watch
- **Live cross-tenant IDORs in draft.** PR #2977 (`gh-issue-2944-retry1`) confirms `backend/servers/api-server/src/routes/violations.rs` `list_evidence`/`list_payments`/`list_comments` discard `AuthUser` and repo methods filter with `&self.pool` (no org, no RLS). PR #2976 (`gh-issue-2945-retry1`) confirms `backend/servers/api-server/src/routes/portfolio_performance.rs` property sub-resource endpoints skip the `org_id` check applied by sibling portfolio-level handlers in the same file. Both are drafts on `needs-human-review`. See `roles/pm-security.md` for the full audit trail.
- **`needs-human-review` queue depth = 7 PRs.** #2977, #2976 (new today, security IDORs) + #2969, #2968, #2967 (reality-web silent-fail mutations, 3–4d old, backed by open backlog rows) + #2902 (screen-map-drift reconcile, 25d open / 7d idle) + #2744 (dispatcher fix, 43d open / 43d idle). The two aged drafts get fresh `stalled-review` rows this run.
- **Stalled review:** newly-fired `stalled-review-pr-2902` (25d open, 7d idle) and `stalled-review-pr-2744` (43d open / 43d idle) — both drafts with `needs-human-review`, past the >7d stalled threshold. Auto-fix comment allowlist would ping them but scores land at 1 < 3 gate (see *Auto-fix*).
- **Reverted:** none this window.
- **Churn hotspots (window: 31h):** **none** — every commit on `dev` in the window touches only `.research/`. `hotspot_history` unchanged; no `churn-hotspot` / `repeated-churn` / `risky-churn` signals emitted.
- **Infra blockers still open** (unchanged from yesterday's brief): #2966 (utoipa-swagger-ui build-script egress-blocked, gates api-server verify), #2951 (jest-expo 56 vs RN 0.87 rot, mobile RN jest), #2652 (closed but governing: KMP AGP egress). Payload's "claimable=8/72 — all remaining backlog cloud-unlandable" is the observable state.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     GraphQL disabled in cloud sandbox (see gh 403 note); REST equivalent would require
     per-PR review-check calls. The 7 needs-human-review drafts identified above have
     the `needs-human-review` label — that's a "not yet approved" signal, not
     approved-and-stale. -->
- none suspected (all 7 open drafts are labelled `needs-human-review`, i.e. explicitly not approved yet)

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Data lives in .research/management/assignments.json on the `planning` branch.
     Latest dispatcher commit on dev (59cdd7ef, 2026-09-25 10:29Z) reports "0 claimed,
     0 reviewed, 0 merge-attempts, 0 failed, 7 active" — nothing has flipped to
     verdict=changes+stalled. -->
- deferred to dispatcher output — latest dispatcher stats: `0 claimed, 0 reviewed, 0 merge-attempts, 0 failed, 7 active, B 8/8 dep-blocked=0`.

## Code review slice
- Segment reviewed: **ppt-web-core** (tie-oldest at 2026-08-29 with reality-web; reality-web has 3 open findings so tie-break went to ppt-web-core, which had 0 open). Findings adopted from the dispatcher's Tier-1d run at `signals/2026-09-25-ppt-web-core-tier1d.json` (same segment, same day) rather than duplicating the pass.
- Experts: frontend (dispatcher pass).
- Findings: 2 new `code-review-finding` signals:
  1. `code-review-ppt-web-core-fault-detail-mutations-swallow-errors` (bug, +2, high) — 7 fault-workflow mutations on `routes/groups/faults.tsx:444-458` fire with no onError, generated hooks in `packages/api-client/src/faults/hooks.ts` don't attach one either; correct pattern exists in `reports.tsx` / `financial.tsx`. Manager-critical UX regression.
  2. `code-review-ppt-web-core-org-provider-value-not-memoized` (bug, +1, medium) — `contexts/OrganizationProvider.tsx:8-10` value not `useMemo`-wrapped; every AuthContext token refresh cascades unrelated re-renders. Render-perf smell, not a crash.
- Next segment: **reality-web** (oldest at 2026-08-29 next run, unless ppt-web-core is bumped again — 3 open findings saturation rule may skip reality-web then).
- Skipped: **no — adopted the dispatcher's same-day Tier-1d pass** on the same segment; segment picked per routine's own `review_cursor` (ppt-web-core = 2026-08-29 → now 2026-09-25).

## Screen-map status
- Total `docs/screens/` files: unchanged (`ppt/` + `reality/`; `mobile/` still not seeded → 0).
- Drift signals this run: **0** — no merged PRs to scan for route-vs-doc drift.
- Orphan screens this run: 0 (nothing to re-scan without churn).
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (no merged PRs to trigger new CI results).

## Backlog deltas
- **New:**
  - [2] ppt-web fault-detail actions (Triage/Resolve/Confirm/Reopen/AddComment/AddAttachment/DeleteAttachment) fire mutations with no onError — see `backlog.md` (`code-review-ppt-web-core-fault-detail-mutations-swallow-errors`).
  - [1] ppt-web OrganizationProvider context value rebuilt every render (`code-review-ppt-web-core-org-provider-value-not-memoized`).
  - [1] stalled review: PR #2902 screen-map-drift-pr-2894-reality reconcile (25d open, 7d idle).
  - [1] stalled review: PR #2744 fix(dispatcher) un-wedge oversize-archive push + retry-remint ghost close (43d open, 43d idle).
- **Bumped:** none.
- **Decayed:** none — 0 open items have `updated_at` older than 14 days (the 3 open reality-web silent-fail items are all from 2026-09-21).
- **Dropped:** none.
- **Resolved:** none — 0 merged PRs to cross-reference; no evidence-gone grep without a merge signal.

## Plans promoted
- _none_ — the 4 new rows land at scores 1–2 (Phase 3 threshold is score ≥ 3, or ≥ 2 for `security` + high confidence). The 3 open reality-web silent-fail items (score 2) already have live PRs (#2967-#2969) — creating plans for rows whose implementations are already in review would just duplicate the PRs. The 2 `ready` mobile-native KMP plans stay cloud-unlandable (issue #2652).

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. Closest calls:
  - `stalled-review-pr-2744` / `stalled-review-pr-2902` are on the allowlist for comment-only pings and are high confidence, but the backlog items land at `score 1` (single +1 delta) < the score≥3 gate. Both flipped straight to `needs-human-judgement` per the existing stalled-review pattern.
  - `code-review-ppt-web-core-fault-detail-mutations-swallow-errors` is high confidence but the allowlist covers only doc-stubs / lockfile bumps / stalled-review comments / FIXME issue-only — code fixes always go to the manual plan path.

## Self-review findings
- none (no `.research/self-improvement/findings.json` under this repo layout).

## Open questions
- **Was issue #2944 / #2945 closure on 2026-09-23 tied to a merged fix or just to PR existence?** The vulnerable code is still on `dev` today; PRs #2976 / #2977 are the retry-1 cycle to recover exposure. Closure criteria for security issues probably need tightening — surfaced as decision by pm-security.
- **Is a fresh `coverage.json` `scan` due?** Current file (`generated: 2026-08-31`, `scan_kind: upkeep`) shows all 49 scanned stories `done` — either everything really is done or the scan is stale. Rotating upkeep only touches one epic per run (this run: epic-82 → 8/13 = ~2 months to full sweep).
- **sprint-status.yaml epics block reconciliation** — epics 6 / 7A / 10B / 80 show `in-progress` / `partial` with stale `stories_completed` counts while every constituent story is `development_status: done`. One-time rollup or documented gap?

## Quiet day?
- **No — new-vector day, not merge day.** 0 merged PRs, but 2 Phase 1.5 findings + 2 stalled-review + 4 new backlog rows + Phase 1.6 role rotation (pm-security lens on live IDORs) all landed. Phase 6 will re-evaluate `quiet` for the Telegram digest: this run is `quiet=false` per the "new vector OR promoted plan OR code-review finding OR auto-fix" rule.
