# 2026-09-25

_Incremental run 23h after 2026-09-24T03:19Z, fired by `buffer-low: claimable=10/72 — refill planner`. Application-code churn since last run is still zero (all 13 commits on `dev` in the window are dispatcher `.research/` chores + one previous routine chore), so Phase 1.5 (routine's own rotating review) and Phase 1.6 (project-management) both skip — but the dispatcher landed **5 Tier-1d code-review-finding signals** in that window that had not been ingested by any routine yet (frontend/packages @ppt/shared × 2; ppt-web-core × 3). Phase 2 ingests all 5 into `backlog.json`; Phase 3 promotes the one row that meets the readiness bar (`code-review-ppt-web-core-aichat-fetch-no-auth`, score=3, confidence=high, files resolve). Routine cursor advances `last_commit_sha` to `12ba18b8`; `seen_signals` grows 491→496; `stats.plans_created` 30→31, `vectors_created` 373→378._

## Since last run
- Merged PRs: **0** (last_pr_seen=2956 unchanged; latest merge on `dev` is still #2956 from 2026-09-08).
- Open PRs touched: **7** — all `dependabot[bot]` group bumps (#2957, #2961, #2962, #2972, #2973, #2974, #2975), rebased on 2026-09-24 10:13–10:26Z.
- New / updated issues: **0** since 2026-09-24T03:19Z.
- Commits: **13** on `dev` in the 23h window — 12× `chore(research): dispatcher …` from the dispatcher account (`Claude` / `Claude Code` / `Martin Janči` bot cycles between 04:40Z and 00:30Z next day), plus the tail commit `225bd183 research: 2026-09-24 brief` from the previous routine run at 03:20Z. **Zero application-code merges.**
- Routine lag: **23h since last run** — under the 36h threshold; no `lag_warning`, no `stale_routine_alert`.
- Phases that failed: **none** (Phase 1.5 + Phase 1.6 skips are explicit per banner above; not failures).

## Shipped
- _nothing_ — 0 PRs merged on `dev` in the window.

## Watch
- **Buffer-low trigger fired.** The dispatcher signalled `claimable=10/72` when this routine was invoked. Root cause: `backlog.json` non-terminal rows totalled just 5 (3 open + 2 ready) before this run. After ingest + promote the count is **10 (7 open + 3 ready)**; the `code-review-ppt-web-core-aichat-fetch-no-auth` plan lands as a fresh ready candidate for the dispatcher's next Phase 3 refill.
- **Two more raw-fetch-bypasses-JWT recurrences.** The dispatcher's ppt-web-core Tier-1d review found the same defect class already fixed by backlog `code-review-ppt-web-core-raw-fetch-bypasses-jwt-interceptor` (status `done`, 2026-08-29) recurring in `useAiChat.ts` (Epic 127) and `useOcrMeterReading.ts` (Epic 128). Both features are dead-on-arrival for authenticated users. The `useAiChat.ts` one is promoted to a plan today (score=3, high confidence). The `useOcrMeterReading.ts` one lands as an open row at score=2 — one more Tier-1d hit or manual bump crosses the promotion threshold. Consider a repo-wide `grep 'raw fetch(' features/**/hooks/**` sweep as a follow-up so recurrence #4 does not need another routine to catch.
- **@ppt/shared has two silent bug classes.** `decodeJwt()` casts JSON as `JwtPayload` without checking `exp` is numeric — a token with a missing/non-numeric `exp` claim reads as *never-expiring* through `isTokenExpired()`; auto-logout never fires (client-side expiry bypass). And every date formatter feeds unguarded `new Date(input)` straight into `Intl.DateTimeFormat.format()` / `toISOString()`, so a malformed timestamp crashes render with `RangeError: Invalid time value`. Both live at score=2 (medium confidence — static read only; no runtime repro yet); a second signal or a runtime crash trace lifts them to promotion.
- **Stalled review:** none — the 5 self-authored open drafts (#2967-#2971) are 3d old; #2902 is 7d old and #2744 is 43d old (both self-authored dispatcher retries), so a `stalled-review` signal would still fire on `updated_at > 7d` but the author is the dispatcher itself, so no ping is landable (the existing brief notes on 2026-09-24 already surfaced these).
- **Reverted:** none this window.
- **Churn hotspots (window: 23h):** **none** — every commit on `dev` in the window touches only `.research/`. No application-code churn to score; `hotspot_history` unchanged; no `churn-hotspot` / `repeated-churn` / `risky-churn` signals emitted.
- **Infra blockers still open** (unchanged from 2026-09-24 brief): #2966 (api-server utoipa-swagger-ui egress in cloud runner); mobile-native AGP egress class (originally #2652).

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     GraphQL disabled in cloud sandbox (see gh 403 note); REST equivalent would require
     a per-PR /pulls/{n}/reviews call to derive reviewDecision. Of the 7 open drafts:
     – #2967/#2968/#2969: 3d old, self-authored implementer drafts (author == dispatcher
       account) — human-gated by design.
     – #2970/#2971: 3d old, martin-janci Wave 1 CI drafts — not implementer flow.
     – #2902: 7d old, martin-janci screen-map-drift reconcile draft.
     – #2744: 43d old, martin-janci fix(dispatcher) draft (surfaced repeatedly).
     None of these are known-approved via the previous brief's notes. -->
- none suspected (7 open drafts inspected; all self-authored or unresolved dispatcher retries — none marked APPROVED in prior briefs).

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Data lives in .research/management/assignments.json on the `planning` branch.
     Latest dispatcher commit on dev (12ba18b8, 2026-09-25 00:30Z) reports "5 active,
     0 reviewed, 0 merge-attempts, 0 failed". -->
- deferred to dispatcher output — latest dispatcher stats: `5 active, 0 reviewed, 0 merge-attempts, 0 failed`, B 10/10 dep-blocked=0.

## Code review slice
- Segment reviewed: **skipped (no application-code churn since last run)** — every commit on `dev` in the 23h window is a `.research/` chore. Firing a fresh Phase 1.5 review over an unchanged code base duplicates dispatcher Tier-1d work already covered by `signals/2026-09-24-{frontend-packages,ppt-web-core,reality-server,reality-web}-tier1d.json`. Instead the routine ingested the 5 net-new dispatcher-emitted signals (see *Backlog deltas*).
- Experts: n/a (skipped for the routine's own Phase 1.5; ingested from dispatcher).
- Findings: 5 new `code-review-finding` signals landed in the backlog (ingested from dispatcher Tier-1d files dated 2026-09-24).
- Next segment (routine's own cursor): `reality-web` (2026-08-29) — tied with `ppt-web-core` (2026-08-29) as oldest-unreviewed. Next real routine run — when application-code churn returns — picks reality-web.
- Skipped: **yes — no application-code churn since last run; ingesting dispatcher-branch review coverage instead (third routine run in a row on this posture).**

## Screen-map status
- Total `docs/screens/` files: unchanged (`ppt/` + `reality/`; `mobile/` still not seeded → 0).
- Drift signals this run: **0** — no merged PRs to scan for route-vs-doc drift.
- Orphan screens this run: 0 (nothing to re-scan without churn).
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (no merged PRs to trigger new CI results).

## Backlog deltas
- **New:** [3] `code-review-ppt-web-core-aichat-fetch-no-auth` — ppt-web ai-chat hooks bypass JWT interceptor; every /api/v1/ai/chat call 401s; useDeleteSession also silently swallows failure. Promoted directly to `ready` (score=3, confidence=high, has files) — see plan below.
- **New:** [2] `code-review-ppt-web-core-ocr-fetch-no-auth` — same raw-fetch class in `useOcrMeterReading.ts`; both authenticated OCR endpoints 401. Confidence high but score=2 (one signal); waits for a second bump.
- **New:** [2] `code-review-frontend-packages-auth-exp-bypass` — `@ppt/shared` `decodeJwt()` casts payload; exp-less tokens read as never-expiring (client-side expiry bypass). Confidence medium.
- **New:** [2] `code-review-frontend-packages-formatdate-rangeerror` — `@ppt/shared` date formatters throw `RangeError` on Invalid Date; render crash on any malformed timestamp. Confidence medium.
- **New:** [1] `code-review-ppt-web-core-orgprovider-unmemoized` — `OrganizationProvider` hands a fresh object literal to `<Provider value>` every render; render-perf smell, not a correctness crash. Confidence medium.
- **Bumped:** none — every new signal creates a new item; no signal id landed on a pre-existing row.
- **Decayed:** none — 0 open items have `updated_at` older than 14 days (the 3 pre-existing open reality-web silent-fail items updated 2026-09-21 are 4d old).
- **Dropped:** none.
- **Resolved:** none — 0 merged PRs to cross-reference; no evidence-gone grep without a merge signal.

## Plans promoted
- `plans/code-review-ppt-web-core-aichat-fetch-no-auth.md` — route `useAiChat.ts` through `getApiClient()` so bearer/refresh/error-transform interceptors apply; fix `useDeleteSession` silent-failure at the same time · adversarial pass: **passed** (evidence → hypothesis → suggested approach → test plan all trace; test plan asserts an Authorization header on the outgoing request, which today would fail against the raw fetch; no placeholder rot in the file).

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The allowlist covers `screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / `dep-update-noise` lockfile-drift / `stalled-review`; today's 5 signals are all `code-review-finding`, which routes through the manual-plan path by design.

## Self-review findings
- none (no `.research/self-improvement/findings.json` under this repo layout).

## Open questions
- **Do the 7 dependabot group-bump PRs (#2957/#2961/#2962/#2972/#2973/#2974/#2975) need a `dep-update-noise` sweep this run?** All were rebased on 2026-09-24; none have merged since. Current policy scores them at 0 and logs in the brief — no backlog entry. Left as noted for the operator; no action taken.
- **Recurring raw-fetch class (3rd occurrence).** `code-review-ppt-web-core-raw-fetch-bypasses-jwt-interceptor` was closed 2026-08-29 as a single-file fix. The class recurred in `useAiChat.ts` and `useOcrMeterReading.ts` because the fix did not add a lint rule or sweep for other callers. Worth a `refactor` vector item to add a Biome/ESLint rule ("no raw fetch() inside features/**/hooks/**") once the two active fixes land — leaving it as an open question for the operator rather than promoting speculatively.

## Quiet day?
- **No** — 5 new backlog vectors ingested from dispatcher Tier-1d signals + 1 plan promoted (`code-review-ppt-web-core-aichat-fetch-no-auth`) + 0 Phase 1.5 findings of the routine's own + 0 Phase 5 auto-fix actions. The buffer-low trigger from the dispatcher is served: non-terminal backlog rows go from 5 → 10, one of which is `ready` with a plan attached.
