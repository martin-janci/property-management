# 2026-09-25

_Second routine run today, fired ~8h after the 14:44Z run by a fresh buffer-low payload variant (`claimable=9/72 — coverage exhausted (49/49 done), backlog empty; refill planner`). The buffer-low condition **self-resolved** between the payload snapshot and this routine turn: dispatcher issue-ingest picked up issue #2978 at 22:26:15Z and pre-spawn-claimed it at 22:30:59Z (commit `34d6b67`, "dispatcher pre-spawn claim — 1 new task(s)"). This run records the new issue as backlog vector `gh-issue-2978` (score=3, vector=bug, confidence=high, status=open) but suppresses promotion — Phase-3 slug-stem uniqueness collides with the identically-named open item in `.research/management/action-list.json`. Cursor advances `41f78e55 → 34d6b67`, `last_issue_seen 2966 → 2978`; `stats.runs 82 → 83`; `stats.vectors_created 373 → 374`; `stats.quiet_days` stays at 5 (this run is **not** quiet — one new backlog vector added). Same-day rerun overwrites the 14:44Z brief + signals file per the idempotent-rewrite rule; the earlier `buffer-low-2026-09-25-payload` signal id stays in `seen_signals` (not re-emitted; today's variant is `-v2`)._

## Since last run
- Merged PRs: 0 (last_pr_seen=2956 unchanged — REST fetch confirms no PR > 2956 has `merged_at` set).
- Open PRs touched: 0 net-new (dispatcher-spawned drafts remain the same set; no new open PR since 14:44Z).
- New / updated issues: **1 — #2978** ("ppt-web AI-chat + OCR feature hooks call the API via raw fetch() with no Authorization header (unauthenticated → 401 in prod) + silent delete"; labels: bug, frontend; author: martin-janci; opened 2026-09-25T20:37Z).
- Commits: 6 on `dev` in the ~8h window — 4 dispatcher tick commits, 1 dispatcher Tier-1d dev-review signal-gen (api-core; 3 landable findings, commit `1118221`), 1 dispatcher pre-spawn-claim (`34d6b67`, "1 new task(s)"). Zero application-code merges.
- Routine lag: ~8h since 14:44Z — well under the 36h threshold; no `lag_warning`; no `stale_routine_alert` (<3d).
- Phases that failed: none (Phase 1.5 + 1.6 skips are explicit per below; not failures).

## Shipped
- _nothing_ — 0 PRs merged in the window.

## Watch
- **Buffer-low payload (self-resolved) + dispatcher fast-refill.** Trigger payload `buffer-low: claimable=9/72 — coverage exhausted (49/49 done), backlog empty; refill planner` fired at ~22:34Z. Dispatcher had already refilled the pipe before the routine acted: issue-ingest added `gh-issue-2978` to `action-list.json` at 22:26:15Z (owner_role=pm-frontend, priority=high) and pre-spawn-claim spawned `auto-impl/gh-issue-2978-ac9bbc2a` at 22:30:59Z. This is exactly the refill path the payload asked for; nothing further routine-actionable on the buffer-low itself. Recorded as `buffer-low-2026-09-25-payload-v2` (score_delta=0).
- **New issue #2978 — ppt-web AI-chat + OCR bypass shared api client.** Three source-verified defects in `frontend/apps/ppt-web/src/features/**`:
  1. `features/ai-chat/hooks/useAiChat.ts` — `apiFetch<T>()` (≈L32) sends only `Content-Type: application/json`; no `Authorization` bearer header. All AI-chat calls (sessions/messages/create/send/feedback/escalated) fail closed with 401 in prod (feature is behind `<ProtectedRoute>`, so it's a broken-feature bug, not data exposure).
  2. `features/meters/hooks/useOcrMeterReading.ts` — `ocrFetch<T>()` (≈L26) same auth-by-omission for `POST /api/v1/ai/ocr/meter-reading` + `/ocr/correction`.
  3. `useAiChat.ts::useDeleteSession` (L131-136) — `await fetch(..., { method: 'DELETE' })` with no `response.ok` check, so a failed delete resolves as success and `onSuccess` invalidates the query, hiding the failure.
  Sibling `features/sentiment/hooks/useSentiment.ts` uses `getApiClient()` from `src/lib/api.ts` with a comment naming the interceptor mechanism — the pattern both hooks skip. Recorded as `gh-issue-2978` (score=3, vector=bug, confidence=high, status=open, plan=null). Dispatcher owns the fix on `auto-impl/gh-issue-2978-ac9bbc2a`.
- **Stalled review:** none new. The two `needs-human-judgement` rows (`stalled-review-pr-2555`, `stalled-review-pr-2559`) remain deferred; PR #2744 is still a long-running draft with active dispatcher metadata (unchanged from prior briefs).
- **Reverted:** none this window.
- **Churn hotspots (window: ~8h):** **none** — every commit on `dev` in the window touches only `.research/`. No application-code churn to score; `hotspot_history` unchanged; no `churn-hotspot` / `repeated-churn` / `risky-churn` signals emitted.
- **dep-update-noise (log-only):** no new dependabot activity in the window; the 2026-09-24 rollup wave (#2972/#2973/#2974/#2975) remains the current open set.
- **Infra blockers still open** (unchanged): #2966 (api-server utoipa-swagger-ui egress in cloud runner); mobile-native AGP egress class (originally #2652).

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     GH GraphQL still 403 in the cloud sandbox; REST path would need per-PR review-decision fetches for the ~11 open drafts. Yesterday's brief made the same call.
     Every open draft is either recent (#2967–#2971 opened 2026-09-21..22; #2976/#2977 opened 2026-09-25) or a long-running dispatcher/branch-map row (#2744 branch-work, #2902 screen-map drift) where "approval" is not the merge gate. None on the observable surface fits approved-and-stale. -->
- none suspected (no approved-and-stale case on the observable open-draft surface)

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Data lives in .research/management/assignments.json on the planning branch. Latest dispatcher commit on dev (`34d6b67`, 2026-09-25 22:30Z) reports "1 new task(s)" pre-spawn-claim; the prior 20:30Z tick (`f8bf1a3`) reports "7 active, 0 reviewed, 0 merge-attempts, 0 failed, B 8/11 dep-blocked=0". No verdict=changes stalls surfaced. -->
- deferred to dispatcher output — latest dispatcher stats: `7 active, 0 reviewed, 0 merge-attempts, 0 failed, B 8/11 dep-blocked=0`.

## Code review slice
- Segment reviewed: **skipped (no code churn since last run)** — every commit on `dev` in the ~8h window is a `.research/` chore. Fourth consecutive routine skip. Firing a fresh Phase 1.5 review over a code base with no delta since 2026-09-20 duplicates work already covered by dispatcher Tier-1d cycles (`signals/2026-09-25-{api-handlers,api-core,ppt-web-core,reality-web}-tier1d.json`).
- Experts: n/a (skipped).
- Findings: 0 new `code-review-finding` signals from this routine run.
- Next segment: `reality-web` and `ppt-web-core` are tied at 2026-08-29 as the routine's oldest cursors; a real next run picks reality-web when application-code churn returns.
- Skipped: **yes — no application-code churn since last run; dispatcher-branch review coverage adopted in place of a fresh pass.**

## Screen-map status
- Total `docs/screens/` files: unchanged (`ppt/` + `reality/`; `mobile/` still not seeded → 0).
- Drift signals this run: **0** — no merged PRs to scan for route-vs-doc drift.
- Orphan screens this run: 0 (nothing to re-scan without churn).
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (no merged PRs to trigger a fresh CI result).

## Backlog deltas
- **New:** [3] `gh-issue-2978` — ppt-web AI-chat + OCR feature hooks bypass shared api client (Authorization header omitted; delete swallows failures) · vector=bug, confidence=high, status=open, plan=null (promotion suppressed under Phase-3 slug-stem uniqueness — stem collides with the open action-list item `gh-issue-2978`).
- **Bumped:** none.
- **Decayed:** none — the 3 open reality-web silent-fail items are `updated_at=2026-09-21` (4d ago, inside the 14d window); the new `gh-issue-2978` is today; 4 remaining non-terminal rows within window.
- **Dropped:** none.
- **Resolved:** none — 0 merged PRs to cross-reference; no evidence-gone grep without a merge signal.

## Plans promoted
- _none_ — `gh-issue-2978` meets every readiness gate (score=3, status=open, files exist on disk, high-confidence source-traced evidence, testable via `pnpm -F ppt-web test`) EXCEPT the slug-stem uniqueness rule: the dispatcher's `action-list.json` already carries an identically-named open item `gh-issue-2978` (owner_role=pm-frontend, first_open_at=2026-09-25T22:26:15Z), and `assignments.json` shows that task claimed in-progress on `auto-impl/gh-issue-2978-ac9bbc2a` since 22:30:59Z. Emitting a routine-side plan for the same stem would double-book the implementer. The dispatcher owns implementation; the backlog row exists for cross-system traceability.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. (Allowlist covers `screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / `dep-update-noise` lockfile-drift / `stalled-review`; none of those types fired today. The `gh-issue-2978` finding is confidence=high + score=3 but its shape is a multi-file code fix that lands outside the allowlist — correct to route through the dispatcher.)

## Self-review findings
- none (no `.research/self-improvement/findings.json` file at HEAD on `dev`; dispatcher-side self-review continues on the planning branch and is outside this file's scope).

## Open questions
- **Should the routine's Phase-3 stem-uniqueness rule also block backlog-row creation when the dispatcher already owns the same stem, or only plan promotion?** Current implementation blocks only promotion — the row is kept for cross-system traceability. Argument for keeping: the routine's backlog is a research view, semi-independent from the dispatcher's queue; a row with `plan=null` doesn't create duplicate work. Argument against: two rows for the same underlying work (routine `backlog.json` + dispatcher `action-list.json`) risk drifting in status; a `done` transition on one side won't automatically reflect on the other. Leaving as-is this run; the resolution check in Phase 2 next run will catch the PR-merge propagation when `auto-impl/gh-issue-2978-ac9bbc2a` merges (via a PR whose body will reference #2978).

## Quiet day?
- **No** — one new backlog vector added (`gh-issue-2978`, score=3, vector=bug, confidence=high). 0 merged PRs, 0 plans promoted, 0 Phase 1.5 findings, 0 Phase 5 auto-fix actions — but the new vector alone flips the quiet flag. The run still qualifies as low-activity (no shipping, no promotions), so the Telegram digest goes out but the substance is thin. `stats.quiet_days` stays at 5 (not bumped); `stats.runs` 82→83; `stats.vectors_created` 373→374; `state.last_issue_seen` 2966→2978; `state.last_commit_sha` 41f78e55→34d6b67; `seen_signals` +2 (`gh-issue-2978`, `buffer-low-2026-09-25-payload-v2`).
