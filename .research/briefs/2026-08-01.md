# 2026-08-01

## Since last run
- Merged PRs: 4 (range #2613–#2616)
- Open PRs touched: 2 (both new drafts opened by the dispatcher — #2618, #2619)
- New / updated issues: 2 new (#2612 OPEN follow-up on PR #2608; #2617 CLOSED as dup)
- Commits: ~10 on `dev` since `dc64271` (mostly dispatcher/research bookkeeping)
- Routine lag: 9h since last run — on schedule
- Phases that failed: none

## Shipped
- **#2613** refactor(api-server): extract scheduler vote-lifecycle jobs to submodule — pure verbatim move (7 add / 391 del in `scheduler/mod.rs`, 412-line new `scheduler/votes.rs`).
- **#2614** test(api-server): integration test for outbound layout webhook replay/timestamp — closes #2485; drives `notify_layout_change` against an in-process receiver, asserts fresh timestamp header + timestamped-signature shape.
- **#2615** dx: harden openapi-ts drift-gate generator steps — silent-no-op guard, `::error::` annotation, entrypoint existence assertion for both `@ppt/api-client` and `@ppt/reality-api-client`.
- **#2616** dx: consolidate admin-web inline primitives onto `@ppt/ui-kit` — Stepper + FileUpload swaps in `TenantLifecyclePage.tsx` (117-line delta), added `vite-env.d.ts` for CSS-module ambient.

## Watch
- Stalled review: none (all 16 open PRs updated <=1d ago; 2 drafts opened this run — #2618 screen-map drift PR-2600 reconciliation; #2619 booking/mod.rs churn reduction).
- Reverted: none.
- Churn hotspots: `backend/servers/api-server/src/services/scheduler/mod.rs` 398 lines (runs_seen=1, intentional split via #2613); `frontend/apps/admin-web/src/pages/TenantLifecyclePage.tsx` 117 lines (runs_seen=1, ui-kit consolidation via #2616); `.github/workflows/api-validation.yml` 47 lines (runs_seen=1, CI hardening via #2615).
- Buffer-low alert (from run payload): `claimable=0/72` — dispatcher's action-list is starved; refill added 5 role-derived items this run (see PM section below).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **api-core** (reason: churn-aligned to `services/scheduler/mod.rs`)
- Experts: rust
- Findings: 0 — reviewer traced every `.expect(`/`unwrap_or_else(|e| e.into_inner())` hit in `scheduler/{mod,votes,retention}.rs` + neighbouring `services/*` and confirmed each was either inside `#[cfg(test)]`, intentional (mutex poisoning pinned by test at `scheduler/mod.rs:1955-1978`), or effectively infallible (`reqwest::Client::builder().build().expect(...)`, `Decimal::from_str("0.8").unwrap()`). No `unsafe`, no reachable `todo!()`/`unimplemented!()`, no findings crossed the "worth flagging" bar.
- Next segment: `reality-server` (oldest unreviewed after this run — last touched 2026-07-18)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: unchanged this run (ppt/ + reality/ populated; `mobile/` still empty → drift detection skipped for mobile)
- Drift signals this run: 0 (no merged PR touched a route trigger file — #2616's admin-web changes are outside the drift trigger set)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (no drift signals triggered)

## Backlog deltas
- **New (done-on-create):** [1] `refactor-churn-hotspot-scheduler-mod-rs-2026-08-01` — created and immediately resolved by merged PR #2613 (audit trail for the churn signal)
- **New (done-on-create):** [1] `refactor-churn-hotspot-tenant-lifecycle-page-2026-08-01` — created and immediately resolved by merged PR #2616
- **New (done-on-create):** [1] `refactor-churn-hotspot-api-validation-yml-2026-08-01` — created and immediately resolved by merged PR #2615
- **Resolved:** `dx-fixme-admin-web-ui-kit-primitives-missing` → done via PR #2616 (grep confirmed `TODO: replace with @ppt/ui-kit` patterns removed from admin-web)
- **Partially resolved:** `dx-fixme-api-client-generator-typescript-error-types` → left open with `[partial]` annotation — PR #2615 hardened the wrapper (silent-no-op guard, `::error::` annotation), but the "weak error types" half is upstream `@hey-api` template and byte-compared by the drift gate; out of reach of the workflow per PR body.
- **Decayed:** none (all open items updated within 14 days)
- **Dropped:** none

## Plans promoted
- none — all 7 open items are `score=2`; readiness bar requires `score>=3` (security fast-track needs `vector=security`, not applicable this run).

## Auto-fix
- `no candidate` — no signal met `confidence=high + score>=3 + allowlist` this run. The 3 new churn signals are `confidence=high` but their created items landed at `score=1` and immediately went to `done`.

## Project management (Phase 1.6)
- **Rotating role:** `pm-frontend` (last ran 2026-06-10). Cursor advanced to `pm-qa`.
- **Coverage upkeep:** re-checked `epic-7a` (Document Management, index 4/13); all 5 stories still `done`, `last_checked` bumped to 2026-08-01. Cursor advanced to epic index 5.
- **Scrum Master top blockers:** dispatcher buffer starved (`claimable=0/72`, `action-list.json` at 4 open / 34 in-progress / +5 added this run); 8a-2 / 8a-3 story gates reference open high-severity issue #480 (JWT-in-logs) yet the stories are marked `done`.
- **New action-list items:** 5 role-derived opens added (`pm-scrum-master-issue-480-ws-jwt-in-logs`, `pm-frontend-tenant-lifecycle-id-param-wiring`, `pm-frontend-jsonld-xss-escape-verify`, `pm-frontend-mobile-6-3-comment-thread-parity`, `pm-frontend-7a-3-permission-authoring-ui`); 4 new risks recorded in `risks.json`. See `.research/management/roles/pm-frontend.md` for full analysis.

## Self-review findings
- `findings.json` present: open=25 (high=5, medium=?, low=?); new this run=0; acknowledged=?.
- fp-? [recurrence=12] — Phase 6 archive-move produced 198KB archive too large for `mcp__github__push_files`; **fix:** push state commit to new branch → PR → server-side merge; carries any file size.
- fp-? [recurrence=8] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked; in this env direct push works when commit author email is `@users.noreply.github.com`; **fix:** replace hard-coded assumption with a runtime capability probe at Phase 1.
- fp-? [recurrence=3] — Phase 2.6 buffer reported `claimable=54/open=54` but real claimable=0 (all 54 were terminal in archive by id); **fix:** two-part — extend `gc1-reconcile.sh` to auto-close open items terminal in archive for ANY id shape incl. `failed`; fix GC3 claimable formula to subtract archive task_ids.
- fp-? [recurrence=2] — Tier-2 upstream planner kick returned `http=401`; DISPATCHER_TOKEN/DISPATCHER_URL rejected; **fix:** operator refresh of secrets + dispatcher-side branching on 401/403 as OPERATOR-CONFIG escalation.
- fp-? [recurrence=1] — action-list.json in-progress rows decoupled from live assignments (31 in-progress vs 3 live); **fix:** Phase-2 reconcile step to reset stale in-progress → open and add self-test T32 asserting bijection.

## Open questions
- Does `buildListingJsonLd` (`frontend/apps/reality-web/src/app/[locale]/listings/[slug]/jsonLd.ts`) escape `</script>` before inline embed, or does the #2600 hardening live elsewhere (layout component, shared sanitizer)? — surfaced by pm-frontend; new action item `pm-frontend-jsonld-xss-escape-verify` opened.
- Should epic-level status fields for `epic-10b` and `epic-80` be reconciled to `done` given all child stories are done? — surfaced by pm-scrum-master.

## Quiet day?
- **No.** 4 PRs merged, 3 new backlog rows, 1 resolved, 1 partially resolved, code-review pass on `api-core` (0 findings), Phase 1.6 rotating role (pm-frontend) + coverage upkeep, 5 new action-list items to counter the buffer-low alert. Telegram digest will send.
