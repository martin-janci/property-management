# 2026-08-01

## Since last run
- Merged PRs: 8 (range #2613–#2622)
- Open PRs touched: 0 (all 14 open PRs — 11 dependabot batch + 3 UC-ACC-05.x — last touched 2026-07-30, before the prior run)
- New / updated issues: 2 (#2621 opened as post-merge follow-up on PR #2549; #2617 closed as dup of #2575)
- Commits: 24 on `dev` (9 non-dispatcher: PRs above + one prior routine commit)
- Routine lag: 0d 15h since last run — on schedule
- Phases that failed: none (Phase 1.6 was skipped by design — see *Auto-fix* and *Open questions*, not a failure)

## Shipped
- #2613 refactor: extract scheduler vote-lifecycle jobs to `scheduler/votes.rs` — mod.rs shrinks 2525→2134; pure code-motion.
- #2614 test(api-server): outbound-webhook replay/timestamp integration test — closes #2485 by driving `notify_layout_change` over-the-wire and asserting `X-Webhook-Timestamp` freshness + timestamp-bound HMAC.
- #2615 dx-fixme(api-validation): harden openapi-ts drift-gate — `set -euo pipefail` + `::error::` annotation + post-generation `client.gen.ts` existence assertion (closes the silent-no-op false-green).
- #2616 dx-fixme(admin-web): consolidate `TenantLifecyclePage` stepper + file-upload onto `@ppt/ui-kit`; wires the token sheet (first admin-web consumer of ui-kit tokens).
- #2618 test-gap: reconcile reality-web `docs/screens/reality/home.md` + `listing-detail.md` with PR #2600 (inline-script serializeForScript hardening) — docs-only.
- #2619 refactor: booking/mod.rs decomposition (top-1 hotspot, runs_seen=3) into `messages.rs` / `client.rs` / `models.rs` / `error.rs` — pure move; `pub use` preserves external paths.
- #2620 feat(epic-84): story 84.5 pgvector RAG observability — histograms/counters via `metrics` facade in `db/repositories/rag_metrics.rs` (no behavior change; recorder-free unit path added).
- #2622 docs(reality-data): mobile-native (KMP) analytics-parity audit — surfaces the 3 uninstrumented funnels (listing view / search / contact-inquiry) with commonMain wire-points for a pm-mobile follow-up.

## Watch
- No reverts this run.
- No stalled reviews (>7 days, no reviewDecision).
- **Churn hotspots this run:** `backend/crates/integrations/src/booking/mod.rs` (1490 lines, runs_seen=4 — decomposition PR #2619 shrunk this file, expect stability now), `booking/messages.rs` (605, first sighting — from the split, not organic), `booking/client.rs` (552, first sighting — same). All three cluster onto PR #2619; the hotspot itself is being *resolved*, not accumulating.
- No new TODO/FIXME markers in merged code this run.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **reality-server** (reason: oldest-unreviewed at 2026-07-18, 13 days stale)
- Experts: rust-backend (static-only scan of `backend/servers/reality-server/src/`)
- Findings: 3 (all `high` confidence — see *Backlog deltas*)
- Next segment: **api-core** (last reviewed 2026-07-20, next oldest)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` 106 + `reality/` 47; `mobile/` not yet seeded → 0)
- Drift signals this run: 0 (PR #2618 pre-emptively reconciled the last drift; PR #2620 is backend-only, PR #2622 docs-only, PRs #2613–#2616 don't touch route files)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (not required by any gate; PR #2618's docs-only change ran the workflow at merge time)

## Backlog deltas
- **New:** [3] `code-review-reality-server-agency-invitation-idor` — SECURITY, high — reality-server `create_invitation`+`accept_invitation` lack membership + email checks (any auth'd user joins any agency at any role). Promoted to plan.
- **New:** [2] `code-review-reality-server-paginated-total-truncated` — BUG, high — 3 reality-server routes return `.len()` after `LIMIT/OFFSET` as `total`; pagination lies.
- **New:** [2] `code-review-reality-server-invitation-idor-test-gap` — TEST-GAP, high — `agencies_authz_tests.rs` covers only 401 paths for create/accept invitation; the two missing cases (non-member-create, email-mismatch-accept) are what let the IDOR above slip through.
- **New:** [1] `refactor-churn-hotspot-booking-messages-2026-08-01` — refactor — new 605-line submodule from PR #2619 decomposition. Will decay in 14 days if no further churn.
- **New:** [1] `refactor-churn-hotspot-booking-client-2026-08-01` — refactor — new 552-line submodule from PR #2619 decomposition. Will decay in 14 days.
- **Bumped:** none (all matched signals were on already-terminal rows; churn re-emits are cumulative, score_delta suppressed per G4).
- **Decayed:** none (backlog has zero open items pre-run — every previously-open vector converged before today).
- **Dropped:** none.

## Plans promoted
- `plans/security-reality-server-agency-invitation-idor.md` — Add `check_agency_membership` to `create_invitation`, add `principal.email == invitation.email` check to `accept_invitation` (inside the accept-transaction), validate `role` against an allowlist, land two red `#[sqlx::test]` cases in `agencies_authz_tests.rs`. Security fast-track (score 3, high confidence). · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal matched the Phase 5 safe-shape allowlist this run. The reality-server IDOR is `code-review-finding` (not on the allowlist — needs manual code fix + tests, not a stub/orphan/lockfile-bump). The two new churn-hotspot rows (booking/messages, booking/client) are score-1 (below the ≥3 certainty bar) and are also not on the allowlist.

## Self-review findings
- open=25 (high=5, medium=17, low=3); new this run=0; acknowledged=19
- fp-null [recurrence=1] — Phase 6 archive-move produced a 198KB assignments-archive.json; mcp__github__push_files cannot inline a file that large (and git push is proxy-403'd), so the archive could not be pushed; fix: push the dispatcher state commit to a NEW branch (new-branch push is NOT proxy-blocked), open a PR via mcp__github__create_pull_request, then land it with mcp__github__merge_pull_request (squash).
- fp-null [recurrence=1] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked; in this env direct `git push origin dev` SUCCEEDED (only GH007 email-privacy blocked it); fix: runtime probe at Phase 1 for git push capability, set PUSH_METHOD=git on success and bypass the 64KiB mcp-push-size-guard.
- fp-null [recurrence=1] — Phase 2.6 buffer reported claimable=54/open=54 but REAL claimable=0; all 54 open action-list items already terminal in assignments-archive; fix: extend gc1-reconcile.sh to auto-close open action-list items whose EXACT task_id is terminal in active+archive for ANY id shape, INCLUDING status=failed; also fix Phase 2.6 buffer-guard open_count + GC3 claimable formula to subtract archive task_ids.
- fp-null [recurrence=1] — Tier-2 upstream planner kick returned http=401; DISPATCHER_TOKEN/DISPATCHER_URL rejected; fix: operator rotate DISPATCHER_TOKEN; dispatcher branch on the HTTP status to surface 401/403 as OPERATOR-CONFIG escalation.
- fp-null [recurrence=1] — action-list.json holds 31 status=in-progress rows but assignments.json has only 3 live rows; the 3 truly-active assignments are marked status=open (not in-progress) in action-list; fix: Phase-2 reconcile step to reset stale in-progress → open + set action-list twin → in-progress on claim; self-test T32 asserting bijection.

## Open questions
- **Phase 1.6 (PM synthesis) skipped this run** — `pm_cursor.next_index=2` (`pm-frontend`, last-run 2026-06-10, 52 days stale) is meaningfully overdue, but the full skill was skipped to stay within the run's token budget after the reality-server review found a promotable HIGH-severity item. G16 becomes a no-op per spec (pm_cursor + coverage_cursor unchanged), but the operator should either run `/loop pm-full` for a full sweep or let the next daily run pick up `pm-frontend` before the gap widens further. `coverage_cursor.next_index=4` (epic-7a).
- **Trigger payload `buffer-low: claimable=3/72 — refill planner`** was received and handled by promoting one real security plan into `plans/`. The dispatcher planner still owns action-list refill; this run added 5 rows to `backlog.json` but only 1 flows to a plan (dispatcher reads plans, not backlog rows directly). If the operator wants deeper refill, a `/loop deep` (30-day sweep) would surface more; today's since-last-run window was too narrow after yesterday's aggressive dispatcher-driven merges.

## Quiet day?
- **No** — 8 PRs merged, 5 new backlog vectors (including a HIGH-severity security IDOR), 1 plan promoted, 3 code-review findings landed. The routine advanced substantially on `.research/state.json`, `backlog.json`, `plans/`, `signals/`, and `hotspot_history`.
