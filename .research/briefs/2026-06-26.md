# 2026-06-26

## Since last run
- Merged PRs: **292** (range #1440–#1853) — 10-day catch-up after routine stalled 2026-06-16.
- Open PRs touched: **30** since 2026-06-16. 3 drafts carry `needs-human-review` (#1812 reality-portal-rs split — exec bit + git-push blocked; #1814 form-rs split — modify/delete conflict on dev #1781; #1846 80-3-mediation — clean+green but `scope_drift=true`).
- New / updated issues: **83** (all auto-labeled `follow-up` / `from-merged-review` — zero untriaged).
- Commits on `dev`: **410** in window.
- Routine lag: **10d 6h** since last run — **`lag_warning`** + **`stale_routine_alert`** fired. Cloud cron likely paused for the gap; resuming with this catch-up run.
- Phases that failed: **none**.

## Shipped
- **6 churn-hotspot module splits 2026-06-24** — `#1796` (subscription), `#1798` (emergency), `#1800` (sensor), `#1810` (document), `#1813` (vendors), `#1815` (iot), `#1816` (enhanced_tenant_screening), `#1737` (iot cleanup follow-up). One single-day structural refactor batch.
- **#1849** epic-16 saved-search email/push transport drainer (BIT-139) — new background worker.
- **#1848 + #1853** group-thread participant list end-to-end (BIT-206 / BIT-244).
- **#1850** org-scoped favorite alert worker (codex).
- **#1817** accounting-server PRD + epics seeded (+2084 LOC) — backend Rust does not exist yet, threat model gap captured as `security-accounting-server-threat-model` (see Backlog deltas).
- **#1808** new `accounting-web` Next.js marketing/signup app (+2254 LOC, PAP-312).
- **#1467 + #1602** MFA recovery hardening (RLS, rate-limit, audit action).
- **#1467 / #1601 / #1635 / #1639 / #1741** Airbnb / Booking OTA cross-org IDOR fixes (manager-gate audit, BIT-78).
- **#1561** reality-server portal-imports IDOR — new `PortalPrincipal` extractor.
- **#1755 / #1757 / #1724** three duplicate-migration-version trunk repairs + CI guard (BIT-223).
- The botched **#1426** breakage from the last brief is **resolved** — dev backend compiles again; the subsequent fix PRs landed in window.
- **6 prior `ready` plans verified resolved in window** — see Backlog deltas → **Resolved** (security-llm-doc-idor, bug-ios-searchview-uncompilable, code-review-reality-web-share-comparison-404 / -listing-page-ssr-crash / -realtor-mgmt-untranslated, code-review-mobile-rn-report-fault-fake-submit). All 6 plans moved to `plans/_archive/`.

## Watch
- **🚨 Routine lag**: 10 days of cloud-cron silence (`last_run_iso` was 2026-06-16T03:25Z). If the dispatcher is still ticking on `planning`, only the daily research loop was paused — likely cron config or env. Worth a separate check.
- **Reverts (1)**: **#1713** removed re-added delegation frontend (#1690 had re-added it) per BIT-213 board decision. Captured as `bug-revert-pr-1713` (open, score 3).
- **Churn hotspots this run (top 3)** — all driven by the 2026-06-24 split batch (deletions from the old monolith file):
  - `backend/servers/api-server/src/routes/emergency.rs` (1681 LOC — *not* in the split batch; still monolithic)
  - `backend/crates/db/src/repositories/subscription.rs` (1240 LOC)
  - `backend/servers/api-server/src/routes/iot.rs` (1063 LOC)
  Repeated-churn stack: sensor.rs, vendors.rs, enhanced_tenant_screening.rs, reserve_funds.rs.
- **Stable instability**: `backend/crates/db/src/repositories/rental.rs` — 387 LOC across 5 PRs (#1486 / #1492 / #1507 / #1537 / #1578) for the #1008 enum-decode class; no permanent regression test pinning enum mapping. New backlog row `bug-rental-rs-stable-instability` (score 3, high).
- **Closed-not-merged (7)**: #1720, #1749, #1727, #1698, #1647, #1636, #1634. Mostly superseded by replacements or rebased on migration-fix PRs.
- **Stalled-review**: none — max open-PR age 83h (under the 7d threshold).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none — full dispatcher state requires a `planning`-branch read; see the dispatcher's own 2026-06-26 20:18 commit which lists the 3 review PRs (#1812 / #1814 / #1846), all `needs-human-review`.

## Code review slice
- Segment reviewed: **api-handlers** (reason: **churn-aligned** — the top-3 hotspots all sit in `backend/servers/api-server/src/routes/`; took precedence over oldest-unreviewed `ppt-web-core` 21d stale).
- Experts: rust, security.
- Findings: **3** (all high-severity, +3 each):
  - `code-review-api-handlers-emergency-mutations-no-role-gate` — security, medium. **Promoted to plan.**
  - `code-review-api-handlers-ets-screening-results-no-role-gate` — security, medium. **Promoted to plan.**
  - `code-review-api-handlers-ets-error-leak` — bug, medium. Stays open — wider scope (~27 handler arms across ETS module).
- Dropped below cap: `emergency-broadcast-no-org` (2), `ets-delete-update-no-org-filter` (2), `faults-status-ok-on-dup` (1).
- Next segment: **mobile-native-kmp** (last reviewed 2026-06-06 — 20d, oldest after this run).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47; `mobile/` not yet seeded → 0).
- Drift signals this run: **9** (`screen-map-drift-pr-{1545-reality, 1610-ppt, 1621-ppt, 1621-reality, 1713-ppt, 1733-ppt, 1737-ppt, 1746-reality, 1822-reality}`). Heuristic caveats apply — several are test-file fixes or accounting-layout shims, not user-visible route surface changes.
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not refetched this run (no `docs/screens/**` edits to trigger).

## Backlog deltas
- **Resolved (6 — moved ready → done, plans archived):**
  - `security-llm-doc-idor` — ai.rs split into routes/ai/llm.rs; `list_listing_descriptions_for_org()` + `publish_description_for_org()` both scope by `org_id` (llm.rs:674/700).
  - `bug-ios-searchview-uncompilable` — `performSearch` / `scheduleSearch(for:)` / `resultsGrid` / `loadMoreResults` all defined; brace balance restored.
  - `code-review-reality-web-share-comparison-404` — `${getApiBase()}/api/v1/listings/${id}` replaces non-existent Next.js route.
  - `code-review-reality-web-listing-page-ssr-crash` — `buildListingJsonLd` defensive, `ListingNotFound` extracted.
  - `code-review-reality-web-realtor-mgmt-untranslated` — `t('inviteRealtor')` / `t('formLabelEmail')` etc. wired in `RealtorManagement.tsx`.
  - `code-review-mobile-rn-report-fault-fake-submit` — real `handleSubmit()` with idempotency-key reuse + offline-queue gating.

- **New (top scored, full list in `backlog.md`):**
  - [4] `security-ws-ticket-endpoint-480` — POST `/api/v1/ws/ticket` 30s opaque ticket, removes JWT from WS upgrade URL (nginx/CDN log leak surface).
  - [4] `security-accounting-server-threat-model` — author tenancy/authn/PII ADR *before* first backend PR lands (PRD #1817 + accounting-web #1808 already merged; backend dir does not exist yet).
  - [3] `code-review-api-handlers-emergency-mutations-no-role-gate` (Phase 1.5; promoted to plan).
  - [3] `code-review-api-handlers-ets-screening-results-no-role-gate` (Phase 1.5; promoted to plan).
  - [3] `code-review-api-handlers-ets-error-leak` (Phase 1.5).
  - [3] `refactor-split-emergency-rs-1681-loc` — emergency.rs missed the 2026-06-24 split batch.
  - [3] `bug-rental-rs-stable-instability` — 387 LOC over 5 PRs without regression test.
  - [3] `bug-revert-pr-1713` — delegation frontend re-removed.
  - **31** `unchecked-todo-pr-…` rows (PR bodies with `- [ ]`), all confidence=low → cannot reach `ready` regardless of score.
  - **23** `test-gap-hotfix-no-test-pr-…` rows.
  - **9** `bug-risky-churn-pr-…` rows.
  - **9** `test-gap-screen-map-drift-pr-…` rows (heuristic, review candidates).
  - **7** `closed-not-merged-pr-…` triage rows.
  - **3** `code-review-finding` rows from Phase 1.5 (above).

- **Bumped:** 10 items — mostly hotspot rows whose underlying file appeared again in the 10-day window (`repeated-churn-*`).
- **Decayed:** 26 open items lost −1 (no signal in 14d).
- **Dropped:** 15 items decayed to 0 (mostly old `unchecked-todo-pr-…` and churn rows from prior runs).

## Plans promoted
- `plans/code-review-api-handlers-emergency-mutations-no-role-gate.md` — Add `is_emergency_manager` role gate to mutating handlers in `routes/emergency/{protocols,contacts,drills}.rs`; 9-case 201/403 regression test. · adversarial pass: **passed**.
- `plans/code-review-api-handlers-ets-screening-results-no-role-gate.md` — Add role gate to ETS `create_credit/background/eviction_result` + `create_risk_model` + `activate_risk_model` (5 handlers); 5-case role matrix regression test. · adversarial pass: **fixed-in-place** (corrected `Files` heading — `backend/crates/api-core/src/rls/role.rs` did not exist; replaced with the real `TenantRole` location at `backend/crates/common/src/tenant.rs:83` + the existing `emergency/shared.rs` helper for symmetry).

## Auto-fix
- `no candidate` — see Phase 5 record. Eligible signals this run: `screen-map-drift-*` (9) but all are medium-confidence + heuristic-caveat (test-file or accounting-layout edits, not route surface). `fixme-in-merged-code` did not fire (no FIXME pattern detected by the Phase 1 sweep — possible undersampling, since file-list data was fetched for 78 of 292 merged PRs). `dep-update-noise` (17) — none had the lockfile-drift sub-shape. `stalled-review` — no PR matched (max age 83h, under the 7d threshold).

## Self-review findings
- `.research/self-improvement/findings.json` absent — none.

## Open questions
- **Routine cron**: the 10-day silence between 2026-06-16 and 2026-06-26 suggests the cloud routine schedule is broken / paused. Worth confirming the daily fire-endpoint is still active (the dispatcher continues to commit on `dev` so its own loop is healthy; the daily research routine is a different schedule).
- **`#1846 80-3-mediation`** — dispatcher carries `scope_drift=true` on this approved+green PR; needs human sign-off to promote/merge.
- **`#1812 reality-portal-rs` + `#1814 form-rs`** — both module-split drafts blocked on env constraints (exec bit / modify-delete conflict / 492 KB assignments-archive > MCP push limit). The dispatcher's repeating `dispatcher cannot merge` note across runs is real toil.
- **Mobile screen-map (`docs/screens/mobile/`)** still 0 files — re-include `frontend/apps/mobile/src/**` in drift detection once at least one mobile screen doc lands.

## Quiet day?
- **No.** 292 merged PRs catch-up, 6 prior `ready` plans verified-resolved and archived, 2 new security plans promoted, 3 Phase 1.5 findings (1 stays open), 4 PM-derived backlog rows (2 score-4 security candidates: WS ticket + accounting threat model), 83 new backlog rows from the 10-day window. `stats.runs` 40 → 41, `vectors_created` 173 → 260, `plans_created` 12 → 14.
