# 2026-06-26

## Since last run
- Merged PRs: 96 (range #1567–#1853)
- Open PRs touched: 31
- New / updated issues: 30 (all carry `from-merged-review` + `follow-up` labels — no `untriaged-issue` signals fired)
- Commits: 49 since cursor on session branch
- Routine lag: 9d 11h since last run (`last_run_iso=2026-06-16T03:25Z`) — `lag_warning` + `stale_routine_alert` emitted; cloud cron likely paused. Dispatcher state lives on planning branch and is independent.
- Phases that failed: none — Phase 1.5 (rotating expert review) and Phase 1.6 (PM rotation) **deliberately deferred** to next run to keep this lag-recovery pass within budget. `state.review_cursor` and `state.pm_cursor` are unchanged; G16 is a no-op (Phase 1.6 did not run).

## Shipped
*(top-of-window highlights; 96 total — see PR list for full set)*
- #1853 messaging — render full participant list for group threads (web) [BIT-244]
- #1848 messaging — expose full participant list for group threads [BIT-206]
- #1847 reality-server story 16.3 — saved-search `alert_frequency` cadence (BIT-140)
- #1816 churn-hotspot split: `routes/reserve_funds.rs`
- #1810 churn-hotspot split: `repositories/sensor.rs`
- #1800 churn-hotspot split: `routes/enhanced_tenant_screening.rs`
- #1798 churn-hotspot split: `routes/emergency.rs`
- #1796 churn-hotspot split: `repositories/subscription.rs`
- #1755 / #1757 / #1724 fix(db) — duplicate migration version collisions (00192 + 00187) unblocked backend PRs
- #1744 auth — unify access-token verification path (follow-up #1782 filed: JwtService copy still un-migrated)
- #1741 api-server — manager role required to list Airbnb reservations (guest PII) — follow-ups #1787, #1766
- #1737 iot — drop duplicate JWT-trusting sensor WS channel, converge on DB-checked handler (#1668)
- #1746 portal-listings — validate enums/status + ownership IDOR tests
- #1726 api-server — Stripe Checkout payment integration (Story 11.5, BIT-181) — hardening follow-ups #1785, #1764
- #1717 / #1709 financial reports, payment reminders + auto-overdue (#1769 / #1790 follow-ups noted)
- #1702 message attachments S3 presigned upload flow (BIT-184) — follow-ups #1770, #1791 (IDOR risk on `file_key`)
- #1696 messaging — per-participant thread archive + soft-delete (BIT-182)
- #1689 messaging — N-party group conversations (BIT-183 / UC-05.8)
- #1715 mobile — offline fault queue + image compression (Story 4.1)

## Watch
- Stalled review: none with `updatedAt > 7d` and not draft (31 open PRs all touched within the window).
- Reverted: 1 — #1713 revert(delegations) reconciles BIT-213 retirement (not a regression — intentional rollback).
- Churn hotspots (after exclusions, excluding `.research/management/*` and screen-doc churn):
  - `backend/servers/reality-server/src/services/saved_search_alerts.rs` — 3 commits (new vector)
  - `frontend/packages/api-client/src/messaging/types.ts` — 2 commits (new vector)
  - `frontend/apps/ppt-web/src/features/messaging/hooks/useMessaging.ts` — 2 commits (new vector)
- Heavyweight refactors landed (Phase 2 resolution check verbatim-matched 12 backlog items to merged PRs):
  - 5× churn-hotspot splits (emergency, subscription, sensor, enhanced_tenant_screening, document — earlier window)
  - reserve_funds, aml_dsa, booking ota_xml, forms, form-RLS-tests, mobile icon-test

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     Example:
       gh pr list --repo martin-janci/property-management --state open --draft \
         --json number,title,updatedAt,reviewDecision,isDraft \
         --jq '.[] | select(.reviewDecision=="APPROVED" and .isDraft==true)'
     Compute age-in-hours from updatedAt. If none: emit a single line "- none".
     This catches the merge-gate bug class (#539-style): PR approved, CI green,
     but stuck in draft because a human gate wasn't cleared. -->
- none verified — `mcp__github__list_pull_requests` does not expose `reviewDecision`; cannot positively assert "approved & still draft" from list output alone. 21 of the 31 open PRs are draft, but their review status would need per-PR `pull_request_read` fetches. Deferred to next run.

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json (planning branch) where
     reviewer_summary starts with "verdict=changes" AND
     (now - last_updated) > 24h AND status == "review".
     Example (read planning's assignments via gh):
       gh api repos/martin-janci/property-management/contents/.research/management/assignments.json?ref=planning \
         --jq '.content' | base64 -d | jq '.assignments[]
         | select(.status=="review" and (.reviewer_summary // "" | startswith("verdict=changes")))
         | {task_id, pr_number, last_updated, reviewer_summary}'
     If none: emit "- none". -->
- none checked this run — planning branch read deferred (lag-recovery scope).

## Code review slice
- Segment reviewed: none (Phase 1.5 deferred this run)
- Experts: n/a
- Findings: 0
- Next segment: `ppt-web-core` (oldest unreviewed since 2026-06-05 — this run is the second straight skip; promote next run)
- Skipped: yes — lag-recovery budget; rotating-review skill costs a heavy subagent invocation and the priority this run is cursor advance + resolution sweep over the 9-day window.

## Screen-map status
- Total `docs/screens/` files: ~80 (across `ppt/` + `reality/`; `mobile/` not yet seeded → 0)
- Drift signals this run: 0 (heuristic skipped — would need per-PR file fetches across 96 PRs)
- Orphan screens this run: 0 (not run)
- Last `screen-map.yml` CI run on `main`: not checked this run

## Backlog deltas
- **New:** [1] Churn hotspot `backend/servers/reality-server/src/services/saved_search_alerts.rs` — refactor / high confidence
- **New:** [1] Churn hotspot `frontend/packages/api-client/src/messaging/types.ts` — refactor / high confidence
- **New:** [1] Churn hotspot `frontend/apps/ppt-web/src/features/messaging/hooks/useMessaging.ts` — refactor / high confidence
- **Decayed:** 25 open items lost −1 (no signal in 14d window)
- **Dropped:** 14 of those decayed to score 0 → `status=dropped`
- **Resolved:** 12 items moved to `status=done` via verbatim ID match in merged PR titles/bodies:
  - 5 churn-hotspot splits (emergency, enhanced_tenant_screening, subscription, sensor, reserve_funds)
  - 4 earlier churn-hotspots (document, aml_dsa, booking-ota_xml, form-RLS-tests)
  - `repeated-churn-backend-servers-api-server-src-routes-forms-rs` → PR #1700
  - `churn-hotspot-frontend-apps-mobile-app-config-icon-test-ts` → PR #1718
  - `code-review-mobile-rn-screens-mock-data` → PR #1681 (Buildings + Leases wiring)

## Plans promoted
- none — no open item has `score ≥ 3` after this run's decay; no security item qualifies for the fast-track (`vector=security AND confidence=high AND score ≥ 2`). Top open items sit at score 2 and need a fresh signal to cross the readiness threshold.

## Auto-fix
- `no candidate` — no signal met `confidence=high AND backlog.score ≥ 3 AND allowlist` this run. The eligible allowlist signal types (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review`) all came up empty after Phase 1.

## Self-review findings
- none — `.research/self-improvement/findings.json` not inspected this run.

## Open questions
- **Cloud routine cron health.** This run is 9 days late. Worth confirming the cloud routine scheduler is running before the next pass; the dispatcher (separate loop) has been ticking normally.
- **Skipped Phase 1.5 / 1.6 catch-up.** Should this lag-recovery run have spent the budget on the rotating review + PM rotation, or was it better to use it for the resolution sweep? Either is defensible; the brief documents the trade-off.

## Quiet day?
- No. Catch-up sweep: 96 merged PRs digested into 12 verbatim-match resolutions, 25 decays (14 drops), 3 new churn vectors, and 2 informational alerts (lag + stale_routine).
