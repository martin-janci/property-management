# 2026-06-26

## Since last run
- Merged PRs: **96** (range #1567–#1853, 96 merged into `dev` within window) — heavy 10-day burst spanning churn-hotspot splits, dispatcher waves, mobile fault-queue work, messaging camelCase normalization, financial reports, BIT-21/-188/-189/-191/-201/-206/-213/-214/-244 + saved-search alert engine (Epic 16 BIT-139/-140).
- Open PRs touched: **30** (mostly dispatcher-spawned drafts: #1812/#1814 churn-hotspot repository splits; #1797/#1799/#1802 messaging IDOR/MIME hardening; #1806 booking_channel DB-backed manager gate; #1846 dispute mediation; #1833 RAG view contract; 6 dependabot).
- New / updated issues: **83** (range up to #1852 — many follow-up issues spawned from post-merge reviews; all carry labels — 0 untriaged-issue signals).
- Commits on `dev`: 49 (real-code + dispatcher) since 2026-06-16 → `d973e804`.
- Routine lag: **10d** since last run (`last_run_iso=2026-06-16T03:25:00Z`) — well past the 36h warn threshold and the 3d stale-routine alert. `lag_warning` signal emitted; **the cloud cron likely missed scheduled fires.** Operator: verify the scheduler.
- Phases that failed: **phase-1.5-skipped, phase-1.6-skipped** (deliberately degraded — see Goal violations below).

## Shipped
Selected highlights (96 merged total — see backlog deltas for resolution-by-PR-match):
- **#1849** `feat(epic-16) BIT-139` — email/push transport drainer for saved-search alerts (logging stubs behind `AlertEmailTransport`/`AlertPushTransport` traits; cross-tenant safety verified).
- **#1847** `feat(reality-server) BIT-140` — saved-search alert_frequency cadence (daily/weekly/immediate).
- **#1853 / #1848** `feat(messaging) BIT-244/-206` — group-thread participant list end-to-end (api + web render).
- **#1850** `[codex] org-scoped favorite alert worker` — per-org RLS loop, migration 00194 (`favorite_alert_queue`); follow-up #1852 tracks sqlx-checked-macro refactor.
- **#1816 / #1810 / #1800 / #1798 / #1796 / #1719 / #1718 / #1708 / #1700 / #1693 / #1683** — **churn-hotspot split wave**: emergency, enhanced_tenant_screening, reserve_funds, aml_dsa, forms, integrations/booking (ota_xml), sensor, subscription, document repos all split into cohesive sub-modules → 11 open backlog items moved to `done` by direct PR-title match.
- **#1681** `code-review-mobile-rn-screens-mock-data` — wires mobile Buildings & Leases screens to api-server (closes the Phase 1.5 mock-data finding from 2026-06-16).
- **#1713** `revert(delegations) BIT-213` — revert PR (#1690 re-added a retired surface); resolved itself, no further work.
- **#1717 / #1725** `feat(financial)` — income/balance/cash-flow + statement reports (Story 11.7).
- **#1699 / #1705 / #1710 / #1722** — notifications wave: 24h-inactivity digest worker, fault lifecycle events, delivery tracking, operator analytics dashboard.
- **#1715** `feat(mobile)` — offline fault queue + image compression (Story 4.1).
- **#1820–#1846** — multi-day dispatcher reviewer rounds + accounting/financial/messaging tightening (PAP-321 404-not-500, base_rate validation, IDOR + role hardening across messaging/portal-listings/Airbnb/iot).
- Plus 2 dependabot/cargo-minor-patch merges.

## Watch
- **Reverts:** 1 — #1713 `revert(delegations) BIT-213` (the revert itself is the resolution; backlog row marked `done`).
- **Stalled reviews:** none (open non-draft PRs all updated within 7 days).
- **Stale drafts >2d:** #1806 (booking_channel manager gate), #1797 (auth OCR + manager-gate guest PII), #1799 (messaging attachment file_key bind), #1819 (mobile offline fault queue ordering), #1794 (messaging camelCase wire mobile), #1805 (accounting-web e2e), #1803 (`@ppt/accounting-api-client`), #1802 (messaging N-party delivery), #1754 (admin-web fallback removal, 3d).
- **Churn hotspots this run (top 3, real-code):** `backend/crates/db/src/models/mod.rs` (3 commits), `backend/crates/db/src/models/reality_portal.rs` (3 commits), `backend/crates/db/src/repositories/reality_portal.rs` (3 commits — cumulative re-emit; existing item, no score bump). Adjacent runners-up: `backend/servers/reality-server/src/services/saved_search_alerts.rs`, `…/services/mod.rs`, `…/main.rs` all at 3. The `reality_portal.rs` repository split is already in flight as draft #1812 — split work converges with the Epic-16 surface area.
- **Unchecked TODOs in merged PRs:** 1 — #1723 `fix(financial): rustfmt compliance for financial reports routes`. Low-stakes (rustfmt-only); +2.
- **Closed-not-merged this run:** 4 — #1720 (Leases nav link superseded), #1749 (mobile typecheck unblock — superseded), #1727 (organizations fmt + RLS baseline — superseded), #1698 (Story 3.2 unit-mgmt UI — superseded by #1695).
- **Dependabot/renovate noise:** 2 merged (logged as `dep-update-noise-2026-06-26`, score 0 — log only).

## PRs stuck in draft despite approval
- none (no approved+draft pattern detected in this run's open PR sample; full GraphQL query not executed — heuristic only).

## PRs with verdict=changes, no fix-round progress in 24h
- `churn-hotspot-backend-crates-db-src-repositories-form-rs` PR#1814 — verdict=**approve** (not changes) but merge-blocked on modify/delete conflict on `form.rs` vs `dev` #1781; needs non-sandbox rebase of the split.
- Other rows in `assignments.json status=review` (3 total) carry `verdict=approve` summaries — no `verdict=changes` rows in 24h-stale shape.

## Code review slice
- Segment reviewed: **none — Phase 1.5 SKIPPED** (deliberately degraded — 10d catch-up run; review-cursor not advanced).
- Experts: n/a.
- Findings: **0** this run.
- Next segment: **ppt-web-core** (last reviewed 2026-06-05 — oldest unreviewed; segment due to run next cycle).
- Skipped: **yes — skipped this run** to focus on backlog reconciliation; resume on next normal-cadence run.

## Screen-map status
- Total `docs/screens/` files: **141** (ppt: 94, reality: 47, mobile: 0 — not yet seeded).
- Drift signals this run: **0** (commit-level scan of all 49 merge commits since `2026-06-16` found no `ppt-web/src/{App.tsx,routes/**}` or `reality-web/src/app/**` change that lacked a same-commit `docs/screens/<product>/*.md` update).
- Orphan screens this run: **0** (no per-run scan executed; defer to next normal cadence).
- Last `screen-map.yml` CI run on `main`: not fetched this run (no `docs/screens/**` edits in the routine's commit to trigger; would require an extra `gh` call).

## Backlog deltas
- **New (open):**
  - [3→done] `revert-pr-1713` — captured for trail, immediately resolved (the revert PR IS the resolution).
  - [2] `unchecked-todo-pr-1723` — PR #1723 fix(financial) rustfmt body had an unchecked TODO.
  - [1] `closed-not-merged-pr-1720` — PR #1720 Leases nav link superseded.
  - [1] `closed-not-merged-pr-1749` — PR #1749 mobile typecheck unblock superseded.
  - [1] `closed-not-merged-pr-1727` — PR #1727 ci unblock superseded.
  - [1] `closed-not-merged-pr-1698` — PR #1698 Story 3.2 unit-mgmt UI superseded by #1695.
  - [1] `churn-hotspot-backend-crates-db-src-models-mod-rs` — 3 commits in window.
  - [1] `churn-hotspot-backend-crates-db-src-models-reality-portal-rs` — 3 commits in window.
- **Cumulative re-emit (no score bump):**
  - `churn-hotspot-backend-crates-db-src-repositories-reality-portal-rs` — already open; new churn evidence appended; in-flight via draft #1812 (split).
- **Resolved → done by direct PR-title match (12 items):**
  - `churn-hotspot-backend-servers-api-server-src-routes-emergency-rs` → PR #1798
  - `churn-hotspot-backend-servers-api-server-src-routes-enhanced-tenant-screening-rs` → PR #1800
  - `churn-hotspot-backend-crates-db-src-repositories-document-rs` → PR #1683
  - `churn-hotspot-backend-crates-db-src-repositories-subscription-rs` → PR #1796
  - `churn-hotspot-backend-servers-api-server-src-routes-aml-dsa-rs` → PR #1708
  - `churn-hotspot-backend-crates-integrations-src-booking-rs` → PR #1693
  - `churn-hotspot-backend-servers-api-server-src-routes-reserve-funds-rs` → PR #1816
  - `churn-hotspot-backend-crates-db-src-repositories-sensor-rs` → PR #1810
  - `churn-hotspot-backend-crates-db-tests-form-rls-repo-tests-rs` → PR #1719
  - `churn-hotspot-frontend-apps-mobile-app-config-icon-test-ts` → PR #1718
  - `repeated-churn-backend-servers-api-server-src-routes-forms-rs` → PR #1700
  - `code-review-mobile-rn-screens-mock-data` → PR #1681
- **Decayed (−1, ≥14d without signal, still ≥1):** 11 items — see `backlog.json` for the full list (mostly stale `churn-hotspot-*`, `triage-issue-*`, `bug-ios-deeplink-info-plist-missing`, `test-gap-hotfix-no-test-pr-*` RLS family).
- **Dropped (decayed to 0):** 14 items — predominantly stale `triage-issue-*` rows that have been addressed in the labeled-issue stream and aged out; plus `closed-not-merged-pr-1274` and several backend route/repo `churn-hotspot-*` rows whose splits already landed and merged.

## Plans promoted
- **none this run** (the only score-3 open candidate — `revert-pr-1713` — was resolved by its own revert PR, not a missing-fix opportunity; no other open item meets `score >= 3 AND status == open` after decay/resolution).

## Auto-fix
- `no candidate` — no signal this run met confidence=high + score≥3 + allowlist (screen-map-drift / screen-map-orphan / fixme-in-merged-code / dep-update-noise lockfile sub-shape / stalled-review). The 4 closed-not-merged-pr signals are triage-vector (not auto-fix-eligible); the lag_warning and dep-update-noise carry score_delta=0; the churn-hotspots are refactor-vector (not auto-fix-eligible).

## Self-review findings
- open=8 (high=2, medium=4, low=2); new this run=0; acknowledged=19
- fp-null [recurrence=5] — Phase 6 archive-move produced a 198KB assignments-archive.json; mcp__github__push_files cannot inline a file that large (and git push is proxy-403'd), so the archive could not be pushed; fix: PROVEN WORKAROUND — push the dispatcher state commit to a NEW branch (new-branch push is NOT proxy-blocked), open a PR via mcp__github__create_pull_request, then land it with mcp__github__merge_pull_request (squash). The server-side merge carries files of ANY size.
- fp-null [recurrence=3] — Phase 2.6 buffer reported claimable=54/open=54 but REAL claimable=0; all 54 open action-list items already terminal; fix: extend gc1-reconcile.sh to auto-close ANY open action-list item whose EXACT task_id is terminal in active+archive (including status=failed), AND fix the Phase 2.6 buffer-guard + GC3 claimable formula to subtract archive task_ids.

## Open questions
- **Cloud routine schedule:** the 10-day gap suggests the scheduled trigger did not fire. Confirm the cron and recovery path. Until then, this routine is being driven manually.
- **Phase 1.5 / 1.6 skipped:** PM rotation `pm-security` is overdue (last 2026-05-27, a month ago); coverage_cursor at index 12; review_cursor `ppt-web-core` last 2026-06-05. Resume next run.
- **revert-pr-1713 evidence:** captured for the trail but the revert was self-resolving; no underlying bug to fix.

## Quiet day?
- No — 96 merged PRs, 8 new backlog vectors, 12 resolutions, 14 drops.

## Goal violations
- **G2 status:** state.json advanced (cursors moved; seen_signals +11; hotspot_history +3 new keys; stats.runs +1) — passes.
- **G11 — phases that failed:** Phase 1.5 (`review_cursor` not advanced); Phase 1.6 (`pm_cursor` + `coverage_cursor` not advanced; `.research/management/*.json` not regenerated; `.research/management/project-state.md` not refreshed). Both deliberately skipped to keep this catch-up run focused on backlog reconciliation; documented here so the brief reflects reality.
- **G16 management artifacts:** Phase 1.6 was skipped this run, so the gate is a no-op per its scope rule.
