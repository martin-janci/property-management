# 2026-06-26

## Since last run
- Merged PRs: **49** (#1453–#1853, span includes 49 application-code + infra commits; the dispatcher was clearly very busy)
- Open PRs touched: **30** (all touched since 2026-06-16; mostly auto-impl/auto-fix branches + 7 dependabot)
- New / updated issues: **50** scanned (47 new since cutoff, 3 updated; cursor advanced #1437 → #1852). All 47 new issues are labeled (`follow-up`/`from-merged-review`) — **0 untriaged**.
- Commits: not enumerated this run (10-day window + heavy dispatcher activity = high token cost; covered indirectly by per-PR review).
- Routine lag: **10d 0h** since last run (last_run_iso=2026-06-16T03:25:00Z) — emitted `lag-warning-2026-06-26` and `stale_routine_alert` (state.json untouched in `dev` for >3 days indicates the cloud routine cron likely missed scheduled runs; check the cloud routine scheduler / GitHub Action / Telegram bot health). Dispatcher state on the planning branch was unaffected (still ticking — 49 PRs merged in window).
- Phases that failed: **Phase 1.5 (code-review)** — *skipped intentionally* due to lag-mode token budget. **Phase 1.6 (project-management)** — *skipped intentionally* for same reason; pm_cursor and coverage_cursor preserved at HEAD values (no advancement). The two phases will pick up on the next normal-cadence run.

## Shipped
49 PRs landed since the cursor at #1439. Highlights below; full list omitted per token discipline (see merged-PR cache in tool-results).
- **Refactor sweep (11 PRs, dispatcher-driven structural splits closing churn-hotspot backlog rows):**
  - #1683 `db/repositories/document.rs` → `document/` module split
  - #1781 `db/repositories/form.rs` — duplicated form helpers collapsed
  - #1796 `db/repositories/subscription.rs` → `subscription/` split (8 sub-modules)
  - #1798 `api-server/routes/emergency.rs` → 6 sub-modules (protocols/contacts/incidents/broadcasts/drills/statistics)
  - #1800 `api-server/routes/enhanced_tenant_screening.rs` → 6 sub-modules
  - #1810 `db/repositories/sensor.rs` → 6 sub-modules
  - #1813 `api-server/routes/vendors.rs` → split
  - #1815 `api-server/routes/iot.rs` → 8 sub-modules (incl. realtime WS tests)
  - #1816 `api-server/routes/reserve_funds.rs` → 7 sub-modules
  - Plus aml_dsa.rs / forms.rs / booking.rs also no longer present in tree (resolved via evidence-gone)
- **Feature deliveries (epics 6/16/18/79/80/84/UC-05.x):**
  - #1689/1702/1689/1768 `messaging` N-party group conversations, attachments S3 presign, camelCase wire-contract fixes
  - #1703/1705/1709 Epic 12 gap-sweep, fault notifications on every transition, scheduler payment reminders + auto-late-fee
  - #1726 `api-server` Stripe Checkout payment flow
  - #1716 `integrations` event bus retry + exactly-once
  - #1750 Epic 18 guest ID-document upload
  - #1808/1817 `accounting-web` public marketing/landing + invoicing docs
  - #1822/1829/1830/1831 reality-web SSO callback, support-data-access, api-client integration, 10b-7 contextual help
  - #1832/1834/1835/1843/1844 Epic 6 announcement creation/viewing/comments/pinned/direct-messaging reconciliation
  - #1849 Epic 16 email/push transport draft
- **Fixes:**
  - #1737 iot duplicate JWT-trusting handler removal
  - #1741 api-server: require manager role to mutate
  - #1743 integrations: validate base_rate
  - #1746 portal-listings: enum/state validation
  - #1755/1757 db: resolve duplicate migration version
  - #1768 messaging: camelCase wire contract (had unchecked TODOs → signal emitted)
  - #1809 accounting: PAP-321 F1/F2 — 404-not-500 + skip-serializing provider secrets

## Watch
- **Stalled reviews:** none — no open PR has been waiting >7 days; oldest is 2d.
- **Reverts:** none detected in the 49-PR window (no PR title starts with "Revert" or contains "revert").
- **Churn hotspots this run:** not enumerated due to lag-mode budget; instead, **11 prior churn-hotspot vectors resolved** by the structural splits above. The "split monolith" loop is working as intended — the dispatcher's auto-impl runs catch churn signals and close them within days.
- **Closed-not-merged:** #1720 (one only; not investigated this run due to scope).
- **Risky-churn / fixme-in-merged-code / screen-map-drift:** not analyzed this run — these all need per-PR file-list fetches over a 49-PR window. Re-enabled next run.

## PRs stuck in draft despite approval
<!-- list_pull_requests does not include reviewDecision; cannot compute precisely this run -->
- unknown — `reviewDecision` not in the list payload. Spot-check: 18 of 30 open PRs are draft, mostly auto-impl branches less than 2 days old (expected). No approved-but-stuck candidates surfaced from manual inspection of titles.

## PRs with verdict=changes, no fix-round progress in 24h
<!-- assignments.json on planning branch not fetched this run (would need cross-branch read) -->
- not checked this run — re-enabled next run (would require `mcp__github__get_file_contents` against `ref=planning`).

## Code review slice
- Segment reviewed: **skipped this run** (lag-mode budget; Phase 1.5 disabled).
- Experts: n/a
- Findings: 0
- Next segment: **ppt-web-core** (last reviewed 2026-06-05 — oldest unreviewed; will pick first on next normal-cadence run).
- Skipped: yes — lag-mode catch-up; `review_cursor` untouched.

## Screen-map status
- Total `docs/screens/` files: not enumerated this run (no-op safe; previous count 127 — ppt: 80, reality: 47, mobile: 0).
- Drift signals this run: not analyzed (would need per-PR file lists).
- Orphan screens this run: not analyzed.
- Last `screen-map.yml` CI run on `main`: not fetched.

## Backlog deltas
- **New:**
  - [3] `unchecked-todo-pr-1768` — bug — PR #1768 `fix(messaging): camelCase wire contract` merged with unchecked TODOs in body.
  - [2] `unchecked-todo-pr-1844` — dx — PR #1844 `docs(epic-6): reconcile 6-5 direct-messaging`
  - [2] `unchecked-todo-pr-1709` — test-gap — PR #1709 `feat(scheduler): payment reminders + auto-late-fee`
  - [2] `unchecked-todo-pr-1705` — test-gap — PR #1705 `feat(faults): fire notifications on every status transition`
  - [2] `unchecked-todo-pr-1703` — test-gap — PR #1703 `feat(api-server): gap-sweep Epic 12`
- **Bumped:** none (no existing rows re-fired this run; the new unchecked-todo signals all opened new rows).
- **Decayed (11):** items with `updated_at <= 2026-06-12` lost −1: top-of-list bug/test-gap mobile-rn/mobile-native items dropped from 2→1; the screen-map drift and hotfix-no-test items from PR #959, #1085, #1100 also dropped 2→1.
- **Dropped (12 → status="dropped" via decay to 0):**
  - `triage-issue-1151` (research dispatcher claimable buffer)
  - `churn-hotspot-mobile-native-listing-detail-kt`, `…-search-screen-kt`
  - `code-review-mobile-native-kmp-deeplink-android-bypasses-shared-router`
  - `refactor-churn-hotspot-mobile-announcements`, `…-announcements-test`, `…-documents`
  - `triage-dispatcher-mcp-push-large-file-issue-1014`
  - `triage-issue-951` (deploy ESIGN_TOKEN_SECRET blocker)
  - `closed-not-merged-pr-1274`
  - `churn-hotspot-backend-servers-api-server-src-routes-api-ecosystem-rs` (file still exists but item went stale)
  - `churn-hotspot-backend-crates-db-src-repositories-reality-portal-rs` (PR #1812 still open / unmerged — file exists)
- **Resolved (11 → status="done"):** churn-hotspot rows for files that no longer exist in tree (split into directory modules during the structural-refactor sweep):
  - `iot-rs` ← PR #1815, `vendors-rs` ← PR #1813, `emergency-rs` ← PR #1798, `enhanced-tenant-screening-rs` ← PR #1800, `reserve-funds-rs` ← PR #1816, `aml-dsa-rs` ← code-gone, `forms-rs` ← code-gone, `sensor-rs` ← PR #1810, `subscription-rs` ← PR #1796, `document-rs` ← PR #1683, `booking-rs` ← code-gone.

## Plans promoted
- **none this run.** 6 ready plans persist from prior runs (all `code-review-*` mobile/web reality items + `bug-ios-searchview-uncompilable` + 1 security IDOR). The new unchecked-todo signals are quasi-noise per historical pattern (every prior `unchecked-todo` row was dropped after manual review — the checkboxes turn out to be CI-gate items, not real follow-ups) so promotion is deferred to give a human a chance to drop them quickly.

## Auto-fix
- `no candidate` — Phase 5 saw `unchecked-todo` (not in allowlist), no `screen-map-drift` / `fixme-in-merged-code` / `dep-update lockfile-drift` this run, and no stalled review. `ROUTINE_AUTOFIX_DISABLED` not set.

## Self-review findings
- `.research/self-improvement/findings.json` absent — none.

## Open questions
- **Cloud routine cron health.** state.json sat unchanged for 10 days while the dispatcher kept merging 49 PRs on the planning branch. The two loops are decoupled, but the daily research routine schedule appears to have been dropped — worth verifying the GitHub Action / Telegram bot trigger that fires `text == ""` each morning.
- **Unchecked-todo signal value.** Every prior `unchecked-todo-pr-*` row in this backlog ended `status=dropped` after manual review (the boxes are CI-gate items, not follow-ups). Consider tightening the signal: only fire when the box text doesn't match known CI-gate patterns (`cargo test`, `pnpm test`, `cargo clippy`, `cargo fmt`, "smoke walkthrough"). Or downgrade to score 0 and surface in IDEAS_TRIAGE instead of the main backlog.
- **Phase 1.5 / Phase 1.6 skipped.** Code-review slice and pm-rotation lost a day; next run should prioritize ppt-web-core (review_cursor oldest) and pm-security (pm_cursor next_index=5).

## Quiet day?
- **No.** 49 PRs merged, 11 backlog rows resolved (churn-hotspot sweep), 12 dropped (decay), 11 decayed, 5 new vectors, lag-warning surfaced. `stats.runs` 40 → 41.

## Goal violations
- **G2 — partial pass:** cursors advanced (last_pr_seen 1439 → 1853, last_issue_seen 1437 → 1852), seen_signals grew by 6 entries, hotspot_history untouched (no new hotspots emitted this run). True progress recorded.
- **G6 — pass:** 0 new plans this run; vacuously true.
- **G16 — vacuous pass:** Phase 1.6 skipped, so the management-artifact gate is a no-op per spec ("If Phase 1.6 was skipped this run, the whole gate is a no-op").
- **lag_warning emitted:** above-threshold lag is the *condition*, not a violation — recorded under "Since last run".
