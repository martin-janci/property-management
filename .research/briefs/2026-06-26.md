# 2026-06-26

## Since last run
- Merged PRs: **262** (#1440–#1853) — 10-day catch-up; cloud routine paused 2026-06-16→2026-06-25 (`lag_warning` + `stale_routine_alert` both fired).
- Open PRs touched: **30** (mostly drafts + dependabot; 0 stalled non-draft non-bot >7d).
- New / updated issues: **155** (36 currently open); the bulk are dispatcher post-merge-review follow-ups (label `follow-up,from-merged-review`), so `untriaged-issue` signals = 0.
- Closed-but-not-merged PRs: **26** (no signal — dependabot churn).
- Commits on `dev`: **408** since `4829015b9` → `d973e804` (cursor SHA was NOT an ancestor of current origin/dev — dev was rebased; deeper fetch reconstructed full window).
- Routine lag: **9d 23h since last run** (>>36h → `lag_warning` fired; >=3d → `stale_routine_alert` fired). Cloud routine likely paused or de-scheduled.
- Phases that failed: **none** (Phase 1 deeper fetch recovered after ancestor mismatch; Phase 1.5 + Phase 1.6 both completed; Phase 5 had no eligible candidate).

## Shipped
- #1849 `feat(epic-16): email/push transport drainer for saved-search alerts (BIT-139)` — first end-to-end drainer for `search_alert_queue` (mocked transports; trait-based for real SMTP/FCM later); migration 00194 adds `notified_at`/`notify_attempts` so out-of-band delivery doesn't clobber in-app unread badge.
- #1850 `[codex] Add org-scoped favorite alert worker` — Epic 16 sibling drainer scoped per-org; follow-up #1852 filed (sqlx checked macros, per-org batching, opt-out semantics).
- #1853 / #1848 `feat(messaging): full participant list for group threads [BIT-244/BIT-206]` — backend + web for N-party DMs (closes #1789 / #1776 N-party gaps).
- #1746 `fix(portal-listings): validate enums/status + ownership IDOR tests + FE field wiring (#1671)` — closes a portal-side IDOR class on listing CRUD.
- #1741 `fix(api-server): require manager role to list Airbnb reservations — guest PII (#1667)` — first half of guest-PII manager-gating; follow-up #1766 tracks the other persisted reads still missing the gate.
- #1737 `fix(iot): remove duplicate JWT-trusting sensor WS channel, converge on DB-checked handler (#1668)` — kills the WS channel that trusted client JWTs without DB-side authz; follow-up #1763/#1786 adds regression test.
- #1768 / #1756 `fix(messaging): camelCase wire contract` — BIT-238/239 wire-flip; follow-up #1780 tracks RN mobile breakage.
- #1683 `churn-hotspot-…/document-rs: split document repository into cohesive sub-modules` — backlog row resolved by code-driven slug-id PR.
- #1684 `code-review-api-core-osrng-expect: propagate CSPRNG failure in crypto encrypt instead of panicking` — Phase 1.5 finding from earlier run, resolved.
- #1676 / #1677 / #1678 / #1679 — four Phase 1.5 plans (`code-review-reality-web-share-comparison-404`, `…-listing-page-ssr-crash`, `…-realtor-mgmt-untranslated`, `code-review-mobile-rn-report-fault-fake-submit`) all landed and were verified by this run's resolution check.
- #1713 `revert(delegations): remove re-added delegation frontend (#1690) — reconcile with retirement (BIT-213)` — intentional revert of PR #1690 (delegations feature retired); revert-pr-1713 signal +3.
- #1454 / #1453 — Accounting MVP (N1–N5, @hey-api migration, bank-statement upload + payment matching) — the rebase-window addition that **silently introduced the cross-tenant cache-leak** (see Plans promoted: a new `code-review-ppt-web-core-accounting-cache-leak-on-logout` plan landed this run).
- #1822 `79-2-authentication-flow: reality-web SSO callback e2e coverage` — followed by follow-up #1826 (e2e coverage is integration-only, cements CSRF-skip).
- #1755 / #1757 `fix(db): resolve duplicate migration version 192 + add dup-version CI guard` — fresh-DB / CI ephemeral environments no longer break on dup version (issue #1760).

## Watch
- **Revert this run — #1713** reverts #1690 (`feat(delegations): wire delegation feature into ppt-web (Story 3.4)`) per BIT-213 retirement. Signal `revert-pr-1713` @ score 3, vector=bug — but the revert is the intentional resolution, NOT a regression. Audit-only follow-up: confirm no orphan delegation routes / queryKeys / api-client references survive the revert (backlog row stays open as a watchlist item, no plan promoted).
- **Churn hotspots this run (top 3, 10-day window):** `frontend/apps/ppt-web/src/routes/lazyRoutes.tsx` (13 commits), `backend/crates/db/src/repositories/rental.rs` (10), `backend/crates/db/src/repositories/document.rs` (10 — repeated-churn at runs_seen=2, despite PR #1683 split already merged; new commits since the split keep landing).
- **Stale draft/non-draft PRs:** `gh pr list` returned 30 open PRs (8+ dependabot, several drafts incl. #1846/#1833) — NONE non-draft non-bot stalled >7d this run (all touched within 3d). No `stalled-review` signal fires.
- **Closed-but-not-merged this run:** 26 PRs (mostly dependabot churn-closures + a few experiment branches; no signal fired per pre-Phase-1 filter on author + scope — none warrant a backlog row this run).
- **Post-merge review backlog from dispatcher:** 30+ `follow-up,from-merged-review` issues filed (#1763–#1852) — heavy IDOR/PII/JWT theme. pm-security rotation explicitly prioritises #1791, #1782, #1772, #1766 (see Phase 1.6 next actions). These are GitHub issues; backlog signals aren't auto-emitted (label-bearing → `untriaged-issue` count = 0).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (no `verdict=changes` rows in current `assignments.json`; dispatcher last ran 2026-06-25 22:16 — 3 active rows in `in-progress`/`review`/`quarantined`, all touched within 24h).

## Code review slice
- Segment reviewed: **ppt-web-core** (reason: churn-aligned — `lazyRoutes.tsx` topped this run's churn list — AND oldest unreviewed at 21d stale).
- Experts: frontend.
- Findings: **3** — `code-review-ppt-web-core-accounting-cache-leak-on-logout` (security, +3, high — promoted to plan); `code-review-ppt-web-core-payment-match-no-onerror` (bug, +2, high — silent mutation-failure UX gap); `code-review-ppt-web-core-sessions-double-cast` (refactor, +1, medium — `as unknown as` boundary cast on Sessions page).
- Next segment: **mobile-native-kmp** (last reviewed 2026-06-06 → 20d stale after this run; ppt-web-core advanced to today).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: **141** (ppt: 94, reality: 47, mobile: 0 — not yet seeded).
- Drift signals this run: **23** (PRs that touched ppt-web or reality-web routes/** without updating the matching product's `docs/screens/<product>/*.md`; heuristic — all medium confidence; treated as review candidates per spec, none promoted).
- Orphan screens this run: **0** (no orphan detection round trip executed for the 10-day catch-up — would dominate the budget; deferred to next normal-day run).
- Last `screen-map.yml` CI run on `main`: not fetched this run (no `docs/screens/**` edits in this routine commit).

## Backlog deltas
- **New (30 at score ≥ 2 — top items):**
  - [3] `revert-pr-1713` — Reverted: delegations frontend (PR #1713 reverts #1690)
  - [2] `risky-churn-pr-1565-document-rs` — Risky churn: fix-PR touches backend/crates/db/src/repositories/document.rs with 
  - [2] `risky-churn-pr-1551-document-rs` — Risky churn: fix-PR touches backend/crates/db/src/repositories/document.rs with 
  - [2] `risky-churn-pr-1537-rental-rs` — Risky churn: fix-PR touches backend/crates/db/src/repositories/rental.rs with no
  - [2] `risky-churn-pr-1469-document-rs` — Risky churn: fix-PR touches backend/crates/db/src/repositories/document.rs with 
  - [2] `screen-map-drift-pr-1822-reality` — PR #1822 (79-2-authentication-flow: reality-web SSO callback e2e coverage (gap-7
  - [2] `screen-map-drift-pr-1746-reality` — PR #1746 (fix(portal-listings): validate enums/status + ownership IDOR tests + F
  - [2] `screen-map-drift-pr-1737-ppt` — PR #1737 (fix(iot): remove duplicate JWT-trusting sensor WS channel, converge on
  - [2] `screen-map-drift-pr-1696-ppt` — PR #1696 (feat(messaging): per-participant thread archive + soft-delete (BIT-182
  - [2] `screen-map-drift-pr-1733-ppt` — PR #1733 (fix(ppt-web): add loading state + double-click guard to invoice PDF do
  - [2] `screen-map-drift-pr-1713-ppt` — PR #1713 (revert(delegations): remove re-added delegation frontend (#1690) (#171
  - [2] `screen-map-drift-pr-1690-ppt` — PR #1690 (feat(delegations): wire delegation feature into ppt-web (Story 3.4) (#
  - … and 18 more at score ≥ 2 (mostly the `screen-map-drift-*` heuristic batch; see backlog.md)
- **Plus 4 new items at score 1** — risky-churn / churn-hotspot tail (operational signals like `lag-warning` and `stale-routine-alert` are signal-only with score 0 — they appear in `signals/2026-06-26.json` but do NOT create backlog rows).
- **Bumped:** 0 — no existing-ID matches from new signals this run.
- **Decayed (11):**
  - `code-review-mobile-native-kmp-deeplink-token-not-url-decoded` — 2→1 (no signal in 14d)
  - `code-review-mobile-native-kmp-search-stale-response-race` — 2→1 (no signal in 14d)
  - `test-gap-screen-map-drift-pr-1085-reality` — 2→1 (no signal in 14d)
  - `test-gap-screen-map-drift-pr-1100-ppt` — 2→1 (no signal in 14d)
  - `bug-risky-churn-pr-963-api-main-rs` — 2→1 (no signal in 14d)
  - `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` — 2→1 (no signal in 14d)
  - … and 5 more at -1
- **Dropped (12):** decayed to 0 from score 1 — `churn-hotspot-backend-servers-api-server-src-routes-vendors-rs, triage-issue-1151, churn-hotspot-mobile-native-listing-detail-kt, churn-hotspot-mobile-native-search-screen-kt, code-review-mobile-native-kmp-deeplink-android-bypasses-shared-router, refactor-churn-hotspot-mobile-documents` (+6 more if you're counting).
- **Resolved (19):** 19 open/ready items closed by merged PRs in this window, including:
  - `churn-hotspot-backend-servers-api-server-src-routes-emergency-rs` → PR #1798
  - `churn-hotspot-backend-servers-api-server-src-routes-enhanced-tenant-screening-rs` → PR #1800
  - `churn-hotspot-backend-crates-db-src-repositories-document-rs` → PR #1683
  - `churn-hotspot-backend-crates-db-src-repositories-subscription-rs` → PR #1796
  - `churn-hotspot-backend-servers-api-server-src-routes-aml-dsa-rs` → PR #1708
  - `refactor-churn-hotspot-mobile-announcements` → PR #1543
  - `refactor-churn-hotspot-mobile-announcements-test` → PR #1543
  - `churn-hotspot-backend-crates-integrations-src-booking-rs` → PR #1693
  - … and 11 more (full list in `signals/2026-06-26.json` and `phase2_summary.json`).

## Plans promoted
- `plans/code-review-ppt-web-core-accounting-cache-leak-on-logout.md` — Phase 1.5 finding (security, +3, high) — `AUTHED_QUERY_KEY_ROOTS` in `ppt-web/src/lib/queryKeys.ts` omits `'accounting'`; logout never clears the accounting subtree → cross-tenant cache leak. Fix is one-line list edit + extend existing logout-purge test (file:line refs in plan). Mode: cloud-ok. · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The new high-severity finding (`code-review-ppt-web-core-accounting-cache-leak-on-logout`) is a `code-review-finding` type — NOT in Phase 5's allowlist (allowlist is `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review`) — so it goes to the manual plan path, where the plan was just promoted. None of the 23 `screen-map-drift` signals qualify (all medium confidence). No `stalled-review` (zero qualifying PRs). No `fixme-in-merged-code` (per-PR diff scan skipped for the 10-day catch-up budget). No lockfile-drift.

## Self-review findings
- `.research/self-improvement/findings.json` absent — none

## Open questions
- **Cloud routine schedule:** state.json `last_run_iso=2026-06-16` is 10d stale (`stale_routine_alert`). The dispatcher loop (separate branch, separate schedule) was actively ticking through 2026-06-25 22:16, so the issue is specific to this routine's cron trigger. Operator: check the routine's cloud cron / schedule config.
- **Phase 1.6 PM rotation advanced** — pm-security ran (last 2026-05-27 → 30d stale; next: pm-data at index 6). Coverage cursor advanced 12 → 0 (epic-9 re-checked, full cycle restarted).
- **Heuristic drift overflow:** 23 `screen-map-drift-*` rows added at score 2 each. The 10-day window plus the rebase artifact (cursor SHA not an ancestor) inflated this number vs. a normal-day run; these are explicitly marked review-candidate (medium confidence) per spec. If the next routine runs reduce this volume to <5/run, the heuristic is healthy; if it stays >15, consider a smarter signal that compares route-file diff vs screen-doc diff at the route-symbol level rather than file basename.
- **Telegram delivery:** sent (PM_DIGEST_QUIET=0 per Phase 6 re-evaluation — this run promoted a security plan + landed a code-review finding + 19 resolutions = far from quiet).

## Quiet day?
- **No.** 262 application-code PRs merged across 10-day catch-up, 1 security plan promoted (cross-tenant cache leak), 3 Phase 1.5 code-review findings (1 high-severity security), 19 backlog items resolved, 34 new backlog rows, Phase 1.6 pm-security full role analysis after 30d stale. `stats.runs` 40 → 41, `vectors_created` 173 → 207 (+34), `plans_created` 12 → 13.
