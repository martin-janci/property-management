# 2026-07-22

## Since last run
- Merged PRs: 0 (last_pr_seen still #2432; #2433 remains open, still <8d old — not stalled)
- Open PRs touched: 0 (nothing updated since 2026-07-21T03:13Z; #2433 hasn't moved)
- New / updated issues: 0 (open issues #2422, #2320, #2318, #2366 unchanged since last run)
- Commits: 0 on `dev` (HEAD unchanged at `7d52c40`)
- Routine lag: 0d 23h since last run — on schedule
- Phases that failed: none

## Shipped
- none — a full 23h observation window with no merged PRs, no new commits, no new issues. First zero-activity window since the 4-day cloud-cron gap (2026-07-16 → 2026-07-20). The Layout & Content Manager cascade paused after #2432 landed; #2433 (iOS listing detail) is still awaiting a Mac builder before it can compile-verify.

## Watch
- Stalled reviews: none — #2433 is ~30h old (opened 2026-07-20T20:27Z), still well under the 7-day stalled-review threshold. If it hasn't picked up a Mac-side compile by 2026-07-27 the routine will begin flagging it.
- Reverts: none.
- **Security follow-up carried forward** (from PR #2430 / #2431 / #2432 bodies): ppt-web still has no frame-protection at its serving layer — the preview-bridge feature makes that gap load-bearing. Unchanged since yesterday's brief; still needs `X-Frame-Options: SAMEORIGIN` / `frame-ancestors` allowlist at ppt-web's edge config. Not a routine-emitted signal type — operator-visible.
- Churn hotspots: **none this run** — no new commits, so no churn to derive. `hotspot_history` unchanged.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **mobile-rn** (reason: oldest-unreviewed — tied with `api-handlers` at 2026-07-14; alphabetical tiebreak → `mobile-rn`, pre-declared in yesterday's brief. `CHURN_FILES` was empty this run, so churn-alignment didn't apply.)
- Experts: frontend (primary) + tester (secondary)
- Findings: 2 (both `vector=bug`, `confidence=medium` — static analysis only)
  - `code-review-mobile-rn-action-queue-dead-press` (+2, bug) — **operationally significant.** `frontend/apps/mobile/src/features/layout/registry.tsx:166` — `ActionQueueSection` renders pending vote actions inside `<Pressable>` with **no `onPress`** handler. Tapping any pending action in the mobile dashboard does nothing (no navigation to /voting, no vote-detail open). Sibling to `code-review-ppt-web-ui-actionqueue-mock-shipped` but in a distinct file/component; shipped via #2432 (layout mobile registries). `NavigateContext` threads `onNavigate` to `DashboardStatsSection` but is never consumed by `ActionQueueSection`.
  - `code-review-mobile-rn-action-queue-hardcoded-type` (+1, bug) — same file, lines 129–137 — the map hardcodes `type: 'vote'`, so the `PendingAction` discriminated union's `payment`/`reading`/`fault` branches (and their icons at 148–156) are unreachable. Section only ever surfaces active votes; the extra three source types were never queried.
- Files examined this pass (7): `features/layout/{registry.tsx,useDashboardLayout.ts,layoutCache.ts,LayoutSections.tsx}`, `hooks/useOfflineSupport.ts`, `hooks/usePushNotifications.ts`, `screens/documents/DocumentUploadScreen.tsx`.
- Signals **not** raised (verified & dismissed): `usePushNotifications.ts:240/267` console.log is `__DEV__`-gated with explicit SECURITY comment (no token in the log body) — clean. `DocumentUploadScreen.tsx:172` `as unknown as Blob` is the documented React Native FormData idiom with eslint-disable — clean. `useOfflineSupport.ts` has a proper `syncingRef` re-entrancy guard (legacy #1767) — clean.
- Next segment: **`api-handlers`** (now oldest-unreviewed at 2026-07-14 — the previous 2026-07-14 tiebreak partner reviewed today).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: **153** unchanged (`ppt/` 106 + `reality/` 47; `mobile/` still not seeded → 0).
- Drift signals this run: 0 (no route files touched — no PR window).
- Orphan screens this run: 0 (nothing scanned; skipped per Phase 1 "just routes touched this run" rule).
- Last `screen-map.yml` CI run on `main`: unchanged from yesterday — 2026-06-04 22:19 UTC. No push-to-main this window.

## Backlog deltas
- **New:** [+2] `code-review-mobile-rn-action-queue-dead-press` (bug) — Mobile ActionQueueSection <Pressable> has no onPress; tapping any dashboard action is a silent no-op.
- **New:** [+1] `code-review-mobile-rn-action-queue-hardcoded-type` (bug) — Mobile ActionQueueSection hardcodes type:'vote'; payment/reading/fault union branches + icons are unreachable dead code.
- **Bumped:** none — no signals re-fired on existing rows (no new PRs / commits / churn to derive from).
- **Decayed:** none — all 13 open items pre-run were updated 2026-07-20 / 2026-07-21; earliest is 6 days old, no item hit the 14-day decay window.
- **Dropped:** none.

## Plans promoted
- none — no item hit the readiness bar (score ≥ 3 general, or security fast-track score ≥ 2 with `confidence=high`). The two new bug findings are `confidence=medium` and cap out at +2 / +1; the pre-existing `code-review-api-core-scheduler-units-target-cross-tenant` (security, medium, score=2) also falls short of the security fast-track's `confidence=high` requirement. Everything stays `open`.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist type (`screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / lockfile-drift / `stalled-review`) this run. Both new mobile findings are code-review type (not on the allowlist by design) and are confidence=medium regardless.

## Self-review findings
- open=21 (high=4, medium=13, low=4); new this run=0; acknowledged=19. (Numbers carried forward from yesterday — `.research/self-improvement/findings.json` unchanged since last run.)
- fp-null [recurrence=10] — dispatcher Phase 6 archive-move produces 198KB+ assignments-archive.json; mcp push_files 64KB inline limit + git-push-proxy-403 combine to drop the archive push; fix: adopt PR+API-merge path as primary push mechanism.
- fp-null [recurrence=6] — dispatcher-prompt hard-codes proxy-403 assumption; in this env `git push origin dev` succeeded once GH007 email was fixed; fix: runtime-probe push capability at Phase 1 and bypass 64KiB size-guard when git push works.
- fp-null [recurrence=3] — Phase 2.6 buffer reported claimable=54/open=54 but real claimable=0; all open action-list items already terminal in archive; fix: extend gc1-reconcile to auto-close ANY id shape (including status=failed) and adjust GC3 formula to subtract archive ids.
- fp-null [recurrence=2] — Tier-2 planner kick returned http=401; DISPATCHER_TOKEN/DISPATCHER_URL rejected; fix: operator rotates the secret + dispatcher branches on 401/403 as OPERATOR-CONFIG escalation.

## Open questions
- **Should ActionQueueSection be behind the layout kill-switch** until the `onPress` wiring lands? The section is already registered as `action-queue.v1` in the mobile registry, so a superadmin can kill it via `POST /platform-admin/layout/kill` without a code change — the same escape hatch #2428 shipped for exactly this scenario. Worth flagging to the operator.
- Same question carries forward for **`code-review-ppt-web-ui-actionqueue-mock-shipped`** (yesterday's finding) — the ppt-web equivalent is `action-queue.v1` on `ppt/dashboard`; a kill-switch flip there would blank the mock-data section in production immediately while the endpoint gets wired.
- Phase 1.6 was **skipped today** (quiet observation window). `pm_cursor.next_index` is still at 6 (next role would be `pm-data`, last-run 2026-05-28); `coverage_cursor.next_index` still at 0. Both will advance on the next non-quiet run. If quiet days pile up, the routine will need a "run PM anyway" cadence — flagging as a design question for the next non-routine session.

## Quiet day?
- **Partly** — no PRs merged, no commits, no issues (definitionally "quiet" per Phase 1 observation); but Phase 1.5 rotating review still produced 2 code-review findings that seeded 2 new backlog rows. Per Phase 6's send-decision rule (any Phase 1.5 finding → not quiet), Telegram digest **will** send. `state.stats.quiet_days` **not** bumped (we did produce signals).
