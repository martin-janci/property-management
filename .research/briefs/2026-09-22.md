# 2026-09-22

## Since last run
- Merged PRs: 0 since #2956 (window quiet on `dev`; the 3 open drafts #2967–#2969 are still in-flight dispatcher fast-path fixes for the reality-web silent-fail rows).
- Open PRs touched: 3 (#2967 syncschedule-save silent-fail, #2968 feedcard mutation silent-fail, #2969 import CRM+Feed create-error silent-fail — all draft, dispatcher-owned, ppt-web-core / reality-web scope).
- New / updated issues: 0 (`last_issue_seen` unchanged at #2966).
- Commits: 18 on `dev` — all `chore(research): dispatcher …` from the planning branch; **no application-code changes** merged this window.
- Routine lag: 1d 9h since last run (`2026-09-20T18:38Z` → now `2026-09-22T04:33Z`) — under the 36h `lag_warning` threshold, no lag signal fired.
- Phases that failed: none.

## Shipped
- _(none — 0 merged PRs on `dev` this window; dispatcher activity is `.research/`-only chore commits, not shipping app-code)_

## Watch
- **Stalled review:** #2559 (UC-ACC-05.8 PAY-by-square QR) and #2555 (UC-ACC-05.17 invoice lifecycle) — still both open, still `needs-human-judgement`; no change from 2026-09-20.
- **Reverted:** none this window.
- **Churn hotspots:** _n/a this run_ — no merged application PRs → no code churn to score. `hotspot_history` untouched.
- **Infra blockers still open:** #2966 (api-server utoipa-swagger-ui egress in cloud), #2951 (mobile-rn jest suite unrunnable in cloud), #2946 (portfolio_analytics cross-tenant IDOR still open); mobile-native AGP egress-blocked persists per prior brief. **This is exactly what the fire-payload flagged** (`buffer-low: claimable=8/72 landable=0 (all open backlog is mobile-native/backend cloud-build-gated)`): the actionable open work is either mobile-native/KMP (AGP-blocked in cloud) or backend (utoipa-blocked in cloud), leaving the dispatcher's cloud claim queue starving for landable frontend work.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     Skipped `gh pr list --draft` filter this run (GraphQL disabled in cloud sandbox); the 3 open drafts observed via REST list (#2967 #2968 #2969) are all <24h old AND still dispatcher-owned in-flight, so they don't match this class today. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Data lives in .research/management/assignments.json on the `planning` branch. Dispatcher's latest commit `53eb2d8` reports `0 reviewed, 0 failed, 5 active`; nothing net-new for this class here. -->
- deferred to dispatcher output — latest dispatcher stats line: `0 claimed, 1 reviewed, 0 merge-attempts, 0 merged-now, 0 failed, 5 active` (commit `53eb2d8` 2026-09-22T02:34Z).

## Code review slice
- Segment reviewed: **skipped** (routine mode: adopt-from-dispatcher). The dispatcher's `signals/2026-09-21-reality-web-tier1d.json` already emitted 2 fresh reality-web `code-review-finding` signals against the current code, and those signals **were not carried into `backlog.json`** by the dispatcher pipeline. Firing a fresh routine review of the same segment on top would duplicate the work; instead this run adopts both dispatcher findings into the routine backlog (see *Backlog deltas*).
- Experts: n/a (skipped — adopted 2 dispatcher findings; upgraded both to `confidence: high` after directly reading `frontend/apps/reality-web/src/lib/format.ts:40-65`, `components/listings/ListingCard.tsx:100-115`, and `components/listings/sections/ListingHeader.tsx:1-25` in-tree).
- Findings: 2 adopted (both `bug` vector, `high` confidence, score 2 each — reality-web currency-formatting crash sinks; both cloud-landable single-package frontend edits).
- Next segment: `reality-web` remains tied-oldest at cursor `2026-08-29` in the routine's own `review_cursor`; realistic next routine-owned pass will pick either `reality-web` again or `ppt-web-core` (also `2026-08-29`). Cursor untouched this run (adopting dispatcher findings ≠ running Phase 1.5).
- Skipped: **yes — adopted dispatcher findings this run in place of a fresh Phase 1.5 pass; token discipline + no fresh churn to align against.**

## Screen-map status
- Total `docs/screens/` files: unchanged (`ppt/` + `reality/` populated; `mobile/` still not seeded → 0).
- Drift signals this run: 0 (no merged application PRs → no route-touch diff to scan).
- Orphan screens this run: 0 (no scan — no merged PRs pulled a route diff into the routine's window).
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (nothing new to trigger the check; last-known-green per 2026-09-20 brief still applies).

## Backlog deltas
- **New:**
  - [2] `code-review-reality-web-currency-code-rangeerror-crash` — reality-web ListingCard / ComparisonView / AgencyListings pass raw `listing.currency` into `Intl.NumberFormat({style:'currency',currency})` with no try/catch. Vector `bug`, confidence `high` (upgraded from dispatcher `medium` by direct-diff verification), source `rotating-expert-review reality-web 2026-09-21 (dispatcher Tier-1d adopted 2026-09-22)`. Cloud-landable single-package fix (add helper mirroring the existing safe pattern at `app/[locale]/realtor/listings/page.tsx:16-22`).
  - [2] `code-review-reality-web-listing-sections-currency-ssr-500` — reality-web `ListingHeader` / `AdditionalInfo` currency guards only check `length===0`; a non-empty malformed code SSR-500s the public listing page. Vector `bug`, confidence `high`, same source stamp. Cloud-landable single-package reality-web edit.
- **Bumped:** none (both new rows are net-new, not incremented; existing open items untouched).
- **Decayed:** none — every `open` item's `updated_at` is within 14 days of today. The 3 pre-existing reality-web silent-fail open rows are all `updated_at: 2026-09-21`; the 2 ready mobile-native-kmp items are `ready`, not `open`, and outside the decay rule.
- **Dropped:** none.
- **Resolved:** none — the 3 open reality-web silent-fail rows (#2967/#2968/#2969) have matching in-flight draft PRs by title, but the resolution check only fires on merged PRs; those PRs stay draft.

## Plans promoted
- _(none)_ — neither new row meets the readiness gate: both are `bug` vector at score 2 (< 3 threshold, and no `security` fast-track applies). Existing `ready` items already have plans. **Deliberately no plan padded to hit the fire-payload buffer target:** promoting a score-2 row to satisfy an external "buffer-low" signal would break the routine's own readiness contract. The correct pipeline path here is: dispatcher's next Tier-1d pass, or an external verification signal, bumps these to score 3 → routine promotes → dispatcher claims.

## Auto-fix
- `no candidate` — neither new signal is on the Phase 5 allowlist (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review`). Score-2 `code-review-finding` rows are manual-implementer work by design.

## Self-review findings
- none (`.research/self-improvement/findings.json` absent).

## Open questions
- **Why does the dispatcher's Tier-1d review emit `code-review-finding` signals into `.research/signals/<date>-*-tier1d.json` but not incorporate them into `.research/backlog.json`?** This is the second incremental run where the routine has had to adopt fresh dispatcher findings by hand (the 3 silent-fail rows landed via the dispatcher's own fast-path capture; today's 2 currency findings did not). Suggested action: audit `backlog-refill.sh` / dispatcher post-review pipeline for the write-through path from `signals/*-tier1d.json` to `backlog.json`.
- **Fire-payload `buffer-low` posture:** the payload asks the routine to "refill planner with reality-web/frontend-landable vectors." Two new cloud-landable frontend rows added this run, but at score 2 they will not surface in `landable` until score 3 is reached. Should the dispatcher's landable-count include `score >= 2, confidence: high, vector != security` frontend rows to reflect that they ARE ready-to-implement even if under the routine's promotion bar? Answering yes would restore the buffer without diluting readiness discipline in the routine.

## Quiet day?
- **No.** 0 merged PRs but 2 new backlog vectors adopted (both high-confidence, cloud-landable frontend bugs), routine state cursors advanced (last_pr_seen 2956→2969, last_commit_sha bumped to `53eb2d8`), and the stale `backlog.md` from 2026-09-20 was regenerated against the current `backlog.json` (which had drifted: the 3 dispatcher-added silent-fail rows and one status flip were missing from the rendered view — G10 would have failed on inspection). All quality gates run against the staged index at commit time; results in `signals/2026-09-22.json goal_checks[]`.
