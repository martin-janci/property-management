# 2026-08-13

## Since last run
- Merged PRs: 0 (last run was 2026-08-12T22:45:00Z, only 4h ago)
- Open PRs touched: 0
- New / updated issues: 1 (#2743 — dispatcher regression follow-up, labeled bug/infra/dispatcher/follow-up)
- Commits: 2 on `dev` — both are `.research/`-only dispatcher planning commits (f050fc1, 9a981ae). No application-code churn.
- Routine lag: 4h since last run — on schedule.
- Phases that failed: none.

## Shipped
- (nothing — 4-hour quiet window)

## Watch
- **Stalled reviews (accounting cluster, 15d open):** #2555 UC-ACC-05.17 sent/cancelled invoice lifecycle · #2558 UC-ACC-05.9 invoice PDF endpoint · #2559 UC-ACC-05.8 PAY-by-square QR — all authored by martin-janci, still waiting on reviewer. Backlog rows already carry `stalled-review-pr-<num>` at `needs-human-judgement`; no re-emit this run.
- **Quarantined PR #2684** (workflow-cond-parse-failopen) — fix_rounds=3 exhausted, CI red on test-shard 1-4 after round-3 clippy fix (commit 926afe8). SLA age ~5d < 7d; still within tolerance but escalation queued (see pm-qa action-list item).
- **Closed-not-merged retry:** #2705 dx-cnm-pr-2385-retry2 (rust-toolchain 1.94.1→1.100.0) still investigation-only; no new evidence this window.
- **Churn hotspots (this run):** none (4h window, zero application-code commits).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (the only `verdict=changes` row is task_id `code-review-api-core-workflow-cond-parse-failopen` PR#2684, but its status is `quarantined`, not `review`, so it is outside the SLA-tracked set)

## Code review slice
- Segment reviewed: **api-handlers** (oldest-unreviewed; tie broken by fallback preference for api-handlers — api-core and reality-server also last-reviewed 2026-08-10 and will pick up next run)
- Experts: rust + security
- Findings: 1 (see Backlog deltas — `code-review-api-handlers-migration-export-mock-url`)
- Next segment: api-core or reality-server (both tied at 2026-08-10 as oldest-unreviewed after this run)
- Skipped: no

## Screen-map status
- Total `docs/screens/` files: 153 (ppt=106 + reality=47; `mobile/` not yet seeded → 0)
- Drift signals this run: 0 (no route files touched)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (no `docs/screens/` diff to gate)

## Backlog deltas
- **New:** [2] `code-review-api-handlers-migration-export-mock-url` — api-handlers rotating review flagged Epic 66 export handlers persisting a hardcoded mock URL as `file_path` and returning it as the `download_url`; feature is non-functional end-to-end for any authenticated platform admin (see `backend/servers/api-server/src/routes/migration.rs:1270,1395`).
- **Bumped:** none
- **Decayed:** none (oldest open item `updated_at = 2026-08-04`, 9d < 14d decay threshold)
- **Dropped:** none
- **Data-hygiene:** repaired 3 pre-existing malformed backlog rows (`code-review-reality-web-{comparison-view,realtor-mgmt,agency-errorstate}-i18n`) that had been ingested in raw-signal shape (missing `score`/`status`/`title`/`vector`). Reconstructed with `score=1 vector=bug confidence=medium status=open updated_at=2026-08-11`; original evidence preserved. Verified evidence still present on disk (ComparisonView.tsx still lacks `useTranslations` and still renders `'Add properties to your comparison from the listings page.'` as literal English at L105).

## Plans promoted
- (none — no open item hit score ≥3 this run; the security fast-track requires vector=security which also has no candidates)

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. `ROUTINE_AUTOFIX_DISABLED` unset. The single new `code-review-finding` is confidence=medium (static analysis), so it stays in the manual-implementer path.

## Self-review findings
- open=33 (high=7, medium=23, low=3); new this run=0; acknowledged=19
- fp-1 [recurrence=15] — Phase 6 archive-move produced a 198KB assignments-archive.json; MCP push cannot inline files past its ceiling; fix: push the dispatcher state commit to a NEW branch (new-branch push is NOT proxy-403'd)
- fp-2 [recurrence=13] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked; fix: replace hard-coded assumption with a runtime probe at Phase 1
- fp-3 [recurrence=5] — buffer drained yet Tier-1d fired against zero-open backlog; fix: add an in-repo signals->backlog promoter that Phase 2 runs before claim
- fp-4 [recurrence=5] — action-list holds 31 in-progress rows but assignments has only 3 live rows; fix: Phase-2 reconcile step to reconcile action-list vs assignments
- fp-5 [recurrence=3] — 5 in-progress rows produced ZERO branches on origin across TWO implementer generations; fix: add implementer-liveness escalation on reclaim

## Open questions
- None net-new this run. Carrying the pm-scrum-master / pm-qa questions from 2026-08-13 decisions section into `.research/management/decisions.md` (dispatcher archive strategy, retry-remint ownership, KMP non-cloud runner routing, PR #2684 disposition, sprint reconciliation).

## Quiet day?
- No — one new backlog vector added (`code-review-api-handlers-migration-export-mock-url`) plus one new issue (#2743) surfaced two recurring dispatcher defects. Coverage upkeep advanced (`coverage_cursor` 5 → 6, epic-80 re-checked), pm rotation advanced (`pm_cursor` 3 → 4, pm-qa role_last_run stamped), review rotation advanced (`api-handlers` 2026-08-10 → 2026-08-13). Data-hygiene cleaned 3 malformed backlog rows.
