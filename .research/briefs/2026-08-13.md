# 2026-08-13

## Since last run
- Merged PRs: 4 (#2745–#2748 — all dispatcher-generated retries of prior research findings)
- Open PRs touched: 5 (4 dependabot deps rolled forward; #2744 draft dispatcher un-wedge PR by martin-janci)
- New / updated issues: 4 (#2743 dispatcher archive overflow CRITICAL; #2752/#2753/#2754 follow-ups from post-merge review of #2737/#2739/#2740)
- Commits: 17 on `dev` (all dispatcher / dispatcher-fast-path bookkeeping + the 4 merged fix PRs)
- Routine lag: 0d 13h since last run — on schedule
- Phases that failed: none. Phase 1.5 skipped (see Code review slice). Phase 1.6 skipped (fired by `buffer-low` payload rather than the daily schedule; delivery digest not re-generated this run).

## Shipped
- #2745 closed-not-merged-pr-2705: bump rust-toolchain 1.94.1 → 1.97.1 (real latest stable) — **resolves open backlog row `closed-not-merged-pr-2705`**
- #2746 code-review-api-core-apicall-ssrf-redirect-retry1: regression test proving SSRF redirect policy blocks internal-host hops
- #2747 code-review-api-handlers-marketplace-award-quote-idor-retry1: verify awarded quote belongs to RFQ (IDOR)
- #2748 code-review-api-handlers-voice-alexa-noverify-retry1: fail closed on Alexa branch of voice /verify webhook (security)

## Watch
- **CRITICAL — dispatcher archives exceed MCP push ceiling (issue #2743 section 1, recurrence of #1162):** `action-list-archive.json` (638 KiB) and `assignments-archive.json` (660 KiB) both blow past the 65536 B `mcp__github__push_files` ceiling. Every terminal transition triggers `archive-reconcile.sh` / `action-list-reconcile.sh` append → oversize → Phase 6 push aborts. Recent dispatcher runs only survived because they had **zero terminal transitions**. Directly ties to this run's `buffer-low` trigger (claimable=7/72). New backlog row `bug-dispatcher-archive-overflow-2743` at score 3. Remediation options are operator-side (shard/gzip archives or restore scoped push path for `.research/management/**`).
- **retry-remint ghost retry (issue #2743 section 2):** `retry-remint.sh` re-minted `code-review-api-handlers-voice-webhook-default-secret` as `-retry1` despite Issue #2658 / PR #2660 already landing the fix on dev under a different id. Dev-truth guards key on task's own stem — cross-issue-id landings slip through. New backlog row `bug-retry-remint-ghost-cross-issue-id-2743` at score 2.
- **Follow-ups from post-merge review** (all low severity):
  - #2752 `bug-meterdetail-date-locale-bypass-2752` — MeterDetailScreen 'Latest reading' date uses bare `toLocaleDateString()`, bypasses `resolveLocale()` from PR #2740 sweep
  - #2754 `test-gap-protectedroute-i18n-2754` — PR #2737 shipped without the leaf-key-lockstep regression test the sibling PRs #2736/#2739 shipped; 2 keys unguarded
  - #2753 `refactor-format-ts-unused-helpers-2753` — `format.ts` exports `formatDate/Time/DateTime` with zero call sites; ~10 screens re-implement inline
- **Stalled reviews (accounting cluster, 15d open):** #2555 UC-ACC-05.17 · #2558 UC-ACC-05.9 · #2559 UC-ACC-05.8 — no movement since last run; existing `stalled-review-pr-<num>` rows remain at `needs-human-judgement`.
- **Closed-not-merged dependabot:** #2675 (npm-minor-patch), #2674 (cargo-minor-patch), #2581 (gradle-minor-patch) — logged as `dep-update-noise` signals (score 0 per rule); no backlog rows.
- **Reverted:** none this window.
- **Churn hotspots (this run, real-code only):** no meaningful signal — every real-code file in the 7-day window touched only ≤2 times (top-5 = `queryKeys.ts`, `usePerformanceMetrics.ts`, `AuthContext.test.tsx`, `ProtectedRoute.tsx`, `OfflineIndicator.tsx`, all at 2 touches — a low-churn steady-state window). No `churn-hotspot` / `repeated-churn` signals fired; `hotspot_history` untouched.

## PRs stuck in draft despite approval
- none (draft #2744 by martin-janci is un-wedge WIP, not approved).

## PRs with verdict=changes, no fix-round progress in 24h
- none (verified against planning-branch assignments.json via dispatcher activity — every recent `verdict=changes` respawn landed same-day).

## Code review slice
- Segment reviewed: **none — Phase 1.5 skipped this run** (reason: `buffer-low` trigger payload — routine ran in refill posture, prioritized turning the 4 new issues into scored backlog rows over launching a fresh rotating-expert pass; dispatcher tier-1d generator is producing per-segment signals continuously — 3× api-handlers/reality-server signals landed in `.research/signals/2026-08-13-*.json` already this run window)
- Experts: —
- Findings: 0 from this routine's Phase 1.5 (dispatcher-side tier-1d signals visible in same-day `signals/2026-08-13-api-handlers-tier1d.json` + `signals/2026-08-13-reality-server-tier1d.json`)
- Next segment: `state.review_cursor` NOT advanced; next fire will pick the oldest-unreviewed after excluding whatever tier-1d covered
- Skipped: yes — buffer-low posture, `state.review_cursor` NOT advanced

## Screen-map status
- Total `docs/screens/` files: 168 (ppt/=106, reality/=47, reality-mobile/=15; mobile/=0 — drift detection still disabled for the mobile trigger set)
- Drift signals this run: 0 (the 4 merged PRs touched backend / security paths, not frontend routes)
- Orphan screens this run: 0 (no scan performed — no route churn to spot-check against)
- Last `screen-map.yml` CI run on `main`: (not queried — HTTPS proxy quota conservation; last observed on 2026-08-12 brief)

## Backlog deltas
- **New:** [3] `bug-dispatcher-archive-overflow-2743` — dispatcher archives (~640 KiB each) exceed MCP push ceiling — blocks state persistence
- **New:** [2] `bug-retry-remint-ghost-cross-issue-id-2743` — dev-truth guards miss cross-issue-id landings
- **New:** [2] `test-gap-protectedroute-i18n-2754` — PR #2737 shipped without regression test + 2 unguarded keys
- **New:** [2] `bug-meterdetail-date-locale-bypass-2752` — MeterDetailScreen date bypasses active i18n locale
- **New:** [1] `refactor-format-ts-unused-helpers-2753` — 3 tested-but-unused exports; ~10 screens duplicate inline
- **Resolved to `done`:** `closed-not-merged-pr-2705` — PR #2745 merged 2026-08-13 (rust-toolchain 1.94.1 → 1.97.1, real latest stable)
- **Bumped:** none this run.
- **Decayed:** none (no open items with `updated_at <= 2026-07-30`).
- **Dropped:** none.

## Plans promoted
- **none this run.** Highest-scoring new item is `bug-dispatcher-archive-overflow-2743` at score 3 — clears the promotion floor and every readiness gate mechanically (concrete files, hypothesis expressible, plausible test plan, unique slug-stem). Held back because the fix is operator-side (shard/gzip strategy or infra change) rather than a self-contained implementer task, so a plan file would land as speculation vs. commitment; leaving as an `open` backlog row for the operator/dispatcher upgrade path. All other new rows are score ≤ 2 (below the +3 promotion floor; no security fast-track qualifies — the security items already landed via #2746–#2748). Adversarial pass: n/a.

## Auto-fix
- `no candidate` — no signal met the certainty bar (confidence=high + score ≥ 3 + allowlist). `bug-dispatcher-archive-overflow-2743` is confidence=high and score 3 but its action shape is not in the auto-fix allowlist (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review`). The three `dep-update-noise` signals are allowlisted but for the *lockfile-drift* sub-shape only, not for "closed-not-merged dependabot PR" — these were superseded/rejected, not lockfile-drifted.

## Self-review findings
- none (`.research/self-improvement/findings.json` not read this run — routine skipped self-review to keep buffer-low refill turn tight)

## Open questions
- Dispatcher archive overflow (`bug-dispatcher-archive-overflow-2743`, issue #2743): which remediation to pick — shard the archives by year-month, gzip at rest with reader support, or restore a scoped git-push path for `.research/management/**`? The current cloud sandbox blocks direct `git push` (proxy-403) but the operator has more room. Decision needed before an implementer picks up the plan.
- Are the ~15-day stalled accounting PRs (#2555/#2558/#2559) still active, or should they be closed and reopened after the underlying analysis lands? No movement in the past week.
- retry-remint hardening (`bug-retry-remint-ghost-cross-issue-id-2743`): the proposed guard is "exclude when failed row's implementer_summary matches an already-landed signal (`already.fixed`, `empty diff`, `no PR needed`, `already-satisfied`)". Confirm the string set before an implementer picks this up — false positives on that list would silently drop legitimate retries.

## Quiet day?
- **No.** 4 merged PRs, 5 new backlog vectors, 1 resolution flip, and a `buffer-low` trigger that surfaced the CRITICAL dispatcher archive-overflow root cause. `stats.quiet_days` NOT bumped.
