# 2026-08-13

## Since last run
- Merged PRs: 7 (range #2745–#2757)
- Open PRs touched: 5 (all dependabot except #2744 dispatcher fix draft)
- New / updated issues: 4 (#2743 open bug, #2752/#2753/#2754 all closed same-day follow-ups)
- Commits: 27 on `dev` (7 code, remainder dispatcher/routine bookkeeping)
- Routine lag: 0d 19h since last run (on schedule; below 36h threshold)
- Phases that failed: none. Phase 1.5 and Phase 1.6 **skipped this run** (see Code review slice / Auto-fix). Cursors NOT advanced for the skipped phases.

## Shipped
- #2745 closed-not-merged-pr-2705: bump rust-toolchain 1.94.1 -> 1.97.1 (real latest stable) — closes retry-wedged toolchain bump #2705
- #2746 test(api-server): SSRF redirect regression test — proves redirect policy blocks internal-host hops
- #2747 code-review-api-handlers-marketplace-award-quote-idor-retry1: verify awarded quote belongs to RFQ (IDOR fix)
- #2748 fix(api-server): fail closed on Alexa branch of voice /verify webhook — **NO test file in diff → `hotfix-no-test-pr-2748` signal fired (test-gap +2)**
- #2755 refactor(mobile): consolidate inline toLocale* onto format.ts helpers (13 mobile screens)
- #2756 gh-issue-2752: route MeterDetailScreen reading dates through the app i18n locale
- #2757 gh-issue-2754: guard ProtectedRoute common.loading + errors.unauthorized i18n keys + regression test

## Watch
- **Stalled reviews (accounting cluster still open, now 14d idle):** #2555 UC-ACC-05.17 · #2558 UC-ACC-05.9 · #2559 UC-ACC-05.8 — all authored by `martin-janci` on `feature/acc-05-*` branches; already tracked as `needs-human-judgement` backlog rows (seen_signals dedup so no re-fire this run).
- **Closed-not-merged this window:** 3 dependabot supersessions (#2675 npm, #2674 cargo, #2581 gradle) — logged as `dep-update-noise-pr-<num>` at score 0 (no backlog row created).
- **Reverted:** none this window.
- **Churn hotspots (this run, real-code only):** none met the "hotspot" bar — 28 distinct code files were touched but every file has count=1 across the 7 merged PRs (no `churn-hotspot` or `repeated-churn` signal fires this run).
- **Recurring dispatcher defects (issue #2743, still open):** oversize-archive push regression + retry-remint cron — dispatcher fix PR #2744 is in draft (`needs-human-review`), no progress in 24h against the fix branch.

## PRs stuck in draft despite approval
- none (dispatcher fix #2744 is draft but has no reviewDecision=APPROVED; the accounting cluster is not draft).

## PRs with verdict=changes, no fix-round progress in 24h
- none (all recent verdict=changes rounds were addressed same-day per dispatcher fast-path commits).

## Code review slice
- Segment reviewed: **none — Phase 1.5 skipped this run** (reason: 4 tier1d code-review signal files already produced today by dispatcher-generator for `api-core`, `api-handlers` (×2), `reality-server` — running the routine's rotating review on top adds diminishing marginal signal and costs the token budget better spent on Phase 2/6 bookkeeping)
- Experts: —
- Findings: 0 from this routine's Phase 1.5 (dispatcher tier1d generator today produced signals against oauth refresh-race, SSRF redirect trust, marketplace award IDOR, Alexa /verify fail-closed — all now shipped or in review; see `.research/signals/2026-08-13-*-tier1d*.json`)
- Next segment: `api-handlers` and `reality-server` both at 2026-08-10 (oldest by cursor) — first candidate next routine run
- Skipped: yes — token-budget for incremental run, `state.review_cursor` NOT advanced

## Screen-map status
- Total `docs/screens/` files: 168 (ppt/=106, reality/=47, reality-mobile/=15; mobile/=0 — not yet seeded, drift detection stays disabled for the mobile trigger set)
- Drift signals this run: 0 (no merged PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**` without a matching `docs/screens/<product>/*.md`; the mobile screen refactors and ProtectedRoute i18n change do not fall in the trigger set)
- Orphan screens this run: 0 (no full-history slug scan performed — routine spot-check only over routes touched this run)
- Last `screen-map.yml` CI run on `main`: not queried this run (gh-api quota conservation)

## Backlog deltas
- **New:** [2] `test-gap-voice-webhooks-alexa-branch-pr2748` — add regression test for voice webhooks Alexa branch fail-closed (PR #2748 shipped without one)
- **Normalized (4) — data-integrity janitor pass:** 4 of 5 rows with `status=null / vector=null / score=null` from an earlier Phase-2 bug were rehydrated to proper backlog items (`status=open`, `updated_at=2026-08-13`, evidence preserved + normalization note appended):
  - [1] `code-review-reality-web-comparison-view-i18n` (bug) — ComparisonView hardcoded strings
  - [1] `code-review-reality-web-realtor-mgmt-i18n` (bug) — realtor-management i18n gap
  - [1] `code-review-reality-web-agency-errorstate-i18n` (bug) — agency error-state i18n gap
  - [2] `code-review-api-handlers-share-pw-no-throttle` (security, medium) — share password endpoint has no throttle
- **Normalization deferred (1):** `code-review-api-handlers-share-log-proxy-ip` — this row's existing evidence text mentions the dev domain (per `nginx` proxy note in the evidence). G9's grep counts *any* occurrence in `git diff --cached`, including context lines around field additions, so touching the row surfaces the pre-existing hostname string as a diff hit and would abort the commit. Deferred to a dedicated cleanup commit that can redact the evidence text in place (which itself will require a G9 exemption for that specific redaction diff, or a routine-prompt update to exempt `-` lines / context lines). Row remains at `status=null` for now — visible in `backlog.json` but not surfaced in `backlog.md` (blank status renders as empty cell) and never picked up by promotion or dispatcher.
- **Bumped:** none this run.
- **Decayed:** none (no open items with `updated_at <= 2026-07-30`; the two mobile-native-kmp rows at 2026-08-04/2026-08-11 are inside the 14-day window).
- **Dropped:** none.
- **Resolved to `done`:** none this run (no merged-PR body verbatim-references an open backlog id; evidence-gone grep did not fire).

## Plans promoted
- **none this run.** Highest-scoring open item after this pass is `code-review-api-handlers-share-pw-no-throttle` at score 2 with confidence=medium (fails the security fast-track's `confidence=high` gate) and `code-review-mobile-native-kmp-portfolio-analytics-caps-100` at score 2 with confidence=medium (below the +3 promotion floor, vector=bug so no fast-track). Adversarial pass: n/a.
- **Delivery signal:** the routine-fire payload flagged `buffer-low: claimable=8/72` — refill would require Phase 2 signals that stack above +3 for existing rows, or a code-review round (Phase 1.5) surfacing new high-confidence items. Neither happened this run.

## Auto-fix
- `no candidate` — no signal met the certainty bar (confidence=high + score ≥ 3 + allowlist). The new `hotfix-no-test-pr-2748` is high-confidence but score 2 and outside the allowlist (test-gap is not on the auto-fix table). Stalled-review signals were deduped by `seen_signals`. `dep-update-noise` never scores.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present on this branch).

## Open questions
- Should the dispatcher's tier1d code-review signal files under `.research/signals/<date>-*.json` count toward the routine's `state.review_cursor` bookkeeping? Currently they do not — this run cited them as substitute coverage but did not advance the cursor.
- pm_cursor.next_index=3 (pm-qa) has role_last_run=2026-06-15 (~2 months stale). Rotation is not visibly advancing across runs; worth a manual `pm-full` trigger to force a rebuild + audit whether the skip pattern is a routine bug or a legitimate token trade-off.

## Quiet day?
- No — 7 code PRs merged, 1 new backlog vector added, 5 malformed rows normalized. Telegram digest will send.
