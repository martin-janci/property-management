# 2026-05-21

## Since last run
- Merged PRs: 11 (#316, #317, #332, #335, #365–#372) — 6 content, 4 version-bump bot, 1 own research brief
- Open PRs touched: 18 (15 dependabot bumps + #371 batch-deps + #373 version bump; #364 research Phase 5 still open, ~2d old)
- New / updated issues: 0
- Commits: 11 on `main`
- Phases that failed: none

## Shipped
- #335 fix(SECURITY-CRITICAL): close X-Tenant-Context auth bypass in 6 peer routes (52 handlers) — replaced client-trusted role branches with hardcoded `false` (fail-closed); left `TODO(security)` markers for real role lookup → see Backlog deltas
- #332 fix(SECURITY-CRITICAL): require JWT auth on `/api/v1/ai/*` (was unauthenticated) — added `ai_auth_tests.rs` regression coverage
- #316 fix(security): reality-server hardening — open redirect, missing auth, SSRF, IDORs (+550/-32) — no route-level tests added → promoted test-gap plan
- #317 fix(security): reality-server input hygiene — length caps, URL-scheme validation, paginate comments (+280/-5) — no test file
- #369 test(db): fix cross-tenant RLS penetration test harness (+ run serially, `--test-threads=1`)
- #367 fix(ci): verify-all smoke points at backend.yml (ci.yml deleted in #326) — 1-line CI script tweak, not scored

## Watch
- Stalled review: none (#364 ~2d, #371 ~1d — both under the 7-day bar)
- Reverted: none
- Churn hotspots this run: `ai.rs` (420 lines, #332 auth rewrite), `url_validator.rs` (302, new SSRF module w/ inline tests), `ai_auth_tests.rs` (219, new test file) — all one-time additions from the security batch, not recurring instability; tracked in `hotspot_history` (runs_seen=1), scored 0. Prior hotspots (integrations/organizations/documents.rs) did not churn this run.

## Screen-map status
- Total `docs/screens/` files: 99 (ppt/ 53 + reality/ 46; mobile/ not yet seeded → 0)
- Drift signals this run: 0 (all 6 content PRs were backend-only; no `ppt-web` routes / `reality-web/app/**` touched)
- Orphan screens this run: 0 (no route files touched → nothing to compare)
- Last `screen-map.yml` CI run on `main`: completed / success (2026-05-20T23:29:33Z)

## Backlog deltas
- **New:** [4] Add route-level regression tests for reality-server security fixes (open-redirect, IDOR, input caps) — `test-gap`, from #316+#317 (2× `hotfix-no-test`); promoted to a plan
- **New:** [2] Restore manager/admin role lookups hardcoded to false by X-Tenant-Context fix (#335) — `bug`, from `fixme-in-merged-code`; one signal short of promotion (see Open questions)
- **Bumped:** none (no new signals matched existing items)
- **Decayed:** none (all open items created 2026-05-20/21, under the 14-day decay window)
- **Dropped:** none

## Plans promoted
- `plans/reality-server-security-route-tests.md` — integration tests pinning the #316/#317 security contracts (open-redirect reject at `sso.rs:124`, one cross-agency IDOR denial, one input-cap rejection); cloud-ok · adversarial pass: passed

## Open questions
- **Role-lookup regression (#335) — fast-track?** The X-Tenant-Context fix was correct and fail-closed, but to ship it fast the manager/admin role branches were hardcoded to `false`. Net effect today: org admins cannot create critical notifications (`critical_notifications.rs` gates on `principal.is_platform()` only) and managers lose manager-gated fault data (`faults.rs:443,935` set `is_manager = false`). This is a live *functional* regression, not a vulnerability. It scores 2 (single `fixme-in-merged-code` signal) so the routine won't auto-promote — but it may warrant a manual bump to promote a plan that wires the `routes/organizations.rs` membership-role pattern into these 3 routes. Want it promoted next run?
- **Dependabot backlog**: 15 individual open dependabot PRs + #371 batch PR (REVIEW_REQUIRED). Worth a single review-and-merge sweep when convenient; logged as `dep-update-noise`, not scored.

## Quiet day?
- No
