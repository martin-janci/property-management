# 2026-08-16

## Since last run
- Merged PRs: 9 (range #2762–#2771; #2684 also closed unmerged this window, superseded by #2769)
- Open PRs touched: 10 (all dependabot bumps or the #2744 dispatcher-fix draft)
- New / updated issues: 1 (#2768, opened + closed same day by dispatcher-quarantine → PR #2769 replay)
- Commits: 9 merged-PR commits on `dev` + 5 dispatcher `chore(research)` commits since last routine run
- Routine lag: 23h since last run — on schedule (below 36h `lag_warning` threshold)
- Phases that failed: none (Phase 1.5 + Phase 1.6 SKIPPED intentionally, not failed — see Open questions)

## Shipped
- #2762 code-review-api-handlers-voice-alexa-cert-fetch-no-timeout — bound Alexa cert fetch with connect+request timeouts + shared client (voice_webhooks.rs +58)
- #2763 churn-hotspot-marketplace-award-quote-idor-tests — 2 more IDOR tests (foreign RFQ via path param, non-existent quote id) on marketplace award endpoint (+117 tests)
- #2764 churn-hotspot-api-call-rs — refuse cross-origin redirects in workflow API-call action; credential-leak defence for custom headers reqwest doesn't strip cross-host (api_call.rs +127/-18, +regression test)
- #2765 code-review-api-handlers-voice-google-verify-fail-open — fail-closed Google voice webhook JWT verification (real RS256 + JWKS + aud/iss/exp; voice_webhooks.rs +311/-96, 6 new tests)
- #2766 code-review-api-handlers-voice-oauth-refresh-unauthenticated — require device-owner auth on voice OAuth refresh + per-user rate-limit (voice_webhooks.rs +86, +270-line integration suite)
- #2767 churn-hotspot-protected-route-i18n-test — extend ProtectedRoute i18n lockstep to runtime resolution + defaultValue parity + key classification (+170 test lines)
- #2769 gh-issue-2768 — fail closed on unparseable workflow condition (workflow_executor.rs +131/-23 + `#[serde(deny_unknown_fields)]` on Condition/ConditionGroup, 4 new tests; supersedes quarantined #2684)
- #2770 84-1-s3-presigned-urls — forward building association on direct-to-S3 upload via `access_scope='building'`+`access_target_ids=[buildingId]` shipped-API mechanism (closes #2366; api-client only, +82/-20)
- #2771 82-1-swiftui-project-setup-retry1 — re-verify SwiftUI project-setup screen-map coverage; gap already satisfied on `dev` since 2026-06-17 (docs-only)

## Watch
- Stalled review: none (all long-idle open PRs are dependabot → `dep-update-noise`, not eligible for `stalled-review`; #2744 is a draft)
- Reverted: none
- Churn hotspots this run: `voice_webhooks.rs` (~556 lines across 3 security PRs, runs_seen 0→1), `protectedRouteI18n.test.ts` (+170, runs_seen 1→2 → `repeated-churn`), `workflow_executor.rs` (+154, runs_seen 1→2 → `repeated-churn`). Both repeated-churn cases are additive fix/coverage — not code instability; log-only.

## PRs stuck in draft despite approval
- #2744 fix(dispatcher): un-wedge oversize-archive push (#1162) + close retry-remint cross-issue-id blind spot (#2743) — draft since 2026-08-13, approved+CI-green but human-gated per dispatcher note (self-PR cannot be bot-approved). Last update 3d ago.

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher `assignments.json` shows 1 active row, no `review`-status entries with `verdict=changes`)

## Code review slice
- Segment reviewed: none — Phase 1.5 SKIPPED
- Experts: n/a
- Findings: 0 fresh (routine did instead SCORE 8 pre-existing dispatcher-Tier1d findings — see Backlog deltas)
- Next segment: mobile-native-kmp (oldest at 2026-08-11) or api-core (churn-aligned with #2764 + #2769)
- Skipped: yes — the payload flagged the claim buffer as starved (claimable=4/72, all mobile-native-gated + unscored). Adding more code-review findings without scoring the existing ones would deepen the exact problem the payload asks to fix. Routine took the demand-side action (score) instead of the supply-side action (generate). Phase 1.5 resumes next run.

## Screen-map status
- Total `docs/screens/` files: 167 (per PR #2771 verification; ppt/ + reality/ + reality-mobile/; mobile/ RN not yet seeded)
- Drift signals this run: 0 (no merged PR touched a frontend route file in the trigger set — #2767 is a test file, #2770 is api-client packages)
- Orphan screens this run: 0 (PR #2771 confirmed 0 orphans tree-wide with `screen-map validate --strict`)
- Last `screen-map.yml` CI run on `main`: not checked this run

## Backlog deltas
- **Scored (was unscored stubs from prior dispatcher Tier-1d runs):**
  - [3] `code-review-api-handlers-share-pw-no-throttle` — SECURITY: public share-password endpoint has no throttle; brute-force at request speed (fast-track eligible)
  - [2] `code-review-api-handlers-share-log-proxy-ip` — audit log records TCP peer (proxy IP) not real client
  - [2] `code-review-ppt-web-ui-createfault-mock-ai` — hardcoded 'plumbing/85%/medium' AI suggestion shipped to every user
  - [2] `code-review-ppt-web-ui-moderation-fabricated-context` — DSA moderator shown fake `previous_violations=0` for every case
  - [2] `code-review-ppt-web-ui-moderation-no-i18n` — DSA enforcement flow on window.prompt/alert, hardcoded English, no validation
  - [1] `code-review-reality-web-comparison-view-i18n` — ComparisonView hardcoded English on 4-locale portal
  - [1] `code-review-reality-web-realtor-mgmt-i18n` — RealtorManagement delete-confirm modal hardcoded English
  - [1] `code-review-reality-web-agency-errorstate-i18n` — AgencyErrorStates no-agency state hardcoded English
- **New:** none (all today's new signals target files already fixed/tracked)
- **Bumped:** none
- **Decayed:** none (only open item >14d — `code-review-mobile-native-kmp-portfolio-analytics-caps-100` at 12d — hasn't crossed threshold yet)
- **Dropped:** none
- **Resolved this window (spot-check of merged PRs vs prior open items):** no open items matched the merged PRs by id or evidence pattern — all closed items on the merged list either landed under the dispatcher chain (issue-mint IDs like `gh-issue-2768`) or via `code-review-*` IDs already `done` on `dev` (#2762 → alexa-cert-fetch, #2765 → google-verify-fail-open, #2766 → oauth-refresh-unauthenticated, #2767 → protected-route-i18n-test).

## Plans promoted
- none this run — quality bar: promoting `code-review-api-handlers-share-pw-no-throttle` (security fast-track eligible now at score 3, confidence high) would benefit from an adversarial pass on the throttling design (per-token vs per-IP vs both, exponential backoff shape, lockout duration, in-process limiter reuse vs new abstraction). Defer to a subsequent run with adversarial capacity so the plan lands ready, not tentative.

## Auto-fix
- `no candidate` — no signal matched the Phase 5 allowlist (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review`) at high confidence and score ≥3 this run. See `signals/2026-08-16.json.auto_fix_skipped_reason`.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present)

## Open questions
- **Phase 1.5 skip:** was skipping the rotating expert review the right call given buffer starvation? Payload said "refill planner / score backlog vectors", so routine treated demand-side action (scoring the 8 dispatcher-Tier1d stubs) as strictly higher-value than generating more stubs. Confirmation would be a follow-up run that promotes the fresh security fast-track item and monitors whether the dispatcher's next backlog-refill picks up any of the 8 newly-scored items into the action-list. If it does, the skip was correct — Phase 1.5 can resume normal cadence.
- **Phase 1.6 skip:** management artifacts weren't refreshed this run. `pm-qa` last ran 2026-06-15 (2mo stale); `role_last_run` cadence is drifting. Next run should either invoke `ppt-project-management` for `pm-qa` or `text: "pm:qa"` to catch up.
- **Security fast-track for `share-pw-no-throttle`:** score 3 + confidence high + vector security — meets fast-track eligibility for immediate promotion. Deferred one run for adversarial-pass headroom. If the dispatcher picks it into the action-list before then, that path takes precedence.

## Quiet day?
- No — 9 merged PRs (5 security fixes on the voice-webhooks cluster + workflow-condition fail-closed + credential-leak defence in workflow api_call), 8 previously-unscored code-review vectors now scored (including 1 security fast-track candidate), 3 churn hotspot signals + 2 repeated-churn signals emitted.
