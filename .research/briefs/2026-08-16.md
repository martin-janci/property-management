# 2026-08-16

## Since last run
- Merged PRs: 9 (range #2762–#2771)
- Open PRs touched: 0
- New / updated issues: 2 (both closed same window — #2768 auto-escalated from stalled PR #2684, closed by merged PR #2769; #2366 building-id direct-upload follow-up, closed by merged PR #2770)
- Commits: 32 on `dev` (the 9 merged PRs above + dispatcher chore commits on planning branch)
- Routine lag: 1d 0h since last run (last_run_iso=2026-08-15T03:11Z; now=2026-08-16T03:14Z) — on schedule, no `lag_warning`.
- Phases that failed: none. Phase 1.6 **skipped this run** (deferred to on-demand local `/ppt-project-management scan` — `pm_cursor` NOT advanced, `coverage_cursor` NOT advanced).
- **Rerun 2026-08-16T06:29Z** — buffer-low trigger (`claimable=6/72 — refill planner (all open candidates mobile-native-gated)`). Window 03:14Z→06:29Z: 0 merged, 0 new issues, 0 new commits on `dev`, 1 open PR touched (#2772 — dispatcher fast-path capture of `churn-hotspot-voice-oauth-refresh-auth-tests`, opened 05:04Z as draft, fresh so no `stalled-review` signal). No new signals emitted; existing 5 signal IDs stay in `state.seen_signals` (score_delta=0 on any re-emit). `stats.quiet_days` bumped 2→3 for G2 advance.

## Shipped
- #2771 82-1-swiftui-project-setup-retry1: re-verify SwiftUI project-setup screen-map coverage (docs/screens/reality-mobile/project-setup.md re-checked; `screen-map validate --strict` 167 maps 0 errors)
- #2770 84-1-s3-presigned-urls: forward building association via `access_scope='building'` + `access_target_ids` on direct-to-S3 document upload (closes #2366; fixes the silent building_id drop from PR #2345 gap-84-1)
- #2769 gh-issue-2768: fail closed on unparseable workflow condition; adds `serde(deny_unknown_fields)` on `Condition` + `ConditionGroup` (closes #2768; retry of the quarantined PR #2684 that hit fix_rounds=3)
- #2766 code-review-api-handlers-voice-oauth-refresh-unauthenticated: require device-owner `AuthUser` + per-user rate limit on voice `/oauth/refresh`, plus 270-line regression suite pinning the 401/403/200 contract (SECURITY; new file `voice_oauth_refresh_auth_tests.rs`)
- #2765 code-review-api-handlers-voice-google-verify-fail-open: fail-closed Google Actions webhook JWT verification — RS256 signature over Google JWKS (cached 1h) + exact `aud=GOOGLE_ACTIONS_PROJECT_ID` + issuer + exp checks; replaces the old substring/STANDARD-base64 fall-through (SECURITY)
- #2767 churn-hotspot-protected-route-i18n-test: extend ProtectedRoute i18n suite with real-i18next runtime resolution, defaultValue↔en parity, key-classification integrity (170 lines)
- #2764 churn-hotspot-api-call-rs: refuse cross-origin redirects in workflow `api_call.rs` action (credential-leak SSRF hardening — reqwest strips only well-known sensitive headers cross-host; custom `X-Api-Key`-style secrets were being forwarded)
- #2763 churn-hotspot-marketplace-award-quote-idor-tests: close two IDOR coverage gaps on marketplace award — T4 foreign-RFQ path-param (org A awarding org B's RFQ using B's own quote), T5 nonexistent quote-id existence probe
- #2762 code-review-api-handlers-voice-alexa-cert-fetch-no-timeout: bound Alexa cert fetch with 5s connect + 10s request timeouts on a shared `reqwest::Client` (prevents unbounded cold-cache TLS client spawn on burst)

## Watch
- **Buffer-low nudge (rerun trigger):** dispatcher's claimable=6/72 with all 6 open action-list-mobile-native items KMP-gated (cannot build in cloud runner). Landable Rust/backend rows in this routine's own backlog (`churn-hotspot-voice-webhooks-rs`, `churn-hotspot-voice-oauth-refresh-auth-tests`) both sit at score 1 — below the +3 promotion floor, no new evidence this run to bump them. The one score≥3 item (`code-review-mobile-native-kmp-inquiries-response-contract`) is already promoted with a plan; it also needs local KMP build. Buffer refill requires either a local implementer session for the mobile-native cluster or fresh Phase 1.5 findings on ppt-web / api-server segments.
- **Fast-path capture in flight (PR #2772)** — dispatcher opened at 05:04Z as draft (auto-impl/churn-hotspot-voice-oauth-refresh-auth-t-65aad2e7). One 82-line test-only addition to `voice_oauth_refresh_auth_tests.rs` covering the platform-admin bypass arm the earlier suite (PR #2766) left untested. `mergeable_state=clean`; awaits CI + ready-flip.
- **Dispatcher archive wedge (issue #2743) still open** — PR #2744 approved+bot-labeled `needs-human-review` and still draft; unchanged since last run.
- **PR #2684 CLOSED via quarantine-exit SLA** — 7-day quarantine expired 2026-08-15, dispatcher opened issue #2768 as a clean-slate re-mint, and PR #2769 landed the fix cleanly today. Loop closed; branch preserved (`auto-impl/code-review-api-core-workflow-cond-parse-dd416f1f`).
- **Stalled accounting-cluster reviews** (#2555 / #2558 / #2559) — 16d open, 16d idle. Signal IDs already in `state.seen_signals` (no re-fire); existing `stalled-review-pr-*` backlog rows still at `needs-human-judgement`.
- **Reverted:** none this window.
- **Churn hotspots (this run, top-3 raw):**
  - `backend/servers/api-server/src/routes/voice_webhooks.rs` — +556 lines across PRs #2762 (+59) / #2765 (+407) / #2766 (+90); runs_seen=1 (**NEW hotspot**). Three back-to-back security landings on one 1.2k-LOC route file — monitor for further pressure.
  - `backend/servers/api-server/tests/suites/voice_oauth_refresh_auth_tests.rs` — +270 lines (new file, PR #2766); runs_seen=1 (**NEW**).
  - `frontend/apps/ppt-web/src/i18n/protectedRouteI18n.test.ts` — +170 lines (PR #2767); runs_seen=1 at start of run → 2 after this run. Re-emit of signal already in `state.seen_signals` (matched backlog row already `status=done`); `score_delta=0` per cumulative rule.
  - No `repeated-churn` firings — none of the top-3 had `runs_seen ≥ 2` at start of run (per the routine's strict-before-run rule).

## PRs stuck in draft despite approval
- #2744 fix(dispatcher): un-wedge oversize-archive push (#1162) + close retry-remint cross-issue blind spot — last update 2026-08-13 (age ~3d), still draft with bot approval only + `needs-human-review` label. This is the fix for issue #2743's archive wedge; needs the operator's ack to un-draft.

## PRs with verdict=changes, no fix-round progress in 24h
- Previously-tracked `code-review-api-core-workflow-cond-parse-failopen` PR#2684 — RESOLVED via the quarantine-exit SLA (closed, re-minted as issue #2768, landed as PR #2769). Removed from watch.
- none other (planning-branch assignments.json not read live this run — HTTPS proxy budget conservation)

## Code review slice
- Segment reviewed: **mobile-native-kmp** (reason: oldest-unreviewed cursor 2026-08-11; no Phase-1 churn hit this segment this run)
- Experts: kotlin + tester
- Findings: 2 (both high-confidence — 1 promoted to a plan, 1 stays at score 2 below promotion floor)
- Next segment: `ppt-web-core` (oldest unreviewed after this run: 2026-08-12)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 168 (ppt=106, reality=47, reality-mobile=16; mobile=0 — not yet seeded, drift detection disabled for the mobile trigger set)
- Drift signals this run: 0 (no PR in-window touched `frontend/apps/{ppt-web/src/{App.tsx,routes/**},reality-web/src/app/**}`; #2771 touched a `docs/screens/reality-mobile/*.md` file directly — a screen-doc update, not a route change)
- Orphan screens this run: 0 (no route-basename churn to trigger the spot-check)
- Last `screen-map.yml` CI run on `main`: (not queried this run — HTTPS proxy budget)

## Backlog deltas
- **New:** [3] `code-review-mobile-native-kmp-inquiries-response-contract` bug (Phase 1.5 finding — HIGH conf, **promoted, plan written**)
- **New:** [2] `code-review-mobile-native-kmp-httpclient-no-timeout` bug (Phase 1.5 finding — HIGH conf, below the score≥3 promotion floor, stays in backlog)
- **New:** [1] `churn-hotspot-voice-webhooks-rs` refactor (3-PR consolidation hotspot; monitor)
- **New:** [1] `churn-hotspot-voice-oauth-refresh-auth-tests` test-gap (new 270-line auth-regression suite from PR #2766; monitor for future contract drift)
- **Bumped:** none this run (all new signal IDs; no re-scoring of existing IDs).
- **Decayed:** none — no `open` items with `updated_at ≤ 2026-08-02`.
- **Dropped:** none.
- **Resolved to `done` (by dispatcher fast-path, verified this run against the merged diffs):** already recorded in `backlog.json` before this routine ran — `code-review-api-handlers-voice-google-verify-fail-open` (PR #2765), `code-review-api-handlers-voice-oauth-refresh-unauthenticated` (PR #2766), `code-review-api-handlers-voice-alexa-cert-fetch-no-timeout` (PR #2762), `churn-hotspot-api-call-rs` (PR #2764), `churn-hotspot-marketplace-award-quote-idor-tests` (PR #2763), `churn-hotspot-protected-route-i18n-test` (PR #2767).
- **Resolution check this run (Phase 2, step 6):** 3 open `mobile-native-kmp` items still have evidence present on `dev`. Grepped: `PortalListingsRepository.kt:113` still contains `limit = 100`; `ListingRepository.kt` still has 3 `catch (e: Exception)` blocks (lines 39/59/74); no `SsoServiceTest.kt` under `commonTest/**/auth/` (only `SsoInitiationTest.kt` and `SsoStateStoreTest.kt`). No merged PR body referenced any of the 3 item IDs verbatim. No transition — all 3 stay `open`.

## Plans promoted
- `plans/code-review-mobile-native-kmp-inquiries-response-contract.md` — align KMP `InquiriesResponse` field (`page_size` → `limit`) + query parameter with reality-server contract; make `page`/`limit` nullable so realtor endpoint `{inquiries, total}` also parses; add first-of-its-kind `InquiryRepositoryTest.kt` with Ktor `MockEngine` · **adversarial pass: passed** (Evidence → Hypothesis → Suggested approach → Test plan chain intact — every hypothesis claim traces to Evidence, approach addresses the DTO-vs-wire drift the hypothesis names, IG3 test would fail on `main` today because no such test exists and would pass after the DTO rename; two alternatives concretely rejected (backend-side rename, per-endpoint DTOs); no placeholder rot in plan text)

## Auto-fix
- `no candidate` — 2 new Phase 1.5 findings are `bug` vector (score 3 and 2), outside the Phase 5 allowlist (which permits only `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review`). The 3 churn-hotspot signals are also outside the allowlist. Stalled-review re-fires suppressed (prior IDs in `seen_signals`). No `screen-map-drift` this run.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present)

## Open questions
- None new. Existing open questions (PR #2744 human ack for archive wedge; the accounting-cluster reviewer bottleneck on #2555/#2558/#2559) unchanged from prior briefs.

## Quiet day?
- No (day-cumulative) — 9 PRs merged, 1 bug plan promoted, 2 mobile-native-kmp code-review findings emitted, 3 churn hotspots recorded (1 re-emit at score_delta=0).
- Yes (this rerun window, 03:14Z→06:29Z) — 0 merges / 0 new issues / 0 new signals; `stats.quiet_days` bumped 2→3.
