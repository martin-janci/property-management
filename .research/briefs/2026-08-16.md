# 2026-08-16

<sub>Two runs today. First run (03:14 UTC) is the daily observation pass and remains the record for merged-PRs / churn / Phase 1.5 review. This is the **second run**, a refill pass fired by the dispatcher's `buffer-low: claimable=6/72` alert — the cloud dispatcher's buffer was starved because every open backlog item was mobile-native-kmp (cloud-unlandable). Same-day rerun; per the routine's idempotent-rewrite rule this file overwrites the 03:14 version, which is preserved in git history at HEAD~1.</sub>

## Since last run
- Merged PRs since 03:14 UTC: **1** (#2772 — churn-hotspot-voice-oauth-refresh-auth-tests: pin platform-admin bypass arm of /oauth/refresh authz). Its resolution against `backlog.json` was already reconciled by the dispatcher fast-path commits before this pass; no new-signal work here.
- Open PRs touched: **0** new. PR #2744 dispatcher archive-wedge still stuck in draft with bot approval + `needs-human-review` — unchanged from the 03:14 brief.
- New / updated issues: **0** since 03:14 UTC.
- Commits on `dev` since 03:14: **10** — of which **1** is a real application change (#2772 merge) and **9** are `chore(research)` dispatcher chore commits on the planning branch (tier1d generator + fast-path capture + hourly ticks + Phase-8 self-review).
- Routine lag: 0d 11h since 03:14 UTC — on schedule (`lag_warning` threshold is 36h; not fired).
- Phases that failed: **none.** Phase 1.5 (rotating expert review) SKIPPED — already ran at 03:14 UTC and picked mobile-native-kmp. Phase 1.6 (project-management) SKIPPED — refill trigger, not a full pipeline; `pm_cursor` and `coverage_cursor` unchanged from HEAD.

## Refill trigger
- Trigger payload: `buffer-low: claimable=6/72 — all open backlog is mobile-native (cloud-unlandable); need landable (backend/frontend) vectors — refill planner`.
- Verified pre-refill state on this branch: 5 `open` backlog items, ALL under `mobile-native/` paths (portfolio-analytics-caps-100, cancellation-swallowed, ssoservice-untested, inquiries-response-contract, httpclient-no-timeout). Zero open backend/frontend rows.
- Root cause of the gap: the dispatcher's Tier-1d demand-driven code-review generator has been quietly producing landable findings for 15+ days into `.research/signals/2026-08-*-<segment>-tier1d*.json`, but those signal files are not ingested by the routine's own Phase 2 unless a routine pass explicitly picks them up. Between 2026-08-15 and 2026-08-16 **29 unseen** Tier-1d code-review findings accumulated across api-core (2), ppt-web-core (5), ppt-web-ui (10), reality-web (5), mobile-native-kmp (3), mobile-rn (2). None of them were in `state.seen_signals`.
- Response: this refill run ingests **all 29** into `backlog.json`, records them in `state.seen_signals`, merges same-defect duplicates, applies the dead-code filter, and promotes **1** landable plan. The rest score-stack in backlog and will promote naturally as more evidence arrives.

## Shipped
- #2772 churn-hotspot-voice-oauth-refresh-auth-tests — pins the platform-admin bypass arm of `/oauth/refresh` authz (single 5-line regression test, already tracked in backlog as `churn-hotspot-voice-oauth-refresh-auth-tests` → status=done).

## Watch
- **Dispatcher buffer starvation is a structural problem, not a one-off.** Prior 2026-08-16 03:14 brief noted the same imbalance obliquely (all-mobile-native open backlog). The dispatcher's Phase-8 self-review commit today (99489b3) explicitly bumped a recurring finding: `buffer-starved=28` (recurrence count). This refill unblocks one cycle but doesn't fix the pipeline — the routine should either (a) run Phase 2 ingestion whenever `.research/signals/*-tier1d*.json` files are newer than `state.last_run_iso`, or (b) the dispatcher's Tier-1d generator should surface a bright signal ("29 findings queued, none ingested") into the daily brief. Filed as an open question below.
- **Dispatcher archive wedge (#2743) still open**, PR #2744 unchanged — reproduced verbatim from the 03:14 brief.
- **Stalled accounting-cluster reviews** #2555 / #2558 / #2559 — 16d+ open, unchanged.
- **Churn hotspots this run:** none new. #2772 touches `voice_oauth_refresh_auth_tests.rs` (already a hotspot at the 03:14 run with `runs_seen=1` → 2 now; re-emit at `score_delta=0` per cumulative rule; matched backlog row already at `status=done`, no re-scoring).

## PRs stuck in draft despite approval
- #2744 fix(dispatcher): un-wedge oversize-archive push (#1162) + close retry-remint cross-issue blind spot — last update 2026-08-13 (age ~3d), still draft with bot approval only + `needs-human-review` label. Unchanged from 03:14 brief.

## PRs with verdict=changes, no fix-round progress in 24h
- none (planning-branch assignments.json not queried live this run — HTTPS proxy budget)

## Code review slice
- Segment reviewed: **none this run** — Phase 1.5 already fired at 03:14 UTC on `mobile-native-kmp`. Cursor unchanged. Next scheduled segment (`ppt-web-core`, oldest unreviewed) is picked up by the next daily run.
- Skipped: yes — same-day refill trigger; skip is the correct behavior (skill re-invocation on the same day would either duplicate the mobile-native-kmp findings or advance the cursor twice).

## Screen-map status
- Total `docs/screens/` files: **168** (ppt=106, reality=47, reality-mobile=16; mobile=0 — unchanged).
- Drift signals this run: **0** (no PR merged since 03:14 UTC touched any of the trigger route files).
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: (not queried this run — HTTPS proxy budget)

## Backlog deltas

**Ingested from dispatcher Tier-1d (29 signals → 26 new rows + 1 merged duplicate demoted to dropped):**

- **New:** [4] `code-review-ppt-web-core-api-onunauthorized-unwired` bug — MERGED (`api-onunauthorized-unwired` HIGH + `api-client-no-onunauthorized` medium; same defect, canonical id = HIGH-conf one). **Promoted — plan written.**
- **New:** [2] `code-review-api-core-delay-duration-overflow` bug — `DelayConfig::to_duration` uses unchecked `*` before its own 7-day range check, so a workflow config `{duration: u64::MAX, unit: "days"}` panics in debug builds and silently wraps in release. Backend-only fix; single-file surgical change to `services/actions/delay.rs`. Score 2 — below promotion floor; will score-stack on next signal.
- **New:** [2] `code-review-ppt-web-core-authctx-init-no-exp-check` bug — cold-boot restores session as authenticated whenever a stored user + stored access token exist, no `exp` check on the token; adjacent-but-distinct from the 401 recovery gap (that one is runtime interception; this one is init-path).
- **New:** [2] `code-review-ppt-web-core-ws-event-name-mismatch` bug **HIGH conf** — WebSocketContext.tsx subscribes to colon-form event names (`entity:announcement`, `fault.created` → `entity:fault` etc.); server publishes dot-form (`fault.created`, `notification.created`). Zero overlap → WebSocket-driven query invalidation is ENTIRELY DEAD. Real-time pushes never invalidate any cache; UI shows stale data until manual refetch. Both ends traced; high confidence. Under promotion floor at score 2 — this is the routine's known "single HIGH-conf bug can't self-promote" pattern; would benefit from a second confirming signal or an explicit user tag.
- **New:** [2] `code-review-ppt-web-ui-moderation-unvalidated-cast` bug — DSA moderation `handleTakeAction` casts a raw `window.prompt` string with `as 'remove'|'restrict'|'warn'|'approve'` and sends to the API with zero runtime validation. Sibling AmlDashboardPage uses `isAmlReviewDecision` guard — same feature dir, divergent safety.
- **New:** [2] `code-review-reality-web-compare-label-currency-guard` bug — reality-web ComparisonView `getHighlight` (line 163-169) has an unguarded numeric-highlight path that mis-renders when a listing's currency differs from the comparison scope.
- **New:** [2] `code-review-reality-web-inquiry-cancel-no-error` bug — `handleCancel` on inquiries/page.tsx fires the cancelInquiry mutation and closes the confirm dialog immediately, with NO `onError` / no `isError` branch / no `mutateAsync` try/catch; on failure the UI silently keeps the inquiry as "active" while the user believes it was cancelled.
- **New:** [2] `code-review-reality-web-journal-newsletter-fake-submit` bug — reality-web journal newsletter subscribe handler is a fire-and-forget stub with no error state; distinct from the ppt-web CreateDisputePage (which turned out to be dead code — see below).
- **New:** [2] `code-review-mobile-native-kmp-decimal-serializer-throws-malformed` bug (mobile-native — not landable in cloud; ingested for tracking).
- **New:** [2] `code-review-mobile-native-kmp-nav-loaders-swallow-errors` bug (mobile-native — not landable).
- **New:** [1] `code-review-api-core-docgen-empty-llm-ok` bug — `document_generation.rs:536-540` collapses a missing LLM completion to an empty string instead of surfacing the error to the caller.
- **New:** [1] `code-review-ppt-web-core-unverified-jwt-role` bug — `AuthContext.decodeJwtPayload` (line 220-233) is an UNVERIFIED base64-JSON decode used to derive the user role for gating; client-forged JWT with `role=admin` would gate a client-side admin route open (server still enforces on the wire).
- **New:** [1] `code-review-mobile-native-kmp-profilestats-serial-fetch` bug (mobile-native — not landable).
- **New:** [1] `code-review-mobile-rn-meters-hardcoded-label`, [1] `code-review-mobile-rn-nfc-wallet-hardcoded-i18n` — mobile-rn i18n gaps.
- **New:** [1] × 8 ppt-web-ui i18n / minor-bug rows (compliance-alert-i18n, compliance-prompt-i18n-gap, confirm-dialog-i18n, disputedetail-i18n-gap, disputelist-i18n-gap, langswitcher-aria-i18n, langswitcher-promise, moderation-full-reload-nav).
- **New:** [1] × 3 reality-web i18n / refactor rows (compare-exportpdf-stub, comparison-i18n-gap, pricemap-hardcoded-locale).

**Dead-code filter fired (Phase-1 rule extension to frontend components):**

- **Dropped:** [0] `code-review-ppt-web-ui-createdispute-fake-submit` merged-canonical (was `createdispute-fake-submit` + `createdispute-stub-discard`). Dead-code note added to evidence: `CreateDisputePage.tsx` is not imported by any route — `grep -rn 'CreateDisputePage' frontend/apps/ppt-web/src` finds only `pages/index.ts` (re-export) and the file itself. The LIVE `/disputes/new` route uses `FileDisputePageRoute` → `FileDisputePage` → `useCreateDispute` (`routes/groups/disputes.tsx:482`, `FileDisputePageRoute.tsx:32-42`) — properly wired. Signal IDs still added to `seen_signals` to prevent re-emit; if `CreateDisputePage.tsx` is later ROUTED or REMOVED, that state change becomes its own signal.

**Bumped / Decayed / Resolved:** none this run (single-purpose refill pass; no re-scoring of existing IDs, no `updated_at ≤ 2026-08-02` items eligible for decay, no PR body verbatim-matched an open ID).

## Plans promoted
- `plans/code-review-ppt-web-core-api-onunauthorized-unwired.md` — repair the `configureApiClient({ getToken })` call site at `AuthContext.tsx:337` to also pass an `onUnauthorized` handler that runs `refreshToken()` with single-flight semantics and falls back to `logout()`; adds two new tests (`api.401-recovery.test.ts` and `AuthContext.401-wiring.test.tsx`) that fail on `main` today because the callback is never passed; scoped to the hand-rolled axios client only, generated `@ppt/api-client` deferred to a separate plan. · **adversarial pass: passed** (Evidence → Hypothesis chain: two independent expert reviews HIGH+medium naming the same file:line pair; Suggested approach → Test plan chain: tests exercise the callback wiring directly, would fail today; Alternatives concretely rejected: fixing on the generated client, deleting the dead branch; Root-cause trace: symptom → interceptor gate → module `let` writer → call site closure; File paths all resolve on disk; no placeholder rot).

## Auto-fix
- `no candidate` — allowlist requires `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, or `stalled-review`; none of those types fired this run. All 29 ingested signals are `code-review-finding` (outside the allowlist by design — code fixes go through the manual/dispatched implementer path, not the routine's auto-fix flow).

## Self-review findings
- none (`.research/self-improvement/findings.json` not present on this branch)

## Open questions
- **Structural: routine → dispatcher Tier-1d ingestion gap.** The dispatcher's demand-driven generator produced 29 landable findings over 2 days that the daily routine never picked up. Should Phase 2 auto-scan `.research/signals/*-tier1d*.json` files newer than `state.last_run_iso` and ingest un-seen IDs? Alternative: dispatcher should surface a bright signal into the routine's brief when its own generator has queued >N un-ingested signals. Prefer the routine-side change — it keeps the ingestion contract in one place — but this needs the operator's ack because it changes the routine's observation surface.
- Existing open questions unchanged: PR #2744 human ack for archive wedge; accounting-cluster reviewer bottleneck on #2555/#2558/#2559.

## Quiet day?
- No — refill trigger fired; 26 net new backlog rows, 1 plan promoted, 1 dead-code demotion, 29 signal IDs added to `seen_signals`. State advanced. Purpose of the run was met: the dispatcher now has a landable `ready` frontend/ppt-web-core plan to claim, breaking the mobile-native-kmp lock.
