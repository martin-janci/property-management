# 2026-08-16

## Since last run
- Merged PRs: 1 (range #2772–#2772; 9-hour delta on top of the 03:14 UTC same-day brief which covered #2762–#2771)
- Open PRs touched: 0
- New / updated issues: 0
- Commits: 10 on `dev` (1 code-landing PR #2772 merge, 4 dispatcher chore commits, 5 dispatcher tier1d dev-review generator commits producing signal files)
- Routine lag: 9h since last run (last_run_iso=2026-08-16T03:14Z) — same-day rerun, no `lag_warning`.
- Phases that failed: none. Phase 1.5 **skipped this rerun** (dispatcher tier1d already ran dev-review on `ppt-web-ui` (×2), `ppt-web-core`, `mobile-rn` today — 10 findings already deposited as signal files and absorbed into backlog this rerun; running the routine's own skill on top would duplicate coverage). Phase 1.6 **skipped this rerun** (already skipped in 03:14 UTC brief; `pm_cursor` and `coverage_cursor` unchanged).

## Shipped
- #2772 churn-hotspot-voice-oauth-refresh-auth-tests: pin platform-admin bypass arm of `/oauth/refresh` authz (**test-only** — +82/-2 in `voice_oauth_refresh_auth_tests.rs`, adds Test 5 for the `is_platform_admin()` non-owner allow path that Test 3 didn't cover; factors `mint_access_token_with_role`; no production code touched).

## Watch
- **Dispatcher archive wedge (issue #2743) still open** — PR #2744 unchanged since 03:14 UTC brief; still draft w/ `needs-human-review`. Blocks the archive-append reconcilers when a terminal transition lands.
- **Buffer-low trigger (`buffer-low: claimable=6/72 — refill planner`)** — dispatcher's claimable pool is starved; all 6 claimable rows are mobile-native-kmp gated (`.research/mobile-native-gate.sh` fails in cloud on AGP `dl.google.com` egress-403, tracked #2652). This rerun did **not** promote any new plan — nothing new met the readiness bar (score ≥ 3, or vector=security + confidence=high + score ≥ 2). The 10 fresh tier1d findings absorbed this run cap at score 2, none are security vector, so the buffer stays starved on the routine's side. Structural unblock (macOS/AGP runner OR bridge-MCP route) is operator-side.
- **Reverted:** none this window.
- **Churn hotspots (this rerun):**
  - `backend/servers/api-server/tests/suites/voice_oauth_refresh_auth_tests.rs` — +82 more lines from PR #2772 (already at runs_seen=1 from 03:14 brief); runs_seen → 2 this rerun. Cumulative-signal rule — signal id `churn-hotspot-voice-oauth-refresh-auth-tests` already in `state.seen_signals` from earlier today; NOT re-scored (`score_delta=0`). New churn evidence appended to hotspot_history only.
  - No new top-3 churn beyond that in the 9-hour window.

## PRs stuck in draft despite approval
- #2744 fix(dispatcher): un-wedge oversize-archive push (#1162) + close retry-remint cross-issue blind spot — last update 2026-08-13 (age ~3d), still draft with bot approval + `needs-human-review` label. Unchanged since 03:14 UTC brief.

## PRs with verdict=changes, no fix-round progress in 24h
- none (planning-branch `assignments.json` on this feature-branch clone shows 1 `review` row; no `verdict=changes` this rerun — planning-branch state not read live for HTTPS-budget reasons)

## Code review slice
- Segment reviewed: **none by the routine this rerun** — dispatcher tier1d already covered 3 segments today (`ppt-web-ui` twice, `ppt-web-core`, `mobile-rn`), and the 03:14 UTC routine covered `mobile-native-kmp`.
- Experts: n/a (routine skill not invoked this rerun)
- Findings absorbed from dispatcher tier1d files → backlog this rerun: **10** (all vector=bug, mostly medium confidence, scores 1–2)
- Next segment (routine cursor): oldest unreviewed is `api-core` / `reality-server` (both 2026-08-13); will be picked by the next full-cadence run.
- Skipped: yes — same-day rerun + dispatcher coverage overlap.

## Screen-map status
- Total `docs/screens/` files: 168 (unchanged from 03:14 UTC brief; no new route churn this window)
- Drift signals this run: 0 (no PR in-window touched `frontend/apps/{ppt-web/src/{App.tsx,routes/**},reality-web/src/app/**}`)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this rerun (HTTPS proxy budget)

## Backlog deltas
- **New:** [2] `code-review-ppt-web-core-authctx-init-no-exp-check` bug (dispatcher tier1d medium; cold-boot restores expired-token session, no exp check)
- **New:** [2] `code-review-ppt-web-core-api-onunauthorized-unwired` bug (dispatcher tier1d HIGH; hand-rolled api.ts 401 branch is dead, onUnauthorizedCallback never wired at the sole configureApiClient call site)
- **New:** [2] `code-review-ppt-web-ui-moderation-unvalidated-cast` bug (dispatcher tier1d medium; DSA moderation window.prompt cast to enum with no validation)
- **New:** [1] `code-review-ppt-web-ui-compliance-prompt-i18n-gap` bug (dispatcher tier1d medium; compliance prompt/alert copy hardcoded English)
- **New:** [1] `code-review-ppt-web-ui-moderation-full-reload-nav` bug (dispatcher tier1d medium; window.location.href full-reload instead of react-router navigate)
- **New:** [1] `code-review-ppt-web-ui-confirm-dialog-i18n` bug (dispatcher tier1d r0821 medium; ConfirmationDialog hardcoded 'Processing...')
- **New:** [1] `code-review-ppt-web-ui-langswitcher-aria-i18n` bug (dispatcher tier1d r0821 medium; LanguageSwitcher aria-label hardcoded English)
- **New:** [1] `code-review-ppt-web-ui-langswitcher-promise` bug (dispatcher tier1d r0821 medium; i18n.changeLanguage Promise discarded)
- **New:** [1] `code-review-mobile-rn-nfc-wallet-hardcoded-i18n` bug (dispatcher tier1d medium; Apple Wallet pkpass fields hardcoded English)
- **New:** [1] `code-review-mobile-rn-meters-hardcoded-label` bug (dispatcher tier1d medium; MetersScreen renders raw meter_number as label)
- **Bumped:** none this rerun (all 10 new signal IDs are new to `state.seen_signals`).
- **Decayed:** none — no `open` items with `updated_at ≤ 2026-08-02`.
- **Dropped:** none.
- **Resolved to `done`:** none this rerun. The single merged PR (#2772) is test-only extension of an already-`done` item (`churn-hotspot-voice-oauth-refresh-auth-tests`); no open backlog id was resolved.
- **Resolution check this rerun:** skipped for the 5 open `mobile-native-kmp` items — earlier 03:14 UTC brief already ran the grep and found evidence still present on `dev`; no merged PR in the 9-hour window referenced any of those ids verbatim; nothing to re-check.

## Plans promoted
- none this rerun — no candidate met the promotion floor.
  - **Standard readiness** (`score ≥ 3`, `status=open`, confidence not all-low, concrete source+files, testable hypothesis): the only open item at score ≥ 3 is `code-review-mobile-native-kmp-inquiries-response-contract`, which already has a plan (promoted this morning).
  - **Security fast-track** (`vector=security`, `confidence=high`, `score ≥ 2`): none of the 5 pre-existing opens nor the 10 new tier1d additions are `security` vector. `code-review-ppt-web-core-api-onunauthorized-unwired` is high-confidence but vector=`bug`, so the fast-track doesn't apply. Quality > volume — buffer stays starved.

## Auto-fix
- `no candidate` — the 10 new tier1d findings are all `bug` vector, outside the Phase 5 allowlist (which permits only `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review`). The single merged PR (#2772) produced no allowlisted signal. No stalled-review re-fires (all older stalled PR ids in `state.seen_signals`). No `screen-map-drift` (no route file touched this window).

## Self-review findings
- none (`.research/self-improvement/findings.json` not present)

## Open questions
- None new. Existing open questions carry over from the 03:14 UTC brief:
  - PR #2744 human ack for the archive-wedge fix (issue #2743) — un-draft + merge, or explicit reject.
  - Accounting-cluster reviewer bottleneck on PR #2555/#2558/#2559 (16d idle) — assign reviewer or close.
  - Structural cloud-runner mobile-native-kmp gate (`#2652`) — macOS/AGP runner OR ppt-bridge MCP route so the 4 mobile-native-kmp claimable rows become landable and stop starving the buffer.

## Quiet day?
- No — 1 additional PR merged, 10 dispatcher tier1d findings absorbed into backlog (2 at score 2, including a HIGH-confidence auth/session bug), churn hotspot advanced (runs_seen 1→2 on the voice-oauth-refresh test file). Aggregate day so far: 10 merged PRs, 1 plan promoted, 12 code-review findings (2 mobile-native-kmp + 10 tier1d).
