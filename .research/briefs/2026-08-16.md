# 2026-08-16

## Since last run
- Merged PRs: 9 (range #2762–#2771; PR #2768 is an issue not a PR)
- Open PRs touched: 0 (no non-dependabot activity since last run window opened; dependabot bulk-updates #2749/#2750/#2751 from 2026-08-13 still open, unchanged)
- New / updated issues: 2 (#2366 closed by PR #2770 — direct-to-S3 upload building_id; #2768 closed by PR #2769 — workflow condition fail-closed; both auto-opened by post-merge review then closed by the follow-up PRs)
- Commits: 29 on `dev` (9 merged PRs + 20 dispatcher chore commits on planning branch that landed via research-land replay onto dev)
- Routine lag: 0d 21h since last run (last_run_iso=2026-08-15T03:11:00Z; now=2026-08-16T00:42Z) — on schedule.
- Phases that failed: none. Phase 1.6 **skipped this run** (deferred to on-demand local `/ppt-project-management scan` — `pm_cursor` NOT advanced; same posture as 2026-08-15).

## Shipped
- #2762 code-review-api-handlers-voice-alexa-cert-fetch-no-timeout: bound Alexa cert fetch with process-wide shared reqwest::Client behind connect(5s)/request(10s) timeouts
- #2763 churn-hotspot-marketplace-award-quote-idor-tests: +2 IDOR regression tests for path-param org layer + missing-quote branch (award endpoint hardening)
- #2764 churn-hotspot-api-call-rs: pin workflow HTTP client redirects to same origin (scheme/host/port) — closes credential-leak SSRF channel on Authorization + custom headers
- #2765 code-review-api-handlers-voice-google-verify-fail-open (SECURITY): real RS256 JWT verification via Google JWKS — replaces prior substring-match + STANDARD-base64-mis-decode fail-open path
- #2766 code-review-api-handlers-voice-oauth-refresh-unauthenticated (SECURITY): require AuthUser + device-owner authorization + shared sliding-window rate-limit on POST /webhooks/voice/oauth/refresh (was accepting anonymous `{device_id}`)
- #2767 churn-hotspot-protected-route-i18n-test: extended `protectedRouteI18n.test.ts` from static JSON/regex assertions to runtime i18next resolution + defaultValue parity + key-classification integrity (16→61 test cases)
- #2769 gh-issue-2768: fail closed on unparseable workflow condition — `#[serde(deny_unknown_fields)]` + parse-error → WorkflowError::ConditionError (was silently coercing unknown JSON to empty And-group evaluating as vacuously true, then fail-open on the impossible-to-reach else branch). Closes #2768.
- #2770 84-1-s3-presigned-urls: forward building association via `access_scope='building' + access_target_ids=[buildingId]` on direct-to-S3 `uploadDocumentDirect` (was silently dropping `buildingId` — building-scoped uploads registered as org-scoped and vanished from building filtering). Closes #2366.
- #2771 82-1-swiftui-project-setup-retry1: record 2026-08-15 re-verification of `docs/screens/reality-mobile/project-setup.md` — coverage gap was stale (map has existed on `dev` since 2026-06-17). Docs-only; no code change.

## Watch
- **PR #2684 closed unmerged** — quarantined 7d+ on `code-review-api-core-workflow-cond-parse-failopen`, then closed by the dispatcher's quarantine-exit SLA. The vector was re-minted as issue #2768 → fixed by PR #2769 (merged 2026-08-15). Clean recovery via the quarantine → re-mint → new-PR loop.
- **Dispatcher archive wedge (issue #2743) still open** — PR #2744 remains draft (approved+bot-labeled `needs-human-review`, self-modifying dispatcher scripts). Unchanged since 2026-08-13; no new activity this run. Every terminal transition on the planning branch continues to risk tripping the 64 KiB MCP push ceiling.
- **Stalled accounting-cluster reviews** (#2555 / #2558 / #2559) — 19d open, 17d idle now. Signal IDs already in `state.seen_signals` so no re-firing; existing `stalled-review-pr-*` backlog rows still at `needs-human-judgement`.
- **Buffer starvation (dispatcher planner side)** — trigger payload reports `claimable=4/72 landable≈0 (backlog is all mobile-native/KMP, unlandable in cloud)`. The routine's own backlog has 3 truly `open` items, all mobile-native/KMP (portfolio caps-100, cancellation-swallowed, sso-service-untested) with score ≤2 — below the promotion floor even under the security fast-track. Nothing landable to promote this run. Refill has to come from Phase 1.5 code-review of landable segments (or the dispatcher's Tier 1d generator).
- **Reverted:** none this window.
- **Churn hotspots (this run, top-3 raw):**
  - `backend/servers/api-server/src/routes/voice_webhooks.rs` — +556 lines this run, runs_seen=1 (new hotspot — three back-to-back security-hardening PRs: #2762 Alexa timeout +59, #2765 Google JWT fail-closed +407, #2766 voice OAuth refresh authz +90; file is now 2686 lines)
  - `backend/servers/api-server/tests/suites/voice_oauth_refresh_auth_tests.rs` — +270 lines this run, runs_seen=1 (new file, from PR #2766 — auth regression suite for the OAuth-refresh gate)
  - `frontend/apps/ppt-web/src/i18n/protectedRouteI18n.test.ts` — +170 lines this run, runs_seen=2 (was runs_seen=1 last run from PR #2757; PR #2767 extended it further). Cumulative churn-hotspot re-emit (score_delta=0) — no `repeated-churn` emitted separately because the underlying vector was resolved by PR #2767 (intentional coverage expansion, not instability).

## PRs stuck in draft despite approval
- #2744 fix(dispatcher): un-wedge oversize-archive push (#1162) + close retry-remint cross-issue blind spot — last update 2026-08-13, still draft with bot approval only + `needs-human-review` label. Age ~3d. Unchanged from prior brief — needs the operator's ack to un-draft.

## PRs with verdict=changes, no fix-round progress in 24h
- none new this run (planning-branch assignments.json not read live this run — HTTPS proxy budget). PR #2684 was the sole prior `verdict=changes,quarantined` row and was closed by the dispatcher's quarantine-exit SLA this run (resolved via re-mint → #2769).

## Code review slice
- Segment reviewed: **api-handlers** (reason: churn-aligned — `voice_webhooks.rs` was the top churn file this run; other candidate segments unreviewed longer, but churn-alignment wins per skill Step 1)
- Experts: rust + security
- Findings: 0 — the three PRs that generated the churn (#2762, #2765, #2766) are themselves the hardening (bounded HTTP client + fail-closed JWT verify + AuthUser/owner/rate-limit); grep passes on the post-fix code (unwrap/expect in non-test paths, fail-open patterns, missing rate-limit coverage) found no residuals. Not a saturation-skip (0 open code-review-finding items for api-handlers with valid `status == "open"`; the 2 raw signal rows folded into `backlog.json` by the dispatcher's Tier 1d generator have `status = null` and don't count).
- Next segment: `reality-server` (oldest-unreviewed after this run: 2026-08-13 — tied with `api-core`, but reality-server ordered first)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 168 (ppt=106, reality=47, reality-mobile=15; mobile=0 — not yet seeded, drift detection disabled for the mobile trigger set)
- Drift signals this run: 0 (no PR in-window touched `frontend/apps/{ppt-web/src/App.tsx,ppt-web/src/routes/**,reality-web/src/app/**}`; PR #2771 touched only `docs/screens/reality-mobile/project-setup.md` which is not in the current drift trigger set)
- Orphan screens this run: 0 (no route-basename churn to trigger the spot-check)
- Last `screen-map.yml` CI run on `main`: (not queried this run — HTTPS proxy budget conservation; PR #2771 docs-only touch would have exercised the workflow)

## Backlog deltas
- **New:** [1] `churn-hotspot-voice-webhooks-rs` refactor — new hotspot after 3 back-to-back security-hardening PRs (#2762/#2765/#2766); monitor for further churn.
- **New:** [1] `churn-hotspot-voice-oauth-refresh-auth-tests-rs` test-gap — new 270-line auth regression suite from PR #2766; monitor for churn indicating coverage gaps.
- **Bumped:** none
- **Decayed:** none (no open item ≥14d without new signal — the 3 open KMP rows are 5–12d old, the 2 new hotspot rows are 0d)
- **Dropped:** none
- **Resolved (already done via dispatcher's Phase 2.6 backlog-reconcile before this run):** `code-review-api-handlers-voice-alexa-cert-fetch-no-timeout` (#2762), `churn-hotspot-marketplace-award-quote-idor-tests` (#2763), `churn-hotspot-api-call-rs` (#2764), `code-review-api-handlers-voice-google-verify-fail-open` (#2765), `code-review-api-handlers-voice-oauth-refresh-unauthenticated` (#2766), `churn-hotspot-protected-route-i18n-test` (#2767) — the routine's Phase 2 resolution check found all matching items already `done` (no state change needed).

## Plans promoted
- none this run — the 3 truly `open` backlog items are all mobile-native/KMP with score ≤ 2 (below promotion floor even under the security fast-track). The 2 hotspot rows added this run are score=1. The 5 raw code-review-finding signals folded into `backlog.json` by the dispatcher's Tier 1d generator (2 api-handlers + 3 reality-web) have `status = null` (broken item shape) — not eligible for promotion by the routine; leaving them to the dispatcher's Phase 2.6 backlog-reconcile to heal.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The two new signals are `churn-hotspot` type (not in Phase 5 allowlist); no screen-map-drift / screen-map-orphan / fixme-in-merged-code / dependabot lockfile-drift / new stalled-review fired this run.

## Self-review findings
- open=34 (high=7, medium=24, low=3); new this run=0; acknowledged=19
- fp-mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=15] — dispatcher hard-assumes `git push` is proxy-403-blocked, so PUSH_METHOD defaults to MCP + 64KiB size-guard; in this env direct `git push origin dev` succeeds; fix: runtime probe at Phase 1 instead of the hard-coded assumption
- fp-mcp-push-cannot-inline-large-archive [recurrence=15] — Phase 6 archive-move produced a 198KB assignments-archive.json but the 64KiB MCP inline ceiling blocks it; fix: push the state commit via a new branch (workaround proven this run) or ship #2744
- fp-tier1d-signals-accumulate-no-promotion-to-backlog [recurrence=7] — buffer drains to 0/72 with 5 raw code-review-finding signals sitting in `backlog.json` at status=null (never materialized to open items); fix: in-repo signals→backlog promoter or lift Tier 1d generator to emit proper open items directly
- fp-stale-in-progress-action-list-decoupled-from-assignments [recurrence=5] — action-list.json holds status=in-progress rows with no matching assignments.json row; fix: extend gc1-reconcile.sh to reconcile stale in-progress rows against assignments
- fp-implementer-sandbox-zero-pushed-branch-multigen [recurrence=3] — implementer claims produce ZERO branches on origin across multiple runs; fix: on reclaim, if reclaim_attempts>=1 AND still zero pushes, mark quarantined and re-mint
- … and 2 more (see findings.json)

## Open questions
- Should the routine's Phase 2 include a step that heals the 5 dispatcher-generated null-status rows in `backlog.json` (materialize them into proper `open` items with `score = score_delta`, `status = "open"`, `title` from evidence, `updated_at = today`)? Doing so would immediately expose 5 landable backend/frontend candidates for the dispatcher's Tier 2 planner to pick up and starve the buffer less. Currently the routine defers to the dispatcher's Phase 2.6 backlog-reconcile, which has not converged this pattern in ≥7 recurrences (fp-tier1d-signals-accumulate).

## Quiet day?
- No — 9 merged PRs shipped, 2 new backlog rows, 0 plans, 0 auto-fix; PR #2684 closed via the quarantine-exit SLA (rare but healthy resolution path). Not a quiet day but not a promotion-heavy one either.
