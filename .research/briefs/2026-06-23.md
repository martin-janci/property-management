# 2026-06-23

> **⚠️ Degraded run — GitHub API blocked.**  This session received HTTP 403 from the GitHub API with the message *"GitHub access is not enabled for this session. An org admin must connect the Claude GitHub App for this organization."*  Phase 1 PR/issue observation, Phase 1.5 (code-review skill, depends on Phase-1 churn), and Phase 1.6 (PM rotation, depends on PR/issue payloads) all skipped.  What you're reading is the lag + decay maintenance the routine could still safely do from local git alone.

## Since last run
- Merged PRs: **unknown — GitHub API unavailable**.  Local git observes ~39 application-code commits + 11 dispatcher/research commits on `origin/dev` between `4829015b` (last seen) and `8b7e8875` (current HEAD).  PR numbers extracted from commit titles span **#951 / #1567 / #1662–#1779** (53 unique refs, likely 30-40 distinct merged PRs once squash-merges land on dev).
- Open PRs touched: **unknown** (no `gh pr list` data).
- New / updated issues: **unknown**.
- Commits on `dev`: 50 since `4829015b` → `8b7e8875` (39 application-code + 11 dispatcher/version-bump).
- Routine lag: **7d 22h** since last run (`last_run_iso = 2026-06-16T03:25:00Z`).  Both `lag_warning` and `stale_routine_alert` fired.
- Phases that failed: **`phase-1-gh-pr-list`, `phase-1-gh-issue-list`, `phase-1-gh-issue-update-sweep`, `phase-1-gh-pr-diff`, `phase-1.5-code-review-skill`, `phase-1.6-pm-rotation`** (all gated on GitHub API access).  Corresponding cursors held — `last_pr_seen` stayed at 1439, `last_issue_seen` stayed at 1437.  `last_commit_sha` advanced via local `git rev-parse origin/dev`.

## Shipped
- **Not enumerable this run** — without `gh pr view` we cannot reliably attribute commits to PR titles or fetch PR bodies for TODO/FIXME scanning.  From local commit subjects, the visible themes were:
  - **Messaging epic (BIT-183/184/208/238/239)** — N-party threads, attachments S3 presigned upload, camelCase wire contracts for attachments + threads/messages/participants, per-participant archive (#1689, #1696, #1702, #1712, #1756, #1768).
  - **Stripe checkout payments + payment reminders** (Story 11.5/11.6, BIT-176/181 — #1709, #1726).
  - **Guest ID-document upload + OCR** (Epic 18, Story 18.2 — #1687/#1750), generic Idempotency-Key middleware (#1688).
  - **Reality-portal listings hardening** — enum/status validation + ownership IDOR tests (#1671/#1746), Airbnb manager-only PII gate (#1667/#1741), Booking.com base_rate validation (#1664/#1743).
  - **Auth refactor** — unify access-token verification + PortalPrincipal negative tests (#1675/#1744).
  - **Schema/migration hygiene** — duplicate migration version 192 collision fix + dup-version CI guard (#1755/#1757), sqlx test-schema idiom doc (#1665/#1752), preflight required-env-var fail-fast (#951/#1753).
  - **Frontend** — AI dashboards through api-client + i18n/screen-map/sitemap (#1674/#1736), iot.* locale coverage (#1666/#1732), IoT alert error states (#1669/#1740), invoice PDF download UX (#1673/#1733), restore exec bit on build-mobile.sh (#1662/#1730).
  - **Dispatcher** — archive-terminal reconciler + unique branch names + collision guard (#1739/#1747/#1751), service-role allowlist in RLS gate (BIT-229 #1729), notification_event.rs reclassified service-role-by-design (#1728).

## Watch
- **🚨 OPERATIONAL — Cloud routine has been silent for 7 days.**  `last_run_iso` was 2026-06-16T03:25Z; today is 2026-06-23T18:13Z.  The cloud cron is either disabled or has been failing silently on the same GitHub-App-not-connected error.  Until an org admin connects the Claude GitHub App for `martin-janci/property-management`, **every routine run will be degraded to local-git-only** — no PR observation, no Phase 1.5, no Phase 1.6.  Operator action: enable the Claude GitHub App (link in the 403 body), then re-run with `text: "deep"` to catch up the 7-day window properly.
- **Reverts:** not observable without `gh` PR labels; commit subjects show no `Revert` titles in the visible 50.
- **Churn hotspots this run:** **deferred** — local-git churn over the 7-day window is dominated by file-restructure commits (heavy "added" diffs for messaging/document/rental modules) that would emit false `repeated-churn` / `risky-churn` signals.  Emitting them honestly requires `gh pr view --files` + per-PR diff inspection.  See the *Phase-1 hotspot deferral note* in `signals/2026-06-23.json`.
- **Stale drafts / stalled review:** **unknown** — needs `gh pr list --state open --draft`.

## PRs stuck in draft despite approval
- **unknown — needs `gh pr list`**

## PRs with verdict=changes, no fix-round progress in 24h
- **unknown — needs `gh api .../contents/.research/management/assignments.json?ref=planning`**

## Code review slice
- Segment reviewed: **none** — Phase 1.5 skipped (the `ppt-dev-review` skill depends on Phase-1 churn input; with that empty the segment selection would fall back to oldest-unreviewed, but running the skill in a degraded session would still spend significant token budget on a static review that wouldn't be reviewed by the team.  Skipped pending GitHub access).
- Experts: n/a
- Findings: 0
- Next segment (when routine resumes): **ppt-web-core** (last reviewed 2026-06-05 — oldest in `review_cursor` after this run).
- Skipped: **yes — GitHub-API blockage degraded Phase 1; skill is not designed to run with empty signals input**.

## Screen-map status
- Total `docs/screens/` files: **140** (ppt: 93, reality: 47, mobile: 0 — not yet seeded).
- Drift signals this run: **0** (cannot run — needs `gh pr view --json files` for each merged PR).
- Orphan screens this run: **0** (not scanned — would need the routes-touched-this-run input from Phase 1).
- Last `screen-map.yml` CI run on `main`: **unknown** (needs `gh run list`).

## Backlog deltas
- **New:** 0 (no Phase-1 signals).
- **Bumped:** 0.
- **Decayed (6 items, 2→1):**
  - `code-review-mobile-native-kmp-deeplink-token-not-url-decoded` — -1 (no signal in 14d).
  - `code-review-mobile-native-kmp-search-stale-response-race` — -1.
  - `test-gap-screen-map-drift-pr-1085-reality` — -1.
  - `test-gap-screen-map-drift-pr-1100-ppt` — -1.
  - `bug-risky-churn-pr-963-api-main-rs` — -1.
  - `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` — -1.
- **Dropped (9 items, decayed to 0):**
  - `triage-issue-1151` (research dispatcher claimable buffer stale).
  - `churn-hotspot-mobile-native-listing-detail-kt`.
  - `churn-hotspot-mobile-native-search-screen-kt`.
  - `code-review-mobile-native-kmp-deeplink-android-bypasses-shared-router`.
  - `refactor-churn-hotspot-mobile-announcements`.
  - `refactor-churn-hotspot-mobile-announcements-test`.
  - `refactor-churn-hotspot-mobile-documents`.
  - `triage-dispatcher-mcp-push-large-file-issue-1014`.
  - `triage-issue-951` (deploy blocker — ESIGN_TOKEN_SECRET requirement; likely resolved by #1753 preflight, but we cannot verify without PR-body access).
- **Resolved (PR-match-based):** **deferred** — the resolution check needs the merged-PR list with bodies, which is unavailable.  Several open items (`triage-issue-951` ESIGN preflight, `test-gap-router-set-parity-tests`, `bug-schema-drift-runtime-sql`) are likely now resolved by #1753, #1755/#1757, #1768, etc., but cannot be confirmed this run.  Next non-degraded run should re-evaluate these against the deep PR window.

## Plans promoted
- **0 plans promoted this run** — Phase 3 readiness requires concrete PR/issue/commit sources for new items; no new signals fired.  Existing `ready` items (6 — `code-review-mobile-rn-report-fault-fake-submit`, `code-review-reality-web-realtor-mgmt-untranslated`, `code-review-reality-web-share-comparison-404`, `code-review-reality-web-listing-page-ssr-crash`, `bug-ios-searchview-uncompilable`, `security-llm-doc-idor`) carry over.

## Auto-fix
- `no candidate` — Phase 5 requires high-confidence + score≥3 + allowlist match; no eligible signals this run (all three emitted signals are operational/degradation alerts with score_delta=0).

## Self-review findings
- `.research/self-improvement/findings.json` absent — none

## Open questions
- **Operator action required: connect the Claude GitHub App for the org.**  Until that lands, every routine run will be a near-no-op like this one.  Once enabled, run `text: "deep"` to sweep the missed 7-day window properly.
- **Resolution-sweep deferral:** several open backlog items look like they may already be resolved on `dev` (the preflight-env-var work plausibly closes `triage-issue-951`; the schema-drift work plausibly closes `bug-schema-drift-runtime-sql-issue-1008` — already `done`, just confirming; the messaging camelCase fixes plausibly close cross-cutting schema-drift triage items).  None can be safely flipped to `done` without verifying against PR bodies and code state.  Next non-degraded run.
- **Phase 1.6 PM rotation paused** — `pm_cursor.next_index` remains 5 (pm-security) from the 2026-06-16 run.  `pm-security` last ran 2026-05-27 → now 27d stale.  Coverage cursor remains at 12.

## Quiet day?
- **No** — 50 commits on `dev`, the routine itself is silently broken at the integration boundary (this is the most important signal of all), 6 backlog items decayed, 9 dropped.  But — and this is the honest framing — the *productive output* of this run is the lag alert and the operator-action callout above.  Everything else is mechanical decay.
