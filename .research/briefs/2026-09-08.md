# 2026-09-08

_Scheduled routine run after a 6d11h gap (cloud cron missed 2026-09-02..2026-09-07); dispatcher (separate branch) kept ticking hourly, so this brief catches up on the routine-branch backlog view only._

## Since last run
- Merged PRs: **14** (range #2922–#2952) — 6 code-review-* auth/i18n/purge PRs, 2 [WIP] fixes, 4 security follow-ups, 1 accounting endpoint, 1 mobile purge.
- Open PRs touched: **4** (dependabot #2939 fresh; #2591/#2590/#2589/#2588/#2584 recycled by dep group rebuilds; #2902 screen-map-drift branch still draft; #2744 dispatcher un-wedge PR still draft).
- New / updated issues: **13** — including 3 OPEN cross-tenant IDOR bugs #2944 / #2945 / #2946 (portfolio_analytics, portfolio_properties, violation-comments) and 2 OPEN infra blockers #2949 (api-server cloud build — utoipa-swagger-ui egress) and #2951 (mobile jest — jest-expo@56 ↔ RN@0.87 rot).
- Commits: ~14 code PRs + ~72 dispatcher `.research/`-only chore commits on `dev`. origin/dev HEAD `818bbe9`.
- Routine lag: **6d 11h since last run** (`lag_warning` + `stale_routine_alert` both fired — see signals). Cloud cron appears to have missed every daily fire between 2026-09-02 and 2026-09-07; dispatcher branch has been running normally throughout.
- Phases that failed: none. Cursors advanced for all phases.
- Trigger payload: `buffer-low: claimable=8/72 — all backlog structurally infra-blocked (mobile-native AGP #2652, api-server build #2949); planner refill of landable coverage requested`. Data-only; this run adds 3 landable non-mobile code-review findings on `ppt-web-core` (auth hot-path) but none reaches the ≥ 3 promotion bar this run.

## Shipped
- **#2952** test(ppt-web): auto-discover feature-local `*Keys` factories in logout-purge coverage — safety net for the PR #2943 gap flagged as follow-up #2948.
- **#2950** fix(mobile): purge NFC access log, QR scan history & offline feedback on session change — closes security follow-up #2947 (prior tenant's data survived logout on shared devices). Merged with red CI on the newly-filed #2951 mobile-jest infra block; treated as pre-existing infra failure, not this PR's regression.
- **#2943** code-review-ppt-web-core-logout-purge-allowlist-fragile — enforce logout-purge allowlist coverage (test-side).
- **#2942** code-review-ppt-web-core-api-401-no-request-replay — single-flight refresh + replay on 401.
- **#2941** code-review-ppt-web-core-authctx-no-boot-expiry-check — validate JWT exp at cold boot.
- **#2940** code-review-ppt-web-ui-confirm-dialog-i18n — externalize ConfirmationDialog loading label.
- **#2938** gh-issue-2937 — invoice PDF renderer + handler test coverage (accounting UC-ACC-05.9).
- **#2932** test(reality-server) — e2e 403 test for agent-review self-review guard (closes follow-up #2930).
- **#2931** gh-issue-2929 — make report_summary snapshot-consistency test fail on pre-fix code.
- **#2928** gh-issue-2924 — snapshot-consistent report_summary counts + entries.
- **#2927** gh-issue-2923 — cover update-error i18n path in EmergencyContactDirectoryPage tests.
- **#2926** code-review-api-core-fcm-legacy-dead-endpoint — remove decommissioned FCM legacy send path.
- **#2925** code-review-api-handlers-compliance-raw-db-leak-regression — route compliance audit-log count error through `db_error`.
- **#2922** code-review-reality-server-saved-search-error-string-status — derive HTTP status from typed error enum.

## Watch
- **Stalled review:** #2559 / #2558 / #2555 UC-ACC-05.x — `needs-human-judgement`, 40+ days idle (unchanged posture — user-decision blocker).
- **Reverted:** none this window.
- **Churn hotspots:** ppt-web `contexts/AuthContext.tsx` (**runs_seen=1**, 3 modifications this window across PRs #2941/#2942/#2943 — all auth/session); ppt-web `lib/api.ts` (runs_seen=1, 2 mods — PR #2942 401 refresh interceptor); mobile-rn `services/resetLocalData.ts` (runs_seen=1, 2 mods — PR #2950 tenant purge). All three are fresh hotspots — no repeated-churn stacking yet.
- **Infra-blocked pile:** the trigger payload flags the buffer-low situation. 5 of 6 pre-run `open`/`ready` rows are mobile-native-kmp (parked on #2652 AGP egress); the 3 OPEN IDOR issues (#2944/#2945/#2946) sit on api-server which is now separately blocked by #2949 (utoipa-swagger-ui build egress) — the routine cannot land IDOR fixes until #2949 clears either.

## PRs stuck in draft despite approval
- #2902 screen-map-drift-pr-2894-reality — draft, unchanged (still branch-contaminated; standing item since 2026-08-31).
- #2744 fix(dispatcher) un-wedge oversize-archive push — draft, unchanged (self-approval blocker; standing item).
- none currently draft AND approved.

## PRs with verdict=changes, no fix-round progress in 24h
- none (planner-branch `.research/management/assignments.json` shows 2 rows both `status=review` with no `verdict=changes`).

## Code review slice
- **Segment reviewed:** `ppt-web-core` (reason: **churn-aligned** — 2 of 3 churn hotspots this run live under `frontend/apps/ppt-web/src/{lib,contexts}/`).
- **Experts:** frontend (primary).
- **Findings:** 3 landable non-mobile items (see Backlog deltas). Highest-severity: `api-default-export-preconfig` (bug, high, score 2) — landmine at `lib/api.ts:370` where `export default getApiClient()` captures the pre-configure singleton, bypassing the JWT interceptor for any future `import api from '../lib/api'`. Currently zero production consumers use the default export, so no active bug — but the export invites a silent-auth-bypass regression.
- **Next segment:** `mobile-native-kmp` (review_cursor 2026-08-25 remains oldest) — deferred; #2652 AGP egress still parks the cloud implementer.
- **Skipped:** no — segment fully reviewed.

## Screen-map status
- Total `docs/screens/` files: unchanged (`ppt/` 108 + `reality/` 47 + `reality-mobile/` 14; `mobile/` still not seeded → 0). Grand total 169.
- Drift signals this run: **0** — of the 6 locally-findable merged PRs, only #2938 touched route files, and it also updated `docs/screens/{ppt,reality}/` in the same PR (huge cross-cutting merge). No drift.
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched this routine (GitHub Actions readable via MCP but skipped for token discipline; dispatcher runs the workflow status check on its own cadence).

## Backlog deltas
- **New (this run — 6 items):**
  - [1] `churn-hotspot-frontend-apps-ppt-web-src-contexts-AuthContext.tsx` — top-1 churn file; auth-critical, 3 mods in 6 days (refactor).
  - [1] `churn-hotspot-frontend-apps-ppt-web-src-lib-api.ts` — top-2 churn file (refactor).
  - [1] `churn-hotspot-frontend-apps-mobile-src-services-resetLocalData.ts` — top-3 churn file, mobile tenant-purge path (refactor).
  - [2] `code-review-ppt-web-core-api-default-export-preconfig` — pre-configure singleton landmine, `lib/api.ts:370` (bug, high).
  - [2] `code-review-ppt-web-core-authctx-user-no-shape-check` — `tokenStorage.getUser()` returns unchecked cast (bug, medium).
  - [1] `code-review-ppt-web-core-authctx-login-stale-tenants` — `login()`/`loginWithSsoCode()` leave stale `ppt_tenants` when `response.tenants` is falsy (bug, medium).
- **Bumped:** none.
- **Decayed:** 2 items — both mobile-native-kmp, both went score 1 → 0 → dropped for lack of new signals in 14d:
  - `code-review-mobile-native-kmp-portfolio-analytics-caps-100` (updated 2026-08-19; 20d idle).
  - `code-review-mobile-native-kmp-cancellation-swallowed` (updated 2026-08-25; 14d idle).
- **Dropped:** 2 (see Decayed above).
- **Resolved:** 0 this run — none of the new/existing open items match a merged PR body verbatim or show evidence-gone under grep. The Phase-2 resolution check ran against 10 pre-existing open/ready items; all still show their cited patterns on disk.

## Plans promoted
- **0 new plans this run.** Same reasoning as the 2026-09-01 supplementary fire (persisted): only score ≥ 3 && confidence == high && status == open && !plan candidate is `code-review-mobile-native-kmp-create-listing-not-wired` (score 4, high). Mobile-native/KMP remains cloud-unlandable (#2652 AGP egress blocker + dispatcher-tier parked); companion `code-review-mobile-native-kmp-inquiries-response-contract` plan is already `ready` and unclaimed on the same parked pile. Promoting a second unlandable plan does not refill the dispatcher's *landable* buffer.
- **Buffer-low response:** the 3 new `ppt-web-core` code-review findings above are the intentional landable-refill deposit (scores 2 / 2 / 1 today). One more high-confidence stacking event will push `api-default-export-preconfig` to score 3 and cross the promotion bar — likely next routine run.

## Auto-fix
- `no candidate` — no signal met `confidence == high` + `score >= 3` + allowlist match. The three new code-review findings are all `bug` vector, so outside the allowlist regardless (allowlist accepts only `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `lockfile-bump`, `stalled-review`).

## Self-review findings
- none (`.research/self-improvement/findings.json` absent).

## Open questions
- **#2949 api-server cloud build (utoipa-swagger-ui egress) — vendor swagger-ui offline?** Now blocks all three OPEN IDOR fixes (#2944 / #2945 / #2946) from entering the cloud implementer's cycle. The dispatcher has been running for a week without landing anything on this stack. Suggested resolution path in the PM digest → `pm-devops` next-action. Do we vendor the swagger-ui static bundle in-tree, or teach the build to fall back to a checked-in copy when egress is refused?
- **#2652 AGP dl.google.com egress — vendor or waive?** Standing question since 2026-08-04; 6 dispatcher cycles at `GC3-buffer-bounds=FAIL`. This run's routine can no longer avoid it: the *only* score-≥3 open row is mobile-native and cannot land.
- **Cloud cron misfire (routine-branch)** — 6-day silent gap on `.research/` writes while the dispatcher branch ticked normally. Worth checking whether the cron entry for the routine-fire endpoint got dropped/renamed/paused separately from the dispatcher's schedule.

## Quiet day?
- **No** — 14 merged PRs since last run, 6 new backlog vectors, 3 landable code-review findings, PM analysis (`pm-security` + Scrum Master), 2 items decayed.
