# 2026-09-08

## Since last run
- Merged PRs: 15 (range #2922–#2952)
- Open PRs touched: 9
- New / updated issues: 13 (7 new: #2944, #2945, #2946, #2947, #2948, #2949, #2951; 6 updated)
- Commits: 50 on `dev` (of which 7 non-dispatcher code merges — the rest are dispatcher chores)
- Routine lag: **7d 08h since last run** (2026-09-01T18:38:58Z → 2026-09-08T03:25Z; `lag_warning` + `stale_routine_alert` — cloud routine was paused/skipped for a week; dispatcher on planning branch continued through the gap)
- Phases that failed: none
- Phases skipped: Phase 1.6 (PM analysis) — skipped this run to conserve tokens on the 7-day catch-up; `pm_cursor` unchanged, next role remains `pm-security` (last ran 2026-07-21). Note this in follow-up.

## Shipped
- #2952 test(ppt-web): auto-discover feature-local *Keys factories in logout-purge coverage — closes coverage escape hatch introduced by #2943.
- #2950 fix(mobile): purge NFC access log, QR scan history & offline feedback on session change (closes #2947 cross-tenant purge gap on shared devices).
- #2942 single-flight refresh + replay on 401 in `frontend/apps/ppt-web/src/lib/api.ts` — race-safe 401 retries. **See Phase 1.5 finding below: this widened a logout-race seam.**
- #2941 validate JWT `exp` at cold boot in `AuthContext.tsx` — no more silent auth on expired tokens.
- #2943 enforce logout purge allowlist coverage (`AUTHED_QUERY_KEY_ROOTS`) — but see Phase 1.5 finding on localStorage drafts that escape it.
- #2940 externalize `ConfirmationDialog` loading label via i18n.
- #2938 gh-issue-2937: invoice PDF renderer + handler test coverage (backend acc-05).
- #2932 e2e 403 test for agent-review self-review guard.
- #2931 report_summary snapshot-consistency test now fails on pre-fix code.
- #2928, #2927, #2926, #2925, #2922 code-review followups (report_summary snapshot, EmergencyContactDirectory i18n, FCM legacy send-path removal, compliance db-error sanitize regression fix, reality-server pagination cap).
- #2558 feat(acc): invoice PDF render endpoint (UC-ACC-05.9).
- #2935 dependabot npm-minor-patch (40 updates); #2673 ktor group; #2583 rust_xlsxwriter; #2585 base64; #2586 validator — noted as `dep-update-noise`.

## Watch
- Stalled review: PR #2591 (expo-secure-store, 40d idle, dependabot), #2590, #2589, #2588, #2584 (all ~40d dependabot); PR #2808 (compose bump, 19d); PR #2933, #2939 (recent cargo/gradle dependabot).
- Stalled draft with `needs-human-review` label: **PR #2902** (screen-map-drift-pr-2894-reality, 8d) and **PR #2744** (dispatcher un-wedge, 26d — blocks two recurring dispatcher failure classes per self-improvement findings).
- **Critical labeled security issues open**: #2944 (Cross-tenant IDOR: violation comments/evidence/payments + internal-notes privilege leak), #2945 (Cross-tenant IDOR: portfolio_properties handlers missing org verification), #2946 (Cross-tenant IDOR: portfolio_analytics computes org_id then discards it). All labeled `bug`+`security`, filed 2026-09-06, no PR yet in this window.
- Cloud-blocker infra issues open: #2949 (api-server crate can't build in cloud runner — utoipa-swagger-ui build-script egress-blocked; blocks backend security fixes #2944/#2945/#2946), #2951 (mobile RN jest can't run in cloud — jest-expo@56 ↔ react-native@0.87 rot). Both labeled `blocked`.
- Churn hotspots: `frontend/apps/ppt-web/src/contexts/AuthContext.tsx` (3 touches, runs_seen=1); `frontend/apps/ppt-web/src/lib/api.ts` (2 touches); `frontend/apps/ppt-web/src/components/ConfirmationDialog.tsx` (2 touches).

## PRs stuck in draft despite approval
<!-- Query not run this session (GraphQL disabled for gh; would require per-PR reviewDecision REST calls). Best-effort skim of open PRs found no non-dependabot PR flagged `APPROVED + draft` in the visible open set. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Assignments file on the current branch shows only research/planning rows; the planning branch's assignments.json was not fetched this session. -->
- none

## Code review slice
- Segment reviewed: **ppt-web-core** (reason: churn-aligned — AuthContext.tsx was #1 churn this run at 3 touches)
- Experts: static review (Phase 1.5 skill via subagent) — findings verified against file:line evidence
- Findings: **2** (both high confidence)
  - `code-review-ppt-web-core-refresh-resurrects-tokens-after-logout` (medium severity, +2) — logout / in-flight refresh race silently re-persists rotated tokens after `tokenStorage.clear()`; widened by PR #2942. **Promoted to plan under security fast-track.**
  - `code-review-ppt-web-core-logout-leaks-form-drafts-across-users` (low severity, +1) — feature-local localStorage keys (`useDraftStorage` DISPUTE_DRAFT_KEY) survive logout; on shared workstation User B sees User A's dispute draft. Left at score 1 (below promotion bar).
- Next segment: `mobile-native-kmp` (oldest unreviewed after this run — last 2026-08-25)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: (count not refreshed this run; `ppt/` + `reality/` populated, `mobile/` still absent)
- Drift signals this run: 0 (no merged PR in the window touched frontend route files under the trigger set)
- Orphan screens this run: 0 (heuristic scan not re-run, no new merges into `App.tsx` / `routes/**` / `reality-web/src/app/**`)
- Last `screen-map.yml` CI run on `main`: not queried this session (GraphQL disabled for gh)

## Backlog deltas
- **New:** [2] ppt-web-core: in-flight refresh resurrects tokens after logout (`code-review-ppt-web-core-refresh-resurrects-tokens-after-logout`) — promoted immediately via security fast-track.
- **New:** [1] ppt-web-core: feature-local localStorage drafts survive logout (`code-review-ppt-web-core-logout-leaks-form-drafts-across-users`).
- **New:** [1] churn-hotspot AuthContext.tsx / api.ts / ConfirmationDialog.tsx (3 items, refactor vector).
- **New:** [1] dx-stalled-review-pr-2902 (screen-map-drift draft needs-human-review); [1] dx-stalled-review-pr-2744 (dispatcher un-wedge draft, 26d).
- **Decayed → Dropped:** `code-review-mobile-native-kmp-portfolio-analytics-caps-100` (score 1→0, updated 2026-08-19 > 14d).
- **Decayed → Dropped:** `code-review-mobile-native-kmp-cancellation-swallowed` (score 1→0, updated 2026-08-25 ≤ 14d cutoff).
- Active open/ready after this run: **8** (3 mobile-native-kmp bugs + 3 churn + 2 dx-stalled) plus 1 promoted-ready security plan and 1 low-score security note.

## Plans promoted
- `plans/code-review-ppt-web-core-refresh-resurrects-tokens-after-logout.md` — security fast-track (score 2, high confidence): logout/single-flight-refresh race writes rotated tokens back to storage after `tokenStorage.clear()`; AbortController-scoped fix + regression test. · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met the certainty bar this run (allowlist requires `screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / lockfile-drift `dep-update-noise` / `stalled-review`; only `stalled-review-pr-2902` + `stalled-review-pr-2744` qualified by shape but their score at promotion is 1 (below the `score >= 3` bar), and PR #2902/#2744 already carry `needs-human-review` labels — a routine ping would be noise, not signal).

## Self-review findings
- open=37 (high=9, medium=25, low=3); new this run=0; acknowledged=19
- fp-<unassigned> [recurrence=18] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked and derives fallback wrong; fix: replace hard-coded assumption with runtime probe at Phase 1
- fp-<unassigned> [recurrence=18] — Phase 6 archive-move produces oversize assignments-archive.json (>64KiB) and mcp__github__push falls back incorrectly; fix: push dispatcher state commit to a NEW branch (see PR #2744 which batches this fix — currently stalled)
- fp-<unassigned> [recurrence=12] — buffer drained (claimable=0/72) despite backlog full; fix: add in-repo signals→backlog promoter (new .research/signals-promoter.sh) OR fold into Phase 1.5
- fp-<unassigned> [recurrence=6] — action-list.json holds 31 in-progress rows but assignments.json has fewer (ghost claims); fix: extend gc1-reconcile.sh to reconcile in-progress rows against assignments
- fp-<unassigned> [recurrence=4] — PR #2744 carries fixes for TWO recurring dispatcher defects (>64KiB archive push-guard + retry-remint); fix: operator merge/un-draft PR #2744 to release the batched fixes
- … and 4 more (see findings.json)

## Open questions
- Should the routine's Phase 1.6 skill run on a lag-catch-up like this one, or is skipping-and-noting acceptable? `pm-security` last ran 2026-07-21 (49d ago) and is next in rotation — this is overdue regardless of lag.
- PR #2744 (dispatcher un-wedge) is stuck 26d in draft with `needs-human-review`; five self-improvement findings point at it. Merge or close?
- Three unresolved cross-tenant IDOR issues (#2944, #2945, #2946) opened 2026-09-06 and blocked by cloud-build infra (#2949) — should the routine promote a `security-cross-tenant-idor-portfolio-and-violations` vector so the dispatcher picks it up locally?

## Quiet day?
- No (routine advanced state after 7d gap; 1 plan promoted; 7 new vectors; 2 fresh security findings surfaced).
