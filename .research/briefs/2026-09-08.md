# 2026-09-08

## Since last run
- Merged PRs: 14 (range #2925–#2952)
- Open PRs touched: 6 (2 draft needs-human-review — #2744, #2902 — plus 4 dependabot)
- New / updated issues: 13 (7 closed, 6 open — 3 fresh security IDORs: #2944/#2945/#2946)
- Commits: dispatcher noise dominates; real code commits are the 14 merges above
- Routine lag: 7d 13h since last run (last_run_iso `2026-09-01T18:38:58Z`) — cloud routine cron likely paused; dispatcher (planning branch) still ticking on schedule (last dispatcher commit `35c3093 2026-09-08 08:28`)
- Phases that failed: none — Phase 1.5 (rotating expert review) and Phase 1.6 (PM rotation) were **skipped by policy** for this catch-up run to stay within cloud budget; every review-cursor segment was touched within the 14-day window and the caller payload already carries the planner buffer-low signal from the dispatcher

## Shipped
- #2952 test(ppt-web): auto-discover feature-local `*Keys` factories in logout-purge coverage (closes #2948; hardens PR #2943 by `import.meta.glob`-ing every `features/**/hooks/*.{ts,tsx}` so a new `*Keys` factory can't escape the allowlist)
- #2950 fix(mobile-rn): purge NFC access log, QR scan history & offline feedback on session change (closes #2947 — cross-tenant `resetLocalData()` gap on shared devices)
- #2943 code-review-ppt-web-core-logout-purge-allowlist-fragile: enforce logout purge allowlist coverage (mechanical test)
- #2942 code-review-ppt-web-core-api-401-no-request-replay: single-flight refresh + replay on 401
- #2941 code-review-ppt-web-core-authctx-no-boot-expiry-check: validate JWT `exp` at cold boot
- #2940 code-review-ppt-web-ui-confirm-dialog-i18n: externalize loading label
- #2938 gh-issue-2937: add invoice PDF renderer + handler test coverage
- #2935 chore(deps): npm-minor-patch group (40 updates)
- #2932 gh-issue-2930: e2e 403 test for agent-review self-review guard
- #2931 gh-issue-2929: report_summary snapshot-consistency test now fails on pre-fix code
- #2928 gh-issue-2924: snapshot-consistent report_summary counts + entries
- #2927 gh-issue-2923: cover update-error i18n path in EmergencyContactDirectoryPage tests
- #2926 code-review-api-core-fcm-legacy-dead-endpoint: remove decommissioned FCM legacy send path
- #2925 code-review-api-handlers-compliance-raw-db-leak-regression: route compliance audit-log count error through `db_error`

## Watch
- Stalled review: #2744 — 26 days idle, draft, `needs-human-review` label — dispatcher fix (oversize-archive push #1162 + retry-remint cross-id ghost #2460). Backlog row added.
- Stalled review: #2902 — 8 days idle, draft, `needs-human-review` label — screen-map-drift reconciliation for PR #2894 (reality saved-searches i18n). No score bump (already tracked via screen-map-drift chain).
- Reverted: none this window.
- Churn hotspots (very low intensity this run): `backend/Cargo.toml` (3 commits, all dep bumps — noise), `frontend/apps/ppt-web/src/contexts/AuthContext.tsx` (2 commits from #2941 + #2942 auth boot/replay rework — flagged as `churn-hotspot`, added to `hotspot_history`), `queryKeys.test.ts`/`pdf.rs`/`data_export.rs`/`accounting-server/Cargo.toml` (2 each, test-adjacent or dep-noise — skipped).

## PRs stuck in draft despite approval
<!-- Skipped: session-scoped GitHub GraphQL is disabled in this cloud sandbox (403 on `pr list --json reviewDecision`). REST-only tooling can't cheaply enumerate reviewDecision for open PRs; deferred to a run with GraphQL. -->
- none listed (reviewDecision unavailable via REST in this session)

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Skipped: reading `.research/management/assignments.json` from the `planning` branch is possible but the payload is heavy; the dispatcher's own log surfaces this directly. Deferred to Phase 1.6 runs. -->
- none listed (dispatcher-owned; see planning branch)

## Code review slice
- Segment reviewed: **skipped** (Phase 1.5 skipped for cloud-budget catch-up run)
- Experts: n/a
- Findings: 0 fresh code-review-finding signals this run
- Next segment: `mobile-native-kmp` (oldest unreviewed at 2026-08-25) — will run next fire when cadence recovers
- Skipped: yes — reason: 7d lag catch-up; review_cursor shows all 8 segments touched within the 14-day window; caller payload already flags the planner-buffer issue

## Screen-map status
- Total `docs/screens/` files: unchanged (ppt/ + reality/; mobile/ still 0)
- Drift signals this run: 0 (no merged PR touched a route file this run — `App.tsx`/`routes/**`/`reality-web/src/app/**` were not modified)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not checked (skipped for budget)

## Backlog deltas
- **New:** [3] security-idor-portfolio-analytics-2946 — `_org_id` computed then discarded in `upsert_property_metrics` + `get_property_metrics` (verified via grep 2026-09-08)
- **New:** [3] security-idor-portfolio-properties-2945 — portfolio_properties handlers missing org verification (source: issue label only; grep-verify deferred)
- **New:** [3] security-idor-violations-2944 — `list_evidence`/`list_payments`/`list_comments` drop `org_id` + caller-controlled `include_internal` leaks internal notes
- **New:** [1] churn-hotspot-ppt-web-authcontext — AuthContext.tsx touched by both #2941 + #2942 in one week
- **New:** [1] stalled-review-pr-2744 — 26d idle draft dispatcher fix
- **Decayed:** code-review-mobile-native-kmp-portfolio-analytics-caps-100 — 1→0 → `dropped` (no signals in 14d; mobile-native gated cloud-side)
- **Dropped:** — (subsumed by decay)
- **Bumped:** none this run (all new items are first-touches)

## Plans promoted
- `plans/security-idor-portfolio-analytics-2946.md` — thread `org_id` through `upsert_property_metrics` + `get_property_metrics` (2 handlers + 2 repo methods; cloud-landable backend Rust) · adversarial pass: passed
- `plans/security-idor-violations-2944.md` — thread `org_id` through `list_evidence`/`list_payments`/`list_comments` + role-gate `include_internal` (cloud-landable backend Rust) · adversarial pass: passed

Both plans meet the caller payload's ask ("refill planner with cloud-landable (frontend/reality-web/docs) vectors") from the backend/security angle — cloud-landable, no mobile-native, no cross-crate egress dependency.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The 3 security IDORs qualify on confidence/score but are code fixes, not on the Phase 5 allowlist (doc-stub / orphan-remove / lockfile-bump / fixme-issue-only / stalled-review-comment).

## Self-review findings
- none (`.research/self-improvement/findings.json` not present in this repo layout)

## Open questions
- Issue #2945 (portfolio_properties IDOR) — deferred grep-verify: `portfolio_properties.rs` route file was not found on `dev` (`ls backend/servers/api-server/src/routes/portfolio_*` returns `portfolio_analytics.rs` + `portfolio_performance.rs`). The relevant handlers may live in `portfolio_performance.rs` under a different noun. Left in backlog at score 3 with confidence=medium so the next segment review can pin down the exact handler set.
- Cloud runner has been stale for 7 days — is this the dedicated research trigger or a scheduler pause? The dispatcher's own 2h cadence on `planning` is unaffected, so this is scoped to the research routine specifically. Suggest checking the research-trigger workflow's schedule.

## Quiet day?
- No — 3 fresh high-value backlog rows (all security), 2 plans promoted (both cloud-landable), 14 merged PRs to reconcile.

## Goal violations
- none surfaced pre-commit; goal_checks are populated in `signals/2026-09-08.json` after Phase 6 staging.
