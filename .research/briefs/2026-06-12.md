# 2026-06-12

> **Second run of 2026-06-12** (rerun at 10:25Z, 7h after the 03:09Z first run). Fired by a
> `buffer-low: claimable=2` webhook — the dispatcher's claim queue was down to 2 truly
> claimable rows (5 `open` in action-list minus 3 already `in-progress` via assignments).
> Routine ran an action-list refill (Phase 1.6 Step 4 of the project-management skill's
> default mode); coverage scan / role rotation deferred per same-day-rerun convention.
>
> The 03:09Z → 10:25Z window had 2 application merges: **#1321 PAP-151 re-land** of
> sensor.rs + reserve_funds.rs RLS executor-pattern (PAP-78/PAP-79 work that was
> marked done but never landed on `dev` — the "done ≠ landed" merge-stall pattern from
> PAP-57; ships with 2 new RLS behavioural test files, 539 lines total) and **#1322
> rustfmt-only fixup** for the same files (whitespace, zero logic, plus 4 clippy
> explicit_auto_deref fixes on iot.rs). Net 3 new churn-hotspot signals (iot.rs,
> reserve_funds.rs routes, sensor.rs db repo); no new security/test-gap items.

## Since last run
- Merged PRs: **2** (#1321 PAP-151 re-land at 08:34Z, #1322 rustfmt fixup at 09:02Z).
  Both application-code; both fix-vector. **#1321 has tests** (sensor_rls_repo_tests.rs +
  reserve_funds_rls_repo_tests.rs, 539 lines); #1322 is fmt+clippy-only (no tests
  warranted).
- Open PRs touched: **8** (#1317–#1320 + #1323–#1326 — dispatcher implementer drafts
  freshly opened or rebased on the post-1322 dev; plus #1275/#1313 dependabot rebase
  cycles).
- New / updated issues: **0** since cursor (last_issue_seen still 1310; dispatcher's
  post-merge-review sweep ran on the morning's 5 application PRs only, no new
  follow-ups from #1321/#1322 yet).
- Commits: 4 on `dev` since `208fa26d` — PR #1321 squash + version bump 0.3.183 + PR
  #1322 squash + version bump 0.3.184. HEAD = `25d68f64`.
- Routine lag: **7h** since last run (well under 36h; no `lag_warning`).
- Phases that failed: **none**.

## Shipped
- #1321 **fix(api-server): re-land sensor.rs + reserve_funds.rs RLS conversions (PAP-151)** —
  re-lands the PAP-78 (sensor.rs) and PAP-79 (reserve_funds.rs) executor-pattern
  conversions that were marked **done** under their original PRs but **never merged to
  `dev`** (the "done ≠ landed" merge-stall pattern surfaced by PAP-57). Following the
  work_order / budget template: `SensorRepository` and `ReserveFundsRepository` now
  stateless (no `pool: PgPool` field); single-statement methods take a generic
  `Executor<'_, Postgres>`, multi-statement methods take `&mut PgConnection` reborrowed
  per call. Handlers in `iot.rs` (sensor) and `routes/reserve_funds.rs` take an
  `RlsConnection`, pass `&mut **rls.conn()` (or `rls.conn()` where the concrete
  `&mut PgConnection` overload binds) to the repo, source the org/user id from
  `rls.tenant_id()` / `rls.user_id()`, and call `rls.release().await` on both Ok and Err
  paths. Drops sensor.rs + reserve_funds.rs from the PAP-68 pool-field baseline. Ships
  with 2 new behavioural tests on a `NOSUPERUSER NOBYPASSRLS` role
  (`sensor_rls_repo_tests.rs` 252 lines, `reserve_funds_rls_repo_tests.rs` 287 lines)
  asserting deny-all without context, own-org visibility with context, cross-tenant
  invisibility, and a successful write under context. Has tests.
- #1322 **fix(api-server): rustfmt sensor.rs/iot.rs after PAP-151 re-land** — pure CI fixup.
  PR #1321 merged unformatted (the original PAP-78/79 branches predated several
  `rustfmt.toml` evolutions on dev, so the re-land introduced fn-signature line-wrap
  drift). Reddened the Backend `check` job (`cargo fmt --all -- --check`) on every
  push; blocking PAP-54's green-CI-on-dev exit gate. Two commits in the PR: (a)
  rustfmt diff for sensor.rs (db repo) + iot.rs (routes), (b) clippy
  `explicit_auto_deref` fix on 4 iot.rs sites (`create_reading`,
  `create_batch_readings`, `apply_threshold_template`, `get_dashboard` — concrete
  `&mut PgConnection` arms, pass `rls.conn()` directly; generic-Executor arms keep
  `&mut **rls.conn()`). Zero logic change; no tests warranted (`cargo fmt --check` is
  its own gate).

## Watch
- Stalled review: **#988** "Epic: reusable Playwright E2E framework" — last updated
  2026-06-08 (4d ago, under 7d threshold this run; not stalled by routine rule yet —
  first eligible 2026-06-15 if no activity).
- Reverted: none. PR #1321 reads like one but is the re-land side of the "done ≠
  landed" merge-stall fix, not a real revert.
- Churn hotspots (this rerun window: post-03:09Z slice):
  - `backend/servers/api-server/src/routes/iot.rs` — 681 line-changes (#1321 + #1322);
    runs_seen=1.
  - `backend/servers/api-server/src/routes/reserve_funds.rs` — 483 line-changes (#1321);
    runs_seen=1.
  - `backend/crates/db/src/repositories/sensor.rs` — 334 line-changes (#1321 + #1322);
    runs_seen=1.
- **PAP-151 done = PAP-54 unblocked.** With sensor.rs + reserve_funds.rs landed and
  rustfmt/clippy green, the Backend `check` job on dev is once again a useful gate for
  PAP-54's green-CI-on-dev exit. Worth surfacing for the next non-same-day Phase 1.6
  run.
- **Buffer-low cascade.** The dispatcher's 02:34Z + 03:04Z + 08:35Z runs claimed 6 +
  reviewed 4 + spawned multiple drafts (#1317–#1320, #1323–#1326), draining the open
  action-list pool from ~18 to 5 (3 of which were already `in-progress`). This is the
  expected curve when no rotating role agent has refilled in 24h — the refill this run
  brought the open pool back to 33 (target 36 — 28 ranked candidates exhausted in
  one pass; no remaining unqueued).

## PRs stuck in draft despite approval
- none (no PR with `reviewDecision=APPROVED && isDraft=true && updated < now-24h` this
  rerun; all 9 open drafts are dispatcher implementer pre-review drafts).

## PRs with verdict=changes, no fix-round progress in 24h
- (not queried this rerun — assignments live on the dispatcher session, not the
  routine.)
- none.

## Code review slice
- Segment reviewed: **skipped** — same-day rerun (7h after the 03:09Z first run; both
  new PRs map to `api-handlers` segment which was reviewed 2026-06-10).
- Experts: none invoked.
- Findings: **0**.
- Next segment: **reality-web** (last reviewed 2026-05-28 — oldest unreviewed) → will
  run on the next non-same-day routine fire.
- Skipped: yes — same-day rerun.

## Screen-map status
- Total `docs/screens/` files: `ppt/` + `reality/` populated; `mobile/` still empty
  (drift detection intentionally excludes `mobile`).
- Drift signals this rerun: **0** — both PRs are backend-only
  (`api-server/src/routes/*` + `crates/db/src/repositories/*`). No frontend route file
  in the trigger set was touched.
- Orphan screens this rerun: **0**.
- Last `screen-map.yml` CI run on `main`: not queried this rerun.

## Backlog deltas
- **New:** [1] `churn-hotspot-backend-servers-api-server-src-routes-iot-rs` — 681
  line-changes from PR #1321 + #1322. vector=`refactor`. confidence=high.
- **New:** [1] `churn-hotspot-backend-servers-api-server-src-routes-reserve-funds-rs` —
  483 line-changes from PR #1321. vector=`refactor`. confidence=high.
- **New:** [1] `churn-hotspot-backend-crates-db-src-repositories-sensor-rs` — 334
  line-changes from PR #1321 + #1322. vector=`refactor`. confidence=high.
- **Bumped:** none.
- **Decayed:** none (no open item with `updated_at <= 2026-05-29`; oldest open is
  `triage-issue-779` at 2026-05-30 = 13d, still below the 14d threshold by 1 day).
- **Dropped:** none.

## Plans promoted
- none — all 3 new churn-hotspot signals at score 1, well below the score-3 readiness
  gate. No new security/test-gap vectors that would qualify for the fast-track. 2
  existing ready plans intact (`bug-ios-searchview-uncompilable.md`,
  `security-llm-doc-idor.md`) — both already in dispatcher's claimable set.

## Auto-fix
- `no candidate` — `churn-hotspot` signals are analytical/cumulative and never in the
  Phase-5 allowlist. PR #1322 looks "fix" + no tests but the diff is pure rustfmt +
  clippy explicit_auto_deref (whitespace/syntactic), so `risky-churn-pr-1322-iot-rs`
  is suppressed via the same visibility-not-actionable principle as the dead-code
  reachability gate. 0 `screen-map-drift`, 0 `fixme-in-merged-code`, 0 `stalled-review`
  eligible this rerun.

## Action-list refill (Phase 1.6 Step 4 — buffer-low trigger response)
- **Before:** 5 open in `action-list.json`, 3 of which were already `in-progress`
  per `assignments.json` (`feat-airbnb-oauth-token-exchange-route-backend`,
  `feat-airbnb-realtime-webhook-handler-backend`,
  `verify-document-folder-organization-backend-promote`). True claimable = **2**
  (matches the webhook payload).
- **Refill:** 28 new gap-scan items appended from `coverage.json` (20 partial stories
  × their gaps; 17 candidates skipped on dedup vs existing IDs in
  action-list/assignments + 1 intra-batch dup + 4 stem matches). Source on every new
  row: `gap-scan 2026-06-12 (buffer-low refill)`.
- **After:** 33 open, 49→77 total items. Target was 36 — 3 short, but 28 ranked
  candidates exhausted (the partial-story gap set is drained for now; deeper coverage
  scan needed to surface more, and that's `local-only` per the skill so won't run
  here).
- **Top 5 newly-queued (high priority):** `feat-folder-organization-mobile-implementation-frontend`,
  `feat-build-configuration-by-environ-app-icon-variants-dev-mobile`,
  `feat-build-configuration-by-environ-app-icons-generated-all-mobile`,
  `feat-build-configuration-by-environ-build-scripts-created-mobile`,
  `feat-build-configuration-by-environ-ios-xcconfig-files-mobile`.
- **Out of scope this rerun:** Scrum Master + role rotation (Phase 1.6 Steps 2+3) and
  `coverage.json` rescan — both deferred to the next non-same-day routine fire.
  `pm_cursor.next_index` and `coverage_cursor.next_index` unchanged at 3 and 10.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present in this repo's research
  scaffold).

## Open questions
- **Should the refill target hold at 36 when coverage candidates are exhausted?** The
  skill's buffer math assumes the candidate pool is deep enough to refill — but
  today's partial-story gap-set ran out at 28. Two options: (a) drop the target to
  match available depth (would mask backlog drain), (b) trigger a coverage scan from
  the routine on exhaustion (currently `local-only`, won't run in cloud). Surfacing
  for owner consideration; no action this run.

## Quiet day?
- No — buffer-low trigger fired action-list refill (+28 new actionable rows); 3 new
  churn-hotspot vectors added; 2 merged PRs (1 architectural re-land + 1 CI fixup).
  Day remains meaningfully active.
