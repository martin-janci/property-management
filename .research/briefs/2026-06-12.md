# 2026-06-12

> First run of 2026-06-12 (08h after the 2026-06-11T18:31:49Z 5th-pass rerun).
> Normal incremental trigger (empty payload). 8 PRs merged in the window —
> 5 application-code (#1293 PAP-171 FORCE-RLS catalog, #1294 epic-83 Booking.com
> retry, #1295 + #1296 + #1311 verify/coverage test-only) and 3 process
> (#1297 PAP-142 IDOR + 2 dispatcher state deltas #1312/#1315). **All 5
> application-code PRs ship with regression tests** (no `hotfix-no-test`
> signals fire — first quiet day on that signal class since 2026-06-09).
> 1 dependabot PR closed-unmerged (#1274 superseded by #1313 after the
> auto-rebase fix from PR #1291 unblocked the workflow). Phase 1.5 + 1.6
> skipped per same-day-rerun convention; dispatcher kept management/ fresh
> at 02:34Z and 03:04Z today.

## Since last run
- Merged PRs: **8** (#1293, #1294, #1295, #1296, #1297, #1311, #1312, #1315 — range #1293–#1315). 5 application-code; 3 process (1 IDOR scoping fix routes-only + 2 dispatcher state deltas).
- Open PRs touched: **6** (#1275 dependabot npm rebased after #1291; #1298 ios deeplink draft; #1299 PAP-138 violations IDOR; #1313 fresh cargo dependabot; #1314 ppt-web hooks wiring draft; #1316 doc-folder verify draft).
- New / updated issues: **12** since cursor (1267 → 1310). 11 are `follow-up`/`from-merged-review` from the dispatcher's post-merge sweep (#1300–#1310); 1 carry-over (#1151 dispatcher buffer stale). None untriaged.
- Commits: **20+ on `dev`** since `5d8231e` — 8 PR merges + version bumps (0.3.175 → 0.3.181) + dispatcher state delta commits + 1 squash merge from #1297.
- Routine lag: **8h** since last run (well under 36h; no `lag_warning`).
- Phases that failed: **none**.

## Shipped
- #1293 **fix(api-server): FORCE-RLS api_ecosystem catalog + developer-portal tables (PAP-171)** — migration `00180` `FORCE ROW LEVEL SECURITY` on 8 catalog + developer-portal tables; `api_ecosystem.rs` rerouted from `catalog_conn` (acquire_public) to new `rls_conn` helper (acquire_with_rls) for 23 authenticated write/portal handlers; the 10 public-read handlers stay on `catalog_conn`. Also adds the missing `is_platform_admin()` gate on `create_connector_action`. Ships with **416-line behavioral RLS test** (`api_ecosystem_developer_force_rls_tests.rs`) on a NOSUPERUSER NOBYPASSRLS role — proves cross-user isolation + deny-all without context + catalog write authorization. Follow-up issue #1303 surfaces 3 sibling tables left un-FORCEd (`developer_oauth_apps` / `_grants` / `api_key_usage_logs`).
- #1294 **feat(epic-83 story 83.2): Booking.com OTA rate & availability push retry/error handling (AC-5)** — `BookingRetryConfig` (max_attempts/initial_delay/max_delay/backoff_multiplier) + `post_ota_with_retry` bounded exponential backoff on transient 408/425/429/5xx; client errors fail fast. 3 new tokio tests with a minimal one-connection-per-request HTTP mock OTA server: retry-then-succeed on 503, no-retry on 400, exhaust-retries on persistent 503. Booking.com is OTA-idempotent (absolute Start/End/InvTypeCode), so the retry is application-safe. Has tests.
- #1295 **test (coverage 8a-3): notification-preference idempotent-apply tests** — additive S5/S6/S7 cases in `notification_preference_realtime_sync_tests.rs` asserting repeat-PATCH convergence on DB state + HTTP status. No production change. Follow-up #1308 surfaces the realtime publish-leg replay gap (the same PR's framing oversells what shipped — DB-state, not realtime-event idempotency). Labeled, not untriaged.
- #1296 **verify (coverage 81-2): re-presign after expired report download URL** — additive e2e test asserting two consecutive `GET /reports/executions/{id}/download` mint fresh presigned URLs. No production change.
- #1297 **fix(reality-server): scope portal import/feed by-id endpoints to owner (PAP-142)** — closes IDOR on the 7 reality-portal by-id endpoints. All by-id repo methods now take `user_id`/`agency_id` and key the WHERE clause; handlers thread `principal.user_id` from `RequestPrincipal`; cross-tenant ids → `RowNotFound` → 404. Ships with **226-line cross-tenant IDOR test** (`portal_imports_cross_org_idor_tests.rs`, 2 sqlx tests). Follow-up #1300 surfaces (a) the test layer doesn't exercise the handler-level extractor wiring (a regression that drops `RequestPrincipal` would compile + slip past), and (b) `feed_subscriptions.agency_id` semantically stores a `users.id` — pre-existing schema/code mismatch worth a future renamer PR.
- #1311 **verify (story 7A.3): document RLS + capability gates + AC-mapped tests** — verifies migration `00172_force_documents_rls.sql` + `DocumentRepository::list_accessible_rls`/`check_access_rls` against story 7A.3 ACs; adds AC-2 (role denial), AC-3 (specific-user grant + outsider denial + creator override) and a building/unit in-memory-gate regression guard in `document_access_test.rs`. Story status flipped `ready-for-dev` → `done`. Tracked gap: building/unit-scope is satisfied in SQL today but the `*_simple_rls` in-memory list path needs a follow-on switch to `list_accessible_rls`.
- #1312, #1315 **chore(research): dispatcher state deltas** — touch only `.research/management/` (assignments, archive, action-list, objective). No application surface; outside this routine's signal scope.

## Watch
- Stalled review: **#988** "Epic: reusable Playwright E2E framework" — last updated 2026-06-08 (4d ago, under 7d threshold this run; not stalled by routine rule yet — first eligible 2026-06-15 if no activity).
- Reverted: none.
- Churn hotspots (this run):
  - `backend/crates/integrations/src/booking.rs` — 433 line-changes (PR #1294 OTA retry); runs_seen=1.
  - `backend/servers/api-server/src/routes/api_ecosystem.rs` — 133 line-changes (PR #1293 PAP-171); runs_seen=1. Second touch in 24h (after PR #1289 PAP-167 the prior day) — RLS migration churn pattern; no repeated-churn yet because `runs_seen<2` at HEAD, the +1 this run lifts it to 1.
  - `backend/crates/db/src/repositories/reality_portal.rs` — 87 line-changes (PR #1297 PAP-142); runs_seen=1.
- **PAP-150 burndown progress.** PR #1293 (PAP-171) is a defense-in-depth follow-up that closes the owner-bypass remainder for the 00102 catalog/developer-portal tables. The 3 `oauth_apps`/`grants`/`api_key_usage_logs` siblings #1303 flagged are the natural next migration.
- **Post-merge review backlog.** Dispatcher's 22:35Z run created 11 follow-up issues (#1300–#1310) covering tests, coverage gaps, and one stale doc reference. None block merges; they collect into the action-list refill for the next planner run.

## PRs stuck in draft despite approval
- none (no PRs with `reviewDecision=APPROVED && isDraft=true && updated < now-24h` this run).

## PRs with verdict=changes, no fix-round progress in 24h
- (not queried — assignments lives on the dispatcher session, not this routine).
- none.

## Code review slice
- Segment reviewed: **skipped** — same-day rerun (8h after yesterday's 18:31Z 5th-pass run). All 4 merged code PRs map to the `api-handlers` segment (reviewed 2026-06-10) and `ppt-web-ui` (2026-06-11).
- Experts: none invoked.
- Findings: **0**.
- Next segment: **reality-web** (last reviewed 2026-05-28 — oldest unreviewed) → will run on the next non-same-day routine fire.
- Skipped: yes — same-day rerun.

## Screen-map status
- Total `docs/screens/` files: `ppt/` + `reality/` populated; `mobile/` still empty (drift detection intentionally excludes `mobile`).
- Drift signals this run: **0** — all 8 merged PRs touch backend (`api-server`, `crates/db`, `reality-server`, `integrations`), `_bmad-output/` (story doc), and `.research/management/`. No frontend route files in the trigger set were touched.
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not queried this rerun.

## Backlog deltas
- **New:** [1] `closed-not-merged-pr-1274` — Dependabot cargo-minor-patch PR #1274 closed unmerged, superseded by fresh #1313. vector=`dx`. confidence=high.
- **New:** [1] `churn-hotspot-backend-crates-integrations-src-booking-rs` — PR #1294 OTA retry adds 404 / -29 in booking.rs. vector=`refactor`. confidence=high.
- **New:** [1] `churn-hotspot-backend-servers-api-server-src-routes-api-ecosystem-rs` — PR #1293 PAP-171 (second touch in 24h). vector=`refactor`. confidence=high.
- **New:** [1] `churn-hotspot-backend-crates-db-src-repositories-reality-portal-rs` — PR #1297 PAP-142 IDOR scoping. vector=`refactor`. confidence=high.
- **Bumped:** none.
- **Decayed:** none (no open item with `updated_at <= 2026-05-29`; oldest open is `triage-issue-779` at 2026-05-30 = 13d, below 14d threshold).
- **Dropped:** none.

## Plans promoted
- none (all 4 new signals at score 1, below score-3 readiness gate; security fast-track inapplicable — no security-vector signals).

## Auto-fix
- `no candidate` — `closed-not-merged-pr` is a dep-update-noise variant (action: ignore, no allowlist match); `churn-hotspot` is analytical/cumulative (never in Phase-5 allowlist); 0 `screen-map-drift`, 0 `fixme-in-merged-code`, 0 `stalled-review` eligible this run.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present in this repo's research scaffold).

## Open questions
- The PR #1297 follow-up (#1300) notes `feed_subscriptions.agency_id` schemantically stores a `users.id` while the FK references `reality_agencies.id` — pre-existing convention mismatch that the IDOR fix had to follow. Worth a focused renamer PR (rename column to `owner_user_id`, repoint FK) but out of scope for the routine to promote without owner input.

## Quiet day?
- No — 4 new vectors, 8 merged PRs in window, 1 closed-unmerged PR, 3 churn hotspots first-recorded. Day is meaningfully active.
