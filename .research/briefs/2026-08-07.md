# 2026-08-07

## Since last run
- Merged PRs: 10 (range #2685–#2695)
- Open PRs touched: 5 (#2684, #2690, #2696, #2697, #2698)
- New / updated issues: 1 new (#2699 — CI blocker) + 1 closed (#2575, resolved by #2687)
- Commits: many on `dev` (10 PR merges plus dispatcher research-chore commits; last routine-committed sha before this run was `819e2cd2`)
- Routine lag: 8h since last run — on schedule
- Phases that failed: none

## Shipped
- #2685 code-review-api-core-workflow-not-group-drops-conds: rewrote `NOT` operator via De Morgan so `NOT(a AND b AND …)` considers all conditions and nested groups, not just `conditions.first()`. Also lifted `evaluate_condition_group`/`evaluate_single_condition`/`get_field_value`/`traverse_json`/`compare_numeric` out of the `WorkflowExecutor` impl block to free functions (457 changed lines in `services/workflow_executor.rs`).
- #2686 pm-scrum-master-wire-ppt-web-direct-s3-84-1-retry1: pinned `DocumentUpload` to the direct-to-S3 path — new integration test (`DocumentUpload.test.tsx`) asserts the direct upload mutation fires and the legacy multipart mutation never does.
- #2687 gh-issue-2575: added window-ordering validation on `GET /disputes/kpis` (422 `VALIDATION_ERROR` when `window_start >= window_end`) plus an executing `#[tokio::test]` regression that runs outside the BIT-440 dev-hostage quarantine; closed #2575.
- #2688 code-review-api-handlers-enhanced-chat-stub: `POST /chat/enhanced` now fails closed with `501 ENHANCED_CHAT_NOT_IMPLEMENTED` instead of returning a fabricated 200 (canned echo, confidence 0.85, tokens 150) — regression test asserts the invented fields are absent.
- #2689 code-review-ppt-web-core-ws-pong-timeout-drop: removed the redundant client-side app-level WS heartbeat that was force-closing healthy sockets every ~40s (server never sent `{type:'pong'}`); liveness now handled purely by protocol-level `Ping`/`Pong`.
- #2691 code-review-ppt-web-core-ws-giveup-no-resume: WebSocket auto-resumes after the retry budget is exhausted (follow-up to the pong-timeout removal).
- #2692 code-review-reality-server-listing-viewcount-hardcoded-zero: `GET /api/v1/listings/{id}` reports real recorded view totals via the new `portal_get_listing_view_count` SECURITY DEFINER function (migration 00220) instead of hardcoded `view_count: 0`; adds FORCE-RLS regression test binding a `NOSUPERUSER NOBYPASSRLS` role. **Side effect: introduced the migration-collision bug (#2699) below.**
- #2693 code-review-reality-server-orphan-schedule-viewing-stub: removed the orphan `schedule_viewing` stub from `handlers/inquiries/mod.rs` (-64 lines, no consumers).
- #2694 code-review-api-handlers-ai-upstream-error-leak: no longer forwards raw upstream LLM/OAuth error text into client-facing error bodies.
- #2695 code-review-mobile-rn-biometric-prompt-i18n: routed the RN biometric OS dialog copy (`promptMessage`, `cancelLabel`, `fallbackLabel`) through i18n across all 6 shipped locales (sk/cs/de/en/pl/hu); integration test asserts translations resolve and English literals are gone.

## Watch
- **CI blocker (dev-hostage): #2699** — two SQLx migrations at prefix `00220` on `dev` (`00220_listing_syndication_view_counters.sql` from cb91cac + `00220_portal_get_listing_view_count.sql` from #2692 8cf0fb5). Backend `check`/`test`/`test-shard` set fails on every PR based on current `dev`. Mechanical fix: `git mv` the later-merged file to `00227` (next free prefix). Verified two-file state via `git ls-tree -r origin/dev -- backend/crates/db/migrations/00220*`. Promoted to plan this run.
- Stalled review: none new. The three ACC-epic PRs open >7d (#2555, #2558, #2559) unchanged; still treating as batched-sprint per prior brief, no `stalled-review` signal emitted.
- Reverted: none this window.
- Churn hotspots (source, not tests): `services/workflow_executor.rs` (457 lines, PR #2685), `lib/websocket.ts` (105 lines, PR #2689), `routes/ai/llm.rs` (~64 lines across #2688 + #2694). All three are new to `hotspot_history` this run (`runs_seen=1`); no `repeated-churn` this run.

## PRs stuck in draft despite approval
- none (the two current drafts #2697 #2698 are fresh — updated <2h ago — and neither is APPROVED yet; #2697's CI is red because of the #2699 migration collision, not a merge-gate stall)

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher's assignments.json rows on the planning branch not inspected this run — no session-scoped `verdict=changes` signal fell out of Phase 1 either)

## Code review slice
- Segment reviewed: **reality-web** (reason: oldest-unreviewed at start-of-run — `review_cursor.reality-web = 2026-08-03`; tier1d file `2026-08-07-reality-web-tier1d.json` pre-computed by dispatcher earlier today, folded verbatim)
- Experts: frontend (Next.js SSR / next-intl)
- Findings: 3 new `code-review-finding` signals (see Backlog deltas). None reach the promotion threshold (top score 2, medium confidence) — held in backlog for corroboration.
- Next segment: `mobile-native-kmp` (last reviewed 2026-08-04) is now the oldest — priority for the next Phase 1.5 tick.
- Skipped: no — pre-computed tier1d folded in without re-invoking the skill (inputs would have been identical).

## Screen-map status
- Total `docs/screens/` files: unchanged from 08-06 baseline (routine did not touch `docs/screens/`; no merged PR touched a screen doc either).
- Drift signals this run: 0 (no merged PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`).
- Orphan screens this run: 0 (no orphan pass done this run — no route files removed in the window).
- Last `screen-map.yml` CI run on `main`: not fetched this run (per-workflow REST call skipped for budget). Green last checked 08-04.

## Backlog deltas
- **New:** [3] `gh-issue-2699-migration-collision` — dev CI broken by duplicate SQLx migration prefix 00220; rename `portal_get_listing_view_count.sql` to 00227.
- **New:** [2] `code-review-reality-web-comparison-share-all-or-nothing` — `ComparisonUrlHandler.tsx:57` uses `Promise.all`; one bad listing id blanks the whole shared comparison URL.
- **New:** [2] `code-review-reality-web-listingform-nan-validation` — `ListingForm.tsx:87-88` submits `Number(area)`/`Number(rooms)` unchecked; non-numeric input yields NaN → serialised as null.
- **New:** [1] `code-review-reality-web-listingform-no-i18n` — `ListingForm.tsx` hardcodes English throughout the realtor create/edit flow in a sk/cs/de/en next-intl app.
- **New:** [1] `churn-hotspot-backend/servers/api-server/src/services/workflow_executor.rs` — 457-line PR #2685 refactor.
- **New:** [1] `churn-hotspot-frontend/apps/ppt-web/src/lib/websocket.ts` — 105-line PR #2689 heartbeat removal.
- **New:** [1] `churn-hotspot-backend/servers/api-server/src/routes/ai/llm.rs` — two-PR churn (#2688 + #2694).
- **Bumped:** none.
- **Decayed:** none — all 5 open items were updated within the last 14 days (cutoff 2026-07-24).
- **Dropped:** none.

## Plans promoted
- `plans/gh-issue-2699-migration-collision.md` — rename the later-merged 00220 migration to 00227 (only free prefix) and regenerate SQLx offline data · adversarial pass: passed

Only 1 promotion this run: `gh-issue-2699-migration-collision` is the sole `open` item at score ≥ 3, and it's a fact-grade (high-confidence) CI blocker with a mechanical fix. The next-highest candidates (`code-review-reality-web-comparison-share-all-or-nothing`, `code-review-reality-web-listingform-nan-validation`) sit at score 2 with medium confidence — neither is a `security` vector so the fast-track doesn't apply, and one more corroborating signal is required.

## Auto-fix
- `no candidate` — no signal met confidence=high + score>=3 + allowlist this run. #2699 is high-confidence and score 3, but the migration-rename shape is NOT in the Phase 5 allowlist (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review`). Handled via the promoted plan instead.

## Self-review findings
- none

## Open questions
- Should the routine's Phase 5 allowlist grow to include the mechanical "migration-prefix collision" shape? Today's #2699 would qualify: SECURITY DEFINER function + test suite land unchanged; only the filename prefix moves; blast radius is single-file `git mv` + SQLx offline regen. Blocker to auto-fix without human review is the cross-workspace `cargo sqlx prepare` step, which touches `.sqlx/` snapshots. Flag for a future Phase 5 expansion — do not auto-fix on this run.

## Quiet day?
- No — 10 PRs merged, 7 new backlog vectors, 1 plan promoted, 1 dev-hostage CI blocker surfaced.
