# 2026-08-07

## Since last run
- Merged PRs: 10 (range #2685–#2695)
- Open PRs touched: 3 non-dependabot (#2696 approved auto-merge, #2690 approved auto-merge, #2684 approved auto-merge); 8 dependabot PRs untouched
- New / updated issues: 0 (issue #2575 closed by PR #2687 merge)
- Commits: 15 on `dev` since last routine sha (`819e2cd2a0`) — 10 PR merges + 5 dispatcher research-chore commits
- Routine lag: 5h since last run — on schedule (buffer-low fire, not a scheduled tick)
- Phases that failed: none

## Shipped
- #2695 code-review-mobile-rn-biometric-prompt-i18n: route six biometric OS-dialog strings through `t('auth.biometric.*')` in all six locales
- #2694 code-review-api-handlers-ai-upstream-error-leak: return fixed generic client messages instead of leaking raw upstream errors on lease-gen + voice-device OAuth exchange (regression tests added)
- #2693 code-review-reality-server-orphan-schedule-viewing-stub: delete dead `InquiriesHandler::schedule_viewing` + `ViewingResult` enum (0 call sites; wrong contract)
- #2692 code-review-reality-server-listing-viewcount-hardcoded-zero: definer-scoped `portal_get_listing_view_count(uuid)` (migration 00220) + `get_view_count` wrapper; live listing detail now reports real total instead of hardcoded 0
- #2691 code-review-ppt-web-core-ws-giveup-no-resume: split explicit `connect()` (resets budget) from private `openConnection()` (backoff loop); resume via `online` event; `maxRetriesExceeded` flag surfaced
- #2689 code-review-ppt-web-core-ws-pong-timeout-drop: drop redundant client app-level WS heartbeat (server never emits `{type:'pong'}`; browser auto-pong at protocol layer covers liveness)
- #2688 code-review-api-handlers-enhanced-chat-stub: fail closed with 501 `ENHANCED_CHAT_NOT_IMPLEMENTED` for stub handler (was returning fabricated confidence/tokens/echo)
- #2687 gh-issue-2575: reject inverted/empty `/disputes/kpis` window with 422; failing-on-main plain `#[tokio::test]` gives executing coverage outside the BIT-440 sqlx-quarantine
- #2686 pm-scrum-master-wire-ppt-web-direct-s3-84-1-retry1: component-level test pins DocumentUpload (Story 39.2) to direct-to-S3 (`useUploadDocumentDirect`); asserts legacy proxying mutation never fires
- #2685 code-review-api-core-workflow-not-group-drops-conds: NOT condition group now negates the FULL conjunction (De Morgan); helpers extracted to module-level free functions; multi-condition + nested-group NOT tests

The dispatcher's fast-path continued draining the code-review + workflow backlog — 7 of the 10 merged PRs directly resolved existing backlog items, matching the dispatcher's `buffer-low: claimable=2/72 — refill planner` fire that woke this routine. This run's refill is 1 net-new item (secondary finding from PR #2696 implementer) and 7 items marked `done`.

## Watch
- Stalled review: none — the same three ACC-epic PRs (#2555 #2558 #2559) remain open >7d; unchanged pacing signal from the ACC-batch sprint. No new `stalled-review` emission (would be a duplicate signal id under the dedup rule).
- Reverted: none this window.
- Churn hotspots: not computed this run — same rationale as the previous run (per-PR file-list fetch would take 10+ REST calls, over budget for a buffer-low incremental refill). The prior window's fixed hotspots (`ThreadDetailScreen.tsx` #2659, `auth.rs` #2671) remain resolved.

## PRs stuck in draft despite approval
- none — #2696, #2690, #2684 are all approved but current (last update <5h ago, auto-merge enabled per dispatcher fast-path).

## PRs with verdict=changes, no fix-round progress in 24h
- none — all `review`-status rows in `assignments.json` have `reviewer_summary.verdict=approve` (PRs #2684 #2685 #2686 #2687 #2688 #2689 #2690 #2691 #2692 #2693 #2694 #2695 #2696).

## Code review slice
- Segment reviewed: **skipped this run** — buffer-low incremental fire, no fresh churn window; prior 5 segments (api-handlers, mobile-rn, ppt-web-core, reality-server, reality-server-inquiries) were saturated by dispatcher-driven tier1d files that already generated the 11 findings the previous run folded in (and 7 of those 11 landed as PRs this window).
- Experts: n/a
- Findings: 0 net-new `code-review-finding` signals from Phase 1.5; 1 net-new implementer-derived finding (`code-review-reality-server-inquiry-notify-route-wiring`) surfaced by PR #2696's implementer session as a secondary bug — same shape as a `code-review-finding` (medium confidence, bug vector, +2 score).
- Next segment: `reality-web` (last reviewed 2026-08-03) is now the oldest — priority for the next non-buffer-low Phase 1.5 tick. `mobile-native-kmp` and `api-core` follow (2026-08-04).
- Skipped: yes — reason: buffer-low incremental fire (5h since last run, no full-review budget).

## Screen-map status
- Total `docs/screens/` files: unchanged (routine did not touch `docs/screens/`; no route files in this window's merged PRs).
- Drift signals this run: 0 (none of the 10 merged PRs touched frontend/apps/ppt-web/src/{App.tsx,routes/**} or frontend/apps/reality-web/src/app/**).
- Orphan screens this run: 0 (no orphan pass done — no route removals).
- Last `screen-map.yml` CI run on `main`: not fetched this run (per-workflow REST call skipped for token budget). Green last checked 08-04.

## Backlog deltas
- **New:** [2] `code-review-reality-server-inquiry-notify-route-wiring` — reality-server public `send_contact_message` bypasses new `InquiryNotifier` seam; realtors still get no notification on live path. Depends on PR #2696 landing first.
- **Bumped confidence:** `code-review-reality-server-inquiries-no-ratelimit` medium→high (direct code inspection of `routes/inquiries.rs:180-215` confirms no `RequestPrincipal` extractor and no per-IP throttle on the anonymous public POST). Vector left as `bug`; score unchanged at 2.
- **Resolved (→done):**
  - `code-review-api-handlers-enhanced-chat-stub` — PR #2688
  - `code-review-api-handlers-ai-upstream-error-leak` — PR #2694
  - `code-review-mobile-rn-biometric-prompt-i18n` — PR #2695
  - `code-review-ppt-web-core-ws-pong-timeout-drop` — PR #2689 (was `ready` with plan)
  - `code-review-ppt-web-core-ws-giveup-no-resume` — PR #2691
  - `code-review-reality-server-listing-viewcount-hardcoded-zero` — PR #2692
  - `code-review-reality-server-orphan-schedule-viewing-stub` — PR #2693
- **Decayed:** none (all open items <14 days old — oldest is `code-review-mobile-native-kmp-portfolio-analytics-caps-100` at 3 days).
- **Dropped:** none.

Post-run active backlog: 6 open items — none meet the standard readiness gate (`score>=3`). `code-review-reality-server-inquiries-no-ratelimit` (score 2, high confidence, vector `bug`) is one signal from promotion; the abuse-vector case is strong but reclassifying `bug`→`security` mid-run is out of scope for a normal signal update.

## Plans promoted
- none — no open item meets the readiness gate this run. Two candidates are one signal away: `code-review-reality-server-inquiries-no-ratelimit` (score 2, high; would clear if reclassified to `security` vector for fast-track) and `code-review-mobile-rn-auth-restore-stale-token` (score 2, medium; PR #2690 already open — no fresh promotion needed).

## Auto-fix
- `no candidate` — the sole new signal (`code-review-reality-server-inquiry-notify-route-wiring`) is a `code-review-finding` type, which is not in the Phase 5 allowlist. Allowlisted shapes (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review`) did not fire this run.

## Self-review findings
- none

## Open questions
- Should `code-review-reality-server-inquiries-no-ratelimit` be reclassified from `bug` to `security` vector? Evidence explicitly names "anonymous spam / DB resource-exhaustion vector" and "abuse control is dead code" — abuse-prevention/rate-limit gaps on unauthenticated endpoints are typically security-vector-tracked. Under the security fast-track (`security + high + score>=2`) it would promote immediately. Deferring the reclassification to a manual triage pass.
- ACC-epic feature PRs #2555/#2558/#2559 now 9+ days open with no reviewDecision — third routine run flagging the same 3-PR window. Confirm whether the ACC lead's absence is expected, or whether these need re-routing.

## Quiet day?
- No — 10 PRs merged, 1 new backlog vector, 7 items resolved, 0 plans promoted.
