# 2026-07-05

## Since last run
- Merged PRs: 7 (range #2094–#2099 + #1979 outages test rebase)
- Open PRs touched: 3 (#2089 draft auto-impl for #2076, #1797 auth OCR/rental-PII draft, #1812 reality_portal split draft)
- New / updated issues: 0 new; 8 closed by dispatcher (#2078–#2087 follow-ups landed via PRs #2094–#2099 + #2093)
- Commits: 16 on `dev` (7 PR merges + 8 dispatcher chores + 1 prior routine brief)
- Routine lag: 6h since prior run (on schedule — same-day rerun after the 03:10 UTC 19d catch-up)
- Phases that failed: none. Phase 1.6 (`ppt-project-management`) deferred at 10:19 UTC then **re-fired at 10:25 UTC by `buffer-low: claimable=6/72 — refill planner`**; Scrum Master + pm-security ran and refilled action-list.json 8→40 open (see *Post-commit re-fire* below).

## Shipped
- #2099 refactor(api-server): enforce SlovakAccountingExport honesty invariant by construction — smart `new(...)` constructor replaces the call-me-or-forget `compute_partial(&mut self)` mutator; closes #2086.
- #2098 test(db): add enum sync guard for currency/country lists — Rust `#[test]` hard-codes canonical wire values, exhaustive match makes drift a compile error; adds `FromStr`/`Display` for `CountryCode`; closes #2083.
- #2097 test(messaging): un-quarantine #1771 soft-delete unread invariant — lifts BIT-351 ignore on `soft_deleted_thread_excluded_from_unread_count`, collapses two near-duplicate ignored tests into one; closes #2082.
- #2096 chore(api-server): enforce quick-xml XXE pin via cargo-deny hard ban — replaces the advisory `pinned-dependency-guard.yml` grep with a `[bans]` entry (two half-range bans as VersionReq lacks `!=`); closes #2084.
- #2095 refactor(api-server): extract assign_fault recipient guards — hoists `assign_fault_recipients` + shared `manager_recipients` fns with unit-test cases (actor-as-assignee, actor-as-reporter, dedup); closes #2085.
- #2094 test(db): reuse shared seed_org fixture in favorite_alert_read idempotency test — drops private copy, imports canonical `common::seed_org`; closes #2087.
- #1979 test(api-server): outages happy-path — mints raw manager JWT (matches disputes_happy_path pattern); unblocks the required `test` gate on 13 outages tests (BIT-414).

## Watch
- Stalled review: none matched criteria (>7d open + non-draft + no reviewDecision + non-dependabot). #1812 remains a draft at 11d (already tracked) and #1797 at 12d (also draft, `fix/backend-authz-ocr-and-rental-pii`).
- Reverted: 0 in window.
- Churn hotspots (6h window): none — 7 merged PRs touched 10 distinct files, each once. No file was hit multiple times; no repeated-churn signal.

## PRs stuck in draft despite approval
- none surfaced this run (no live reviewDecision query — deferred with Phase 1.6).

## PRs with verdict=changes, no fix-round progress in 24h
- none surfaced this run (dispatcher `assignments.json` not deep-scanned this run).

## Code review slice
- Segment reviewed: `ppt-web-core` (reason: oldest-unreviewed, last touched 2026-06-05)
- Experts: Frontend
- Findings: 3 (see Backlog deltas for `code-review-finding` signals)
- Next segment: `mobile-native-kmp` (last reviewed 2026-06-06 — now the oldest after ppt-web-core advanced to 2026-07-05)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: not re-counted this run (no touching PR)
- Drift signals this run: 0 (no `frontend/apps/{ppt-web,reality-web}` route files touched by the 7 merged PRs — all backend/tests)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run

## Backlog deltas
- **New:** [3] `code-review-ppt-web-core-useaichat-unauthed` — useAiChat.ts apiFetch bypasses auth token; all `/api/v1/ai/chat/*` calls 401
- **New:** [3] `code-review-ppt-web-core-person-months-units-fetch-unauthed` — person-months.tsx raw fetch of `/api/v1/units` drops Authorization; unit dropdown always empty
- **New:** [2] `code-review-ppt-web-core-app-tsx-missing-error-boundary` — App.tsx Suspense<AppRoutes> has no ErrorBoundary; ChunkLoadError blanks the SPA post-deploy
- **Bumped:** none
- **Decayed:** none (all open items updated_at=2026-07-05 from the earlier catch-up run; nothing crossed the 14-day decay threshold)
- **Dropped:** none

## Plans promoted
- `plans/code-review-ppt-web-core-useaichat-unauthed.md` — route useAiChat.ts through `readAccessToken()` and attach the `Authorization` header with a bearer token, then gate `useDeleteSession` on `response.ok`; test via Vitest + mocked fetch · adversarial pass: passed
- `plans/code-review-ppt-web-core-person-months-units-fetch-unauthed.md` — swap raw fetch for generated `unitsApiList` from `@ppt/api-client`; delete hand-typed row shape · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met the allowlist this run. All 3 new code-review findings are `code-review-finding` type (not in Phase 5's allowlist which is: `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-shape, `stalled-review`). Manual-implementer path applies.

## Self-review findings
- none

## Open questions
- **Phase 1.6 deferral rationale**: this is a same-day rerun 6h after the 03:10 UTC 19d catch-up (which itself skipped 1.6). Delta is 7 auto-impl PRs, none of which map to a `coverage.json` story. Deferring 1.6 keeps pm-security's 39d gap intact — the next incremental run should NOT defer again.
- **AI-chat feature has zero TypeSpec coverage** (grep confirmed): the fix in `plans/code-review-ppt-web-core-useaichat-unauthed.md` deliberately patches the client-side helper rather than routing through `@ppt/api-client`, because the generated client lacks all `AiChat*` symbols. A follow-up plan to add TypeSpec for `/api/v1/ai/chat/*` would let both this fix and any future consumer switch to the generated client.

## Quiet day?
- No — 7 PRs shipped since last cursor, 4 new code-review-finding vectors (3 morning + 1 news-feature systemic finding from pm-security), 2 plans promoted (both high-confidence auth bugs in ppt-web-core), Phase 1.6 re-fired with buffer refill.

## Post-commit re-fire — buffer-low signal (2026-07-05 10:25 → 10:29 UTC)

Dispatcher fired the routine with payload `buffer-low: claimable=6/72 — refill planner` six minutes after the morning commit. Confirmed: `.research/management/action-list.json` had 8 open + 8 in-progress = 16 non-terminal, below the 36 target. Ran the previously-deferred Phase 1.6:

- **Scrum Master** (mandatory) + **pm-security** (rotating role — 39d overdue).
- **New backlog vector:** [3] `code-review-ppt-web-core-news-feature-fetch-unauthed` — pm-security's frontend sweep found the `useAiChat.ts`/`person-months.tsx` missing-`Authorization`-header bug is a class-of-defects: ≥15 raw `fetch()` sites across `frontend/apps/ppt-web/src/features/news/pages/{ArticleDetailPage,CreateArticlePage,EditArticlePage,NewsListPage}.tsx` drop the header, including manager-gated `publish`/`archive`/`pin`/comments/reactions. Not promoted this run — daily cap (2) already hit; will promote on the next incremental if score holds.
- **Buffer refill:** 8 → 40 open (12 role next_actions + 20 coverage-gap candidates merged into `.research/management/action-list.json`).
- **State advanced:** `pm_cursor.next_index` 5 → 6 (pm-data next), `role_last_run.pm-security` = 2026-07-05, `role_last_run.pm-scrum-master` = 2026-07-05, `coverage_cursor.next_index` 12 → 0 (wrap: epic-10a next). `stats.runs` 42 → 43, `stats.vectors_created` 178 → 179.
- **Management artifacts refreshed:** `project-state.md`, `action-list.{json,md}`, `risks.{json,md}`, `roadmap.md` (Buffer line), `roles/pm-scrum-master.md`, `roles/pm-security.md`, `decisions.md` (5 new decisions appended).
- **Governance findings surfaced:** (a) `sprint-status.yaml` epic-level rollups stale vs story detail (epic-10b `in-progress` despite 7/7 done; epic-10a gated on possibly-resolved #481/#482/#487). (b) Test-hardening batch rule violated in practice — 8a-2/8a-3 marked `done` while batch items #480/#484 still open. (c) Two stale drafts still need explicit disposition: #1812 (11d) and #1797 (12d, security-sensitive).
