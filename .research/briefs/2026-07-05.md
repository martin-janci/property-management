# 2026-07-05

## Since last run
- Merged PRs: **452** (span #1440–#2093) — massive catch-up. Only 60 newest enumerated in detail (see `signals/2026-07-05.json`); older 392 rolled up as aggregate context.
- Open PRs touched: **9** — most dependabot; two draft security/refactor stalls (#1797 11d, #1812 9d) newly qualified as `stalled-review`.
- New / updated issues: **193** — all labelled (`follow-up,from-merged-review` dominant); **no untriaged issues** this batch.
- Commits on `dev`: 50 raw / **21 non-dispatcher-non-dep** since `4829015b` → `a7bae9f`.
- Routine lag: **18d 22h** since last run — `lag_warning` + `stale_routine_alert` fired. Cloud cron for the daily routine appears paused (dispatcher on planning branch is healthy).
- Phases that failed: **none** (Phases 1.5 + 1.6 ran lightweight versions — see notes below).

## Shipped
- **#1987** `fix(reality-web)` — closed SSO callback CSRF state-bypass (security-critical, no in-brief regression check possible with current cursors).
- **#1993** `fix(messaging)` — unread soft-delete leak (#1771/#1776/#1789 batch); pinned by test #2039.
- **#1994 / #2038** — SQL-side migration template pagination (#1905) + index+stable-sort test.
- **#2011** `dep-bump-ammonia-rustsec` — RUSTSEC XSS sanitizer bypass, closed advisory.
- **#2026** `fix(dispatcher)` — planner-independent backlog refill + claim-sort aging (starvation).
- **#2043** `fix(reality-server)` — transactional favorite-alert enqueue/advance (#1999).
- **#2073** — closes backlog row `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` (route-level pagination clamp regression).
- **#2070** — closes backlog row `screen-map-drift-pr-1418-ppt` (announcements-route drift reconciled).
- **#2074** — closes backlog row `code-review-api-core-osrng-expect` (propagate CSPRNG errors in accounting crypto).
- **#2075** — closes backlog row `test-gap-screen-map-drift-pr-922-ppt` (ppt screen-map route drift).
- **#2077** — closes backlog row `refactor-churn-hotspot-mobile-documents` (extract document data layer from DocumentsScreen.tsx).
- **#2059 + #2093** — close backlog row `churn-hotspot-mobile-native-listing-detail-kt` (split monolithic Kotlin listing-detail screen; hoist state into ListingDetailViewModel).
- Dependabot: **5 merged** (androidx.compose-bom, ktor group 7-updates, mobile-native gradle 3, npm minor-patch 24, cargo minor-patch 3); 4 open.

## Watch
- **Stalled draft — #1797 (11d)**: `fix(api-server): auth on OCR endpoints + manager-gate rental guest PII reads` — security fix sitting draft since 2026-06-24. Backlog row `stalled-review-pr-1797` (score 1, dx).
- **Stalled draft — #1812 (9d)**: `churn-hotspot-backend-crates-db-src-repositories-reality-portal-rs` split — refactor sitting draft since 2026-06-26. Backlog row `stalled-review-pr-1812` (score 1, dx).
- **Reverts:** none this window.
- **Churn hotspots this run:** raw churn thin (max 2 touches per real-code file). Top-3 candidates — `ListingDetailScreen.kt`, `DocumentsScreen.tsx`, `regional_compliance.rs` — all touches were intentional refactors (not instability). Signals **not fired** because the corresponding backlog rows were resolved via the same PRs.
- **Closed-not-merged this run:** 97 — most dependabot supersessions. No abnormal close-reasons observed.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none  (dispatcher assignments.json not read this run — belongs to planning-branch scope, unchanged since last routine run.)

## Code review slice
- Segment reviewed: **ppt-web-core** (reason: oldest unreviewed at 30d stale — last 2026-06-05).
- Experts: frontend (light static-analysis pass; no subagent spawned this run due to catch-up context budget).
- Findings: **1** — `code-review-ppt-web-core-action-queue-mock-data` (score +2, high, vector `bug`). `useActionQueue.ts:243` returns `generateMockData(role)` + `setTimeout(300)`, and `executeAction.mutationFn:284` awaits `setTimeout(500)` and returns `{success:true}` without hitting the backend. Dashboard's Action Queue widget renders fabricated items and its Dismiss/handle-action buttons are no-op. Same class as the mobile-RN mock-data finding from 2026-06-16.
- Next segment: **ppt-web-core** advances to 2026-07-05; next oldest becomes **ppt-web-core** was oldest; after this run's advance the oldest-unreviewed rotates back — see `state.review_cursor`.
- Skipped: no — light review completed.

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0 — mobile scope still not seeded).
- Drift signals this run: **0** — per-PR file-list scan skipped (452 PR range too large to fan out under context budget). Known drifts closed via #2070 + #2075 this window.
- Orphan screens this run: **0** — not scanned (deferred).
- Last `screen-map.yml` CI run on `main`: `completed/success` at 2026-06-04T22:21Z (workflow is path-gated; no `docs/screens/**` changes since).

## Backlog deltas
- **New:**
  - [2] `code-review-ppt-web-core-action-queue-mock-data` — Phase 1.5 ppt-web-core finding (`useActionQueue.ts` mock queryFn/mutation, high).
  - [1] `stalled-review-pr-1812` — reality-portal repo split PR draft 9d.
  - [1] `stalled-review-pr-1797` — OCR-auth/PII-gate security PR draft 11d.
- **Resolved (→ done):**
  - `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` (PR #2073).
  - `screen-map-drift-pr-1418-ppt` (PR #2070).
  - `code-review-api-core-osrng-expect` (PR #2074).
  - `test-gap-screen-map-drift-pr-922-ppt` (PR #2075).
  - `refactor-churn-hotspot-mobile-documents` (PR #2077).
  - `churn-hotspot-mobile-native-listing-detail-kt` (PR #2059 + #2093).
- **Decayed / dropped:** **38** items lost 1 point each (open with `updated_at ≤ 2026-06-21`) — this catch-up run applied 19d worth of stale-item decay in one pass; items falling to score 0 flipped to `status: dropped`. See `backlog.md` for the full list — most are stale churn-hotspot entries from the mid-June window whose files have since settled.

## Plans promoted
- **none** — top candidate `code-review-ppt-web-core-action-queue-mock-data` at score 2 (below the ≥3 threshold; not on the security fast-track). All previously-promoted plans remain in `plans/` unchanged.

## Auto-fix
- `no candidate` — no signal met (confidence=high AND score≥3 AND allowlist) this run.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present in this scope).

## Open questions
- Cloud cron for the daily research routine appears paused (18d gap). Verify the trigger before next scheduled run — or the next run will keep piling up catch-up work. Dispatcher (planning branch) is unaffected and continues.
- Should the 38 decayed items be re-surfaced for a manual triage sweep? The decay was mechanical — many are legitimately stale (churn hotspots that resolved themselves as files settled) but a few (e.g. `test-gap-hotfix-no-test-pr-1287-rls-llm-sessions` at score 1, RLS-migration test-gap) may still be real gaps worth manual review.

## Quiet day?
- **No** — 452 merged PRs consumed, 6 backlog rows resolved, 3 new rows created, 1 code-review finding, 38 items decayed. Not quiet by any measure — just a very late catch-up.
