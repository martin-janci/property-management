# 2026-07-05

## Since last run
- Merged PRs: **435** (range #1440–#2099) — 19-day catch-up window. Includes 5-of-the-6 previously-ready plans landing via consolidation PR **#1955** (`ac3fc2c`), plus heavy accounting / messaging / regional-compliance clusters.
- Open PRs touched: **8** (2 stalled drafts newly flagged: #1797, #1812; the rest carry over from prior briefs).
- New / updated issues: **30+** (page-1 subset — all had labels; no fresh `untriaged-issue` signals).
- Commits on `dev`: 200+ (from `4829015b` → `7e09d77d`, plus a dispatcher tick at `18953b0`). state.json cursor was already advanced by an early-morning dispatcher run.
- Routine lag: **19d 4h since last committed routine run** — `lag-warning-2026-07-05` already recorded in seen_signals by the morning partial run, `stale_routine_alert` re-affirmed in this brief.
- Phases that failed: **none**.

## Shipped
- **PR #1955** — integration/consolidation of 22 reviewed/compiling PRs. This is the mechanism that landed most previously-ready plans; individual plan slugs archive today.
- **PR #2099** — refactor(api-server) `SlovakAccountingExport` honesty invariant enforced by construction.
- **PR #2097** — test(messaging) un-quarantine #1771 soft-delete unread invariant (BIT-351 lift).
- **PR #2096** — chore(api-server) `quick-xml` XXE pin enforced via `cargo-deny` hard ban (was advisory-only grep guard).
- **PR #2074** — code-review-api-core-osrng-expect: propagate CSPRNG errors instead of panicking (crypto no-panic).
- **PR #2060** — fix `units u.name` schema drift in `lease.rs` (4 read paths 500 → 200).
- **PR #2059** — churn-hotspot mobile-native listing detail split.
- **PR #2043** — fix(reality-server) transactional favorite alert enqueue/advance (closes #1999).
- **PR #2026** — fix(dispatcher) planner-independent backlog refill + claim-sort aging.
- **PR #1993** — fix(messaging) unread soft-delete leak + set-based block check (#1771 invariant).
- **PR #1987** — fix(reality-web) close SSO callback CSRF state-bypass.
- **PR #1946** — fix(acc) TOTP + capability-token hardening (RFC-6238 TOTP, CSPRNG tokens, SHA-256 at-rest, constant-time compare). KMS-encrypt of the TOTP secret + replay/`last_used_step` guard **explicitly deferred** — surfaced as new risk `pm-security-totp-kms-encryption-deferred`.
- **PR #1861** — fix(api-server) 19 regional-compliance endpoints (Epic 72) — merged with `- [ ]` unchecked in body → new backlog vector `unchecked-todo-pr-1861`.
- **PR #1859** — fix(api-server) 23 tenant-migration endpoints (BIT-260) — same class → `unchecked-todo-pr-1859`.
- **PR #1862** — test(BIT-265) platform-admin authz regression — 25 endpoints partial→done, merged with unchecked TODOs → **new score-3 vector** `unchecked-todo-pr-1862` (security lens).

## Watch
- 🚨 **Dispatcher buffer low** — routine fire payload reported `claimable=6/72` at 2026-07-05 (see routine-fire-payload). Refill planner is needed; not surfaced by state.json alone.
- **Stalled draft — PR #1797** (`fix(api-server) auth on OCR endpoints + manager-gate rental guest PII reads`) — 12 days open, mechanical rebase at 04:27 UTC today, no reviewDecision. Sensitive authz work (`ai/ocr.rs` + `rentals/bookings.rs`) dangling. New signal `stalled-review-pr-1797` (+1) and new risk `pm-security-pr-1797-ocr-pii-draft-stalled`.
- **Stalled draft — PR #1812** (churn-hotspot `reality_portal.rs` split) — 11 days open, `needs-human-review` label, +1922/−1803 pure-move refactor. Signal `stalled-review-pr-1812`.
- **New churn hotspots (top 3):**
  - `backend/servers/api-server/src/routes/accounting.rs` — ~8 fix PRs in window (state-machine, secret-serialize, symmetric-404, quorum, TOTP).
  - `backend/servers/api-server/src/routes/messaging.rs` — 6+ fix PRs (unread soft-delete leak, N-party delivery, per-participant watermark, attachment MIME).
  - `backend/crates/db/src/repositories/lease.rs` — 3 PRs on schema drift (`units.designation` join). #2060 fixed a 42703 500.
- **Closed-not-merged cluster (5 tracked):** #1802 (messaging N-party — superseded by #1789/#1993), #1804 (scheduler dedup abandoned), #1857 (security-llm-doc-idor tests — actually landed in #1955 rollup; item is done, plan archived), #1919 (bug-risky-churn-pr-963 abandoned), #1958 (bulk BIT-360 batch too large; smaller batches #1957/#1964 landed).
- **Dep-update noise:** 23 dependabot merges in window, 6 dependabot PRs still open (`#2014, #2016, #2018, #2020, #2022-2025`). Logged, not scored.

## PRs stuck in draft despite approval
- none (no `isDraft=true && reviewDecision=APPROVED && >24h` candidates identified this run; only 2 drafts sampled from 8 open PRs — full reviewDecision fetch skipped to conserve tool budget).

## PRs with verdict=changes, no fix-round progress in 24h
- none (`.research/management/assignments.json` shows 6 rows `review` with `verdict=approve` and 1 `quarantined` — no `verdict=changes && >24h` candidates).

## Code review slice
- Segment reviewed: **ppt-web-core** (reason: oldest-unreviewed at 30d stale; no churn-aligned candidate — the 3 new hotspots are all backend).
- Experts: frontend.
- Findings: **3** — added to backlog:
  - `code-review-ppt-web-core-api-retry-non-idempotent` (score 2, bug, high) — `lib/api.ts:184` retries POST/PUT on 5xx without idempotency check.
  - `code-review-ppt-web-core-perf-metrics-listener-leak` (score 2, bug, high) — `usePerformanceMetrics.ts:142/153` addEventListener leaks on remount.
  - `code-review-ppt-web-core-dashboard-route-unprotected` (score 3, security, high) — **promoted this run** to `plans/code-review-ppt-web-core-dashboard-route-unprotected.md`.
- Next segment: **mobile-native-kmp** (last reviewed 2026-06-06 — oldest after this run).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0 — not yet seeded).
- Drift signals this run: **0** (no ppt-web-routes / reality-web-app touches without matching doc — the Phase 1 sweep found no new drift class in the 435-PR window that wasn't already scored).
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not fetched this run (no `docs/screens/**` edits to trigger).

## Backlog deltas
- **New (16):**
  - [3] `code-review-ppt-web-core-dashboard-route-unprotected` — /dashboard/manager mounted bare, no ProtectedRoute (Phase 1.5, security fast-track, promoted).
  - [3] `unchecked-todo-pr-1862` — PR #1862 (BIT-265 platform-admin authz) merged with unchecked TODOs (security lens).
  - [2] `code-review-ppt-web-core-api-retry-non-idempotent` — POST/PUT retry hazard.
  - [2] `code-review-ppt-web-core-perf-metrics-listener-leak` — addEventListener leak.
  - [2] `unchecked-todo-pr-1861` — Epic 72 regional-compliance endpoints, unchecked scope.
  - [2] `unchecked-todo-pr-1859` — BIT-260 tenant-migration endpoints, unchecked scope.
  - [2] `unchecked-todo-pr-2037` — recent PR body has unchecked `- [ ]`.
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-accounting-rs` — 8+ fix PRs across window.
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-messaging-rs` — 6+ fix PRs.
  - [1] `churn-hotspot-backend-crates-db-src-repositories-lease-rs` — schema-drift cluster.
  - [1] `stalled-review-pr-1797` — 12d authz draft.
  - [1] `stalled-review-pr-1812` — 11d refactor draft.
  - [1] `closed-not-merged-pr-1919` — bug-risky-churn-pr-963 abandoned.
  - [1] `closed-not-merged-pr-1958` — BIT-360 bulk re-enable abandoned.
  - [1] `closed-not-merged-pr-1802` — messaging N-party superseded.
  - [1] `closed-not-merged-pr-1804` — scheduler payment-reminder dedup abandoned.
- **Bumped:** none this run.
- **Decayed:** 14 open items lost −1 each (updated_at ≤ 2026-06-21). Full list in `backlog.json` (each has `decayed: no new signals in 14 days` evidence line).
- **Dropped by decay:** 38 items at score 1 → 0 → `dropped`. This is the 19-day catch-up settlement; expected shape.
- **Resolved (`done`) via PR-body match + evidence-gone grep on `origin/dev`:**
  - `security-llm-doc-idor` — PR #1955; ai/llm.rs 3 handlers now use `RlsConnection`, regression tests at `ai_llm_cross_tenant_idor_tests.rs` (520 lines).
  - `bug-ios-searchview-uncompilable` — SearchView.swift now defines `performSearch()` + `scheduleSearch(for:)`.
  - `code-review-mobile-rn-report-fault-fake-submit` — ReportFaultScreen.tsx (moved to `screens/faults/`) now calls `apiRequest('/api/v1/faults', method:POST)`; `ReportFaultScreen.test.tsx` regression guard present.
  - `code-review-reality-web-share-comparison-404` — ComparisonUrlHandler.tsx now fetches `${getApiBase()}/api/v1/listings/${id}`.
  - `code-review-reality-web-listing-page-ssr-crash` — `buildListingJsonLd` extracted to standalone module with defensive `unknown → object|null` validation.
- **Rolled back to `open`:** `code-review-reality-web-realtor-mgmt-untranslated` — verified on `origin/dev`: 23 hardcoded English strings still present in RealtorManagement.tsx alongside 18 `t()` calls. The plan was over-scoped for what actually shipped; needs a fresh translation pass. Plan moved to `_archive/`.
- **Also archived (no backlog row, hand-authored plan):** `p0-10-mock-route-wiring.md` — App.tsx grep on `origin/dev` returned 0 mock hits, so the mock-route-wiring work is done.

## Plans promoted
- `plans/code-review-ppt-web-core-dashboard-route-unprotected.md` — wrap `/dashboard/manager` and `/dashboard/resident` routes in `<ProtectedRoute>`; add `routes/groups/core.test.tsx` asserting unauth-redirect. Mode: cloud-ok. · adversarial pass: passed (chain verified via `sed -n '149,151p' core.tsx` — bare `<Route>` mount confirmed; `useAuth` grep in `ManagerDashboardPage.tsx` returned 0 — confirms in-component gate absent; test would fail on `main`).

## Auto-fix
- `no candidate` — the 2 eligible `stalled-review` signals are at score 1 (below the ≥3 certainty bar). All other signals this run (`churn-hotspot`, `closed-not-merged-pr`, `code-review-finding`, `unchecked-todo`) fall outside the Phase 5 allowlist. `ROUTINE_AUTOFIX_DISABLED` was not set.

## Self-review findings
- none (`.research/self-improvement/findings.json` present but not evaluated this catch-up run; a targeted refresh belongs to the next non-catchup pass).

## Open questions
- **Session-branch drift** — the checked-out branch `claude/wonderful-gates-dc4ucc` is **171 commits behind `origin/dev`**. The pm-security subagent worked around this by reading via `git show origin/dev:<path>`; every code-existence claim in this brief was cross-checked the same way. Worth confirming whether the routine should rebase the session branch before future runs, or whether the pattern of "always read via `origin/dev`" is the intended contract for cloud sessions.
- **PR #1797 unblock** — pm-security flagged it as med-severity (sensitive authz + PII in flight). Who owns the review nudge — dispatcher, or a manual assign?
- **Buffer-low signal (dispatcher fire payload):** `claimable=6/72` was reported to this routine. State.json doesn't record dispatcher assignment counts, so the routine can only surface it here — the refill workflow itself lives in the dispatcher/planner. Should the routine auto-file a `dispatcher-buffer-low` triage row when this signal arrives?
- **PM rotation cadence** — pm-security ran today after 39 days stale. Two consecutive daily runs missed the queue (routine was stalled 19d). Consider a rotation-freshness check that surfaces "role X hasn't run in >21d" independently of the routine's own lag.

## Quiet day?
- **No.** 435 merged PRs in the window, 5 previously-ready plans resolved to `done` (via #1955 rollup), 16 new backlog vectors, 1 plan promoted, 3 code-review findings, 3 pm-security risks added + 1 retired, 38 decayed dropouts. `stats.runs` 41 → 42, `vectors_created` 175 → 191, `plans_created` 12 → 13.
