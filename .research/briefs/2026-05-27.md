# 2026-05-27

> Second run today (~20h after the 03:15Z run). This brief supersedes the earlier same-day brief and covers activity since `last_run_iso` 2026-05-27T03:15:46Z.

## Since last run
- Merged PRs: 25 (range #563–#634)
- Open PRs touched: 12
- New / updated issues: ~30 (all labeled `follow-up` / `from-merged-review` — none untriaged)
- Commits: 297 on `dev`
- Routine lag: on schedule (~20h since last run)
- Phases that failed: none

## Shipped
- #565 session cookie scope hardening (P0-12) — security-labeled; follow-up #617 flags the cookie `Path=/api/v1/sso` as a breaking change to audit
- #604 FolderTree error toasts routed through t() (issue #574) — security-labeled, 13 files
- #610 backend integration tests for POST /api/v1/documents/upload (902 add) — top test delivery this run
- #611 report_executions table + real SQLx execution-history repo (follow-up #616)
- #623 report schedule editor modal (cron picker, recipient list, enabled toggle)
- #607 booking-push endpoints — added batch-cap + non-negative guards, no test (→ new backlog item)
- #568 / #609 SSO `/auth/callback` route + AuthContext.loginWithSsoCode + E2E
- #612 P1-04 stable Display for audit fields; #613 RBAC + cron validation tests for report schedules

## Watch
- Closed-unmerged security/correctness fixes (dispatcher supersede sweep):
  - #602 (document download/preview intra-org IDOR, issue #582) — **verified resolved in current code**: `documents/core.rs` `get_download_url`/`get_preview_url` now call `document_access_allowed()` for non-managers. Superseded, not dropped.
  - #603 (scheduler fail-closed URL signing + cookie-domain injection guard, issue #571) — closed unmerged, **issue #571 still open**; not re-verified in code this run. Possible dropped fix — needs a look.
  - #599 (iOS SSO `beginSsoFlow()` never called, issue #578) — closed unmerged, **issue #578 still open**; iOS SSO login may still be broken.
- Churn hotspots this run are all feature-delivery (single-commit): `MoveFolderDialog.tsx` (802, new), `document_upload_tests.rs` (796, new test file), `FileDisputePage.tsx` (546). Not instability — no backlog items, tracked in `hotspot_history` only.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: mobile-rn (reason: churn-aligned — `AnnouncementDetailScreen.tsx` churned this run — and never previously reviewed)
- Experts: frontend + tester
- Findings: 3 (all in `frontend/apps/mobile/src/screens/voting/VotingScreen.tsx`; see Backlog deltas)
- Next segment: reality-web (only remaining unreviewed segment)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: ppt/ + reality/ populated; mobile/ not yet seeded
- Drift signals this run: 0 — ppt routes (`App.tsx`, `lazyRoutes.tsx`) were touched by #564/#568/#623 but each PR also updated a matching `docs/screens/ppt/*.md` (9 ppt screen docs updated this run)
- Orphan screens this run: 0
- Resolved: `test-gap-screen-map-drift-ppt-report-history` → done (reports screen doc updated this run alongside #611/#623)

## Backlog deltas
- **New:** [2] Booking push availability/rates endpoints add guards with no regression test (#607) — `test-gap`
- **New:** [2] ReportSchedule.update_schedule stores cron in `time` workaround; documented UPDATE never runs (#611, ties #616) — `bug`
- **New:** [2] DocumentsBrowse MoveFolderDialog cannot pre-select current folder (#623) — `dx`
- **New:** [2] Mobile VotingScreen double-casts API result — render-time crash on unexpected shape — `bug`
- **New:** [1] Mobile VotingScreen hardcodes en-US in toLocaleDateString — `bug`
- **New:** [1] Mobile VotingScreen pure transforms toUiStatus/toUiVote untested — `test-gap`
- **Done:** Screen-map drift: report execution-history route — reports screen doc updated this run
- **Decayed:** none (all open items updated within 14 days)
- **Dropped:** none

## Plans promoted
- none — highest new/open item score is 2; no item cleared the readiness threshold (3, or 2 for high-confidence security), and no open `security` vector remains. Quality over volume.

## Auto-fix
- `no candidate` — no allowlist signal met confidence=high + backlog score≥3 this run. The two `fixme-in-merged-code` signals are score 2; the report-schedule one is already tracked by issue #616.

## Open questions
- Were #603 (scheduler unsigned e-signature URL fallback; cookie-domain injection guard) and #599 (iOS SSO `beginSsoFlow`) actually landed elsewhere, or dropped when their PRs were closed unmerged? Issues #571 and #578 remain open. A code check would confirm whether these are live regressions.

## Quiet day?
- No
