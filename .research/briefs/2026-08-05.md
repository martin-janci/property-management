# 2026-08-05

## Since last run
- Merged PRs: 7 (range #2653–#2661; #2643 was an issue, not a PR)
- Open PRs touched: 0 (all currently-open PRs last updated ≤ 2026-07-30, no touches this window)
- New / updated issues: 4 (#2651, #2652, #2654, #2658 — all filed AND closed in-window by the corresponding fix PRs)
- Commits: ~24 on `dev` (7 review-PR merges + ~17 dispatcher/generator bookkeeping)
- Routine lag: 1d 9h since last run (`last_run_iso=2026-08-04T03:15Z`; threshold 36h; on schedule)
- Phases that failed: none
- Phase 1.5 note: adopted dispatcher Tier1d output (reality-server, 3 findings) rather than re-running the skill — reality-server was the oldest-unreviewed segment AND the dispatcher already reviewed it today (2026-08-05 03:17Z); duplicating would waste tokens
- Phase 1.6 skipped: incremental delta since 2026-08-04 is dominated by dispatcher bookkeeping (project ~96 % converged: coverage 47/49 done + 2 partial-→-done reconcile pending, backlog drained pre-run); heavy PM subagent skipped; pm_cursor + coverage_cursor NOT advanced; G16 becomes no-op per its "skip" clause

## Shipped
- #2653 gh-issue-2651 — pick-target-epic normalises epic_prefix to lowercase kebab (dispatcher T21 self-test regression fix)
- #2655 gh-issue-2652 — dispatcher claim-time gate for mobile-native/KMP tasks (unlandable in cloud runner; issue #2652 closed)
- #2656 gh-issue-2654 — ThreadDetailScreen send-error banner now routes through `t('messages.threadSendError')` across all 6 mobile locales
- #2657 screen-map-drift-pr-2648-ppt — docs/screens/ppt/ reconciled with rentals-auth-nonnull PR #2648
- #2659 churn-hotspot-…-ThreadDetailScreen-tsx — malformed-timestamp guard (formatMessageTime returns '' on invalid; NaN-safe sentAtSortKey sinks anomalous rows to the end)
- #2660 gh-issue-2658 — voice_webhook `authenticate_voice_user` now validates the presented OAuth access token in constant time and fails closed; `/verify` HMAC path also hardened (VOICE_WEBHOOK_SECRET fail-closed + constant-time compare); issue #2658 closed
- #2661 screen-map-drift-pr-2649-ppt — docs/screens/ppt/financial-payments.md reconciled with PR #2649 onError toasts (docs-only)

## Watch
- Stalled review: none new — the 12 currently-open PRs are 3 dependabot batches + 3 stalled acc-epic PRs from 2026-07-30; nothing crossed the >7d threshold *this run* that hasn't already been flagged
- Reverted: none
- Churn hotspots this run: `frontend/apps/mobile/src/screens/messages/ThreadDetailScreen.tsx` (2 commits — #2656, #2659; runs_seen advances 1 → 2 — repeated-churn signal fires). No other file crossed the top-3 threshold this window.

## PRs stuck in draft despite approval
- none (per open-PR list — no drafts currently marked approved)

## PRs with verdict=changes, no fix-round progress in 24h
- none (assignments.json empty this run — no active in-progress rows)

## Code review slice
- Segment reviewed: reality-server (reason: oldest-unreviewed AT 2026-08-02 AND dispatcher Tier1d already produced findings today; adopting saves a redundant skill invocation)
- Experts: rust (as executed by the dispatcher generator)
- Findings: 3 adopted from `signals/2026-08-05-reality-server-tier1d.json` (2 × +2 bug, 1 × +1 bug) — all promoted to `open` backlog rows this run
- Next segment: `ppt-web-ui` / `ppt-web-core` / `reality-web` / `mobile-rn` all tied at 2026-08-03; picker will take the tied set in a future run
- Skipped: no — adopted from dispatcher tier-1d

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` 106 + `reality/` 47; `mobile/` not yet seeded → 0)
- Drift signals this run: 0 — no PR touched a frontend route in the trigger set without an accompanying screen-doc update (PRs #2656/#2659 touched `frontend/apps/mobile/src/screens/**` which is excluded until `docs/screens/mobile/` exists; #2660 is backend-only; #2657/#2661 *are* the docs updates for #2648/#2649 drift)
- Orphan screens this run: 0 (no scan performed — routes touched this run had no matching orphan basenames)
- Last `screen-map.yml` CI run on `main`: not queried this run (skipped to save token budget on incremental run)

## Backlog deltas
- **New:** [2] `code-review-reality-server-link-pm-account-noop` — `link_pm_account()` returns Success but never writes `pm_user_id` (silent no-op mutation)
- **New:** [2] `code-review-reality-server-pwreset-token-dropped-prod` — password-reset token minted+stored but dropped in release builds; feature dead in prod
- **New:** [1] `code-review-reality-server-inquiry-detail-empty-msgs` — inquiry-detail always returns `messages: vec![]`; realtor thread never fetched
- **New:** [1] `repeated-churn-frontend/apps/mobile/src/screens/messages/ThreadDetailScreen.tsx` — hotspot_history runs_seen 1→2 (this run's #2656/#2659 stack on last run's #2641/#2642); both windows tested, no risky-churn
- **Resolved:** `screen-map-drift-pr-2649-ppt` → done (PR #2661 merged 2026-08-04 — docs-only reconcile)
- **Bumped:** none
- **Decayed:** none (no `open` items older than 14 days at run start)
- **Dropped:** none

## Plans promoted
- none — the 4 new items all score ≤ 2 (below the +3 promotion floor); none are `vector: security`, so the security fast-track (score ≥ 2) doesn't apply; the 3 reality-server bugs are real user-visible defects — they'll accrue score on subsequent runs (post-merge review, further evidence) or a human can promote by hand

## Auto-fix
- no candidate — no open item met confidence=high + score≥3 + allowlist match this run

## Self-review findings
- open=30 (high=6, medium=21, low=3); new this run=0; acknowledged=19
- fp-mcp-push-cannot-inline-large-archive [recurrence=14] — Phase 6 archive-move >64KB blocks `mcp__github__push_files` inline path; fix: adopt PR+API-merge as primary push path (proven working)
- fp-mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=11] — dispatcher-prompt hard-assumes git push is proxy-403-blocked; fix: runtime probe + PUSH_METHOD=git switch + bypass 64KiB guard when git works
- fp-stale-in-progress-action-list-decoupled-from-assignments [recurrence=5] — action-list.json in-progress bijection broken vs assignments.json; fix: extend gc1-reconcile + T32 self-test
- fp-implementer-sandbox-zero-pushed-branch-multigen [recurrence=3] — reclaimed rows still branchless on origin after two implementer generations; fix: escalate to status=failed after N reclaims + branchless + age>Nh
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — buffer claimable count doesn't subtract archive task_ids; fix: extend gc1-reconcile close step + fix Phase 2.6/GC3 metric
- … and 1 more (see findings.json)

## Open questions
- Buffer-refill fire payload (`buffer-low: claimable=1/72 — refill planner`) suggests the dispatcher is asking for more work: this run adds 4 open rows (3 reality-server bugs + 1 repeated-churn refactor). Of those, 1 (mobile-native-portfolio-caps-100, carried from 2026-08-04) is cloud-gated by the `.research/mobile-native-gate.sh` claim-time gate landed in #2655; the 3 reality-server bugs and the repeated-churn refactor are all landable in cloud. Whether the dispatcher's Tier-1 refill can lift these to `ready` (score-3 gate) without human bumping is the next open question.
- Coverage-cursor staleness (`coverage stale` per the fire payload) is unresolved — the dispatcher's 2026-08-05 08:18/10:12 runs deferred the coverage.json 84-1/84-2 partial→done reconcile (too large for MCP inline push). A hand or direct-git-push follow-up is needed.

## Quiet day?
- No — 7 PRs shipped, 4 new backlog vectors (3 reality-server bugs + 1 repeated-churn), 1 open item resolved by merged PR
