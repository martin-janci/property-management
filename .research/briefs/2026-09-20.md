# 2026-09-20

_Normal incremental run. 19-day cold-start after the 2026-09-01 supplementary fire. Trigger payload: `buffer-low: claimable=12/72 but ALL open backlog is infra-blocked (#2966 utoipa-swagger-ui egress + mobile-native AGP egress); need CLOUD-LANDABLE frontend/reality-web vectors`. This run folds 10 pre-generated dispatcher Tier-1d signals into the backlog (all cloud-landable frontend/reality-web + api-core) — the requested refill._

## Since last run
- Merged PRs: 17 (range #2922–#2956) — snapshot from `gh api pulls?state=closed&base=dev` filtered to `.number > 2921 and merged_at != null`; page-1 only (no truncation warning: total returned matches expected — 17 within window).
- Open PRs touched: ≥7 dependabot batches (`#2957`, `#2960`, `#2961–#2965`) plus the standing drafts (`#2902` screen-map-drift-pr-2894-reality, `#2744` dispatcher un-wedge, `#2559`/`#2555` UC-ACC-05.x — still `needs-human-judgement`, 50+ days).
- New / updated issues: 6 (issues since #2847 → #2946, #2951, #2953, #2954, #2966; several closed same window). Highest issue seen: `#2966` (open: api-server utoipa-swagger-ui egress-blocked — direct cause of the cloud-only buffer starvation).
- Commits: 45 on `dev` (mostly dispatcher `.research/*` chore commits; 17 real code PRs). HEAD `e67b2e8`.
- Routine lag: **19d 18h** since last run — `lag-warning-2026-09-20` emitted (Δ=0). Also `stale-routine-alert-2026-09-20` (P4 rule; ≥3d threshold). Cloud routine cron likely paused; dispatcher (planning branch) still active (most recent 2026-09-20 10:34 assignments commit).
- Phases that failed: none.

## Shipped
- Full merged-PR list this window (backend + frontend fixes, no route changes; no screen-map drift):
  - **Backend security / IDOR follow-ups:** #2925 (compliance audit-log db-error leak regression), #2926 (FCM legacy dead endpoint removal), #2928 + #2931 (report_summary snapshot-consistency + test), #2932 (agent-review self-review 403 e2e), #2938 (invoice PDF renderer + tests), #2922 (reality-server saved-search typed error enum).
  - **Frontend ppt-web-core (auth / query hygiene):** #2941 (JWT exp cold-boot check in AuthContext), #2942 (single-flight refresh + 401 replay in api.ts), #2943 (logout-purge allowlist coverage test), #2952 (queryKeys.test.ts auto-discovery), #2956 (queryKeys.test.ts hardening).
  - **Frontend ppt-web-ui:** #2927 (EmergencyContactDirectoryPage error-i18n test), #2940 (ConfirmationDialog i18n loading label).
  - **Frontend mobile-rn (session-change data purge):** #2950 (NFC access log + QR scan history purge), #2955 (SecureStore NFC credential purge).
  - **Deps:** #2935 (npm-minor-patch bump — 40 updates).
- **No route file (`ppt-web/src/{App.tsx,routes/**}` / `reality-web/src/app/**`) touched** in any of the 17 PRs — so **zero `screen-map-drift` signals** this run.

## Watch
- Stalled review: #2744 (dispatcher un-wedge — draft, `needs-human-judgement` since 2026-08-13, 38d idle), #2559 / #2555 (UC-ACC-05.x, 52d idle).
- Reverted: none this window.
- Churn hotspots (top-3 non-lockfile code files, 19-day window): `oauth_integration_tests.rs` 3427 lines (runs_seen 1→2); `voice_webhooks.rs` 2999 lines (runs_seen 3→4 — repeated-churn stacks +1); `integrations/webhook.rs` 2799 lines (runs_seen 2→3 — repeated-churn stacks +1). All three are backend, all cloud-unlandable while #2966 blocks the api-server verify gate.

## PRs stuck in draft despite approval
- #2902 screen-map-drift-pr-2894-reality — still contaminated branch, no fix-round in 24h.
- #2744 fix(dispatcher): un-wedge oversize-archive push — draft with `needs-human-judgement` since 2026-08-13.
- (No fresh cases surfaced this run; page-1 open-PR list did not re-query `reviewDecision==APPROVED and isDraft==true` — surfaces above are carry-over from 2026-09-01 brief.)

## PRs with verdict=changes, no fix-round progress in 24h
- #2902 — unchanged from prior brief.
- Not re-queried against planning-branch assignments.json this run (routine budget; dispatcher continues to reconcile).

## Code review slice
- Segment reviewed: **none this run** — dispatcher tier1d already emitted 5 fresh code-review signal files across 4 segments (reality-web × 2 on 2026-09-19, api-core / ppt-web-core / ppt-web-ui on 2026-09-20) between the last routine commit and now. Routine folded all 10 findings into `backlog.json` as `open` rows.
- Experts covered by the adopted tier1d output: `frontend` (reality-web, ppt-web-core, ppt-web-ui) + `rust` (api-core).
- Findings: 10 net-new (see Backlog deltas). All medium confidence; none above the +2 promotion bar or the security fast-track (+2 requires **high** confidence — these are medium).
- Next segment: `api-handlers` (segment cursor 2026-08-31, one of two now co-oldest with `mobile-rn` 2026-09-09; api-handlers wins the age tiebreak).
- Skipped: yes — dispatcher tier1d already produced 10 code-review-finding signals across 4 segments since last routine run.

## Screen-map status
- Total `docs/screens/` files: unchanged from 2026-09-01 brief (no additions in the 19-day window; docs/screens/mobile/ still absent).
- Drift signals this run: **0** — of the 17 merged PRs, none touched the trigger set (`ppt-web/src/{App.tsx,routes/**}` or `reality-web/src/app/**`).
- Orphan screens this run: **0** — no route-file changes to scan against.
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (page-1 cap on gh calls).

## Backlog deltas
- **New (13 rows):**
  - **Cloud-landable frontend / reality-web (7 rows, all `open`):** `code-review-reality-web-unvalidated-href-url-scheme` [2, security], `code-review-reality-web-csv-import-filepick-no-type-guard` [1, bug], `code-review-reality-web-comparison-share-id-unencoded` [1, bug], `code-review-ppt-web-core-routes-missing-auth-guard` [2, bug], `code-review-ppt-web-core-errorhandler-raw-message` [1, bug], `code-review-ppt-web-ui-budgetform-index-key` [2, bug], `code-review-ppt-web-ui-hardcoded-en-us-locale` [2, bug], `code-review-ppt-web-ui-registry-full-reload-nav` [1, bug].
  - **api-core (2 rows, `open`, currently cloud-unlandable until #2966 clears):** `code-review-api-core-statement-upload-not-atomic` [2, bug], `code-review-api-core-statement-account-iban-hardcoded` [1, bug].
  - **Churn hotspots (3 rows, `open`, refactor):** `churn-hotspot-…-oauth-integration-tests-rs` [1], `churn-hotspot-…-voice-webhooks-rs` [2, includes repeated-churn stack], `churn-hotspot-…-integrations-webhook-rs` [2, includes repeated-churn stack].
- **Bumped:** none.
- **Decayed:** `code-review-mobile-native-kmp-create-listing-not-wired` 4→3 (open, updated_at 2026-09-01 ≤ cutoff 2026-09-06).
- **Dropped (decay to 0):** `code-review-mobile-native-kmp-portfolio-analytics-caps-100` (1→0), `code-review-mobile-native-kmp-cancellation-swallowed` (1→0), `code-review-mobile-native-kmp-httpclient-no-timeout` (1→0). All three receive evidence line "decayed: no new signals in 14 days".
- **Resolved:** none this run — of the 5 pre-existing open/ready mobile-native-kmp items, `code-review-mobile-native-kmp-inquiries-response-contract` (ready, plan exists) had no matching PR merge in-window; its files still exist and the mismatch pattern is still in-tree. Left unchanged.
- **Cloud-landable open rows after this run (score ≥ 1):** 8 (7 cloud-landable frontend + 3 churn refactor — subtract 2 api-core rows that remain cloud-unlandable). This directly answers the trigger payload's ask.

## Plans promoted
- **0 new plans this run.** All 10 tier1d findings are medium-confidence with Δ ≤ 2 — below both the standard promotion bar (score ≥ 3) and the security fast-track (needs confidence=high). One high-confidence security row could have qualified but none exist in the ingest. The one pre-existing `ready` row (`code-review-mobile-native-kmp-inquiries-response-contract`, score 3) already has `plans/code-review-mobile-native-kmp-inquiries-response-contract.md` — do NOT re-promote.
- Adversarial pass: n/a (no promotions).

## Auto-fix
- `no candidate` — the 10 tier1d code-review-finding signals are not on the Phase-5 allowlist (allowlist: screen-map-drift, screen-map-orphan, fixme-in-merged-code, lockfile-drift, stalled-review). Zero allowlisted signals emitted this run. `ROUTINE_AUTOFIX_DISABLED` unset — kill switch not in play; the phase is a legitimate no-op.

## Self-review findings
- none (`.research/self-improvement/findings.json` unchanged; last self-review pass on planning branch 2026-09-19, 60 findings, +2 new — not folded into routine brief per phase separation).

## Open questions
- **Cloud routine cron paused** — 19-day lag on `state.last_run_iso` suggests the routine's cloud fire hasn't run since 2026-09-01. Dispatcher still ticks on planning branch. Confirm the routine cron is scheduled (this run is manual/session-triggered).
- **#2966 utoipa-swagger-ui egress blocker** — the direct cause of the buffer-low payload; the 2 api-core rows added this run remain unclaimable in cloud until this clears. Same posture as #2652 (mobile-native AGP). No new decision from the routine — the 8 cloud-landable rows added this run mitigate.
- **telegram: skipped by executor** — routine on session branch skipping the optional Telegram send per task instructions.

## Quiet day?
- **No.** 17 PRs merged since last run, 13 new backlog rows added, 1 decayed, 3 dropped by decay. Phase-1.5 skipped (adopted dispatcher tier1d output). Phase-5 no-op by allowlist. Goal violations: see `signals/2026-09-20.json` `goal_checks[]`.
