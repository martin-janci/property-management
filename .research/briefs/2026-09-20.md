# 2026-09-20

_Catch-up run: the cloud routine last committed on 2026-09-01 (19d gap). The dispatcher branch (`planning`) kept ticking through the window — this run reconciles routine cursors, decays open items, resolves one stalled-review item, and promotes 1 plan. Phase 1.5 (rotating expert review) and Phase 1.6 (project-management) skipped: the dispatcher's Tier-1d review cycles have covered the ppt-web-core / reality-web / api-core segments (evidence in `signals/2026-09-1{8,9}-*.json` and `2026-09-20-*-tier1d.json`); firing a fresh routine review over the same 19d window is duplicative._

## Since last run
- Merged PRs: 23 since #2921 (window #2583–#2956; range includes ~5 back-dated dependabot merges + 18 code PRs on the dispatcher's Tier-1d cycles).
- Open PRs touched: 12 (11 dependabot deps + #2902 `screen-map-drift-pr-2894-reality` draft, still contaminated).
- New / updated issues: 18 (highest #2966 — `blocked infra: api-server verify gate cannot build in cloud runner — utoipa-swagger-ui build script egress-blocked`; new incarnation of the closed #2949 that first surfaced 2026-09-11).
- Commits: 100+ on `dev` (majority `chore(research): dispatcher …` commits from the planning branch; API-side code work merged in via PRs).
- Routine lag: **19d 0h since last run** — `lag_warning` + `stale_routine_alert` both fire (state.last_run_iso=2026-09-01T18:38:58Z; cloud routine paused/failing; dispatcher unaffected, last dispatcher commit at 2026-09-20T16:29Z).
- Phases that failed: none (skips are explicit — see banner above).

## Shipped
_High-signal subset only; full titles in `git log origin/dev --oneline` for the window._
- #2956 harden `queryKeys.test.ts` auto-discovery (fixes suite-crash risk + glob-depth gap) — follow-up from PR #2952.
- #2955 purge NFC credential material from SecureStore on session change (mobile-rn cross-tenant hygiene, follow-up from #2947).
- #2952 auto-discover feature-local *Keys factories in logout-purge coverage test — resolves `logout-purge-allowlist-fragile` follow-up.
- #2950 purge NFC access log, QR scan history & offline feedback on session change — resolves #2947.
- #2943 `code-review-ppt-web-core-logout-purge-allowlist-fragile` — enforce logout purge allowlist coverage.
- #2942 `code-review-ppt-web-core-api-401-no-request-replay` — single-flight refresh + replay on 401.
- #2941 `code-review-ppt-web-core-authctx-no-boot-expiry-check` — validate JWT `exp` at cold boot.
- #2940 `code-review-ppt-web-ui-confirm-dialog-i18n` — externalize ConfirmationDialog loading label via i18n.
- #2938 add invoice PDF renderer + handler test coverage (closes #2937 follow-up on PR #2558).
- #2932 add e2e 403 test for agent-review self-review guard (closes #2930 follow-up on PR #2919).
- #2931 make `report_summary` snapshot-consistency test fail on pre-fix code (closes #2929 follow-up on PR #2928).
- #2928 snapshot-consistent `report_summary` counts + entries (closes #2924 follow-up on PR #2916).
- #2927 cover update-error i18n path in `EmergencyContactDirectoryPage` tests (closes #2923 follow-up on PR #2913).
- #2925 `code-review-api-handlers-compliance-raw-db-leak-regression` — route compliance audit-log count error through `db_error`.
- #2922 `code-review-reality-server-saved-search-error-string-status` — typed error enum for saved-search HTTP status.
- #2919 `code-review-reality-server-agent-review-self-review` — reject self-review on agent review endpoint.
- #2926 `code-review-api-core-fcm-legacy-dead-endpoint` — remove decommissioned FCM legacy send path.
- #2558 (back-dated merge 2026-09-04) feat(acc) UC-ACC-05.9 invoice PDF render endpoint — resolves `stalled-review-pr-2558`.
- Dependabot merges: #2583 rust_xlsxwriter, #2585 base64, #2586 validator, #2673 ktor group, #2935 npm-minor-patch group.

## Watch
- **Stalled review:** #2559 (UC-ACC-05.8 PAY-by-square QR endpoint) — still open, 47d+ idle, `needs-human-judgement` unchanged. #2555 (UC-ACC-05.17 invoice lifecycle) — still open, 47d+ idle, `needs-human-judgement` unchanged. Both accounting-adjacent; both need product decision, not code review.
- **Reverted:** none this window (no PR title starts with `Revert`).
- **Churn hotspots (window: 19d):** `backend/servers/api-server/src/routes/voice_webhooks.rs` (2999 net, runs_seen 3→4), `backend/servers/api-server/src/routes/integrations/webhook.rs` (2799 net, runs_seen 2→3 — **new repeated-churn signal, backlog row `refactor-repeated-churn-integrations-webhook` added at score 1**), `backend/servers/api-server/src/services/push_fanout.rs` (2564 net, runs_seen 1→2). Runner-up: `backend/servers/api-server/tests/suites/oauth_integration_tests.rs` (3427 net — test churn, not a code-instability signal).
- **Infra blockers still open:** #2966 (api-server utoipa-swagger-ui egress in cloud runner), #2951 (mobile-rn jest suite unrunnable in cloud), #2946 (portfolio_analytics cross-tenant IDOR — the `_org_id` computed-then-discarded case, still open); #2652 (mobile-native AGP egress-blocked) closed as follow-up but the block persists per fire-payload data.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     Skipped `gh pr list --draft` filter this run (GraphQL disabled); scan by field on the open-PR list would need per-PR `pulls/{n}/reviews`. -->
- **not enumerated this run** — GraphQL blocked in the cloud sandbox; no cheap REST path lists approved-drafts in one call. Deferred to the next full run; #2902 remains the only known draft (screen-map-drift) and it has no `APPROVED` review, so it doesn't match this class today.

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Data lives in .research/management/assignments.json on the `planning` branch. This routine run doesn't touch planning; the dispatcher's own hourly cycles surface this class — see the latest dispatcher commit's stats line on `dev`. -->
- **deferred to dispatcher output** — the dispatcher branch is on hourly cadence and the latest commit `5d4560a` reports `0 reviewed, 0 failed, 2 active`. Nothing net-new here; the routine's copy of this section would duplicate the dispatcher's live view.

## Code review slice
- Segment reviewed: **skipped (catch-up run)** — the dispatcher's Tier-1d cycles over the 19d window covered ppt-web-core, ppt-web-ui, reality-web, admin-web, api-core, mobile-native-kmp (evidence in `signals/2026-09-1{8,9}-*.json` + `2026-09-20-*-tier1d.json`), and the resulting `code-review-*` PRs (`#2919`–`#2943`) already landed the findings. Firing a fresh Phase 1.5 review across the same window would duplicate that work.
- Experts: n/a (skipped).
- Findings: 0 new `code-review-finding` signals from this routine run (dispatcher already emitted a full set into the backlog and drove them to PRs).
- Next segment: `reality-web` (`review_cursor.reality-web = 2026-08-29`, oldest along with `ppt-web-core` at 2026-08-29 — dispatcher touched ppt-web-core 2026-09-18/19 but the routine's own cursor is untouched; next real routine run should pick reality-web).
- Skipped: **yes — routine catch-up mode after 19d lag; dispatcher-branch review coverage adopted in place of a fresh pass.**

## Screen-map status
- Total `docs/screens/` files: unchanged (`ppt/` + `reality/`; `mobile/` still not seeded → 0 for mobile).
- Drift signals this run: **not scanned per-PR** (23-PR fetch would need per-PR `files` REST calls; skipped in the catch-up run — deferred to next incremental run against `last_pr_seen=2956`).
- Orphan screens this run: not scanned (same rationale).
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (routine catch-up scope; dispatcher hourly runs cover CI failure surfacing).

## Backlog deltas
- **New:**
  - [1] `refactor-repeated-churn-integrations-webhook` — backend api-server routes/integrations/webhook.rs (2799 net churn this window, runs_seen 2→3). Vector `refactor`, confidence medium, source `churn 2026-09-20`.
- **Bumped:** none.
- **Decayed (open, >14d without new evidence):**
  - `code-review-mobile-native-kmp-portfolio-analytics-caps-100` — 1 → 0 → **dropped** (2026-08-19).
  - `code-review-mobile-native-kmp-cancellation-swallowed` — 1 → 0 → **dropped** (2026-08-25).
  - `code-review-mobile-native-kmp-httpclient-no-timeout` — 1 → 0 → **dropped** (2026-08-30).
  - `code-review-mobile-native-kmp-create-listing-not-wired` — 4 → 3 (2026-09-01; still open, becomes today's promotion candidate).
- **Dropped:** 3 (per decay above — all mobile-native-kmp; segment cursor is stale and the dispatcher's own Tier-1d for mobile-native-kmp is parked on #2652 AGP-egress block, so these findings have been sitting idle rather than lost).
- **Resolved:** 1
  - `stalled-review-pr-2558` — PR #2558 merged 2026-09-04T07:13Z (feat(acc) UC-ACC-05.9 invoice PDF render endpoint). Status → `done`.
- **Bulk (deferred):** the dispatcher's own tier-1d backlog rows already landed as merged PRs #2919–#2943 within the window; those rows were flipped to `done` by the dispatcher itself, not this routine, so no delta here.

## Plans promoted
- `plans/code-review-mobile-native-kmp-create-listing-not-wired.md` — Wire KMP realtor `CreateListingScreen.onSubmit` to a new `PortalListingsRepository.createListing()` calling `POST /api/v1/my/listings` (backend endpoint verified live at `reality-server/src/routes/portal_listings.rs:207`). Mode: local-only (C5 — mobile-native-kmp; cloud AGP blocked). · **adversarial pass: passed** (source→hypothesis→approach→test chain verified; no placeholder rot; all 4 file paths resolve on disk).

**Not promoted despite meeting mechanical gates:** none this run (`code-review-mobile-native-kmp-inquiries-response-contract` already carries a plan; no other `open`+score≥3 items after decay).

**Landability note (fire-payload guidance):** the dispatcher fire-payload flagged buffer-low with all backlog infra-blocked and asked for LANDABLE frontend/docs vectors. This run adds one KMP mobile-native plan — genuinely infra-blocked in cloud (same class as the fire-payload complaint), promoted because it meets every routine gate and is a real bug (`NotImplementedError` on the realtor create-listing flow). Deliberately no cloud-landable frontend plan was fabricated: the dispatcher's own Tier-1d cycles already emitted and merged the readily-landable ppt-web-core / reality-web findings for the window (`code-review-ppt-web-ui-confirm-dialog-i18n` → PR #2940, `code-review-ppt-web-core-authctx-no-boot-expiry-check` → PR #2941, `code-review-ppt-web-core-api-401-no-request-replay` → PR #2942, etc.), and inventing more without fresh evidence would just be padding.

## Auto-fix
- `no candidate` — no signal met `confidence == high` + `score >= 3` + allowlist match this run. The 5 dependabot merges are `dep-update-noise` (score_delta 0; and the allowlist auto-fix only covers the lockfile-drift sub-shape, not raw dep bumps that already merged). No `screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / `stalled-review` candidates surfaced by the (deliberately narrow) catch-up scan.

## Self-review findings
- none (`.research/self-improvement/findings.json` absent).

## Open questions
- **Why did the cloud routine stop for 19d while the dispatcher kept ticking?** Same repo, different branch (`main` for routine, `planning` for dispatcher), different scheduler entry. Recommend: check the routine's cloud cron config, confirm it's still enabled, and re-trigger — or fold the routine into the dispatcher's cadence if the split has stopped earning its cost.
- **Is `code-review-mobile-native-kmp-create-listing-not-wired` landable now?** The routine promotes it because the code shape is unambiguous, but per `#2652`/`#2951` the mobile-native CI can't run in cloud. Suggested action: claim locally on a Mac worktree via `ppt-implement`, not from the dispatcher's cloud queue.

## Quiet day?
- **No.** 23 merged PRs, 1 new backlog row, 3 decay-drops, 1 resolved (stalled-review), 1 plan promoted. Routine lag itself is the top signal of the run. All 17 goal checks pass (`goal_checks[]` in `signals/2026-09-20.json`).
