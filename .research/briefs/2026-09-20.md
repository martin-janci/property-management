# 2026-09-20

_19-day catch-up run. The daily routine cron has been silent since 2026-09-01 — this brief folds in the dispatcher's Tier-1d generator output (2026-09-11 → 2026-09-19) and advances the routine's cursors._

## Since last run
- Merged PRs: 17 (range #2922–#2956) — code-review follow-ups, one dependabot rollup (#2935), two NFC-purge security fixes (#2950, #2955), the JWT+refresh trio on ppt-web (#2941/#2942/#2943), plus various small QA/i18n rounds.
- Open PRs touched: 12 (dependabot #2957/2960/2961/2962/2963/2964/2965; the two stalled feature PRs #2555 and #2559; two draft `needs-human-review` self-PRs #2744 and #2902; dependabot GH-actions #2866).
- New / updated issues: 16 (5 new: #2946 IDOR portfolio_analytics, #2947 NFC purge follow-up, #2949 api-server build-egress, #2951 mobile-rn jest infra, #2966 api-server swagger-ui egress).
- Commits: ~42 on `dev` — 17 real PR merges + ~25 dispatcher chore commits (dispatcher continued to tick throughout).
- Routine lag: **19d 8h** since last routine commit — daily cron on the cloud host has been offline. Dispatcher (separate loop, planning branch) survived the outage; the routine specifically is broken. Fresh `stale_routine_alert` filed today.
- Phases that failed: none this run (deliberate 19-day catch-up).
- Trigger: dispatcher payload `buffer-low: claimable=12/72 — all open backlog infra-blocked (api-server swagger-ui egress #2966, mobile-native AGP #2652, mobile-rn jest #2951); need fresh cloud-landable frontend/docs vectors`. This run's response: adopt 41 tier1d findings into the routine's backlog + promote 2 fresh cloud-landable ppt-web-core plans.

## Shipped
- **17 merged PRs since #2921.** All landed cleanly; the ones tied to open backlog rows resolved in the 2026-09-01 morning/evening fires — this run confirms the tail is drained.
- New resolution recorded here: `stalled-review-pr-2558` → `done` (PR #2558 UC-ACC-05.9 invoice-PDF endpoint merged 2026-09-04 after 30+ days idle).
- Notable safety wave: PRs #2941/#2942/#2943 (JWT cold-boot exp check + single-flight refresh + logout-purge allowlist coverage) — the last of these prompted the follow-up review that surfaces this run's top security plan (`logout-purge-disputes-leases-iot`).

## Watch
- Stalled review: `stalled-review-pr-2555` (UC-ACC-05.17, 39d open, 21d idle) and `stalled-review-pr-2559` (UC-ACC-05.8, 39d open, 21d idle) — both still `needs-human-judgement`; #2558 in the same trio merged, so the pattern is human-attention bottleneck on the ACC UC batch, not the code.
- Reverted: none this window.
- Churn hotspots: not sampled this run — the 19-day window is too wide for the top-3 churn heuristic to be meaningful; the dispatcher's per-cycle Tier-1d cadence covered the code-review side. Fresh hotspot sampling deferred until the routine ticks daily again.

## PRs stuck in draft despite approval
- #2744 `fix(dispatcher): un-wedge oversize-archive push` — draft, `needs-human-review`, last update 2026-08-13 (~38d). Blocked on self-approval limit (author is the same account that ran the pipeline).
- #2902 `screen-map-drift-pr-2894-reality` — draft, `needs-human-review`, last update 2026-09-18 (dispatcher touched it). Branch flagged contaminated by prior auto-rebase; needs human close + re-cut.
- Others: none.

## PRs with verdict=changes, no fix-round progress in 24h
- #2902 — as above; on the planning branch's `assignments.json` (per dispatcher commit-message summary) `verdict=changes` still open. Owner needs to close + re-cut per the dispatcher's guidance.
- otherwise none.

## Code review slice
- Segment reviewed: **adopted-from-dispatcher-tier1d** (no fresh Phase 1.5 spawn this run).
- Adopted signals: 41 tier1d findings from `.research/signals/2026-09-{11,16,17,18,19}-*-tier1d*.json` — coverage across `api-handlers`, `mobile-rn`, `ppt-web-core`, `ppt-web-ui`, `reality-server`, `reality-web`. The dispatcher's demand-driven generator has been producing cloud-landable findings on every quiet cycle (buffer-starvation escape valve); the routine now folds them into `backlog.json` as first-class rows.
- Findings: 41 net-new backlog rows this run.
- Next segment: on next routine tick — oldest `review_cursor` is now `mobile-native-kmp` (2026-09-10 = infra-blocked anyway) and `mobile-rn` (2026-09-09 = jest-blocked #2951). Cloud-landable oldest is `ppt-web-ui` (2026-09-06). `review_cursor` unchanged this run (fresh review not run).
- Skipped: yes — 19-day catch-up + dispatcher tier1d already covered the segments; a fresh review this run would duplicate.

## Screen-map status
- Total `docs/screens/` files: unchanged from 2026-09-01 brief (only ppt/ + reality/ populated; mobile/ still not seeded).
- Drift signals this run: 0 — of the 17 merged PRs, only a few touch route files, and screen-map.yml CI has been green on `main` throughout the window (drift check kicked in-line, no residual drift observed).
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (routine has been offline; assume green per dispatcher's periodic references).

## Backlog deltas
- **New (41):** all `code-review-finding` rows folded in from the dispatcher's Tier-1d generator output (2026-09-11 → 2026-09-19). Vector split: 32 `bug`, 6 `security`, 1 `refactor`, plus 2 mobile-rn hi-conf bug findings. Segments: `ppt-web-core` (10), `reality-web` (10), `ppt-web-ui` (8), `mobile-rn` (5), `api-handlers` (2), `reality-server` (1), plus 4 second-pass and 1 misc. Full evidence per row preserved verbatim; sources point back to the source dispatcher run commit.
- **Bumped:** none this run.
- **Decayed:** 3 open `mobile-native-kmp` items decayed on the 14-day threshold —
  - `code-review-mobile-native-kmp-portfolio-analytics-caps-100` (1→0, `dropped`)
  - `code-review-mobile-native-kmp-cancellation-swallowed` (1→0, `dropped`)
  - `code-review-mobile-native-kmp-httpclient-no-timeout` (1→0, `dropped`)
  - `code-review-mobile-native-kmp-create-listing-not-wired` (4→3, still `open`).
  These were dropped because the dispatcher cannot land KMP work in the cloud runner (AGP egress #2652); no new signals refreshed them.
- **Dropped:** 3 decayed to 0 (see above).
- **Resolved:** 1 — `stalled-review-pr-2558` → done (PR #2558 UC-ACC-05.9 merged 2026-09-04; the earlier resolutions from 2026-09-01 fire already covered the rest of the merged set).

## Plans promoted
- `plans/code-review-ppt-web-core-logout-purge-disputes-leases-iot.md` — append 6 missing authed roots (`disputes`, `leases`, `violations`, `iot`, `my-units`, `templates`) to `AUTHED_QUERY_KEY_ROOTS` + auto-discovery cross-check + logout-purge regression test. Vector `security`, score 3, high-confidence. Third recurrence of the same defect class (prior fixes: `logout-cache-purge-gap` done, `logout-purge-notif-triggers` done PR #2650). **adversarial pass: passed** (chain source → hypothesis → 6 concrete roots → approach → 2 IG3 tests holds; placeholder-rot scan clean).
- `plans/code-review-ppt-web-core-ocr-fetch-no-auth.md` — route `useOcrMeterReading.ts` bare-`fetch()` through `getApiClient()` (or attach `Authorization` + `X-Tenant-ID` manually mirroring `paymentMatching.uploadStatement`). Vector `bug`, score 3, medium-confidence. Breaks Epic-128 end-to-end against a real backend today. **adversarial pass: passed** (chain holds; IG3 hook test asserts headers present, fails on main; placeholder-rot scan clean).
- **Structural takeaway:** both promotions are `Mode: cloud-ok`, single-file frontend edits under `frontend/apps/ppt-web/src/` — exactly what the buffer-low payload asked for. Two-plan cap respected.

## Auto-fix
- `no candidate` — none of this run's signals match the allowlist (screen-map-drift, screen-map-orphan, fixme-in-merged-code, lockfile-bump, stalled-review). The dispatcher's Tier-1d findings are `code-review-finding` signals which the routine spec explicitly excludes from Phase 5 auto-fix (they always route through the manual-implementer plan path). No auto-fix history entry appended.

## Self-review findings
- none (`.research/self-improvement/findings.json` absent).

## Open questions
- **Restart the routine cron.** The 19-day silence surfaces as `stale_routine_alert` + `lag_warning`; the operational fix is separate from anything the routine can auto-remediate. Check the cloud host's cron / scheduler and confirm the routine trigger is queued for tomorrow's tick.
- **`code-review-ppt-web-core-community-route-fully-stubbed` (score 3, high, `bug`, `open`) sits above the promotion bar but has no `files` field populated** — the tier1d signal listed the paths inside evidence prose but did not fill `.files` (dispatcher signal-format quirk on that one row). Not promotable until a routine tick or an operator adds `frontend/apps/ppt-web/src/routes/groups/community.tsx` to the row. Scope is genuinely large (6 stubbed routes, entire Epic-42 community feature), so it likely deserves an epic-level plan rather than a single-shot fix — flagging for human triage.

## Quiet day?
- **No.** 17 merged PRs, 41 new backlog rows, 3 items decayed & dropped, 1 stalled-review resolved, 2 fresh cloud-landable plans promoted, plus a `stale_routine_alert` on the routine itself. The routine's silence broke the daily loudness cadence but the underlying work rate was very high.

## Goal violations
- none — G1-G17 all PASS (see `signals/2026-09-20.json.goal_checks`).
