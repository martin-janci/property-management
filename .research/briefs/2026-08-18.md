# 2026-08-18

## Since last run
- Merged PRs: 17 (range #2773–#2790; contiguous #2773–#2788 plus #2790, all authored by martin-janci)
- Open PRs touched: 1 new (#2791 draft — auto-impl of `code-review-api-handlers-voice-refresh-simulated-token-release`, opened by dispatcher fast-path 02:54Z)
- New / updated issues: 1 (#2789 opened 14:46Z as post-merge follow-up on PR #2784's proxy-header IP, closed 22:26Z by PR #2790)
- Commits: 24 on `dev` (17 PR merges + 7 dispatcher chores/reconciles/tier1d generators)
- Routine lag: 0d 25h since last run — on schedule (under 36h threshold, no `lag_warning` emitted)
- Phases that failed: none
- Trigger payload: `buffer-low: claimable=8/72 — refill planner` — informational (routine has no buffer-refill mode; see Open questions)

## Shipped
- #2773 code-review-api-handlers-share-pw-no-throttle — rate-limit public protected-share password verification (token:ip sliding window, 10/15min, +4 tests)
- #2774 code-review-api-core-payment-matcher-multi-suggest — surface only best-scoring PaymentMatch tier (ties kept) instead of every VS-above-threshold candidate
- #2775 code-review-mobile-rn-nfc-accesslog-parse-unguarded — guard NFCCredentialManager `getAccessLog` JSON.parse against corrupted blob
- #2776 code-review-ppt-web-ui-moderation-fabricated-context — drop hardcoded `report_source='user'` + `previous_violations=0` + `age_hours=0` fallbacks the moderation API never returns
- #2777 code-review-reality-web-realtor-mgmt-i18n — route RealtorManagement (agency namespace) through next-intl across 6 locales
- #2778 code-review-ppt-web-ui-createfault-mock-ai — stop shipping fabricated AI triage; gate mock behind `import.meta.env.DEV + VITE_ENABLE_MOCK_AI_TRIAGE`, add honest "unavailable" state
- #2779 code-review-ppt-web-core-errorhandler-i18n-gap — route `errorHandler.ts` client error catalogue through react-i18next (19 keys × 6 locales) + regression tests
- #2780 code-review-mobile-rn-nfc-revoke-local-persists-offline — clear NFC credential SecureStore cache before the server-revoke call so offline revocation isn't defeated
- #2781 code-review-reality-web-agency-errorstate-i18n — route `NoAgencyMessage` through `useTranslations('agency')` across 6 locales
- #2782 code-review-api-core-payment-matcher-vs-only-threshold — honour counterparty-IBAN tie-breaker (+0.05 corroboration, normalised) documented in migration 00183
- #2783 code-review-ppt-web-ui-moderation-no-i18n — internationalize content moderation dashboard + ModerationCaseCard + ModerationQueueStats (new `moderation` ns, 6 locales)
- #2784 code-review-api-handlers-share-log-proxy-ip — unwind `CF-Connecting-IP` / left-most `X-Forwarded-For` for share access-log client IP (initial pass — superseded by #2790's trusted-proxy gate)
- #2785 code-review-reality-web-comparison-view-i18n — route ComparisonView (labels, actions, toasts, rows, transactionType, currency) through next-intl + `useLocale()` for currency formatting; add stable `id` per row so highlight logic is translation-independent
- #2786 code-review-mobile-rn-nfc-securestore-2kb-blob — chunk NFC credentials across SecureStore slots (~2KB cap) with manifest at existing key + legacy-format read fallback
- #2787 repeated-churn voice_webhooks.rs — replace `.expect("build Google JWKS HTTP client")` with fallback to `reqwest::Client::new()` (matches alexa_cert_http_client rationale) so a TLS-backend failure can't panic a handler
- #2788 code-review-api-core-voice-actions-fabricated-empty-retry1 — voice `check-announcements` / `check-meter` now query `count_unread_rls` / new `get_pending_readings_for_unit_rls` instead of returning fabricated empty responses; replaces "fails-honestly" stopgap
- #2790 gh-issue-2789 — gate proxy-header client IP on a trusted-proxy allowlist (new `client_ip.rs`; applied to shares + forms audit path; CF-Connecting-IP + rightmost-untrusted-XFF; IPv4-mapped-IPv6 normalised; TrustedProxies boot-loaded into AppState; +13 unit tests incl. IG3)

## Watch
- Stalled review: #2555 (feat(acc) PAY by square QR, open 21d), #2558 (feat(acc) invoice PDF, 21d), #2559 (feat(acc) invoice lifecycle, 21d) — already in backlog at `needs-human-judgement`, no new activity
- Dependabot backlog: 12 open dep PRs (rust: #2583/#2584/#2585/#2586, gradle: #2673/#2749, npm: #2588/#2589/#2590/#2591/#2751, rust: #2750) — all already in `seen_signals`, none re-scored
- Reverted: none
- Churn hotspots: `backend/servers/api-server/src/routes/voice_webhooks.rs` (touched by #2787 + carries all 3 tier1d findings on lines 436-1585, runs_seen bumped 2 → 3)

## PRs stuck in draft despite approval
- #2744 fix(dispatcher) un-wedge oversize-archive push — last update 4d ago (age=91h); labeled `needs-human-review` (self-PR, dispatcher-authored). Assignment marks reviewer verdict=approve + CI-green; blocked on operator merge (self-PR approval limitation).
- #2791 code-review-api-handlers-voice-refresh-simulated-token-release — draft opened by dispatcher fast-path 02:54Z; too new to be stuck.

## PRs with verdict=changes, no fix-round progress in 24h
- none (both live assignments `gh-issue-2743` and `code-review-api-handlers-voice-refresh-simulated-token-release` are verdict=approve, awaiting merge)

## Code review slice
- Segment reviewed: `api-handlers` (reason: churn-aligned — voice_webhooks.rs runs_seen=3; consumed dispatcher-generated tier1d artifact `signals/2026-08-18-api-handlers-tier1d.json`, produced 22:31Z overnight)
- Experts: rust + security
- Findings: 3 (voice-refresh-simulated-token-release, voice-oauth-exchange-no-rate-limit, voice-exchange-device-dedup) — all pre-applied to `backlog.json` by dispatcher pre-Phase-2; recorded in `signals/2026-08-18.json` with `score_delta=0` and added to `state.seen_signals` this run to prevent double-count
- Next segment: `reality-web` (review_cursor=2026-08-12, oldest unreviewed after this run advances api-handlers to 2026-08-18)
- Skipped: no — reviewed via consumed dispatcher artifact

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/`=106, `reality/`=47, `mobile/`=0 — mobile not yet seeded)
- Drift signals this run: 0 (5 frontend PRs touched components under `features/` or `components/` — #2776 `features/compliance/`, #2778 `features/faults/`, #2777/#2781 `components/agency/`, #2785 `components/comparison/` — none under `App.tsx` / `routes/**` / `src/app/**` trigger set)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: unknown (session cannot query Actions runs)

## Backlog deltas
- **New:** 3 tier1d signals folded (already in backlog.json via dispatcher pre-Phase-2; IDs added to `state.seen_signals` this run):
  - `code-review-api-handlers-voice-refresh-simulated-token-release` (score=2, security, medium)
  - `code-review-api-handlers-voice-oauth-exchange-no-rate-limit` (score=1, security, medium)
  - `code-review-api-handlers-voice-exchange-device-dedup` (score=1, bug, medium)
- **Bumped:** none
- **Decayed:** `code-review-mobile-native-kmp-portfolio-analytics-caps-100` — score 2 → 1 (updated 2026-08-04 = 14 days ago, no new signal)
- **Dropped:** none
- **Resolved (done) this run:** 0 flipped — dispatcher already flipped 15 backlog items across the 2026-08-17 PR wave (PRs #2773-#2788); no PR body/id match against the remaining 8 open items this run

## Plans promoted
- none — 0 open items pass all readiness gates this run:
  - `code-review-mobile-native-kmp-inquiries-response-contract` (score=3, high, bug) — already has `plans/code-review-mobile-native-kmp-inquiries-response-contract.md`
  - `code-review-api-handlers-voice-refresh-simulated-token-release` (score=2, security, medium) — misses security fast-track (needs `confidence=high`) AND stem-collides with active `code-review-api-handlers-voice-refresh-simulated-token-release` assignment in `assignments.json` (PR #2791 in review)
  - `code-review-mobile-native-kmp-httpclient-no-timeout` (score=2, high, bug) — high-confidence but wrong vector for fast-track (bug ≠ security), so still needs score ≥ 3
  - remaining 5 opens are score ≤ 2 without the security fast-track path (medium confidence and/or bug/test-gap vector)

## Auto-fix
- `no candidate` — no signal met the certainty bar (confidence=high + score ≥ 3 + allowlist type) this run. Kill switch `ROUTINE_AUTOFIX_DISABLED` unset.

## Self-review findings
- open=34 (high=7, medium=24, low=3); new this run=0; acknowledged=19
- fp-mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=15] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked and derives PUSH_METH from it; fix: replace hard-coded proxy-403 assumption with a runtime probe at Phase 1
- fp-mcp-push-cannot-inline-large-archive [recurrence=15] — Phase 6 archive-move produced a 198KB assignments-archive.json; `mcp__github__push_files` can't inline it; fix: push dispatcher state commit to a NEW branch (new-branch push not proxy-blocked) then open PR
- fp-tier1d-signals-accumulate-no-promotion-to-backlog [recurrence=8] — buffer drained (claimable=8/72 per today's payload; open=8 all mobile-native-KMP-gated or in-flight) while tier1d signals accumulate; fix: add an in-repo signals→backlog promoter that Phase 2.6 Tier-1b invokes when honest_claimable < floor
- fp-stale-in-progress-action-list-decoupled-from-assignments [recurrence=5] — action-list.json carries in-progress rows without a live assignments.json row; fix: Phase-2 reconcile extension in gc1-reconcile.sh
- fp-implementer-sandbox-zero-pushed-branch-multigen [recurrence=3] — reclaim rows produce ZERO branches on origin across two implementer generations; fix: implementer-liveness escalation on reclaim

## Open questions
- **Buffer-refill (payload said claimable=8/72).** Every remaining open item is either (a) mobile-native/KMP — structurally unbuildable in cloud sandbox per issue #2652, (b) in-flight via active PR (#2791), or (c) score < 3 without a security fast-track qualifier. The routine's Phase-3 promoter cannot manufacture readiness — the buffer starvation is a downstream symptom of the KMP cloud-build gate. Two off-routine unblocks: (i) hand-author plans for the mobile-native items so they can be tackled locally, or (ii) merge the 12 dependabot PRs so their `dep-update-noise` rows churn out of backlog. Neither is a routine action.
- The 3 stale `feat(acc)` PRs (#2555/#2558/#2559, open 21d) are marked `needs-human-judgement` in backlog but haven't moved — needs an explicit accept/reject decision.

## Quiet day?
- No — 17 merged PRs (largest resolution wave since epic-cadence weeks), 3 tier1d findings folded, 1 decay applied, next code-review segment advanced.
