# 2026-07-10

## Since last run
- Merged PRs: 9 (range #2186–#2217)
- Open PRs touched: 17 (11 dispatcher-authored drafts + 6 dependabot)
- New / updated issues: 12 (2 closed: #2202, #2200 — both resolved by matching PRs this run)
- Commits: 11 real code + ~32 research/dispatcher on `dev`
- Routine lag: 1d 17h since last run (41h, above the 36h threshold — `lag_warning` emitted)
- Phases that failed: none

## Shipped
- #2217 gh-issue-2202 — make `/settings/integrations` discoverable (App.tsx nav-link + command-palette), i18n every hardcoded string under `settings.integrations.*`, and stop sending a frontend-origin `redirect_uri` for Airbnb OAuth (backend-owned callback). Follow-up #2224 already filed: en.json only, five locales still fall back to English.
- #2216 gh-issue-2200 — ProtectedRoute `/login` return-URL guard: the `does not store the login page as a return URL` test previously mounted the sibling `/login` route and never exercised the guard; new `renderProtectedAtLogin()` + `/login?next=` companion make both halves of the guard genuinely covered.
- #2194 gap-83-2 — wire Booking.com property-mapping wizard into `/settings/integrations` (hotel ID + Supply-XML creds → `useConnectBooking`, sync/disconnect once connected, cross-platform conflict display). Booking.com api-client calls now route through `authenticatedFetchJson` matching Airbnb.
- #2193 gap-screens-uc-29 — new `docs/screens/ppt/rentals-dashboard.md` covers Short-Term Rental Management (UC-29 + UC-30), the missing primary surface previously misattributed to `settings-integrations`.
- #2188 / #2191 / #2192 gap-screens — link UC-10 → person-months, UC-40 → financial-budgets, UC-33 → lease-application(-detail), restoring screen-map ↔ use-case referential integrity.
- #2187 booking OAuth token-exchange coverage — +5 handler branches (missing/malformed X-Tenant-ID, empty-code short-circuit ordering, malformed JSON, GET-on-POST-only).
- #2186 booking/airbnb OAuth CSRF coverage — +5 handler-level tests (manager-role gate, callback auth, non-UUID state prefix, extra-segment state, membership-only gate).

## Watch
- Stalled review: none — every open PR is <7 days old (drafts + dependabot).
- Reverted: none.
- Churn hotspots:
  - `frontend/apps/ppt-web/src/features/settings/integrations/IntegrationsPage.tsx` — 709 lines add+del across PR#2184/#2194/#2217 in 48h (runs_seen=1, fresh). Watch for feature-scoped split (AirbnbCard / BookingCard / PropertyMappingWizard).
  - `backend/servers/api-server/tests/booking_oauth_routes_tests.rs` — 217 lines this window (runs_seen 1 → 2, `repeated-churn` emitted). PR#1393 create, PR#2187 strengthen.
  - `backend/servers/api-server/tests/booking_oauth_csrf_tests.rs` — 159 lines this window (runs_seen 1 → 2, `repeated-churn` emitted). PR#1393/#1424 create, PR#2186 strengthen.

## PRs stuck in draft despite approval
- #2223 gh-issue-2222 (shard CI backend `test` job to end `-p common --lib` timeout wedge) — reviewer approved (per dispatcher fast-path log, dfaa8e5), still `isDraft=true` at 18:21 UTC (~2h). Merge-gate check: unblocking the CI wedge unblocks the other 10 dispatcher drafts.
- All other drafts are dispatcher-owned iterations that have not yet reached `reviewDecision=APPROVED`.

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher rows currently at status=review or in-progress, all touched <24h ago per commit trail).

## Code review slice
- Segment reviewed: `ppt-web-ui` (reason: churn-aligned — IntegrationsPage.tsx was the top-1 hotspot; segment last reviewed 2026-06-11, ~29d ago)
- Experts: frontend
- Findings: 3 (all `code-review-finding` — see Backlog deltas)
- Next segment: `mobile-native-kmp` (oldest unreviewed, 2026-06-06 = ~34d)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 165 (per PR#2193 `screen-map cli validate` — 163 ppt + reality baseline plus 2 added this run; `mobile/` still not seeded → 0)
- Drift signals this run: 1 (`screen-map-drift-pr-2217-ppt` — App.tsx nav-link + i18n without a `docs/screens/ppt/*` update; heuristic caveat: PR added no new route, human should confirm before promoting)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (cloud routine skips per-workflow gh polling — check locally via `gh run list --workflow=screen-map.yml --branch=main --limit=1` if needed)

## Backlog deltas
- **New:** [2] `test-gap-screen-map-drift-pr-2217-ppt` — App.tsx nav-link + i18n without matching screen-map update (heuristic).
- **New:** [2] `code-review-ppt-web-ui-payment-mutation-errors` — PaymentMatchingPage confirm/reject mutations have no isError UI.
- **New:** [1] `refactor-churn-hotspot-ppt-web-integrations-page` — IntegrationsPage.tsx 709 LOC churn across 3 PRs in 48h.
- **New:** [1] `test-gap-repeated-churn-booking-oauth-routes-tests` — runs_seen 1 → 2 (PR#1393 → PR#2187).
- **New:** [1] `test-gap-repeated-churn-booking-oauth-csrf-tests` — runs_seen 1 → 2 (PR#1393/#1424 → PR#2186).
- **New:** [1] `code-review-ppt-web-ui-payment-page-no-i18n` — entire PaymentMatchingPage.tsx surface hardcoded English.
- **New:** [1] `code-review-ppt-web-ui-cmd-palette-en-keywords` — command palette keywords English-only; sk/cs/de search misses.
- **Bumped:** none (Phase 1.5 signals landed as fresh items, not re-scoring existing rows).
- **Decayed:** none (all open items were updated within the 14d window).
- **Dropped:** none this run.

## Plans promoted
- none — no open item hit the readiness bar this run (highest new score = 2; security fast-track doesn't apply because none of the score-2 items are `vector: security`).

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist match this run (the only score-2 signals — payment mutation errors + screen-map drift — are not in the auto-fix allowlist; drift is on a doc-stub shape but only the `screen-map-drift` allowlist entry could match and its safe-shape requires a new route file, not a nav-link change).

## Self-review findings
- open=8 (high=2, medium=4, low=2); new this run=0; acknowledged=19
- fp-<unlabeled> [recurrence=5] — Phase 6 archive-move produced a 198KB assignments-archive.json that mcp__github__push_files could not inline; fix: adopt PR+API-merge path as primary push mechanism (proven via PR#1163).
- fp-<unlabeled> [recurrence=3] — Phase 2.6 buffer reports claimable=54 while REAL claimable=0 (all 54 open action-list items terminal in archive); fix: extend `gc1-reconcile.sh` to auto-close open items whose task_id is terminal in active+archive (including status=failed), and fix the buffer-guard formula.

## Open questions
- Confirm whether `screen-map-drift-pr-2217-ppt` is a real drift (nav-link + i18n added to an existing routed page — no new route). If treated as false-positive, mark the row `dropped` with `evidence: heuristic false-positive`.
- PR#2223 approved but still draft ~2h — is a human gate holding it back, or is the dispatcher waiting on shards to finish before marking `ready-for-review`?

## Quiet day?
- No — 9 PRs shipped, 7 new backlog vectors, 3 code-review findings, 2 test-gap hotspot re-emits, +1 fresh churn hotspot.
