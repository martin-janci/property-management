# 2026-07-15

## Since last run
- Merged PRs: 31 (range #2273–#2316)
- Open PRs touched: 1 (#2332 fix(ai-chat) — draft, opened 2026-07-15 01:48Z)
- New / updated issues: 15 (#2317–#2331, all labeled `follow-up`/`from-merged-review`)
- Commits: 100 on `dev` (heavy dispatcher/state churn + 31 feature PRs)
- Routine lag: 2d 1h since last run (2026-07-13 02:11 UTC → 2026-07-15 03:08 UTC) — `lag_warning` fired (>36h threshold)
- Phases that failed: none. Phase 1.5 (rotating expert review) and Phase 1.6 (PM role rotation) skipped by policy — see *Code review slice* and *Open questions*.

## Shipped
- #2273 test-gap(backend) gh-issue-2272 — extract RAG provenance model_filter assertions into own `#[sqlx::test]`
- #2274 refactor-churn-hotspots-api-server-auth-2026-07-12 — extract `authenticated_user_id()` helper in api-server
- #2278 gh-issue-2275 — portal inquiry webhook returned 500 on lead persistence failure; now fails safe
- #2280 gh-issue-2277 — reality-web AgencyDashboard showed misleading "No Agency Found" on any API error
- #2281 gh-issue-2276 — reality-web listing detail crashed SSR (500) on partial 200 body
- #2285 gap-84-3-reality-favorites-screen-map-not-shipped — ship reality/favorites screen-map baseline
- #2286 gap-83-3-replay-protection — add freshness/timestamp window to portal webhook (follow-up #2330 tracks per-portal receiver widening)
- #2287 gap-84-3 — price-alert subscription surface in reality-web (follow-up #2326 Decimal wire-fmt bug)
- #2288 gap-84-3 — mobile-native price-tracking surface for favorites (follow-up #2331 KMP Decimal decode)
- #2289 gh-issue-2279 — scope AI chat by-session-id handlers to owning user (within-tenant IDOR); follow-up #2317 tracks the adjacent gaps → PR #2332 (draft)
- #2290 gap-84-4 — route pipeline push through FCM+APNs combined adapter (real iOS transport); hardening in #2310
- #2291 gap-84-5-migrate-embeddings-to-pgvector-not-wired — wire migrate_embeddings_to_pgvector to HTTP route (follow-up #2321 RLS + provenance gaps)
- #2292 gap-81-1 — wire Create-new-schedule flow to backend (follow-up #2324: UI hits nonexistent list endpoints — flow still broken in prod)
- #2293 gap-84-4 — notification-trigger management UI (ppt-web) (follow-up #2325 hooks/nav/i18n)
- #2294 gap-83-3 — portal-webhook status manager surface (ppt-web) (follow-up #2322 role gate/pagination/screen-map/OpenAPI)
- #2295 gap-81-1 — recompute report schedule `next_run_at` on cron edit
- #2296 chore(backend) — bump yanked spin 0.9.8/0.10.0 → 0.9.9/0.10.1 (unblock cargo-deny)
- #2297 gap-screens-normalize — normalize story-id-style epic refs in screen-map frontmatter
- #2298 gap-tracking-reconcile — reconcile shipped-story tracking drift (epics 79/82/84/85/9)
- #2304 gh-issue-2282 — mobile dashboard surfaces load errors instead of silent zeros (follow-up #2323 extract shared error banner)
- #2305 gh-issue-2283 — test mobile `useApi` JWT decode + X-Tenant-ID injection (follow-up #2329 consolidate JWT tenant-decode)
- #2306 gap-84-2 — add screen-map for signer-facing e-sign page
- #2307 gh-issue-2299 — tick Tasks/Subtasks in 15 reconciled done story-md files
- #2308 gh-issue-2284 — test mobile `DocumentUploadScreen` upload/validate/icon paths (follow-up #2327 unauth-upload + multipart obsolescence)
- #2309 gap-84-1 — add presigned PUT endpoint for direct client-to-S3 upload (follow-up #2320 file_key IDOR + size-cap enforcement)
- #2310 gh-issue-2301 — harden gap-84-4 push wiring (honest metrics, single token fetch, routing guard) (follow-up #2319 warn-vs-info + docs)
- #2311 gh-issue-2302 — capture letter-suffixed epics (epic-10B etc) in screen-map drift extractor (follow-up #2328: schema-invalid mixed-case candidate ids)
- #2312 gh-issue-2300 — pgvector back-fill stamps provenance + set-based UPDATE
- #2313 gh-issue-2303 — wire `report_schedules` due-work consumer + unify cadence model (follow-up #2318 RLS no-op in prod)
- #2315 gap-83-2 — exercise Booking.com `fetch_property` outbound happy path
- #2316 gh-issue-2314 — scope `news_articles` handlers to caller org (cross-tenant IDOR + data-loss fix)

## Watch
- Stalled review: none (#2332 is 1h old, draft — well below the 7-day threshold)
- Reverted: none this window
- Churn hotspots (this run, source-code only, exclusions applied):
  - `backend/servers/api-server/src/routes/reports.rs` — 3269 lines (runs_seen 1→2)
  - `backend/crates/integrations/src/booking/mod.rs` — 3185 lines (runs_seen 1→2)
  - `backend/servers/api-server/src/routes/auth.rs` — 2950 lines (runs_seen 3→4, repeated-churn re-emit)

## PRs stuck in draft despite approval
- none (PR #2332 is draft but has no `APPROVED` reviewDecision — human-gated by design)

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher assignments cursor on `planning` branch is not checked from this session; nothing surfaced in the daily digest)

## Code review slice
- Segment reviewed: **skipped this run**
- Experts: n/a
- Findings: 0 emitted by Phase 1.5 (rotating expert review deferred — see below)
- Next segment: `ppt-web-core` (oldest unreviewed at 2026-07-07) followed by `mobile-native-kmp` (2026-07-12)
- Skipped: yes — post-merge review pass on 2026-07-15 already opened **15 follow-up issues** (#2317–#2331) covering the exact segments this rotation would sweep (mobile-native-kmp, ppt-web-core, api-handlers, mobile-rn, reality-web). Running Phase 1.5 on top would duplicate signals with lower confidence. `review_cursor` held.

## Screen-map status
- Total `docs/screens/` files: 167 (ppt + reality + reality-mobile). `mobile/` still empty → drift detector excludes `frontend/apps/mobile/src/**` (per routine-prompt scope carve-out).
- Drift signals this run: 0 (PRs #2280/#2281/#2287/#2293/#2294 all touched a `docs/screens/<product>/` file in the same commit — no drift heuristic tripped; #2285 and #2306 explicitly seeded new screen-map entries; #2297 normalized frontmatter across the map).
- Orphan screens this run: 0 (no candidate slug lost its matching route in the touched trigger set).
- Last `screen-map.yml` CI run on `main`: not checked from session — the workflow is triggered per-PR against `dev`, and the daily routine already relies on per-PR CI to gate any drift-inducing merge.

## Backlog deltas
- **New:** [1] `refactor-churn-hotspots-2026-07-15` — churn cluster (booking/mod.rs, routes/reports.rs, routes/auth.rs); score below promotion threshold (3), stays in backlog for decay.
- **Bumped:** none
- **Decayed:** none (no `open` items were older than 14 days at start of run — the backlog is currently 100% terminal apart from the row created this run)
- **Dropped:** none

## Plans promoted
- none — the only new backlog row (score=1) is below the promotion threshold. Nothing else on the backlog is `open` (all 186 prior rows are `done`/`dropped`/`closed`).

## Auto-fix
- `no candidate` — no signal this run matched the certainty bar (`confidence=high` AND `score≥3` AND allowlisted action shape). All 5 signals this run are lag-warning or cumulative churn; none fits `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, or `stalled-review`.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0
- fp-<null> [recurrence=7] — Phase 6 archive-move can produce a >64KB assignments-archive.json that `mcp__github__push_files` cannot inline (and git push is proxy-403'd); fix: adopt the proven PR+API-merge workaround (create branch → open PR → merge via `mcp__github__merge_pull_request`) as Phase 6's primary push path for large archives.
- fp-<null> [recurrence=3] — Phase 2.6 buffer / GC3 claimable formula subtract only active assignment ids; archive-terminal open action-list rows are counted as claimable, inflating the buffer while the queue is actually drained; fix: (1) auto-close `open` action-list rows whose task_id is terminal in active+archive for any status incl. `failed`; (2) subtract archive task_ids in both the Phase 2.6 buffer-guard and the T15 self-test.

## Open questions
- Should Phase 1.5 (rotating expert review) also skip when a same-day post-merge review pass has already covered the segment, to avoid duplicate `code-review-finding` signals? The routine as written does not gate on this — today's skip is an operator judgement call. A rule of thumb worth codifying: "skip Phase 1.5 for a segment already reviewed via post-merge on the same UTC day."
- The two long-recurring high-severity self-review findings (dispatcher-archive-corruption at recurrence 7, buffer-guard formula at recurrence 3) remain unlanded despite the fix path being proven — worth converting to explicit backlog vectors so they surface in the implementer's queue rather than only in the self-improvement journal.

## Quiet day?
- No. 31 PRs merged in-window, 15 new issues, 1 new backlog vector added.
