# 2026-07-15

## Since last run
- Merged PRs: 44 (range #2273–#2353; heavy dispatcher-driven day)
- Open PRs touched: 7 (all opened today, 5 in draft; oldest 8h — none stalled)
- New / updated issues: 32 (mostly `follow-up`/`from-merged-review` closures from the post-merge review pipeline; #2354 opened as the newest webhook-hardening follow-up)
- Commits: 51 on `dev` — mix of feature/fix PRs (#2273–#2353) and dispatcher/routine plumbing (`chore(research):`)
- Routine lag: 2d 14h since last run (last: 2026-07-13T02:11:42Z) — `lag_warning` fires (>36h threshold, well under 3d stale-alert)
- Phases that failed: none (Phase 1.5 dev-review and Phase 1.6 project-management deliberately skipped this run — see *Code review slice* and *Auto-fix*)

## Shipped
- #2353 — retry (5xx) portal inquiry webhook on unmatched syndication instead of silent 200 drop
- #2351 — reality-web listing detail hardened (type-guard features/photos, real 404 for missing listings)
- #2349 — quiet benign push skips + drop dead `PushTransport` param (follow-up to #2310)
- #2348 — fix price-alert Decimal-string direction + complete AC-5 watch toggle
- #2346 — harden mobile document upload (fail-fast auth, size/MIME guard, JWT dedup)
- #2345 — wire ppt-web direct-to-S3 document upload (gap-84-1)
- #2344 — lower-case synthesised epic candidate ids + tighten suffix regex
- #2338 — wire create-schedule UI to real endpoints (reports)
- #2337 — move notification-trigger hooks to `@ppt/api-client`, wire nav + `ProtectedRoute`, i18n
- #2336 — harden portal-webhooks screen (role gate, pagination, i18n, screen-map)
- #2335 — cross-org rag/migrate RLS + assumed-provenance & CI pgvector gaps
- #2334 — extract `QueryErrorBanner` + sweep raw error-message leaks on mobile
- #2333 — reconcile stale statuses (gap-tracking)
- #2316 — cross-tenant IDOR + data-loss fix in `news_articles` handlers (`security`, needs-human-review label)
- #2312 — pgvector back-fill stamps provenance + set-based UPDATE
- #2311 — capture letter-suffixed epics in screen-map drift extractor
- #2308 — test mobile `DocumentUploadScreen` upload/validate/icon paths
- #2305 — test mobile `useApi` JWT decode + `X-Tenant-ID` injection
- #2304 — mobile dashboard surfaces load errors instead of silent zeros
- #2294 — portal webhook status manager surface (ppt-web)
- #2293 — notification-trigger management UI (ppt-web, gap-84-4)
- #2292 — Create-new-schedule flow → backend (gap-81-1)
- #2288 — mobile-native price-tracking surface (gap-84-3)
- #2287 — price-alert subscription surface in reality-web (gap-84-3)
- #2286 — freshness/timestamp window to portal webhook (gap-83-3 replay protection)
- #2281 — reality-web listing detail SSR crash on partial 200 body (gh-issue-2276)
- #2280 — reality-web AgencyDashboard misleading "No Agency Found" (gh-issue-2277)
- #2278 — portal inquiry webhook 500 on lead persistence failure (gh-issue-2275)
- #2274 — refactor(api-server): extract `authenticated_user_id()` helper
- #2273 — test-gap(backend): RAG provenance `model_filter` assertions (gh-issue-2272)
- (14 more feature/fix PRs merged this window; see `git log 1f924a0..389d4b2 --oneline`.)

## Watch
- Stalled review: **none** — every open PR was updated within the last 8h
- Reverted: **0** (no `Revert` titles in the window)
- Churn hotspots this run (application code):
  - `backend/servers/api-server/src/routes/reports.rs` — 3355 line-changes (runs_seen 1→2 after this run)
  - `backend/crates/integrations/src/booking/mod.rs` — 3185 line-changes (runs_seen 1→2)
  - `backend/servers/api-server/src/routes/auth.rs` — 2950 line-changes (runs_seen 3→4; `repeated-churn` still firing)
  - **Caveat:** most of the numbers above are dominated by the chore commit `a7ba591` (a dispatcher fast-path capture that also carried a mass rename touching many `*.rs` files). The persistent auth.rs signal is still real (multi-run cluster); the reports/booking spikes are largely one-shot noise from that same commit. Not scoring these into a new plan — the cumulative signal type stacks evidence, not score.
- Documentation-heavy churn: `docs/functional-requirements.md` (7116), `_bmad-output/epics.md` (5514), `docs/superpowers/plans/2026-05-06-deploy-server-phase-0-1.md` (4204), `frontend/apps/ppt-web/messages/en.json` (3897), `frontend/packages/sitemap/src/json/sitemap.json` (3727). All excluded from hotspot scoring (docs / i18n / sitemap).

## PRs stuck in draft despite approval
- none — the 5 draft PRs opened today (#2347, #2350, #2352, #2354, #2332) are all fresh (<24h), none `APPROVED`.

## PRs with verdict=changes, no fix-round progress in 24h
- none — the assignments.json snapshot has 9 active rows, all created today via the dispatcher fast-path.

## Code review slice
- Segment reviewed: **skipped by the routine**
- Reason: dispatcher already ran multiple Tier-1d `dev-review` passes in this window (api-core, mobile-native-kmp, mobile-rn — see `chore(research): dispatcher … dev-review signals` commits on 07-14 and 07-15). Every segment except `ppt-web-core` (last: 2026-07-07) was reviewed within the last 48h; running the same skill from this routine on top of the dispatcher's coverage would duplicate work.
- Experts: n/a (skipped)
- Findings: 0 (routine skipped)
- Next segment: `ppt-web-core` — oldest unreviewed (2026-07-07); pick this up on the next incremental run once the buffer has drained.
- Skipped: yes — dispatcher covered this in the intervening 62h.

## Screen-map status
- Total `docs/screens/` files: ~165 (ppt/ + reality/; mobile/ still not seeded → 0). Not enumerated fresh this run — the a7ba591 chore commit reshuffled screen doc paths, best inspected via `/screens validate` locally.
- Drift signals this run: 1 (`screen-map-drift-pr-2351-reality` — reality-web listing/[slug]/page.tsx touched with no matching `docs/screens/reality/` update in-PR; likely already covered by an existing doc)
- Orphan screens this run: 0 (heuristic not run — light-touch this run given the chore-rename churn would produce false positives)
- Last `screen-map.yml` CI run on `main`: not queried this run

## Backlog deltas
- **New:** [2] screen-map-drift-pr-2351-reality — reality-web listing detail route touched without doc update (heuristic — verify before promoting)
- **Bumped:** none
- **Decayed:** none (backlog had **zero** open items entering this run, so nothing to decay)
- **Dropped:** none

## Plans promoted
- none — the sole new backlog item (`screen-map-drift-pr-2351-reality`, score=2) is below the score≥3 promotion threshold and is a heuristic-grade drift signal (routine spec explicitly says these are review candidates, not promotable plans without human OK).

## Auto-fix
- `no candidate` — the only new signal (`screen-map-drift-pr-2351-reality`) is confidence=`medium`, score=2. Auto-fix bar requires confidence=high AND score≥3 (allowlist doc-stub write). Skipped.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0 (state carried forward from prior briefs — `.research/self-improvement/findings.json` not re-scanned this run)
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — mcp__github__push_files can't inline the >64KiB assignments-archive.json; fix: adopt PR+API-merge as the dispatcher's primary push path for the archive (proven this env)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend gc1-reconcile to auto-close any exact-id-terminal row (including status=failed)

## Open questions
- The dispatcher fire payload for this run was `buffer-low: claimable=2/72 — refill planner`. The routine surfaces vectors, not planner refills — flag for the planner pipeline (out-of-scope for this routine, in-scope for `.research/management/action-list.json`).
- Should Phase 1.6 role rotation and Phase 1.5 dev-review be cadence-throttled to avoid duplicating the dispatcher's per-PR / per-tier coverage? Current setup double-spends compute on days like today.

## Quiet day?
- No — 44 merged PRs, 1 new backlog vector, 0 promoted plans, 0 code-review findings, 0 auto-fixes. Not quiet; Telegram digest send is unblocked. (Phase 1.6 was skipped this run so the `pm` digest is stale; skipping the Telegram send anyway — see Phase 6 note in the routine prompt.)
