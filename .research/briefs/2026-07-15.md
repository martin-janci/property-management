# 2026-07-15

## Since last run
- Merged PRs: 50 (range #2273–#2356)
- Open PRs touched: 3 (#2339 open-nondraft, #2340 open-nondraft, #2332 open-draft-superseded)
- New / updated issues: 30 (all `follow-up`/`from-merged-review` filed by dispatcher; 28 already CLOSED by their fix PRs; 2 OPEN — #2318, #2320 — both have active fix PRs in flight)
- Commits: 49 on `dev` since 1691c9d (~half app-code, half `chore(research): dispatcher …` heartbeats)
- Routine lag: **~2d 20h** since last run (last: 2026-07-13T02:11:42Z). `lag_warning-2026-07-15` signal emitted; not yet at the 3-day `stale_routine_alert` threshold, but at the edge. Dispatcher kept running unmodified — this run is a catch-up ingest of its accumulated code-review signals.
- Phases that failed: none. Phase 1.5 and Phase 1.6 **skipped intentionally** (see below), not failed.

## Shipped
Full-run summary rather than per-PR (50 merges compressed):

**Security / IDOR / RLS (7):** #2316 (news_articles cross-tenant IDOR + FORCE-RLS gap — closes #2314); #2289 (AI chat within-tenant IDOR by owning user); #2335 (rag/migrate cross-org RLS + assumed-provenance + CI pgvector gap); #2356 (AI chat: role-gate `/escalated`, owner-scope feedback, remove unscoped dead repo methods — closes #2317); #2278 (portal inquiry webhook 500 on lead persistence failure); #2286 (portal webhook replay/timestamp-window protection); #2354 (extend replay protection to per-portal receivers + dedup + overflow edge).

**Frontend correctness / UX (12):** #2280 (reality-web AgencyDashboard 404 vs error distinction); #2281 (reality-web listing detail SSR crash on partial 200 body); #2282→#2334 (mobile dashboard load-error surfacing + QueryErrorBanner extraction); #2287 (price-alert subscription surface in reality-web); #2288 (mobile-native price-tracking surface); #2293/#2337 (notification-trigger UI + hooks-in-api-client); #2294/#2336 (portal-webhooks screen + role gate + pagination + i18n); #2304 (mobile dashboard silent-zero); #2338 (create-schedule UI wired to real endpoints); #2347 (signer-facing document-sign page); #2351 (reality-web listing detail type-guards + 404); #2352 (JWT tenant-decode + logout query-cache clear).

**Report/schedule + push infra (6):** #2295 (cron-edit recomputes next_run_at); #2310 (harden gap-84-4 push wiring — honest metrics + single token fetch + routing guard); #2313 (report_schedules due-work consumer + cadence unification — but #2318 shows the consumer still no-ops due to RLS, fix in flight in #2340); #2349 (quiet benign push skips + drop dead PushTransport param); #2290 (FCM+APNs combined push adapter).

**Documents / e-sign / gap-84 (5):** #2306 (screen-map entry for e-sign); #2308 (mobile DocumentUploadScreen tests); #2309 (presigned PUT endpoint); #2345 (ppt-web direct-to-S3 upload wire-up); #2346 (mobile document upload hardening).

**KMP / mobile-native (2):** #2305 (mobile useApi JWT decode + X-Tenant-ID injection tests); #2350 (KMP Decimal-as-string wire mismatch for favorites/price-tracking).

**Screen-map + tooling (8):** #2273 (RAG provenance model_filter test extraction); #2285 (reality/favorites screen-map baseline); #2291 (wire migrate_embeddings_to_pgvector to HTTP); #2296 (bump yanked spin versions — unblocks cargo-deny); #2297 (normalize story-id epic refs in screen-map frontmatter); #2298 (reconcile shipped-story tracking drift); #2307 (tick Tasks/Subtasks in 15 reconciled done story-md files); #2311 (letter-suffixed epic capture); #2312 (pgvector back-fill provenance + set-based UPDATE); #2315 (Booking.com fetch_property happy-path); #2333 (reconcile stale statuses); #2344 (lower-case synthesized epic ids + suffix regex); #2274 (auth.rs churn-hotspot refactor — extract `authenticated_user_id()`); #2353 (inquiry-webhook 5xx retry on unmatched syndication).

## Watch
- Stalled review: none — every open PR is <1d old.
- Reverted: 0 (no `Revert` PRs merged this window).
- Churn hotspots (new): `backend/servers/api-server/src/routes/portal_webhooks.rs` — 3 touches, ~1289 net line-churn in this window (PR #2286/#2294/#2336/#2353/#2354 stacked). New backlog row `refactor-churn-hotspots-api-server-portal_webhooks-2026-07-15` (score 1).
- Repeated-churn (existing hotspot_history hits still in play but not re-scored this run): `App.tsx` (runs=3), `auth.rs` (runs=3), `oauth_integration_tests.rs` (runs=3), `AnnouncementsScreen.tsx` (runs=2), `oauth.rs` (runs=2), `mobile/App.tsx` (runs=2). The routine intentionally does not stack a second `+1` on these this run — the two-day catch-up window overcounts a normal cadence signal.

## PRs stuck in draft despite approval
- #2332 — `fix(gh-issue-2317)` AI-chat scoping draft; **superseded** by #2356 which merged with the same #2317 scope 5h ago. Author (dispatcher) will need to close it. Not blocking anything.

## PRs with verdict=changes, no fix-round progress in 24h
- none — active assignments file (`gh-issue-2330`) is fresh review with verdict=approve already applied per commits `150f5d7`, `779ff1f`.

## Code review slice
- Segment reviewed: **skipped by the routine**
- Reason: dispatcher's Phase 2.5 already ran full-segment reviews across api-core, api-handlers, mobile-native-kmp, mobile-rn (×3), ppt-web-core (×2), ppt-web-ui, reality-server, reality-web in the 2026-07-14 → 2026-07-15 window (see `.research/signals/2026-07-14-*.json`, `.research/signals/2026-07-15-*.json`). This run ingested 32 fresh `code-review-finding` signals from those files rather than re-running its own pass.
- Findings this run: 32 ingested (see Backlog deltas). Next segment: any of the eight — cursor snapshot: `mobile-native-kmp` last-seen 2026-07-12 is now the oldest of the eight (all others touched 2026-07-13 or later).
- Skipped: yes — dispatcher already covered every segment.

## Screen-map status
- Total `docs/screens/` files: 168 (106 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` — the React Native app — still not seeded → 0)
- Drift signals this run: 0 (the route-touching PRs #2337/#2345/#2347 all shipped with their `docs/screens/ppt/*.md` companion updates; the dispatcher's per-PR gate held).
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not fetched this run (no route-file drift observed).

## Backlog deltas
- **New (33 total):** 32 `code-review-finding` items ingested from dispatcher signals (see IDs below); 1 `refactor-churn-hotspots-api-server-portal_webhooks-2026-07-15` (score 1).
  - Top-of-list by score:
    - [3/medium/test-gap] `code-review-mobile-rn-nfc-no-tests` — NFCCredentialManager+NFCAccessController have zero tests
    - [3/medium/bug] `code-review-api-handlers-missing-authz-article` — publish/archive/restore/pin/delete/media handlers lack manager gate
  - Notable score-2 items: `code-review-ppt-web-ui-portfolio-dash-mock` (Epic-144 dashboard renders fabricated financials), `code-review-ppt-web-ui-paymatch-mut-noerr` (silent-failure in payment matching), `code-review-ppt-web-ui-invoice-mut-noerr` (silent-failure in invoice create/delete), `code-review-ppt-web-core-auth-stale-token-init` (returning user with expired-but-refreshable token gets bounced to login instead of refreshed), `code-review-api-handlers-unenforced-toggle-flags` (article.comments_enabled ignored on create_comment).
- **Bumped:** none (all 32 are first-time signals).
- **Decayed:** none — every open item was created today; the one prior open row (`refactor-churn-hotspots-api-server-auth-2026-07-12`, updated 2026-07-12) is 3d old, well under the 14d threshold.
- **Dropped:** none.
- **Resolved (marked done via resolution check):** 1 — `code-review-api-handlers-news-articles-idor` → PR #2316 (issue #2314 closed).

## Plans promoted
- `plans/code-review-api-handlers-missing-authz-article.md` — add `is_manager` gate to publish/archive/restore/pin/delete/add_media/delete_media in `routes/news_articles.rs`; regression tests in new `news_articles_rbac_tests.rs`. · adversarial pass: passed
- `plans/code-review-mobile-rn-nfc-no-tests.md` — pin `NFCCredentialManager` + `NFCAccessController` decision paths (expiry, midnight-crossing time-windows, corrupt-blob discard, legacy-key migration, emergency-revoke) via two new `__tests__/*.test.ts` files. · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met `confidence=high` + `score≥3` + allowlist this run. The two high-score (3) code-review-finding rows are `test-gap`/`bug` vectors not covered by the auto-fix allowlist (which is limited to screen-map-drift, screen-map-orphan, fixme-in-merged-code, dep-update-noise lockfile drift, and stalled-review comments).

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=19
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — mcp__github__push_files can't inline the >64KiB assignments-archive.json; fix: adopt PR+API-merge as the dispatcher's primary push path for the archive (proven this env)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend gc1-reconcile to auto-close any exact-id-terminal row (including status=failed)

## Open questions
- **Trigger-payload discrepancy:** the routine was fired with `buffer-low: claimable=0/72`, but the actual `backlog.json` pre-run had 0 open items across 186 total (125 done + 60 dropped + 1 closed). The `72` figure appears to come from the dispatcher's action-list/assignments accounting rather than `backlog.json`; worth reconciling `Phase 2.6 buffer_low` accounting with the backlog metric (probably related to `fp-buffer-metric-blind-to-archived-open-items` above).
- **Phase 1.6 skipped**: pm_cursor (`pm-security` at index 5, last run 2026-05-27 — now 49 days stale) not advanced this run. The routine deferred to prioritise the buffer-refill; next run should run at least the Scrum Master synthesis to keep the delivery digest fresh.

## Quiet day?
- No — 33 new backlog vectors, 2 plans promoted, 1 resolution recorded, 1 new churn-hotspot signal. Phase 6 will send the Telegram digest.
