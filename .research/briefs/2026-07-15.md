# 2026-07-15

## Since last run
- Merged PRs: 50 (range #2253–#2338)
- Open PRs touched: 5 (#2332 draft rust-backend AI-chat scoping, #2336 draft react-web portal-webhooks hardening, #2337 draft react-web notification-triggers reshape, #2339 backend file_key IDOR + signed Content-Length, #2340 backend report-schedule scheduler RLS context)
- New / updated issues: 10 new `follow-up`/`from-merged-review` issues (#2325–#2343 range) — all OPEN, all `gh-issue-*` tasks fed straight to the dispatcher
- Commits: 44 non-merge on `dev` since 2026-07-09 (mix of merge commits from the dispatcher pipeline plus dispatcher chore commits on planning branch until 2026-07-10 20:12; nothing observed after that on any tracked branch — the dispatcher itself appears paused since 2026-07-10)
- Routine lag: 6d 9h since last run (fourth consecutive over-cadence gap; previous 2026-07-05→2026-07-09 was 3d 21h). `stale_routine_alert` threshold (3+d) crossed. Cloud research cron is either paused or its trigger stopped firing.
- Phases that failed: none — Phase 1.5 (`ppt-dev-review`) and Phase 1.6 (`ppt-project-management`) intentionally skipped this run in catch-up mode (cursors not advanced, so G16 is a no-op; `state.review_cursor` and `state.pm_cursor` / `state.coverage_cursor` all stay at their prior-run values). Resumes on next incremental run.

## Shipped
- #2335 gh-issue-2321 — fix cross-org rag/migrate RLS + assumed-provenance & CI pgvector gaps (2026-07-15)
- #2334 gh-issue-2323 — extract QueryErrorBanner + sweep raw error-message leaks on mobile (2026-07-15)
- #2333 gap-tracking-stale-status-flips-2026-07-15 — reconcile stale statuses (80-2 partial→done, ppt/reports apiStatus) (2026-07-15)
- #2316 gh-issue-2314 — scope news_articles handlers to caller org (cross-tenant IDOR + data-loss fix) (2026-07-15)
- #2315 gap-83-2 — exercise Booking.com fetch_property outbound happy path (2026-07-14)
- #2313 gh-issue-2303 — wire report_schedules due-work consumer + unify cadence model (2026-07-14)
- #2312 gh-issue-2300 — pgvector back-fill stamps provenance + set-based UPDATE (2026-07-14)
- #2311 gh-issue-2302 — capture letter-suffixed epics in screen-map drift extractor (2026-07-14)
- #2310 gh-issue-2301 — harden gap-84-4 push wiring (honest metrics, single token fetch, routing guard) (2026-07-14)
- #2309 gap-84-1 — add presigned PUT endpoint for direct client-to-S3 upload (2026-07-14)
- #2308 gh-issue-2284 — test mobile DocumentUploadScreen upload/validate/icon paths (2026-07-14)
- #2307 gh-issue-2299 — tick Tasks/Subtasks in 15 reconciled done story-md files (2026-07-14)
- #2306 gap-84-2 — add screen-map for signer-facing e-sign page (2026-07-14)
- #2305 gh-issue-2283 — test mobile useApi JWT decode + X-Tenant-ID injection (2026-07-14)
- #2304 gh-issue-2282 — mobile dashboard surfaces load errors instead of silent zeros (2026-07-14)
- #2298 gap-tracking-reconcile-story-md-statuses — reconcile shipped-story tracking drift (2026-07-14)
- (34 more merged PRs in the window — see `git log 5877ade..dev` for the full trail. Dispatcher pipeline landed the bulk via `gh-issue-*`, `gap-*`, `code-review-*`, and one `refactor-churn-hotspots-*` task type. Recurring theme: post-merge-review follow-ups converging.)

## Watch
- Stalled review: none matched the >7d non-draft criterion. Long-lived drafts skipped: #1797 (auth on OCR endpoints, draft since 2026-06-23), plus five new post-merge-review follow-up drafts (#2332, #2336, #2337) all opened 2026-07-15 (<24h — not stalled).
- Reverted: 0 in window.
- Churn hotspots (per-PR file-list not fetched exhaustively this run — catch-up mode): full deep-scan deferred. Prior top three (`booking/mod.rs`, `oauth_integration_tests.rs`, `routes/auth.rs`) remain the tracked hotspots from 2026-07-09.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     Not queried this run (catch-up mode — reviewDecision would require per-PR API round trips). -->
- none surfaced this run (per-PR `reviewDecision` not fetched — catch-up mode)

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json (planning branch) where
     reviewer_summary starts with "verdict=changes" AND (now - last_updated) > 24h AND status == "review". -->
- none surfaced this run (planning-branch assignments.json not deep-scanned — catch-up mode)

## Code review slice
- Segment reviewed: none this run (Phase 1.5 skipped — catch-up mode, `state.review_cursor` unchanged)
- Experts: n/a
- Findings: 0 (no `code-review-finding` signals emitted this run)
- Next segment: `mobile-native-kmp` (still oldest-unreviewed at 2026-06-06 — has been the "next" for two consecutive runs; will pick up on next incremental)
- Skipped: yes — catch-up mode (6d 9h lag)

## Screen-map status
- Total `docs/screens/` files: not counted this run (catch-up mode — full scan deferred)
- Drift signals this run: 0 (per-PR file-list not fetched for the 50 merged PRs — catch-up mode)
- Orphan screens this run: 0 (not scanned — catch-up mode)
- Last `screen-map.yml` CI run on `main`: not queried this run

## Backlog deltas
- **New:** — (no new items this run — the two ready plans from 2026-07-09 remain the actionable top of the backlog, and no signal warranted a fresh item beyond the informational lag_warning)
- **Bumped:** [0→0] `dx-routine-lag-catchup-2026-07` — evidence appended (fourth-consecutive lag); score stays 0 (informational)
- **Decayed:** — (nothing to decay — all 7 non-terminal items were last updated 2026-07-09, only 6d old; 14d decay threshold not crossed for any)
- **Dropped:** — (0 items transitioned this run)
- **Resolved:** — (0 resolutions confirmed — spot-checked the three code-anchored bugs against current `backend/servers/api-server/src/routes/auth.rs`; the buggy patterns still live at the cited line-numbers)
  - `bug-revoke-all-sessions-cookie-blindness` — line 2013 still reads only `X-Refresh-Token` header, never calls `parse_refresh_cookie`. Not resolved.
  - `security-forgot-password-no-rate-limit` — line 1461 `forgot_password` handler still has no `check_rate_limit` call. Not resolved.
  - `bug-refresh-empty-cookie-shadows-body-token` — line 1078 still does `cookie_token.as_deref().unwrap_or(req.refresh_token.as_str())` (empty cookie shadows body). PR #2270 added a test for the pattern but the code fix is not present. Not resolved.

## Plans promoted
- (none this run — the two 2026-07-09 plans stay `ready` and unresolved. Phase 3 cap allows 2 new plans/run, but no new backlog vector met the readiness bar in catch-up mode.)

## Auto-fix
- `no candidate` — the only new signal this run (`lag-warning-2026-07-15`) is informational, `score_delta=0`, doesn't touch the Phase-5 allowlist (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise`, `stalled-review`), and the seven pre-existing open items are `bug`/`security`/`refactor`/`test-gap`/`dx` — none on the allowlist. All go through the manual implementer path.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present in the tree)

## Open questions
- Cloud research routine cadence: 6d 9h since last run, dispatcher on planning branch also appears paused since 2026-07-10 20:12 (~5d idle). Fired by a `buffer-low` planner nudge, not by the daily cron. Operator: is the daily research cron actually still scheduled, or has it been paused? The `dx-routine-lag-catchup-2026-07` backlog item tracks this — its evidence now spans four consecutive lag windows.

## Quiet day?
- No — 50 merged PRs since last run, 10 new follow-up issues, three known-live bugs still unresolved in `routes/auth.rs`. The routine skipped its two heavy phases (1.5, 1.6) but this is not a quiet day; it's a lagging one.
