# 2026-07-13

## Since last run
- Merged PRs: 2 (range #2273–#2274)
- Open PRs touched: 0 (dev queue empty end-of-window)
- New / updated issues: 0 (window since 02:11 UTC; issue #2272 already closed by PR #2273 within the prior brief window)
- Commits: 11 on `dev` — 2 application-file commits (PRs #2273, #2274) + 9 `chore(research):` dispatcher/routine plumbing
- Routine lag: 6h 2m since last run (last: 2026-07-13T02:11:42Z) — on schedule
- Phases that failed: none

## Shipped
- #2273 `test(db)`: extract RAG provenance model_filter guard into own `#[sqlx::test]` — test-only restructure (+40/−24 in `backend/crates/db/tests/rag_similarity_search_tests.rs`); both functions remain `#[ignore]`d under BIT-351 quarantine (repair tracked in BIT-352). Closes #2272 (the post-merge-review follow-up filed at 2026-07-13T00:13Z).
- #2274 `refactor(api-server)`: extract shared bearer-auth preamble into `authenticated_user_id()` helper across the five bearer-authenticated handlers in `backend/servers/api-server/src/routes/auth.rs` (+28/−48 net −20 LOC). PR title contains the tracking-item id `refactor-churn-hotspots-api-server-auth-2026-07-12` verbatim → resolves it (see Backlog deltas).

## Watch
- Stalled review: none — dev queue is empty (all open PRs merged this window).
- Reverted: 0
- Churn hotspots: `backend/servers/api-server/src/routes/auth.rs` churned again (+28/−48 in PR #2274 itself — the refactor that resolves the tracking backlog item). `hotspot_history[auth.rs]` bumped `runs_seen` 3 → 4, `last_seen` 2026-07-12 → 2026-07-13, `recent_churn` 4 → 76. Cumulative-type signals (`churn-hotspot`, `repeated-churn`) re-emitted with `score_delta=0` per G4 carve-out — no new backlog item is created (the file's own tracking item just moved to `done`, and PR #2274 is behavior-preserving).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none — dispatcher assignments flight is empty after PR #2274 merged (see `chore(research)` reconcile at commit f06d656).

## Code review slice
- Segment reviewed: **skipped by the routine**
- Reason: dispatcher already ran a Tier-1d `dev-review` on `reality-server` earlier today (`signals/2026-07-13-reality-server.json`, 0 defects) plus a Tier-1d `review-merged` sweep of 8 PRs (0 issues opened). The 6h window since 02:11 UTC contributed only PR #2273 (test-only) and PR #2274 (behavior-preserving extraction) — no new route/handler surface to lens a fresh routine slice against.
- Findings: 0 (dispatcher slice) + 0 (routine skipped)
- Next segment: `ppt-web-ui` (oldest-unreviewed, cursor 2026-06-11)
- Skipped: yes — dispatcher already covered today and no fresh app-surface churn arrived to warrant a second pass

## Screen-map status
- Total `docs/screens/` files: 165 (103 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` — the React Native app — still not yet seeded → 0)
- Drift signals this run: 0 (neither PR #2273 nor #2274 touched frontend route files)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (no route-file activity → no meaningful delta since 2026-07-12 brief)

## Backlog deltas
- **New:** none — the two merged PRs both close out existing tracking work (PR #2273 closes issue #2272 which had already been claimed as `gh-issue-2272`; PR #2274 closes the auth churn-cluster backlog row).
- **Bumped:** none — cumulative auth.rs churn signals re-emitted at `score_delta=0` per G4 (would only re-score under the churn-hotspot allowlist if a non-cumulative signal type had also fired).
- **Decayed:** none — the sole open row moved to `done` today; nothing else is ≥14d stale.
- **Dropped:** none — no items decayed to 0 this run.
- **Resolved:** `refactor-churn-hotspots-api-server-auth-2026-07-12` (score=2, refactor) → `status=done`. Resolution matrix hit: PR-body match=YES (PR #2274 title contains the item id verbatim); evidence-gone=INCONCLUSIVE (item evidence is narrative churn observations — no grep-anchored TODO/FIXME pattern). Appended evidence: `"resolved: PR #2274 merged 2026-07-13 — refactor(api-server): extract shared bearer-auth preamble in auth routes; code patterns not independently verified (no grep-extractable TODO/FIXME anchor in evidence)"`. Note: PR #2274 explicitly left `auth_tests.rs` + `reality-server/routes/sso.rs` untouched by scope choice — the item resolves under the strict matrix rule (verbatim-id claim + inconclusive grep) but if either file re-churns, a fresh churn-hotspot signal will spin a new tracking item.

## Plans promoted
- none — the resolved item's plan field was already `null` (no plan file to archive); backlog now has 0 open/ready/deferred rows.

## Auto-fix
- `no candidate` — no signal met `confidence=high` + `score>=3` + allowlist this run. The only high-confidence signals this run are the cumulative auth.rs churn re-emits (`churn-hotspot`, `repeated-churn`), which are analytical-not-actionable by the G15/Phase-5 allowlist.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — mcp__github__push_files can't inline the >64KiB assignments-archive.json; fix: adopt PR+API-merge as the dispatcher's primary push path for the archive (proven this env)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend gc1-reconcile to auto-close any exact-id-terminal row (including status=failed)

## Open questions
- none — the auth churn-cluster tracking item is closed under strict matrix rules; if the untouched files (`auth_tests.rs`, `reality-server/routes/sso.rs`) drive fresh churn in a future window, that will re-open the conversation as a new item.

## Quiet day?
- No — 2 application PRs merged (#2273, #2274), the only open backlog item resolved to `done`, and `hotspot_history` advanced. `stats.quiet_days` unchanged at 2.
