# 2026-07-13

## Since last run
- Merged PRs: 2 (#2273 RAG provenance test extract, #2274 auth churn-hotspot helper extraction)
- Open PRs touched: 0 (queue drained after #2274 merged)
- New / updated issues: 1 (#2275 — dispatcher's Tier-1d dev-review filed portal_webhooks.rs inquiry-lead swallow-and-ack finding; already labelled backend/follow-up)
- Commits: 16 on `dev` — 2 app-code merges (`refactor(api-server)`, `test(db)`) + 14 `chore(research):` dispatcher/routine plumbing
- Routine lag: 12h 8m since last run (last: 2026-07-13T02:11:42Z) — on schedule
- Phases that failed: none

## Shipped
- #2274 refactor(api-server): extract shared bearer-auth preamble in auth routes — collapses five verbatim `extract_bearer_token → validate_access_token → claims.sub.parse()` blocks in `routes/auth.rs` into a single `authenticated_user_id()` helper (+28/-48 net, error semantics byte-preserved). Directly resolves backlog `refactor-churn-hotspots-api-server-auth-2026-07-12` (dispatcher had already flipped it open→done at 3bcef308).
- #2273 test(db): extract RAG provenance `model_filter` guard into own `#[sqlx::test]` — moves section E of `rag_retrieval_correctness` into a named `search_documents_pgvector_model_filter_provenance` function so the #2239 provenance regression assertion has an independent case (still `#[ignore]` under BIT-351). Closes #2272 (which itself came from a post-merge review follow-up).

## Watch
- Stalled review: none — no open PRs against `dev`
- Reverted: 0
- Churn hotspots: none new this run — only 2 files touched (`routes/auth.rs` from #2274, `rag_similarity_search_tests.rs` from #2273); both small diffs, neither warrants a hotspot signal. `hotspot_history` unchanged. Note: `routes/auth.rs` was the target of #2274's refactor (i.e. remediation of the prior repeated-churn signal), so the file's remaining churn is intentional stabilisation.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none — no active assignments in review state; dispatcher's `flight` was empty at 12:10 UTC and stayed empty since.

## Code review slice
- Segment reviewed: **skipped by the routine**
- Reason: dispatcher ran three Tier-1d `ppt-dev-review` passes today ahead of the routine — api-core (1 HIGH finding: `code-review-api-core-apicall-ssrf-redirect` re-confirmation, unfixed since 2026-07-10), ppt-web-ui (3 MEDIUM findings: protected-route hardcoded strings, offline-indicator no-i18n, access-denied dead-end), reality-server (0 defects). All three segments' `review_cursor` entries already stamp 2026-07-13.
- Experts: n/a (routine skipped)
- Findings: 2 tracked (both api-core calevent-IDOR signals from a prior dispatcher pass, now landing in backlog and seen_signals) + 4 additional dispatcher findings noted but not merged into routine signal set (SSRF re-emit conflicts with a `done` backlog row; 3 UI findings will re-appear on next Phase 1.5)
- Next segment: `reality-web` (2026-06-14 — oldest unreviewed)
- Skipped: yes — dispatcher already covered today's high-priority segments

## Screen-map status
- Total `docs/screens/` files: 165 (103 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` — the React Native app — still not yet seeded → 0)
- Drift signals this run: 0 (neither #2273 nor #2274 touched frontend route files)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (no route-file activity → no meaningful delta since 2026-07-12 brief)

## Backlog deltas
- **New:** [3] `code-review-api-core-sync-calevent-create-idor` — cross-org calendar-event write IDOR in `create_calendar_event` (path id ignored, no `verify_org_access`, no RLS backstop)
- **New:** [2] `code-review-api-core-sync-calevent-list-idor` — inconsistent authz on `list_calendar_events` (no `verify_org_access`; potential read IDOR)
- **Bumped:** none
- **Decayed:** none — no `open` items with `updated_at ≤ 2026-06-29` at start of run (all prior backlog rows terminal after yesterday's `refactor-churn-hotspots-api-server-auth-2026-07-12` flip)
- **Dropped:** none
- **Resolved by merged PR (already terminal):** `refactor-churn-hotspots-api-server-auth-2026-07-12` — PR #2274 shipped the helper-extraction on `auth.rs`; sources appended (dispatcher had already flipped status to `done` at 3bcef308, no further change needed)

## Plans promoted
- `plans/security-calevent-create-idor.md` — extract path-id + call `verify_org_access` + reject mismatched body `connection_id` + add RLS policy on `calendar_events`; test plan covers the cross-org POST repro · adversarial pass: passed (hypothesis traces to the two file:line evidence bullets; test plan will fail on `main` today because no handler-level authz exists; suggested-approach step 3 adds a new 400 pathway the test plan doesn't yet assert — noted, acceptable for medium-confidence promotion, adversarial reviewer to expand test coverage if warranted)

## Auto-fix
- `no candidate` — the sole `score>=3` new signal is `confidence=medium` (vector=bug), below the `confidence=high AND score>=3 AND allowlisted-type` bar. The two merged PRs (#2273, #2274) did not surface any auto-fixable signal shapes (no screen-map drift, no orphan screen, no new FIXME in merged diff, no stalled review, no lockfile drift).

## Self-review findings
- none — `.research/self-improvement/findings.json` not present in this run's read set.

## Open questions
- **SSRF re-emit vs `done` history** — dispatcher's `code-review-api-core-apicall-ssrf-redirect` finding (unfixed since 2026-07-10) references `services/actions/api_call.rs` (redirect follow-through + DNS-rebind + unbounded body), but the closest backlog row `security-ssrf-outbound-url-validation` is `done` and archived (fixed a different SSRF surface, `signatures.rs` / `integrations.rs`). Should the routine reopen it, mint a new `security-api-call-ssrf-redirect` row, or wait for the next Phase 1.5 to independently re-derive it? Left in the dispatcher's issue-ingest path for now.
- **Dispatcher ppt-web-ui findings** — three score-1 UI findings (protected-route i18n gap, offline-indicator no-i18n, access-denied dead-end) landed in dispatcher signal files today but not this routine's backlog. Confirm whether the routine's next Phase 1.5 (when it runs against `ppt-web-core` or re-covers `ppt-web-ui`) should absorb them, or whether the dispatcher's own issue-ingest flow will file them.

## Quiet day?
- No — 2 app-code merges, 2 new backlog rows, 1 plan promoted, and 1 dispatcher-filed issue landed since the last routine.
