# 2026-07-13

## Since last run
- Merged PRs: 2 (range #2273–#2274)
- Open PRs touched: 0 (dev queue drained)
- New / updated issues: 0 (issue #2272 was closed by PR #2273 but was already the previous run's `last_issue_seen` cursor)
- Commits: 12 on `dev` — 2 app-code merges (#2273, #2274), 10 `chore(research):` dispatcher/routine plumbing
- Routine lag: 7h 58m since last (02:11 UTC) routine run — on schedule
- Trigger: `buffer-low: claimable=0/72 — refill planner (dispatcher 2026-07-13)` (dispatcher-fire — routine re-ran on the same-day brief with a refill focus)
- Phases that failed: none

## Shipped
- #2273 test-gap(backend): gh-issue-2272 — extract RAG provenance `model_filter` assertions into own `#[sqlx::test]` (retrieval-half provenance guard now has independent identity for when BIT-352 restores the DB lane; still `#[ignore]`d under the BIT-351 quarantine — no green-CI signal today)
- #2274 refactor(api-server): extract `authenticated_user_id()` helper in api-server auth routes — collapses 5 byte-for-byte identical `extract_bearer_token → validate_access_token → claims.sub.parse()` preambles in `list_sessions/revoke_session/revoke_all_sessions/get_me/update_me`. Pure behaviour-preserving refactor. **Resolves backlog item `refactor-churn-hotspots-api-server-auth-2026-07-12`.**

## Watch
- Stalled review: none — 0 open PRs on `dev`
- Reverted: 0
- Churn hotspots: `backend/servers/api-server/src/routes/auth.rs` runs_seen=4 (+76 lines PR #2274) — this run's touch IS the tracked remediation landing, not fresh instability. `backend/crates/db/tests/rag_similarity_search_tests.rs` runs_seen=1 (+64 lines PR #2273) — first-touch, planned test-extraction follow-up. No new hotspot backlog rows created (both emitted score_delta=0, audit-only).
- **HIGH-severity SSRF on `api_call.rs` — INGESTED THIS RUN.** Dispatcher's Tier-1d dev-review re-confirmed on 2026-07-10 and 2026-07-13 that `backend/servers/api-server/src/services/actions/api_call.rs` has two unfixed HIGH SSRF findings: (a) `reqwest::Client` at line 106 lacks a redirect policy, following up to 10 hops with no per-hop validation; (b) `validate_external_url` at line 179 only inspects literal-IP hosts — DNS names bypass the guard entirely. Both re-verified by direct file read this run. Dispatcher buffer is 0/72 so the signals never became claimable via that path. Routine ingested all 3 findings (redirect-hop, DNS-rebinding, unbounded-body) and promoted the 2 HIGH ones as plans (security fast-track).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **skipped by the routine**
- Reason: dispatcher already ran 3 Tier-1d dev-review slices today — `api-core` (1 HIGH signal re-confirmed → NOW INGESTED as backlog rows), `ppt-web-ui` (3 Low findings), `reality-server` (0 defects). Routine's next-oldest segment `reality-web` (cursor 2026-06-14) has no fresh churn in this window to justify a second pass.
- Findings: 0 (routine skipped) + 4 (dispatcher: 1 HIGH SSRF re-confirm on api-core, 3 Low on ppt-web-ui)
- Next segment: `reality-web` (oldest-unreviewed by routine cursor)
- Skipped: yes — dispatcher already covered 3 segments today

## Screen-map status
- Total `docs/screens/` files: 165 (103 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` — the React Native app — still not yet seeded → 0)
- Drift signals this run: 0 (no route files touched — only `backend/**` changes in this window)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (no route-file activity → no meaningful delta since 2026-07-12 brief)

## Backlog deltas
- **New (3 dispatcher-ingested rows):**
  - [3] `security-api-call-ssrf-redirect-hop` — `api_call.rs` reqwest client follows 10 redirect hops with no per-hop validation → SSRF via 30x to 169.254.169.254 / 10.x
  - [3] `security-api-call-ssrf-dns-rebinding` — `validate_external_url` only checks IP literals; DNS names resolving to internal IPs bypass the guard (+ open to DNS rebinding)
  - [2] `bug-api-call-response-body-unbounded` — `response.text()` at line 322 reads unbounded body → memory-DoS + persisted-row bloat
- **Bumped:** none
- **Decayed:** none
- **Resolved (→ done):** `refactor-churn-hotspots-api-server-auth-2026-07-12` — PR #2274 verbatim-ID match in title AND direct diff verification of the collapsed preambles; sources += PR #2274; evidence line appended; updated_at=2026-07-13
- **Dropped:** none

## Plans promoted
- `plans/security-api-call-ssrf-redirect-hop.md` — SSRF redirect-hop fix: install a custom `reqwest::redirect::Policy` that re-runs `validate_external_url` on every hop; cap hops at 3; add integration test with a mock 302→internal-IP server. Security fast-track (score=3, confidence=high, vector=security). · **adversarial pass: passed**
- `plans/security-api-call-ssrf-dns-rebinding.md` — SSRF DNS-based fix: install a `PinnedDnsResolver` on the reqwest client that resolves the hostname, validates every resolved IP against `is_blocked_v4/v6`, and pins the connection to the first-validated IP (closes the TOCTOU/rebinding window). Add integration test with a static-override resolver mapping `test-rebind.local → 127.0.0.1`. Security fast-track (score=3, confidence=high, vector=security). · **adversarial pass: passed**

## Auto-fix
- `no candidate` — the 3 ingested signals are code fixes (SSRF handler rework + response-body streaming) which are not on the Phase 5 allowlist (that only covers screen-map-drift, screen-map-orphan, fixme-in-merged-code issue-only, dep-update-noise lockfile-bump, stalled-review comment-only). Correct path: manual implementer via the promoted plans.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — mcp__github__push_files can't inline the >64KiB assignments-archive.json; fix: adopt PR+API-merge as the dispatcher's primary push path for the archive (proven this env)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend gc1-reconcile to auto-close any exact-id-terminal row (including status=failed)

## Open questions
- Should the routine ingest dispatcher-generated `code-review-finding` signals by default rather than only under a buffer-low fire? The current separation (dispatcher owns Tier-1c/1d → assignment; routine owns backlog → plan) leaves HIGH-confidence findings stranded when the dispatcher's own conversion stalls. This run's buffer-low fire proves the failure mode is real (3 days unfixed HIGH SSRF); worth a self-improvement finding — but tabled for the user to decide the ingestion policy.

## Quiet day?
- No — 2 PRs merged, 1 backlog item resolved, 3 new backlog rows created (from dispatcher-ingested signals under the buffer-low fire), 2 plans promoted under the security fast-track. Not incrementing `state.stats.quiet_days`.
