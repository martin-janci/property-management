# 2026-07-13

## Since last run
- Merged PRs: 2 (#2273 test-gap RAG provenance extraction, #2274 auth churn-hotspot refactor)
- Open PRs touched: 0 (dev queue drained)
- New / updated issues: 0 (last issue #2272 closed at 04:07 UTC as PR #2273 resolved it; cursor unchanged)
- Commits: 10 on `dev` since cursor `1691c9d` — 2 application (PR #2273/#2274 squash-merges) + 8 `chore(research):` dispatcher/routine plumbing
- Routine lag: 0d 10h since last run (last: 2026-07-13T02:11:42Z) — on schedule
- Phases that failed: none

## Shipped
- #2273 test-gap(backend): gh-issue-2272 — extract RAG provenance model_filter assertions into own `#[sqlx::test]` (+40/-24, single file `backend/crates/db/tests/rag_similarity_search_tests.rs`; still `#[ignore]`d under BIT-351, but the guard now has independent identity for when BIT-352 un-quarantines the DB lane — no product-code change)
- #2274 refactor(api-server): extract `authenticated_user_id()` helper for the five bearer-authenticated `/sessions*` + `/me` handlers (+28/-48 net −20 LOC; single file `backend/servers/api-server/src/routes/auth.rs`; error responses byte-identical — this is the churn-hotspot maintainability refactor that PR #2274's dispatcher task teed up)

## Watch
- Stalled review: none — every recent PR merged fast
- Reverted: 0
- Churn hotspots: `backend/servers/api-server/src/routes/auth.rs` re-touched by PR #2274 (76 changed lines this run, runs_seen 3→4) — cumulative re-emit only, score_delta=0; the touch itself is the maintainability refactor that addresses the cluster, not fresh chaos

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none — the sole recent review flow (PR #2274) approved and merged inside the 4-hour window between the 02:11 UTC routine commit and this run

## Code review slice
- Segment reviewed: **skipped by the routine**
- Reason: dispatcher already ran two Tier-1d `ppt-dev-review` passes today (`api-core` at 04:12 UTC — 2 IDOR findings still tracked in `signals/2026-07-13-api-core.json`; `ppt-web-ui` at 08:10 UTC — 3 Low findings). Both segments' `review_cursor` entries advance to 2026-07-13. Oldest-unreviewed after today's dispatcher touches is `reality-web` (2026-06-14, ~29 days) — routine defers to the next Tier-1d fire or tomorrow's slice; running a third fresh pass this window would burn tokens on top of dispatcher coverage.
- Findings: 0 (routine skipped) + 5 (dispatcher, tracked separately in dispatcher signal files)
- Next segment: `reality-web` (oldest-unreviewed after today's dispatcher touches)
- Skipped: yes — dispatcher already covered fresh churn today

## Screen-map status
- Total `docs/screens/` files: 165 (103 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` — the React Native app — still not yet seeded → 0)
- Drift signals this run: 0 (no route files touched — both merged PRs are backend-only)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (no frontend-route activity)

## Backlog deltas
- **New:** none — the two merged PRs are dispatcher-authored resolutions of already-closed backlog rows (`gh-issue-2272`, `refactor-churn-hotspots-api-server-auth-2026-07-12`); both were flipped `open→done` by the dispatcher's earlier `backlog-reconcile` this morning (commits `3bcef30`, `f06d656`), not by this routine run
- **Bumped:** none
- **Decayed:** none — zero `open` items on entry (backlog is fully terminal: 125 done / 60 dropped / 1 closed / 52 triage-done-or-dropped)
- **Dropped:** none

## Plans promoted
- none — no open items, no readiness gate could pass. All 11 active `plans/` files are already assigned or terminal.

## Auto-fix
- `no candidate` — no signal met `confidence=high` + `score>=3` + allowlist this run. The single re-emitted signal (`repeated-churn-...auth.rs`, score_delta=0) is analytical, not actionable — cumulative signal types (`churn-hotspot`, `repeated-churn`) are explicitly excluded from Phase 5 per the routine spec.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — mcp__github__push_files can't inline the >64KiB assignments-archive.json; fix: adopt the PR+API-merge path proven in this env (push to new branch → open PR → server-side squash merge) as the dispatcher's primary write mechanism for the large archive files
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend `gc1-reconcile.sh` to auto-close any exact-id-terminal row (including `status=failed`)

## Open questions
- none

## Quiet day?
- No — 2 application PRs merged (#2273, #2274) resolve real work; the run is intentionally light on Phase 1.5 / 1.6 output because the dispatcher already covered same-day review and management upkeep, but the shipped-PR list alone is enough to break the quiet-day threshold. `state.stats.quiet_days` unchanged.
