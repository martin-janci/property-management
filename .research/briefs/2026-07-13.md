# 2026-07-13

## Since last run
- Merged PRs: 13 (range #2273–#2294)
- Open PRs touched: 5 (#2290, #2291, #2295 all `auto-impl` drafts opened this evening; #2296 non-draft cargo-deny lockfile fix; #2297 new dispatcher-spawned `gap-screens-normalize` draft, opened 22:18 UTC — in review)
- New / updated issues: 8 (5 closed today alongside their fixes — #2272/#2275/#2276/#2277/#2279; 3 still open — #2282, #2283, #2284 all mobile-rn tester findings from today's dispatcher Tier-1d review)
- Commits: 46 on `dev` since 1691c9d — 13 application-code merges + 33 dispatcher/research chore commits (including three from the last 20 minutes: the reality-web Tier-1d review, the dispatcher batch that merged 3 more PRs, and the #2297 fast-path)
- Routine lag: 20h 22m since last successful routine run (last: 2026-07-13T02:11:42Z, this: 2026-07-13T22:34Z) — within cadence (no `lag_warning` — threshold is >36h)
- Trigger payload received: `buffer-low: claimable=9/72 — refill planner` (fired at 22:20 UTC by dispatcher after buffer dropped). Treated as normal-run trigger (payload not in the recognized trigger set); the *effect* — surfacing a fresh promotable plan — is delivered by this reconciliation run
- Phases that failed: none

## Shipped
- **#2273** — `test(db): extract RAG provenance model_filter guard into own #[sqlx::test]` — closes issue #2272 (post-merge follow-up on PR #2265; provenance guard now runs as its own named case instead of riding on `#[ignore]`d fixture)
- **#2274** — `refactor(api-server): extract shared bearer-auth preamble in auth routes` — resolves last-run's only open backlog row (`refactor-churn-hotspots-api-server-auth-2026-07-12`), auth.rs runs_seen=3 cluster
- **#2278** — `gh-issue-2275: portal inquiry webhook returns 500 on lead persistence failure` — inquiry writes now surface DB errors instead of ack'ing 200
- **#2280** — `gh-issue-2277: reality-web AgencyDashboard shows misleading "No Agency Found" on any API error` — isError branch added
- **#2281** — `fix(reality-web): guard listing detail SSR against partial 200 bodies` — resolves #2276; JSON-LD guard consistent with content renderer
- **#2285** — `docs(reality-web): ship reality/favorites screen-map baseline (gap-84-3)` — new docs/screens/reality/favorites.md
- **#2286** — `fix(83-3-portal-webhooks): add replay/freshness protection to portal webhook` — `{timestamp}.{body}` signed material + 5-min window. Tests are inline in `webhook.rs` — see *Watch*
- **#2287** — `feat(reality-web): price-alert subscription surface for favorites (story 84.3)` — new `favorites/alerts/` route + `PriceAlerts.tsx` component (+283 lines)
- **#2288** — `gap-84-3-no-mobile-native-price-tracking-surface: mobile-native price-tracking surface for favorites` — Kotlin ViewModel + 386 lines of tests
- **#2289** — `fix(api-server): scope AI chat by-session-id handlers to owning user` — resolves #2279 within-tenant IDOR
- **#2292** — `feat(reports): wire Create-new-schedule flow to backend (gap-81-1)` — routes/groups/reports.tsx now hits POST /reports/schedules
- **#2293** — `gap-84-4: add notification-trigger management UI (ppt-web)` — new admin UI for notification triggers
- **#2294** — `gap-83-3: portal webhook status manager surface (ppt-web)` — new /settings/webhooks page (+303 lines PortalWebhooksPage.tsx). No matching `docs/screens/ppt/portal-webhooks.md` — see *Screen-map status*

## Watch
- Stalled review: none — every open PR was opened this evening (all 5 <5h old at cut).
- Reverted: 0
- Churn hotspots this run (top-3 after exclusions):
  - `backend/servers/api-server/src/routes/integrations/webhook.rs` — 390 lines (+325/-65) via PR #2286; `runs_seen 1→2`, hotspot re-appearance (last seen 2026-05-30). Cumulative churn-hotspot signal re-emitted with `score_delta=0`.
  - `frontend/apps/ppt-web/src/features/settings/webhooks/PortalWebhooksPage.tsx` — 303 new lines via PR #2294; first appearance (`runs_seen=1`).
  - `frontend/apps/reality-web/src/components/favorites/PriceAlerts.tsx` — 283 new lines via PR #2287; first appearance (`runs_seen=1`).
- False-positive suppressions this run:
  - `hotfix-no-test-pr-2286` → suppressed to `score_delta=0`. Title starts `fix(` and diff contains no `*test*` filename, but the diff **did** add 6 inline `#[test]` cases inside `webhook.rs`'s existing `#[cfg(test)] mod tests` block (stale/future/missing/non-numeric timestamp + timestamp-swap rejection). Tests exist; the heuristic misfires on inline-tests layout.
  - `risky-churn-pr-2286-webhook-rs` → suppressed for the same reason.

## PRs stuck in draft despite approval
- none — every open PR (#2290, #2291, #2295, #2296, #2297) was opened this evening; none can meet the ≥24h staleness bar.

## PRs with verdict=changes, no fix-round progress in 24h
- none — local `.research/management/assignments.json` at cut has 0 `verdict=changes` rows; dispatcher cleared the 2189/2213 pair before the routine's reconcile window opened.

## Code review slice
- Segment reviewed: **skipped by the routine** — dispatcher already covered 6 segments today (Tier-1d signals for `api-core`, `api-handlers`, `mobile-rn`, `ppt-web-ui`, `reality-server`, plus `reality-web` this same run: `.research/signals/2026-07-13.json` now carries 5 `code-review-finding` rows from dispatcher's parallel activity — 2 backend calendar-event IDORs and 3 reality-web import-flow finds).
- Findings: 5 (dispatcher — see *Backlog deltas*) + 0 (routine skipped) = **5 total, all folded into today's signals file**
- Next segment (routine cursor): `mobile-rn` (2026-06-16 cursor is oldest-stale for the routine's own cursor; dispatcher covered it via issues #2282–#2284 without advancing `state.review_cursor.mobile-rn`)
- Skipped: yes — dispatcher covered every segment fresh

## Screen-map status
- Total `docs/screens/` files: 165 (103 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` — the React Native app — still not yet seeded → 0)
- Drift signals this run: 4 (PRs #2281, #2287, #2292, #2294 — consolidated into one `needs-human-judgement` cluster row; only PR #2294's `portal-webhooks.md` is real new-screen drift)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (no route-file activity on `main` since 2026-07-12)

## Backlog deltas
- **New (routine, 3):**
  - [8] `test-gap-screen-map-drift-batch-2026-07-13` — 4-PR drift cluster · **status=needs-human-judgement** (Phase 3 gate excludes)
  - [1] `refactor-churn-hotspot-portal-webhooks-page-2026-07-13` — PortalWebhooksPage.tsx watch-list
  - [1] `refactor-churn-hotspot-price-alerts-2026-07-13` — PriceAlerts.tsx watch-list
- **New (dispatcher Tier-1d, folded 5):**
  - [3] `bug-code-review-api-core-sync-calevent-create-idor` — **PROMOTED** (see *Plans promoted*)
  - [2] `bug-code-review-api-core-sync-calevent-list-idor` — companion read-side IDOR
  - [2] `bug-code-review-reality-web-crm-create-no-catch` — CrmConnection handleCreate no try/catch
  - [2] `bug-code-review-reality-web-feed-create-no-catch` — FeedImport handleCreate no try/catch
  - [1] `bug-code-review-reality-web-import-i18n` — hardcoded English strings in import flow
- **Bumped:** none
- **Decayed:** none (no prior `open` items with `updated_at` >14d)
- **Dropped:** none

## Plans promoted
- `plans/bug-code-review-api-core-sync-calevent-create-idor.md` — Calendar-event create IDOR fix: verify_org_access on connection id + RLS policy for calendar_events + regression `#[sqlx::test]`. Score 3, confidence medium, bug vector. Feeds the buffer-low ask. · **adversarial pass: passed** (Hypothesis traces to Evidence via `sync.rs:991`/`integration.rs:374`/migrations grep; Suggested approach addresses all three layers named in Hypothesis; Test plan asserts 403/404 on cross-org POST which fails-on-`dev`-today — 201 Created — and passes after fix; no placeholder rot; all 4 metadata fields + 11 required headings present; Files bullets both resolve on disk; C1/C3/C6/C7 ticked; Mode: cloud-ok declared.)

## Auto-fix
- `no candidate` — no signal met `confidence=high` + `score>=3` + allowlist this run. Dispatcher's 5 findings are `medium` confidence (below the certainty bar). The 4 drift signals are on the eligible allowlist but confidence is `medium`, not `high`. The suppressed PR #2286 hotfix signals are `score_delta=0`. Churn hotspots are `refactor`, off-allowlist.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — Phase 6 archive-move produced a 198KB assignments-archive.json; `mcp__github__push_files` can't inline the >64KiB payload; fix: PROVEN WORKAROUND this run: push the dispatcher state commit to a NEW auto-fix branch and PR-merge (dispatcher's primary path for the archive)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer reported claimable=54/open=54 but REAL claimable=0 because terminal-in-archive open rows aren't subtracted; fix: (1) CLOSE step - extend gc1-reconcile to auto-close any exact-id-terminal row (including status=failed); (2) update Phase 2.6 buffer + GC3 to subtract archive task_ids from `claimable`

## Open questions
- none — the drift-batch NHJ row exists intentionally to keep the noisy heuristic out of the promotion path.

## Quiet day?
- No — 13 merged PRs, 8 new backlog vectors (3 routine + 5 dispatcher-folded), 1 promoted plan, 5 dispatcher code-review findings, 0 auto-fix actions. Not incrementing `stats.quiet_days`.
