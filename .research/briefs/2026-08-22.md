# 2026-08-22

## Since last run
- Merged PRs: 4 (range #2821–#2827; window 2026-08-22T03:03:20Z → 2026-08-22T12:37:57Z)
- Open PRs touched: 2 (#2828 draft, #2829 draft — dispatcher-owned auto-impls for the two new ppt-web-ui items; both created ~10:42–10:48Z, CI/review pending)
- New / updated issues: 3 (#2822, #2823, #2824 — dispatcher-created follow-ups from Tier 1d post-merge review; all 3 CLOSED same-window by the PRs below)
- Commits: 17 on `dev` since last routine commit (4 application-code merges + 13 dispatcher chores touching only `.research/{management,backlog}.json`)
- Routine lag: 9h 34m since last run (well under the 36h threshold — no lag_warning)
- Phases that failed: none — Phase 1.5 skipped (dispatcher's Tier 1d already reviewed ppt-web-ui at 08:31Z; other segments were reviewed 2026-08-16 through 2026-08-22, no oldest-unreviewed jump warranted a duplicate slice), Phase 1.6 skipped (dispatcher already accounted for the 4 merges in `.research/management/assignments.json`; `pm_cursor` stays at 3 → `pm-qa`, `coverage_cursor` stays at 5)

## Shipped
- #2821 code-review-api-handlers-booking-connect-non-manager-hijack — gate direct-connect OTA credential writes on manager role (security; closes the "any org member can overwrite Booking.com creds" hijack)
- #2825 gh-issue-2824 — i18n VerificationBadge expiry copy + de-duplicate expiry logic (i18n follow-up to PR #2819; adds `marketplace.verificationBadge` namespace across all 6 locales, extracts `getExpiryState` helper)
- #2826 gh-issue-2823 — per-channel bookkeeping + bounded retry for held-notification drain (backend follow-up to PR #2729; adds `delivered_channels`/`attempts`/`dead_lettered_at` columns via migration 00234, dead-letters after `QUIET_HOURS_DRAIN_MAX_ATTEMPTS`)
- #2827 gh-issue-2822 — neutralize CR/LF in CSV export sanitizer (security follow-up to PR #2731; closes CSV row-injection via free-form vote titles)

## Watch
- Stalled review: none new since last run (#2744 still `needs-human-review` on self-PR approve gate — logged in earlier runs)
- Reverted: none
- Churn hotspots: 4 backend/frontend files touched by the ship burst — no dedicated hotspot scan this fire (dispatcher already refreshed hotspots implicitly via Tier 1d ppt-web-ui review at 08:31Z); `hotspot_history` unchanged, top-3 remains `oauth_integration_tests.rs` (runs_seen=1, 3427 lines), `voice_webhooks.rs` (runs_seen=3, 2871 lines), `integrations/webhook.rs` (runs_seen=2, 2799 lines)

## PRs stuck in draft despite approval
- #2828 `code-review-ppt-web-ui-facilities-booking-silent-errors` — draft, created 10:42Z (age <2h) — dispatcher fast-path capture, reviewer/CI still pending; NOT stuck yet, well under 24h bar
- #2829 `code-review-ppt-web-ui-aml-dashboard-prompt-alert-flow` — draft, created 10:48Z (age <2h) — same shape as #2828; NOT stuck yet
- none stuck ≥24h

## PRs with verdict=changes, no fix-round progress in 24h
- none — the 4 merged PRs this window all landed with `verdict=approve` (per dispatcher's Phase 5 reviewer notes in commits c390239, 2f20a0f, 7db3431, 466a0f8); nothing in `verdict=changes` state stalled >24h

## Code review slice
- Segment reviewed: SKIPPED (dispatcher's Tier 1d at 08:31Z ran `ppt-dev-review` on `ppt-web-ui` segment and added 3 findings to `backlog.json`; running Phase 1.5 again this fire would duplicate that work)
- Experts: n/a (this fire — dispatcher's Tier 1d used the ppt-web-ui expert)
- Findings: 0 this fire (3 findings were added by dispatcher at 08:31Z: `code-review-ppt-web-ui-facilities-booking-silent-errors` score 2, `code-review-ppt-web-ui-facilities-booking-hardcoded-i18n` score 1, `code-review-ppt-web-ui-aml-dashboard-prompt-alert-flow` score 1 — all 3 already have status flips or draft PRs in flight)
- Next segment: `ppt-web-ui` OR `mobile-native-kmp` (both share 2026-08-16 as oldest-unreviewed timestamp; `review_cursor.ppt-web-ui` was NOT updated by dispatcher's Tier 1d because Tier 1d writes only to `backlog.json` — the routine's `review_cursor` remains authoritative for Phase 1.5 rotation)
- Skipped: yes — same-day rerun; dispatcher's Tier 1d covered the natural next slice

## Screen-map status
- Total `docs/screens/` files: unchanged from 03:03 brief (`ppt/` + `reality/` populated; `mobile/` still not seeded)
- Drift signals this run: 0 (spot-checked the 4 merged PRs — #2821 touches only `backend/servers/api-server/src/handlers/install.rs` + `oauth.rs` + tests, no frontend routes; #2825 touches VerificationBadge components + locale JSONs, no route file; #2826 touches `backend/servers/api-server/src/routes/notifications.rs` + drain worker + migration, no frontend; #2827 touches `backend/servers/api-server/src/handlers/reports.rs` + shared sanitizer, no frontend)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not re-queried this window (no frontend route files touched by the 4 merges)

## Backlog deltas
- **New:** none this fire (3 rows were added by dispatcher's Tier 1d at 08:31Z: `code-review-ppt-web-ui-facilities-booking-silent-errors` score 2, `code-review-ppt-web-ui-facilities-booking-hardcoded-i18n` score 1, `code-review-ppt-web-ui-aml-dashboard-prompt-alert-flow` score 1 — see commit bf2ff43 on dev)
- **Bumped:** none (no signal double-count; dispatcher's Tier 1d applied score deltas at row creation)
- **Decayed:** none (no `open` item is older than 14 days as of today — oldest open is 2026-08-11 for the mobile-native-kmp cancellation-swallowed row; cutoff is 2026-08-08)
- **Dropped:** none
- **Flipped by dispatcher (informational):** `code-review-api-handlers-booking-connect-non-manager-hijack` open→done via PR #2821 merge at 08:23Z (evidence-line added by dispatcher at commit c8135bf: `resolved: PR #2821 merged 2026-08-22 — code-review-api-handlers-booking-connect-non-manager-hijack: gate direct-connect OTA credential writes on manager role`)

## Plans promoted
- none this run — no open backlog item met the readiness gates (score ≥ 3, plan-absent) besides `code-review-mobile-native-kmp-inquiries-response-contract` which ALREADY has a plan (`plans/code-review-mobile-native-kmp-inquiries-response-contract.md`) awaiting a local-only implementer; the 5 mobile-native-kmp open rows are structurally unlandable in cloud (issue #2652), and the 3 ppt-web-ui rows are either already-in-flight (drafts #2828/#2829) or file-overlap with a claimed sibling (`facilities-booking-hardcoded-i18n` shares files with `-silent-errors` claimed by dispatcher)

## Auto-fix
- `no candidate` — 0 signals emitted this fire met confidence=high + score≥3 + allowlist (allowlist requires `screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / `dep-update-noise` lockfile-drift / `stalled-review`); none of the 4 merged PRs left an unchecked-TODO in body and no drift/orphan/FIXME/lockfile signals fired this window

## Self-review findings
- none — no `.research/self-improvement/findings.json` file present in the repo

## Open questions
- none new — the pre-existing `needs-human-judgement` rows still hold: the 3 `feat(acc)` accounting PRs (#2555 / #2558 / #2559) blocked on the accounting spec resolution, and the operator-payload observation `buffer-low: claimable=7/72 — refill planner (landable pool exhausted: 6 mobile-native gated + 1 file-overlap)` reflects a structural constraint (mobile-native cloud-gated per issue #2652) that only a local-implementer session or the reopening of issue #2652 can unblock — nothing this routine can auto-promote

## Quiet day?
- No — 4 merged PRs shipped since last run (#2821, #2825, #2826, #2827), 3 issues opened+closed same-window (#2822/#2823/#2824), 2 new dispatcher-owned drafts (#2828, #2829), and dispatcher's Tier 1d added 3 backlog rows. `stats.quiet_days` stays at 3; `stats.runs` bumped 69 → 70. Cursors advance: `last_pr_seen` 2820 → 2829, `last_issue_seen` 2816 → 2824, `last_commit_sha` e59dca8 → 0581bc6.
