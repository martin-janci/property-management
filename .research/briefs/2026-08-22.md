# 2026-08-22

## Since last run
- Merged PRs: 14 (#2799, #2800, #2801, #2803, #2805, #2806, #2811, #2812, #2813, #2815, #2817, #2818, #2819, #2820)
- Open PRs touched: 5 (all dependabot / dep-update noise — #2810, #2809, #2808, #2749, #2673)
- New / updated issues: 6 since #2797 — all CLOSED dispatcher follow-ups from post-merge reviews (#2802/#2804/#2807/#2814/#2816 + #2794); each already folded into a subsequent PR
- Commits: 49 on `dev` since last run (mix of dispatcher chore commits and 14 substantive merges)
- Routine lag: 2d 19h since last run (67h > 36h threshold → `lag_warning` fired; no cloud-cron miss visible in dispatcher log — same one-off skip pattern as 2026-08-19)
- Phases that failed: none — Phase 1.6 intentionally skipped (buffer-refill single-purpose run per fire payload; `pm_cursor` NOT advanced, still at 3 → `pm-qa`)

## Shipped
- #2820 code-review-ppt-web-ui-vote-detail-hardcoded-en — i18n VoteDetailPage user-facing strings
- #2819 code-review-ppt-web-ui-verification-badge-expired-as-soon — render expired verification badges as expired (not "expiring soon")
- #2818 code-review-ppt-web-ui-submission-preview-modal-orphan — remove orphaned SubmissionPreviewModal dead component
- #2817 gh-issue-2816: remove orphaned `saved_searches` int4 path (follow-up hardening from #2815)
- #2815 gh-issue-2814: widen `saved_searches.match_count` int4 → bigint (kills silent watermark-wedge on overflow)
- #2813 code-review-reality-server-inquiry-detail-empty-messages: return persisted inquiry messages
- #2812 fix(reality-server): stop discarding saved-search watermark-advance error (root cause for #2814)
- #2811 gh-issue-2807: route ai/voice link path through single-writer voice-device upsert (closes the second linker gap #2794 uncovered)
- #2806 code-review-api-handlers-voice-webhook-default-secret-retry2: voice webhook HMAC fails closed on empty secret
- #2805 gh-issue-2804: scope RUSTSEC-2026-0258 h2 DoS ignore to h2 0.3.x only (keeps 0.4.x under advisory watch)
- #2803 gh-issue-2802: bound apt-get network waits in CI so a stalled mirror can't wedge test-shard
- #2801 code-review-reality-server-list-my-reports-total-page-length: return the true total row count
- #2800 code-review-reality-server-submit-report-no-rate-limit: rate-limit anonymous POST /api/v1/reports per IP (from this run's prior brief promotion — landed cleanly)
- #2799 code-review-reality-server-create-review-body-length-uncapped: cap `create_review` body length

## Watch
- Stalled review: none new since last run — the 3 `feat(acc)` PRs (#2555 / #2558 / #2559, 24d old) already fired `stalled-review-*` signals in prior runs; sit at `needs-human-judgement` (blocked on the accounting spec resolution)
- Reverted: none
- Churn hotspots: `backend/servers/api-server/tests/suites/oauth_integration_tests.rs` (3427 lines, NEW hotspot — path moved from `tests/oauth_integration_tests.rs`, runs_seen=1); `backend/servers/api-server/src/routes/voice_webhooks.rs` (2871 lines, repeated-churn runs_seen=3, cumulative re-emit, no score bump); `backend/servers/api-server/src/routes/integrations/webhook.rs` (2799 lines, cumulative re-emit)

## PRs stuck in draft despite approval
- none — no open drafts with `reviewDecision==APPROVED` and age >24h this run (the two long-lived drafts #2744 and #2796 are still draft but have no fresh reviewer approval waiting; not the same bug class)

## PRs with verdict=changes, no fix-round progress in 24h
- none in current window — `.research/management/assignments.json` (planning branch) reports 0 `verdict=changes` rows this window (1 active row: `gh-issue-2743` in `review`, post-approve fast-path)

## Code review slice
- Segment reviewed: `api-handlers` (reason: churn-aligned — `voice_webhooks.rs` + `integrations/webhook.rs` were top-3 raw-churn source files this run; both landable Rust)
- Experts: rust backend (single-agent focused review)
- Findings: 1 high-severity, high-confidence — `code-review-api-handlers-booking-connect-non-manager-hijack` (see Backlog deltas)
- Next segment: `mobile-native-kmp` or `ppt-web-ui` (2026-08-16 = 6d old, oldest unreviewed) — but for cloud-landable priority, `ppt-web-ui` wins next run since mobile-native isn't cloud-buildable
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 153 (ppt/=106, reality/=47, mobile/=0)
- Drift signals this run: 0 (14 merged PRs — none touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`; the ppt-web-ui merges (#2818/2819/2820) all touched components/pages under those, not the router-shape files the drift detector watches)
- Orphan screens this run: 0 (not scanned in this single-purpose run)
- Last `screen-map.yml` CI run on `main`: unknown (session cannot query Actions runs)

## Backlog deltas
- **New:** [3] `code-review-api-handlers-booking-connect-non-manager-hijack` — SECURITY: `connect_booking` gates only on `verify_org_access`, so any org member can overwrite the org's Booking.com creds (missing manager gate parallels `direct_connect_airbnb` + `booking_token_exchange`; same missing-gate pattern on 3 sibling handlers). Directly refills the cloud-landable buffer per fire payload.
- **Bumped:** none this run (single Phase 1.5 finding; other signals were cumulative-type re-emits with score_delta=0)
- **Decayed:** none — oldest open items are `cancellation-swallowed` and `ssoservice-untested` at 2026-08-11 (11d), still under the 14-day decay threshold
- **Dropped:** none this run
- **Resolved (done):** the 3 reality-server code-review items from the 2026-08-19 Phase 1.5 slice already flipped to `status: done` in `backlog.json` by the dispatcher's reconciler (PRs #2799 / #2800 / #2801 all merged); no additional action needed this run

## Plans promoted
- `plans/code-review-api-handlers-booking-connect-non-manager-hijack.md` — add `verify_manager_role_in_org` to `connect_booking` (install.rs:907), `disconnect_booking` (:1247), `disconnect_airbnb` (:756), and `airbnb_oauth_callback` (oauth.rs:773); regression test seeds Manager + plain-member and asserts 403 + connection untouched · adversarial pass: passed (source→hypothesis→approach→test chain unbroken; test fails today because current code returns 200 and overwrites the connection, passes after the manager gate is added; 3/3 cited files exist on disk; Mode: cloud-ok; no placeholder rot; scope pruned — unmounted `sync` router explicitly out of scope, matches `integrations/mod.rs:29-36` comment)

## Auto-fix
- `no candidate` — the single high-confidence security finding is not in the allowlist (allowlist covers only screen-map-drift / orphan / fixme-in-merged-code / lockfile-drift / stalled-review; a manager-gate wire-up needs a real plan). No dep-update lockfile-drift, no screen-map drift/orphan, no fresh unchecked FIXMEs in merged code. No stalled-review candidates that haven't already fired.

## Self-review findings
- open=34 (high=7, medium=24, low=3); new this run=0; acknowledged=19 (per prior run's snapshot — file not re-scanned this single-purpose run)
- fp-mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=15] — dispatcher-prompt still hard-assumes push is proxy-403-blocked; fix: replace hard-coded assumption with runtime probe
- fp-mcp-push-cannot-inline-large-archive [recurrence=15] — archive-move produced a 198KB assignments-archive.json; fix: chunked push-files fallback landed but underlying prompt guidance still assumes single-shot inline push
- fp-tier1d-signals-accumulate-no-promotion-to-backlog [recurrence=9] — this run again addresses via Phase 1.5 promotion to backlog + 1 landable plan; automation gap still open (each run needs a human/Claude-authored promotion)
- fp-stale-in-progress-action-list-decoupled-from-assignments [recurrence=5] — action-list.json rows drift from assignments.json; fix: extend gc1-reconcile.sh
- fp-implementer-sandbox-zero-pushed-branch-multigen [recurrence=3] — implementers claim then produce zero branches; fix: liveness escalation on reclaim
- … and 2 more (see findings.json)

## Open questions
- **Phase 1.6 intentionally skipped again this run.** Second consecutive single-purpose buffer-refill run. `pm_cursor.next_index` still at 3 (`pm-qa`) — the daily rotation is 3 days behind. If the operator wants Phase 1.6 to run alongside buffer-refill, say so and the next revision will always include the Scrum Master synthesis (which is the cheap part) even on single-purpose runs.
- **Plan hygiene: 20+ plans in `plans/` are for `status: done` or `status: dropped` items and should be moved to `plans/_archive/`.** Not touched this run (G13 archive-only-grows lets us skip, and each move would need a matching backlog update). Worth a dedicated hygiene sweep — the accumulation makes stem-collision checks noisier than needed. The dispatcher's after-merge flow was expected to handle it but hasn't.
- **The promoted plan hits multiple sibling handlers in the same fix.** It's still one PR — implementation touches `install.rs` + `oauth.rs`. If the operator wants strict "one handler per PR" splitting, the plan can be sliced into 4 (connect_booking, disconnect_booking, disconnect_airbnb, airbnb_oauth_callback) — but the shared-root-cause + same-file-diff nature argues for one atomic security patch.

## Quiet day?
- No — 14 substantive merges since last run, 1 new landable security vector (score=3), 1 landable plan promoted (refills the cloud-landable buffer that the fire payload flagged as empty).
