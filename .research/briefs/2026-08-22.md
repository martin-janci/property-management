# 2026-08-22

## Since last run
- Merged PRs: 7 (range #2821–#2830) — 4 dispatcher-owned code-review PRs (#2821, #2828, #2829, #2830) + 3 issue-followup PRs (#2825, #2826, #2827)
- Open PRs touched: 0 (no non-dependabot open PR updated in the window 03:03Z → 18:41Z; #2810/#2809/#2749/#2673/#2808 last touched 2026-08-20)
- New / updated issues: 5 (created #2822–#2824 → all CLOSED same-day by #2825/#2826/#2827; created #2831, #2832 → both OPEN, tagged `follow-up` + `from-merged-review`)
- Commits: 26 on `dev` since 03:03Z (7 application-code PRs + 19 dispatcher `chore(research)` commits on `.research/management/`)
- Routine lag: 15h 38m since last run (well under the 36h threshold — no lag_warning)
- Trigger payload: `buffer-low: claimable=6/72 — all open backlog is mobile-native (cloud-unlandable); need landable vector refill` — addressed by this run: **+3 new landable vectors** (2 promoted to plans, 1 open) plus 2 more high-quality open items for future promotion
- Phases that failed: none

## Shipped
- #2821 code-review-api-handlers-booking-connect-non-manager-hijack — SECURITY: added manager gate to `connect_booking`/`disconnect_booking`/`disconnect_airbnb`/`airbnb_oauth_callback` (backend/servers/api-server/src/routes/integrations/install.rs + oauth.rs); 5 new IG3 regression tests
- #2825 gh-issue-2824 — i18n VerificationBadge expiry copy + de-dup `getExpiryState` helper (frontend/apps/ppt-web/src/features/marketplace/components/VerificationBadge.tsx + all 6 locale bundles); closed #2824
- #2826 gh-issue-2823 — per-channel bookkeeping + bounded retry for the quiet-hours held-notification drain (backend/servers/api-server/src/services/quiet_hours_drain.rs + notification_pipeline.rs + new migration 00234); closed #2823
- #2827 gh-issue-2822 — CSV export sanitizer neutralises CR/LF in `sanitize_csv_cell` (backend/servers/api-server/src/routes/admin/audit.rs + reports/helpers.rs); closed #2822
- #2828 code-review-ppt-web-ui-facilities-booking-silent-errors — surface fetch/approve/reject/cancel errors via `useToast()` on the 3 facilities booking pages
- #2829 code-review-ppt-web-ui-aml-dashboard-prompt-alert-flow — replace `window.prompt`/`alert` in AML dashboard with `InitiateEddDialog`/`ReviewAssessmentDialog` + i18n across all 6 locales
- #2830 code-review-ppt-web-ui-facilities-booking-hardcoded-i18n — wire `useTranslation` into `MyBookingsPage`/`PendingBookingsPage`/`BookFacilityPage`/`components/BookingList` + add `facilities.bookings` namespace to all 6 locales

## Watch
- Stalled review: none new since last run — the 3 pre-existing needs-human-judgement rows (`stalled-review-pr-2555/2558/2559`, feat(acc)) still hold at 21d idle; still blocked on the accounting spec resolution
- Reverted: none
- Churn hotspots: no dominant hotspot this run — `frontend/apps/ppt-web/messages/{en,sk,cs,de,pl,hu}.json` each touched 3× (locale-bundle additions from #2825/#2829/#2830 — expected cadence, not instability); `PendingBookingsPage.tsx`/`MyBookingsPage.tsx` touched 2× each (#2828 + #2830). All excluded from `hotspot_history` bumps (below single-file top-3 threshold)

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none in current window — the dispatcher assignments.json has `code-review-mobile-native-kmp-inquiries-response-contract` as its only active row (planning branch), which is Kotlin/cloud-unlandable and not blocked on verdict=changes

## Code review slice
- Segment reviewed: `reality-web` (reason: oldest-unreviewed at 2026-08-18 → 4d)
- Experts: frontend
- Findings: 3 (see Backlog deltas for `code-review-reality-web-*` signals)
- Next segment: `mobile-native-kmp` (state.review_cursor last_reviewed=2026-08-16, oldest after this run alongside `ppt-web-ui: 2026-08-16`; churn-alignment inactive — no churn hotspot this run)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: unchanged (ppt/ + reality/ populated; mobile/ still not seeded)
- Drift signals this run: 0 (no merged PR touched a trigger route file — the 4 ppt-web PRs touched `features/` and `messages/`, not `routes/**` or `App.tsx`; no reality-web `app/**` touch this window)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not re-queried this window (no route-file merge → no CI trigger to re-check)

## Backlog deltas
- **New:** [3] `gh-issue-2831-quiet-hours-drain-concurrent-race` — Rust backend, concurrent-worker race in quiet-hours drain (no FOR UPDATE SKIP LOCKED); verified against current tree
- **New:** [3] `code-review-reality-web-realtor-mock-ignores-id` — reality-web `/[locale]/realtor/[id]` renders MOCK_AGENT for every id; SEO-indexed public route
- **New:** [2] `gh-issue-2832-aml-dialog-stale-state` — AML EDD/Review dialogs mounted-forever → stale reason/notes across assessments (audit-trail hazard)
- **New:** [2] `code-review-reality-web-agency-soft-404` — `/[locale]/agency/[slug]` client-side 200 on unknown slug (crawler pollution)
- **New:** [1] `code-review-reality-web-agency-i18n-slovak` — hardcoded Slovak strings on a `[locale]` route serving en/cs/de/hu/pl
- **Bumped:** none (all 5 new items are fresh; no existing item got a score increment this run)
- **Decayed:** none (0 open items with `updated_at ≤ 2026-08-08`)
- **Dropped:** none
- **Resolved (open → done):** [1] `code-review-ppt-web-ui-facilities-booking-hardcoded-i18n` → done via PR #2830 (PR title matches item id verbatim; PR body describes wiring `useTranslation` across the exact files listed in item.files)

## Plans promoted
- `plans/gh-issue-2831-quiet-hours-drain-concurrent-race.md` — Rust backend fix: switch `get_notifications_to_release` to a `FOR UPDATE SKIP LOCKED` atomic-claim CTE with SQL `LIMIT`; add concurrent-worker regression test. Mode: cloud-ok. · adversarial pass: passed
- `plans/code-review-reality-web-realtor-mock-ignores-id.md` — reality-web fix: convert `/[locale]/realtor/[id]/page.tsx` to server component, fetch via `GET /api/v1/realtors/{user_id}/profile`, call `notFound()` on 404, extract client contact-form subcomponent. Mode: cloud-ok. · adversarial pass: passed

## Auto-fix
- `no candidate` — none of this run's 5 signals matches Phase 5's safe-shape allowlist (screen-map-drift/orphan, fixme-in-merged-code, lockfile-drift, stalled-review). `code-review-finding` is not on the allowlist — those go through the manual implementer path

## Self-review findings
- none — no `.research/self-improvement/findings.json` file present in the repo

## Open questions
- none new — the pre-existing 3 needs-human-judgement rows still hold (accounting-spec `stalled-review-pr-2555/2558/2559`)

## Quiet day?
- No — 7 PRs merged, 5 new vectors created, 2 plans promoted, 5 new signals derived. `stats.quiet_days` unchanged; `stats.runs` bumped 69 → 70. Cursors advanced: `last_pr_seen` 2820 → 2830, `last_issue_seen` 2816 → 2832, `last_commit_sha` → `7335031`, `review_cursor.reality-web` → 2026-08-22. This run directly addresses the `buffer-low` trigger — 2 landable plans + 3 additional open items refill the cloud-claimable queue
