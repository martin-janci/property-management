# 2026-06-21

## Since last run
- Merged PRs: **100** (#1483–#1631) — 5-day catch-up window. Includes 30 `fix(*)` PRs (heavy enum-cast / IDOR / RLS hardening), 18 `feat(*)`, 17 `test(*)`, 8 `chore(deps)`, 7 `chore(research)` dispatcher state PRs.
- Open PRs touched: **5** newly active (4 dependabot deps, 1 mobile RN app-icon WIP — #1554). No new stalled-review candidates (>7d, undecided) this run.
- New / updated issues: **86** total touched; **7 currently open** — the high-signal ones are #1538 (ci), #1584 (PortalPrincipal follow-up — body explicitly says "core security claim checks out — NOT a block"), #1604 + #1607 (**dispatcher stalled, coverage.json stale**), #1626 (security follow-up, severity low), #1627 (correctness follow-up). All other 79 issues are now closed.
- Commits on `dev`: **202** since `4829015b` → `967ccec0`. 49 are real PR merges (the rest are dispatcher-state / version-bump commits).
- Routine lag: **5d 5h since last run** (last `2026-06-16T03:25Z`, now `2026-06-21T08:30Z`) — `lag_warning` + `stale_routine_alert` fired (>36h + >=3d thresholds). Cloud cron likely paused or broken; operator should verify the scheduler.
- Phases that failed: **none** (Phase 1.5 + Phase 1.6 deliberately skipped during the 5-day catch-up — see *Code review slice* + *Self-review findings*).

## Shipped
*Categorised — 100 PRs is too many to enumerate per row. Cross-reference `git log --since=2026-06-16` for the full list.*

- **🛑 Dev-pipeline rescue (2026-06-16 → 2026-06-17):** #1495 / #1487 / #1499 / #1486 / #1489 / #1492 / #1505 / #1537 / #1551 / #1565 / #1578 / #1486 — sustained enum-cast cascade plus the form-RLS grant + rental_platform_connections enum-decode fix. Unblocked the `dev` backend test pipeline that the (still-open) #1437 / #1426 break created in the last run. The cascade fanned out to #1531/#1532/#1534/#1535 follow-ups (now all closed) and eventually #1605 made `rustfmt` a required check (closes #1592).
- **Security (this run):** #1539 (OAuth reject public client w/ secret), #1552 (OTA token-exchange manager-of-path-org gate, closes #1525), #1561 + #1601 + #1620 (PortalPrincipal hardening on imports), #1580 + #1602 (MFA recovery-code rate-limit + dedicated audit action, closes #1523), #1606 (accounting list_contacts manager-only role gate PAP-281), #1500 (dispute/violation add_evidence/add_comment IDOR — landed via later PRs after the closed-unmerged superseder), #1500 + #1502 (Booking.com credential at-rest encryption IG3), #1517 (canonical seed_membership migration).
- **Tests added** (closing many `follow-up` issues): #1490 (RLS baseline strict fail #1340), #1501 (reality-server feeds IDOR), #1503 / #1506 (booking-oauth seed fixes), #1507 (IDOR test seed_membership restore), #1542 (FavoritesService session-gating + reality-mobile screen-map sync), #1543 (mobile AnnouncementsScreen TZ=UTC stabilize), #1544 (dispute draft auto-save coverage), #1548 (reports presigned download/retry coverage), #1561 (reality-server admit-public-agencies + tests), #1562 + #1599 (XXE/DTD regression guards #1519/#1591), #1563 + #1600 (developer OAuth/usage RLS isolation), #1564 + #1608 (mark_invitation_viewed COALESCE), #1582 + #1618 (useFolderTree wiring), #1611 (ScheduleDetailPage download/retry), #1617 (combined OTA push wiring), #1620 (portal imports caller scope).
- **Bug fixes:** #1485 (Booking.com client timeout + Retry-After + RateLimited), #1491 (mark_invitation_viewed idempotent), #1545 (reality-web /accounting production-build unbreak), #1582 (useFolderTree wiring), #1610 (faults delete + closed_count + refresh stats), #1613 (folder detach-to-root via parent_id=null), #1619 (infinite-scroll double-fetch race), #1621 (payment-matching relocate to ppt-web + correct api-server auth), #1622 (in-app saved-search alerts delivery), #1629 (financial-client auth header), #1631 (api-client coerce financial decimal strings to numbers — TODAY).
- **Refactors:** #1609 (folder-tree flatten utils single-source-of-truth), #1612 (accounting queryKeys factory + drop console.log stub), #1616 (centralized @hey-api auth interceptor + optional auth headers closes #1522), #1624 (ppt-web payment-matching adopts generated SDK).
- **Mobile native (KMP):** #1536 (spotless unused import fix), #1556 (APNs HTTP/2 push delivery wiring), #1619 (infinite-scroll race close).
- **Feat (sizeable):** #1556 (APNs/FCM push), #1558 (e-signature webhook idempotency terminal-state guard), #1559 (announcement comments optimistic add Story 6.3), #1560 (combined OTA rate/availability push partial-failure outcome), #1577 (typed OTA HotelRateAmountNotif models Story 83.2), #1615 (DB-backed Airbnb linked-listing list Coverage 83-1), #1622 (in-app saved-search alerts Story 16.3), #1625 (voting PDF generation + archive PAP-288 Story 5.6/5.8), #1628 (org-wide payment management backend #975.5), #1630 (Payment Management page wiring #975.5).
- **TypeSpec / API model:** #1623 (accounting bank-statements & payment-matching in TypeSpec).
- **Dependabot merges:** #1567, #1568, #1569, #1574, #1572, #1575, #1576, #1571, #1573, #1570 — gradle/cargo/npm minor-patch bumps (no breakage observed; lockfile-drift heuristic did not fire).

## Watch
- **🚨 OPERATIONAL — Routine cron stale 5 days.** The last routine run was 2026-06-16T03:25Z. Today (2026-06-21) is the first run since. Cloud scheduler may be paused/broken — verify the trigger. Dispatcher (separate planning-branch loop) appears to have kept going (200+ commits in window incl. ~30 dispatcher-state PRs), but the daily routine output was silent during the catch-up window.
- **🚨 OPERATIONAL — Dispatcher backlog exhausted (#1604, #1607).** The autonomous dispatcher is stalled because `coverage.json` is dated 2026-06-16 — every "gap" it surfaces is already shipped on `dev`. Run the on-demand local `/ppt-project-management scan` to refresh `coverage.json`, then drain the 14 quarantined rows. Both issues escalate the same root cause from the dispatcher itself.
- **🚨 OPERATIONAL — Required-check gap (#1538).** Backend `test` job is not in `required_status_checks` on `dev`. Red-test PRs can be (and have been) squash-merged — this is the systemic root cause of the multi-round #1331/#1332/#1008 cascade. **Recommend ranking #1538 ahead of feature work.** (Note: #1605 made `rustfmt` required this run, but `test` itself still isn't.)
- **Reverts:** none this run.
- **Closed-not-merged this run:** 9 PRs (#1496, #1497, #1498, #1500, #1503, #1504, #1505, #1506, #1511) — all superseded by alternative landed fixes during the test-cascade rescue. None are residual gaps; they're audit trail only.
- **Churn hotspots this run (top 3, after exclusions):**
  - `backend/crates/integrations/src/booking.rs` (4365 lines, cumulative re-emit — already on backlog at score 1; new evidence appended)
  - `backend/crates/db/src/repositories/document.rs` (3077 lines, cumulative re-emit — already on backlog at score 1)
  - `backend/crates/db/src/repositories/rental.rs` (2837 lines, **NEW** churn-hotspot signal +1) — driven entirely by the enum-cast cascade response to PR #1008's schema drift.
- **No `repeated-churn` fired:** none of the top 3 file paths were in `hotspot_history` with `runs_seen >= 1` under the slug-key match. (Legacy slash-form ids exist for related paths — those won't stack with the new hyphen-form ids.)

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **none — Phase 1.5 deliberately skipped this run.**
- Reason: 5-day catch-up window with 100 merged PRs already consumed the routine's analysis budget. The rotating-expert review is a heavy skill invocation; running it on top of the catch-up would have starved the brief itself. `state.review_cursor` was **not** advanced for any segment — next routine run will pick up where the rotation left off (oldest unreviewed = `ppt-web-core` at 2026-06-05, ~16d stale).
- Experts: n/a
- Findings: 0
- Next segment: `ppt-web-core` (oldest unreviewed)
- Skipped: **yes** — catch-up budget protection.

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0 — not yet seeded).
- Drift signals this run: **3** (`screen-map-drift-pr-1545-reality`, `screen-map-drift-pr-1555-ppt`, `screen-map-drift-pr-1621-ppt`). Heuristic caveat — #1545 is a production-fix scope, #1555 is feature-wiring on existing routes, #1621 is the biggest legitimate route surface change (payment-matching relocation across both products). Treat as review candidates only; none are promotable plans.
- Drift signals deferred (touched route files but lower priority): #1559 (announcements optimistic add), #1610 (faults delete + stats refresh), #1611 (reports download-retry test), #1630 (financial.tsx). All landed as part of broader feature work; the route is unchanged in surface — only handler wiring.
- Orphan screens this run: **0** (no orphan grep performed; routine ran in catch-up mode without a deep `docs/screens/` walk).
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New (9):**
  - [2] `security-airbnb-listings-readable-by-residents` — Airbnb linked-listing list IDOR (#1626, severity:low, vector=security, confidence=high)
  - [2] `screen-map-drift-pr-1545-reality` — reality-web /accounting (heuristic)
  - [2] `screen-map-drift-pr-1555-ppt` — gap-79-1 faults wiring (heuristic)
  - [2] `screen-map-drift-pr-1621-ppt` — payment-matching relocate (largest route-surface change)
  - [1] `bug-saved-search-alerts-truncation` — list silently truncates at LIMIT 100 (#1627)
  - [1] `triage-issue-1538` — Backend `test` not required check (CI structural gap, **high-impact**)
  - [1] `triage-issue-1604` — Dispatcher backlog exhausted / stale coverage
  - [1] `triage-issue-1607` — Dispatcher stalled / 14 quarantined rows
  - [1] `churn-hotspot-backend-crates-db-src-repositories-rental-rs` — 2837 lines, enum-cast cascade
- **Bumped:** none (all incoming signals matched on either a brand-new id or a cumulative-type re-emit with `score_delta=0`).
- **Decayed:** 6 (`code-review-mobile-native-kmp-deeplink-token-not-url-decoded` 2→1, `code-review-mobile-native-kmp-search-stale-response-race` 2→1, `test-gap-screen-map-drift-pr-1085-reality` 2→1, `test-gap-screen-map-drift-pr-1100-ppt` 2→1, `bug-risky-churn-pr-963-api-main-rs` 2→1, `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` 2→1). All went from 2→1; not dropped.
- **Dropped (9):** `triage-issue-1151`, `triage-issue-951`, `triage-dispatcher-mcp-push-large-file-issue-1014` (the last two ran out the 14-day clock and decayed to 0), plus `churn-hotspot-mobile-native-listing-detail-kt`, `churn-hotspot-mobile-native-search-screen-kt`, `code-review-mobile-native-kmp-deeplink-android-bypasses-shared-router`, `refactor-churn-hotspot-mobile-announcements`, `refactor-churn-hotspot-mobile-announcements-test`, `refactor-churn-hotspot-mobile-documents`.
- **Resolved (4):** `dx-stalled-review-pr-988` (PR #988 merged 2026-06-16), `dx-stalled-review-pr-1179` (PR #1179 merged 2026-06-16), `triage-issue-1331` (issue closed 2026-06-17 by the dev-test cascade rescue), `triage-issue-1380` (issue closed 2026-06-15).

## Plans promoted
- **none.** Highest-scored new item this run (`security-airbnb-listings-readable-by-residents` at score 2 + vector=security + confidence=high) qualifies under the security fast-track (score ≥2 with high confidence). However it sits at the +2 baseline of a single signal — under the fast-track rule it's promotable, but the issue body itself classifies severity as **low** (read-only metadata exposure, not write-path) and recommends a 1-line role check. **Leaving open at score 2 for the implementer to pick up via the existing PR-author follow-up flow rather than authoring a fresh plan** — the issue text already names the file (`sync.rs:118`) and the fix shape (same TenantRole pattern as #1552). Plan-author hand-edit allowed per the routine's "Plan-author hand-edits" rule if the user wants a formal plan.

## Auto-fix
- `no candidate` — no signal met all of confidence=high + score≥3 + allowlist + idempotency. The 3 screen-map-drift signals are confidence=medium with heuristic caveat (Phase 5 requires high). The 5 high-confidence high-impact signals (`security-airbnb-listings-readable-by-residents`, `triage-issue-1538`, `triage-issue-1604`, `triage-issue-1607`, `bug-saved-search-alerts-truncation`) are all `unchecked-todo`/`untriaged-issue` types, which are not in the Phase 5 allowlist for code-touching auto-fix. The dispatcher-stalled signals route to operator action, not routine action.

## Self-review findings
- `.research/self-improvement/findings.json` absent — none

## Open questions
- **Cloud cron status (operator action):** Why did the routine miss 4 daily runs? `state.last_run_iso` jump 06-16 → 06-21 with no intervening file. Check `/.github/workflows/research-routine*.yml` or the equivalent scheduler — likely a workflow/env failure or a Cloudflare-tunnel breakage on the trigger endpoint.
- **Refresh `coverage.json` (operator action):** Run local `/ppt-project-management scan` to regenerate coverage. Until then the dispatcher will continue claiming-0 and quarantining no-op tasks (#1604/#1607). Skipping Phase 1.6's cheap upkeep this run did NOT contribute to the stall — the stall pre-existed; upkeep can't fix a stale scan, only a real scan can.
- **Required-check gap #1538:** Make `test` (the full DB-backed backend suite) a required status check on `dev` — single config change in branch-protection. Without it, the dispatcher's "merge on `check` green alone" path keeps re-reddening the pipeline.
- **PortalPrincipal #1584:** Issue body says the core security claim of PR #1561 verified end-to-end; the open follow-up is documentation/test-coverage of the agency_id=user_id collapse, not a fix. Confirm with the author before re-classifying.

## Quiet day?
- **No.** 100 merged PRs, 3 critical operational signals (#1538, #1604, #1607), 1 new security finding (#1626), 1 correctness finding (#1627), 4 resolved backlog items, 9 dropped via decay, 9 new vectors, 3 churn-hotspot re-emits, 5-day routine lag. `stats.runs` 40 → 41, `vectors_created` 173 → 182.
