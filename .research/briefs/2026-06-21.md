# 2026-06-21

## Since last run
- Merged PRs: **96** (PR#1197 — PR#1655 by `merged_at`, since 2026-06-16T03:25:00Z; range dominated by 5-day catch-up window — actual application code on `dev` is roughly #1500–#1655 across 50 commits on the branch).
- Open PRs touched: **12** (all <5h old at run time — no stalled-review signals).
- New / updated issues: **91 since cursor** (33 with number > #1437). Almost all are CLOSED follow-up items from the dispatcher's post-merge review; **one OPEN bug — #1645 (labeled `bug`) — drives this run's top promotion.**
- Commits on `dev`: 50 since `4829015b` → `716e2b6` (mix of application code + dispatcher state).
- Routine lag: **~125h (5d) since 2026-06-16 03:25 UTC** — `lag_warning` fired (signal id `lag-warning-2026-06-21`, score 0). `stale_routine_alert` raised: state.json had not advanced in 3+ days; the dispatcher (separate loop) is still ticking on `planning`.
- Phases that failed: **Phase 1.5 (rotating expert review) — SKIPPED this run** (operational constraint of this routine invocation; review_cursor untouched, oldest unreviewed segment `ppt-web-core` last-reviewed 2026-06-05 will be picked next run). **Phase 1.6 (project management) — SKIPPED this run** (same reason; `pm_cursor.next_index=5` → next role `pm-security`; `coverage_cursor.next_index=12` untouched).

## Shipped (highlights from 96 PRs)
- **#1625** `feat(voting): PDF generation + archive for vote reports (PAP-288, Story 5.6/5.8)` — **shipped red**: the two new `pdf_report::*` tests have been failing on `dev` since the 2026-06-20 22:24 UTC merge, blocking all backend PR CI (see Watch + Plans promoted).
- **#1622 / #1643** — saved-search alerts: in-app delivery (#983 story 16.3) + the pagination fix that closed reviewer follow-up #1627 within a day.
- **#1628 / #1630 / #1631 / #1654 / #1648 / #1623 / #1624 / #1621 / #1606 / #1612** — `#975` accounting / payment-matching tower: org-wide payment management backend, financial client auth + decimal coercion, invoice PDF backend + UI button, accounting bank-statements + payment-matching TypeSpec model, PaymentMatchingPage relocated to ppt-web with correct auth (closes #1521), manager-only `list_contacts` (PAP-281), accounting queryKeys factory refactor.
- **#1640 / #1644 / #1646 / #1649 / #1652 / #1653** — IoT Epic 14: real-time sensor reading WebSocket channel (Story 14.3), sensor registry + register/edit pages (FR71), 6-locale i18n, IoT alerts page screen-map, per-sensor threshold UI (BIT-148).
- **#1641** `feat(ppt-web): Sentiment & Predictive Maintenance dashboards (Epic 13, Story 13.2/13.3)` — shipped with 5 unchecked verification checkboxes (see Backlog deltas).
- **Security follow-up cycle:** MFA recovery limiter rate-limit (#1580/#1602), OTA manager-gate (#1552/#1601/#1639), portal-import per-agency scoping (#1561/#1620/#1632), `PortalPrincipal` owner-scoping pin (#1603), behavioural cross-user RLS isolation (#1563/#1600), XXE/DTD regression guard (#1562/#1599), public-client `client_secret` rejection (#1539), accounting `list_contacts` manager gate (#1606), `count_accessible_rls` enum-cast fix (#1551).
- **Hotfix cluster:** #1545 (reality-web prod build), #1565 (document get-by-id `u.name` + enum decode), #1578 (booking enum-cast + NULL-fee), #1597 (invoice line rounding header-total guard), #1598 (board_meetings `u.full_name` -> `u.name`), #1613 (folder detach to root), #1617 (combined OTA push wired into route), #1619 (mobile-native infinite-scroll race), #1631 (financial decimal-string coercion), #1639 (Airbnb linked-listing manager-gate).
- **Process / CI:** rustfmt enforced in required check job (#1605), pre-push fmt/clippy ADR (#1529), required-check verification (#1530), pure-frontend PR check-context emission (#1655 — BIT-157).
- **Dispatcher hygiene:** 15 stale quarantined rows cleared (#1637, BIT-137 Lane B wind-down).

## Watch
- **🚨 CRITICAL — Issue #1645 (open, [bug])** — `backend.yml` red on `dev` since 2026-06-20 22:24 UTC, commit `3c63a6d3` (#1625 voting PDF). Both new `pdf_report::*` tests panic at setup (`assert_eq! 500 ≠ 201` in `tests/common/mod.rs:507`), so every backend PR is currently blocked. Promoted to plan this run (see Plans promoted).
- **Reverts:** none.
- **Risky churn:** none — booking.rs / document.rs / rental.rs (top-3 hotspots) were NOT touched by any fix-PR-without-tests this run.
- **Churn hotspots this run (top 3):**
  - `backend/crates/integrations/src/booking.rs` (4295 lines; `runs_seen` 1→2 → `repeated-churn` +1, original `churn-hotspot` re-emitted with +0)
  - `backend/crates/db/src/repositories/document.rs` (3077 lines; `runs_seen` 1→2 → `repeated-churn` +1, original re-emitted +0)
  - `backend/crates/db/src/repositories/rental.rs` (2837 lines; cumulative re-emit, +0)
- **Stalled review:** none (all 12 open PRs <5h old).
- **Closed-not-merged this run:** #1647 (superseded by #1646 — IoT sensor register), #1636 (superseded by #1638 / #1632 — portal-import + principal-extractor split), #1511 (superseded by #1581 rustfmt-only follow-up).
- **Dep updates:** 7 dependabot PRs this run (logged as noise; no scoring).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **(skipped this run — operational constraint)**
- Experts: n/a
- Findings: 0 (no `code-review-finding` signals from Phase 1.5)
- Next segment: **ppt-web-core** (last reviewed 2026-06-05 — oldest unreviewed); pick after that is `mobile-native-kmp` (2026-06-06). `review_cursor` untouched.
- Skipped: **yes — operational constraint, rotating expert review not run this routine invocation.**

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0 — not yet seeded; mobile detection still excluded until `docs/screens/mobile/` lands).
- Drift signals this run: **7** — PRs #1610 (ppt: faults), #1611 (ppt: reports), #1621 (ppt + reality: accounting payment-matching), #1630 (ppt: financial), #1641 (ppt: sentiment + predictive-maintenance), #1654 (ppt: invoice management). Heuristic — review candidates only, none promotable per Phase 3 gate.
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not fetched this run (no `docs/screens/**` edits to trigger; many drift signals indicate the workflow keeps catching the same pattern without being acted on).

## Backlog deltas
- **New:**
  - [3] `bug-ci-red-voting-pdf-1645` — dev backend CI red since #1625; pdf_report tests 500 at setup, blocks all backend merges. Promoted to plan.
  - [3] `security-unchecked-todo-pr-1606` — PR #1606 (accounting `list_contacts` manager gate, PAP-281) merged with 1 unchecked checklist item (`cargo test -p api-server accounting_contacts_authz`).
  - [3] `security-unchecked-todo-pr-1625` — PR #1625 (voting PDF) merged with 4 unchecked checklist items (build/clippy/test boxes). Couples with #1645 — same root cause.
  - [2] `bug-unchecked-todo-pr-1641` — PR #1641 (Sentiment/Predictive dashboards) merged with 5 unchecked verification items (KPI cards, trend SVG, alerts list, etc.).
  - [2] `test-gap-hotfix-no-test-pr-1617` — fix(integrations) wire combined OTA push without a test diff.
  - [2] `test-gap-hotfix-no-test-pr-1639` — fix(api-server) Airbnb linked-listing manager-gate (closes #1626) without a test diff.
  - [2] × 7 `test-gap-screen-map-drift-pr-{1610,1611,1621-ppt,1621-reality,1630,1641,1654}-{product}` — see Screen-map status above (heuristic, review candidates).
  - [1] `closed-not-merged-pr-1647` — superseded by #1646.
  - [1] `closed-not-merged-pr-1636` — superseded by #1638 / #1632.
  - [1] `closed-not-merged-pr-1511` — superseded by #1581.
- **Bumped:** none this run (no existing item received a new scoring signal; the 4 `unchecked-todo` items are all NEW PRs).
- **Decayed:** 6 items lost 1 score (no signals in 14d): `code-review-mobile-native-kmp-deeplink-token-not-url-decoded` 2→1, `code-review-mobile-native-kmp-search-stale-response-race` 2→1, `test-gap-screen-map-drift-pr-1085-reality` 2→1, `test-gap-screen-map-drift-pr-1100-ppt` 2→1, `bug-risky-churn-pr-963-api-main-rs` 2→1, `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` 2→1.
- **Dropped (decayed to 0):** 9 items moved to `status=dropped`: `triage-issue-1151`, `churn-hotspot-mobile-native-listing-detail-kt`, `churn-hotspot-mobile-native-search-screen-kt`, `code-review-mobile-native-kmp-deeplink-android-bypasses-shared-router`, `refactor-churn-hotspot-mobile-announcements`, `refactor-churn-hotspot-mobile-announcements-test`, `refactor-churn-hotspot-mobile-documents`, `triage-dispatcher-mcp-push-large-file-issue-1014`, `triage-issue-951`. (Note: the two mobile-announcements rows would have been resolved by PR #1543's TZ=UTC pin; they happened to decay on the same run because they were both at score 1 with `updated_at = 2026-06-05` — net outcome is equivalent: `dropped` ≡ `done` for backlog purposes.)
- **Repeated-churn signals (cumulative):**
  - `repeated-churn-backend-crates-integrations-src-booking-rs` — NEW +1 (booking.rs runs_seen 1→2).
  - `repeated-churn-backend-crates-db-src-repositories-document-rs` — NEW +1 (document.rs runs_seen 1→2).

## Plans promoted
- `plans/bug-ci-red-voting-pdf-1645.md` — repair the voting-PDF migration/schema regression introduced by #1625; gates on `cargo test -p api-server --test voting_tests pdf_report` going green plus full-workspace + next `backend.yml` push on `dev` going green. · adversarial pass: passed

## Auto-fix
- `no candidate` — none of this run's signals met `confidence=high + score≥3 + allowlist` (the only signal at score≥3 high-conf is `bug-ci-red-voting-pdf-1645`, which is a real code-fix migration repair — explicitly NOT in the auto-fix allowlist; goes through the manual implementer plan path).

## Self-review findings
- none

## Open questions
- Phase 1.5 + 1.6 were skipped this run because of operational constraints on the routine invocation itself (long 5-day catch-up window, large signal set). They should run normally on the next invocation — `review_cursor` and `pm_cursor` are intact and will resume where they left off. If the operator sees this skip pattern recur, that's a real `stale_routine_alert` worth chasing (the cloud cron may be paused).

## Quiet day?
- **No.** 96 merged PRs, 19 new backlog vectors, 1 plan promoted, 1 critical-priority issue surfaced (#1645). Run is NOT quiet — Telegram digest should send.

## Goal violations
- G2 (every run advances state) — PASS once committed (cursors + seen_signals + hotspot_history + stats all advanced).
- G16 (management artifacts) — NO-OP (Phase 1.6 skipped).
- All other goals — re-verified by Phase 6 quality gates before commit.
