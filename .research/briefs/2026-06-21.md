# 2026-06-21

## Since last run
- Merged PRs: **135** (#1440 → #1630, gap-filled across the 5-day window — see signals).
- Open PRs touched: **5** (all dependabot/build except #1554 — RN app-icon generation).
- New / updated issues: **86** total (32 created since #1437 — 6 OPEN, 26 already CLOSED by follow-up PRs).
- Commits on `dev`: **49** (linear; PR count higher because of squash-merge fan-in).
- Routine lag: **5d 2h** since last run (last_run_iso=2026-06-16T03:25Z). `lag_warning` + `stale_routine_alert` fired — cloud cron likely paused while the merge wave landed; daily routine itself was offline.
- Phases that failed: none (Phase 1.5 + 1.6 intentionally skipped this run — see Open questions).

## Shipped (highlights — full list under Merged PRs above)
- **#1465** `fix(mobile-native): iOS stale-response guard, staging deep-link host, orphaned plist, xcconfig drift` — closes-by-reclassification GH #1267 (deep-link plist/entitlement: PR #1298 already wired CFBundleURLTypes + applinks:); fixes the SearchView.swift compile-break (performSearch / scheduleSearch / loadMoreResults restored, 668 LOC rewrite) and the stale-response race on loadMoreResults. Resolves 3 backlog rows in one PR (see Backlog deltas).
- **#1454 + follow-on chain (#1512, #1597, #1612, #1621, #1623, #1624, #1628–#1630)** — native accounting MVP + @hey-api migration shake-out. Now lives in ppt-web (#1621 relocation) with org-wide payment management endpoints (#1628 backend + #1630 frontend). New TODO `Date proximity, IBAN match` at `accounting.rs:128` is the only outstanding correctness gap surfaced this run.
- **#1485** `fix(integrations): Booking.com client timeout + Retry-After + RateLimited (BIT-110, GH #1338)` — touched `booking.rs` (a top-3 churn hotspot) with no test diff; `test_push_429_with_retry_after_returns_rate_limited` named in PR body as an unchecked test-plan item.
- **#1622** `feat(reality-server): in-app delivery for saved-search alerts (#983 story 16.3)` — but follow-up #1627 already filed: list silently truncates at LIMIT 100 with no pagination.
- **#1615** Airbnb linked-listing list route + follow-up #1626: list is readable by any org member incl. residents (IDOR-class).
- **#1625** voting PDF generation + archive (PAP-288, Story 5.6/5.8) — 4 unchecked test-plan items in the PR body.
- **#1561 / #1601 / #1602 / #1603 / #1620** — reality-server PortalPrincipal + OTA manager-gate + airbnb cross-org tests + MFA recovery limiter housekeeping (closes #1300, #1525, #1583, #1584 finding 3).
- **#1605** `ci(backend): enforce rustfmt in the required check job` — closes #1592 (rustfmt was non-blocking). #1538 (backend `test` still not required) still open.
- **#1179** docs(epics) catalog backfill (37 modules) finally merged — was the stalled-review row.

## Watch
- **Research dispatcher stalled (#1607 + #1604).** coverage.json on planning branch is stale; 14 rows are stuck in `quarantined` and the backlog is emitting only no-op (already-shipped) tasks. New `ops-research-dispatcher-stalled` row at score 3 / vector `dx` — needs human cleanup before the dispatcher loop resumes. The 5-day lag in this routine is a symptom of the same outage.
- **#1538 (open, [ci])** Backend `test` job not a required check on `dev` — red-test PRs merge freely and re-red the merge pipeline. Same class as #1592 but for tests, not rustfmt. (Existing backlog row carries this.)
- **#1626 / #1627 / #1584 (open, [follow-up,from-merged-review])** — dispatcher post-merge review created concrete follow-ups; the dispatcher itself can pick them up once #1607 is cleared.
- **Churn hotspots this run (top 3, after exclusions):** `frontend/apps/ppt-web/src/features/accounting/pages/AccountingInvoiceManagementPage.tsx` (3), `backend/crates/integrations/src/booking.rs` (3 — `runs_seen` now 2, repeated-churn fires next run if it stays hot), `frontend/apps/ppt-web/src/lib/queryKeys.ts` (3).
- **Reverts:** none.
- **Closed-not-merged this run:** 10 (#1494–#1511 cluster — backend rustfmt/clippy/dev-compile firefight PRs from 2026-06-17 superseded once the proper fix landed; #1496/#1497/#1498/#1505 etc., see merged-PR list).
- **Dependabot noise (logged, not scored):** #1566, #1570, #1571, #1572, #1573, #1575, #1576.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **none — Phase 1.5 skipped this run**
- Experts: n/a
- Findings: 0
- Next segment: `ppt-web-core` (oldest unreviewed at 2026-06-05, 16d stale) — pick up next run
- Skipped: yes — 5-day catch-up window prioritised observe + backlog upkeep over code-segment review

## Screen-map status
- Total `docs/screens/` files: ~150 (across `ppt/` + `reality/`; `mobile/` not yet seeded → 0)
- Drift signals this run: **3** (#1630-ppt + #1621-ppt + #1621-reality — all in the accounting/payment-matching relocation flow)
- Orphan screens this run: 0 (no spot-check matches against this run's route basenames; heuristic only)
- Last `screen-map.yml` CI run on `main`: green at the time of writing (not re-queried this run — Phase 1.6 skipped)

## Backlog deltas
- **Resolved → done (4):**
  - `bug-ios-deeplink-info-plist-missing` — PR #1465 reclassified #1267; Info.plist + entitlements grep-verified with `CFBundleURLTypes` + `realityportal`.
  - `bug-ios-searchview-uncompilable` (was `ready` with plan) — PR #1465 rewrote SearchView.swift (668 LOC, perform/schedule/loadMore restored). Plan moved to `plans/_archive/`.
  - `code-review-mobile-native-kmp-search-stale-response-race` — PR #1465 added requestSeq guard to loadMoreResults parity with performSearch.
  - `dx-stalled-review-pr-1179` — PR #1179 merged 2026-06-16T17:56Z.
- **New (8):**
  - [3] `ops-research-dispatcher-stalled` (dx, high) — sources Issues #1607 + #1604; recommend cleanup of stale coverage.json + 14 quarantined rows before dispatcher resumes.
  - [2] `risky-churn-pr-1485-booking` (bug, medium) — `booking.rs` 429/Retry-After fix shipped without the named 429 retry-after test.
  - [2] `bug-payment-matching-todo-iban-proximity` (bug, high) — `accounting.rs:128` `// TODO: Date proximity, IBAN match` in payment-matching scoring.
  - [2] `test-gap-voting-pdf-pr-1625` (test-gap, medium) — 4 unchecked test-plan items in PR #1625.
  - [2] `test-gap-screen-map-drift-pr-1630-ppt` (test-gap, medium)
  - [2] `test-gap-screen-map-drift-pr-1621-ppt` (test-gap, medium)
  - [2] `test-gap-screen-map-drift-pr-1621-reality` (test-gap, medium)
  - [2] `test-gap-hotfix-no-test-pr-1539-oauth-public-client` (test-gap, high) — OAuth public-client secret rejection has no regression test.
- **Bumped:** none (every new signal targeted a new id; cumulative `churn-hotspot` for `booking.rs` re-emitted with score_delta=0 per the G4 carve-out).
- **Decayed −1 (6 still open):** 6 items aged past 14d kept open at score≥1 (see backlog.md for current scores).
- **Dropped (decayed → 0, 8):** `churn-hotspot-mobile-native-{listing-detail,search-screen}-kt`, `code-review-mobile-native-kmp-deeplink-android-bypasses-shared-router`, `refactor-churn-hotspot-mobile-{announcements,announcements-test,documents}`, `triage-dispatcher-mcp-push-large-file-issue-1014`, `triage-issue-951` — no new signal in 14 days.

## Plans promoted
- **none this run** — no open item meets the readiness gate (`score ≥ 3` + `confidence` + concrete files + clean evidence + slug uniqueness). The new `ops-research-dispatcher-stalled` is at score 3 but vector=`dx` and the action is operational (human cleanup of planning-branch state), not a code change — explicitly outside the plan path; surfaced in the brief instead.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist + idempotency this run. The OAuth-test-gap signal (#1539) is high-confidence but score 2; the dispatcher-stalled item is score 3 but vector `dx` is out-of-allowlist (no safe mechanical fix).

## Self-review findings
- none (`.research/self-improvement/findings.json` is not present in this checkout)

## Open questions
- **Phase 1.5 (rotating expert review) skipped** — should the next routine run pick `ppt-web-core` (oldest, 16d stale) or churn-align to the accounting/payment-matching cluster (`AccountingInvoiceManagementPage.tsx` + `queryKeys.ts`)? Recommend `ppt-web-core` since the same cluster dominates this run's churn anyway.
- **Phase 1.6 (PM rotation) skipped** — `state.pm_cursor.next_index` left at 5 (pm-security up next). `pm-security` last ran 2026-05-27 (25d ago) — overdue. Consider triggering `text: "pm:security"` on the next run.
- **Dispatcher cleanup** — Issues #1607 + #1604 ask for human triage. The backlog row points at the same files; this is not the routine's job to mechanically fix.

## Quiet day?
- **No.** 135 merged PRs + 4 resolutions + 8 new vectors + 0 plans + 0 auto-fix.
