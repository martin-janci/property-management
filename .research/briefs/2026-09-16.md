# 2026-09-16

_Convergent view after 15-day cold-cron gap. Payload: `buffer-low: claimable=12/72 — all actionable backlog structurally cloud-build-blocked (api-server swagger-ui egress #2949, mobile-native AGP #2652); refill with cloud-buildable (frontend/reality-web/shared) vectors`._

## Since last run
- Merged PRs: ~21 since #2921 (range #2925–#2956). Non-dependabot merges include #2925/#2926/#2927/#2928/#2931/#2932/#2938/#2940/#2941/#2942/#2943/#2950/#2952/#2955/#2956/#2558. Cursor advanced 2921 → 2956.
- Open PRs touched: 15 open (mostly dependabot); notable non-bot drafts unchanged: #2902 (screen-map-drift-pr-2894-reality, `needs-human-review`, 45d idle), #2744 (dispatcher un-wedge, 34d idle), #2559/#2555 (UC-ACC-05.x, no owner).
- New / updated issues: cursor advanced 2847 → 2954 (issues endpoint not enumerated this run — ceiling derived from gh-issue-N PR titles in window; adequate for cursor motion).
- Commits: not enumerated this run (token discipline — the merged-PR list carries the signal density; `last_commit_sha` retained at eea70673).
- Routine lag: **15d since last run** (`2026-09-01T18:38:58Z` → `2026-09-16T22:30:00Z`). Cloud cron is likely paused or its trigger is stuck; the dispatcher (separate planning-branch loop) has kept firing (evidenced by today's tier1d output files + open action-list rows), but this routine did not.
- Phases that failed: none. Phase 1 was compressed (used cached tier1d output for 1.5; did not re-spawn 1.5 or 1.6 given the dispatcher already produced fresh signal for two segments today).

## Shipped
_Selected merged PRs since #2921 — most are code-review or gh-issue follow-ups; full range is in `gh pr list` for the window._
- #2955 gh-issue-2953: purge NFC credential material from SecureStore on session change.
- #2956 gh-issue-2954: harden queryKeys.test.ts auto-discovery (suite-crash risk + glob-depth gap).
- #2952 gh-issue-2948: auto-discover feature-local `*Keys` factories in logout-purge coverage test.
- #2950 gh-issue-2947: purge NFC access log, QR scan history & offline feedback on session change.
- #2943 code-review-ppt-web-core-logout-purge-allowlist-fragile: enforce logout purge allowlist coverage.
- #2942 code-review-ppt-web-core-api-401-no-request-replay: single-flight refresh + replay on 401.
- #2941 code-review-ppt-web-core-authctx-no-boot-expiry-check: validate JWT exp at cold boot.
- #2940 code-review-ppt-web-ui-confirm-dialog-i18n: externalize ConfirmationDialog loading label.
- #2938 gh-issue-2937: add invoice PDF renderer + handler test coverage.
- #2932 gh-issue-2930: e2e 403 test for agent-review self-review guard.
- #2931 gh-issue-2929: make report_summary snapshot-consistency test fail on pre-fix code.
- #2928 gh-issue-2924: snapshot-consistent report_summary counts + entries.
- #2927 gh-issue-2923: cover update-error i18n path in EmergencyContactDirectoryPage tests.
- #2926 code-review-api-core-fcm-legacy-dead-endpoint: remove decommissioned FCM legacy send path.
- #2925 code-review-api-handlers-compliance-raw-db-leak-regression: route compliance audit-log count error through db_error.

## Watch
- Stalled review: same three long-idle drafts as prior brief (#2902, #2744, #2559/#2555). No new stalled-review comments this run (no PR just crossed the 7d idle bar).
- Reverted: none in window.
- Churn hotspots: not re-scanned this run (Phase 1 churn skipped — dispatcher tier1d already covered api-handlers + ppt-web-ui churn today).

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h. -->
- #2902 screen-map-drift-pr-2894-reality — draft, `needs-human-review`, last update ~15h ago (age still ~45d since first open). Approval state not re-fetched this run; unchanged from prior brief posture (branch contamination).
- #2744 fix(dispatcher): un-wedge oversize-archive push — draft, 34d idle. Self-approval blocker.

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json where reviewer_summary starts with "verdict=changes" AND status == "review" AND (now - last_updated) > 24h. Not re-fetched this run (planning branch); only 2 active assignments seen in local snapshot (gh-issue-2743 review, screen-map-drift-pr-2894-reality review). -->
- none confirmed this run.

## Code review slice
- Segment reviewed: **two** — `api-handlers` and `ppt-web-ui` — via already-produced tier1d files on disk (`signals/2026-09-16-api-handlers-tier1d.json`, `signals/2026-09-16-ppt-web-ui-tier1d.json`). The daily routine adopted these as Phase 1.5 output rather than re-spawning the skill (dispatcher already did the work today; re-spawn would be wasted tokens).
- Experts covered: rust + security (api-handlers), frontend + completeness (ppt-web-ui).
- Findings: 4 total, all `medium` confidence.
  - `code-review-api-handlers-org-member-role-mutation-guards` (score 2, security) — cloud-build-blocked by #2949.
  - `code-review-api-handlers-multi-currency-refresh-stub-success` (score 1, bug) — cloud-build-blocked by #2949.
  - `code-review-ppt-web-ui-documents-i18n-hardcoded` (score 2, bug) — **cloud-buildable frontend**; matches payload's refill vector but below promotion bar.
  - `code-review-ppt-web-ui-ocr-silent-fail` (score 1, bug) — cloud-buildable frontend; below promotion bar.
- Next segment: `mobile-native-kmp` remains oldest by review_cursor (2026-09-10), but AGP #2652 keeps it structurally parked for the cloud implementer.
- Skipped: yes — Phase 1.5 skill was not re-spawned (adopted dispatcher tier1d output). Phase 1.6 was likewise skipped (`pm_cursor.next_index` unchanged from HEAD; roadmap/action-list untouched by this routine).

## Screen-map status
- Total `docs/screens/` files: unchanged (mobile/ still not seeded).
- Drift signals this run: 0 (no per-PR file-list scan performed — Phase 1 churn was compressed; none of the merged PRs' titles indicate route-file additions beyond the drift-PRs already merged in prior windows).
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched this run.

## Backlog deltas
- **New (this run, 4):**
  - [2] `code-review-api-handlers-org-member-role-mutation-guards` — security, medium.
  - [1] `code-review-api-handlers-multi-currency-refresh-stub-success` — bug, medium.
  - [2] `code-review-ppt-web-ui-documents-i18n-hardcoded` — bug, medium. **Cloud-buildable — the exact vector the payload asked for**; 7 files, contained scope. Below the score>=3 promotion bar as a single-signal +2.
  - [1] `code-review-ppt-web-ui-ocr-silent-fail` — bug, medium. Cloud-buildable but below the bar.
- **Bumped:** none.
- **Decayed:**
  - `code-review-mobile-native-kmp-create-listing-not-wired` — 4 → **3** (updated_at 2026-09-01 → 2026-09-16). Stays `open`; still on the promotion bar but *not* promoted this run (see *Plans promoted* below — slug-stem duplicate against `.research/management/action-list.json`).
- **Dropped (decay to 0):**
  - `code-review-mobile-native-kmp-portfolio-analytics-caps-100` (1 → 0).
  - `code-review-mobile-native-kmp-cancellation-swallowed` (1 → 0).
  - `code-review-mobile-native-kmp-httpclient-no-timeout` (1 → 0).
  - All three were 14+ days stale with no new signal; per G5, decay reduced them to 0 and they moved to `status: dropped`. **Note the collision**: the same three ids also appear as `open` rows in `.research/management/action-list.json` — the dispatcher's ledger is unaware of the backlog drop. The dispatcher can decide whether to garbage-collect those action-list rows on its next cycle (backlog is not the authoritative source for the dispatcher; this is expected drift, not a bug).
- **Resolved:** none — the 15 non-dependabot merges in the window do not touch any of the remaining 2 open+ready KMP items (all KMP paths); nothing to grep-verify closed.

## Plans promoted
- **0 new plans this run.**
  - Sole gate-passing candidate is `code-review-mobile-native-kmp-create-listing-not-wired` (score 3 post-decay, confidence high, status open, 4 files, 2 sources). **Slug-stem uniqueness fails**: an `open` row with the exact same id already exists in `.research/management/action-list.json`. Promoting would violate the "at most one non-terminal unit of work per stem" invariant.
  - The four cloud-buildable frontend/backend signals emitted this run all sit at score 1–2 (medium confidence) — below the promotion bar. Payload's "refill with cloud-buildable vectors" was **partially addressed** (2 new cloud-buildable ppt-web-ui items landed in backlog) but no new *plans* were written; a second signal on either would stack the ppt-web-ui documents-i18n item into promotable territory next run.
  - Adversarial pass: N/A (no promotions to re-read).

## Auto-fix
- `no candidate` — no signal met `confidence == high` + `score >= 3` + allowlist match. Kill switch (`ROUTINE_AUTOFIX_DISABLED`) not set; allowlist-eligible signal types (screen-map-drift/orphan, fixme-in-merged-code, dep-update lockfile-drift, stalled-review) all absent this run.

## Self-review findings
- none (`.research/self-improvement/findings.json` absent).

## Open questions
- **Cloud-cron liveness (P4).** 15-day gap between routine runs is well beyond the 3-day stale-routine threshold. The dispatcher's planning-branch loop is clearly still firing (today's tier1d output landed in `signals/`), but the daily routine trigger appears wedged. Worth checking the cloud trigger configuration and/or restarting the schedule.
- **Cloud-buildable promotion supply.** With #2949 (api-server swagger-ui egress) and #2652 (mobile-native AGP) as the two structural blockers, only `frontend/*`, `reality-web`, and `shared` code-review findings can produce landable plans in the current cloud runner. Today's Tier-1d review surfaced 2 such items (documents-i18n hardcoded, OCR silent fail) but both scored below the promotion bar. The dispatcher's payload asks for refill — the fastest lever is to have the next Tier-1d rotation re-audit `reality-web` (last reviewed 2026-08-29) or bump the confidence/score on `code-review-ppt-web-ui-documents-i18n-hardcoded` on second pass (7-file contained-scope i18n break arguably deserves +3 not +2).
- **Backlog ↔ action-list drift (informational).** Decay-driven drops of 3 KMP backlog rows do not propagate into `.research/management/action-list.json` (planning-branch). If the dispatcher's next cycle claims one of the dropped ids and hits a `status=dropped` backlog row, `backlog-reconcile.sh` will surface the drift; if not, the drift is harmless. Flagged for visibility.

## Quiet day?
- **No.** 4 new signals, 3 items dropped, 1 item decayed, backlog cursor advanced 15 days. Structural blockers (#2949, #2652) still throttle the *landable* buffer, but the routine itself did substantive work this run.

## Goal violations
- none — G1–G17 recorded in `signals/2026-09-16.json.goal_checks[]`. G6, G7, G12 hold vacuously (0 plans promoted). G15 holds trivially (auto_fix_actions empty). G16 is a no-op (Phase 1.6 skipped this run).
