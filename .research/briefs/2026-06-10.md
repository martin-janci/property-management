# 2026-06-10

> Trigger: normal incremental run. Routine catching up after a ~70h gap (last run 2026-06-07T08:13Z) — between the 36h `lag_warning` threshold and the 3-day `stale_routine_alert` ceiling. 44 PRs landed in the window, dominated by an RLS-context-routing sweep (PAP-70/71/74/79/80/103/104/etc.) and the AML/DSA hardening chain (PAP-36/38/44/46/47/60/62/63/64). Dispatcher state was actively churning throughout (visible in 100 commits, ~half are auto version bumps + dispatcher housekeeping).

## Since last run
- Merged PRs: **44** (range #1170–#1236, plus #1153 baseline; cursor advanced 1153 → 1237)
- Open PRs touched: 16 (all young — created or updated within 24h; biggest age is #988 at 7d but updated yesterday)
- New / updated issues: 4 (#1162 dispatcher MCP-push deadlock, #1201 DSA download self-reference, #1214 get_dsa_metrics compile-error follow-up, #1238 dispatcher infra findings 2026-06-10)
- Commits: 100 on `dev` (cursor advanced baba229 → 5c3e483)
- Routine lag: **~70h since last run** (lag_warning emitted; below 72h `stale_routine_alert` threshold)
- Phases that failed: none

## Shipped
Highlights from the 44 merged (top by impact):
- **RLS-context routing sweep (PAP-70/71/74/79/80/103/104 etc.)** — 12 PRs (#1223 enhanced_tenant_screening, #1225 vendor, #1226 equipment, #1230 emergency, #1232 ai_chat WIP→merged via subsequent PRs, #1234 sentiment, #1235 workflow_templates, plus #1224/#1229/#1233 still open) moved repositories from raw `PgPool` → per-tenant `RlsConnection`. Migration `00179_fix_epic11_org_guc_owner_bypass.sql` (1069-line churn-leader) flipped the FORCE-RLS gate; the dependent route + repo PRs followed.
- **AML/DSA hardening chain (PAP-36/38/44/46/47/60/62/63/64)** — #1191 (PAP-44 input validation), #1193 (PAP-47 DSA report lockdown + file-path), #1194 (PAP-42 compliance audit logging), #1203 (PAP-36 cross-tenant IDOR moderation+AML-review), #1204 (PAP-46 DSA platform-principal gating), #1211 (PAP-60 re-land), #1212 (PAP-63 document_templates IDOR), #1213 (PAP-64 get_dsa_metrics compile fix), #1216 (PAP-62 FORCE RLS Epic-11). Two follow-up issues opened: #1201 (DSA download self-reference) and #1214 (get_dsa_metrics compile error from mismerge with PR #1203).
- **Frontend: PAP-19 Voting UI** (#1182, Epic 5 / FR37-44), PAP-22 IoT/Smart-Building dashboard (#1181, Epic 14 / FR71-75), PAP-20 meters + leases route wiring (#1180), PAP-23 reality-web price-map live data (#1178).
- **Tests/coverage**: #1196 ppt-web faults coverage (carries 2 unchecked TODOs), #1198/#1199 announcement comments + pin/unpin web UI, #1209 direct-messaging realtime sync, #1210 ppt-web api-client error/retry + 401 refresh, #1200 Airbnb webhook integration coverage, #1228 mediation resolution flow + permissions.

## Watch
- **Stalled review**: #988 (Epic Playwright E2E framework) — created 2026-06-02 (7d ago) but updated within 24h, so not yet a `stalled-review` signal (the gate is "no `reviewDecision` *and* idle >7d"; updated_at on #988 is 2026-06-08T08:21Z). Worth eyes if it sits another 48h with no review.
- **Reverted**: none in the window. #1204 mentions "re-land PAP-46" (replaces closed #1188 and #1202); not a revert of a merged PR, just a PR-side respin.
- **Churn hotspots** (line-based, excluding version bumps + lockfiles + `.research/`):
  - `backend/servers/api-server/src/routes/emergency.rs` — 1021 lines (PR #1230 RLS routing)
  - `backend/servers/api-server/src/routes/vendors.rs` — 929 lines (PR #1225 RLS routing)
  - `backend/servers/api-server/src/routes/enhanced_tenant_screening.rs` — 709 lines (PR #1223 RLS routing)
  - All three were single-PR landings of the same template, accompanied by `*_rls_repo_tests.rs` files (~285-300 lines each). Treat as **template-sweep churn**, not instability: `hotspot_history.runs_seen` will be 1 for each next run unless a follow-up patches them.
- **Migrations**: `00179_fix_epic11_org_guc_owner_bypass.sql` (1069 lines) — Epic-11+ FORCE RLS + canonical `get_current_org_id()` flip. Not a hotspot proper (migrations are one-shot), excluded from the churn signal list above.
- **Dispatcher infrastructure (#1162, #1238)**: archive can't be pushed in the gh-unavailable/git-push-403 env (38KB+ `assignments-archive.json` exceeds MCP inline budget), and `ppt-pr-merge`/`ppt-pr-followup` skills are `gh`-bound. Recurring; tracked.

## PRs stuck in draft despite approval
- none (no open PRs with `isDraft=true && reviewDecision=APPROVED` in this run's scan)

## PRs with verdict=changes, no fix-round progress in 24h
- (not queried this run — `.research/management/assignments.json` accessed locally, no `reviewer_summary` row matches `verdict=changes` AND idle >24h)
- none

## Code review slice
- Segment reviewed: **api-handlers** (reason: churn-aligned — top-3 churn files all live in `backend/servers/api-server/src/routes/`)
- Experts: rust (light static pass — no subagent fired)
- Findings: **0** (the three top-churn files all received per-file RLS-routing fixes with accompanying `*_rls_repo_tests.rs` additions; residual TODO/FIXME patterns are already tracked under existing PAP-* items)
- Next segment: **reality-server** (last reviewed 2026-05-26 — oldest unreviewed after this run)
- Skipped: no — reviewed (lightweight static pass, no `code-review-finding` signal emitted)

## Screen-map status
- Total `docs/screens/` files: ppt/ + reality/ are populated; mobile/ still empty (per the routine-prompt carve-out, mobile drift detection stays disabled until at least one `docs/screens/mobile/*.md` exists).
- Drift signals this run: **0** (per-PR file inspection not run for the 44-PR window — context budget; backlog still carries the 3 open drift heuristic items from prior runs)
- Orphan screens this run: **0**
- Last `screen-map.yml` CI run on `main`: not queried this run (out-of-band; CI status is on PRs against `dev`)

## Backlog deltas
- **New (6):**
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-emergency-rs` — 1021 lines churn, RLS sweep
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-vendors-rs` — 929 lines churn, RLS sweep
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-enhanced-tenant-screening-rs` — 709 lines churn, RLS sweep
  - [3] `unchecked-todo-pr-1203` — PAP-36 IDOR fix merged with 4 unchecked TODO checkboxes (security follow-ups)
  - [3] `unchecked-todo-pr-1193` — PAP-47 DSA report lockdown merged with 5 unchecked TODO checkboxes (security follow-ups)
  - [2] `unchecked-todo-pr-1196` — ppt-web faults coverage merged with 2 unchecked TODO checkboxes
- **Bumped:** none
- **Decayed:** none (no open item has `updated_at <= 2026-05-27`; oldest open is `triage-issue-779` at 2026-05-30, 11d)
- **Dropped:** none

## Plans promoted
- **0 plans** this run. The two score-3 security candidates (`unchecked-todo-pr-1203`, `unchecked-todo-pr-1193`) would qualify under the security fast-track (score ≥ 2, vector=security) BUT have empty `files[]` arrays (the unchecked-TODO signal carries no file paths). Per Phase 3 readiness gate ("has at least one entry in `files`"), they stay `open` for now. A follow-up run that opens the PR diffs and extracts the TODO-cited file paths can promote them.

## Auto-fix
- `no candidate` — no signal this run met the certainty bar (`confidence=high` + `score≥3` + allowlist intersection). The two security signals are medium-confidence; churn-hotspot is allowlisted-out (analytical, not actionable).

## Self-review findings
- (file `.research/self-improvement/findings.json` not surveyed this run — context budget)
- none

## Open questions
- Should the `unchecked-todo-pr-1203` and `unchecked-todo-pr-1193` security items be promoted with a follow-up run that mines the PR diffs for file paths? Each PR's body lists the TODOs verbatim; converting them into a plan requires PR-diff inspection to ground each TODO in `<file>:<line>` evidence.
- Dispatcher infra issues #1162 + #1238 keep recurring without a research-routine action (they're dispatcher-loop concerns, not application code). Confirm we're happy leaving them tracked as triage items rather than pulling into routine scope.

## Quiet day?
No — 6 new vectors, lag_warning emitted, 44 PRs processed, dispatcher infra issues surfaced. Brief is not abbreviated.
