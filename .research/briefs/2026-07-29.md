# 2026-07-29

## Since last run
- Merged PRs: **7** (range #2433–#2554) — the two-day catch-up wave: layout-vertical went from spec-only events to fully-wired producers + sink, iOS shipped the shared resolved layout, three long-open reviews landed together at 01:40Z, plus BIT-313 signature-request routing and a dependabot bump.
- Open PRs touched: 4 (three brand-new accounting-server PRs — #2555/#2558/#2559 — opened 21:24–21:58Z by the same author; #2482 refactor-churn repo-map.md still open at 6d).
- New / updated issues: 3 — **#2557** (dev-team follow-ups on #2487 test-binary consolidation, `backend` label), **#2556** (reality-api-client drift gate deferred from #2487, `ci` label), **#2532** (layout event tracking) closed by #2549.
- Commits: 7 on `dev` (matches merged PRs; no direct pushes).
- Routine lag: **24h since last run** (2026-07-28T03:20Z → 2026-07-29T03:22Z) — on cadence, no `lag_warning`.
- Phases that failed: none.

## Shipped
- **#2504** `fix(api-server): signature-request list/create unreachable — mount as document sub-resource (BIT-313)` — reroutes list/create to `/api/v1/documents/{id}/signature-requests` so `Path<Uuid>` binds; adds happy-path tests (create 201, list 200 empty + populated).
- **#2553** `code-review-ppt-web-core-authctx-init-stale-role: route AuthContext cold-boot init through refreshTokenInternal` — closes the #574 gap; regression test `AuthContext.init-refresh.test.tsx` seeded a cold-boot session and asserts fresh-role derivation. **Resolves backlog item** `code-review-ppt-web-core-authctx-init-stale-role`.
- **#2491** `chore(deps): bump the npm-minor-patch group across 1 directory with 5 updates` — react 19.2.0→19.2.8, react-dom+react-test-renderer bump, @types/react 19.2.14→19.2.17, next-intl 4.13.3→4.13.4; lockfile had to be regenerated to fix `ERR_PNPM_LOCKFILE_CONFIG_MISMATCH` (dependabot pinned react-test-renderer at 19.2.8 while `package.json` overrides said 19.2.0).
- **#2554** `chore(research): refill starved dispatcher stack — 7 new vectors, 14 promoted` — yesterday's own routine commit landed to `dev`.
- **#2433** `feat(mobile-native): iOS listing detail renders through the shared resolved layout` — SwiftUI dispatch over ResolvedLayoutScreen from the KMP shared package; seeds from `DEFAULT_LISTING_DETAIL_LAYOUT`, dedup + cancellation-propagation tests, unknown-type warn-once log.
- **#2478** `fix(layout): review-hardening sweep — authz, publish TOCTOU, webhook replay, defensive rendering` — the big one: DB-validated role gate on tenant PUT, publish TOCTOU guard via `AND draft=$3` + new `LayoutPublishError::DraftChanged`→409, nil-org sentinel, canonical override JSON, timestamped webhook HMAC (`{ts}.{body}`), reality-web 16KiB body cap + 300s skew window, connection-ordering fix. Introduced `internal_error()` helper in admin.rs + tenant.rs — **but skipped resolved.rs** (see *Backlog deltas*).
- **#2549** `gh-issue-2532: wire layout publish/webhook/revalidate event emission + sink` — closes #2532 gaps A/C/D; new migration 00225 (append-only `layout_change_events` sink, 730d retention), `LayoutRepository::record_change_event`, delivery_id threaded through the notifier, reality-web `trackEvent` on every return path.

## Watch
- **New accounting-cluster PRs (#2555/#2558/#2559)** all opened by the same author within 34 minutes and with intra-PR dependencies: #2555 (invoice sent/cancelled lifecycle) adds `InvoiceStatus::{Sent,Cancelled}` enum variants that #2558 (invoice PDF) and #2559 (PAY-by-square QR) both consume. Merge order matters — #2555 first. Surfaced as a risk in `.research/management/risks.json` this run.
- **Stalled review: #2482** `refactor-churn-hotspot repo-map.md` — 6 days open, last update 2026-07-28T21:05Z (base biome-lint red on dev pre-existed this branch). Just under the 7d `stalled-review` threshold; will fire next run if still open.
- **Reverted:** none.
- **Churn hotspots:** all three top-3 hotspots this run are `backend/servers/api-server/src/routes/layout/*.rs` — direct consequence of #2478 + #2549 landing in one window:
  - `layout/tenant.rs` — 262 lines churn, runs_seen=1
  - `layout/admin.rs` — 240 lines churn, runs_seen=1
  - `layout/webhook.rs` — 217 lines churn, runs_seen=1

## PRs stuck in draft despite approval
- none — #2549 (the perennial stuck-in-draft item from prior briefs) merged this run.

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **api-handlers** (reason: churn-aligned — three top-3 churn hotspots this run all live under `backend/servers/api-server/src/routes/layout/*.rs`).
- Experts: Rust (+ Security lens).
- Findings: **3** — one **Medium** security (`resolved.rs` leaks raw sqlx/serde error text: PR #2478's `internal_error()` defensive pattern was applied to admin.rs + tenant.rs but skipped resolved.rs), one **Low** bug (`admin.rs` coerces `extract_super_admin_token`'s 401 into 403 in all 11 handlers — breaks SDK token-refresh semantics), one **Low** refactor (`resolved.rs` still uses unvalidated TenantExtractor for org_id + nil-org check while sibling tenant.rs migrated to `rls.tenant_id()`).
- Next segment: `api-core` (last reviewed 2026-07-20, will be oldest-unreviewed after this run advances `api-handlers` to 2026-07-29).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: **168** (ppt/=106, reality/=47, reality-mobile/=15; mobile/ still not seeded — 0).
- Drift signals this run: **2** — both false-positive candidates: PR #2478 and PR #2549 each touched `frontend/apps/reality-web/src/app/api/layout-revalidate/route.ts` (Next.js Route Handler — an internal webhook receiver, not a user-facing screen). Same class as the already-open `screen-map-drift-pr-2497-reality` from a prior run.
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not queried (no genuine drift to root-cause; both signals are heuristic false positives on `app/api/**` paths).

## Backlog deltas
- **New:** [5→5 unchanged] all pre-existing high-priority open items unchanged. Fresh items:
  - **[2]** `code-review-api-handlers-resolved-leaks-internal-errors` (security, high) — **promoted to plan** (see below). PR #2478's hardening sweep skipped resolved.rs; err500 closure leaks raw sqlx/serde error text on 5xx.
  - **[2]** `screen-map-drift-pr-2478-reality` (test-gap, medium) — false-positive candidate: internal API route change, not a screen.
  - **[2]** `screen-map-drift-pr-2549-reality` (test-gap, medium) — same class as above.
  - **[1]** `code-review-api-handlers-admin-401-coerced-403` (bug, high) — 11 admin handlers flatten 401→403 discarding `extract_super_admin_token`'s real status; breaks SDK token-refresh.
  - **[1]** `code-review-api-handlers-resolved-unvalidated-tenant` (refactor, medium) — sibling tenant.rs migrated to `rls.tenant_id()` in #2478; resolved.rs still uses unvalidated TenantExtractor.
  - **[1]** `churn-hotspot-layout-tenant-rs-2026-07-29` (refactor, high) — 262 lines, first-time hotspot.
  - **[1]** `churn-hotspot-layout-admin-rs-2026-07-29` (refactor, high) — 240 lines, first-time hotspot.
  - **[1]** `churn-hotspot-layout-webhook-rs-2026-07-29` (refactor, high) — 217 lines, first-time hotspot.
- **Bumped:** none — no existing item's ID matched an incoming signal.
- **Decayed:** none — all open items updated within 14 days (oldest open is `refactor-churn-hotspot-repo-map-md-2026-07-20` at 9d).
- **Resolved (→ done):** `code-review-ppt-web-core-authctx-init-stale-role` — PR #2553 verbatim ID match in title, and the pre-fix inline refresh block at `AuthContext.tsx:394-411` no longer exists (routed through `refreshTokenInternal`). Table row: PR #2553 matched, evidence gone → done.
- **Dropped:** none.

## Plans promoted
- `plans/code-review-api-handlers-resolved-leaks-internal-errors.md` — copy admin.rs's `internal_error()` helper into resolved.rs (or lift to `layout/mod.rs`), replace 8 `err500(format!(...))` call sites with `.map_err(internal_error)`, add `layout_resolved_error_leak_tests.rs` asserting the 500 body is generic. Security fast-track (score=2, confidence=high, vector=security). · adversarial pass: passed.

  **Three other candidates blocked at Phase 3 by stem-collision:** `security-voice-webhook-alexa-signature-not-verified` (score 5), `repeated-churn-api-server-routes-auth-rs-2026-07-28` (score 3), and `dx-api-validation-drift-gate-never-runs-on-dev` (score 3) each have an *open* row in `.research/management/action-list.json` (dispatcher-backlog-refill from PR #2554). Per the slug-stem uniqueness rule, promoting a plan under the same stem while the dispatcher path is active would double-count the work. They stay in backlog at open status; if the dispatcher's implementer path stalls or is cancelled, the next routine run can promote them.

## Auto-fix
- **no candidate** — the four PR-shape allowlist actions (screen-map-drift stub, screen-map-orphan remove, lockfile-drift bump, fixme-in-merged-code issue) either didn't fire (no orphan, no new FIXME) or fired only as false-positive candidates (the two `screen-map-drift-pr-{2478,2549}-reality` signals cite internal API routes, not user-facing screens — Phase 5 shouldn't auto-stub a screen doc that shouldn't exist). No `stalled-review` signal this run either (#2482 at 6d, below the 7d threshold). `dep-update-noise-pr-2491` fired but not as lockfile-drift — dependabot correctly regenerated the lockfile in a follow-up commit on the same PR.

## Self-review findings
- open=21 (high=4, medium=13, low=4); new this run=0; acknowledged=19
- fp-mcp-push-cannot-inline-large-archive [recurrence=10] — dispatcher archive-push exceeds mcp push_files inline limit → merged-total undercount; fix: adopt push-to-branch → PR → server-side merge as the primary large-file push mechanism in Phase 6
- fp-hardcoded-proxy-403-push-assumption [recurrence=6] — dispatcher assumes `git push` is proxy-blocked → forces MCP path even where direct push works; fix: runtime probe at Phase 1, set PUSH_METHOD=git on success, bypass the 64KiB inline guard
- fp-action-list-open-count-blind-to-archive [recurrence=3] — Phase 2.6 buffer-guard subtracts only active assignment ids (not archived terminal ids), so buffer nears starvation while metric reports healthy; fix: gc1-reconcile auto-close open items terminal-in-archive AND fix GC3 formula to subtract archive ids
- fp-tier2-planner-auth-swallow [recurrence=2] — Tier-2 401/403 from planner kick folded into generic "kick failed" log; fix: branch on status, treat auth failures as OPERATOR-CONFIG escalation surfaced loudly in Phase 7

## Open questions
- **Should the routine promote `security-voice-webhook-alexa-signature-not-verified` even when the dispatcher has already claimed it?** The Alexa-signature bypass is the highest-score open item (5, security, high-confidence) and has been on the list since 2026-07-28. The dispatcher path may not carry the same forensic detail a routine-authored plan would (Alexa signature spec, cert chain fetch, timestamp window). Worth clarifying whether stem-collision should hard-block or just add a "co-tracked" annotation.
- **`docs/screens/mobile/` still empty.** Carried from prior briefs — bootstrapping would unblock drift detection on RN mobile.
- **PM digest reported epics_done=3/5**, but `_bmad-output` epics catalog has 13 distinct epics tracked in `coverage.json`. Confirm the sprint scope framing before treating "3/5" as the delivery metric in the Telegram digest.

## Quiet day?
- **No** — genuinely busy: 7 PRs merged, 8 new backlog vectors (including 3 code-review findings and 3 first-time churn hotspots on `layout/*.rs`), 1 plan promoted (`resolved-leaks-internal-errors`), pm-backend rotating role ran after 53 days, PM PR-cluster risks surfaced. `stats.quiet_days` unchanged (stays at 2).
