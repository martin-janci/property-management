# 2026-07-11

## Since last run
- Merged PRs: 20 (range #2186–#2225)
- Open PRs touched: 8 (7 dependabot #2207–#2212 + auto-fix draft #2213; #1797 long-lived draft + #2018 long-lived dependabot unchanged)
- New / updated issues: 10 in window (2 open follow-ups #2195, #2204; 7 post-merge review follow-ups #2196–#2203 closed by their fix PRs; CI wedge #2222 closed by PR #2223; i18n #2224 open); last_issue_seen advanced 2087 → 2224
- Commits: 50 on `dev` since 5877ade (about half `chore(research): dispatcher …` scheduler chores; the other half feature/fix PRs)
- Routine lag: 62h (2d 14h) since last run — over the 36h threshold, second consecutive lag_warning (previous was 3d 21h); still under the 72h stale-routine alert
- Phases that failed: none — Phase 1.6 (`ppt-project-management`) intentionally skipped this run (dispatcher keeps `.research/management/` fresh; `pm_cursor` not advanced so G16 is a no-op); Phase 1.5 ran (mobile-native-kmp) and returned 2 findings

## Shipped
- #2225 fix(common) — update sitemap `test_metadata` `ppt_web_routes` count 13 → 19 to unblock CI (the sitemap test was the last of 8 blocked PRs)
- #2223 gh-issue-2222 — shard backend CI `test` job to end the `-p common --lib` timeout wedge (was blocking 8 approved PRs; `dev` baseline was ~119 min)
- #2221 fix(api-server) — harden RAG embedding-write path (Closes #2201: connection-holding, provider provenance, FK error shape)
- #2220 fix(api-server) — validate report_id / timezone and set next_run_at on schedule create (Closes #2198)
- #2219 gh-issue-2203 — cover OAuth CSRF replay/single-use rejection at handler level
- #2218 fix(reality-server) — scope per-listing analytics reads to portal owners via SECURITY DEFINER (Closes #2199 — RLS gap: analytics rows silently filtered out for portal-owned listings; new migration 00213)
- #2217 fix(ppt-web) — make `/settings/integrations` discoverable (nav + command palette), i18n it, reconcile Airbnb OAuth `redirect_uri` (Closes #2202)
- #2216 test(ppt-web) — exercise ProtectedRoute /login return-URL guard (Closes #2200)
- #2215 gh-issue-2197 — enforce share↔document scoping at the repo layer (defense-in-depth for revoke_share IDOR fix in PR #2181)
- #2214 docs(api-server) — provision PORTAL_WEBHOOK_SECRET + webhook-secret env docs (Closes #2196)
- #2213 [DRAFT open] — url-encode remaining KMP path params + shared `asPathSegment` helper (Closes #2195)
- #2205 bug-refresh-empty-cookie-shadows-body-token — empty refresh_token cookie no longer shadows body token (resolves backlog row of same id)
- #2194 gap-83-2-frontend-property-mapping-wizard-not-wired — wire Booking.com property-mapping wizard into integrations settings
- #2193 gap-screens-link-uc-29 — link UC-29 to a rentals-dashboard screen-map
- #2192 gap-screens-link-uc-33 — link UC-33 (Tenant Screening) to lease-application screen-maps
- #2191 gap-screens-link-uc-40 — link UC-40 to financial-budgets screen-map
- #2190 bug-revoke-all-sessions-cookie-blindness — resolve current session from cookie in revoke-all/list-sessions (resolves backlog row + plan of same id)
- #2189 gap-83-1 — test coverage for Airbnb direct-connect and availability-sync enqueue handlers (adds 688-line new test file — top-1 churn hotspot this run)
- #2188 gap-screens-link-uc-10 — link UC-10 to person-months screen-map
- #2187 / #2186 — hotspot follow-ups (booking_oauth_*_tests.rs) strengthening CSRF / airbnb-direct coverage

## Watch
- Stalled review: none over 7d that isn't dependabot/draft. Long-lived drafts: #1797 (`fix(api-server): auth on OCR endpoints + manager-gate rental guest PII reads`, draft since 2026-06-23) and #2213 (auto-fix in-flight for #2195).
- Reverted: 0 in window.
- Closed-not-merged PRs: 1 — #2206 (dependabot `dtolnay/rust-toolchain 1.94.1 → 1.100.0`, closed as invalid version).
- Churn hotspots (2d window, after excluding lockfiles / generated / docs / .research):
  - `backend/servers/api-server/tests/airbnb_direct_connect_and_availability_sync_tests.rs` — 688 lines add+del. NEW hotspot (runs_seen 0→1). One-shot new-file spike from PR #2189; not organic instability.
  - `frontend/apps/ppt-web/src/features/settings/integrations/IntegrationsPage.tsx` — 470 lines add+del. NEW hotspot (runs_seen 0→1). PR #2217 (discoverable + i18n) + PR #2194 (mapping wizard). Rapid rework of a freshly-shipped surface; watch for stabilisation next run.
  - `.github/workflows/backend.yml` — 245 lines add+del. NEW hotspot (runs_seen 0→1). PR #2223 CI shard rework for issue #2222. Watch whether the shards need further tuning.
- Backend `auth.rs` (last run's runs_seen=2 file) was touched this run (135 lines) by PR #2190 + #2205 but did not enter this run's top-3 raw churn, so no fresh `churn-hotspot`/`repeated-churn` signal. Two of the three prior `code-review-api-handlers-*` findings on `auth.rs` merged this run (see Backlog deltas — done transitions).

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     Example:
       gh pr list --repo martin-janci/property-management --state open --draft \
         --json number,title,updatedAt,reviewDecision,isDraft \
         --jq '.[] | select(.reviewDecision=="APPROVED" and .isDraft==true)'
     Compute age-in-hours from updatedAt. If none: emit a single line "- none".
     This catches the merge-gate bug class (#539-style): PR approved, CI green,
     but stuck in draft because a human gate wasn't cleared. -->
- none surfaced this run (per-PR `reviewDecision` not fetched — cloud gh-CLI GraphQL is restricted; MCP list output is bulk and doesn't carry `reviewDecision`)

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json (planning branch) where
     reviewer_summary starts with "verdict=changes" AND
     (now - last_updated) > 24h AND status == "review".
     Example (read planning's assignments via gh):
       gh api repos/martin-janci/property-management/contents/.research/management/assignments.json?ref=planning \
         --jq '.content' | base64 -d | jq '.assignments[]
         | select(.status=="review" and (.reviewer_summary // "" | startswith("verdict=changes")))
         | {task_id, pr_number, last_updated, reviewer_summary}'
     If none: emit "- none". -->
- none surfaced this run (planning-branch assignments.json not deep-scanned)

## Code review slice
- Segment reviewed: `mobile-native-kmp` (reason: oldest-unreviewed — state.review_cursor said 2026-06-06, 35 days stale; dispatcher touched the segment via signals/2026-07-09-mobile-native-kmp.json but state.review_cursor doesn't reflect dispatcher activity)
- Experts: kotlin-mp (single reviewer — static scan; heavy multi-expert pass not needed for a light rotating pick)
- Findings: 2 (see Backlog deltas for the `code-review-mobile-native-kmp-*` signals). Skipped: the 9 outstanding path-splice sites (already tracked in issue #2195 + PR #2213 in-flight) and the `NetworkError.isNetworkError()` coverage gap (cosmetic).
- Next segment: `ppt-web-core` (state.review_cursor 2026-07-07 — oldest after this run's advance)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: not enumerated this run (defer to next run); `docs/screens/ppt/settings-integrations.md` confirmed present (no drift on #2217's nav-link touch)
- Drift signals this run: 0 (PR #2217 touched `App.tsx` but only added a nav link; no route change and the screen doc already exists — not a genuine drift)
- Orphan screens this run: 0 (union spot-check not run — routes only touched via nav link)
- Last `screen-map.yml` CI run on `main`: not queried this run

## Backlog deltas
- **New:** [2] `code-review-mobile-native-kmp-profilestats-sequential-fanout` (perf, high) — `ProfileStatsLoader.load()` chains three independent HTTP calls sequentially; ~3× Account-screen stat-strip latency vs `coroutineScope { async … } + awaitAll`
- **New:** [1] `code-review-mobile-native-kmp-apiconfig-wsurl-naive-replace` (bug, medium; latent) — `ApiConfig.wsUrl` uses unanchored `String.replace("http","ws")`; will mangle any host/path containing `http`. Dead code today, latent trap on first WebSocket wiring.
- **New:** [1] `churn-hotspot-backend-servers-api-server-tests-airbnb-direct-connect-tests-rs` — new-file spike
- **New:** [1] `churn-hotspot-frontend-apps-ppt-web-features-settings-integrations-integrations-page-tsx` — post-launch rework
- **New:** [1] `churn-hotspot-github-workflows-backend-yml` — CI shard rework
- **Bumped:** — (no existing item eligible for a bump this run; new signals landed only on new items)
- **Decayed:** — (0 open items with `updated_at ≤ 2026-06-27`; nothing to decay this run)
- **Dropped:** — (no items transitioned to dropped this run)
- **Done (resolution check):**
  - `bug-revoke-all-sessions-cookie-blindness` — resolved: PR #2190 merged 2026-07-11 — plan moved to `plans/_archive/`
  - `bug-refresh-empty-cookie-shadows-body-token` — resolved: PR #2205 merged 2026-07-11
- **Touched (routine self-tracking):** `dx-routine-lag-catchup-2026-07` — second consecutive lag_warning appended to evidence; score unchanged (still 0)

## Plans promoted
- (none this run — top new item is score 2 perf, not eligible under the standard ≥3 gate and not covered by the security fast-track; keep `security-forgot-password-no-rate-limit.md` in `plans/` from prior run)

## Auto-fix
- `no candidate` — the two `code-review-finding` items are perf + latent-bug (not on the Phase-5 allowlist which covers `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review`). The three `churn-hotspot` items are analytical, also not on the allowlist. `lag_warning` is log-only. No candidate matched.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present in the tree)

## Open questions
- Cloud routine cadence remains slightly over 36h (62h this run) — second consecutive lag_warning. If the next run comes within 24h the recovery is on track; otherwise `dx-routine-lag-catchup-2026-07` warrants a bump and an operator ping.
- Phase 1.6 has been skipped two consecutive runs. The dispatcher has been keeping `.research/management/` fresh (assignments-archive, action-list, risks all updated within the last few hours), so no immediate signal loss — but `pm_cursor` has been stuck at index 5 (pm-security, last run 2026-05-27, 45 days ago). Worth a manual `text: "pm:security"` trigger next opportunity.

## Quiet day?
- No — 2 merged PRs closed the two `open` `bug-*` backlog rows; 5 new backlog vectors landed (2 code-review findings + 3 churn watches); Phase 1.5 reviewed `mobile-native-kmp` after 35 days stale.
