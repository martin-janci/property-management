# 2026-09-10

_Catch-up run — routine on `dev` had not ticked since 2026-09-01 18:38Z (8d 15h lag). The dispatcher on `planning` kept working through the gap; this brief reconciles that history into the routine's cursors and applies the deferred decay + resolution passes._

## Since last run
- Merged PRs: 17 (range #2922–#2956). Breakdown by day:
  - 2026-09-01 (post-cursor): #2922 `code-review-reality-server-saved-search-error-string-status`, #2925 `code-review-api-handlers-compliance-raw-db-leak-regression`
  - 2026-09-02: #2926 `code-review-api-core-fcm-legacy-dead-endpoint`, #2927 `gh-issue-2923 emergency-contact-directory update-error i18n test`, #2928 `gh-issue-2924 report_summary snapshot-consistent counts`
  - 2026-09-03: #2931 `gh-issue-2929 report_summary snapshot-consistency IG3 test`, #2932 `gh-issue-2930 e2e 403 agent-review self-review`
  - 2026-09-04: #2558 UC-ACC-05.9 invoice PDF endpoint, #2583/#2585/#2586 cargo deps, #2673 ktor group, #2935 npm-minor-patch group (dependabot)
  - 2026-09-05: #2938 `gh-issue-2937 invoice PDF renderer + handler test coverage`
  - 2026-09-06: #2940 `code-review-ppt-web-ui-confirm-dialog-i18n`, #2941 `code-review-ppt-web-core-authctx-no-boot-expiry-check` (JWT exp at cold boot), #2942 `code-review-ppt-web-core-api-401-no-request-replay` (single-flight refresh + replay), #2943 `code-review-ppt-web-core-logout-purge-allowlist-fragile`
  - 2026-09-07: #2950 `gh-issue-2947 NFC access log + QR scan history + offline feedback purge`, #2952 `gh-issue-2948 feature-local *Keys factories auto-discovery`
  - 2026-09-08: #2955 `gh-issue-2953 NFC credential material purge`, #2956 `gh-issue-2954 queryKeys.test.ts auto-discovery hardening`
- Open PRs touched: 13 (12 dependabot / dep-update-noise + #2902 draft screen-map-drift `needs-human-review` + #2744 dispatcher un-wedge, both unchanged).
- New / updated issues: 15 (12 CLOSED as their PR merged, 3 OPEN — #2946, #2945 are cross-tenant IDOR security issues on portfolio_analytics/portfolio_properties; #2951 blocked-infra on mobile RN jest cloud-runner; #2949 blocked-infra on api-server utoipa-swagger-ui build). See "Watch — Open infra blockers" below.
- Commits: 50 on `dev` since cursor (dispatcher-heavy: 42 chore(research) dispatcher tick commits + 8 code PRs merged via squash).
- Routine lag: **8d 15h** since last run (state.json `last_run_iso=2026-09-01T18:38:58Z`) — well beyond the 36h lag_warning threshold and past the 3d stale_routine_alert threshold. The **cloud dispatcher** (planning branch) continued executing (last dispatcher commit 2026-09-10 02:26Z), so the pipeline itself was healthy; the *routine* cron on dev is what fell silent. Operator should verify the routine-side schedule survived any recent cloud-cron edits.
- Phases that failed: none (Phase 1.5 & 1.6 skipped by design during catch-up — see "Phases skipped" below).

## Phases skipped
- **Phase 1.5 (rotating expert review):** skipped this run. The dispatcher's Tier-1d generator has been emitting `code-review-finding` signals across all 8 segments daily during the lag window (see `.research/signals/2026-09-02..09-*-*-tier1d.json` — 26 signal files, ~50 findings, most already converted to plans → PRs → merged by the dispatcher). Re-running the ppt-dev-review skill here would duplicate work the dispatcher already did. `state.review_cursor` is left as-is (dispatcher already updated it on merge-driven cursor bumps).
- **Phase 1.6 (project management):** skipped this run. `.research/management/action-list.json`, `risks.json`, `coverage.json`, and `project-state.md` are all present and internally consistent; `state.pm_cursor.next_index=5` (pm-security next) and `state.coverage_cursor.next_index=7` (of 13 epics) — advancing cursors here without actually running the skill would silently fail G16 (which requires the artifacts to change when the cursor advances). Left to the next non-catch-up run.

## Shipped
See the day-by-day breakdown under "Since last run" above. Themes:

- **RLS residuals cleared**: PRs #2941/#2942/#2943 finished the `ppt-web-core` auth/purge/replay trio (cold-boot JWT exp check, single-flight 401→refresh→replay, logout-purge allowlist tests).
- **NFC / cross-tenant cleanup**: PRs #2950/#2955 landed the resetLocalData purge of NFC credentials + access log + QR scan history on session change (surfaced by dispatcher issues #2947/#2953). PR #2952/#2956 hardened the queryKeys auto-discovery test infra that gates future purge coverage.
- **api-core dead code removed**: PR #2926 removed the decommissioned FCM legacy send path (Google-decommissioned endpoint — code could never deliver in production). This was the score-1 dead-code `code-review-api-core-fcm-legacy-dead-endpoint` from the 2026-09-01 morning fire.
- **reality-server hardening**: PR #2922 gave saved-search endpoints a typed error enum (replaces string-typed error comparison for HTTP status derivation).
- **api-handlers regression closed**: PR #2925 routed the compliance audit-log count error through `db_error()` — closing the raw-`e.to_string()` regression that PR #2915 (2026-09-01) had reintroduced 7 min before PR #2920 swept the rest.
- **Accounting UC-ACC-05.9 shipped**: PR #2558 (invoice PDF render endpoint) merged after 37 days open — the last of the AC line: `feat(acc): PAY by square QR (#2559, still open)` and `feat(acc): sent/cancelled invoice lifecycle (#2555, still open)` remain stalled on human review.
- **Dependency hygiene**: 5 dep-bump PRs merged (rust_xlsxwriter, base64, validator, ktor group, npm-minor-patch group of 40 packages). All labeled `post-merge-reviewed`.

## Watch
- **Open cross-tenant IDOR issues (both `bug` + `security` label; unclaimed by dispatcher as of 2026-09-10):**
  - Issue #2945 — portfolio_properties handlers missing org verification
  - Issue #2946 — portfolio_analytics property metrics computed `org_id` then discarded
  - Both filed 2026-09-06 after the security cluster PR #2944 (violation comments IDOR) landed. Not yet in `backlog.json` because the dispatcher's fast-path handles these — the routine defers to that channel and leaves the tracking here for visibility.
- **Open infra blockers:**
  - Issue #2951 — mobile-rn jest suite cannot run in cloud runner (jest-expo@56 ↔ react-native@0.87 version rot). Parallels the standing #2652 (AGP egress) unlandability for mobile-native — a second mobile-track cloud blocker.
  - Issue #2949 — api-server crate cannot build in cloud runner (utoipa-swagger-ui build-script egress-blocked). Strands the reported backend security follow-ups (#2944/#2945/#2946).
- **Stalled review:** #2559 (72d), #2555 (72d) UC-ACC-05.x — human-judgement-gated, unchanged. #2744 (28d) dispatcher un-wedge, still human-gated. #2902 (10d) screen-map-drift-2894 draft, `needs-human-review`, unchanged.
- **Reverted:** none this window.
- **Churn hotspots this window (top 3, non-lockfile, non-`.research/`):** 3 files tied at 3 edits each — all coordinated cleanup on merged security fixes, not instability:
  - `frontend/apps/ppt-web/src/lib/queryKeys.test.ts` (PRs #2952/#2956 auto-discovery hardening)
  - `frontend/apps/mobile/src/services/resetLocalData.ts` (PRs #2950/#2953/#2955 NFC purge)
  - `frontend/apps/mobile/src/services/resetLocalData.test.ts` (same three PRs)
  - None appear in `hotspot_history` prior to this run → no repeated-churn signals fire.

## PRs stuck in draft despite approval
- none (both draft PRs #2902 and #2744 are `needs-human-review`, not APPROVED-in-draft).

## PRs with verdict=changes, no fix-round progress in 24h
- #2902 — unchanged from 2026-09-01 brief.
- otherwise none.

## Code review slice
- Segment reviewed: none (Phase 1.5 skipped this run — see *Phases skipped*).
- Experts: — (skipped).
- Findings: 0 direct; 50+ indirect (dispatcher Tier-1d output during lag window; already routed).
- Next segment: `mobile-native-kmp` (`state.review_cursor.mobile-native-kmp=2026-08-25`, oldest unreviewed) — deferred pending #2652 AGP egress.
- Skipped: yes — routine catch-up run; dispatcher already covered the segments during the lag window.

## Screen-map status
- Total `docs/screens/` files: unchanged from 2026-09-01 (mobile/ still not seeded).
- Drift signals this run: 0 (heuristic scan across the 17 merged PRs — none touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`; the mobile changes touched `frontend/apps/mobile/src/services/` and `frontend/apps/mobile/src/nfc/`, both excluded per the mobile-drift carve-out).
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched this catch-up run.

## Backlog deltas
- **New:** none. The 4 signals derived this run — 3 churn-hotspots on already-resolved fix work and 1 lag_warning — do not create fresh backlog rows (churn tied directly to a bug fix is expected, not instability; lag_warning is process-only, score_delta=0).
- **Bumped:** none.
- **Decayed:** 2 items (updated_at older than 14 days, score 1 → 0 → dropped):
  - `code-review-mobile-native-kmp-portfolio-analytics-caps-100` (last updated 2026-08-19, 22d) — decayed to `dropped`.
  - `code-review-mobile-native-kmp-cancellation-swallowed` (last updated 2026-08-25, 16d) — decayed to `dropped`.
- **Resolved:** 1 item — `code-review-mobile-native-kmp-create-listing-not-wired` → `done`. Grep confirms `NotImplementedError` absent from all 4 cited files; `CreateListingScreen.onSubmit` is now a typed `suspend (CreateListingInput) -> Result<Unit>` wired via `onClick`. No PR match by verbatim id (this is mobile-native, cloud-unlandable per #2652 AGP egress — resolution must have landed via a local operator push not captured by the dispatcher's PR trail).
- **Dropped:** none additional beyond the 2 decays above.

## Plans promoted
- **0 new plans this run.** The only active `ready` item is `code-review-mobile-native-kmp-inquiries-response-contract` (score 3, high, plan `plans/code-review-mobile-native-kmp-inquiries-response-contract.md` already written 2026-08-25). It stays parked on the mobile-native cloud-unlandable pile (#2652). Grep confirms the evidence (InquiryModels.kt required `page_size` field) still present.
- **Structural takeaway:** active backlog is now 2 items (1 open + 1 ready) — the dispatcher's Tier-1d + fast-path has drained the routine's canonical backlog over the last 8d. This is the intended steady-state.

## Auto-fix
- `no candidate` — no signal met `confidence == high` + `score >= 3` + allowlist match this run. The 3 churn-hotspots are `medium` confidence and the allowlist doesn't accept `churn-hotspot` (analytical, not actionable). The `lag_warning` is `high` confidence but `score_delta=0` (process signal). See G15 audit trail in signals file.

## Self-review findings
- none (`.research/self-improvement/findings.json` absent).

## Open questions
- **Routine cron on `dev` fell silent between 2026-09-02 and 2026-09-09** while the dispatcher on `planning` kept ticking. The cloud dispatcher's `cloud-setup.sh` may have been redeployed without the routine-side cron. Follow-up: operator to confirm the routine schedule survived any recent scheduler config edits, and consider raising a `dx` backlog row (`cloud-routine-cadence-recovery`) if a second gap opens.
- **#2652 AGP egress structural block** — 4+ mobile-native/KMP items are score-parked on the same blocker (`inquiries-response-contract`, `httpclient-no-timeout`, and 2 just-decayed-dropped items). Kotlin ecosystem work is effectively cloud-unlandable until this is resolved (operator handles locally, per the 2026-09-01 brief's morning-fire deferred decision).

## Quiet day?
- **No.** 17 PRs merged, 2 items decayed, 1 resolved, and the routine itself just recovered from an 8-day gap. First non-quiet, non-catch-up state resumes tomorrow.
