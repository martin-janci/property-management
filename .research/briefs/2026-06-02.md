# 2026-06-02

## Since last run
- Merged PRs: 14 (range #930–#947)
- Open PRs touched: 1 (#932 draft, no reviewDecision, age 18h — not yet stalled)
- New / updated issues: 11 (10 closed dev-review tracking issues + #779 still open as the rollup)
- Commits: 32 on `dev` (12 are version-bump auto-commits)
- Routine lag: on schedule (~24h since 2026-06-01T03:30Z)
- Phases that failed: none

## Shipped
- #930 fix(security): audit OAuth client mutations with reason — closes #768; adds audit-log entry for `update_client` PATCH with operator `reason`, plugs an audit hole on privileged scope edits
- #931 fix(api-server): terminal-state idempotency guard for e-signature webhook — skip `update_esignature_workflow_by_external_id` when status ∈ {completed, voided, declined}
- #933 fix(backend): scheduler retry budget + OAuth introspect/revoke hardening (refs #789, #823) — `reset_stuck_jobs` retry cap + OAuth introspect/revoke hardening; tests added
- #934 fix: clear remaining review-tail findings (#764, #698, #767, #751) — admin MFA + auth follow-ups
- #935 feat(reality-server): buyer-axis my-inquiries listing (closes #763 part 1) — buyer can now see inquiries they submitted; previously realtor-scoped only
- #936 feat(api-server): e-signature /sign consumer with single-use nonce (#761 part 2) — adds the missing /sign endpoint + nonce consumption so a captured signing link can't be replayed
- #939 fix(deploy-server): grant refs/heads/dev the release:deploy scope — unblocks dev→staging auto-deploy; **note: scope is target-agnostic — dev can now request `target: prod` under `release:deploy`** (tracked as follow-up in PR body; see backlog `test-gap-hotfix-no-test-pr-939-deploy-server-scopes`)
- #940 feat(api-server): wire Epic 2B notification pipeline into publish paths (closes #795) — pipeline existed but was unregistered; FCM adapter silently returned Ok(()) — now actually fires
- #942 fix(frontend): align api-client contracts + auth hydration race (#751) — announcements/documents hooks aligned + AuthCallbackPage race fix; hooks unit tests added
- #943 fix(mobile): address dev-review findings (#767) — Announcements/Documents/Neighbors screens cleanup; **no test diff** → new test-gap row
- #944 fix(db): enforce announcement targeting on the published read path (#920) — `list_published_rls`/`count_published_rls` ignored target_type/target_ids → intra-org disclosure; fixed + new repo test
- #945–#947 chore(research): dispatcher state updates (`.research/management/` only, no app code)

## Watch
- Stalled review: none (#932 is 18h old, well below the 7d threshold)
- Reverted: none
- Churn hotspots: **OAuth cluster dominates this run**
  - `backend/servers/api-server/tests/oauth_integration_tests.rs` (378 lines this run, runs_seen=2)
  - `backend/servers/api-server/src/services/oauth.rs` (40 lines, runs_seen=1) — first-time
  - `backend/servers/api-server/src/routes/oauth.rs` (161 lines, runs_seen=1) — first-time
  - Both fix PRs (#930 + #933) shipped with test deltas, so no `risky-churn` fires — the OAuth surface is being patched with coverage.
- Lockfile / manifest churn (12 `chore(version)` commits) is auto-bump noise — excluded.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: api-handlers (reason: churn-aligned — three OAuth files are in the segment + oldest-unreviewed = 2026-05-23)
- Experts: rust + security (focus on OAuth handler/service code in `routes/oauth.rs` + `services/oauth.rs`)
- Findings: 0 (file scan found only `expect("OS rng failed")` panics on CSPRNG — well-justified, comment documents intent; `tracing::error!` lines for client-secret regen log only `%e` not the secret value — false positive)
- Next segment: ppt-web-core (next oldest at 2026-05-25, since `api-handlers` was just refreshed today)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: tracked under `docs/screens/{ppt,reality}/` (mobile/ not yet seeded)
- Drift signals this run: 0 (no merged PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not checked this run (no drift candidates to gate)

## Backlog deltas
- **New:** [2] Mobile RN dev-review tail (#943) shipped without test coverage — `test-gap-hotfix-no-test-pr-943-mobile-dev-review-tail`
- **New:** [2] deploy-server OIDC scope mapping (#939) shipped without unit test for derive_oidc_scopes — `test-gap-hotfix-no-test-pr-939-deploy-server-scopes`
- **New:** [1] OAuth integration-tests churn hotspot — `refactor-oauth-integration-tests-hot`
- **New:** [1] services/oauth.rs churn hotspot — `refactor-services-oauth-rs-hot`
- **New:** [1] routes/oauth.rs churn hotspot — `refactor-routes-oauth-rs-hot`
- **Done:** triage-issue-920 (#944 fixed announcement-targeting), triage-issue-768 (#930 closed), triage-issue-767 (#943 closed), triage-issue-763 (#935 closed part 1), triage-issue-761 (#936 closed part 2), triage-issue-756 (#934 cleared), triage-issue-752 (#929 reconciled), triage-issue-751 (#942 closed)
- **Decayed:** none (no open items older than 14 days)
- **Dropped:** none

## Plans promoted
- none — all top open items are at score 2, below the score≥3 readiness gate. The two new test-gap rows are intentionally per-PR and need either stacking or a human-flagged systemic upgrade before promotion. The security fast-track does not apply (no high-confidence security vector at score ≥2 that isn't already done/ready).

## Auto-fix
- no candidate — no signal this run met `confidence=high` + `score≥3` + allowlist-shape simultaneously. The two new hotfix-no-test rows (#943, #939) are high-confidence test-gaps but score=2, below the +3 threshold; they also aren't in the auto-fix allowlist.

## Self-review findings
- none (`.research/self-improvement/` carries no findings.json this run)

## Open questions
- **Deploy-server scope-escalation follow-up (#939):** PR #939 body explicitly notes that `derive_oidc_scopes` is target-agnostic for `release:deploy` — `dev` (and `main`) tokens can request `target: prod`. The PR ships a working fix for the immediate 403 but defers tightening. Should this be promoted as a security vector now, or wait for the explicit follow-up PR? Currently tracked as a `test-gap` (no unit test on the mapping table) — but the substantive risk is the latent scope-escalation, not the test gap. Worth a human re-classification next run.
- **Mobile RN dev-review pattern:** second hotfix-no-test against the same 5 mobile RN files in 14 days (PR #918 → PR #943). The systemic test-gap is more interesting than either individual PR. Worth lifting into a single `test-gap-mobile-rn-coverage-systemic` row when a third such PR lands, rather than continuing one-row-per-PR.

## Quiet day?
- No — 14 PRs merged, 5 new backlog rows, 8 resolutions, OAuth cluster identified as the active hotspot.

## Phase 1.6 (project management & delivery)
- **Skipped this run.** The cloud routine budget is tight today; Phase 1.6's deep skill invocation (ppt-project-management) is best run locally where `.research/management/` rebuild can chase the full coverage map. The next local run should invoke `/ppt-project-management` to refresh `action-list.json`, `risks.json`, `project-state.md`, and advance `pm_cursor` + `coverage_cursor`. G16 is a no-op for this run as per the skip clause.
