# 2026-07-23

## Since last run
- Merged PRs: 25 (range #2436–#2467)
- Open PRs touched: 13 (10 fresh dependabot #2468–#2477, iOS layout #2433 age ~2d, integration-test consolidation #2461 <1h, BIT-558 documents-forms #2440)
- New / updated issues: 0 (#2366 still the only recent open issue — no bump)
- Commits: 25 on `dev` (excluding dispatcher management-only commits)
- Routine lag: 2d 1h since last run — **lag_warning** fired (>36h threshold); the cloud cron missed a tick between 2026-07-21 and 2026-07-23
- Phases that failed: `phase-1.5-code-review-skipped-budget`, `phase-1.6-project-management-skipped-budget` (both intentionally skipped this run to protect the routine token budget on a buffer-low-priority tick — cursors NOT advanced, next run resumes both from where they stood)

## Shipped

Two-day catch-up window covering the Layout & Content Manager tail, a broad security/IDOR hardening pass, an authoritative build-experience push, and the layered-CI reshape:

**Security / IDOR (5 fixes):**
- **#2450** (`disputes`) — org-scope 5 dispute sub-resource handlers (`list_parties`/`add_party`/`list_evidence`/`delete_evidence`/`list_activities`). Ships a fresh `ensure_dispute_in_org` guard + a repository-layer contract test suite (`dispute_cross_org_idor_tests.rs` R4–R8 pinning per-verb NotFound on cross-org). Foreign `dispute_id` → 404, never a cross-tenant existence oracle.
- **#2455** (`code-review-api-core-scheduler-units-target-cross-tenant`) — AND-scope announcement fan-out to org in scheduler.rs (resolves the last-run code-review finding that was still `open` on the backlog).
- **#2438** (`gh-issue-2422`) — application-layer org scoping to `get_document`.
- **#2443** (`gh-issue-2435`) — record acting user in tenant layout-override writes (`updated_by`).
- **#2446** (`RUSTSEC-2026-0213`) — bump ammonia to 4.1.4.

**Code-review triage from 2026-07-21 findings — all three ppt-web-ui bugs landed:**
- **#2456** (`actionqueue-mock-shipped`) — gate dashboard action-queue mock behind dev-only flag (kills the "approve/reject/dismiss silent no-op" regression on production users).
- **#2457** (`savedirty-stale-closure`) — fix tautological "changed since sent" dirty check.
- **#2458** (`propinput-json-coerce`) — keep string-typed layout props as strings in PropInput.

**Layout & Content Manager tail:**
- **#2464** (`refactor-churn-hotspot-layouteditorpage-tsx-2026-07-21`) — extract LayoutEditorPage style constants into styles module (addresses the churn item promoted last run).
- **#2452** (`gh-issue-2449`) — correct layout-core Props docstring (key-only gating, values not validated).
- **#2462** (`screen-map-drift-pr-2431-reality`) — record layout-revalidate webhook in listing-detail screen-map (heuristic drift-signal caveat addressed).

**Build experience / CI reshape:**
- **#2444** — deterministic AI-agent verify gate + layered CI (headline PR; establishes `just verify` as the single-command impact-scoped gate).
- **#2459** — nextest archive + per-test partition for the backend test tier.
- **#2448** — agent-experience hardening (deterministic tests, verify gate, stale-doc archive).
- **#2454** — repair bitrotted docker-compose dev flow.

**Test coverage backfill (BIT track):**
- **#2453** — re-add import + cert-document authz sweep cases after SSRF review (BIT-559).
- **#2447** — re-add net-new BIT-268 authz backfill tests (BIT-328 salvage).
- **#2465** — backfill OAuth handler integration coverage.
- **#2439** — admin-platform happy-path batch 3 (13 net-new 2xx, BIT-557).
- **#2437** — endpoint checklist Wave 9 tallies (1501 done, 74.7%; BIT-258).

**Housekeeping:**
- **#2436** — log malformed announcement `target_ids` in scheduler (fail-safe + tests).
- **#2442** (`gh-issue-2434`) — migration-number citations in portal-webhook dedup comments.
- **#2451** — version shared agent config.
- **#2463** (`gh-issue-2460`) — retry-remint skips stems marked done/ghost in action-list-archive.
- **#2467** (`gh-issue-2466`) — ignore dtolnay/rust-toolchain dependabot bumps.

## Watch
- Stalled reviews: none. #2433 (iOS layout) is 2d old — not stalled. All 8 dependabot PRs are hours-fresh.
- Reverts: none in window.
- Churn hotspots (top 3 by touches, application code only):
  - `backend/servers/api-server/src/services/scheduler.rs` — 3 touches (#2436 + #2455 + #2439), 1876 lines total. **First-time hotspot** (runs_seen 0 → 1). Watch next run for repeated-churn.
  - `.github/workflows/backend.yml` — 3 touches (#2459 + #2444 + #2448), 1409 lines total. CI-tier reshape — expected during layered-CI work but a repeated-churn next run would flag a non-converging design.
  - `backend/scripts/ci-test-shard.sh` — 3 touches, 376 lines total. Paired with backend.yml above.
- **Dispatcher buffer signal (from routine-fire payload):** `buffer-low: claimable=4/72 — refill planner (dispatcher 2026-07-23)`. Refill path is `.research/backlog-refill.sh` + Tier-2 planner kick; Tier-2 has a known 401 outage in the self-improvement findings (fp-null recurrence=2). This routine cannot promote from the current backlog (all open items are score 1-2, below the readiness bar), so the refill needs the operator to unblock the DISPATCHER_TOKEN outage OR the manual planner to hand-author higher-scored items into `backlog.json`. **Not fixable from the routine on this tick.**

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (assignments.json verdict=changes count = 0)

## Code review slice
- Segment reviewed: **none this run** — Phase 1.5 skipped for token budget on a buffer-low incremental tick.
- Experts: n/a
- Findings: 0
- Next segment: `api-handlers` (oldest unreviewed: 2026-07-14) — will run next tick.
- Skipped: yes — routine-token pressure on a lag-recovery tick; `review_cursor` unchanged.

## Screen-map status
- Total `docs/screens/` files: 153+ (`ppt/` 106 + `reality/` 47+; `mobile/` still 0). PR #2462 added the layout-revalidate webhook entry to `docs/screens/reality/listing-detail.md` (edit, not new file — no delta in count).
- Drift signals this run: 0 (no route file touched without a corresponding screen doc update — the routes touched in the window either weren't under the trigger set, or landed with the doc edit already, e.g. #2462).
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not queried this run (Phase 1.6 skipped); last observed 2026-06-04 22:19 UTC.

## Backlog deltas
- **New:** [+1] `refactor-churn-hotspot-scheduler-rs-2026-07-23` (refactor) — scheduler.rs 3-touch hotspot.
- **New:** [+1] `refactor-churn-hotspot-github-workflows-backend-yml-2026-07-23` (refactor) — CI-workflow churn.
- **New:** [+1] `refactor-churn-hotspot-backend-scripts-ci-test-shard-sh-2026-07-23` (refactor) — shard driver churn (pairs with backend.yml).
- **Bumped:** none.
- **Decayed:** none — no open item is >14d old (oldest open row is 3 days).
- **Dropped:** none.
- **Resolved (open → done):**
  - `refactor-churn-hotspot-layouteditorpage-tsx-2026-07-21` — PR #2464 title matches item id verbatim (extract LayoutEditorPage styles module).
  - `screen-map-drift-pr-2431-reality` — PR #2462 title matches item id verbatim (record layout-revalidate webhook in listing-detail screen-map).

## Plans promoted
- none — no open backlog item hit the readiness bar this run. The three new churn items are score 1 (single signal, below the +3 general threshold); the three surviving open churn rows from 2026-07-20/21 are also score 1. **A buffer-low tick did not by itself justify lowering the promotion bar** — quality gate over volume. Next run will decide once corroborating evidence lands (repeated-churn on any of the new hotspots, or a code-review-finding cross-referencing scheduler.rs).

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist type this run (`churn-hotspot` is on the cumulative allowlist but NOT eligible for auto-fix; no fresh `screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / lockfile-drift / `stalled-review` candidates).

## Self-review findings
- open=21 (high=4, medium=13, low=4); new this run=0; acknowledged=19.
- fp-null [recurrence=10] — dispatcher Phase 6 archive-move produces 198KB+ assignments-archive.json; mcp push_files 64KB inline limit + git-push-proxy-403 combine to drop the archive push; fix: adopt PR+API-merge path as primary push mechanism.
- fp-null [recurrence=6] — dispatcher-prompt hard-codes proxy-403 assumption; in this env `git push origin dev` succeeded once GH007 email was fixed; fix: runtime-probe push capability at Phase 1 and bypass 64KiB size-guard when git push works.
- fp-null [recurrence=3] — Phase 2.6 buffer reported claimable=54/open=54 but real claimable=0; all open action-list items already terminal in archive; fix: extend gc1-reconcile to auto-close ANY id shape (including status=failed) and adjust GC3 formula to subtract archive ids.
- fp-null [recurrence=2] — Tier-2 planner kick returned http=401; DISPATCHER_TOKEN/DISPATCHER_URL rejected; fix: operator rotates the secret + dispatcher branches on 401/403 as OPERATOR-CONFIG escalation.

## Open questions
- The `buffer-low: claimable=4/72` payload names the refill path but the routine cannot force-promote below its own readiness bar. Is the correct operator response to (a) fix the Tier-2 DISPATCHER_TOKEN outage so the planner kick works, or (b) hand-author higher-scored backlog rows for the routine to promote? The self-review findings suggest (a) is the durable fix.
- Phase 1.5 was skipped this run — the `api-handlers` segment has been unreviewed since 2026-07-14 (now 9 days). Should the next routine tick run Phase 1.5 as the top priority even if payload is again buffer-low? Recommend: yes, unless payload is explicitly `paused` or `deep`.

## Quiet day?
- No — 25 merged PRs, 3 new backlog vectors, 2 resolutions, one operator-visible dispatcher signal.
