# 2026-07-23

## Since last run
- Merged PRs: 19 (range #2436–#2458 — Layout & Content Manager follow-up cascade + security-issue cluster resolution)
- Open PRs touched: 9 (#2433, #2440, #2459, #2461, #2462, #2463, #2464, #2465, #2467 draft)
- New / updated issues: 2 open (#2466 dtolnay/rust-toolchain dependabot ignore; #2460 retry-remint ghost re-mint bug) + 6 closed (all resolved by merged PRs — see *Backlog deltas*)
- Commits: 47 on `dev` (19 real merged PRs + 27 chore(research) dispatcher commits + 1 prior routine commit)
- Routine lag: 1d 47h since last run — **Wednesday run skipped**; not stale yet (would need 3+ days) but the cloud cron missed a fire. `lag-warning-2026-07-23` recorded (score 0).
- Phases that failed: none (Phase 1.5 formal skill invocation intentionally skipped — see *Code review slice* — this is a scheduled skip, not a failure).

## Shipped

Biggest cluster in weeks — **9 net-new security/correctness fixes landed** plus test coverage backfill and a deterministic verify gate:

- **#2450** (`gh-issue-2441`) — Org-scoped **5 dispute sub-resource handlers** for cross-tenant IDOR (`disputes.rs` `delete_evidence`/`list_parties`/`list_evidence`/`add_party`/`list_activities`) — closes the Tier-1d finding filed 2026-07-22. High-severity IDOR remediation.
- **#2438** (`gh-issue-2422`) — Added application-layer org scoping to `get_document` (defence-in-depth; already RLS-protected). Closes the last document-read handler with no explicit org guard.
- **#2455** (`code-review-api-core-scheduler-units-target-cross-tenant`) — AND-scoped announcement fan-out to org in scheduler (cross-tenant defence).
- **#2456** (`code-review-ppt-web-ui-actionqueue-mock-shipped`) — Dashboard action-queue mock gated behind dev-only flag (fixes the "fabricated action items shown to production users" bug flagged in the 2026-07-21 brief).
- **#2457** (`code-review-ppt-web-ui-savedirty-stale-closure`) — Fixed tautological "changed since sent" dirty check in `DashboardCustomizePage`.
- **#2458** (`code-review-ppt-web-ui-propinput-json-coerce`) — Kept string-typed layout props as strings in `PropInput` (fixes JSON.parse coercion drop).
- **#2452** (`gh-issue-2449`) — Corrected `layout-core` Props docstring (key-only gating, values not validated).
- **#2443** (`gh-issue-2435`) — Recorded acting user in tenant layout-override writes (`updated_by`).
- **#2442** (`gh-issue-2434`) — Fixed migration-number citations in portal-webhook dedup comments (00218 → 00219).
- **#2446** (`fix(api-server)`) — Bumped `ammonia` to 4.1.4 (RUSTSEC-2026-0213) — unblocks `cargo-deny` on every backend PR (closes #2445).
- **#2444** (`feat(build)`) — Deterministic AI-agent verify gate (`just verify` + impact-scoped fmt/clippy/test/typecheck) + layered CI (build-experience roadmap).
- **#2448** (`chore`) — Agent-experience hardening: deterministic pnpm test, verify gate, dev-base branch helpers, stale-doc archive.
- **#2451** (`chore(claude)`) — Versioned shared agent config (settings, guard hooks, commands, ppt skills).
- **#2447** (`test(api-server)`) — Re-added net-new BIT-268 authz backfill tests (BIT-328 salvage).
- **#2453** (`test(api-server)`) — Re-added import + cert-document authz sweep cases after SSRF review (BIT-559).
- **#2439** (`test(api-server)`) — Admin-platform happy-path batch 3 (13 net-new 2xx, BIT-557).
- **#2454** (`fix(dev-stack)`) — Repaired bitrotted docker-compose dev flow.
- **#2436** (`fix(api-server)`) — Log malformed announcement `target_ids` in scheduler (defensive logging).
- **#2437** (`docs`) — Endpoint checklist Wave 9 tallies — 1501 done (74.7%) (BIT-258).

Not shipped this window: iOS SwiftUI dispatch (#2433 still open >2 days now), 84-1 s3-presigned-urls / 84-2 e-signature-email story completion (still `partial`), `layout_editor_*` capability (deferred), ppt-web frame-protection (still tracked as follow-up in earlier briefs).

## Watch
- **New HIGH-severity IDOR from Tier-1d review (2026-07-23, api-handlers segment):** `marketplace::award_quote` binds a client-supplied `quote_id` without validating it belongs to the authorized RFQ. Provider-quote status can be flipped across RFQs (and across tenants). Promoted to plan today (`plans/security-marketplace-award-quote-cross-tenant-idor.md`, score 3, confidence high). Same IDOR class as the just-shipped disputes cluster (#2450) and the ai/ocr `submit_correction` finding.
- Two new medium-severity `layout` handler bugs from the same Tier-1d sweep: admin.rs maps DB-infrastructure errors to 422 (silent 5xx-based monitor blindness) and 7 sites do `serde_json::to_value(row).unwrap_or_default()` (returns 200 OK with `null` body on serialization failure). Both in backlog at score 2/1 respectively — below promotion bar, deferred.
- Stalled reviews: none. All 5 open PRs in dispatcher review carry `verdict=approve`; they're waiting to merge, not for reviewers.
- Reverts: none in window.
- Churn hotspots (top 3, all NET-NEW test files — low-signal, expected coverage growth, not organic instability):
  - `backend/servers/api-server/tests/platform_admin_authz_batch2_tests.rs` — 417 lines, PR #2447 (BIT-268 authz backfill; single-PR add)
  - `backend/servers/api-server/tests/org_property_authz_backfill_tests.rs` — 412 lines, PRs #2447+#2453 (added 2447, cert-document sweep re-added 2453 after SSRF review)
  - `backend/servers/api-server/tests/admin_platform_happy_path_batch3_tests.rs` — 384 lines, PR #2439 (BIT-557 happy-path; single-PR add)
  - Note: `scripts/verify-impact.sh` at 303 lines (PR #2444) is real production tooling churn but below top-3 raw.
- Risky-churn: no PR intersected the top-3 hotspot set (which are all tests). `PR #2436` touched `services/scheduler.rs` at rank ~#10 churn (shared with #2455 for cross-tenant fan-out fix); flagged as `hotfix-no-test-pr-2436` at low confidence — change is defensive logging, no behavior-change to guard.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none — all 5 review-state assignments carry `verdict=approve`.

## Code review slice
- Segment reviewed: **api-handlers** (reason: dispatcher's Tier-1d generator ran twice today on this segment — churn-aligned + oldest-unreviewed — producing 3 fresh code-review findings; the routine's formal Phase 1.5 slice was intentionally skipped to avoid duplicating work and preserve token budget)
- Experts: rust (primary) + security (secondary)
- Findings: 3 (from Tier-1d, ingested into backlog by this run)
  - `code-review-api-handlers-marketplace-award-quote-idor` (+3, security, high) — **PROMOTED to plan today** — cross-RFQ/cross-tenant write IDOR in `award_quote`; provider-quote `accepted` status flipped across tenant boundaries; sibling reject step at `marketplace.rs:820-831` proves the accept step's missing `rfq_id` predicate is an in-file inconsistency, not intent.
  - `code-review-api-handlers-layout-admin-db-err-422` (+2, bug, medium) — every layout-admin handler funnels DB/infrastructure errors through `bad_request()` (422), silencing 5xx-based SLO alerting; sibling `tenant.rs`/`resolved.rs` correctly return 500. Backlog only (below promotion bar).
  - `code-review-api-handlers-layout-to-value-null-swallow` (+1, bug, medium) — 7 write/read sites in `layout/{admin,tenant}.rs` do `serde_json::to_value(row).unwrap_or_default()` — a serialization failure returns 200 OK with a literal `null` body instead of 500. Latent silent-failure. Backlog only.
- Next segment (per state.review_cursor after today's advance): **ppt-web-core** (last 2026-07-15, oldest of the 8 remaining segments; api-handlers now 2026-07-23)
- Skipped: yes — Tier-1d already delivered fresh findings for the highest-priority segment today; running the formal skill again would duplicate work.

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` 106 + `reality/` 47; `mobile/` still not seeded → 0)
- Drift signals this run: 0 (no merged PR this window touched a ppt-web/reality-web route file; the layout/dashboard changes were in `features/` not `routes/**` or `app/**`).
- Orphan screens this run: 0 (no trigger files touched → no scan performed per spec).
- Last `screen-map.yml` CI run on `main`: not queried this run (no push-to-main in window; all 19 merges to `dev`).

## Backlog deltas
- **New:** [+3] `code-review-api-handlers-marketplace-award-quote-idor` (security, high) — cross-tenant IDOR in marketplace RFQ award.
- **New:** [+2] `code-review-api-handlers-layout-admin-db-err-422` (bug, medium) — layout-admin maps DB errors to 422 instead of 500.
- **New:** [+1] `code-review-api-handlers-layout-to-value-null-swallow` (bug, medium) — 7 sites default `to_value` failures to `null` and 200 OK.
- **New:** [+1] `refactor-churn-hotspot-platform_admin_authz_batch2_tests-rs-2026-07-23` (refactor) — 417 lines net-new tests (BIT-268).
- **New:** [+1] `refactor-churn-hotspot-org_property_authz_backfill_tests-rs-2026-07-23` (refactor) — 412 lines across 2 PRs (BIT-268 + BIT-559).
- **New:** [+1] `refactor-churn-hotspot-admin_platform_happy_path_batch3_tests-rs-2026-07-23` (refactor) — 384 lines net-new tests (BIT-557).
- **New:** [+2] `test-gap-hotfix-no-test-pr-2436` (test-gap, low) — defensive logging PR mechanically flagged (no behavior change → triage candidate).
- **New:** [+2] `test-gap-hotfix-no-test-pr-2454` (test-gap, low) — dev-stack docker fix mechanically flagged (infra config, no test surface → triage candidate).
- **Bumped:** none.
- **Decayed:** none — no open item is >14 days old (all pre-run open items updated 2026-07-20/2026-07-21).
- **Dropped:** none this run.
- **Resolution check:** no `open` backlog item's `id` appeared verbatim in a merged PR body/title this run (all recent security fixes were addressed via issue numbers, not by referencing backlog ids). Manual reconciliation: the three ppt-web bugs from 2026-07-21 (`code-review-ppt-web-ui-actionqueue-mock-shipped/-propinput-json-coerce/-savedirty-stale-closure`) were already marked `done` in `backlog.json` by prior dispatcher activity; PRs #2456/#2458/#2457 that landed today match those fixes but the resolution PRs were not appended to `sources` because the items were already terminal (no `open`→`done` transition to trigger the append).

## Plans promoted
- `plans/security-marketplace-award-quote-cross-tenant-idor.md` — cross-RFQ/cross-tenant IDOR fix in `MarketplaceRepository::award_rfq`; two-line SQL predicate addition + 3 net-new `#[sqlx::test]` regression tests (foreign-RFQ / cross-tenant / already-awarded). Security fast-track (score 3, confidence high, vector security). Files verified on disk. **adversarial pass: passed** — source→hypothesis→approach chain intact, sibling reject step at `marketplace.rs:820-831` cited as proof-of-inconsistency; no placeholder rot; all 15 required plan sections present; Mode: cloud-ok.

## Auto-fix
- `no candidate` — no signal met the certainty bar this run. The one high-confidence high-scored signal (`code-review-api-handlers-marketplace-award-quote-idor`, +3, security, high) is a **code change**, which is explicitly **NOT** in the Phase 5 allowlist (only doc-stub / orphan-remove / lockfile-bump / issue-only / comment-only). Correctly routed to the manual implementer path via the plan promotion above.

## Self-review findings
- open=21 (high=4, medium=13, low=4); new this run=0; acknowledged=19.
- fp-null [recurrence=10] — dispatcher Phase 6 archive-move produces 198KB+ assignments-archive.json; mcp push_files 64KB inline limit + git-push-proxy-403 combine to drop the archive push; fix: adopt PR+API-merge path as primary push mechanism.
- fp-null [recurrence=6] — dispatcher-prompt hard-codes proxy-403 assumption; in this env `git push origin dev` succeeded once GH007 email was fixed; fix: runtime-probe push capability at Phase 1 and bypass 64KiB size-guard when git push works.
- fp-null [recurrence=3] — Phase 2.6 buffer reported claimable=54/open=54 but real claimable=0; all open action-list items already terminal in archive; fix: extend gc1-reconcile to auto-close ANY id shape (including status=failed) and adjust GC3 formula to subtract archive ids.
- fp-null [recurrence=2] — Tier-2 planner kick returned http=401; DISPATCHER_TOKEN/DISPATCHER_URL rejected; fix: operator rotates the secret + dispatcher branches on 401/403 as OPERATOR-CONFIG escalation.

## Open questions
- The 3 ppt-web dashboard bugs from 2026-07-21 (`actionqueue-mock-shipped`, `propinput-json-coerce`, `savedirty-stale-closure`) show `status=done` in `backlog.json` but no resolving PR in `sources`. PRs #2456/#2458/#2457 clearly resolve them (titles literally quote the finding ids). Should the routine backfill `sources` on already-done items when a merged PR mentions their id? Current spec only appends on `open→done`. Low-severity data-hygiene question — surfaced for the operator.
- `services/scheduler.rs` is at rank ~#10 churn this window (both #2436 defensive-logging and #2455 cross-tenant fan-out fix touched it). Not top-3, but it's the file with the most real churn (excluding test-file coverage growth). Worth a targeted review on the next api-core cycle?

## Quiet day?
- No — 19 merged PRs (biggest cluster in weeks), 8 new backlog vectors, 1 plan promoted (security fast-track), 3 fresh Tier-1d code-review findings. Not remotely quiet.
