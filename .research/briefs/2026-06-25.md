# 2026-06-25

## Since last run
- **Routine lag: ~9 days (~225h) since 2026-06-16T03:25:00Z — `lag_warning` + `stale_routine_alert` both fired.** The cloud-cron routine has missed runs while the dispatcher (separate loop on `planning`) has remained healthy and shipped 95 PRs.
- Merged PRs: **95** (range #1567–#1843; non-contiguous because the dispatcher merged out-of-order while old open PRs were drained).
- Open PRs touched: **31** (21 draft, 10 non-draft; oldest open `days_open=2`, so no `stalled-review` candidates this run).
- New / updated issues: **82 new** (#1438–#1844 cursor; all labeled — `untriaged-issue` did **not** fire). 74 of 82 carry `follow-up` + `from-merged-review` = dispatcher post-merge review traffic.
- Commits on `dev`: 49 application-code commits + a large dispatcher state stream. Local `4829015b` cursor is no longer in the tree (force-update history) — re-anchored on `origin/dev` HEAD.
- Phases that failed: **none** — Phase 1.5 and Phase 1.6 deliberately **skipped** for catch-up (see below).

## Shipped
*Hand-curated highlights from the 95-PR stream; full audit trail in
`.research/management/assignments.json` (planning branch) + `_bmad-output/implementation-artifacts/sprint-status.yaml`. Most fix PRs are post-merge-review follow-ups carrying the `from-merged-review` label.*

- **Hotfix sequence on the duplicate-migration dev break:** #1724 (resolve dup version 00187), #1755 (resolve dup version 192 + dup-version CI guard), #1757 (the 00192 unblock), #1721 (rustfmt sweep), #1728 (notification_event RLS baseline), #1731 (person-months UNWIRED reconcile), #1748 (mobile @types/node typecheck fix), #1742 (docker copy @ppt/e2e). The earlier `feature/epic-…` queue was unblocked by 2026-06-23.
- **Repo hygiene / churn-hotspot module splits (8 landed):** #1683 (document repo), #1796 (subscription repo), #1810 (sensor repo), #1798 (emergency routes), #1800 (enhanced_tenant_screening), #1813 (vendors), #1815 (iot), #1816 (reserve_funds), #1708 (aml_dsa), #1700 (forms), #1693 (booking ota_xml). 11 hotspot items resolved → `done` (see Backlog deltas).
- **Phase 1.5 code-review findings resolved by the dispatcher:** #1679 closes `code-review-mobile-rn-report-fault-fake-submit` (wire ReportFaultScreen submit to POST /api/v1/faults); #1684 closes `code-review-api-core-osrng-expect` (propagate CSPRNG failure instead of panic); #1681 partially closes `code-review-mobile-rn-screens-mock-data` (Buildings + Leases done, 5 screens still on MOCK_* arrays).
- **Security follow-ups in flight (mostly still draft, opened today):** #1823 guest ID-document PII hardening (#1760, #1783), #1824 Stripe Checkout hardening (#1764, #1785), #1797 OCR auth + rental PII manager-gate (#1772, #1766), #1799 messaging attachment IDOR (#1791, #1770), #1801 event-bus subscription liveness (#1792, #1765), #1802 N-party messaging (#1773, #1771), #1804/#1807 scheduler dedup (#1790, #1777), #1806 booking_channel DB-manager gate (#1787), #1795 portal listings DB-CHECK (#1762, #1784), #1819 mobile offline queue (#1767), #1825 Booking.com currency allowlist (#1759).
- **New features (Epic 11 financial + Epic 18 guest PII + ACC accounting):** #1717 income statement / balance sheet / cash flow + PDF/xlsx (Story 11.7 — has unchecked TODOs); #1725 financial-statement reports UI; #1726 Stripe Checkout (Story 11.5); #1750 guest ID-document upload + OCR (Story 18.2); #1817 ACC spec; #1808 accounting-web public/landing/signup (PAP-312). #1821 ACC standalone server epic still open (draft).
- **N-party messaging / attachments:** #1689 (N-party group conversations), #1702 (attachment presigned upload — has unchecked TODOs), #1696 (per-participant archive + soft-delete), #1712 (ppt-web attachment UI), #1756 (camelCase attachment wire), #1768 (camelCase thread/message wire — has unchecked TODOs).
- **Dispatcher-style story-reconciliation PRs (today):** #1829 (10b-5), #1830 (79-1), #1831 (10b-7), #1832 (6-1), #1835 (6-3), #1843 (6-4).

## Watch
- **🚨 Revert:** #1713 reverted #1690 (delegation feature re-add) — reconciles with BIT-213 retirement. New backlog row `revert-pr-1713` (score 3, open). No follow-up plan promoted (process resolution, not a code vector).
- **Unchecked TODOs in merged PRs:** 7 new — #1705 BIT-185 fault notifications, #1709 Story 11.6 payment reminders, #1768 BIT-239 messaging camelCase, #1703 Epic 12 meter/OCR stubs, #1717 Story 11.7 financial reports, #1685 BIT-145 IoT WS dashboard, #1723 (rustfmt-only — likely false positive but logged). All score 2, vector `bug`.
- **Closed-not-merged this window:** #1720, #1749, #1727, #1698, #1634 — four are supersede-noise (replaced by a successor that landed the same fix); #1634 saved-search alerts cursor pagination still needs to land.
- **New churn hotspots (top 3, all backend route monoliths still un-split):** `routes/financial.rs` (2380), `routes/buildings.rs` (2347), `routes/integrations/sync.rs` (2293). These join the 11 already split by PRs #1683/#1700/#1708/#1796/#1798/#1800/#1810/#1813/#1815/#1816 — same pattern, expect refactor PRs from the dispatcher next cycle.
- **Coverage-restoration churn:** #1808/#1817/#1821/#1825/#1824/#1823 (ACC + late security follow-ups) make `_bmad-output/implementation-artifacts/sprint-status.yaml` the most-churned single file (6 commits this window).
- **Stalled / draft signals:** no PR open > 7d with no reviewDecision (oldest open is #1754, 2d). The earlier `stalled-review-pr-988` and `stalled-review-pr-1179` rows remain seen but their PRs are not in this window's open set — no re-emit.
- **Best-effort gaps this run (deferred — catch-up scope):** per-PR `hotfix-no-test`, `risky-churn`, `screen-map-drift`, `fixme-in-merged-code` scans were **not run** across all 95 PRs (would have blown the routine budget). These are deferred to the next incremental run, where the cursor moves only ~1d.

## PRs stuck in draft despite approval
- none (the `list_pull_requests` payload doesn't carry `reviewDecision`; only per-PR `pull_request_read` calls reveal it. Sampling skipped this run due to catch-up scope.)

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **skipped this run** (catch-up scope; dispatcher's post-merge-review loop ran 74 follow-ups since cursor — marginal value of routine Phase 1.5 was low).
- Experts: n/a
- Findings: 0 routine Phase 1.5 findings (every code-review-finding signal in seen_signals from prior runs is either resolved or still tracked).
- Next segment: **`ppt-web-core`** — oldest unreviewed (2026-06-05, 20d stale). `review_cursor` advanced by 0 segments this run.
- Skipped: yes — routine catch-up run.

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0 — not yet seeded).
- Drift signals this run: **0 (scan deferred)** — the 95-PR window includes many `frontend/apps/reality-web/src/app/**` and `frontend/apps/ppt-web/src/routes/**` touches plus updates to 3 `docs/screens/ppt/*.md` files (announcements.md, admin-contextual-help.md, admin-support-data.md); per-PR drift scanning was not run this catch-up cycle.
- Orphan screens this run: **0** (same — scan deferred).
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New (16, all from this routine — not from the dispatcher):**
  - [3] `revert-pr-1713` — Revert of #1690 delegation feature (BIT-213 retirement reconcile).
  - [2] `unchecked-todo-pr-1685` — IoT real-time dashboard WS (Epic 14) merged with unchecked TODOs.
  - [2] `unchecked-todo-pr-1703` — Epic 12 meter reminders + OCR stubs merged with unchecked TODOs.
  - [2] `unchecked-todo-pr-1705` — BIT-185 fault notification lifecycle merged with unchecked TODOs.
  - [2] `unchecked-todo-pr-1709` — Story 11.6 payment reminders + auto-overdue merged with unchecked TODOs.
  - [2] `unchecked-todo-pr-1717` — Story 11.7 financial reports merged with unchecked TODOs.
  - [2] `unchecked-todo-pr-1723` — rustfmt-only follow-up has unchecked TODO box (likely false positive — review).
  - [2] `unchecked-todo-pr-1768` — BIT-239 messaging camelCase wire merged with unchecked TODOs.
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-financial-rs` — 2380 lines churn, candidate for split.
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-buildings-rs` — 2347 lines churn, candidate for split.
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-integrations-sync-rs` — 2293 lines churn, candidate for split.
  - [1] `closed-not-merged-pr-1720` — superseded by #1694.
  - [1] `closed-not-merged-pr-1749` — superseded by #1748.
  - [1] `closed-not-merged-pr-1727` — superseded by #1721 + #1728.
  - [1] `closed-not-merged-pr-1698` — superseded by #1695.
  - [1] `closed-not-merged-pr-1634` — saved-search alerts cursor pagination still needs to land.
- **Bumped:** none this run (no incoming signal targeted an existing-id with `score_delta > 0` carrying a high bump).
- **Decayed (6 items, score −1 each — last activity > 14d):** `code-review-mobile-native-kmp-deeplink-token-not-url-decoded` (2→1), `code-review-mobile-native-kmp-search-stale-response-race` (2→1), `test-gap-screen-map-drift-pr-1085-reality` (2→1), `test-gap-screen-map-drift-pr-1100-ppt` (2→1), `bug-risky-churn-pr-963-api-main-rs` (2→1), `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` (2→1).
- **Dropped to `dropped` (9 items, decayed below 0):** `triage-issue-1151`, `churn-hotspot-mobile-native-listing-detail-kt`, `churn-hotspot-mobile-native-search-screen-kt`, `code-review-mobile-native-kmp-deeplink-android-bypasses-shared-router`, `refactor-churn-hotspot-mobile-announcements`, `refactor-churn-hotspot-mobile-announcements-test`, `refactor-churn-hotspot-mobile-documents`, `triage-dispatcher-mcp-push-large-file-issue-1014`, `triage-issue-951`.
- **Resolved (15 items, `done`):**
  - `code-review-mobile-rn-report-fault-fake-submit` → PR #1679 (plan archived to `plans/_archive/`).
  - `code-review-api-core-osrng-expect` → PR #1684.
  - 11 churn-hotspot rows resolved by module-split PRs: `document.rs` (#1683), `subscription.rs` (#1796), `sensor.rs` (#1810), `emergency.rs` (#1798), `enhanced_tenant_screening.rs` (#1800), `vendors.rs` (#1813), `iot.rs` (#1815), `reserve_funds.rs` (#1816), `aml_dsa.rs` (#1708), `forms.rs` (#1700) + `repeated-churn-…-forms.rs` (same PR).
  - `churn-hotspot-frontend-apps-mobile-app-config-icon-test-ts` → PR #1718 (helpers extracted).
  - `churn-hotspot-backend-crates-db-tests-form-rls-repo-tests-rs` → PR #1719 (dedupe inline RLS setup).
- **Partial (kept `open`, evidence appended):**
  - `code-review-mobile-rn-screens-mock-data` — PR #1681 wired Buildings + Leases; Meters/PersonMonths/Notifications/Threads/Forms still on MOCK_* arrays.
  - `churn-hotspot-backend-crates-integrations-src-booking-rs` — PR #1693 split ota_xml into a submodule; booking/mod.rs still churned ~2.8k lines this window.

## Plans promoted
- **none this run.** Only one item crossed the readiness gate (`revert-pr-1713`, score 3, vector bug) but Phase 3.5 adversarial pass rolled it back: the revert is a process resolution (BIT-213 reconcile), not an actionable code vector — the hypothesis would be hand-wavy ("investigate why #1690 was merged after retirement"), the test plan would be a thin guard-test of low value. Mark in evidence: `promotion rolled back: revert is process noise, not code work`.

## Auto-fix
- `no candidate` — the eligible candidate set was zero this run. Of the 16 new signals, none matched the certainty allowlist with `confidence == high` AND `score >= 3` AND a safe-shape match: the unchecked-TODO PRs and the revert score 2-3 but neither produces an auto-fix shape on the allowlist (no `screen-map-drift` / `screen-map-orphan` / `lockfile-drift` / `fixme-in-merged-code` / `stalled-review` qualifier fired this run because their detail scans were deferred to next cycle).

## Self-review findings
- `.research/self-improvement/findings.json` absent / empty — none

## Open questions
- **Phase 1.5 (rotating expert review) skipped** — should it run on the next incremental cycle even though the dispatcher is already producing post-merge findings? Recommendation: yes — Phase 1.5 reviews *segments that the dispatcher hasn't touched* (e.g. `ppt-web-core` last reviewed 20d ago). `review_cursor` not advanced this run.
- **Phase 1.6 (project management) skipped** — `pm_cursor.next_index=5` (pm-security, last run 2026-05-27 = 29d stale). Dispatcher's post-merge-review reports cover most of this surface but the rotating PM artifacts (`.research/management/action-list.json`, `risks.json`, `project-state.md`) were last regenerated 2026-06-23 (deep scan) — no new role rotation entry today. `pm_cursor` not advanced this run.
- **`unchecked-todo-pr-1723`** — rustfmt-only PR shouldn't have an unchecked TODO box; the agent flag may be a false positive (e.g. the PR body quotes a `- [ ]` from elsewhere). Verify next cycle and drop if so.
- **Cursor reanchor** — `last_commit_sha = 4829015b` was no longer in the local tree (force-update history). The reanchor uses `origin/dev` HEAD; if the dispatcher rewrites history again, the next run will detect via the `git rev-parse` fallback and emit `lag_warning` again rather than silently misreporting churn.

## Quiet day?
- **No.** Catch-up run after 9-day cloud-cron lag. 95 merged PRs surveyed, 16 new backlog vectors, 15 resolutions (incl. 11 churn-hotspot splits landed by the dispatcher), 2 partial resolutions, 9 dropped, 6 decayed, 1 plan archived. `stats.runs` 40 → 41, `vectors_created` 173 → 189, `plans_created` 12 (unchanged), `quiet_days` 1 (unchanged). The dispatcher carried the load — this routine is the read-side audit that catches what the dispatcher's per-PR review didn't, and surfaces the lag.
