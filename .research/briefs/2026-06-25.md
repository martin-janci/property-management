# 2026-06-25

## Since last run
- Merged PRs: **95** (range #1567–#1829) — span covers ~9 days of accumulated activity (routine missed cycles).
- Open PRs touched: **22** (19 drafts, 3 review-ready: #1811, #1818, #1820); all 22 created within last 2 days.
- New / updated issues: **50** since #1437 (range #1663–#1828) — 34 open, 16 closed; **0 untriaged** (every recent issue carries at least one label, so triage queue did not grow from issues this run).
- Commits on `dev`: 49 since 2026-06-16 (origin/dev was force-pushed mid-window — the stored `last_commit_sha=4829015b` is no longer reachable; cursor fell back to `last_run_iso` timestamp).
- Routine lag: **8d 23h since last run** (`lag_warning` fired; signal id `lag-warning-2026-06-25`). Cloud routine missed cycles between 2026-06-16 and today.
- **Stale-routine alert:** state.json `last_run_iso=2026-06-16T03:25:00Z` is 8d old — cloud routine likely paused or scheduler misfired. Dispatcher state on `planning` branch is current (last update 2026-06-25 02:11 per dev commit log), so the gap is the *daily research* loop only.
- Phases that failed: **none**. Phase 1.6 (`ppt-project-management`) ran in a background subagent; results folded in below.

## Shipped
**95 PRs across 9 days — pulling the high-signal subset (full list in `signals/2026-06-25.json` audit trail and `merged_prs.json` cache).**

**Refactor / churn-resolution wave** (resolves 10+ open churn-hotspot backlog items):
- **#1683** — split `db/src/repositories/document.rs` into cohesive sub-modules (resolves `churn-hotspot-…-document-rs`).
- **#1796** — split subscription repository (resolves `…-subscription-rs`).
- **#1810** — split sensor repository (resolves `…-sensor-rs`).
- **#1693** — split `integrations/booking` OTA XML (resolves `…-booking-rs`).
- **#1700** — split forms.rs into submodules (resolves `repeated-churn-…-forms-rs`).
- **#1798** — split emergency routes into `emergency/{incidents,broadcasts,protocols,mod}.rs` (resolves `…-emergency-rs`). **⚠ Regression introduced — see Watch.**
- **#1800** — split enhanced_tenant_screening route (resolves `…-enhanced-tenant-screening-rs`).
- **#1708** — split aml_dsa route (resolves `…-aml-dsa-rs`).
- **#1815 / #1813** — split iot.rs + vendors.rs route monoliths.
- **#1816** — split reserve_funds route (resolves `…-reserve-funds-rs`).
- **#1781** — collapse duplicated `form.list()` sort SQL.

**Security / hardening fixes:**
- **#1684** — `propagate CSPRNG failure in crypto encrypt instead of expect()` (resolves `code-review-api-core-osrng-expect` from prior Phase 1.5 review).
- **#1741** — require manager role to list Airbnb reservations (guest-PII gate, #1667).
- **#1737** — remove duplicate JWT-trusting sensor WS channel; converge on DB-checked handler (#1668).
- **#1746** — validate portal-listings enums/status + ownership IDOR tests + FE field wiring (#1671).
- **#1755** — resolve duplicate migration version 192 + add dup-version CI guard.
- **#1779** — harden report presigned-download path (gap-81-2).

**Features (Epic 3 / 11 / 18 / accounting / messaging):**
- **#1689** — N-party group conversations (BIT-183 / UC-05.8).
- **#1702** — message attachments S3 presigned upload flow (BIT-184, UC-05.9).
- **#1705** — fire notifications on every fault lifecycle event (BIT-185).
- **#1709** — payment reminders + auto-overdue transitions (Story 11.6 / BIT-176).
- **#1716** — event bus retry + exponential backoff (Story 2B.1).
- **#1726** — Stripe Checkout payment integration (Story 11.5, BIT-181).
- **#1750** — guest ID-document upload + OCR extract seam (Story 18.2).
- **#1808 / #1817 / #1809** — accounting MVP: public marketing/landing + signup, generalized invoicing spec, PAP-321 F1/F2 hardening follow-ups.
- **#1681** — wire mobile Buildings & Leases screens to real API (resolves `code-review-mobile-rn-screens-mock-data`).
- **#1822** — reality-web SSO callback e2e coverage (gap-79-2-auth-callback-e2e).
- **#1829** — finish Support Data Access (fix refresh_tokens column bug + reconcile).

**Reverts:**
- **#1713** — `revert(delegations)`: removes the re-added delegation frontend that #1690 had landed; reconciliation with the BIT-213 retirement decision (intentional revert, not a regression rollback). Signal `revert-pr-1713` at +3, vector=`refactor`.

## Watch
- **🚨 SECURITY (high) — `code-review-api-handlers-install-no-manager-gate`** (score 3, ready): `integrations/install.rs` Airbnb/Booking connect/disconnect + portal-connection handlers only call `verify_org_access` (membership) and never `verify_manager_role_in_org` (defined in sibling `integrations/sync.rs:161` with explicit `SECURITY #1525` warning). Any regular org member can wire or unwire the org's OAuth credentials. Plan promoted; see `plans/code-review-api-handlers-install-no-manager-gate.md`.
- **🚨 SECURITY (medium) — `code-review-api-handlers-emergency-lifecycle-no-gate`** (score 2, security fast-track, ready): `emergency/incidents.rs` `update_incident`, `resolve_incident`, `close_incident` lost the `is_emergency_manager` gate while sibling `create_incident` + `acknowledge_incident` + `broadcasts.rs::deactivate_broadcast` all hold it. **Likely regression from the PR #1798 split** (merged 2026-06-24). Plan promoted; see `plans/code-review-api-handlers-emergency-lifecycle-no-gate.md`.
- **BUG (medium) — `code-review-api-handlers-buildings-rls-leak`** (score 2, NOT promoted — bug threshold is score≥3): `buildings.rs::assign_unit_owner` (lines 1891–1989) has multiple `?`-early-returns that don't call `rls.release().await` first; `RlsConnection::Drop` emits `tracing::warn!` and spawns background cleanup on every un-released drop. Per-PR warn-spam + delayed pool return. Will decay if no new evidence; current open in backlog.
- **Reverts:** 1 this run (#1713 — intentional delegations retirement reconciliation; no instability indicator).
- **Churn hotspots this run (top 3, after exclusions):**
  - `backend/crates/db/src/repositories/document.rs` (6,146 lines — driven by PR #1683 module split; resolved-via-PR, not real instability)
  - `frontend/apps/ppt-web/messages/en.json` (3,732 lines — i18n string churn from accounting/messaging/IoT feature waves; NEW hotspot, runs_seen=1)
  - `backend/servers/api-server/src/routes/emergency.rs` (3,362 lines — PR #1798 split; resolved-via-PR, but the split itself introduced the gate regression above)
- **Stalled review / approved-but-draft:** none this run (all 22 open PRs are 0–1 day old).
- **Closed-not-merged this run:** 5 (#1634 saved-search alerts truncation, #1698 unit management UI superseded, #1720 leases nav link, #1727 CI unblock, #1749 mobile tsc typecheck exclude) — no new backlog rows (informational).
- **Decay sweep:** 11 open items lost −1 (no signal in 14d); **10 dropped** at score 0 (mostly stale mobile-native churn rows and process-tracking triage stubs).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **api-handlers** (`backend/servers/api-server/src/routes/`) — reason: churn-aligned (top hotspot `emergency.rs` split this run + 15d since last review).
- Experts: **rust**.
- Findings: **3** — `code-review-api-handlers-install-no-manager-gate` (high, +3), `code-review-api-handlers-emergency-lifecycle-no-gate` (high, +2 with security fast-track), `code-review-api-handlers-buildings-rls-leak` (high, +2 bug-vector). Two promoted to plans; one (buildings rls-leak) below bug-vector promotion threshold (needs score ≥ 3).
- Next segment: **ppt-web-core** (last reviewed 2026-06-05 — oldest after this run advances `api-handlers`).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: **141** (ppt: 94, reality: 47, mobile: 0 — not yet seeded).
- Drift signals this run: **0**. PR #1822 (reality-web SSO callback e2e) touched `frontend/apps/reality-web/src/app/[locale]/auth/callback/page.test.tsx` — a test-only file, not a real route change. Signal **filtered as false-positive** per heuristic caveat (test files in route directories don't reflect screen-map drift); documented here for audit transparency.
- Orphan screens this run: **0** (no route basenames added/removed this run — only test-file additions and Rust refactors).
- Last `screen-map.yml` CI run on `main`: not fetched this run (no `docs/screens/**` edits in the merge stream to trigger).

## Backlog deltas
- **New:** [3] `code-review-api-handlers-install-no-manager-gate` — security/high → promoted
- **New:** [3] `revert-pr-1713` — refactor/high (intentional delegations retirement)
- **New:** [2] `code-review-api-handlers-emergency-lifecycle-no-gate` — security/high → promoted (fast-track)
- **New:** [2] `code-review-api-handlers-buildings-rls-leak` — bug/high (below promotion threshold)
- **New:** [2 × 8] `unchecked-todo-pr-{1641,1685,1703,1705,1709,1717,1723,1768}` — 8 unchecked-TODO signals from this window's merged feature PRs; mostly CI-checklist boxes (`low` confidence) except #1641, #1685, #1768 which carry real manual-QA gaps (`medium`).
- **New:** [1] `churn-hotspot-frontend-apps-ppt-web-messages-en-json` — refactor/high (i18n string churn, runs_seen=1)
- **Cumulative re-emit (no score delta):** `churn-hotspot-…-document-rs`, `churn-hotspot-…-emergency-rs` — both already resolved-via-PR; new churn evidence appended for record.
- **Resolved (open → done):** 13 — 10 churn-hotspot items via dispatcher-shipped splits (#1683, #1693, #1700, #1708, #1718, #1719, #1796, #1798, #1800, #1810, #1816), 1 code-review item (`code-review-api-core-osrng-expect` via #1684), 1 mobile screen wiring (`code-review-mobile-rn-screens-mock-data` via #1681), 1 forms-rs repeated-churn via #1700. All marked via Phase-2 PR-body verbatim-ID match (code patterns not independently verified — implementer can re-open if false positives surface).
- **Decayed (−1, no signal in 14d):** 11 — see backlog.md for the full list.
- **Dropped (decay → score 0):** 10 — `churn-hotspot-…-vendors-rs`, `triage-issue-1151`, `churn-hotspot-mobile-native-listing-detail-kt`, `churn-hotspot-mobile-native-search-screen-kt`, `code-review-mobile-native-kmp-deeplink-android-bypasses-shared-router`, `refactor-churn-hotspot-mobile-announcements`, `refactor-churn-hotspot-mobile-announcements-test`, `refactor-churn-hotspot-mobile-documents`, `triage-dispatcher-mcp-push-large-file-issue-1014`, `triage-issue-951`.

## Plans promoted
- `plans/code-review-api-handlers-install-no-manager-gate.md` — add `verify_manager_role_in_org` to all mutating Airbnb/Booking/portal install handlers · adversarial pass: **passed** (source→hypothesis→approach→test chain unbroken; test would 200 today, 403 after fix — real regression test; Files paths all resolve on disk; no placeholder rot).
- `plans/code-review-api-handlers-emergency-lifecycle-no-gate.md` — restore `is_emergency_manager` gate to `update_incident` / `resolve_incident` / `close_incident` in `emergency/incidents.rs` (likely PR #1798 split regression) · adversarial pass: **passed** (same chain check; root-cause traced to PR #1798; positive + negative test scenarios named).

## Auto-fix
- `no candidate` — no signal this run met `confidence == "high"` + `score >= 3` + allowlist (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, lockfile-drift sub-shape of `dep-update-noise`, `stalled-review`). The two promoted security items are real code fixes, not within the safe-shape allowlist; manual implementer path. (Lag-warning fired but is informational; auto-fix never targets `lag_warning`.)

## Self-review findings
- open=8 (high=2, medium=4, low=2); new this run=0; acknowledged=19
- fp-? [recurrence=5] — Phase 6 archive-move produced 198KB `assignments-archive.json`; `mcp__github__push_files` can't inline + git push proxy-403'd; fix: push state commit to NEW branch, open PR via MCP, merge via MCP (workaround proven this run-window).
- fp-? [recurrence=3] — Phase 2.6 buffer reports claimable=54/open=54 but REAL claimable=0; all 54 already terminal in assignments-archive; fix: extend gc1-reconcile or add action-list-reconcile pass to auto-close terminal-ID matches.

## PM rotation (Phase 1.6 — pm-security + Scrum Master)
- Sprint: **Epic 6, 7A, 8A & 10A — Announcements, Documents, Notifications & OAuth** · **2/6** epics done.
- pm-security rotation surfaced **3 release-blocking gaps not previously tracked** (folded into action-list, not promoted as routine plans — pm-security owns these):
  1. **Unauthenticated `/api/v1/ai/ocr/*` endpoints** (`process_meter_reading` + `submit_correction` in `backend/servers/api-server/src/routes/ai/ocr.rs`) — callable without any token.
  2. **Third standalone `JwtService` token-verifier copy** (issue #1782) not yet migrated to unified path from PR #1744.
  3. **OAuth refresh-token revocation bypass** (issue #481) — `revoked_at IS NULL` predicate missing in `services/oauth.rs` refresh path (RFC 9700 violation; gates stories 10a-1 and 10a-3).
- Confirmed resolved: **#1829** fixed the 10b-5 support-data `refresh_tokens` column bug; `refresh_tokens` schema + `SessionRepository` queries now consistent.
- Coverage upkeep: 10b-5 promoted partial→done (#1829); 79-2 evidence appended (#1822); 6-5 evidence appended (5 messaging PRs); 5 churn-hotspot + 5 in-progress action-list items closed by merged splits. Action-list refilled to 36/36 open.
- Risks.json grew by 10 (5 pm-security, 5 pm-scrum-master); 1 prior risk (`pm-devops-dev-ci-discipline-1437`) resolved.
- Cursors advanced: `pm_cursor.next_index 5→6` (next role: **pm-data**, 28d stale); `coverage_cursor.next_index` wrapped `12→0` (one full epic rotation completed).
- Standing flag from Scrum Master: **sprint-status.yaml materially out of sync with coverage.json** — multiple done stories still labeled `ready-for-dev`. ACC workstream lacks sprint plan / story owners (6+ open drafts). These reconciliations live in `.research/management/action-list.json`; not promoted to routine plans (delivery-management scope, not implementation-scope).

## Open questions
- Should the routine treat 8d-old `last_run_iso` as a *hard* failure (refuse to commit, emit operator alert) instead of `lag_warning` + continue? Argument for: if cloud cron actually stopped, silent recovery hides the failure mode. Argument against: forcing intervention every time the cron misses a beat is over-zealous; the brief surfaces it already.
- The `screen-map-drift` heuristic continues to over-fire on test-file additions to route directories (filtered manually this run; PR #1822). Suggestion: add a `.test.tsx`/`.test.ts`/`.spec.*` exclusion to the trigger set in the routine prompt's drift-detection rules.
- Phase 1.6 surfaced three pm-security-owned release-blocking gaps that overlap with the Phase 1.5 / Phase 3 promotions in spirit but live in a different artifact (`.research/management/action-list.json`). Should the routine cross-link these — i.e., when pm-security surfaces an OAuth/auth gap, automatically scaffold a matching backlog row so the implementation agent picks it up? Currently they live in two parallel queues.

## Quiet day?
- No — 95 merged PRs, 15 new backlog vectors, 2 plans promoted, 3 code-review findings, 13 backlog items resolved-via-PR, 10 decay drops. Substantial state delta.
