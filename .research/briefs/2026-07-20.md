# 2026-07-20

## Since last run
- Merged PRs: 42 (range #2371–#2427)
- Open PRs touched: 1 (#2428 layout editor MVP, opened today)
- New / updated issues: 12 new (#2399–#2418, #2422); 13 older issues transitioned closed
- Commits: 50 on `dev`
- Routine lag: **4d 0h since last run** — stale_routine_alert fired (≥3d threshold). Cloud routine cron may be paused/rate-limited; dispatcher loop (separate branch) kept ticking through the gap.
- Phases that failed: none. Phase 1.6 (PM/scrum-master) **skipped in catch-up mode** — pm_cursor unchanged (still at index 5 = pm-security); dispatcher-owned management artifacts (post-merge-review.json, gc1-orphan-triage.md) were refreshed today by the separate dispatcher loop.

## Shipped
The 4-day window ate one big theme (Layout & Content Manager slices) and one large post-merge-review cleanup wave:

- **Layout & Content Manager (3 slices, all merged to `dev`)** — #2424 layout-core contract crate, #2426 control-plane tables + admin/tenant routes + resolved endpoints, #2427 defensive rendering on pilot screens (ppt dashboard + reality listing detail). Slice 4 (#2428, admin editor MVP) opened today, still open.
- **12 post-merge-review follow-up fixes (`gh-issue-XXXX` PRs)** landing between 2026-07-16 and 2026-07-17:
  - **Security**: #2374 role-gate deny-path regression test for `GET /ai/chat/escalated`; #2378 strip signing token from `/sign` URL + suppress Referer; #2416 fix cross-tenant IDOR in `government_portal` connection get/update/delete/test; #2421 guard cross-org `/rag/migrate` RLS under a NOBYPASSRLS role.
  - **Bugs**: #2372 mobile login clears whole query cache (cross-tenant stale data); #2376 `portal_webhook_events` dedup net for idempotent inquiry retries; #2377 gate portal view-webhook counter on dedup (no replay/retry inflation); #2380 validate every synthesized candidate id against `IdSchema` in `scanCandidates`; #2381 sync category enabled badge on optimistic trigger toggle; #2408 fix `/sign` reload regression + strip only the token param; #2409 reconcile favorites client to real reality-server DTO (fix no-op optimistic toggle); #2412 purge persisted AsyncStorage caches on mobile session change; #2419 fix scheduler-RLS comments citing wrong migration number (00216 → 00218); #2420 recover poisoned scheduler metrics mutex.
  - **UX/i18n**: #2384 localize create-schedule report labels + backend error toast; #2411 localize create-schedule form + map remaining error codes; #2415 delete orphaned `packages` i18n namespace from cs/de/hu/pl/sk.
  - **Test-gap / DX**: #2371 wire KMP MyListings + ListingAnalytics to portal API; #2373 consolidate reality-web listing-detail shape validation into `parseListingDetail()`; #2375 `/agencies/me` resolves only active memberships + tests; #2379 guard Decimal-as-string decode trap on sibling reality-server surfaces; #2382 cover AC-5 favorite price-alert toggle + optimistic OFF state; #2383 guard mobile upload allow-list drift + enforce photo MIME; #2407 guard against `src/data ↔ sitemap.json` drift; #2410 transcode iOS HEIC/HEIF to JPEG in DocumentUploadScreen photo picker; #2414 validate `IdSchema` at screen-map init write boundary.
  - **Routine-authored**: #2395 drop orphaned delegation/packages i18n namespaces from ppt-web/en.json; #2396 `@ppt/sitemap` default export for CJS resolution (E2E DX); #2397 make generated `sitemap.json` deterministic; #2398 persist Booking.com push sync state on connection.
- **Dependency bumps (9 merged, 2 rejected)**: minor-patch cascades across Cargo, npm, Kotlin, Gradle, and Expo 56→57; rejected #2385 (dtolnay/rust-toolchain 1.94→1.100 — too big a jump) and #2387 (npm 15-update rollup — superseded by the 19-update #2423).

## Watch
- Stalled reviews: none — #2428 is < 1 day old.
- Reverts: none in window.
- Churn hotspots (top 3, code-signal weak — mostly dep-bump / doc-refresh):
  - `frontend/apps/mobile/package.json` — 5 touches (Expo 56→57 cascade, dependabot rollup)
  - `docs/repo-map.md` — 4 touches (per-PR route-map refresh)
  - `backend/Cargo.toml` — 3 touches (layout-core add + cargo-minor-patch rollup + toolchain bumps)
- Follow-up issue still OPEN: **#2422** (backend, follow-up) — `get_document lacks application-layer org scoping (defense-in-depth gap; mitigated by FORCE RLS)`. Not yet auto-labeled untriaged (has labels), but worth a defense-in-depth pass alongside the new scheduler-cross-tenant finding below.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none — dispatcher-side `.research/management/assignments.json` shows 3 in-progress + 1 quarantined; no rows in `review` state with `verdict=changes`.

## Code review slice
- Segment reviewed: **api-core** (backend/servers/api-server/src/ excluding handlers/) (reason: oldest-unreviewed — tied 2026-07-13 with ppt-web-ui, alphabetical tiebreak; no churn alignment this run)
- Experts: rust (static analysis, no compile)
- Findings: 2 (both anchored in `backend/servers/api-server/src/services/scheduler.rs`)
  - `code-review-api-core-scheduler-target-ids-silent-parse` (+1 bug, confidence=high) — `serde_json::from_value(..).unwrap_or_default()` at :391 silently swallows malformed `target_ids`, downstream `ANY('{}')` notifies nobody with no log.
  - `code-review-api-core-scheduler-units-target-cross-tenant` (+2 security, confidence=medium) — units/buildings target queries lack `organization_id` AND-scope; defense-in-depth via join to `units.organization_id` would fail-closed regardless of handler validation. Complements open issue #2422 (same pattern class).
- Next segment: `ppt-web-ui` (oldest unreviewed after this run: 2026-07-13)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` 106 + `reality/` 47; `mobile/` not yet seeded → 0)
- Drift signals this run: 0 (no merged PR in window touched a route file without a matching product screen-doc edit)
- Orphan screens this run: 0 (no route→doc filename mismatch detected in this window)
- Last `screen-map.yml` CI run on `main`: 2026-06-04 22:19 UTC (`id=26982999099`, conclusion=success). No push-to-main in window (all merges were to `dev`).

## Backlog deltas
- **New:** [+1] `code-review-api-core-scheduler-target-ids-silent-parse` (bug) — see backlog.md
- **New:** [+2] `code-review-api-core-scheduler-units-target-cross-tenant` (security, medium-confidence — does NOT trigger security-fast-track which requires confidence=high) — see backlog.md
- **New:** [+1] `refactor-churn-hotspot-mobile-package-json-2026-07-20` (refactor) — dep-bump noise, not code drift
- **New:** [+1] `refactor-churn-hotspot-repo-map-md-2026-07-20` (refactor) — per-PR doc refresh
- **New:** [+1] `refactor-churn-hotspot-backend-cargo-toml-2026-07-20` (refactor) — workspace surface
- **New:** [+1] `dx-closed-not-merged-pr-2385` (dx) — dependabot rollup rejected
- **New:** [+1] `dx-closed-not-merged-pr-2387` (dx) — dependabot rollup superseded
- **Bumped:** none — no signal ID re-fired this run
- **Decayed:** none — no items in `open`/`ready`/`deferred` state pre-run (last surviving open items were reconciled to `done` by the 2026-07-16 run)
- **Dropped:** none this run

## Plans promoted
- none — no new item hit the readiness bar (score ≥ 3 general, or security-fast-track score ≥ 2 with confidence=high). Security finding is confidence=medium so it stays open awaiting confirmation from the create-announcement path.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist (screen-map-drift / screen-map-orphan / fixme-in-merged-code / dep-update-noise lockfile-drift / stalled-review) this run.

## Self-review findings
- open=21 (high=4, medium=13, low=4); new this run=0; acknowledged=19.
- fp-null [recurrence=10] — dispatcher Phase 6 archive-move produces 198KB+ assignments-archive.json; mcp push_files 64KB inline limit + git-push-proxy-403 combine to drop the archive push; fix: adopt PR+API-merge path as primary push mechanism.
- fp-null [recurrence=6] — dispatcher-prompt hard-codes proxy-403 assumption; in this env `git push origin dev` succeeded once GH007 email was fixed; fix: runtime-probe push capability at Phase 1 and bypass 64KiB size-guard when git push works.
- fp-null [recurrence=3] — Phase 2.6 buffer reported claimable=54/open=54 but real claimable=0; all open action-list items already terminal in archive; fix: extend gc1-reconcile to auto-close ANY id shape (including status=failed) and adjust GC3 formula to subtract archive ids.
- fp-null [recurrence=2] — Tier-2 planner kick returned http=401; DISPATCHER_TOKEN/DISPATCHER_URL rejected; fix: operator rotates the secret + dispatcher branches on 401/403 as OPERATOR-CONFIG escalation.

## Open questions
- Is the scheduler cross-tenant `target_ids` finding exploitable in practice? Needs a read of the create-announcement path to confirm whether `target_ids` (units/buildings) are validated against the caller's org before persist. If yes → defense-in-depth only (low priority); if no → real IDOR (promote to plan next run).
- Why is the routine cron 4 days stale? Dispatcher branch kept ticking, so the schedule itself works — routine-specific trigger is the suspect. Operator should check the cloud cron config.

## Quiet day?
- No — 42 PRs merged in window (though not on today's calendar day), 7 new backlog vectors added, 2 code-review findings surfaced, 1 open follow-up issue (#2422) still on the board.
