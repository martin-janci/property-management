# 2026-08-24

## Since last run
- Merged PRs: 13 (range #2821–#2838)
- Open PRs touched: 0 (17 open, all last-updated before the last run window)
- New / updated issues: 6 (all now closed; 5 `from-merged-review` follow-ups + #2743 dispatcher recurrence)
- Commits: 62 on `dev`
- Routine lag: 2d 0h since last run (last_run_iso 2026-08-22T03:03:20Z; over the 36h lag threshold)
- Phases that failed: none

## Shipped
- #2821 code-review-api-handlers-booking-connect-non-manager-hijack — gate direct-connect OTA credential writes on manager role
- #2825 gh-issue-2824 — VerificationBadge expiry i18n + de-duplicate expiry logic
- #2826 gh-issue-2823 — per-channel bookkeeping + bounded retry for held-notification drain
- #2827 gh-issue-2822 — CSV export sanitizer neutralises embedded CR/LF (row-injection fix, no new TODO)
- #2828 code-review-ppt-web-ui-facilities-booking-silent-errors — surface booking fetch/approve/reject/cancel errors
- #2829 code-review-ppt-web-ui-aml-dashboard-prompt-alert-flow — replace AML dashboard prompt/alert with in-app dialogs + i18n
- #2830 code-review-ppt-web-ui-facilities-booking-hardcoded-i18n — i18n facilities booking UI strings
- #2833 gh-issue-2832 — reset AML EDD/Review dialog state per assessment (follow-up to #2829)
- #2834 gh-issue-2831 — atomic replica claim so quiet-hours drain delivers held notifications at-most-once (follow-up to #2826)
- #2835 code-review-mobile-rn-votedetail-conditional-hooks — run VoteDetailScreen hooks before the voteId early return
- #2836 code-review-mobile-rn-thread-detail-hardcoded-en — localize ThreadDetailScreen UI strings
- #2837 code-review-mobile-rn-voice-cmd-hardcoded-en — localize voice-assistant confirmation / error strings
- #2838 churn-hotspot-voice-webhooks-rs-retry1 — centralize voice OAuth token encryption

## Watch
- Stalled review: **PR #2744** (11d) — approved by the dispatcher reviewer, CI green on all 28 checks, `mergeable_state=clean`, but stuck in DRAFT with label `needs-human-review`. This PR fixes the recurrence-15 self-improvement finding (dispatcher archive-push wedge). Human un-draft + merge is the only unblock.
- Stalled review: PR #2555 / #2558 / #2559 (26d, all UC-ACC-05 accounting) — unchanged since last run; already in backlog as `needs-human-judgement`.
- Reverted: none this run.
- Churn hotspots: `backend/servers/api-server/src/services/quiet_hours_drain.rs` (242 lines this run, runs_seen=1), `frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.tsx` (199 lines, runs_seen=1), `frontend/apps/ppt-web/src/features/compliance/components/ReviewAssessmentDialog.tsx` (153 lines, runs_seen=1). Two files patched twice within 48h each — pm-qa flagged the same double-patch pattern as evidence the pre-merge review gate is under-catching.

## PRs stuck in draft despite approval
- #2744 fix(dispatcher): un-wedge oversize-archive push (#1162) + close retry-remint cross-id ghost (#2460) — last update 2026-08-13T06:50Z, age ≈ 11d (~264h). Dispatcher-side reviewer verdict=approve; 28 checks all green/skipped, verify-all.sh --quick success. Blocks the whole implementer pipeline (buffer 0/6 on every dispatcher tick since 2026-08-23 12:25).

## PRs with verdict=changes, no fix-round progress in 24h
- none — the sole active row in `assignments.json` is PR #2744 with verdict=**approve** (see above).

## Code review slice
- Segment reviewed: **mobile-native-kmp** (reason: tied for oldest-unreviewed at 2026-08-16 and, unlike ppt-web-ui, had had no dispatcher Tier-1d review in the last 48h)
- Experts: kotlin + tester (per skill defaults for the segment)
- Findings: 3 landable (see Backlog deltas — one high-severity Android auth-persistence bug, two medium-severity Android "fake data on live release routes")
- Next segment: mobile-native-kmp still holds the oldest date for anything not touched today; the ordering rotates to the next-oldest segment on the next run (ppt-web-ui @ 2026-08-16 → api-core / ppt-web-core @ 2026-08-21 → api-handlers @ 2026-08-22, once cursor tiebreaks apply).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` 106, `reality/` 47, `mobile/` 0 — mobile still unseeded; drift detection remains disabled for it per the routine prompt)
- Drift signals this run: 0 (no merged PR this window touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**` — all UI churn was inside `features/`)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not checked this run (routes untouched, no reason to scan CI)

## Backlog deltas
- **New:** [3] code-review-mobile-native-kmp-android-session-not-persisted — Android SsoService scoped to MainActivity + no session store; rotation / cold restart silently signs the user out
- **New:** [2] code-review-mobile-native-kmp-create-listing-stub-submit — CreateListingScreen Publish handler is a hard `NotImplementedError` stub while `POST /api/v1/my/listings` is a live 201-returning endpoint
- **New:** [2] code-review-mobile-native-kmp-agency-hub-fabricated-metrics — AgencyHubScreen renders hardcoded "42 active / 18 new / 2.4h avg" stats and an invented activity feed on a live release route
- **New:** [1] stalled-review-pr-2744 — dispatcher PR blocked in DRAFT for 11d despite approval + green CI (see *Watch*)

## Plans promoted
- `plans/code-review-mobile-native-kmp-android-session-not-persisted.md` — persist SSO session on Android (KMP `expect/actual SessionTokenStore` + Application-scoped `SsoService` + startup `restoreSession()`) · adversarial pass: passed
- (2nd plan slot deliberately unused this run — the other two mobile-native findings sit at score 2, below the promotion floor of 3, and they merit their own PRs rather than being bundled)

## Auto-fix
- no candidate — the only Phase 5 allowlist match this run was `stalled-review-pr-2744`, and the routine intentionally does not post a nag comment: a Claude-Code-authored ping on a PR whose problem is "waiting on the human to click Ready-for-review" would be noise that trains the human to ignore future routine comments. The push notification tied to this brief carries the exact same information to the right addressee.

## Self-review findings
- open=34 (high=7, medium=24, low=3); new this run=0; acknowledged=19
- fp-null [recurrence=15] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked → PUSH_METHOD defaults to MCP `push_files` + 64KiB size-guard fail-closes; PR #2744 (open) is the fix. Same finding drives *Watch* above.
- fp-null [recurrence=15] — Phase 6 archive-move produced 198KB assignments-archive.json (now 758KB); MCP inline push can't carry it → merged PR #1019 row dropped without archive persistence, Merged-total understated by 1. PR #2744 also lands the fix.
- fp-null [recurrence=8] — Buffer drained (claimable=0/72) yet Tier-1d fired again; 61 `*tier1d*.json` files under `.research/signals/` never became claimable backlog rows. Signals-to-backlog conversion path is broken.
- fp-null [recurrence=5] — action-list.json holds 31 status=in-progress rows but assignments.json has only 3 live rows; the 3 live rows are twin-status inverted (open in action-list). Twin-status coupling is decoupled.
- fp-null [recurrence=3] — implementer sandbox grace expiry (5 in-progress rows produced 0 branches on origin across 2 implementer generations). Local sandbox never produces a remote branch.
- fp-null [recurrence=3] — Phase 2.6 buffer reported claimable=54 but real claimable=0; the 54 open items were already terminal in `assignments-archive` by exact id. Reconciler misses cross-file terminal state.
- fp-null [recurrence=2] — Tier-2 upstream planner kick returned HTTP 401; DISPATCHER_TOKEN/DISPATCHER_URL rejected → no working refill path when Tier-1b avail=0.

## Open questions
- Nothing needing user judgement beyond the PR-#2744 un-draft ask above.

## Quiet day?
- **No.** 13 merged PRs, 3 verified new code-review findings (one promoted), 4 new backlog vectors, 5 new hotspot/lag process signals, and one high-signal push notification.
