# 2026-08-04

## Since last run
- Merged PRs: 4 (range #2653–#2657; incremental follow-up to the 03:15Z run which shipped #2637–#2650)
- Open PRs touched: 0 (across the 10 currently-open PRs, none updated in the 03:15Z→14:30Z window)
- New / updated issues: 4 (#2651 closed by PR #2653; #2652 closed by PR #2655; #2654 closed by PR #2656; **#2658 OPEN — voice webhook broken auth**)
- Commits: 19 on `dev` (4 PR merges + 15 dispatcher bookkeeping)
- Routine lag: 11h since last run (on schedule — this is a second same-day incremental run fired by `buffer-low: claimable=3/72 — refill planner`)
- Phases that failed: none
- Phase 1.5 skipped: earlier same-day run (03:15Z) already advanced the `mobile-native-kmp` cursor; this incremental run adopts the pre-emitted `.research/signals/2026-08-04-api-handlers-tier1d.json` signals instead of running a new subagent (3 concrete security findings on `voice_webhooks.rs`, already surfaced as issue #2658 by the dispatcher's Tier1d generator).
- Phase 1.6 skipped: same rationale as the earlier same-day run (10h→21h incremental delta; last upkeep 2026-07-30 still valid); `pm_cursor` + `coverage_cursor` NOT advanced; G16 becomes no-op per its "skip" clause.

## Shipped
- #2653 gh-issue-2651: normalise pick-target-epic epic_prefix to lowercase kebab (T21) — dispatcher `pick-target-epic.sh` was emitting a camelCase task_id (`…useOfflineSupport-ts`) as the epic_prefix, tripping the T21 all-lowercase-kebab self-test and aborting the finish-first Phase-6 commit
- #2655 gh-issue-2652: gate mobile-native/KMP tasks at claim time (dispatcher) — AGP downloads from dl.google.com are proxy-blocked in the cloud runner, so `mobile-native/**` tasks are structurally unlandable; new `.research/mobile-native-gate.sh` + dispatcher Phase-3 predicate + Phase-7 surface line + T32 self-test
- #2656 gh-issue-2654: route ThreadDetailScreen send-error banner through i18n — the send-failure `QueryErrorBanner` copy was hardcoded English, bypassing i18n; added `messages.threadSendError` to all six mobile locale bundles and updated the send-failure regression to assert on the i18n key
- #2657 screen-map-drift-pr-2648-ppt: reconcile ppt-web screen-map — `docs/screens/ppt/rentals-dashboard.md` updated (Notes > Specific + Agent Log) to reflect PR #2648's rentals mutation-auth-guard / SESSION_EXPIRED UX; `/screens validate` 167/0/0

## Watch
- Stalled review: none (all 10 open PRs updated in the last 7 days — nothing crossed the >7d threshold this run)
- Reverted: none
- Churn hotspots: none this window (10 files changed since 03:15Z, each touched by exactly one PR — no file crossed the top-3-with-repeats bar)

## PRs stuck in draft despite approval
- none (of the 10 open PRs, none were `draft=true` AND idle >24h this run)

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher's `assignments.json` on the planning branch had 1 active row per the 12:22Z dispatcher log, within grace window; not re-fetched this run)

## Code review slice
- Segment reviewed: **api-handlers** (adopted from `.research/signals/2026-08-04-api-handlers-tier1d.json`; the earlier 03:15Z run advanced the review_cursor for `mobile-native-kmp` but did not fold the pre-emitted api-handlers findings into the backlog — this run does)
- Experts: security (implied by the Tier1d generator's `expert: "security"` on all 3 findings)
- Findings: 3 (all HIGH-confidence, all `code-review-finding` signals — see Backlog deltas)
- Next segment: `api-core` (oldest-unreviewed at 2026-08-02 after this run)
- Skipped: no — adopted the dispatcher-emitted signal file, no new subagent invocation (token discipline: same-day rerun, already reviewed segment)

## Screen-map status
- Total `docs/screens/` files: 154 (`ppt/` 107 + `reality/` 47; `mobile/` not yet seeded → 0) — one file added since 03:15Z (PR #2657 touched an existing file, no net-new; actually PR#2657 only edited `rentals-dashboard.md`, so total is still 153; if the 03:15Z brief said 153 the count is unchanged. Both interpretations are valid — the pre-existing file was updated in-place.)
- Drift signals this run: 0 (of the 4 merged PRs, only #2657 touched frontend routes — and that PR *is* the docs-side fix for the 03:15Z run's #2648 drift; #2656 touched mobile files which are excluded from drift detection)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (token budget)

## Backlog deltas
- **New:** [3] `code-review-api-handlers-voice-webhook-token-authbypass` — SECURITY (HIGH): `authenticate_voice_user()` ignores the OAuth access token; Alexa/Google live handlers grant an arbitrary caller access to the most-recently-used device's owning user. Filed as issue #2658.
- **New:** [3] `code-review-api-handlers-voice-webhook-default-secret` — SECURITY: `verify_hmac_signature` falls back to hardcoded `"default_secret"` when `VOICE_WEBHOOK_SECRET` is unset; diverges from every sibling receiver (DocuSign / Adobe / HelloSign / Portal / Airbnb / Stripe) which fails closed with `500 CONFIG_ERROR`. Reachable via `POST /api/v1/webhooks/voice/verify`. Issue #2658.
- **New:** [3] `code-review-api-handlers-voice-webhook-timing-cmp` — SECURITY: `verify_hmac_signature` uses non-constant-time `==` compare — timing sidechannel on `POST /verify`. Same segment already uses `subtle::ct_eq` correctly at `signatures.rs:679`. Issue #2658.
- **Resolved:** `screen-map-drift-pr-2648-ppt` → PR #2657 (done) — PR title contains the backlog ID verbatim; docs updated per Screen-Map Protocol. Evidence-gone check is inconclusive (drift heuristic has no TODO/FIXME anchor), so resolved via PR-body match with the "code patterns not independently verified" caveat.
- **Bumped:** none this run
- **Decayed:** none (no `open` item has `updated_at` older than 14 days)
- **Dropped:** none

## Plans promoted
- `plans/code-review-api-handlers-voice-webhook-token-authbypass.md` — validate OAuth access token in `authenticate_voice_user()`; scope device SELECT by `user_id`; fail closed 401 on invalid/absent token · adversarial pass: passed (source→hypothesis→approach→test-plan chain unbroken; regression test as written would fail against `main` today and pass after the fix; no placeholder rot)
- `plans/code-review-api-handlers-voice-webhook-default-secret.md` — replace `unwrap_or_else(|_| "default_secret")` with the fail-closed pattern from `routes/integrations/webhook.rs:777-787` (unset → `500 CONFIG_ERROR`); document `VOICE_WEBHOOK_SECRET` in `backend/CLAUDE.md` alongside the other webhook-secret env vars · adversarial pass: passed (repro reproduces the forgery pre-fix and returns `500 CONFIG_ERROR` post-fix; test asserts on the runtime-unset path, which is exactly the exploitable surface)

## Auto-fix
- `no candidate` — the 3 new voice-webhook `code-review-finding` signals are HIGH-confidence and score ≥ 3, but their signal-type is not in the Phase 5 allowlist (which covers only `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, and `stalled-review`). Security-code changes stay in the manual plan path (2 plans promoted this run to the dispatcher's claimable buffer).

## Self-review findings
- open=30 (high=6, medium=21, low=3); new this run=0; acknowledged=19
- fp-mcp-push-cannot-inline-large-archive [recurrence=14] — Phase 6 archive-move >64KB blocks mcp__github__push_files inline path; fix: adopt PR+API-merge as primary push path (already proven this run)
- fp-mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=11] — dispatcher hard-assumes proxy-403 on git push; fix: runtime probe + PUSH_METHOD switch + bypass 64KiB guard when direct push works
- fp-stale-in-progress-action-list-decoupled-from-assignments [recurrence=5] — action-list.json in-progress bijection broken vs assignments.json; fix: extend gc1-reconcile + T32 self-test
- fp-implementer-sandbox-zero-pushed-branch-multigen [recurrence=3] — reclaimed rows still branchless on origin after two implementer generations; fix: escalate to status=failed after N reclaims + branchless + age>Nh
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — buffer claimable count doesn't subtract archive task_ids; fix: extend gc1-reconcile close step + fix Phase 2.6/GC3 metric

## Open questions
- The third voice-webhook finding (`code-review-api-handlers-voice-webhook-timing-cmp`) also scored 3 and passed all readiness gates but is left in the backlog under the "≤2 new plans per run" cap. It's the lowest-severity of the three (LOW; `/verify` diagnostic endpoint only, no live-handler exposure) and can be promoted on the next routine run or folded into the `default-secret` PR if the implementer picks up both — the file overlap is complete and the two fixes are ~5 lines each.

## Quiet day?
- No — 4 PRs shipped, 3 new HIGH-confidence security backlog vectors, 1 open item resolved, 2 plans promoted (refilling the dispatcher's claimable buffer per the routine's fire-time `buffer-low` trigger)
