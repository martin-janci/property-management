# 2026-09-26

_Incremental run 26h after 2026-09-25T14:44Z. Trigger payload from the cloud dispatcher: `buffer-low: claimable=9/72 (honest~1; 8/9 mobile-native-gated #2652) — refill planner`. Action taken: ingested 12 already-emitted-but-never-rolled-in dispatcher Tier-1d code-review-finding signals (today's `signals/2026-09-26-*-tier1d.json`) into `backlog.json` + `seen_signals`, and promoted the one row that met the score-3 readiness gate (`code-review-ppt-web-core-authed-roots-session-leak`) to `plans/`. Cursor advances `41f78e55 → 3e2239d9`, `last_pr_seen 2956 → 2980`, `last_issue_seen 2966 → 2982`. `stats.runs 82→83`, `vectors_created 373→385`, `plans_created 30→31`. Phase 1.5 (fresh rotating expert review) and Phase 1.6 (project-management skill) skipped this run — dispatcher Tier-1d already covers the segment review cadence; running the skill again over an empty routine-side code delta duplicates spend. Same call as briefs 2026-09-23/24/25._

## Since last run
- Merged PRs: 2 (#2979, #2980) — `last_pr_seen` advances 2956 → 2980.
- Open PRs touched: 5 (#2957 dependabot, #2970 Wave 1 CI unfreeze draft, #2971 Wave 1 tag contract, #2981 dependabot cargo-minor-patch 18-update rollup, #2983 new draft — follow-up to PR #2979 on news + notification-analytics).
- Closed-not-merged PRs: 1 (#2974 dependabot cargo-minor-patch — superseded by #2981).
- New / updated issues: 2 (#2978 closed by #2979; #2982 new follow-up from merged-review — has labels `follow-up`, `from-merged-review`, so NOT an `untriaged-issue`).
- Commits: 22 on `dev` since `41f78e55` — 20 dispatcher/self-review commits + PR #2979 merge (`7c7f341b`) + PR #2980 merge (`a241d118`); 5 of the dispatcher commits are Tier-1d signal-generator commits (ppt-web-ui, ppt-web-core, mobile-rn, reality-server, reality-web).
- Routine lag: **26h since last run** — under the 36h threshold; no `lag_warning`, no `stale_routine_alert`.
- Phases that failed: none.

## Shipped
- **#2979** — `gh-issue-2978: route ppt-web AI-chat + OCR hooks through the authenticated API client` — fixes the 2978-reported bug where `useAiChat` + `useOcrMeterReading` used raw `fetch()` with no `Authorization` header (401 in prod, silent delete on the OCR path). Follow-up issue #2982 opened same day for remaining raw-fetch sites (news + notification-analytics); draft PR #2983 is in flight.
- **#2980** — `chore(deps): bump rustls past RUSTSEC-2026-0285 and chacha20 off the yanked 0.10.1` — advisory cleanup, lockfile-only (`backend/Cargo.lock`, `backend/servers/deploy-server/Cargo.lock`).

## Watch
- **Buffer-low context (trigger payload)** — dispatcher reports `claimable=9/72 (honest~1; 8/9 mobile-native-gated #2652)`. Root cause is the mobile-native AGP egress class (#2652) blocking mobile-native-kmp implementations in cloud runners; the routine-side refill delta today is the 12 Tier-1d rows ingested below (5 of which are `mobile-rn` / `reality-web` / `reality-server` / `ppt-web-*` — all cloud-landable, orthogonal to #2652), plus the one promoted plan. Recorded as `buffer-low-2026-09-26-payload` in `seen_signals` (score_delta=0).
- **Stalled review** — no NEW human-authored stalled-review signals this run. `stalled-review-pr-2555` and `stalled-review-pr-2559` (both 58d old, `feat(acc): UC-ACC-05.8` / `UC-ACC-05.17`) remain in `seen_signals` from prior runs — one-shot dedup rule prevents re-scoring. 6 dependabot PRs (#2588–#2590, #2866, #2960, #2957) are >7d idle; filtered from stalled-review classification by the routine's dependabot exclusion.
- **Reverted:** none this window.
- **Churn hotspots (window: 26h):** 3 real files, all from PR #2979 (ppt-web AI-chat + OCR migration):
  - `frontend/apps/ppt-web/src/features/meters/hooks/useOcrMeterReading.test.tsx` — 187 lines (test-file dominant), `runs_seen=1` (first appearance).
  - `frontend/apps/ppt-web/src/features/ai-chat/hooks/useAiChat.test.tsx` — 172 lines, `runs_seen=1`.
  - `frontend/apps/ppt-web/src/features/ai-chat/hooks/useAiChat.ts` — 83 lines, `runs_seen=1`.
  - **Interpretation:** one-shot refactor to route hooks through the authenticated axios client, not repeated instability. Test-file churn dominates source-file churn (a good sign — the fix landed with matching test coverage). Recorded as signals (each +1) but no `repeated-churn` / `risky-churn` triggers.
- **dep-update-noise (log-only):** #2981 (new dependabot cargo-minor-patch 18-update rollup, open); #2957 (open, existing). #2974 closed unmerged, superseded by #2981.
- **Infra blockers still open** (unchanged from prior briefs): #2966 (api-server utoipa-swagger-ui egress in cloud runner); mobile-native AGP egress class (originally #2652) — still causing the 8/9 mobile-native-gated buffer imbalance surfaced in the trigger payload.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     REST-only path via cloud sandbox: `gh api repos/martin-janci/property-management/pulls/{n}/reviews` per PR is costly at the current draft count. Fast-path check: open drafts touched this run are #2970 (Wave 1 CI unfreeze) and #2983 (news-hooks follow-up) — both updated <8h ago, neither approved. Nothing in the observable surface fits approved-and-stale. -->
- none suspected

## PRs with verdict=changes, no fix-round progress in 24h
<!-- .research/management/assignments.json (on this dev branch, dispatcher-managed) has 7 rows: 3 `code-review-reality-web-*-silent-fail` (in-progress, live drafts), 2 `gh-issue-294{4,5}-retry1` (in-progress), 1 `gh-issue-2743` (in-progress), 1 `screen-map-drift-pr-2894-reality` (in-progress). None currently in `review` with `verdict=changes`. -->
- none

## Code review slice
- Segment reviewed: **skipped by this routine — dispatcher Tier-1d already generated 5 segment-review signal files today** (`signals/2026-09-26-{mobile-rn,ppt-web-core,ppt-web-ui,reality-server,reality-web}-tier1d.json`, 12 code-review-findings total). Firing a fresh Phase 1.5 review over the same segments would duplicate work.
- Experts (from dispatcher Tier-1d): frontend (ppt-web-core, ppt-web-ui, reality-web, mobile-rn), rust (reality-server).
- Findings: **12 ingested** into backlog this run (see *Backlog deltas*). Highest-severity is `code-review-ppt-web-core-authed-roots-session-leak` (score 3, high, bug) — cross-session TanStack Query cache leak on shared workstations; promoted to a plan below.
- Next segment: The routine's own `review_cursor` shows `ppt-web-core` and `reality-web` tied at their oldest cursor dates; the dispatcher Tier-1d cycle already refreshed both today. Next-run candidates: `api-handlers` (last 2026-08-31), `reality-server` (2026-08-31), or `api-core` (2026-09-01).
- Skipped: **yes — dispatcher Tier-1d generator has today's segment coverage.** Fourth consecutive routine skip (same call as 2026-09-23/24/25).

## Screen-map status
- Total `docs/screens/` files: unchanged (`ppt/` + `reality/`; `mobile/` still not seeded → 0).
- Drift signals this run: **0** — PR #2979 (`useAiChat.ts`, `useOcrMeterReading.ts` hooks under `src/features/`) does not touch the trigger set (`App.tsx`, `routes/**`, `reality-web app/**`). PR #2980 is lockfile-only.
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched (no route-file touching merge).

## Backlog deltas
- **New** (12 rows, all from today's dispatcher Tier-1d signal files):
  - [3] `code-review-ppt-web-core-authed-roots-session-leak` — logout allow-list omits mounted route roots (community/disputes/iot/outages/leases/voting); cache survives across logins on shared workstations. **Promoted → `plans/code-review-ppt-web-core-authed-roots-session-leak.md`.**
  - [2] `code-review-ppt-web-core-ws-category-map-gap` — `WebSocketContext.categoryToQueryKeys` misses `community` + `system` NotificationCategory values; community feed stays stale on realtime pushes.
  - [1] `code-review-ppt-web-core-api-default-export-orphan` — `lib/api.ts:370` eagerly-created default export is orphaned by `configureApiClient()`; latent footgun.
  - [2] `code-review-ppt-web-ui-automation-rules-mutations-swallow-errors` — automation-rules mutations return without user-visible failure.
  - [1] `code-review-ppt-web-ui-automation-feature-no-i18n` — hardcoded strings under `frontend/apps/ppt-web/src/features/automation`.
  - [1] `code-review-ppt-web-ui-createrule-partial-cast-boundary` — boundary cast of Partial payload → silent shape mismatch.
  - [2] `code-review-mobile-rn-fault-photos-silently-dropped` — fault-report photo pipeline swallows upload errors.
  - [2] `code-review-mobile-rn-offline-permanent-4xx-silent-drop` — offline queue drops permanent 4xx without feedback.
  - [1] `code-review-mobile-rn-meter-reading-no-numeric-validation` — meter-reading input accepts non-numeric values.
  - [2] `code-review-reality-web-listings-page-no-error-state` — listings page renders empty state on API error.
  - [1] `code-review-reality-web-callback-hardcoded-login-string` — SSO callback carries hardcoded `login` redirect string.
  - [1] `code-review-reality-server-dup-url-validator` — duplicated URL-validation helper; merge into shared crate.
- **Bumped:** none — no signal touched an existing row.
- **Decayed:** none — 0 open rows had `updated_at <= 2026-09-12` (all non-terminal rows sit within the 14d window).
- **Dropped:** none.
- **Resolved:** none — PRs #2979 / #2980 do not mention any open backlog `id`s in title/body, and their file lists don't overlap with the 3 existing `open` reality-web silent-fail rows.

## Plans promoted
- `plans/code-review-ppt-web-core-authed-roots-session-leak.md` — fix `AUTHED_QUERY_KEY_ROOTS` allow-list drift so `logout()` actually purges every mounted-route TanStack Query root (community, disputes, iot, outages, leases, `voting` — currently listed as `votes`, a naming drift). Adds a guard test that asserts every imported `*Keys.all[0]` root is in the allow-list, plus a Vitest logout-integrity test that seeds each root and asserts the cache is empty after logout. Cloud-landable (frontend-only, `pnpm --filter @ppt/web typecheck && test`). · adversarial pass: passed (Hypothesis→Evidence→Suggested approach→Test plan chain is unbroken; test plan asserts a failing-on-main state; alternatives considered explicitly reject the blunt `queryClient.clear()` regression from Issue #712).

## Auto-fix
- `no candidate` — the one score-3 finding is a multi-file code change that requires reasoning about a query-key allow-list and adding new tests; that falls outside the Phase 5 allowlist (`screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / `dep-update-noise` lockfile-drift / `stalled-review` only). Manual plan path is correct here.

## Self-review findings
- none (no `.research/self-improvement/findings.json` file at HEAD on `dev` for this routine; dispatcher-side self-review lives under `.research/self-improvement/` as ISO-timestamped `.md` reports from the dispatcher loop, distinct from the routine's own findings file).

## Open questions
- **Should the routine roll in dispatcher-emitted Tier-1d signals proactively every run, or only when the buffer-low trigger fires?** This run demonstrates that yesterday's Tier-1d signal files (4 files, 11 findings) sat un-ingested — because Phase 1.5 was skipped and there is no glue between the dispatcher's `signals/*-tier1d.json` files and the routine's `seen_signals`/`backlog.json`. Today's ingestion is a one-shot response to the buffer-low payload. If the dispatcher is expected to keep producing these files, the routine prompt should either (a) always merge them in Phase 1 (before dedup), or (b) codify Phase 1.5's "skipped when dispatcher Tier-1d covers this cycle" behaviour and *still* ingest the dispatcher's output. Leaving the routine prompt untouched this run — the operator can pick the shape they want. Note: yesterday's uningested Tier-1d IDs (api-core / api-handlers / ppt-web-core / reality-web from 2026-09-25) still sit outside `seen_signals` after this run; deliberately not backfilled to keep this run's audit trail focused.

## Quiet day?
- **No** — 2 PRs merged, 12 new backlog vectors, 1 plan promoted, 12 code-review-finding signals ingested. State advanced on every axis (cursors, seen_signals +17, hotspot_history +3, stats.runs 82→83). PM digest send: this run is NOT quiet, so `PM_DIGEST_QUIET=0`; whether the send happens depends on `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` being set at commit-time.
