# 2026-09-26

_Two routine invocations today. **17:14 UTC (pass1)** — trigger payload `buffer-low: claimable=9/72 (honest~1; 8/9 mobile-native-gated #2652) — refill planner`; ingested 12 already-emitted-but-never-rolled-in dispatcher Tier-1d code-review-finding signals into `backlog.json` + `seen_signals`, and promoted the one row that met the score-3 readiness gate (`code-review-ppt-web-core-authed-roots-session-leak`) to `plans/`; cursors advanced `41f78e55 → 3e2239d9`, `last_pr_seen 2956 → 2980`, `last_issue_seen 2966 → 2982`, `stats.runs 82→83`, `vectors_created 373→385`, `plans_created 30→31`. **22:42 UTC (pass2 — this run)** — trigger payload `buffer-low: claimable=12/72 — refill planner`; between-run delta is limited to two new dispatcher Tier-1d pass2 signals under `signals/2026-09-26-ppt-web-core-tier1d-2.json` (both `code-review-ppt-web-core-*`, score 2, confidence medium, vector bug) that never touched `seen_signals`/`backlog.json`; ingested both, no new plan promotion (both fall below the score-3 gate); cursors advanced `3e2239d9 → 3177967a` (dispatcher-only .research commits between the two routine runs), `stats.runs 83→84`, `vectors_created 385→387`. No new merged PRs, no new issues, no code-touching commits between passes. Phase 1.5 and Phase 1.6 skipped for both passes for the same reason: dispatcher Tier-1d already covers segment review, and Phase 1.6 already ran in pass1 today._

## Since last run
- Merged PRs: 2 (#2979, #2980) — `last_pr_seen` advances 2956 → 2980. **Pass2 delta:** 0 (still at 2980).
- Open PRs touched: 5 total across the day (#2957 dependabot, #2970 Wave 1 CI unfreeze draft, #2971 Wave 1 tag contract, #2981 dependabot cargo-minor-patch 18-update rollup, #2983 new draft — follow-up to PR #2979 on news + notification-analytics). **Pass2 delta:** 1 (#2983 saved at 22:34 UTC, still draft, no reviewDecision).
- Closed-not-merged PRs: 1 (#2974 dependabot cargo-minor-patch — superseded by #2981). Pass2 delta: 0.
- New / updated issues: 2 (#2978 closed by #2979; #2982 new follow-up from merged-review — has labels `follow-up`, `from-merged-review`, so NOT an `untriaged-issue`). Pass2 delta: 0.
- Commits: 22 on `dev` since `41f78e55` (pass1's starting cursor) — 20 dispatcher/self-review commits + PR #2979 merge (`7c7f341b`) + PR #2980 merge (`a241d118`). **Pass2 delta:** 4 dispatcher chore commits between the two routine runs (`326a1a5`, `0d58ea1`, `ee2e011`, `3177967` — all `.research/`-only, no code diff); `0d58ea1` created the pass2 tier-1d signal file that pass2 (this run) ingested.
- Routine lag: **5h28m since pass1** (pass1 was 17:14:48 UTC, pass2 is 22:42 UTC) — well under the 36h threshold; no `lag_warning`, no `stale_routine_alert`. Total lag from pass1 back to prior real run (2026-09-25) was ~26h.
- Phases that failed: none across either pass.

## Shipped
- **#2979** — `gh-issue-2978: route ppt-web AI-chat + OCR hooks through the authenticated API client` — fixes the 2978-reported bug where `useAiChat` + `useOcrMeterReading` used raw `fetch()` with no `Authorization` header (401 in prod, silent delete on the OCR path). Follow-up issue #2982 opened same day for remaining raw-fetch sites (news + notification-analytics); draft PR #2983 is in flight.
- **#2980** — `chore(deps): bump rustls past RUSTSEC-2026-0285 and chacha20 off the yanked 0.10.1` — advisory cleanup, lockfile-only (`backend/Cargo.lock`, `backend/servers/deploy-server/Cargo.lock`).

## Watch
- **Buffer-low context (pass2 trigger payload)** — dispatcher reports `claimable=12/72`, up from `9/72` at pass1's 17:14 UTC snapshot. Pass1's ingestion of 12 Tier-1d rows accounts for most of that increase; pass2 adds 2 more (both `ppt-web-core`, score 2, so they show up in `claimable` but do NOT clear the score-3 gate on their own). Root cause of the persistent ceiling is unchanged from pass1: mobile-native AGP egress class (#2652) blocks mobile-native-kmp implementations in cloud runners. Recorded as `buffer-low-2026-09-26-payload-pass2` in `seen_signals` (score_delta=0). Pass1's `buffer-low-2026-09-26-payload` remains in place — separate observation, separate ID.
- **Stalled review** — no NEW human-authored stalled-review signals in either pass. `stalled-review-pr-2555` and `stalled-review-pr-2559` (both 58d old, `feat(acc): UC-ACC-05.8` / `UC-ACC-05.17`) remain in `seen_signals` from prior runs — one-shot dedup rule prevents re-scoring. 6 dependabot PRs (#2588–#2590, #2866, #2960, #2957) are >7d idle; filtered from stalled-review classification by the routine's dependabot exclusion.
- **Reverted:** none this window.
- **Churn hotspots (14d window):** 3 real files, all from PR #2979 (ppt-web AI-chat + OCR migration) — recorded in pass1's `seen_signals` and NOT re-fired in pass2 (dedup rule; no new churn evidence). See pass1's Watch section for the file list; pass2 has no additional hotspots because no code merged between the two passes.
- **dep-update-noise (log-only):** #2981 (dependabot cargo-minor-patch 18-update rollup, open); #2957 (open, existing). #2974 closed unmerged, superseded by #2981. Pass2 has no new dep-update-noise.
- **Infra blockers still open** (unchanged from pass1 and prior briefs): #2966 (api-server utoipa-swagger-ui egress in cloud runner); mobile-native AGP egress class (originally #2652) — still causing the 8/9 mobile-native-gated buffer imbalance surfaced in both trigger payloads.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     REST-only path via cloud sandbox: `gh api repos/martin-janci/property-management/pulls/{n}/reviews` per PR is costly at the current draft count. Fast-path check: open drafts touched today are #2970 (Wave 1 CI unfreeze) and #2983 (news-hooks follow-up) — both updated <13h ago, neither approved. Nothing in the observable surface fits approved-and-stale. -->
- none suspected

## PRs with verdict=changes, no fix-round progress in 24h
<!-- .research/management/assignments.json (on this dev branch, dispatcher-managed) has 14 active rows at pass2 (up from 7 at pass1 — dispatcher pre-spawn `ee2e011` claimed 6 new task(s), then 22:34 dispatcher run took `claimable=12/24` view of buffer). None currently in `review` with `verdict=changes`. -->
- none

## Code review slice
- Segment reviewed: **skipped by this routine (both passes) — dispatcher Tier-1d already generated 6 segment-review signal files today.** Pass1 saw 5 files (`signals/2026-09-26-{mobile-rn,ppt-web-core,ppt-web-ui,reality-server,reality-web}-tier1d.json` + `ppt-web-ui-tier1d-2.json`), pass2 additionally saw `signals/2026-09-26-ppt-web-core-tier1d-2.json` (dispatcher's second-pass sweep of ppt-web-core, created at 18:42 UTC between the two routine runs). Firing a fresh Phase 1.5 review over the same segments would duplicate work.
- Experts (from dispatcher Tier-1d): frontend (ppt-web-core, ppt-web-ui, reality-web, mobile-rn), rust (reality-server).
- Findings: **14 ingested this day (12 in pass1, 2 in pass2).** Highest-severity remains `code-review-ppt-web-core-authed-roots-session-leak` (score 3, high, bug) — cross-session TanStack Query cache leak on shared workstations; promoted to a plan in pass1. Pass2's 2 additions are both score 2 / confidence medium / vector bug and do not clear the score-3 gate.
- Next segment: The routine's own `review_cursor` shows `ppt-web-core` and `reality-web` tied at their oldest cursor dates; the dispatcher Tier-1d cycle already refreshed both today. Next-run candidates: `api-handlers` (last 2026-08-31), `reality-server` (2026-08-31), or `api-core` (2026-09-01).
- Skipped: **yes — dispatcher Tier-1d generator has today's segment coverage.** Fifth consecutive routine skip when counting pass1+pass2 as the same day (same call as 2026-09-23/24/25 and today's pass1).

## Screen-map status
- Total `docs/screens/` files: unchanged (`ppt/` + `reality/`; `mobile/` still not seeded → 0).
- Drift signals this run: **0** — PR #2979 (`useAiChat.ts`, `useOcrMeterReading.ts` hooks under `src/features/`) does not touch the trigger set (`App.tsx`, `routes/**`, `reality-web app/**`). PR #2980 is lockfile-only. Pass2 has no new merged PRs, so no new drift check to run.
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched (no route-file touching merge in either pass).

## Backlog deltas
- **New (pass1, 12 rows, all from today's dispatcher Tier-1d signal files):**
  - [3] `code-review-ppt-web-core-authed-roots-session-leak` — logout allow-list omits mounted route roots (community/disputes/iot/outages/leases/voting); cache survives across logins on shared workstations. **Promoted → `plans/code-review-ppt-web-core-authed-roots-session-leak.md`.**
  - [2] `code-review-ppt-web-core-ws-category-map-gap` — `WebSocketContext.categoryToQueryKeys` misses `community` + `system` NotificationCategory values; community feed stays stale on realtime pushes.
  - [1] `code-review-ppt-web-core-api-default-export-orphan` — `lib/api.ts:370` eagerly-created default export is orphaned by `configureApiClient()`; latent footgun.
  - [2] `code-review-ppt-web-ui-automation-rules-mutations-swallow-errors` — automation-rules mutations return without user-visible failure.
  - [1] `code-review-ppt-web-ui-automation-feature-no-i18n` — hardcoded strings under `frontend/apps/ppt-web/src/features/automation`.
  - [1] `code-review-ppt-web-ui-createrule-partial-cast-boundary` — boundary cast of Partial payload → silent shape mismatch.
  - [2] `code-review-mobile-rn-fault-photos-silently-dropped` — fault-report photo pipeline swallows upload errors.
  - [2] `code-review-mobile-rn-offline-permanent-4xx-silent-drop` — offline queue drops permanent 4xx without feedback.
  - [1] `code-review-mobile-rn-meter-reading-no-numeric-validation` — meter-reading input accepts non-numeric values.
  - [2] `code-review-reality-web-listings-page-no-error-state` — listings page renders empty state on API error.
  - [1] `code-review-reality-web-callback-hardcoded-login-string` — SSO callback carries hardcoded `login` redirect string.
  - [1] `code-review-reality-server-dup-url-validator` — duplicated URL-validation helper; merge into shared crate.
- **New (pass2, 2 rows, from `signals/2026-09-26-ppt-web-core-tier1d-2.json`):**
  - [2] `code-review-ppt-web-core-api-base-url-env-split` — ppt-web reads the API base URL from TWO competing env vars: `VITE_API_URL` (declared in `vite-env.d.ts`, used by 4 files incl. `lib/api.ts:77` + main.tsx OpenAPI client) and `VITE_API_BASE_URL` (**undeclared anywhere**, used by 8 files incl. `contexts/AuthContext.tsx:97` `getAuthApi`). Split-origin production deploys following the documented `VITE_API_URL` contract leave the auth path (login/refresh/logout) and 7 feature modules falling back to `''` (same-origin). Confidence medium (topology-conditioned).
  - [2] `code-review-ppt-web-core-route-query-silent-error` — `routes/groups/financial.tsx:70-96` (3 useQuery: AR-aging, overdue-invoices, list-invoices) and `routes/groups/rentals.tsx:233-265` (2 useQuery: reservations, connections) destructure only `{ data, isLoading }` — no `isError`. On failure both dashboards render as `€0 outstanding / €0 overdue / €0 total` (financial) and `totalBookings 0 / totalRevenue 0 / occupancyRate 0` (rentals), with `isLoading=false`, no error banner, no retry affordance. Money-bearing screens; inconsistent with sibling mutations in the same files (which DO handle `onError`). Confidence medium.
- **Bumped:** none — no pass2 signal touched an existing row.
- **Decayed:** none across the day — 0 open rows had `updated_at <= 2026-09-12` (all non-terminal rows sit within the 14d window).
- **Dropped:** none.
- **Resolved:** none — no new merged PR since pass1, so no new resolution candidates to check; the pass1 resolution scan (against #2979 / #2980) already found no `id` matches or evidence-gone flips.

## Plans promoted
- `plans/code-review-ppt-web-core-authed-roots-session-leak.md` — **promoted in pass1**; fix `AUTHED_QUERY_KEY_ROOTS` allow-list drift so `logout()` actually purges every mounted-route TanStack Query root (community, disputes, iot, outages, leases, `voting` — currently listed as `votes`, a naming drift). Adds a guard test that asserts every imported `*Keys.all[0]` root is in the allow-list, plus a Vitest logout-integrity test that seeds each root and asserts the cache is empty after logout. Cloud-landable (frontend-only, `pnpm --filter @ppt/web typecheck && test`). · adversarial pass: passed (Hypothesis→Evidence→Suggested approach→Test plan chain is unbroken; test plan asserts a failing-on-main state; alternatives considered explicitly reject the blunt `queryClient.clear()` regression from Issue #712).
- **Pass2 promotions:** none — both new tier-1d pass2 rows are score 2 / confidence medium and do not clear the readiness gate (score ≥ 3 for non-security, or score ≥ 2 + high-confidence for the security fast-track).

## Auto-fix
- `no candidate` (both passes) — every code-review-finding signal ingested today is a multi-file code change requiring reasoning about a query-key allow-list, error-state UX, or an env-var contract; none fits the Phase 5 allowlist (`screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / `dep-update-noise` lockfile-drift / `stalled-review` only). Manual plan path is correct here.

## Self-review findings
- none (no `.research/self-improvement/findings.json` file at HEAD on `dev` for this routine; dispatcher-side self-review lives under `.research/self-improvement/` as ISO-timestamped `.md` reports from the dispatcher loop, distinct from the routine's own findings file).

## Open questions
- **(Carried from pass1)** Should the routine roll in dispatcher-emitted Tier-1d signals proactively every run, or only when the buffer-low trigger fires? Pass2 confirms the same shape holds — a second dispatcher Tier-1d pass2 file appeared between the routine runs today, and pass2 ingested it only because the buffer-low payload prompted a re-fire. If dispatchers are expected to keep producing these files, the routine prompt should either (a) always merge them in Phase 1 (before dedup), or (b) codify Phase 1.5's "skipped when dispatcher Tier-1d covers this cycle" behaviour and *still* ingest the dispatcher's output. Leaving the routine prompt untouched this run — the operator can pick the shape they want. Note: yesterday's uningested Tier-1d IDs (api-core / api-handlers / ppt-web-core / reality-web from 2026-09-25) still sit outside `seen_signals` after this run; deliberately not backfilled to keep this run's audit trail focused.

## Quiet day?
- **No** — 2 PRs merged, 14 new backlog vectors across the day (12 pass1 + 2 pass2), 1 plan promoted (pass1), 14 code-review-finding signals ingested. State advanced on every axis (cursors, seen_signals +19, hotspot_history +3, stats.runs 82→84 across two passes). PM digest send: this run (pass2) is NOT quiet in the routine's strict sense (2 new backlog vectors added), so `PM_DIGEST_QUIET=0`; whether the send happens depends on `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` being set at commit-time.
