# 2026-08-26

## Since last run
- Merged PRs: 5 (range #2848–#2852)
- Open PRs touched: 0
- New / updated issues: 0
- Commits: 20 on `dev` (5 human-authored merges + 15 dispatcher chore commits)
- Routine lag: 18h since last run — on schedule
- Phases that failed: none

## Shipped
- #2848 churn-hotspot AML dashboard extract — refactored `AmlDashboardPage.tsx` 385→157 lines by extracting `AmlThresholdsSection`, `AmlCountryRisksTable`, `AmlFiltersPanel`, and a `useAmlDashboard` hook; behavior-preserving.
- #2849 make moderation overdue affordance filter the queue — replaced the no-op overdue-alert handler with a real client-side `age_hours >= 24h && status ∈ {pending, under_review}` narrowing, plus active-filter chip + `overdueActive`/`clearOverdue` i18n keys across all six locales.
- #2850 verification-badge expiry i18n snapshot — new visual-diff regression across sk/cs/de/en with hard assertions (not snapshots) that non-English renders never leak English "Expired" / "Expiring soon" / "Soon" literals; `vitest -u` cannot silently bless a regression.
- #2851 voice OAuth token encryption round-trip tests — unit test on the shared `encrypt_voice_token_pair` helper plus a real HTTP+Postgres integration suite for alexa/google_assistant (no plaintext at rest, round-trip decrypt, HMAC lock-step with `access_token_hash` #2662) — closes #2838 QA gap.
- #2852 AML dashboard test-helper dedup — churn-hotspot maintenance on `AmlDashboardPage.test.tsx`: consolidated labeled-field lookups and the fragile "last matching EDD button" logic into small accessors; suite unchanged 6/6.

## Watch
- Stalled review: #2555 / #2558 / #2559 (UC-ACC-05.17 / 05.9 / 05.8 accounting) — 28 days idle, martin-janci authored, no reviewDecision. Already surfaced in prior runs (in `state.seen_signals`).
- Stalled review: #2744 (dispatcher un-wedge for oversize-archive push, #1162/#2743) — approved but `needs-human-review`-gated, 12 days stale. Its own delay is blocking dispatcher throughput — pm-devops flagged as high-priority escalation this run.
- Churn hotspots (this run):
  - `frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.tsx` — 326 churn, `runs_seen=2` (a refactor extract just landed, expected to stabilize next run).
  - `frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.test.tsx` — 114 churn, `runs_seen=1` (regression tests for #2849 overdue affordance).
  - `backend/servers/api-server/src/routes/voice_webhooks.rs` — 103 churn, `runs_seen=4` (still churning after #2844 refactor-plan landed; decomposition pending).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **api-handlers** (reason: churn-aligned — `voice_webhooks.rs` is a repeat hotspot, 4th run; segment was 4 days unreviewed)
- Experts: rust
- Findings: 3 (see Backlog deltas for `code-review-finding` signals)
- Next segment: **mobile-rn** (oldest-unreviewed after this run, `2026-08-17`)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 153 (ppt/ 106 + reality/ 47; mobile/ not yet seeded → 0)
- Drift signals this run: 0 (none of the merged PRs touched `ppt-web/src/{App.tsx,routes/**}` or `reality-web/src/app/**`)
- Orphan screens this run: 0 (no trigger files touched)
- Last `screen-map.yml` CI run on `main`: not queried (no route change to gate on)

## Backlog deltas
- **New:** [2] `code-review-api-handlers-voice-refresh-silent-noop` — `oauth_token_refresh` swallows `Ok(None)` from the device-tokens update and returns `success:true` while the row still holds the pre-rotation refresh token (upstream may treat as revoked).
- **New:** [2] `code-review-api-handlers-audit-swallowed` — three government_portal handlers `let _ =` `create_audit(...)` regulatory-submission audit rows; DB failure silently loses the compliance record.
- **New:** [1] `code-review-api-handlers-booking-sync-bookkeeping-dup` — booking_channel + install both repeat the "best-effort let_= update_connection_last_sync" idiom; centralization opportunity (next PR #2851-shaped extract).
- **New:** [1] `churn-hotspot-.../ContentModerationPage.test.tsx` — top-3 churn (114 lines, PR #2849).
- **New:** [1] `repeated-churn-.../AmlDashboardPage.tsx` — `runs_seen=2` bump; a refactor extract just landed, expected to stabilize.
- **Bumped:** none
- **Decayed:** none (no open item > 14 days without new evidence)
- **Dropped:** none

## Plans promoted
- none this run — the only ready-eligible items either already have a plan (`code-review-mobile-native-kmp-inquiries-response-contract`, `status=ready`) or sit one signal short of the readiness gate (all four score-2 items are `bug` vector, so no security fast-track; `httpclient-no-timeout` is also blocked by stem uniqueness against an existing open action-list item).

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The two high-confidence bugs surfaced by Phase 1.5 (`voice-refresh-silent-noop`, `audit-swallowed`) both sit at score 2 (single-signal baseline); the auto-fix bar is score≥3 plus a mechanically-verifiable safe shape (doc-stub, orphan-remove, lockfile-bump, stalled-review comment) which neither of these bugs matches.

## Self-review findings
- none

## Open questions
- Should `code-review-api-handlers-audit-swallowed` be reclassified from `bug` to `security` given it's compliance-audit integrity for a government-portal integration (would trigger security fast-track at score 2)?
- Should the pm-scrum-master `Request a full coverage.json scan` action-list item be automatically triggered next local session, or does someone need to explicitly `/ppt-project-management scan`?

## Quiet day?
- No — 5 merged PRs, 5 new backlog vectors (3 code-review + 2 churn), 11 new PM action-list items, 10 new risks, coverage-cursor advanced (epic-8a refreshed).
