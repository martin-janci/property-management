# 2026-08-26

## Since last run
- Merged PRs: 5 (range #2848–#2852)
- Open PRs touched: 0 net; sole persistently-open self-authored PR #2744 (draft, needs-human-review, 13d stale)
- New / updated issues: 0
- Commits: 21 on `dev` (all dispatcher/routine chatter — 0 app-code commits from anyone but the 5 merged PRs)
- Routine lag: 1d 1h since last run (previous run was the 08-25 buffer-low refire)
- Phases that failed: none

## Shipped
- #2848 churn-hotspot-aml-dashboard-page: extract AML dashboard sections + data hook (`AmlDashboardPage.tsx` 385→157 lines; 3 extracted components + `useAmlDashboard` hook)
- #2849 code-review-ppt-web-ui-content-mod-overdue-noop: make moderation overdue affordance filter the queue (resolves the "TODO: add overdue filter when API supports it" no-op; adds i18n keys + regression tests)
- #2850 pm-qa-verification-badge-expiry-i18n-visual-diff: snapshot VerificationBadge expiry copy across sk/cs/de/en
- #2851 pm-qa-voice-oauth-token-encryption-roundtrip-test: unit + integration coverage for centralized voice OAuth token encryption
- #2852 churn-hotspot AmlDashboardPage.test.tsx: de-duplicate decision-flow test helpers (pure test refactor; 6/6 assertions unchanged)

## Watch
- Stalled review: **#2744** — 13d idle, draft, `needs-human-review` label; blocks the archive-push wedge fix (#1162/#2743) and freezes any full-size assignments-archive push, which is why the dispatcher has been idling. Author: martin-janci (self-PR). Escalating to a `stalled-review-pr-2744` signal in the audit trail; no auto-fix comment posted (Phase 5 gate: `confidence=high` ✓ but this is a self-PR, comment noise is worse than value).
- Reverted: none
- Churn hotspots: `frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.tsx` — 2 PRs touching the file/family in one day (#2848 refactor + #2852 test dedup); 386 lines total churn; hotspot_history run counter incremented.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: skipped this run (mid-day buffer-low refire — Phase 1.5 the dispatcher already fired today at 04:31 UTC (api-handlers, 2 landable findings) and 06:31 UTC (mobile-rn, 3 landable findings). Ingesting those 5 findings into the backlog is the more useful work than firing a redundant 3rd segment.)
- Experts: n/a
- Findings: 5 ingested (2 api-handlers + 3 mobile-rn — see Backlog deltas below)
- Next segment: api-core or reality-server (both haven't been reviewed since 2026-08-25; the daily rotation will pick tomorrow)
- Skipped: yes — redundant (2 slices already ran today via the dispatcher tier1d generator)

## Screen-map status
- Total `docs/screens/` files: unchanged since last run (`ppt/` + `reality/` populated; `mobile/` not yet seeded → 0)
- Drift signals this run: 0 (all 5 merged PRs touched `frontend/apps/ppt-web/src/features/compliance/…`, not route files under `src/App.tsx`/`src/routes/**`)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not checked (mid-day refire — will be picked up on the next daily run)

## Backlog deltas
- **New:** [3] `code-review-api-handlers-hellosign-unbound-envelope` — SECURITY: `verify_hellosign_signature` MACs only `event_time+event_type`, envelope_id forgery enables cross-tenant workflow hijack (confidence upgraded to `high` via in-run read of `sync.rs:226-240`).
- **New:** [2] `code-review-api-handlers-esign-webhook-replay` — `esignature_webhook` has no timestamp/freshness check for any provider; captured deliveries are replayable to re-drive workflow state (sibling of the above).
- **New:** [1] `code-review-mobile-rn-raw-status-untranslated` — FaultsListScreen renders raw api-server status enum in an otherwise-translated screen.
- **New:** [1] `code-review-mobile-rn-faultslist-i18n-gap` — FaultsListScreen report-fault CTA hardcoded English `+ Report`.
- **New:** [1] `code-review-mobile-rn-hardcoded-button-labels` — MessagesScreen compose CTA hardcoded English `+ New message`.
- **Bumped:** none (no signal matched an existing open item this run)
- **Decayed:** none (no open item is >14d old — all 4 pre-existing opens carry `updated_at >= 2026-08-16`)
- **Dropped:** none

Net effect on the claimable pool: **6 → 11**, of which **2 are cloud-landable Rust/backend** (api-handlers) and **3 are cloud-landable TS mobile-rn** (React Native, not KMP). Only the top security item was promoted to a plan (per the ≤2 cap and the readiness gate); the other 4 sit at score 1–2 and can wait for the next run's compounding.

## Plans promoted
- `plans/code-review-api-handlers-hellosign-unbound-envelope.md` — plan for the HelloSign envelope-id unbound MAC forgery gap · adversarial pass: passed (reviewed source→hypothesis→approach→test-plan chain; hypothesis traces every claim to a file:line in Evidence; suggested approach directly addresses the unsigned-field root cause and adds the provider-consistency and freshness guards; test plan asserts the failing case (substituted envelope_id) rejects with 401 — that assertion fails on `main` today).

## Auto-fix
- `no candidate` — no eligible signal this run:
  - The one `high`-confidence signal (`code-review-api-handlers-hellosign-unbound-envelope`) doesn't match any Phase 5 allowlist shape (it's a real code fix, not a doc-stub / orphan-remove / lockfile-bump / FIXME issue / stalled-review comment).
  - `stalled-review-pr-2744` is `high` confidence and score would qualify (comment-only), but the PR is a self-authored draft under `needs-human-review` — pinging the author's own PR adds noise, not signal.

## Self-review findings
- open=35 (high=8, medium=15, low=1, critical=11); new this run=0; acknowledged=19
- fp-<n/a> [recurrence=17] — dispatcher `mcp-push-size-guard` blocks archive files >64KiB even when direct `git push` works in this env; fix: runtime push-capability probe in Phase 1 + bypass size-guard when PUSH_METHOD=git. (Same root cause as PR #2744 which is exactly the un-wedge fix — still 13d stale.)
- fp-<n/a> [recurrence=17] — Phase 6 archive-move produces a 198KB assignments-archive.json that MCP `push_files` can't inline; workaround (proven this run): push to new branch → open PR → API-merge. Same PR #2744 addresses this.
- fp-<n/a> [recurrence=10] — Tier-1d dev-review generator keeps firing while unconsumed `*tier1d*.json` signals pile up in `.research/signals/`; fix: signals→backlog promoter or gate Tier-1d on unconsumed-count threshold. **Partially validated this run** — routine (this run) manually ingested 5 unpromoted tier1d signals into the backlog, refilling the claim pool 6→11.
- fp-<n/a> [recurrence=6] — `action-list.json` in-progress rows decoupled from live `assignments.json`; fix: extend `gc1-reconcile.sh` to reconcile the twin status.
- fp-<n/a> [recurrence=3] — implementer sandboxes reclaimed with `reclaim_attempts>=1` and still branchless after ≥6h; fix: escalate to `status=failed (sandbox-no-branch-after-Ngen)` instead of granting another grace window.
- … and 3 more (see findings.json)

## Open questions
- Should the routine start auto-promoting unpromoted `*tier1d*.json` findings on every daily run (turning the recurrence=10 finding into a first-class ingest phase), or leave that responsibility to the dispatcher's signals-promote path? This run did it manually; codifying it in the routine would prevent the buffer-starvation feedback loop.
- Should PR #2744 be picked up and rebased/reworked by someone other than the self-author, given the 13d stall blocking the entire dispatcher archive path?

## Quiet day?
- No — 5 PRs merged, 5 backlog items added, 1 plan promoted, 1 stalled-review flagged.
