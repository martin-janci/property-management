# 2026-08-23

## Since last run
- Merged PRs: 9 (range #2821–#2834)
- Open PRs touched: 0 (no open PR updated since 2026-08-22T03:03:20Z)
- New / updated issues: 5 (all created + closed same window as follow-ups: #2822, #2823, #2824, #2831, #2832 — every one addressed by a merged PR)
- Commits: 25+ on `dev` since last routine commit (mix of merged PRs + dispatcher chore commits + tier-1d generator commits)
- Routine lag: 25h since last run (under the 36h threshold — no lag_warning)
- Phases that failed: none

## Shipped
- #2821 code-review-api-handlers-booking-connect-non-manager-hijack — gate direct-connect OTA credential writes on manager role (security; landed 2026-08-22 08:23Z)
- #2825 gh-issue-2824 — i18n VerificationBadge expiry copy + de-duplicate `getExpiryState` helper (frontend)
- #2826 gh-issue-2823 — per-channel bookkeeping + bounded retry for held-notification drain (backend; +migration 00234)
- #2827 gh-issue-2822 — neutralize CR/LF in CSV export sanitizer (backend; closes report-tampering hole)
- #2828 code-review-ppt-web-ui-facilities-booking-silent-errors — surface booking fetch/approve/reject/cancel errors via useToast (frontend)
- #2829 code-review-ppt-web-ui-aml-dashboard-prompt-alert-flow — replace AML `window.prompt`/`alert` with in-app dialogs + i18n (frontend; 6 locales)
- #2830 code-review-ppt-web-ui-facilities-booking-hardcoded-i18n — i18n facilities booking UI strings (frontend; 6 locales)
- #2833 gh-issue-2832 — key AML EDD/Review dialogs per-assessment so state doesn't leak (frontend; regression test added)
- #2834 gh-issue-2831 — atomic claim so quiet-hours drain delivers held notifications at-most-once across replicas (backend; +migration 00235 `claimed_at` lease)

## Watch
- Stalled review: none escalated this run — the 3 accounting feature PRs (#2555, #2558, #2559) remain parked pending spec resolution (>24d, `needs-human-judgement`); the auto-impl draft #2744 stays `needs-human-review` (>10d) but is human-gated by design.
- Reverted: none this window.
- Churn hotspots: 3 fresh (all first-time in `hotspot_history`) — `backend/servers/api-server/src/services/quiet_hours_drain.rs` (touched by #2826 + #2834, ~165 net lines), `frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.tsx` (touched by #2829 + #2833, ~90 net lines), `backend/crates/db/repositories/granular_notification.rs` (touched by #2826 + #2834, ~100 net lines). None of them is real code-instability — each pair is a bug-fix chain landing tightly together. Flagging as `refactor` candidate to watch, not as `risky-churn` (both fix PRs shipped with tests).

## PRs stuck in draft despite approval
- #2744 fix(dispatcher): un-wedge oversize-archive push + close retry-remint cross-id ghost — draft, `needs-human-review` label, age ~10d. Reviewer verdict is approve but the PR touches the dispatcher's own `.research/` scripts (high blast radius), so it is human-gated by design. No auto-action.

## PRs with verdict=changes, no fix-round progress in 24h
- none — no active dispatcher row is currently `verdict=changes` (last change-verdict was #2834 which auto-round-1'd to green + merged in this window).

## Code review slice
- Segment reviewed: `reality-web` (routine cursor: oldest-unreviewed cloud-landable — last touch 2026-08-18 → 5d stale). Payload asked for backend/frontend/reality refill; reality-web is the natural pick.
- Experts: frontend (via Explore subagent, brief-mode). Reality-server signals from the dispatcher's own 2026-08-23 tier1d run (`.research/signals/2026-08-23-reality-server-tier1d.json`) also folded in.
- Findings: 7 total across both segments (4 reality-web, 3 reality-server).
- Next segment: `ppt-web-ui` (cursor stuck at 2026-08-16 — oldest unreviewed after this run at 7d).
- Skipped: no.

## Screen-map status
- Total `docs/screens/` files: unchanged from 2026-08-22 (`ppt/` + `reality/` populated; `mobile/` still not seeded).
- Drift signals this run: 0 (none of the 9 merged PRs touched `frontend/apps/ppt-web/src/App.tsx`, `.../routes/**`, or `frontend/apps/reality-web/src/app/**` — all touches were in `features/**` / `components/**` / `services/**` / migrations).
- Orphan screens this run: 0 (no route file changed → skipping the union spot-check per routine caveat).
- Last `screen-map.yml` CI run on `main`: not re-queried (no drift trigger this run).

## Backlog deltas
- **New:** [3] reality-web ListingCard uses next/link, dropping locale prefix on every card click — see backlog.md
- **New:** [2] reality-web CategoryCards uses missing i18n key `browseByTypeSubtitle` with fake `defaultValue` option
- **New:** [2] reality-web LanguageSwitcher aria-labels are hardcoded English across all six locales
- **New:** [1] reality-web CategoryCards passes preformatted string to ICU count, breaking Slavic plural + locale-aware grouping
- **New:** [2] reality-server search_alert_drainer redelivers already-sent alerts when mark_search_alert_notified fails
- **New:** [2] reality-server PendingSsoSession in-memory map grows unbounded under abandoned SSO flows — DoS pivot
- **New:** [1] reality-server ipv4_forbidden_reason omits CGNAT / TEST-NET / benchmarking ranges — SSRF defense-in-depth gap
- **Bumped:** none this run (no dedup match against existing rows — all 7 are net-new ids)
- **Decayed:** none — no open item is older than 14 days (oldest open row is `code-review-mobile-native-kmp-cancellation-swallowed` at 2026-08-11 → 12d)
- **Dropped:** none

## Plans promoted
- `plans/code-review-reality-web-listing-card-locale-loss.md` — swap `next/link` for the locale-aware `Link` from `@/i18n/routing` in `ListingCard.tsx` + add a regression test asserting the anchor href preserves the locale prefix. Two files, source-compatible one-line import swap. · adversarial pass: passed

## Auto-fix
- `no candidate` — 7 code-review-finding signals + 3 churn-hotspot signals emitted this run; none match the Phase 5 allowlist (screen-map-drift, screen-map-orphan, fixme-in-merged-code, dep-update-noise lockfile-drift, stalled-review). All code-review items route through the manual plan path.

## Self-review findings
- none — no `.research/self-improvement/findings.json` file present in the repo

## Open questions
- none new — the 3 pre-existing `needs-human-judgement` rows still hold (the 3 `feat(acc)` accounting PRs #2555 / #2558 / #2559 blocked on the accounting spec resolution).
- Buffer-refill lens: this run adds 7 landable code-review-* rows (4 reality-web + 3 reality-server) — the claim pool should recover from mobile-native-only stall. If the dispatcher's next Tier-1b promotion pulls the 3 `score=2, confidence=high|medium` reality-web items, that's 3 more landable frontend tasks even without waiting for score compounding.

## Quiet day?
- No. 9 PRs merged, 7 new landable backlog vectors added, 1 plan promoted, 3 new churn hotspots recorded, 2 review-cursor segments advanced. `stats.runs` bumped 69→70; `stats.vectors_created` 318→325; `stats.plans_created` 27→28; `quiet_days` unchanged at 3.
