# 2026-08-23

## Since last run
- Merged PRs: 9 (range #2821–#2834): #2821 booking-connect manager gate; #2825 verificationBadge i18n + DRY; #2826 held-notification per-channel + bounded retry; #2827 CSV CR/LF sanitizer; #2828 booking-page error toasts (UC-56); #2829 AML dashboard dialogs + i18n (epic-90); #2830 facilities-booking i18n; #2833 AML dialog state key fix (UC-90); #2834 quiet-hours drain atomic claim across replicas (gh-issue-2831).
- Open PRs touched: 6 (5 dependabot bumps at 2026-08-20; #2744 dispatcher self-fix in draft since 2026-08-13, labelled `needs-human-review`).
- New / updated issues: 5 (#2822 #2823 #2824 #2831 #2832 — all follow-up issues from post-merge review, all CLOSED same-day by dispatcher's fix PRs).
- Commits: ~32 on `dev` since #e59dca8b (9 PR merges + 23 dispatcher chore commits touching only `.research/management/`).
- Routine lag: 0d 23h since last run — on schedule (well under 36h threshold).
- Phases that failed: none — Phase 1.6 skipped (budget: focused on ingesting dispatcher Tier 1d + minting 2 KMP plans).

## Shipped
- #2834 gh-issue-2831 — atomic claim (migration 00235 + FOR UPDATE SKIP LOCKED) so the quiet-hours drain delivers held notifications at-most-once across api-server replicas.
- #2833 UC-90 — key each AML EDD/Review dialog by assessment id so React remounts a fresh instance per assessment (fixes stale-notes leak, #2832).
- #2830 code-review-ppt-web-ui-facilities-booking-hardcoded-i18n — route facilities-booking titles + empty-state through react-i18next; keys added to all 6 locales.
- #2829 epic-90 — replace AML dashboard `window.prompt`/`alert` with in-app dialogs + `t()`; decision constrained to approve|reject|escalate via `<select>`; success/error via shared Toast.
- #2828 UC-56 — surface booking fetch/approve/reject/cancel failures via useToast() instead of console.error only.
- #2827 gh-issue-2822 — neutralize CR/LF in shared `sanitize_csv_cell` so a crafted vote title cannot inject a fabricated record into the CSV export.
- #2826 gh-issue-2823 — per-channel `delivered_channels` bookkeeping + bounded `attempts`/`dead_lettered_at` on held notifications so a partial-failure retry never re-delivers a channel and a permanently-failing row is dead-lettered.
- #2825 gh-issue-2824 — i18n the marketplace `VerificationBadge` expiry copy + extract `getExpiryState()` DRY helper (30-day boundary in one place); 6-locale i18n key-presence regression test added.
- #2821 code-review-api-handlers-booking-connect-non-manager-hijack — gate direct-connect OTA credential writes (`connect_booking` / `disconnect_booking` / `disconnect_airbnb` / `airbnb_oauth_callback`) on manager role; +4 IG3 regression tests.

## Watch
- Stalled review: #2744 (dispatcher self-fix) — draft, no reviewDecision, 10d since last update, labeled `needs-human-review` (human-gated, not routine-eligible). #2555/#2558/#2559 (feat(acc) trio) already in backlog as `needs-human-judgement` — no change.
- Reverted: none.
- Churn hotspots: `backend/servers/api-server/src/services/quiet_hours_drain.rs` (~242 lines across #2826 + #2834, runs_seen=1); `backend/crates/db/src/repositories/granular_notification.rs` (~99 lines, runs_seen=1); `backend/servers/api-server/tests/suites/integrations_cross_org_idor_tests.rs` (~162 lines, test-only, runs_seen=1). Same drain chain (#2823 → #2831 → #2834) — if a third round lands, `quiet_hours_drain.rs` will trigger `repeated-churn` next run.

## PRs stuck in draft despite approval
- none — the only draft PR is #2744 (no reviewDecision, human-gated by label).

## PRs with verdict=changes, no fix-round progress in 24h
- none — the sole `verdict=changes` row this cycle (PR #2834 at 22:38Z) was fixed and re-reviewed within 2h and merged 00:30Z.

## Code review slice
- Segment reviewed: mobile-native-kmp (reason: oldest-unreviewed, last 2026-08-16; churn this run was all api-server-side and those segments — api-core 08-21, api-handlers 08-22 — were reviewed within 48h).
- Experts: kotlin
- Findings: 2 (both high-confidence bugs, both promoted this run — see *Plans promoted*).
- Next segment: `ppt-web-ui` (last-reviewed 2026-08-16, tied for oldest but hit repeatedly by dispatcher's Tier 1d today — cursor still 08-16 in `state.review_cursor`; will pick naturally next routine run).
- Skipped: no — reviewed inline via Explore subagent; separately, dispatcher already ran Tier 1d on `reality-server` at 05:00Z (3 medium-confidence bug findings), ingested into backlog here rather than re-running.

## Screen-map status
- Total `docs/screens/` files: unchanged (`ppt/` + `reality/` populated; `mobile/` still not seeded).
- Drift signals this run: 0 (no merged PR touched a route-file trigger — checked #2828/#2829/#2830/#2833/#2825/#2826/#2827/#2821/#2834; the frontend PRs all touched feature pages, not `App.tsx`/`routes/**`/`app/**`).
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-queried this run (no `docs/screens/` changes to trigger it).

## Backlog deltas
- **New:** [3] `code-review-mobile-native-kmp-notification-repository-dead-endpoints` — KMP NotificationRepository targets a router that doesn't exist on reality-server (all 9 methods 404). Promoted this run.
- **New:** [3] `code-review-mobile-native-kmp-inquiry-viewing-endpoints-wrong-path` — KMP InquiryRepository posts to paths reality-server never mounts (send/reply/all viewings 404). Promoted this run.
- **New:** [2] `code-review-reality-server-drainer-mark-fail-redeliver` — search-alert drainer re-drains after mark-notified DB failure → unbounded duplicate email+push. Ingested from dispatcher Tier 1d.
- **New:** [2] `code-review-reality-server-sso-sessions-unbounded-map` — PendingSsoSession map has no background sweep → memory-exhaustion DoS on abandoned SSO flows. Ingested from dispatcher Tier 1d.
- **New:** [1] `code-review-reality-server-url-validator-cgnat-gap` — SSRF defense-in-depth guard omits CGNAT/TEST-NET ranges. Ingested from dispatcher Tier 1d.
- **Bumped:** none.
- **Decayed:** none — no `open` item is older than 14 days (oldest open at 2026-08-11).
- **Dropped:** none.

## Plans promoted
- `plans/code-review-mobile-native-kmp-notification-repository-dead-endpoints.md` — KMP NotificationRepository → non-existent reality-server routes; fix by either mounting `/api/v1/notifications` on reality-server or re-pointing at api-server + adding contract tests · adversarial pass: passed
- `plans/code-review-mobile-native-kmp-inquiry-viewing-endpoints-wrong-path.md` — KMP InquiryRepository URLs vs `routes/inquiries.rs` real routes; rewrite client to `contact/{id}` + `{id}/respond` + `viewing/{id}`, decide viewings LIST/DELETE fate · adversarial pass: passed

## Auto-fix
- `no candidate` — 0 signals this run met the certainty bar (confidence=high + score≥3 + allowlist). The two high-confidence KMP findings score 3 each but their allowlist action shape (KMP HTTP-URL rewrites + backend router additions) is not in the Phase-5 allowlist; both go through the manual-implementer path via the promoted plans.

## Self-review findings
- none — no `.research/self-improvement/findings.json` file present in the repo.

## Open questions
- Should reality-server own notifications at all, or should the KMP `NotificationRepository` re-point at api-server for portal push? (Plan proposes either — implementer picks based on the product intent for portal users.)

## Quiet day?
- No — 9 merged PRs, 5 new backlog vectors, 2 plans promoted, 0 auto-fix. `stats.runs` bumped 69 → 70; `stats.vectors_created` 318 → 323; `stats.plans_created` 27 → 29; `quiet_days` unchanged at 3.
