# 2026-05-30

## Since last run
- Merged PRs: 26 (range #639–#786) — includes 3 late-merged dependabot dep-bumps (#665 axum-test, #666 sqlx 0.8→0.9, #672 metrics) and the iOS Reality Portal late-merges (#641, #639, #705)
- Open PRs touched: 4 (#745 draft auto-impl e-signature nonce v2; #792 doc-upload assertion tighten; #787 WS Redis CI; #783 Capability Display fix)
- New / updated issues: 50 — 33 OPEN unlabeled "Current dev review:" / "Dev review rounds N-M" cluster (#749–#791, no labels), 17 CLOSED follow-ups
- Commits: 30 on `dev` (mix of squashed feature merges + dispatcher chore + bot version-bumps)
- Routine lag: on schedule (~23.8h since the 2026-05-29 03:18Z pass)
- Phases that failed: phase-1.5 (skipped — `ppt-dev-review` skill not invoked in this routine pass; `review_cursor` not advanced), phase-1.6 (skipped — `ppt-project-management` skill not invoked; `pm_cursor` + `coverage_cursor` not advanced), phase-1-churn-hotspots (skipped — per-PR file-list scan over 26 PRs exceeded the cloud routine time budget; `hotspot_history` not advanced)

## Shipped
App-code slice (the bulk of the window was research/dispatcher infra + CI + Dependabot — listed last):
- #723 gap-9-2 MFA recovery codes backend (Story 9.2) — closes the Epic-9 MFA recovery-codes gap
- #725 pm-security-fix-ai-maintenance-idor — IDOR fix on `ai_chat`/`equipment.find_maintenance_by_id`/`equipment.acknowledge_prediction`/`sentiment.acknowledge_alert` (closes the in-flight verdict=changes from 2026-05-29); does **not** address `security-llm-doc-idor` (different cluster — see Backlog deltas)
- #738 fix(security): org-scope `get_by_id`/`pause`/`resume` on report schedules (closes #710) — another tenant-scope leg on `report_schedule.rs`
- #742 refactor(auth): scope logout cache invalidation + add logout flow test (closes #712)
- #732 refactor(api-client): make `authenticatedFetchJson` retry private + deprecate mfa-handler shim
- #739/#733 fix(mobile): restore prod safety guard + EXPO_PUBLIC_ prefix + Android 33+ granular media perms
- #705 gap-82-5 iOS Keychain + PushNotificationManager (Reality Portal late-merge)
- #641 gap-82-4 SwiftUI ListingDetailView + FavoritesView + FavoritesService (iOS Reality late-merge)
- #639 gap-82-3 search-filter stepper i18n for 5 non-EN locales (iOS Reality late-merge)
- #782 fix(ppt-web): memoize STATUS_OPTIONS in HistoryFilters (closes #621 — perf)
- #785 test(reports): CronPicker unit tests (closes #714)
- Dependency bumps (Dependabot, all clean): #665 axum-test 20.0→20.1, #666 sqlx 0.8.6→0.9.0 (workspace breaking change resolved — open question from 2026-05-29 brief now closed), #672 metrics 0.21→0.24
- CI / research infra: #744 pin Rust 1.94.1; #731 self-check branch-protection security gate; #786 mobile EAS CI cleanup; #734/#758/#780/#784 dispatcher run-level lock + GC2/GC3 + reconcile script; #735/#736 dispatcher scripts; #740/#781 review-comment + admin-web ownership chores

## Watch
- Stalled review: none (oldest open PR #745 is 1d, draft auto-impl branch)
- Reverted: none in window
- Closed-unmerged: none flagged
- In-flight security: `security-llm-doc-idor` (score 3, plan ready) — not addressed by any merged PR this run (verified `routes/ai.rs:2622/2643/2870` and `repositories/llm_document.rs:358/395/977` still unscoped); next merge cycle should pick it up. #745 (e-signature nonce persistence, closes #673) is the highest-priority security item in the open-PR queue (draft, awaiting CI).
- Churn hotspots this run: not derived (phase-1-churn skipped); `hotspot_history` carried over unchanged

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (the prior #725 verdict=changes from 2026-05-28 was merged this run at 2026-05-29T13:15Z; no current `verdict=changes` stalled rows)

## Code review slice
- Segment reviewed: none (phase-1.5 skipped — `ppt-dev-review` skill not invoked in this one-shot routine pass; the cursor still points at `api-handlers`=2026-05-23 as oldest, but that segment was deleted in PR #437 and is unreviewable — `ppt-web-core`=2026-05-25 is the next-oldest in practice)
- Experts: n/a
- Findings: 0
- Next segment: `ppt-web-core` (oldest unreviewed, will be picked when the skill runs)
- Skipped: yes — `ppt-dev-review` skill not invoked

## Screen-map status
- Total `docs/screens/` files: 108 (`ppt/` 62 + `reality/` 46; `mobile/` not yet seeded → 0) — count unchanged this run (no merged PR added or removed a screen doc)
- Drift signals this run: 0 (no merged PR touched a tracked ppt-web `App.tsx`/`routes/**` or reality-web `src/app/**` route file; mobile excluded per routine rule)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not re-checked this run (cloud time budget); last brief noted 2026-05-28T09:09Z success

## Backlog deltas
- **New (triage):** 33 × `triage-issue-<N>` rows for the unlabeled "Current dev review" / "Dev review rounds" cluster — issues #749, #750, #751, #752, #753, #754, #755, #756, #757, #760, #761, #762, #763, #764, #765, #766, #767, #768, #769, #770, #771, #772, #773, #774, #775, #776, #777, #778, #779, #788, #789, #790, #791 — all `vector=triage`, score=1, surfaced into `IDEAS_TRIAGE.md` (never promoted to plans). These appear to be dispatcher / dev-team review output; group-triage them by hand in the UI.
- **New (non-triage):** none — the only non-triage candidate signals (dep-update-noise on #665/#666/#672) carry `score_delta=0` per routine rule and don't seed backlog rows
- **Bumped:** none
- **Decayed:** none (oldest open non-triage item `dx-portfolio-dashboard-stubs` updated 2026-05-20 = 10d < 14d threshold)
- **Dropped:** none
- **Resolution checks (open items re-verified):**
  - `security-llm-doc-idor` (plan ready) — files `routes/ai.rs` + `llm_document.rs` re-grepped; `publish_description` (`ai.rs:2643`), `list_listing_descriptions` (`ai.rs:2622`), `get_photo_enhancement` (`ai.rs:2870`), and their unscoped repo methods (`llm_document.rs:358/395/977`) are all still present. PR #725 fixed a sibling IDOR cluster (`ai_chat`/`equipment`/`sentiment`) but did not touch this one. Stays `ready`.
  - `bug-report-schedule-update-no-sql` — PR #738 closed the org-scope leg (`get_by_id`/`pause`/`resume`); the cron-column / `update_schedule` no-SQL bug is a distinct concern and remains. Stays `open`.
  - No PR-body verbatim id matched any open backlog row; the merged-PR set otherwise covers research infra, CI, mobile, and Dependabot work that doesn't intersect open backlog files.

## Plans promoted
- none this run — `security-llm-doc-idor` is already `status=ready` with `plans/security-llm-doc-idor.md` active from the 2026-05-29 run; no fresh `score>=3` (or `vector==security` fast-track `score>=2 + confidence=high`) candidates surfaced this run beyond the triage cluster.

## Auto-fix
- `no candidate` — no signal matched the Phase 5 certainty bar + allowlist. The 33 triage signals are vector=`triage` (out of scope for auto-fix); the 3 dep-update-noise signals all reflect clean dependabot merges where the lockfile already matches the manifest (no lockfile-drift sub-shape); no screen-map drift/orphan, no `fixme-in-merged-code` (skipped derivation), no stalled review to ping.

## Self-review findings
- open=11 (high=3, medium=4, low=3) + 1 critical; new this run=0; acknowledged=9; source: dispatcher Phase-8 self-review (`.research/self-improvement/findings.json`), not routine-generated
- `subagent-race-on-dev-push` [critical, rec=1] — concurrent subagent pushes to `dev` create rapid version-bump cycles; fix: serialize dev pushes / advisory lock
- `no-run-level-lock-parallel-overlap` [high, rec=4] — two dispatcher cycles overlap with no lock → mid-run reset + re-run on push conflict; fix: run-level advisory lock at Phase 1 (PRs #734/#758 land Phase 0.5 advisory-lock — re-verify acknowledged status on next dispatcher run)
- `goal-check-gc1-orphan-action-list-items` [high, rec=1] — GC1 referential-integrity FAILs with 105 orphan action-list items; fix: Phase 2.6 cascade prune/relink
- `goal-check-gc3-buffer-overshoot` [high, rec=1] — GC3 buffer-bounds FAILs (claimable=117 ≈ 2× upper bound); fix: bound-aware Phase 2.6 refill (PR #734 landed buffer cap — re-verify)

## Open questions
- The "Current dev review" / "Dev review rounds" issue cluster (33 unlabeled OPEN issues #749–#791, opened 2026-05-29 21:12Z → 2026-05-30 01:49Z) appears to be dispatcher / dev-team review output. Should the routine label them automatically (e.g. `dev-review`, `from-routine`), or is the user planning to triage by hand? Right now they flood `IDEAS_TRIAGE.md` and may distort future digest reads. Surfaced for human judgement.
- `sqlx 0.8→0.9` is now landed (PR #666 merged 2026-05-29T11:01Z). Re-verify workspace compiles clean on `dev` (CI did pass for #666, but the `56bad7a fix(db): remove sqlx::query::AssertSqlSafe — not available in sqlx 0.8` commit further down the log suggests a fix-forward happened — confirm no straggler call-sites remain).

## Quiet day?
- No — 33 new triage rows, 26 merged PRs (real iOS Reality + MFA backend + IDOR fix shipments), 1 high-priority security plan (`security-llm-doc-idor`) still ready and untouched by this run's merges. Phase 1.5 + Phase 1.6 + churn-hotspot derivation were skipped (cloud time budget / skill not invoked) and are listed under "Phases that failed".
