# 2026-05-30

> **Note:** Same-day incremental rerun. The morning brief (03:11Z) covered through PR #786 / issue #791 / commit ce56a3b. This pass overwrites that brief to converge on the day's full picture, layering in 23 more merged PRs, 9 new triage issues, a screen-map drift signal, and three churn observations from the 03:05Z → ~20:44Z window.

## Since last run
- Merged PRs: 49 cumulative today (morning: 26 in #639–#786; this pass: 23 more in #783–#860 — note: numbered ranges interleave because dev landed back-dated PRs alongside fresh merges; see Shipped)
- Open PRs touched: 3 — #745 (e-sig nonce replay v2 draft, 1d+), #854 (data-retention/privacy doc draft, opened today 14:36Z), #861 (lease review RBAC closes #806, opened today 20:10Z, security label)
- New / updated issues: 13 (9 OPEN unlabeled code-review/bug + 4 CLOSED by merged PRs in this window: #818/#822/#832/#852)
- Commits: 29 on `dev` since the morning cursor (ce56a3b..8bd4d44) — mix of squashed security fixes, test-only follow-ups, dispatcher chore checkpoints
- Routine lag: on schedule (~17.7h since 2026-05-30T03:05Z, no lag_warning)
- Phases that failed: phase-1.5 (skipped — `ppt-dev-review` skill not invoked in this one-shot routine pass; `review_cursor` not advanced), phase-1.6 (skipped — `ppt-project-management` skill not invoked; `pm_cursor` + `coverage_cursor` not advanced)

## Shipped (this incremental window — 23 merged PRs)

**Security / IDOR hardening (5, all labeled `security`):**
- #860 fix(security): org-scope `export_report` + `get_export_job_status` (closes #832) — both now reject cross-tenant via `RlsConnection`; real-JWT regression tests cover manager-in-Org-B → 403/404
- #858 fix(security): owner-scope `GET /bookings/{id}` + fix sqlx 0.9 enum decode (closes #852) — see also #859 (still OPEN, broader sqlx 0.9 enum-column regression)
- #857 fix(security): enforce agency membership on agency read/import handlers (closes #818) — adds `agency_authz_idor_tests.rs`
- #855 fix(security): reject refresh tokens in `RequestPrincipal` extractor (closes #822) — `auth.rs` token-type discrimination; new `principal_token_type_tests.rs`
- #861 (still OPEN, security label): fix(security): require manager role to review tenant applications (closes #806) — `lease_review_rbac_tests.rs` resident → 403, manager → not-403

**Test-only follow-ups (5):**
- #853 test(api-server): unit tests for `validate_cron_expression` (gap-81-1)
- #844 gap-10b-7-help-backend-v2: contextual help handler regression tests
- #843 test(api-server): tighten OAuth security test assertions (closes #704) — R2/R3/R4 from PR #661 post-merge review
- #813 test(api-server): exercise document-folder org-scope with real auth (closes #679)
- #812 test(api-server): authenticated cross-tenant tests for report-schedule sibling-scope (closes #693)
- #811 test(reports): exercise RBAC + cross-tenant predicates with real JWTs (closes #696)
- #792 test(api-server): tighten document_upload_tests assertions (closes #701)

**Backend bugfixes (5):**
- #815 fix(db): add `cron_expression` column to report_schedules + doc references (closes #616) — **resolves `bug-report-schedule-update-no-sql` backlog item**: migration 00163 adds the dedicated column; `update_schedule` now writes via `SET cron_expression = COALESCE($3, cron_expression)` at report_schedule.rs:511 (TODO workaround gone)
- #794 fix(db): support_tooling_events — preserve audit on user/org delete + RLS (closes #699)
- #793 fix(db): wrap `get_support_data` in REPEATABLE READ transaction for consistent snapshot (closes #628)
- #841 fix(integrations): Airbnb webhook dedup table + cache env on AppState (closes #711) — **partial resolution of `bug-webhook-airbnb-dup-sync-jobs`**: migration 00169 adds `airbnb_webhook_events` dedup ledger by `event_id` (suppresses Airbnb at-least-once replays at webhook.rs:1014-1052); residual TODO at webhook.rs:1073 for inner-loop distinct-event dedup still present
- #837 fix(auth): `/auth/refresh` returns fresh tenant list from DB (closes #676)
- #783 fix(admin-core, db): Capability Display + canonical_name regression tests (closes #615)

**CI / unblock (2):**
- #856 fix(backend): restore `Cargo.lock` to sqlx 0.9.0 — unblocks all backend CI after the morning research-brief commit (3061d21) accidentally regressed the lockfile
- #787 fix(ci): add Redis service + dedicated step for WS pub/sub fanout test (closes #681)

**Frontend / a11y / i18n (3):**
- #839 fix(ppt-web): localize evidence-upload partial-fail toast + pass list via router state (closes #627) — **fires `screen-map-drift-pr-839-ppt` (heuristic; internal logic change, not route topology)**
- #840 fix(admin-web): sequence onboarding-tour mutations + raise overlay z-index + drop skip-step alias (closes #702 bugs)
- #838 fix(disputes): restore dialog a11y attrs (closes #703 a11y findings) — PR #633 extraction dropped `aria-labelledby`/`aria-describedby`

**Mobile-native (1):**
- #842 fix(mobile-native): empty-body construction, `isFavorite` repo test, `priceChanged` direction (closes #697)

**Tooling / research (1):**
- #814 fix(research): dispatcher self-review findings — cadence/lock/GC + rebase-ours + push-method — addresses `subagent-race-on-dev-push`, `no-run-level-lock-parallel-overlap`, `goal-check-gc3-buffer-overshoot` from dispatcher Phase-8 self-review

## Watch
- Stalled review: none (oldest open PR #745 is ~37h, draft; #854 and #861 both opened today)
- Reverted: none in window
- Closed-unmerged: none in window
- In-flight security: `security-llm-doc-idor` (score 3, plan ready) — still untouched by today's merges. Verified `routes/ai.rs:2622/2643/2870` and `repositories/llm_document.rs:358/395/977` continue to discard `_principal`; the 5 security PRs this window addressed sibling clusters (report export, booking owner, agency membership, RequestPrincipal token-type, lease review) but **not** LLM document IDOR. Next merge cycle should pick it up.
- Churn hotspots this run (sub-day window — score-suppressed): `reports.rs` (134 lines, PR #860 + #793), `auth.rs` (78 lines, PR #855), `webhook.rs` (75 lines, PR #841). Added to `hotspot_history` for trend tracking but no backlog rows seeded (micro-window security-fix churn, not unstable hotspot pattern).
- sqlx 0.9 fallout: PR #858 fixed one enum-decode regression (booking); **issue #859 (OPEN) reports the broader pattern** — `SELECT *` reads on tables with Postgres enum columns are returning 500. Now triaged as `triage-issue-859` but should be re-vectored to `bug` by a human reviewer.

## PRs stuck in draft despite approval
- none (#745 is draft and awaiting CI; not approved)

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: none (phase-1.5 skipped — `ppt-dev-review` skill not invoked in this one-shot routine pass)
- Experts: n/a
- Findings: 0
- Next segment: `ppt-web-core` (oldest unreviewed in practice — `api-handlers` cursor=2026-05-23 points to a segment deleted by PR #437)
- Skipped: yes — `ppt-dev-review` skill not invoked

## Screen-map status
- Total `docs/screens/` files: 108 (`ppt/` 62 + `reality/` 46; `mobile/` not yet seeded → 0) — count unchanged this run
- Drift signals this run: 1 — `screen-map-drift-pr-839-ppt` (PR #839 touched App.tsx without docs/screens/ppt update; heuristic — change is internal logic, not route topology)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not re-checked this run

## Backlog deltas
- **New (test-gap):** [2] Screen-map drift: PR #839 modified ppt-web App.tsx (FileDisputePageRoute) without a docs/screens/ppt update — `test-gap-screen-map-drift-pr-839-ppt`
- **New (triage):** 9 × `triage-issue-<N>` rows for new OPEN unlabeled code-review/bug issues — #836, #845, #846, #847, #848, #849, #850, #851, #859 — all `vector=triage`, score=1, surfaced into `IDEAS_TRIAGE.md` (never promoted to plans)
- **Bumped:** none
- **Decayed:** none (oldest open non-triage item `dx-portfolio-dashboard-stubs` updated 2026-05-20 = 10d < 14d threshold)
- **Dropped:** none
- **Resolved → done:**
  - `bug-report-schedule-update-no-sql` — PR #815 added `cron_expression` column (migration 00163) and rewrote `update_schedule` at report_schedule.rs:511 to the documented UPDATE. Closes #616. Evidence-gone confirmed: `grep -n "cron_expression\|update_schedule"` shows the dedicated column path; the overloaded `time` workaround is gone.
- **Resolution checks (open items re-verified):**
  - `bug-webhook-airbnb-dup-sync-jobs` — PR #841 (closes #711) added the `airbnb_webhook_events` dedup ledger (migration 00169) that suppresses Airbnb at-least-once replays by `event_id` at webhook.rs:1014-1052. **[partial]** — the inner-loop TODO at webhook.rs:1073 about distinct-event-rapid-succession SYNC_EXTERNAL dedup is still present. Stays `open`.
  - `security-llm-doc-idor` (plan ready) — files `routes/ai.rs` + `llm_document.rs` re-grepped; handlers still discard `_principal`. Stays `ready`.
  - `test-gap-booking-push-validation-untested` — no merged PR added tests for the #607 batch-cap/non-negative guards on `push_booking_availability`/`push_booking_rates`. Stays `open`.

## Plans promoted
- none this run — `security-llm-doc-idor` is already `status=ready` with `plans/security-llm-doc-idor.md` active from 2026-05-29. No fresh `score≥3` (or `vector==security` fast-track `score≥2 + confidence=high`) candidates surfaced this run; the only new non-triage signal (screen-map-drift) sits at score 2 with confidence `medium` (doesn't qualify for the security fast-track either).

## Auto-fix
- `no candidate` — no signal met the Phase 5 certainty bar + allowlist this run.
  - `screen-map-drift-pr-839-ppt` is in the allowlist BUT score=2 (< 3 threshold) AND the change is internal logic (not a route addition), so a stub-doc auto-fix would be wrong shape.
  - 9 triage signals are `vector=triage` (out of allowlist).
  - 3 churn-hotspot signals were score-suppressed (out of action).

## Self-review findings
- open=11 (high=3, medium=4, low=3) + 1 critical; new this run=0; acknowledged=9; source: dispatcher Phase-8 self-review (`.research/self-improvement/findings.json`), not routine-generated
- `subagent-race-on-dev-push` [critical, rec=1] — PR #814 landed in this window (closed status pending re-verification on next dispatcher run)
- `no-run-level-lock-parallel-overlap` [high, rec=4] — PR #814 (and earlier #734/#758) added Phase 0.5 run-level advisory lock; **re-verify acknowledged status** on next dispatcher run
- `goal-check-gc1-orphan-action-list-items` [high, rec=1] — GC1 referential-integrity orphan-action-list-items; fix: Phase 2.6 cascade prune/relink (not yet landed)
- `goal-check-gc3-buffer-overshoot` [high, rec=1] — PR #814 confirms GC3 buffer cap landed; re-verify

## Open questions
- The "Current dev review" / "Dev review rounds" / "Code review: Epic …" issue cluster keeps growing (now 42 OPEN unlabeled in IDEAS_TRIAGE — 33 from the morning plus 9 added this pass). They appear to be dispatcher / dev-team output. Should the routine apply a default label (e.g. `dev-review`, `from-routine`) so they stop firing `untriaged-issue` signals, or is hand-triage the intent? Right now they dominate the triage digest.
- Issue #859 (sqlx 0.9 SELECT * enum decode 500) is substantive — likely a real bug, not a triage row. PR #858 fixed one occurrence (booking); the issue body documents the general pattern. A human should re-vector it to `bug` with the relevant repository file(s) cited.
- PR #861 (lease review RBAC, closes #806) is open with the `security` label and includes regression tests. Worth a fast-track review — it sits next to the in-flight `security-llm-doc-idor` plan as the second highest-priority security item in the open-PR queue.

## Quiet day?
- No — 23 more PRs merged this pass (49 total today), 10 new backlog rows (1 non-triage + 9 triage), 1 backlog item resolved to `done`, 1 partial-resolution evidence appended. Phase 1.5 + Phase 1.6 + churn-promotion were skipped (no skill invocation in one-shot routine pass; sub-day churn score-suppressed) and are listed under "Phases that failed".
