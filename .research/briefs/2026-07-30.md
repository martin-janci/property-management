# 2026-07-30

_Same-day rerun (2nd pass, 20:20Z), fired by `buffer-low: claimable=12/72 — refill planner` — this brief supersedes the 03:32Z brief and covers the full day through 20:20Z._

## Since last run
- **Two windows this brief covers**:
  - Morning window (2026-07-28T03:20Z → 2026-07-30T03:32Z): 17 merged PRs (#2433–#2576), covered by the 03:32Z pass.
  - Afternoon delta (2026-07-30T03:32Z → 2026-07-30T20:20Z): 12 additional merged PRs (#2577–#2601 non-contiguous), 3 new draft PRs (#2600 #2602 #2603), 11 new open dependabot PRs (#2581–#2591), 1 new open issue (#2598), 2 new closed issues (#2573 #2574 both auto-closed by their fix PRs).
- Merged PRs (total this brief): **29** (17 morning + 12 afternoon).
- Open PRs touched (afternoon): **14 new open** — 3 draft dispatcher implementations, 11 dependabot batch opened 10:14–10:22Z, 3 accounting PRs (#2555/#2558/#2559) still open at ~46h age (no reviewDecision, not draft — under 7d stalled-review threshold).
- New / updated issues (afternoon): **3** — #2598 opened 16:39Z (`bug`+`frontend` labels — pre-existing verify-gate blockers for admin-web / reality-web; already being fixed by draft PR #2602), #2573 + #2574 closed by their auto-generated fix PRs.
- Commits (afternoon): **27** on `dev` (12 feature merges + 15 `chore(research):` dispatcher bookkeeping).
- Routine lag: **on schedule** — 16h 48m between passes (well under the 36h `lag_warning` threshold).
- Phases that failed: **none**.

## Shipped

**Morning window (17 PRs — see 03:32Z brief text preserved below):**
- #2576 gh-issue-2563: schedule layout_change_events retention prune — adds a daily scheduler tick + 3 unit tests (`test_layout_change_events_prune_due_*` in scheduler.rs:2458+); closes the retention gap surfaced by #2563.
- #2572 gh-issue-2562: wire get_dispute_kpis into a reporting endpoint — new `/disputes/kpis` route with happy-path test suite; regressed via #2575 (window-ordering + test quarantined).
- #2571 gh-issue-2564: org-scoped DELETE-by-file_key route for direct-upload orphan cleanup — regressed via #2573 (deletes still-referenced objects within same org).
- #2570 gh-issue-2557: dedupe `seed_org`/`set_ctx` helpers across 51 db test suites (Closes #2557) — 51-file mechanical refactor, no runtime code changed.
- #2569 dx-api-validation-drift-gate-never-runs-on-dev: run SDK drift gate on client + workflow changes.
- #2568 code-review-mobile-native-kmp-android-sso-deeplink-missing-csrf-state: verify CSRF state on Android SSO deep-link — regressed via #2574 (SsoStateStore.mint() has no call site, every reality://sso callback now rejected).
- #2567 code-review-api-core-scheduler-rls-leak-retry1: clear scheduler global-read RLS GUC before returning connection to pool.
- #2566 gh-issue-2561: rebase+retry version-bump push to fix GH006 on concurrent dev merges (workflow hardening).
- #2565 gh-issue-2560: fix reality-web Docker build — copy api-client node_modules into builder stage (closes 6-week image outage).
- #2554 chore(research): refill starved dispatcher stack — 7 new vectors, 14 promoted.
- #2553 code-review-ppt-web-core-authctx-init-stale-role: route AuthContext cold-boot through `refreshTokenInternal`.
- #2549 gh-issue-2532: wire layout publish/webhook/revalidate event emission + sink (adds migration `00225_create_layout_change_events.sql`).
- #2504 fix(api-server): signature-request list/create unreachable — mount as document sub-resource (BIT-313).
- #2491 chore(deps): npm-minor-patch group (dependabot; logged as `dep-update-noise` score 0).
- #2482 refactor-churn-hotspot-repo-map-md-2026-07-20: reconcile `docs/repo-map.md` with current tree.
- #2478 fix(layout): review-hardening sweep — authz, publish TOCTOU, webhook replay, defensive rendering.
- #2433 feat(mobile-native): iOS listing detail renders through the shared resolved layout.

**Afternoon delta (12 PRs merged 03:32Z–20:20Z):**
- #2601 dx-admin-web-platform-settings-mobile-config-permanent-noop: **fix no-op Save paths** in admin-web platform-settings + mobile-config — resolves the score-2 dx vector opened 2026-07-28. Marked `done` this run.
- #2599 refactor-churn-hotspot-api-server-reports-2026-07-27: extract schedule cadence/cron helpers from reports.rs — 887 lines moved into new `routes/reports/schedule_cadence.rs` submodule, `reports.rs` collapses to `reports/mod.rs`. Behavior-preserving; resolves the score-2 refactor vector. Marked `done` this run.
- #2597 fix(api-server): guard `DELETE /documents/by-file-key` against reaping a referenced object (Closes #2573) — adds `exists_by_file_key` repo method + 115-line test suite. Follows-up + closes the intra-tenant data-integrity IDOR spotted in #2571 post-merge review.
- #2596 code-review-ppt-web-aml-review-decision-untrusted-cast: validate AML review decision before submit — replaces unchecked `as` cast on `window.prompt` text with a validated union check. Marked `done` in backlog (dispatcher path, before this rerun).
- #2595 test(mobile-native): pin portfolio-analytics zero-view-day behaviour — reverts a phantom "fix" whose test passed unmodified on dev (IG3 fail-first violation), keeps only the passing regression pin on the correct existing behaviour. **Note the IG3 discipline** — the implementer honestly rolled back their own PR.
- #2594 test(api-server): DB-backed regression for support_tooling_events retention prune (follow-up to PR #2547 unit-test-only ship) — adds `#[sqlx::test]` driving the real method against Postgres. Closes the hotfix-no-test vector for #2547 (backlog `bug-hotfix-no-test-pr-2547` was already marked `done` by dispatcher pipeline).
- #2593 gh-issue-2574: fix(mobile-native) wire Android SSO initiation leg (mint CSRF nonce) — adds `SsoInitiation.kt` (commonMain) as the KMP mirror of iOS `LoginView.loginWithSso`, wires the "Sign in via PM App" action into Android LoginScreen. Closes follow-up #2574 opened at 00:24Z by the merged-review skill.
- #2592 fix(ppt-web): gate WebSocket console diagnostics behind `import.meta.env.DEV` — routes 10 `console.warn/error` calls through dev-only helpers so Vite DCE's them from prod bundle. **New signal**: `hotfix-no-test-pr-2592` (score 2, test-gap, high) — no test in diff. Backlog row created; note the fix is testing-inconvenient by nature (would need `import.meta.env` stubbing).
- #2580 refactor-churn-hotspot-platform-admin-authz-batch2-2026-07-23: de-duplicate platform_admin authz batch2 tests. Resolves an already-`done` backlog vector.
- #2579 refactor(api-server): de-duplicate org-property authz backfill tests — extract `finish()` + `assert_all_client_error()` helpers.
- #2578 code-review-ppt-web-core-ws-token-rotation-stale: re-auth WebSocket on token rotation instead of leaving a stale socket — 87-line failing-first regression + 69-line fix (`connect()` now tracks the token per open socket). Marked `done` in backlog.
- #2577 docs(reality-web): reconcile listing-detail screen-map with PR #2497 layout-revalidate hardening — updates the write-side note (replay-protection over `{timestamp}.{body}`, 16 KiB body cap, `layout_revalidate_received` analytics). Decision unchanged: internal infra endpoint → no standalone screen-map. **Resolves the previous brief's open question about `screen-map-drift-pr-2497-reality`** — already marked `done`.

## Watch
- Stalled review: **none** — the 3 accounting PRs (#2555/#2558/#2559, still open, still not-draft, still no reviewDecision) are at ~46h since open — under the 7d threshold. 11 dependabot PRs opened this morning are only 10h old.
- Reverted: **none** in either window.
- Churn hotspots (cumulative through afternoon):
  - `backend/servers/api-server/src/services/scheduler.rs` — 465 lines (PRs #2567, #2576, #2594; **+118 from #2594** cumulative-update, no score re-add per Phase 1 cumulative rule), runs_seen=1
  - `backend/servers/api-server/src/routes/layout/tenant.rs` — 262 lines (PR #2478), runs_seen=1
  - `backend/servers/api-server/src/routes/layout/admin.rs` — 240 lines (PRs #2478, #2549), runs_seen=1
- **Buffer-low signal (from routine trigger)**: caller reported `claimable=12/72 — refill planner`. See *Plans promoted* — 0 candidates meet readiness gates this run; **buffer-low cannot be relieved by relaxing the routine's gates** (that trades planner-buffer for implementer-noise). Genuine relief comes from either landing 1–2 of the 3 open score-2 items (through the dispatcher's existing flow), or from an operator hand-authored plan against a score-1 or score-2 open row.

## PRs stuck in draft despite approval
- none — none of the 3 new draft PRs (#2600 #2602 #2603) or the pre-existing 3 accounting PRs carry `reviewDecision=APPROVED` while stuck in draft. (Query was not re-executed live this run; last live check at 03:32Z showed zero.)

## PRs with verdict=changes, no fix-round progress in 24h
- none — no `status=review` row in `.research/management/assignments.json` carries `verdict=changes` at read time; the two dispatcher flows currently `verdict=changes` (the reality-web XSS + admin-web verify-gate) are already respawned as PRs #2600 and #2602 within the last few hours.

## Code review slice
- Segment reviewed: **api-core** (morning pass — reason: churn-aligned, all three CHURN_FILES land under `backend/servers/api-server/src/` non-handlers).
- Experts: rust (single expert; skill defines no secondary for `api-core`).
- Findings: **3** — one **Medium** (`resolved.rs` leaks raw sqlx/serde error text to public GET) → backlog row `code-review-api-core-resolved-rs-leaks-db-error` (score 2, medium); two **Low** (`admin.rs`+`tenant.rs` swallowed serialize → 200/null on failure; `scheduler.rs` silently swallows DB errors on notification target lookups at 3 sites).
- Cursor advanced: `review_cursor.api-core: 2026-07-20 → 2026-07-30` — this run **also fixed the earlier state-write miss** (the 03:32Z pass wrote the brief claiming api-core advance but did not update `state.json.review_cursor`). Next segment: `reality-server` (last reviewed 2026-07-18, oldest at 12 days).
- Skipped afternoon Phase 1.5: yes — same-day rerun does not spawn a second review pass; the segment cursor is now correct and the next daily run will pick the true oldest.

## Screen-map status
- Total `docs/screens/` files: **168** unchanged this window (ppt/=106, reality/=47, reality-mobile/=15; mobile RN app still lacks `docs/screens/mobile/` — orphan-detection remains disabled for the RN app).
- Drift signals this run (afternoon-only sweep): **0** — none of the 12 afternoon-merged PRs touch `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**` outside `app/api/**` (the internal ISR endpoint carve-out).
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not queried this run.

## Backlog deltas
- **New (afternoon delta):**
  - [2] test-gap: `bug-hotfix-no-test-pr-2592` — `fix(ppt-web)` gate WebSocket console diagnostics behind DEV shipped without a regression test. Score 2, confidence high, vector test-gap. (See caveat above — testing-inconvenient.)
- **Morning (6 new — carried from 03:32Z pass):**
  - [1] refactor: `refactor-churn-hotspot-api-server-scheduler-2026-07-30` — evidence cumulatively-updated this run (PR #2594 added +118 lines; score unchanged)
  - [1] refactor: `refactor-churn-hotspot-api-server-layout-tenant-2026-07-30`
  - [1] refactor: `refactor-churn-hotspot-api-server-layout-admin-2026-07-30`
  - [2] bug: `code-review-api-core-resolved-rs-leaks-db-error` (SECURITY-LITE)
  - [1] bug: `code-review-api-core-admin-rs-swallowed-serialize`
  - [1] bug: `code-review-api-core-scheduler-rs-silent-target-err`
- **Bumped:** none this run.
- **Decayed:** none — no `open` item is older than 14 days.
- **Resolved to `done` this run (afternoon):**
  - `refactor-churn-hotspot-api-server-reports-2026-07-27` — verbatim PR title match on #2599.
  - `dx-admin-web-platform-settings-mobile-config-permanent-noop` — verbatim PR title match on #2601.
- **Dropped:** none.

## Plans promoted
- **none this run.** Open backlog top:
  - `code-review-reality-web-inline-tenant-json-xss` (score 2, security, medium) — security fast-track needs high confidence; NOT ready. Also has active draft PR #2600 (dispatcher stem-in-flight — collision would block promotion anyway).
  - `test-gap-voice-webhooks-zero-coverage` (score 2, test-gap, high) — needs score ≥3.
  - `code-review-api-core-resolved-rs-leaks-db-error` (score 2, bug, medium) — needs score ≥3 (medium confidence blocks security fast-track).
  - `bug-hotfix-no-test-pr-2592` (score 2, test-gap, high, new this run) — needs score ≥3.
- Nothing hits promotion thresholds. **Buffer-low cannot be relieved without violating the readiness gates.**

## Auto-fix
- `no candidate` — no signal this run meets confidence=high + score≥3 + allowlist type. The 1 new `hotfix-no-test` signal is score 2 and its shape isn't on the auto-fix allowlist. `ROUTINE_AUTOFIX_DISABLED` unset. `state.auto_fix_history` remains empty.

## Self-review findings
- open=21 (high=4, medium=13, low=4); new this run=0; acknowledged=19
- fp-<unknown> [recurrence=10] — Phase 6 archive-move push produces 198KB payloads that mcp__github__push_files rejects; fix: push dispatcher state to a NEW branch (recorded workaround, still unlanded).
- fp-<unknown> [recurrence=6] — dispatcher-prompt hard-assumes `git push` proxy-403 and derives failure from it; fix: replace with a runtime probe at Phase 1.
- fp-<unknown> [recurrence=3] — Phase 2.6 buffer reported claimable=54 but real claimable=0; fix: (1) CLOSE step extend gc1-reconciliation, (2) still unlanded as of ce9a2cb6a.
- fp-<unknown> [recurrence=2] — Tier-2 upstream planner kick returns http=401; fix: operator to rotate/refresh DISPATCHER_TOKEN.
- **New behavioral finding (this run, unfiled in `findings.json`):** the 03:32Z routine pass wrote the brief claiming `review_cursor.api-core` advanced to 2026-07-30 but did NOT write that update to `state.json` — the brief and state disagreed until this rerun fixed it. Root-cause worth capturing: Phase 4's state-write happens BEFORE Phase 1.5's `state.review_cursor.<segment>` update, so if the writer collects the cursor snapshot before Phase 1.5 returns, the update is lost. Filed as an operational observation for the next routine-prompt maintenance PR — not urgent enough to open an issue.

## Open questions
- **Should the routine's Phase 3 promotion gate be relaxed when the caller-supplied `buffer-low` payload fires?** Current answer: **no** — quality gates exist for a reason and the dispatcher can consume score-2 items directly via its own PR path (as seen with #2600/#2603 today). But if buffer-starvation persists across multiple daily runs, an explicit "buffer-relief mode" that promotes the top-N by `(score, updated_at)` regardless of gate might be worth prototyping. Not doing it this run.

## Quiet day?
- **No** — 12 additional shipped PRs + 1 new backlog vector this rerun, on top of the morning window's 17 PRs / 6 vectors. Full day: 29 PRs shipped, 7 new backlog vectors (6 morning + 1 afternoon), 2 items resolved-to-done this afternoon, 1 significant new open issue (#2598). Above the noise floor by every metric.
