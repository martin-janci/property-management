# 2026-07-30

## Since last run
- Merged PRs: 17 (range #2433–#2576)
- Open PRs touched: 3 (`feat(acc):` MVP-loop trio #2555/#2558/#2559 opened 2026-07-28 21:24–21:58Z by martin-janci — none draft, all reviewDecision-less, all <2h old at cursor time)
- New / updated issues: 10 (3 still open — all `follow-up`/`from-merged-review` from the Tier-1d auto-review of 2026-07-30 00:24Z: #2573 #2574 #2575)
- Commits: 43 on `dev` (includes dispatcher `chore(research):` bookkeeping)
- Routine lag: **1d 23h since last run** (2026-07-28T03:20Z → 2026-07-30T03:32Z) — **above** the 36h `lag_warning` threshold, emitted as signal `lag-warning-2026-07-30` (score 0). Cause: routine did not tick 2026-07-29; cloud scheduler likely paused for a day.
- Phases that failed: **none**

## Shipped
- #2576 gh-issue-2563: schedule layout_change_events retention prune — adds a daily scheduler tick + 3 unit tests (`test_layout_change_events_prune_due_*` in scheduler.rs:2458+); closes the retention gap surfaced by #2563.
- #2572 gh-issue-2562: wire get_dispute_kpis into a reporting endpoint — new `/disputes/kpis` route with happy-path test suite; regressed via #2575 (window-ordering + test quarantined).
- #2571 gh-issue-2564: org-scoped DELETE-by-file_key route for direct-upload orphan cleanup — regressed via #2573 (deletes still-referenced objects within same org).
- #2570 gh-issue-2557: dedupe `seed_org`/`set_ctx` helpers across 51 db test suites (Closes #2557) — 51-file mechanical refactor, no runtime code changed.
- #2569 dx-api-validation-drift-gate-never-runs-on-dev: run SDK drift gate on client + workflow changes — resolves open backlog vector (marked done).
- #2568 code-review-mobile-native-kmp-android-sso-deeplink-missing-csrf-state: verify CSRF state on Android SSO deep-link — regressed via #2574 (SsoStateStore.mint() has no call site, every reality://sso callback now rejected).
- #2567 code-review-api-core-scheduler-rls-leak-retry1: clear scheduler global-read RLS GUC before returning connection to pool.
- #2566 gh-issue-2561: rebase+retry version-bump push to fix GH006 on concurrent dev merges (workflow hardening).
- #2565 gh-issue-2560: fix reality-web Docker build — copy api-client node_modules into builder stage (closes 6-week image outage).
- #2554 chore(research): refill starved dispatcher stack — 7 new vectors, 14 promoted.
- #2553 code-review-ppt-web-core-authctx-init-stale-role: route AuthContext cold-boot through `refreshTokenInternal` — resolves open backlog vector (marked done).
- #2549 gh-issue-2532: wire layout publish/webhook/revalidate event emission + sink — adds migration `00225_create_layout_change_events.sql`, retention test suite, and reality-web `route.ts` + tests.
- #2504 fix(api-server): signature-request list/create unreachable — mount as document sub-resource (BIT-313) — adds `signature_request_happy_path_tests.rs`.
- #2491 chore(deps): bump the npm-minor-patch group across 1 directory with 5 updates — dependabot; logged as `dep-update-noise` (score 0).
- #2482 refactor-churn-hotspot-repo-map-md-2026-07-20: reconcile `docs/repo-map.md` with current tree.
- #2478 fix(layout): review-hardening sweep — authz, publish TOCTOU, webhook replay, defensive rendering (46-file cross-stack change; drives 2 of this run's 3 churn hotspots).
- #2433 feat(mobile-native): iOS listing detail renders through the shared resolved layout (KMP + iOS SwiftUI).

## Watch
- Stalled review: **none** — 3 open PRs are all <2h old (well under the 7d threshold).
- Reverted: **none**.
- Churn hotspots (this run):
  - `backend/servers/api-server/src/services/scheduler.rs` — 347 lines (PRs #2567, #2576), runs_seen=1 (new)
  - `backend/servers/api-server/src/routes/layout/tenant.rs` — 262 lines (PR #2478), runs_seen=1 (new)
  - `backend/servers/api-server/src/routes/layout/admin.rs` — 240 lines (PRs #2478, #2549), runs_seen=1 (new)

## PRs stuck in draft despite approval
- none — no open PR in the window is both `isDraft=true` and `reviewDecision=APPROVED`. The 2026-07-28 blocker (#2549 stuck in draft with pm-backend approval) landed via merge in this window.

## PRs with verdict=changes, no fix-round progress in 24h
- none — all `status=review` rows in `.research/management/assignments.json` carry either `verdict=approve` or a null `reviewer_summary`.

## Code review slice
- Segment reviewed: **api-core** (reason: churn-aligned — all three CHURN_FILES land under `backend/servers/api-server/src/` non-handlers). Saturation check: 0 open `code-review-api-core-*` in backlog → not saturated.
- Experts: rust (single expert; skill defines no secondary for `api-core`).
- Findings: **3** — one **Medium** (`resolved.rs` leaks raw sqlx/serde error text to public GET), two **Low** (`admin.rs`+`tenant.rs` serialize-swallow → 200 OK/null on failure; `scheduler.rs` silently swallows DB errors on notification target lookups at 3 sites). All `confidence=medium` (static analysis, no runtime confirmation).
- Next segment: `reality-server` (last reviewed 2026-07-18, now oldest at 12 days after this run advances `api-core` to 2026-07-30).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: **168** (ppt/=106, reality/=47, reality-mobile/=15; mobile RN app has no `docs/screens/mobile/` yet — orphan-detection remains disabled for the RN app per Phase 1 rule)
- Drift signals this run: **0** — two candidate hits on PRs #2478 and #2549 were suppressed after tightening the route regex to exclude `frontend/apps/reality-web/src/app/api/**` (internal HMAC-signed ISR revalidation endpoint already noted "no standalone screen-map warranted" in `docs/screens/reality/listing-detail.md` Agent Log 2026-07-23).
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not queried this run.

## Backlog deltas
- **New (6):**
  - [1] refactor: `refactor-churn-hotspot-api-server-scheduler-2026-07-30`
  - [1] refactor: `refactor-churn-hotspot-api-server-layout-tenant-2026-07-30`
  - [1] refactor: `refactor-churn-hotspot-api-server-layout-admin-2026-07-30`
  - [2] bug: `code-review-api-core-resolved-rs-leaks-db-error` (SECURITY-LITE — public GET leaks raw sqlx error text)
  - [1] bug: `code-review-api-core-admin-rs-swallowed-serialize`
  - [1] bug: `code-review-api-core-scheduler-rs-silent-target-err`
- **Bumped:** none — no signal this run targeted an existing open item.
- **Decayed:** none — no `open` item is older than 14 days.
- **Dropped:** none.
- **Resolved (already `done` by dispatcher via merged PRs):** `dx-api-validation-drift-gate-never-runs-on-dev` (PR #2569), `code-review-mobile-native-kmp-android-sso-deeplink-missing-csrf-state` (PR #2568), `code-review-ppt-web-core-authctx-init-stale-role` (PR #2553).

## Plans promoted
- none this run. Both eligible open candidates — `security-voice-webhook-alexa-signature-not-verified` (score 5, security/high) and `repeated-churn-api-server-routes-auth-rs-2026-07-28` (score 3, refactor/high) — are already `in-progress` on the dispatcher (matching action-list rows + `assignments.json` claims). Promoting would violate the "at most one non-terminal unit of work per stem" invariant. The three code-review-finding rows created this run all sit at score 1–2 and are below the readiness threshold.

## Auto-fix
- `no candidate` — no signal this run met the certainty bar (confidence=high + score≥3 + allowlist type). The 3 `code-review-finding` signals are `confidence=medium` (static analysis) and the 3 `churn-hotspot` signals are analytical, not actionable; `lag_warning` and `dep-update-noise` are score-0 log-only signals. `ROUTINE_AUTOFIX_DISABLED` unset.

## Self-review findings
- open=21 (high=4, medium=13, low=4); new this run=0; acknowledged=19
- fp-<unknown> [recurrence=10] — Phase 6 archive-move push produces 198KB payloads that mcp__github__push_files rejects; fix: push dispatcher state to a NEW branch (recorded workaround, still unlanded).
- fp-<unknown> [recurrence=6] — dispatcher-prompt hard-assumes `git push` proxy-403 and derives failure from it; fix: replace with a runtime probe at Phase 1.
- fp-<unknown> [recurrence=3] — Phase 2.6 buffer reported claimable=54 but real claimable=0; fix: (1) CLOSE step extend gc1-reconciliation, (2) still unlanded as of ce9a2cb6a.
- fp-<unknown> [recurrence=2] — Tier-2 upstream planner kick returns http=401; fix: operator to rotate/refresh DISPATCHER_TOKEN.

## Open questions
- **Should `screen-map-drift-pr-2497-reality` (score 2, open) be dropped now that the drift regex excludes `frontend/apps/reality-web/src/app/api/**`?** The underlying route (`layout-revalidate/route.ts`) is documented as "no standalone screen-map warranted"; the item's evidence is stale. Left `open` this run — needs human ack before flipping to `dropped`.

## Quiet day?
- **No** — 17 shipped, 3 fresh open PRs, 6 new backlog vectors (including 1 Medium and 3 Low code-review-findings), 4 new PM risks, 2 new PM decisions, 43 commits. Above the noise floor by every metric.
