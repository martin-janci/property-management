# 2026-08-10

## Since last run
- Merged PRs: **25** (range #2706–#2730; #2705 closed unmerged)
- Open PRs touched: **13** (#2684 updated 08-08; 12 dependabot bumps)
- New / updated issues: **3** — #2703 (SSRF DNS-rebinding, closed by #2710), #2704 (unbounded response-body, closed by #2707), #2612 (scheduled notification fire-once, closed by #2714)
- Commits: **~100** on `dev` (29 non-chore fixes + dispatcher chore stream)
- Routine lag: **3d 10h** since last run (2026-08-07T06:24 → 2026-08-10T16:15) — dispatcher ran meanwhile, routine did not. `lag_warning` + `stale_routine_alert` emitted.
- Phases that failed: none

## Shipped
- **Fail-closed on quiet-hours read (#2730)** — resolves `code-review-api-core-quiet-schedule-err-failopen`. Pure `resolve_push_quiet_gate` helper + regression tests.
- **Voice-actions fail honestly (#2728)** and **quiet-drain retention on failure (#2729)** — sibling fail-open fixes on the notification/voice pipeline.
- **Agency members IDOR (#2727)** — reality-server requires agency membership for member list.
- **SSO session invalidate (#2726)** — reality-server sync_session now surfaces portal invalidate failure.
- **Password reset transport (#2725)** + **DB error routing (#2724)** — reality-server auth hardening.
- **Community authz retry (#2722)** and **layout webhook HMAC test (#2718)** — security backfill.
- **Announcement fan-out metrics (#2723)**, **dispute add_evidence audit (#2712)**, **admin PATCH/GET endpoints (#2716/#2717)**, **inquiry notify wiring (#2719)** — sprint delivery.
- **RAG partial-batch fail-closed (#2706)**, **workflow response-body cap (#2707)**, **workflow non-finite compare (#2708)**, **SSRF DNS-rebinding fix (#2710)** — Rust workflow hardening batch.
- **Refactor stack:** #2711/#2713/#2715/#2720/#2721 — churn hotspot follow-ups (layout tenant, layout admin, auth, reports, layout tenant retry).
- **Feature backfills:** #2714 (scheduled dispatch decouple), #2709 (reality-web ListingForm i18n).

## Watch
- **PR #2705 closed unmerged** — rust-toolchain 1.94→1.100 bump did not land; dependabot successor not visible in-window (`closed-not-merged-pr-2705` emitted).
- **PR #2684 open** — `code-review-api-core-workflow-cond-parse-failopen` still awaiting review since 2026-08-06 (~4d — under stalled threshold).
- **Feat/acc stalled reviews:** #2555, #2558, #2559 — all opened 2026-07-28, last touched 2026-07-30 (12d idle). Three `stalled-review` signals emitted.
- **Dependabot backlog:** 12 open bumps (Rust + JS + Kotlin). Logged as `dep-update-noise`, no scoring.
- **Churn hotspots this window:**
  - `frontend/packages/sitemap/src/json/sitemap.json` — 4207 lines (likely generated by sitemap tooling; suppress in next run if confirmed generated)
  - `frontend/apps/ppt-web/messages/en.json` — 3984 lines (i18n catalog churn — expected during feat/acc work)
  - `backend/servers/api-server/tests/suites/oauth_integration_tests.rs` — 3416 lines (test suite growth around oauth — healthy, not repeated)
- Historical repeated hotspots (`auth.rs` runs_seen=5, `reports.rs` runs_seen=4, `booking/mod.rs` runs_seen=3) were not touched this window — no `repeated-churn` re-emit.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none — dispatcher's assignments.json on the `planning` branch not fetched this run (out-of-scope for the daily routine); dispatcher chore stream shows the fix-round loop is turning.

## Code review slice
- Segment reviewed: **api-handlers** (reason: churn-aligned — 3 of the top-6 code-only hotspots this window are inside `routes/`: `webhook.rs`, `scheduler/mod.rs`, `financial.rs`, `buildings.rs`)
- Experts: Rust + Security
- Findings: **1** — `code-review-api-handlers-buildings-role-gate-gap` (score +3, security, medium confidence). Six unit + owner mutation handlers in `buildings.rs` (lines 1205/1429/1598/1882/2104/2266) lack the Manager role gate that `create_building:501` uses.
- Next segment: **mobile-native-kmp** (`review_cursor` cutoff 2026-08-04 = new oldest)
- Skipped: no

## Screen-map status
- Total `docs/screens/` files: unchanged (`ppt/` + `reality/` populated; `mobile/` not seeded)
- Drift signals this run: **0** (no PRs in-window touched `frontend/apps/{ppt-web,reality-web}` route files — all merges were backend or shared)
- Orphan screens this run: **0** (no scan this run — screens-map orphan scan requires touched routes as anchor)
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (budget)

## Backlog deltas
- **Resolved (open → done):**
  - [1] `code-review-api-core-quiet-schedule-err-failopen` — PR #2730 merged 2026-08-10T14:11; evidence-gone confirmed (`get_user_schedule.*.ok().flatten()` pattern removed from primary dispatch path).
- **New:**
  - [3] `code-review-api-handlers-buildings-role-gate-gap` (security, medium) — Manager role gate missing on 6 unit+owner mutations in `buildings.rs`. **Promoted to plan this run.**
  - [2] `qa-rag-partial-batch-handler-regression-2026-08-10` (test-gap, medium) — #2706 landed without a handler-level regression for the fail-closed guard; test file exists at `tests/suites/rag_index_embedding_write_tests.rs` with zero partial-batch cases.
- **Bumped:** none
- **Decayed:** none — the sole remaining open item at cutoff (`code-review-mobile-native-kmp-portfolio-analytics-caps-100`, updated 2026-08-04) is 6d old, under the 14d decay threshold.
- **Dropped:** none

## Plans promoted
- `plans/code-review-api-handlers-buildings-role-gate-gap.md` — patch six unit/owner mutation handlers in `buildings.rs` to mirror `create_building:501`'s Manager gate + add negative-path integration test `buildings_authorization.rs`. Cloud-ok. · adversarial pass: passed (files exist on disk, no placeholder rot, test file listed in Test plan not Files, all 4 metadata fields + 11 headings + Mode declaration present)

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The single promotable item (`buildings-role-gate-gap`) is a security-vector Rust handler patch — not on the allowlist (allowlist is `screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / `dep-update-noise` lockfile drift / `stalled-review`). Kill switch not set.

## Self-review findings
- none

## Open questions
- **Coverage exhaustion.** `.research/management/coverage.json` shows 47/49 stories done. The two remaining (`84-1-s3-presigned-urls` frontend consumer, `84-2-esignature-email` document-sign page in ppt-web) are frontend-only wiring gaps. Should the routine start emitting `coverage-gap` signals against these to feed the buffer, or is the plan to wind down and rely on the code-review + PM lenses to generate new vectors from operational drift?
- **Routine cadence.** 3d gap between routine runs while dispatcher kept ticking — the daily cron may not be firing. Worth verifying via `ScheduleWakeup`/`CronList` on the cloud side.

## Quiet day?
- No — 1 new plan promoted, 2 new backlog vectors created, 1 backlog item resolved, 1 code-review finding surfaced. Delivery digest sent (Phase 6).
