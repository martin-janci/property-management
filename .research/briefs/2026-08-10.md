# 2026-08-10

## Since last run
- Merged PRs: 24 in window (`#2706` → `#2729`; range 2026-08-07 14:19Z → 2026-08-10 10:13Z). Dispatcher productive across 3 days.
- Open PRs touched: 0 (all in-flight PRs closed/merged during the window).
- New / updated issues: 3 — #2703 (SSRF DNS-rebinding TOCTOU, CLOSED by #2710), #2704 (unbounded workflow api_call body read, CLOSED by #2707), #2612 CLOSED by #2714. Highest issue number seen: 2704.
- Commits: `origin/dev` head advanced `a2c0f53d` → `8f3dff51` (mix of PR merges + dispatcher research-chore commits).
- Routine lag: **3d 06h since last run** — cloud routine tick apparently missed 2026-08-08 and 2026-08-09. `lag_warning` + `stale_routine_alert` both emitted; dispatcher kept running independently on its own branch, so backlog resolution was tracked live via its commits.
- Trigger payload observed: `buffer-low: claimable=2/72 — refill planner`. Same shape as 2026-08-07's rerun: dispatcher's ready-to-implement queue drained. Routine's answer is unchanged and remains load-bearing: `score >= 3` (or security fast-track `score >= 2 + confidence=high`) is a readiness gate, not a queue-depth throttle.
- Phases that failed: none. Phases skipped: 1.5 (`ppt-dev-review`) and 1.6 (`ppt-project-management`) — cloud sandbox has no `gh` CLI and the skills need it for rotating segment reads + PM role snapshots; running them without their tools would produce hollow signals worse than a skip. Cursors for both remain at their prior values; the next daily tick with a working `gh` should pick them up.

## Shipped
- **24 PRs merged in the window.** Dispatcher-driven backlog burn-down + a few churn-hotspot follow-ups. High-signal landings:
  - `#2707` — closed #2704 (unbounded workflow api_call response-body read → memory-amplification DoS). Cap enforced at `MAX_RESPONSE_BYTES`.
  - `#2710` — closed #2703 (SSRF DNS-rebinding TOCTOU in workflow api_call). Anti-rebinding gate now atomic vs. resolution.
  - `#2712` — dispute add_evidence audit event emitted (IDOR-fix follow-up).
  - `#2714` — closed #2612 (scheduled-notification fire-once). Publish/activate/close decoupled from dispatch.
  - `#2718` — layout webhook HMAC parity + body-binding test.
  - `#2719` — anonymous inquiry POSTs routed through InquiriesHandler.
  - `#2722` — community.rs read handlers gated on principal + tenant (community unauthenticated-reads follow-up).
  - `#2725` — reality-server password-reset email transport wired.
  - `#2726` — reality-server SSO `sync_session` surfaces portal invalidation failure.
  - `#2727` — reality-server agency members list requires membership check.
  - `#2728` — voice check-announcements / check-meter fail honestly (resolves `code-review-api-core-voice-actions-fabricated-empty`).
  - `#2729` — quiet-hours drain no longer releases held notifications on failed delivery (resolves `code-review-api-core-quiet-drain-drops-failed-delivery`).
- Remaining 12 PRs are churn-hotspot refactors + smaller review-driven fixes; not restated individually.

## Watch
- **CI blockers:** none active as of `origin/dev@8f3dff51`. #2699/#2700 collision resolved on 2026-08-07; nothing new red today.
- **Reverted:** none observed in window.
- **Churn hotspots (window snapshot, from PR titles — no per-file diff pull this run to preserve budget):** `services/workflow_executor.rs`, `routes/ai/llm.rs`, `layout/**` (auth + admin + tenant handlers — three refactor PRs), `reports/**`. No file rose above the routine's `+2` risky-churn threshold; the workflow / layout files churn is driven by cleanup PRs (`refactor-churn-hotspot-*`) not bug fixes.

## PRs stuck in draft despite approval
- none — no drafts remain open on `origin/dev` per the MCP snapshot pulled at 12:09Z.

## PRs with verdict=changes, no fix-round progress in 24h
- none — planning branch `.research/management/assignments.json` currently holds 2 rows (1 in-progress + 1 quarantined); neither carries a `verdict=changes` reviewer summary in the current copy.

## Code review slice
- Segment reviewed: **none this run** — Phase 1.5 skipped (see *Skipped* note in *Since last run*). Cursors preserved: `api-handlers=2026-08-06`, `api-core=2026-08-10`, `reality-server=2026-08-06`, `ppt-web-ui=2026-08-06`, `ppt-web-core=2026-08-06`, `reality-web=2026-08-07`, `mobile-rn=2026-08-06`, `mobile-native-kmp=2026-08-04`.
- Experts: n/a.
- Findings: 0 emitted this run. However, tier1d dispatcher-generator signals already landed today under `signals/2026-08-10-{api-core,api-handlers,mobile-native-kmp,reality-server}-tier1d.json` — those were folded into `backlog.json` by their respective dispatcher commits before this routine ran, so they are not re-scored here.
- Next segment: `mobile-native-kmp` (oldest — 2026-08-04) OR `api-handlers`/`reality-server`/`ppt-web-ui`/`ppt-web-core`/`mobile-rn` (tied at 2026-08-06).
- Skipped: **yes** — cloud sandbox lacks `gh` CLI needed by the skill; would produce noise, not signal.

## Screen-map status
- Total `docs/screens/` files: unchanged (no window PR touched `docs/screens/` or route files under the trigger set — verified against titles; `frontend/**` PRs in the window are all `reality-web` component/form/ListingHandler work not route mutations).
- Drift signals this run: 0.
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (per-workflow REST call skipped for budget; would need `gh` CLI).

## Backlog deltas
- **Resolved (open → done via PR-body match):**
  - `code-review-api-core-voice-actions-fabricated-empty` — PR #2728 merged 2026-08-10 10:13Z. Title contains verbatim item id.
  - `code-review-api-core-quiet-drain-drops-failed-delivery` — PR #2729 merged 2026-08-10 10:12Z. Title contains verbatim item id.
- **New:** none (this-run signal capture is intentionally light; the 24 merged PRs each closed a preexisting backlog item or a churn-refactor task tracked in the dispatcher's action-list, not new organic bug material).
- **Bumped:** none.
- **Decayed:** none — the only pre-window `open` item, `code-review-mobile-native-kmp-portfolio-analytics-caps-100`, was last touched 2026-08-04 (6 days ago; 14-day cutoff is 2026-07-27, safely inside the window).
- **Dropped:** none.

**Open at score ≥ 3 (promotable pool):** **0 items.** Same shape as 2026-08-07 06:24Z rerun. Remaining `open`:
  - `code-review-mobile-native-kmp-portfolio-analytics-caps-100` (score 2, no PR).
  - `code-review-api-core-quiet-schedule-err-failopen` (score 1, no PR).

Buffer refill blocked on: (a) organic new-signal arrival in the next full daily tick's Phase 1/1.5 (which needs `gh` CLI), or (b) dispatcher tier1d signals bumping an existing item's score.

## Plans promoted
- **none this run.** No open item at `score >= 3`, and no `security` + `confidence=high` + `score >= 2` fast-track candidate. Promotion cap is not the bottleneck; the readiness gate is.

## Auto-fix
- `no candidate` — `ROUTINE_AUTOFIX_DISABLED` not set; no signal met confidence=high + score≥3 + allowlist this run. Nothing in the window fits the `screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / lockfile-drift / `stalled-review` shapes.

## Self-review findings
- none

## Open questions
- **Why is the cloud routine tick missing days?** `state.last_run_iso` jumped from 2026-08-07T06:24Z to 2026-08-10T12:09Z. Dispatcher kept ticking (its own branch/loop), but the research routine's daily cron/wakeup did not. Trigger for this invocation was a dispatcher `buffer-low` fire, not the daily tick. **Recommendation:** operator check the cloud routine scheduler for `martin-janci/property-management` — the daily research tick appears stopped.
- **Phase 1.5 / 1.6 in sandbox without `gh`.** Both skills need the GitHub CLI for their reads; MCP tools work but the skill Bash scripts are hard-coded to `gh`. Consider either an MCP-aware skill fork or a `gh`-installed sandbox for the routine's cloud host.
- Unchanged from 2026-08-07: should Phase 5 allowlist grow to include migration-prefix collision fix shape? Still blocked on cross-workspace `cargo sqlx prepare`.

## Quiet day?
- **No** — real state advance (last_pr_seen 2702→2729, last_issue_seen 2699→2704, last_commit_sha a2c0f53d→8f3dff51, 2 backlog items resolved). Not a quiet day; `stats.quiet_days` NOT incremented this run.
