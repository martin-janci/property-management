# 2026-07-17

## Since last run
- Merged PRs: 26 (range #2371–#2416)
- Open PRs touched: 10 (9 dependabot bumps opened same-day #2386–#2394 + draft #2377)
- New / updated issues: 21 (all follow-up issues opened by post-merge review; all resolved by merged PRs within this window)
- Commits: 50 on `dev` (application code + dispatcher artifacts)
- Routine lag: 1d 7h since last run (on schedule)
- Phases that failed: **phase-1.5-rotating-expert-review (skipped)**, **phase-1.6-project-management (skipped)** — both intentionally deferred this run (see rationale below)

## Shipped
26 merged PRs in a tight 31-hour window. Highlights (grouped):
- **Cross-tenant IDOR fix (1 PR)** — #2416 (gh-issue-2413) `government_portal` connection get/update/delete/test unscoped; owner-bypassed RLS + missing org filter. Ships with `government_portal_connection_cross_org_idor_tests.rs`.
- **Post-merge follow-ups (13 PRs)** — the dispatcher's post-merge review pipeline turned 13 review findings into merged fixes within the same window: #2408 (/sign token URL hygiene + reload), #2409 (favorites DTO reconcile), #2410 (iOS HEIC transcode), #2411 (create-schedule localization), #2412 (mobile AsyncStorage purge on session change), #2414 (screen-map init IdSchema validation), #2415 (packages i18n namespace deletion from cs/de/hu/pl/sk), #2384 (create-schedule report labels + backend error toast), #2376 (portal_webhook_events dedup net), plus #2373/#2375/#2378/#2379/#2380/#2381/#2382/#2383.
- **Churn hotspot resolution (2 PRs)** — #2395 (drop orphaned `packages`/`delegation` i18n from ppt-web/en.json), #2397 (make generated `sitemap.json` deterministic to stop churn) — both directly close prior-run refactor items.
- **Sitemap CI guard (1 PR)** — #2407 (guard against `src/data ↔ sitemap.json` drift, Closes #2404).
- **Stalled-review recovery (1 PR)** — #2396 (dx-stalled-review-pr-988-retry2 — @ppt/sitemap default export for CJS resolution).
- **Story-83 delivery (1 PR)** — #2398 (persist Booking.com push sync state on connection, gap-83-2 rate/availability push integration).

## Watch
- Stalled review: **none** — all 10 open PRs are ≤2 days old (oldest is #2332 draft at 2 days).
- Reverted: 0
- Churn hotspots this run (raw, after exclusions):
  - `frontend/packages/sitemap/src/json/sitemap.json` — **+868 lines** (runs_seen: 1 → 2 after this run; cumulative re-emit only, score_delta=0; already resolved by PR #2397 deterministic-output refactor from the prior run — no new work).
  - `backend/crates/db/tests/report_schedule_scheduler_rls_tests.rs` — **+225 lines** (first hotspot appearance; NEW RLS test file added by PR #2384/#2411 area — healthy test churn).
  - `mobile-native/shared/src/commonTest/kotlin/three/two/bit/ppt/reality/realtor/PortalListingsRepositoryTest.kt` — **+203 lines** (first hotspot appearance; NEW KMP test file paired with new PortalListingsRepository.kt — healthy new-feature coverage).
- Runner-ups in code churn (informational): `DocumentSignPage.tokenHygiene.test.tsx` (+189), `DecimalWireContractTest.kt` (+185), `DocumentUploadScreen.test.tsx` (+184), `en.json` (+182 — down from +3926 last run, PR #2395/#2415 completed the cleanup), `favorites/page.test.tsx` (+181), `government_portal_connection_cross_org_idor_tests.rs` (+160), `PortalListingsRepository.kt` (+158). Note: 6 of top-10 non-hotspot churn slots are **new test files** — the sprint's post-merge review loop is landing regression tests as fast as the fixes.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     None found — the two open drafts (#2332 age 2d, #2377 age 1d) don't yet carry APPROVED reviewDecision (both were in-flight per the dispatcher). -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json where reviewer_summary starts with "verdict=changes"
     AND (now - last_updated) > 24h AND status == "review".
     No verdict=changes threads currently exceed the 24h fix-round budget; the dispatcher fast-path handled the recent verdicts within-window (e.g. #2371 ktfmt lint in prior run). -->
- none

## Code review slice
- Segment reviewed: **skipped by the routine this run**
- Reason: the caller payload — `buffer-low: claimable=0/72 — refill planner (coverage 47/49, 2 partial)` — asks the routine to prioritize planner refill signals. The routine cursor already lags the dispatcher's Tier-1d reviews (all 8 segments were touched by the dispatcher between 2026-07-13 and 2026-07-17). Adding a routine-side pass this run would duplicate the dispatcher's coverage without new backlog surface.
- Findings: 0 (routine skipped) — dispatcher tier1d files remain at `.research/signals/2026-07-1[3-6]-*.json`
- Next segment (when routine resumes): **`api-core`** (last reviewed by routine cursor 2026-07-13 — tied for oldest with `reality-server`, `ppt-web-ui`; `mobile-rn` at 2026-07-14 next after).
- Skipped: yes — planner-refill priority + dispatcher coverage already ahead of routine cursor.

## Screen-map status
- Total `docs/screens/` files: 168 (106 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` still not seeded → 0)
- Drift signals this run: **0** — spot-check across 26 merged PRs confirmed every commit touching `frontend/apps/ppt-web/src/routes/**` (5 commits) or `frontend/apps/reality-web/src/app/**` (3 commits) ALSO touched the matching product's `docs/screens/{ppt,reality}/*.md`. Notable: PR #2414 explicitly landed IdSchema validation at the screen-map init write boundary.
- Orphan screens this run: 0 (spot-check on trigger files touched this run)
- Last `screen-map.yml` CI run on `main`: not queried this run (best-effort)

## Backlog deltas
- **New:**
  - [1] refactor · `refactor-churn-hotspots-report-schedule-rls-tests-2026-07-17` — churn hotspot: `backend/crates/db/tests/report_schedule_scheduler_rls_tests.rs` (+225 lines, first appearance) · confidence=high · analytical only — dropped-on-create.
  - [1] refactor · `refactor-churn-hotspots-portal-listings-repo-test-kt-2026-07-17` — churn hotspot: KMP `PortalListingsRepositoryTest.kt` (+203 lines, first appearance) · confidence=high · analytical only — dropped-on-create.
  - [1] dep-update · `closed-not-merged-pr-2385` — PR #2385 (dependabot dtolnay/rust-toolchain 1.94.1→1.100.0) closed unmerged; superseded by grouped `actions-minor-patch` promotion · dropped-on-create.
- **Bumped:** none
- **Decayed:** none (backlog has 0 non-terminal items — full convergence maintained since the 2026-07-16 run)
- **Dropped:** none (the 3 new items are dropped-on-create — analytical/observational, not actionable)
- **Notable non-creation:** `churn-hotspot-frontend/packages/sitemap/src/json/sitemap.json` re-emitted with `score_delta=0` (cumulative signal, already in `seen_signals`; existing item `refactor-churn-hotspots-sitemap-json-2026-07-16` is `status=done` — PR #2397 resolved it). Hotspot_history bumped `sitemap.json` runs_seen 1 → 2.

## Plans promoted
- none — no ready items (all 3 new backlog rows are score=1 and immediately `dropped`; no `open`/`ready` items exist to promote)

## Auto-fix
- `no candidate` — no signal met `confidence=high` + `score>=3` + allowlist this run. The three new rows are analytical churn/dep-noise vectors, not on the auto-fix allowlist.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — `mcp__github__push_files` can't inline the >64 KiB `assignments-archive.json`; fix: adopt PR + API-merge as the dispatcher's primary push path for the archive (proven this env — landed PR #1163 that way)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend `gc1-reconcile.sh` to auto-close any exact-id-terminal row (including status=failed)

## Goal violations
- **Planner refill (caller payload):** the caller reported `buffer-low: claimable=0/72 — refill planner (coverage 47/49, 2 partial)`. This run added 3 dropped-on-create rows only — the backlog still has 0 non-terminal items. Planner starvation is a **supply-side** issue: 26 merged PRs closed all recent backlog surface, and this quiet-signal window produced no new actionable vectors. **Recommendation:** next non-quiet run should invoke Phase 1.5 (`ppt-dev-review`, oldest cursor: `api-core`, `reality-server`, `ppt-web-ui` — all 2026-07-13) to surface fresh code-review-finding signals, or manually trigger the `deep` payload to open the window to a 30-day sweep.
- **G11 — phase-failure honesty:** Phase 1.5 and Phase 1.6 were skipped (not "failed"); `review_cursor`, `pm_cursor`, and `coverage_cursor` intentionally not advanced this run. Cursors that WOULD be affected by those phases remain at prior values (G16 gate is a no-op when Phase 1.6 didn't run this run).
- G2 — advance state: expected pass (last_pr_seen 2356 → 2416; last_issue_seen 2370 → 2413; last_commit_sha 1006a4c → c580202; seen_signals grew by 3; hotspot_history grew by 2; stats.runs 45 → 46).
- No G8/G9/G17 blocking violations expected — routine commit is `.research/` only, no secrets/hostnames/telegram tokens in added lines.

## Open questions
- **The planner is starved despite 26 merged PRs.** The dispatcher's dev-review + post-merge-review loop is producing GitHub issues, but they land as PRs within the same day, leaving `.research/backlog.json` with zero non-terminal items. Should the routine adopt one of: (a) automatic promotion of dispatcher-generated `code-review-finding` signals into `backlog.json` at open-status, so the planner sees them as claimable? (b) a mode where the routine invokes `ppt-dev-review` on every run instead of the daily cursor rotation, since the dispatcher is closing tickets faster than the cursor completes a segment cycle? Recording as an operator question — no autonomous action taken.
- **The 2 partial coverage stories (`84-1-s3-presigned-urls`, `84-2-esignature-email`)** are Epic-84 (E-Signature & Leases). Both have backend implementations shipped but likely missing frontend polish or E2E coverage. Should these be manually promoted to `open` backlog items to unblock the planner?

## Quiet day?
- **No** — 26 merged PRs, 3 new backlog vectors (dropped-on-create), 1 IDOR fix, 2 churn hotspots resolved by same-window refactor PRs. The dispatcher fast-path is metabolizing all actionable signal in real time; this brief documents the volume the routine observed.
