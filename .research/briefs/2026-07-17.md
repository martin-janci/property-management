# 2026-07-17

## Since last run
- Merged PRs: 23 (range #2371–#2412)
- Open PRs touched: 13 (all <25h old — 9 dependabot bumps opened yesterday + 3 dispatcher fast-path drafts opened today + PR #2377 draft at 18h)
- New / updated issues: 21 total touched (9 new: #2399–#2406, #2413); zero untriaged (all carry `security`, `follow-up`, or `from-merged-review` labels)
- Commits: 50 on `dev` (application code + heavy dispatcher artifact churn)
- Routine lag: 29h since last run (2026-07-16T03:04Z → 2026-07-17T10:00Z) — on schedule, below 36h warning
- Phases that failed: **phase-1.5-rotating-expert-review (skipped)**, **phase-1.6-project-management (skipped)** — both intentionally deferred (see rationale below)

## Shipped
23 merged PRs across ~24h. Highlights (grouped):
- **Reality-web / mobile favorites & DTO reconciliation (4 PRs)** — #2409 (favorites client → real reality-server DTO, fixes no-op optimistic toggle), #2382 (AC-5 favorite price-alert toggle + optimistic OFF), #2379 (Decimal-as-string decode-trap guard on reality-server sibling surfaces), #2373 (parseListingDetail() normalizer refactor).
- **Mobile RN quality (5 PRs)** — #2410 (iOS HEIC/HEIF → JPEG transcode in DocumentUploadScreen), #2412 (purge AsyncStorage caches on session change — cross-tenant), #2372 (mobile login clears whole query cache), #2383 (upload allow-list drift + photo MIME enforce), #2374 (deny-path regression for GET /ai/chat/escalated).
- **Reports / cron scheduling (3 PRs)** — #2411 (localize create-schedule form + error codes), #2384 (localize create-schedule labels), #2340 (scheduler worker RLS context).
- **Document signing / hygiene (2 PRs)** — #2408 (fix /sign reload regression + strip only `token` query param), #2378 (strip signing token from /sign URL + Referer).
- **Screen-map / churn hotspot follow-ups (3 PRs)** — #2395 (drop orphaned delegation/packages i18n namespaces), #2397 (make sitemap.json deterministic to stop churn), #2407 (guard src/data ↔ sitemap.json drift).
- **Portal webhook / booking / gap (2 PRs)** — #2376 (portal_webhook_events dedup net for idempotent inquiry retries), #2398 (persist Booking.com push sync state on connection).
- **Backend / KMP misc (4 PRs)** — #2380 (validate synthesized candidate id against IdSchema), #2381 (sync category enabled badge on optimistic trigger toggle), #2375 (/agencies/me resolves only active memberships), #2339 (org-scoped file_key + signed Content-Length on presigned document PUT), #2371 (wire KMP MyListings + ListingAnalytics stub), #2396 (@ppt/sitemap default export for CJS resolution).

## Watch
- Stalled review: **none** — all 13 open PRs are <25h old. No PR crosses the 7-day threshold.
- Reverted: 0
- Churn hotspots this run (raw, after code + docs-planning exclusions):
  - `frontend/packages/sitemap/src/json/sitemap.json` — **+4399/-192 = 4591 lines** (runs_seen 1 → 2 → `repeated-churn` fires first time; PR #2397 makes it deterministic — churn should stop next run)
  - `frontend/apps/ppt-web/messages/en.json` — **+3987/-119 = 4106 lines** (runs_seen 1 → 2 → `repeated-churn` fires; PR #2395 dropped orphaned namespaces yesterday)
  - `backend/servers/api-server/src/routes/reports.rs` — **+3329/-0 = 3329 lines** (runs_seen 2 → 3 → `repeated-churn` stacks; report-schedules feature area still landing via #2318/#2340/#2384/#2411 — candidate for module split)
- Runner-ups (informational only): `backend/crates/integrations/src/booking/mod.rs` (+3185), `backend/servers/api-server/src/routes/auth.rs` (+2950), `oauth_integration_tests.rs` (+2719), `financial.rs` (+2411), `buildings.rs` (+2347), `lease.rs` (+2303), `integrations/{sync,install,webhook}.rs` (+2293/+2283/+2250), `llm_document.rs` (+2225), `push_fanout.rs` (+2198), `vote.rs` (+2198), `api_ecosystem.rs` (+2184).
- Doc/planning churn (excluded from hotspot signals): `docs/functional-requirements.md` (+7116), `_bmad-output/epics.md` (+5514), `docs/superpowers/plans/2026-05-06-deploy-server-phase-0-1.md` (+4204), `docs/technical-design.md` (+3255), `docs/superpowers/plans/2026-05-07-screen-map-phase-2.md` (+3233) — legitimate planning artifact updates, not code instability.

## PRs stuck in draft despite approval
- none — the 3 dispatcher fast-path drafts (#2414/#2415/#2416) are new (opened 3–4h ago); no dependabot draft is approved-yet-stuck.

## PRs with verdict=changes, no fix-round progress in 24h
- none — dispatcher assignments show 3 active drafts (2414/2415/2416) all opened within the last 4h; no verdict=changes rows cross the 24h threshold.

## Code review slice
- Segment reviewed: **skipped by the routine (same catch-up rationale as 2026-07-16)**
- Reason: dispatcher's tier1d dev-review persists per-segment reports at `.research/signals/2026-07-15-*.json` and `.research/signals/2026-07-16-*.json`, keeping all 8 segments within ~2-4 days of the routine's cursor. Running the routine-side rotating review this early would be pure duplication.
- Findings: 0 (routine skipped); dispatcher tier1d coverage remains ahead
- Next segment (when routine resumes): `mobile-native-kmp` (last routine-reviewed 2026-07-12; dispatcher touched 2026-07-16)
- Skipped: yes — routine catch-up (dispatcher tier1d + Phase 2.5 post-merge review provided sustained coverage in the interim)

## Screen-map status
- Total `docs/screens/` files: 168 (106 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` still not seeded → 0)
- Drift signals this run: **5** — PR #2411 (ppt, reports.tsx), PR #2409 (reality, favorites/page.test.tsx), PR #2384 (ppt, reports.tsx), PR #2382 (reality, favorites/page.test.tsx), PR #2373 (reality, listings/[slug]/jsonLd.ts). All medium-confidence heuristics: 4 of 5 are test-file-only or internal refactors with no visible surface change; PR #2411/#2384 are i18n copy on an existing screen. Review candidates, not promotable plans.
- Orphan screens this run: 0 (union-scan not run — deferred to full-cycle run once buffer refill lands)
- Last `screen-map.yml` CI run on `main`: not queried this run

## Backlog deltas
- **New (9):**
  - [2] test-gap · `screen-map-drift-pr-2411-ppt` — PR #2411 touched routes/reports.tsx without docs update (heuristic)
  - [2] test-gap · `screen-map-drift-pr-2409-reality` — PR #2409 touched favorites/page.test.tsx without docs update (heuristic)
  - [2] test-gap · `screen-map-drift-pr-2384-ppt` — PR #2384 touched routes/reports.tsx without docs update (heuristic)
  - [2] test-gap · `screen-map-drift-pr-2382-reality` — PR #2382 touched favorites/page.test.tsx without docs update (heuristic, test-only)
  - [2] test-gap · `screen-map-drift-pr-2373-reality` — PR #2373 touched listings/[slug]/jsonLd without docs update (heuristic, refactor)
  - [1] refactor · `refactor-repeated-churn-en-json-2026-07-17` — en.json runs_seen 1→2; PR #2395 fix already landed
  - [1] refactor · `refactor-repeated-churn-sitemap-json-2026-07-17` — sitemap.json runs_seen 1→2; PR #2397 fix already landed
  - [1] refactor · `refactor-repeated-churn-reports-rs-2026-07-17` — reports.rs runs_seen 2→3; module-split candidate
  - [1] dep-update · `dep-update-closed-superseded-pr-2385` — PR #2385 (dependabot rust-toolchain bump) closed unmerged (auto-superseded)
- **Bumped:** none
- **Decayed:** none (backlog had 0 non-terminal items entering this run)
- **Dropped:** none
- **Notable non-creation:** the three `churn-hotspot-*` signals for en.json / sitemap.json / reports.rs re-emitted with `score_delta=0` (cumulative type, already in `seen_signals`). Hotspot_history bumped runs_seen for all three.

## Plans promoted
- none — no ready items. The 5 screen-map-drift rows (score 2) are heuristic review-candidates and stay below the 3-point promotion threshold; the 3 repeated-churn refactor rows (score 1) are similarly below. No `vector: security` items to trigger the fast-track.

## Auto-fix
- `no candidate` — no signal met `confidence=high` + `score≥3` + allowlist this run. The 5 screen-map-drift signals sit at confidence=medium (below the high bar); the 3 repeated-churn signals are refactor-vector at score 1; the closed-not-merged-pr signal is a dep-update no-op.

## Self-review findings
- open=17 (critical=4 or acknowledged, high=15 total / open subset, medium=15 total, low=4); new this run=0; acknowledged=19
- fp-mcp-push-cannot-inline-large-archive [recurrence=8] — `mcp__github__push_files` can't inline the >64 KiB `assignments-archive.json`; fix: adopt PR + API-merge as the dispatcher's primary push path for the archive (proven this env — landed PR #1163 that way)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend `gc1-reconcile.sh` to auto-close any exact-id-terminal row (including status=failed)
- fp-git-push-hard-assumed-blocked [recurrence=2] — dispatcher-prompt hard-assumes `git push` is proxy-403 blocked and defaults PUSH_METHOD=mcp; in THIS env direct `git push` works after fixing the commit-author email; fix: runtime probe at Phase 1, bypass the 64 KiB guard when PUSH_METHOD=git
- fp-planner-tier2-kick-payload-rejected-400 [recurrence=3] — Tier-2 upstream planner kick returned HTTP 400 with `claimable: Extra inputs are not permitted`; fix: dispatcher-prompt Phase 2.6 Tier-2 curl body must be `{"text":"..."}` only + `anthropic-version: 2023-06-01` header

## Open questions
- Trigger payload (`buffer-low: claimable=0/72 — coverage exhausted (47/49 stories done); refill planner / author new coverage`) is a dispatcher-space alert, not routine-space. The routine has no mechanism to refill dispatcher coverage; this must be authored by the planner or via `/ppt-project-management scan` in an on-demand local session. Recommend: operator runs the planner scan to author new coverage stories for the remaining backlog surface (or moves to a new epic).
- Should `screen-map.mobile/` be seeded so the drift/orphan heuristic can start firing on the mobile RN app? Currently zero mobile screen docs → mobile route churn is invisible to the drift detector.

## Quiet day?
- **No** — 23 merged PRs since last run, 9 new backlog vectors, 3 first-time `repeated-churn` fires (en.json + sitemap.json + reports.rs). The dispatcher fast-path handled the actionable work in real time.
