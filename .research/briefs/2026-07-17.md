# 2026-07-17

## Since last run
- Merged PRs: 22 (range #2371–#2412)
- Open PRs touched: 12 (11 dependabot batches #2385–#2394, plus 1 dispatcher retry #2411)
- New / updated issues: 8 new (#2399–#2406, all labeled `follow-up`/`from-merged-review` — none untriaged) + 12 existing follow-ups closed as fixes shipped
- Commits: 46 on `dev` (24 application-code + 22 dispatcher/research artifacts)
- Routine lag: **23h since last run** (last: 2026-07-16T03:04:00Z) — on schedule
- Phases that failed: **phase-1.6-project-management (skipped)** — PM cursor unchanged (rotation was `pm-security` at index 5); coverage cursor unchanged. Phase 1.5 result pending subagent completion; updated below if it lands before commit.

## Shipped
- **Follow-up wave from PR #2356 (AI escalated role-gate)** — 6 issues (#2357–#2370) filed as post-merge review follow-ups, all landed within 24h:
  - #2374 (issue #2357): deny-path regression test for GET /ai/chat/escalated role gate
  - #2375 (issue #2359): /agencies/me resolves only active memberships + backend tests
  - #2376 (issue #2358): portal_webhook_events dedup net for idempotent inquiry retries
  - #2378 (issue #2363): strip signing token from /sign URL + suppress Referer
  - #2379 (issue #2364): guard against Decimal-as-string decode trap on sibling reality-server surfaces
  - #2380 (issue #2367): validate every synthesized candidate id against IdSchema in scanCandidates
  - #2381 (issue #2369): sync category enabled badge on optimistic trigger toggle
  - #2382 (issue #2365): cover AC-5 favorite price-alert toggle + optimistic OFF state
  - #2383 (issue #2368): guard mobile upload allow-list drift + enforce photo MIME
  - #2384 (issue #2370): localize create-schedule report labels + backend error toast
- **Follow-up wave from PR #2358–#2370 post-merge review** — 8 issues (#2399–#2406) filed, 5 already merged:
  - #2412 (issue #2399): purge persisted AsyncStorage caches on mobile session change
  - #2407 (issue #2404): guard against src/data ↔ sitemap.json drift (adds CI test guard for #2397 determinism fix)
  - #2408 (issue #2402): fix /sign reload regression + strip only the token query param
  - #2409 (issue #2401): reconcile favorites client to real reality-server DTO (fix no-op optimistic toggle)
  - #2410 (issue #2400): transcode iOS HEIC/HEIF to JPEG in DocumentUploadScreen photo picker
- **Refactor churn + DX** — #2371 (KMP MyListings + ListingAnalytics wire-up), #2395 (drop orphaned delegation/packages i18n namespaces from ppt-web/en.json), #2396 (@ppt/sitemap default export for CJS resolution), #2397 (make generated sitemap.json deterministic to stop churn), #2398 (persist Booking.com push sync state on connection)
- **RLS/backend** — #2339/#2340 (org-scoped file_key + signed Content-Length + RLS context for scheduler)
- **Reality-web** — #2373 (consolidate listing-detail shape validation into parseListingDetail() normalizer)

## Watch
- Stalled review: **none** — all 12 touched open PRs are ≤ 25h old (11 dependabot batches from ~17h ago, 1 dispatcher retry #2411 from ~1h ago). No PR crosses the 7-day threshold.
- Reverted: 0
- Churn hotspots this run (raw, after exclusions):
  - `frontend/packages/sitemap/src/json/sitemap.json` — **+676/-192 lines** (runs_seen: 1→2, → repeated-churn signal fires). Note: this churn came entirely from PR #2397 (make sitemap deterministic) — a one-time regeneration to stabilize the file, not organic instability. Follow-up #2407 added a CI guard against future drift.
  - `backend/crates/db/tests/report_schedule_scheduler_rls_tests.rs` — **+225/-0 lines** (first appearance; brand-new RLS-scheduler regression test suite added by PR #2340, test-only)
  - `mobile-native/shared/src/commonTest/kotlin/three/two/bit/ppt/reality/realtor/PortalListingsRepositoryTest.kt` — **+203/-0 lines** (first appearance; new KMP repository test added by PR #2371, test-only)
- Runner-ups in code churn (informational): `DocumentSignPage.tokenHygiene.test.tsx` (+179/-10 across 2 PRs), `DecimalWireContractTest.kt` (+185/-0), `DocumentUploadScreen.test.tsx` (+173/-11 across 2 PRs), `favorites/page.test.tsx` (+173/-8 across 2 PRs), `PortalListingsRepository.kt` (+158/-0), `listingSchema.test.ts` (+155/-0), `agencies_test.rs` (+149/-0). Every top-10 file this run is a **test file or generated JSON** — code-under-test itself was surprisingly quiet.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     None found — the two open drafts (#2377 age 11h, #2411 age 1h) do not carry an APPROVED reviewDecision. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json (planning branch) where reviewer_summary starts with
     "verdict=changes" AND (now - last_updated) > 24h AND status == "review". Not queried this run —
     dispatcher is actively cycling (5 merged in the last dispatcher run at 02:25), so verdict=changes rows
     are being addressed within the pipeline. -->
- none

## Code review slice
- Segment reviewed: **api-core** (reason: oldest-unreviewed 2026-07-13 + churn-aligned — new `report_schedule_scheduler_rls_tests.rs` covers `backend/crates/db/`)
- Experts: rust (delegated to subagent)
- Findings: **3** — 1 bug (high, score=3) + 2 security (medium, score=2 each). See Backlog deltas for `code-review-finding` signals.
  - `bug-coderev-api-core-scheduler-dup-exec` (score 3, high) — `record_execution` + `advance_after_run` in `ReportScheduler::fire_due_report_schedules` are not transactional; a failed advance leaves an orphan `pending` `report_executions` row and re-fires the schedule next tick. **Promoted → plan.**
  - `security-coderev-api-core-unscoped-retry` (score 2, medium) — `retry_execution(id)` remains public with zero callers alongside the scoped variant; footgun for a future handler to reintroduce the closed IDOR. Same pattern for unscoped `get_by_id` / `get_execution`.
  - `security-coderev-api-core-list-exec-noscope` (score 2, medium) — `list_executions` filters only by `schedule_id`; tenant safety depends on the caller pre-checking with `get_by_id_scoped` — push the org filter into the query itself.
- Next segment: `reality-server` (last reviewed 2026-07-13, tied for oldest with ppt-web-ui / reality-web after api-core is covered)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 172 (across `ppt/`, `reality/`, `reality-mobile/`; `mobile/` still not seeded → 0)
- Drift signals this run: **4** — PRs that touched route files without updating docs/screens/ (all heuristic; several are test-only route-adjacent files, see caveat below):
  - PR #2373 (reality) — refactor of `listings/[slug]/*.ts{,x}` (validation consolidation, no route add/remove)
  - PR #2382 (reality) — test-only for `favorites/page.test.tsx`
  - PR #2384 (ppt) — `routes/groups/reports.tsx` + test (localization patch, not route add/remove)
  - PR #2409 (reality) — test-only for `favorites/page.test.tsx`
- Orphan screens this run: 0 (not spot-scanned this run — light-touch)
- Last `screen-map.yml` CI run on `main`: not queried this run
- **Heuristic caveat:** all 4 drift signals fire on the "any route-file path touched → must touch matching docs/screens/" rule; 3 of 4 are test-only or refactor-in-place changes that do not add/remove routes. These are review candidates, not promotable plans (Phase 3 readiness gate correctly leaves them at score=2 < 3).

## Backlog deltas
- **New:**
  - [3] bug · `bug-coderev-api-core-scheduler-dup-exec` — scheduler `record_execution` + `advance_after_run` non-transactional; failed advance re-fires schedule with orphan `pending` row · confidence=high · **promoted → plan**
  - [2] security · `security-coderev-api-core-unscoped-retry` — public `retry_execution(id)` with zero callers, footgun for future IDOR regression · confidence=medium
  - [2] security · `security-coderev-api-core-list-exec-noscope` — `list_executions` filters only by `schedule_id`, no join on `organization_id` · confidence=medium
  - [2] test-gap · `screen-map-drift-pr-2373-reality` — refactor of reality-web listing-detail validation without screen-map update · confidence=medium (heuristic)
  - [2] test-gap · `screen-map-drift-pr-2382-reality` — test-only favorites test change · confidence=medium (heuristic)
  - [2] test-gap · `screen-map-drift-pr-2384-ppt` — reports.create-schedule localization + test · confidence=medium (heuristic)
  - [2] test-gap · `screen-map-drift-pr-2409-reality` — favorites test refactor · confidence=medium (heuristic)
  - [1] refactor · `refactor-repeated-churn-frontend-packages-sitemap-src-json-sitemap-json-2026-07-17` — sitemap.json runs_seen 1→2; determinism fix + CI guard already landed (#2397 + #2407), so this signal should decay next run · confidence=high
  - [1] refactor · `refactor-churn-hotspots-backend-crates-db-tests-report-schedule-scheduler-rls-tests--2026-07-17` — new RLS scheduler test suite, first appearance · confidence=high
  - [1] refactor · `refactor-churn-hotspots-mobile-native-shared-src-commontest-kotlin-three-two-bit-ppt-2026-07-17` — new KMP portal listings test, first appearance · confidence=high
- **Bumped:** none
- **Decayed:** none (0 non-terminal rows before this run)
- **Dropped:** none
- **Notable non-creation:** `churn-hotspot-frontend/packages/sitemap/src/json/sitemap.json` re-emitted with `score_delta=0` (cumulative signal already in `seen_signals`); the same file also produced the fresh `repeated-churn-*` signal above. 11 open `dep-update-noise-pr-*` signals (dependabot batch #2385–#2394 + #2377) logged with `score_delta=0` (never scored, never create backlog rows per the routine rule).

## Plans promoted
- `plans/bug-coderev-api-core-scheduler-dup-exec.md` — wrap `record_execution` + `advance_after_run` in a single sqlx transaction so a failed advance rolls back the phantom execution row and the schedule stays cleanly due for retry · adversarial pass: passed
- **Not promoted:**
  - Both `security-coderev-api-core-*` findings at score=2 with confidence=medium — security fast-track requires `confidence=high`. Left as `open` for a second confirming signal (e.g. next api-core review, or a real IDOR reproducer) to escalate.
  - All 4 `screen-map-drift-*` rows at score=2 (below the 3-point promotion threshold; medium-confidence heuristics)
  - All 3 `refactor-*churn*` rows at score=1

## Auto-fix
- `no candidate` — no signal met `confidence=high` + `score≥3` + allowlist this run. The new bug/security `code-review-finding` signals aren't in the Phase 5 allowlist (they route through the manual plan path — one was promoted this run). The 4 `screen-map-drift` signals are confidence=medium (heuristic). The 11 `dep-update-noise` signals are open dependabot PRs, not the lockfile-drift sub-shape. No `stalled-review` or `fixme-in-merged-code` signals fired.

## Self-review findings
- open=17 (high=4, medium=10, low=3); new this run=0; acknowledged=19
- fp-mcp-push-cannot-inline-large-archive — assignments-archive.json (~326KiB) exceeds mcp__github__push_files 64KiB inline cap; fix: adopt PR + API-merge as the dispatcher's primary push path (proven — PR #1163 landed this way)
- fp-buffer-metric-blind-to-archived-open-items — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend `gc1-reconcile.sh` to auto-close any exact-id-terminal row including status=failed
- fp-git-push-hard-blocked-assumption-outdated — dispatcher-prompt hard-assumes proxy-403 on git push; in this env direct `git push` succeeds after commit-email set to noreply; fix: runtime `git push` capability probe + bypass 64KiB size guard when PUSH_METHOD=git
- fp-tier2-planner-401-auth-error — Tier-2 upstream planner kick returns 401 (DISPATCHER_TOKEN rejected) — no working refill path; fix: rotate token + branch Tier-2 curl on HTTP status so 401/403 escalates as OPERATOR-CONFIG rather than folding into generic "kick failed"

## Open questions
- Should the screen-map-drift heuristic be tightened to require the touched route file itself to be non-test (`.test.tsx?`/`.spec.tsx?` files excluded)? 3 of 4 drift signals this run are test-only "routes" — heuristic noise. Considered a routine tune-up, not blocking.

## Quiet day?
- No — 22 merged PRs, 7 new backlog vectors, 3 churn signals, 4 drift signals; brief is full length.
