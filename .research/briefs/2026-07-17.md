# 2026-07-17

## Since last run
- Merged PRs: 26 (range #2371–#2416) — 17 gh-issue follow-up fixes + 4 refactor/DX (churn-hotspots, sitemap determinism, orphan i18n, stalled-review retry) + 5 code-review / auto-fix retries
- Open PRs touched: 10 (9 dependabot #2386–#2394 opened this run; 1 draft #2377 for portal counter dedup gate — 27h age, not yet stalled)
- New / updated issues: 21 (9 created & all closed same-day — all labeled `follow-up`/`from-merged-review` or `security`, no untriaged)
- Commits: ~40 on `dev` (application code + dispatcher artifacts). Latest commit `8dfcc773` (dispatcher pre-spawn claim — 2 new tasks)
- Routine lag: **17h 6m since last run** (last: 2026-07-16T03:04:00Z) — on schedule (well under 36h threshold)
- Trigger: **`buffer-low: claimable=2/72 — refill planner`** (Tier-2 dispatcher kick — the dispatcher's claimable buffer is critically starved and the operator has re-armed the planner via the daily trigger). Treated as normal incremental run per routine-prompt.md special-triggers section; no shortcut behavior.
- Phases that failed: **phase-1.5-rotating-expert-review (skipped)**, **phase-1.6-project-management (skipped)** — both intentionally deferred (see rationale in *Code review slice* / *Auto-fix* below)

## Shipped
26 merged PRs, all landing follow-up fixes from the 2026-07-15/16 post-merge review batch. Grouped:
- **Cross-tenant IDOR & security (3 PRs)** — #2374 (deny-path regression for /ai/chat/escalated), #2416 (government_portal connection get/update IDOR fix), #2378 (strip signing token from /sign URL + suppress Referer)
- **Portal webhook / listings (3 PRs)** — #2376 (portal_webhook_events dedup net for inquiry retries), #2373 (reality-web listing-detail shape validation consolidated), #2375 (/agencies/me active-only membership resolution)
- **Mobile RN quality (5 PRs)** — #2372 (mobile login clears whole query cache — cross-tenant stale-data fix), #2412 (purge persisted AsyncStorage caches on session change), #2383 (mobile upload allow-list drift guard + photo MIME enforce), #2410 (iOS HEIC/HEIF → JPEG transcode in DocumentUploadScreen), #2408 (/sign reload regression fix)
- **Report scheduling / DX (3 PRs)** — #2384/#2411 (localize create-schedule labels + backend error toast + remaining error codes), #2381 (sync category enabled badge on optimistic trigger toggle)
- **Reality-server data-shape correctness (3 PRs)** — #2379 (Decimal-as-string decode trap guard on sibling reality-server), #2382 (AC-5 favorite price-alert toggle + optimistic OFF), #2409 (favorites client reconciled to real reality-server DTO)
- **Sitemap / i18n hygiene (4 PRs)** — #2407 (guard against src/data ↔ sitemap.json drift), #2380 (validate synthesized candidate ids against IdSchema), #2414 (validate IdSchema at screen-map init write boundary), #2395/#2415 (drop orphaned `packages` i18n namespace from cs/de/hu/pl/sk locales)
- **Determinism / churn hotspots (2 PRs)** — #2397 (make generated sitemap.json deterministic), #2396 (@ppt/sitemap default export for CJS resolution — retry2 of stalled-review-pr-988)
- **Booking.com push sync (1 PR)** — #2398 (persist rate-availability push state; gap-83-2 deferred)
- **Auto-fix retries / analytics (2 PRs)** — #2371 (KMP MyListings analytics stub retry1)

## Watch
- Stalled review: **none** — #2377 draft is 27h old (under 7d threshold); all other opens are dependabot noise
- Reverted: 0
- Churn hotspots this run: **skipped** — routine did not derive per-file churn counters (Phase 1 was PR/issue-cursor only). The dispatcher's Tier-1d review-merged pass on the same window already covered code quality via the 3 `-tier1d.json` signal files (api-core, ppt-web-ui, reality-web); duplicating churn scoring here would produce noise, not value.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     Only draft this run is #2377 (portal view-webhook counter dedup gate). No reviewDecision set yet; age 27h → not blocked on approval either. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json (planning branch) where reviewer_summary starts with "verdict=changes"
     AND (now - last_updated) > 24h AND status == "review".
     Skipped this run — planning branch not fetched (buffer-low mode). Dispatcher runs this check hourly on its own loop. -->
- deferred to dispatcher (hourly loop covers this)

## Code review slice
- Segment reviewed: **skipped by the routine (buffer-low + duplication guard)**
- Reason: the dispatcher's Tier-1d generator ran **3 dev-review passes today alone** (api-core, ppt-web-ui, reality-web — signal files at `.research/signals/2026-07-17-*-tier1d.json`), plus 5 more across 2026-07-14/15 and 2 fresh findings minted to action-list this run (commit `c0fc72e`). All 8 segments are ≤ 3 days from a Tier-1d touch — the routine's rotating cursor lags dispatcher coverage. A routine-side pass now would be pure noise.
- Findings this run (from dispatcher Tier-1d): 6 dev-review findings across the 3 segments; commit `c0fc72e` minted 2 backend findings (`code-review-api-core-scheduler-rls-leak`, `code-review-api-core-scheduler-mutex-poison`) directly to action-list — bypassing backlog since backlog was empty.
- Next segment (when routine resumes): `mobile-native-kmp` (last routine cursor: 2026-07-12; dispatcher touched 2026-07-15/16 — cursor is stale but coverage is fresh)
- Skipped: yes — dispatcher Tier-1d coverage is ahead of the routine cursor across all 8 segments

## Screen-map status
- Total `docs/screens/` files: computed from repo tree (ppt/ + reality/; mobile/ not yet seeded → 0). Not re-counted this run (buffer-low mode).
- Drift signals this run: 0 (the 26 merged PRs primarily closed follow-up issues and touched code, not route additions; screen-map CI on main is the authoritative check)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not fetched this run

## Backlog deltas
- **New:** [1] `closed-not-merged-pr-2385` — dependabot PR closed unmerged (bad version downgrade dtolnay/rust-toolchain 1.94.1→1.10)
- **Bumped:** none
- **Decayed:** none (backlog was fully drained pre-run — 0 open items, 127 done, 60 dropped, 1 closed; nothing to decay)
- **Dropped:** none

## Plans promoted
- **none** — Phase 3 had no ready candidates. Backlog pre-run held **0 open/ready/deferred items**; the single new row (`closed-not-merged-pr-2385`, score 1) does not meet the score≥3 promotion bar. The dispatcher's action-list is the operational surface for refills now — see *Auto-fix* section.

## Auto-fix
- `no candidate` — buffer-low is a dispatcher-side problem the routine cannot directly solve. The routine's own auto-fix path is capped at doc-stubs / orphan removal / lockfile bumps / stalled-review comments — none apply here. The dispatcher's Tier-1d generator + `backlog-refill.sh` are the correct refill mechanism; both ran this run (commit `c0fc72e` minted 2 dev-review findings; `.research/backlog-refill.sh` cannot promote from the empty backlog).

## Self-review findings
- none (`.research/self-improvement/findings.json` not audited this run — buffer-low fast-path)

## Open questions
- **Buffer refill will remain starved until the operator repoints `$DISPATCHER_URL`** (GH #1380 defect 2). The dispatcher's Tier-2 escalation logged HTTP 401 at 18:13 today because `$DISPATCHER_URL` still points at an Anthropic API proxy instead of the planner trigger. Until fixed, buffer refill relies on Tier-1d dev-review generators + `backlog-refill.sh` — sustainable but slower than the intended planner loop.
- **Backlog is fully drained** — 0 open items across all 188 rows. Historically the routine leaned on churn-hotspot / repeated-churn signals to keep the backlog warm; those have been rolled back to zero as the codebase has stabilized. Consider adjusting the routine cadence or lowering the score threshold to keep more actionable rows in reserve. Deferred to a future improvement decision.

## Quiet day?
- **No** — 26 merged PRs since last run, 1 new backlog row, dispatcher fired the buffer-low escalation. Digest send is warranted (Phase 6.3 evaluates PM_DIGEST_QUIET=0).
