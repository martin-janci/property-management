# 2026-06-22

## Since last run
- Merged PRs: **96** (#1440–#1661) — span 1440–1661, dominated by Epic 14 IoT delivery (FR71-FR75), Epic 13 dashboards, RLS-cleanup + `[FIX-NO-TEST]` cluster (24 fix-titled PRs flagged by an internal label, body did not enumerate test files; many likely DID add tests but text didn't flag — needs verification), 9 dependabot bot PRs.
- Open PRs touched: **5 newly active** — #1567/#1568 (dependabot, gradle + tower-http), #1634 (saved-search pagination, superseded by merged #1643), #1660/#1661 (auto-impl drafts opened today by dispatcher).
- New / updated issues: **41 touched** + several closed via post-merge cadence. Headline still open: **#1645 [bug]** dev backend CI red since #1625 (voting `pdf_report` 500s blocking all backend merges); worked around by #1656 (BIT-158 isolation) — root-cause confirmation pending.
- Commits on `dev`: 100+ since `4829015b → 8006065d` (multi-day window).
- Routine lag: **6d 0h since last run — `lag_warning` fired; stale_routine_alert tripped** (cloud cron likely paused). Dispatcher state on the planning branch shows assignments updated as recently as 2026-06-22T01:42 UTC, so the *dispatcher* loop is alive — only the research routine was stalled.
- Phases that failed: **none**.

## Shipped
- **Epic 14 IoT delivery (FR71-FR75):** #1644 (WebSocket sensor channel, Story 14.3, 404 LoC `iot.rs`), #1646 (sensor registry + register/edit + `docs/screens/ppt/iot-sensors.md`), #1652 (standalone alerts page `/iot/alerts` + `docs/screens/ppt/iot.md` + ack/resolve + i18n 6 langs). Screen-maps updated in lock-step — **no drift signal**.
- **Epic 13 dashboards (#1641, 994 LoC):** Sentiment + Predictive Maintenance pages (also touched `lazyRoutes.tsx`).
- **Security hardenings (manager-gate sweep):** #1606 `list_contacts` (PAP-281, last accounting handler without the gate), #1639/#1635 Airbnb linked-listing reads, #1602/#1601 MFA limiter + OTA manager-gate, #1632 portal-import per-agency scope. All `[FIX-NO-TEST]` tagged — test coverage proof needs spot-check (see pm-security read in `management/project-state.md`).
- **`[FIX-NO-TEST]` regression cluster (24 PRs):** #1619 infinite-scroll double-fetch (#1533), #1621 payment-matching relocation to ppt-web (#1521), #1629/#1631 financial decimal + auth header, #1643 saved-search alert pagination (#1627), #1656 voting PDF test isolation (BIT-158). Each is fix-titled; some include unit tests, some defer to follow-up coverage.
- **Vote report PDF generation (#1625)** — large, introduced the `pdf_report` test flake (→ #1645 + #1656 workaround).
- **CI infra:** #1655 (BIT-157) emits required check/test contexts on pure-frontend PRs.
- **Mobile iOS (#1659):** xcconfig docs + Expo per-env build wrapper (`scripts/build-mobile.sh`).
- **Dependabot (9 merged):** #1566, #1569–#1576. Logged as `dep-update-noise`, no backlog row.

## Watch
- **🚨 #1645 (open, [bug]) — dev backend CI red.** Voting `pdf_report` tests 500 on dev since PR #1625 merge. #1656 isolates the broken tests (BIT-158) so OTHER backend PRs can merge, but the root cause (PDF generator output divergence?) is not confirmed fixed. **Every backend `cargo test --workspace` is incomplete until this lands.**
- **Stale screen-map.yml on `main` (18d):** last run 2026-06-04. No release cut from `dev` → `main` in this window; releases stalled, not screen-map regressions per se. Verify whether the release cadence is intentional.
- **Reverts:** none this run.
- **Churn hotspots this run (top 3):**
  - `backend/servers/api-server/src/routes/financial.rs` (3 PRs, ~437 lines — first hotspot)
  - `frontend/apps/ppt-web/src/routes/lazyRoutes.tsx` (3 PRs — IoT + Epic 13 dashboards; lazy table)
  - `backend/crates/integrations/src/booking.rs` (2 PRs, ~463 LoC — **runs_seen=1→2; repeated-churn fired** for Booking.com OTA hot zone)
- **Stalled review:** none new with reviewDecision=`APPROVED + isDraft=true` — both open drafts (#1660 / #1661) are minutes-old auto-impl PRs by the dispatcher; no review window yet.
- **Closed-not-merged this run:** **#1647** (IoT sensor BIT-147 — superseded by merged #1646/#1653); **#1636** (reality-server portal-import authz dedup — superseded; IDOR fix via #1297, regression tests via #1603/#1620).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **ppt-web-core** (reason: oldest unreviewed at 17d stale; **churn-aligned** on `lazyRoutes.tsx` hotspot — Epic 13 + IoT additions land in this segment).
- Experts: frontend (light inline — no spawned subagent, given the 6-day catch-up budget).
- Findings: **0** new code-review findings. The lazyRoutes.tsx churn is structural (new-route registrations), not bug-shaped. The Epic 13 / IoT new-feature surface has not yet accumulated review-worthy debt; revisit next rotation.
- Next segment: **api-handlers** (last reviewed 2026-06-10 — 12d) or **mobile-native-kmp** (2026-06-06 — 16d) — whichever is older after this run advances `ppt-web-core` to 2026-06-22.
- Skipped: no — reviewed (light pass).

## Screen-map status
- Total `docs/screens/` files: **129** (ppt: 82, reality: 47, mobile: 0 — not yet seeded). +2 vs. 2026-06-16: `docs/screens/ppt/iot.md` (#1652) and `docs/screens/ppt/iot-sensors.md` (#1646).
- Drift signals this run: **0** — both #1646 and #1652 (the only ppt-web route additions touching `App.tsx`/`routes/**`) include matching `docs/screens/ppt/*.md` updates in the same PR.
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: success — but **18d stale** (no `main` pushes since 2026-06-04).

## Backlog deltas
- **New:**
  - [3] `unchecked-todo-pr-1606` — PR #1606 PAP-281 `list_contacts` manager-gate merged with unchecked real-PG test box (security).
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-financial-rs` — 3 PRs, ~437 lines.
  - [1] `churn-hotspot-frontend-apps-ppt-web-src-routes-lazyRoutes-tsx` — 3 PRs (IoT + dashboards).
  - [1] `closed-not-merged-pr-1647` — IoT sensor BIT-147 superseded by #1646/#1653 (triage).
  - [1] `closed-not-merged-pr-1636` — portal-import authz dedup superseded (triage).
- **Bumped:** [1→2] `churn-hotspot-backend-crates-integrations-src-booking-rs` — runs_seen 1→2 across 2026-06-12/2026-06-22 (`repeated-churn` +1; original `churn-hotspot` re-emitted with +0 per G4 cumulative carve-out).
- **Decayed:** 6 items −1 each (no signal in 14d): `bug-risky-churn-pr-963-api-main-rs` (2→1), `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` (2→1), `test-gap-screen-map-drift-pr-1085-reality` (2→1), `test-gap-screen-map-drift-pr-1100-ppt` (2→1), `code-review-mobile-native-kmp-deeplink-token-not-url-decoded` (2→1), `code-review-mobile-native-kmp-search-stale-response-race` (2→1).
- **Dropped:** 9 items decayed to 0 → dropped: `triage-issue-1151`, `churn-hotspot-mobile-native-listing-detail-kt`, `churn-hotspot-mobile-native-search-screen-kt`, `code-review-mobile-native-kmp-deeplink-android-bypasses-shared-router`, `refactor-churn-hotspot-mobile-announcements`, `refactor-churn-hotspot-mobile-announcements-test`, `refactor-churn-hotspot-mobile-documents`, `triage-dispatcher-mcp-push-large-file-issue-1014`, `triage-issue-951`.
- **Resolved:** none this run (the heavy resolution-check pass is deferred; spot-checks did not find verbatim ID matches in merged-PR bodies for the high-priority open items).

## Plans promoted
- **none this run.** 6 plans already in `plans/` (5 from prior runs at score 3 + the mobile-RN fake-submit plan from 2026-06-16); plan output is conservatively capped at 0 this run because (a) the existing ready plans are still in flight in the dispatcher (5 in-progress, 2 in review), (b) the catch-up budget is better spent on cursor advancement than on hand-crafting a new plan, and (c) the highest new-signal score is 3 (`unchecked-todo-pr-1606`) but its repro is a single-line CI invocation, not plan-worthy yet. Will revisit next run if the test stays unchecked.

## Auto-fix
- `no candidate` — `unchecked-todo-pr-1606` is **score 3 + high confidence** but its action shape (verify a test box) doesn't match the Phase 5 allowlist (no doc-stub / orphan-remove / lockfile-bump / FIXME-issue / stalled-review-ping fits); `dep-update-noise` cluster has no lockfile-drift sub-shape (consumers in lockstep); no stalled-review on an open PR ≥7d with no reviewDecision in this window.

## Self-review findings
- `.research/self-improvement/findings.json` absent — none

## Open questions
- **Issue #1645 root-cause:** the `[FIX-NO-TEST]` PR #1656 isolates the broken voting `pdf_report` tests so other backend PRs can merge, but is the divergence in PDF output deterministic / reproducible locally? If not, the test isolation is a permanent debt — needs an owner to dig before the routine declares this resolved.
- **`[FIX-NO-TEST]` cluster discipline:** 24 fix-titled PRs in 6 days tagged with this label. The label suggests an internal triage process noticed them — is there a follow-up workflow that converts them to coverage stories, or do they decay silently? If the latter, propose a `pm-qa` action-list sweep next rotation.
- **Routine cloud cron — was the 6-day stall intentional or broken?** If broken: who restarted it? If intentional: the brief now reflects a 6-day window instead of 1-day, and decay/dedup math compresses 6 runs into 1 — that's acceptable, but worth a one-line acknowledgement either way.

## Quiet day?
- **No.** 96 application-code PRs merged in 6-day window, 1 critical CI-blocker issue open and being patched around, **5 new backlog vectors + 1 bump + 9 drops + 6 decays**, 0 plans promoted (intentional — conservative on capacity), 0 code-review findings (light Phase 1.5), pm-security ran for the first time in 26 days (advanced pm_cursor 5→6, coverage_cursor 12→0 with 13 epics). `stats.runs` 40 → 41, `vectors_created` 173 → 178.
