# 2026-08-21

> **Buffer alert (from routine trigger payload):** dispatcher `claimable=6/72`; every open row is `mobile-native-kmp` (cloud-unlandable). **This run did NOT promote a new landable plan** — no candidate cleared the score≥3 + landable gate this window. Operator action: run `/ppt-research-trigger deep` or a targeted `ppt-dev-review` pass on `ppt-web-ui` / `ppt-web-core` / `reality-server` (last reviewed 2026-08-16..2026-08-19) to refill the landable buffer; alternatively re-classify one of the KMP items with a backend-only fix path so the dispatcher can claim it.

## Since last run
- Merged PRs: 11 (range #2799–#2817)
- Open PRs touched: 5 (all dependabot — see *Watch*)
- New / updated issues: 6 (all created + closed within the window — dispatcher-driven follow-ups: #2802, #2804, #2807, #2814, #2816; all closed after their linked PR merged)
- Commits: ~50 on `dev` (mix of merged fix commits + dispatcher chore commits)
- Routine lag: 2d 8h since last run (last_run_iso=2026-08-19T04:36Z; dispatcher continued to tick on its own branch — most recent dispatcher commit 2026-08-21T10:35Z)
- Phases that failed: none (Phase 1.5 and Phase 1.6 **skipped deliberately** to conserve tokens this run — cursors NOT advanced; see brief sections below)

## Shipped
- #2799 code-review-reality-server-create-review-body-length-uncapped: cap create_review body length — reality-server `agent_reviews.rs` now rejects `body > MAX_REVIEW_BODY_LEN` (5000). Test added.
- #2800 code-review-reality-server-submit-report-no-rate-limit: rate-limit anonymous POST /api/v1/reports per IP — reuses `inquiry_rate_limiters` + `client_ip_bucket`; two regression tests (per-IP quota + per-IP bucketing).
- #2801 code-review-reality-server-list-my-reports-total-page-length: list_my_reports returns true total row count — added COUNT query with same filters; regression test asserts `total` reflects full match count, not page length.
- #2803 gh-issue-2802: bound apt-get network waits in CI so a stalled mirror can't wedge test-shard — wrapped `apt-get update/install` in `timeout 90` + 3× retry + `ForceIPv4` for `test-shard` fonts and `check` ripgrep steps (backend.yml). Closes #2802.
- #2805 gh-issue-2804: scope RUSTSEC-2026-0258 h2 DoS ignore to the h2 0.3.x line — cargo-deny 0.19 can't scope advisory ignores, so added a `[bans]` half-range guard denying `h2 >=0.4.0, <0.4.16` (patched-floor). Closes #2804.
- #2806 code-review-api-handlers-voice-webhook-default-secret-retry2: voice webhook HMAC fails closed on empty secret — `verify_hmac_signature` now rejects empty `VOICE_WEBHOOK_SECRET`; test covers empty-var case.
- #2811 gh-issue-2807: route ai/voice link path through single-writer voice-device upsert — `link_voice_device` now calls `upsert_active_voice_device` (matches `oauth_token_exchange`); regression test `link_voice_device_relink_keeps_single_active_row` proves 201 on re-link + one active row + stable `device_id`. Closes #2807.
- #2812 fix(reality-server): stop discarding saved-search watermark-advance error — new `enqueue_and_advance_saved_search` commits enqueue + watermark atomically; 2 unit tests (`enqueue_and_advance_commits_both_writes`, `failed_watermark_advance_rolls_back_enqueue`).
- #2813 code-review-reality-server-inquiry-detail-empty-messages: return persisted inquiry messages — `get_inquiry` no longer hardcodes `messages: []`; new repo method `list_inquiry_messages`; regression test (`#[ignore]`, requires Postgres).
- #2815 gh-issue-2814: widen saved-search match_count to bigint — migration `00232_widen_portal_saved_searches_match_count.sql` + type change `i32 → i64`; new `watermark_advances_past_i32_max_boundary` regression proves the ceiling is gone. Closes #2814.
- #2817 gh-issue-2816: remove orphaned saved_searches int4 path — migration `00233_drop_orphan_saved_searches.sql` drops the dead `saved_searches` table + trigger + RLS policy; removes `PortalRepository` saved-search cluster (7 orphan methods) + `SavedSearch` model. Closes #2816.

## Watch
- **Dep-update noise (5 open dependabot PRs):**
  - #2673 ktor group (7 updates) — open 14 days, mobile-native-kmp, no reviewDecision
  - #2749 gradle-minor-patch group (5 updates) — open 7 days, mobile-native-kmp
  - #2808 androidx.compose:compose-bom 2026.06.01 → 2026.08.00 — mobile-native-kmp, fresh (opened today)
  - #2809 cargo-minor-patch group (9 updates) — backend, fresh (opened today)
  - #2810 npm-minor-patch group (32 updates) — frontend, fresh (opened today)
  - Two dependabot PRs closed unmerged as superseded: #2750 (cargo minor) → #2809, #2751 (npm minor) → #2810.
- **Churn hotspots this run (all test files added alongside fixes — expected, not risky):**
  - `backend/servers/reality-server/tests/suites/reports_tests.rs` (+253 across #2800/#2801)
  - `backend/crates/db/tests/suites/saved_search_watermark_advance_tests.rs` (added #2812 +191, re-edited #2815 +67/-9)
  - `backend/servers/api-server/tests/suites/ai_llm_workflow_batch5_tests.rs` (+88 #2811)
- Non-dependabot stalled PR: #2744 (dispatcher un-wedge oversize-archive push) — **draft** for 7 days; reviewer approved via comment (formal APPROVE blocked on self-PR). No routine action; owner (dispatcher/martin-janci) to mark ready-for-review.
- Reverts this run: **none**.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (planning branch `.research/management/assignments.json`: 0 rows match `status=review AND reviewer_summary starts with "verdict=changes"`)

## Code review slice
- Segment reviewed: **none — Phase 1.5 skipped this run** (routine token budget prioritised the mechanical + brief work; dispatcher already emitted Tier-1d dev-review signals for `api-core` today and for `reality-web`/`ppt-web-ui` in the last 24h — those cover the immediate churn).
- Experts: n/a
- Findings: 0 (from routine); dispatcher's daily Tier-1d generator ran independently — see dispatcher commits `079068fd09` (api-core), `c3c24f5ec8` (ppt-web-ui), `6c4910a195` (reality-web), `fd7659bef6` (ppt-web-core) since 2026-08-19.
- Next segment (oldest unreviewed after this run): tie between `ppt-web-ui` and `mobile-native-kmp` at 2026-08-16 — pick `ppt-web-ui` next (landable, addresses buffer-low).
- Skipped: **yes — token-budget triage; cursor NOT advanced.**

## Screen-map status
- Total `docs/screens/` files: 153 (106 in `ppt/`, 47 in `reality/`; `mobile/` not yet seeded → 0)
- Drift signals this run: 0 (no merged PR touched a route trigger file; every merged PR was backend-only)
- Orphan screens this run: 0 (no route changes → skipped orphan spot-check per Phase 1 scope rule)
- Last `screen-map.yml` CI run on `main`: not queried this run (no docs/screens changes to gate)

## Backlog deltas
- **New:** none (no signal type this run created a fresh backlog row — churn-hotspots hit test files not tied to any open item; dep-update-noise is 0-score; lag_warning is 0-score)
- **Bumped:** none (churn-hotspot signals fired on 3 test files that don't map to any open backlog item)
- **Decayed:** none (no open item has `updated_at <= 2026-08-07`)
- **Dropped:** none
- **Resolved this run:** none — all 5 open items (mobile-native-kmp) evidence patterns (`@SerialName("page_size")`, `HttpTimeout`, `catch(e: Exception)`) still present in code; no merged PR body cites any of the 5 IDs verbatim.

## Plans promoted
- **none** — see the top *Buffer alert*. Existing plan `plans/code-review-mobile-native-kmp-inquiries-response-contract.md` is still open in backlog (status=open, score=3) despite being fully drafted; it is NOT cloud-landable per the dispatcher's classifier so it does not relieve buffer pressure. Adversarial pass: n/a (no promotion).

## Auto-fix
- `no candidate` — no signal met the certainty bar this run. `screen-map-drift`/`screen-map-orphan` never fired (no route touches). `fixme-in-merged-code` never fired (no new TODO/FIXME added in merged diffs). `dep-update-noise` lockfile-drift path did not fire (all 5 open dependabot PRs update lockfile + manifest atomically). `stalled-review` skipped (all >7d open PRs are dependabot, excluded from this path).

## Self-review findings
- open=34 (high=7, medium=24, low=3); new this run=0; acknowledged=19
- fp-1 [recurrence=15] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked and derives fallback path from that; fix: rewrite Phase-6 push contract to try direct push first, fall back only on real 403
- fp-2 [recurrence=15] — Phase 6 archive-move produced a 198KB assignments-archive.json; mcp__github__push_files size-guard trips; fix: shard assignments-archive when >150KB (durable sharded write) rather than relying on one-shot push
- fp-3 [recurrence=8] — Buffer drained (claimable=0/72, backlog 166 done/81 dropped/1 closed = 0 open landable); fix: dispatcher must escalate to Tier-2 planner kick when buffer<10% of concurrency cap, not only on hard-zero
- fp-4 [recurrence=5] — action-list.json holds 31 status=in-progress rows but assignments.json has only a subset; fix: reconciliation pass on every Phase 2 to prune orphan in-progress rows
- fp-5 [recurrence=3] — 5 in-progress rows claimed 2026-08-01T20:10Z produced ZERO branches on origin; fix: heartbeat-then-terminate stalled implementers after 4h no-branch
- … and 2 more (see findings.json)

## Open questions
- Buffer-low remediation strategy: (a) invoke `ppt-dev-review` on `ppt-web-ui` / `reality-server` this session (adds ~15k tokens for one segment, up to 3 landable findings), or (b) hand-carve a landable follow-up plan from the KMP inquiries-response mismatch by fixing the reality-server side to alias `page_size` alongside `limit` (backend-only, cloud-landable). Deferred to operator — routine did not act autonomously.

## Quiet day?
- **No.** 11 PRs merged, 3 churn hotspots recorded, 5 open dependabot PRs, 6 issues closed. Not quiet.
