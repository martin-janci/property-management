# 2026-08-21

## Since last run
- Merged PRs: 10 (range #2799–#2815)
- Open PRs touched: 5 (all dependabot: #2673, #2749, #2808, #2809, #2810)
- New / updated issues: 5 closed follow-ups (#2794, #2797, #2802, #2804, #2807, #2814 — all resolved by merged PRs above)
- Commits: 50 on `dev` (10 code merges + 40 dispatcher/research chore commits)
- Routine lag: 2d 0h since last run (48h > 36h threshold → `lag_warning` fired)
- Phases that failed: none

## Shipped
- #2799 code-review-reality-server-create-review-body-length-uncapped — cap `create_review` body length (only rating was validated; multi-MiB blobs possible) — reality-server hygiene.
- #2800 code-review-reality-server-submit-report-no-rate-limit — rate-limit anonymous `POST /api/v1/reports` per IP (unbounded report flood surface closed).
- #2801 code-review-reality-server-list-my-reports-total-page-length — `list_my_reports` now returns the true total row count (was returning page length → pagination broken past page 1).
- #2803 gh-issue-2802 — bound apt-get network waits in CI so a stalled mirror can't wedge the `test-shard` job (unwedges backend PR merge pipeline).
- #2805 gh-issue-2804 — scope RUSTSEC-2026-0258 (h2 empty-DATA-frame DoS) advisory ignore to the h2 0.3.x line only, so a future in-range regression on inbound-server `h2 0.4.x` still trips `cargo-deny` (partial mitigation; h2 0.3.x still on tree via AWS SDK).
- #2806 code-review-api-handlers-voice-webhook-default-secret-retry2 — voice-webhook HMAC now fails closed on **empty** `VOICE_WEBHOOK_SECRET` (previously accepted empty string as a valid HMAC key, matching sibling receivers).
- #2811 gh-issue-2807 — route `ai/voice` link path through the same single-writer `upsert_active_voice_device` the OAuth webhook uses (closes divergence between the two link paths that would have thrown 500 on re-link after migration 00231).
- #2812 fix(reality-server) — stop discarding saved-search watermark-advance error (`let _ =` swallowed it; now atomic enqueue+advance with error propagation).
- #2813 code-review-reality-server-inquiry-detail-empty-messages — inquiry-detail endpoint returns persisted messages (was hardcoded `messages: []`).
- #2815 gh-issue-2814 — widen `portal_saved_searches.match_count` to `bigint` (was `int4`, could wedge alerts forever once counter approached `i32::MAX`).

## Watch
- Stalled review: #2673 (dependabot ktor group, 14d idle) — dep-update-noise, log-only per rule.
- Reverted: none this window.
- Churn hotspots: `backend/servers/api-server/tests/suites/oauth_integration_tests.rs` (+3427 lines this run, runs_seen=1), `backend/servers/api-server/src/routes/voice_webhooks.rs` (+3033 lines, runs_seen bumped 2→3 — repeated-churn fired), `backend/servers/api-server/src/routes/integrations/webhook.rs` (+2799 lines, runs_seen bumped 1→2).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **ppt-web-ui** (reason: oldest-unreviewed after churn-aligned `api-core` was already covered by today's dispatcher tier-1d)
- Experts: frontend (React 19 / TS / a11y lens)
- Findings: 3 (see Backlog deltas for `code-review-ppt-web-ui-*` signals — 1 high, 1 medium, 1 low, all `bug` vector)
- Next segment: **mobile-native-kmp** or **ppt-web-core** (both last reviewed 2026-08-16/17); `mobile-native-kmp` currently saturated with 5 open findings so it would be skipped
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` = 106, `reality/` = 47, `mobile/` = 0 — not yet seeded)
- Drift signals this run: 0 (no merged PR touched `frontend/apps/{ppt-web,reality-web}` route files without also updating the matching product's screen docs)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (`gh` per-workflow lookup skipped for token budget)

## Backlog deltas
- **New:** [3] `code-review-ppt-web-ui-authcallback-double-decode` — ppt-web AuthCallbackPage double-decodes error_description → URIError leaves SSO callback stuck on spinner
- **New:** [2] `code-review-ppt-web-ui-toast-role-alert-for-all-types` — Toast forces role="alert" on every variant, over-interrupting screen readers
- **New:** [1] `code-review-ppt-web-ui-confirmationdialog-hardcoded-processing` — ConfirmationDialog leaks hardcoded English 'Processing...' bypassing i18n
- **New:** [1] `churn-hotspot-...-tests-suites-oauth-integration-tests-rs-2026-08-21` — 3427-line test-suite append into `tests/suites/`
- **New:** [1] `repeated-churn-...voice_webhooks.rs-2026-08-21` — runs_seen bumped 2→3 (instability tracking only)
- **New:** [2] `unchecked-todo-pr-2806` — template test-plan checkboxes left unchecked on a security fix (kept `confidence: low` since checkboxes are template scaffolding, not code TODOs — can't auto-promote)
- **Bumped:** none this run
- **Decayed:** none (all 5 open items updated within the 14d window)
- **Dropped:** none

## Plans promoted
- `plans/code-review-ppt-web-ui-authcallback-double-decode.md` — drop the redundant `decodeURIComponent(...)` around `error_description` in `AuthCallbackPage.tsx:100`; add Vitest coverage for the `%25` payload · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review`) this run. The 3 code-review findings this run are code-fix candidates that Phase 5's allowlist explicitly excludes — they route through the manual implementer path via `plans/`. The dependabot PRs open on the repo have no matching `package.json` / `Cargo.toml` lockfile drift shape.

## Self-review findings
- open=34 (high=7, medium=24, low=3); new this run=0; acknowledged=0
- fp-<no-id> [recurrence=15] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked; in this env direct `git push origin dev` succeeds after commit-author-email fix; fix: replace hard-coded proxy-403 assumption with a runtime `git push` capability probe in dispatcher Phase 1, then bypass the 64KiB mcp-push-size-guard when PUSH_METHOD=git.
- fp-<no-id> [recurrence=15] — Phase 6 archive-move produced 198KB assignments-archive.json that mcp__github__push_files can't inline; fix: adopt the PROVEN workaround as primary — push dispatcher-state to a new branch, open PR via `mcp__github__create_pull_request`, then `mcp__github__merge_pull_request` (squash) so server-side merge carries any file size.
- fp-<no-id> [recurrence=8] — buffer drains to 0 while Tier-1d keeps manufacturing dev-review signals nobody promotes; fix: add `.research/signals-promote.sh` for Tier-1b to consume unconsumed `*tier1d*` signals into scored backlog rows, or gate Tier-1d when unconsumed threshold exceeds a ceiling; add self-test T3x for the ceiling.
- fp-<no-id> [recurrence=5] — action-list.json holds 31 in-progress rows but assignments.json has only 3 live; twin status inverted; fix: extend `gc1-reconcile.sh` to sync twin status on claim + reclaim, plus self-test T32 asserting bijection.
- fp-<no-id> [recurrence=3] — 5 in-progress rows produced zero branches on origin across two implementer generations; fix: on reclaim, if `reclaim_attempts>=1` AND still branchless AND elapsed>Nh, escalate to `status=failed` (sandbox-no-branch-after-Ngen) instead of another grace window.
- … and 2 more (see findings.json)

## Open questions
- None new this run. `code-review-mobile-native-kmp-inquiries-response-contract` has been at score=3 / plan-attached / status=open since 2026-08-16 — worth flipping to `ready` on next dispatcher pass or manually so the implementer picks it up (routine leaves the row alone since no fresh signal touched it).

## Quiet day?
- No (10 PRs shipped, 6 new backlog vectors, 1 plan promoted, 3 code-review findings, PM digest ready)
