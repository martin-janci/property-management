# 2026-08-21

## Since last run
- Merged PRs: 11 (range #2799–#2817)
- Open PRs touched: 5 (all dependabot: #2808, #2809, #2810, #2749, #2673)
- New / updated issues: 6 (all labelled, all closed — no triage rows created)
- Commits: 50 on `dev` (11 feature squashes + dispatcher state commits)
- Routine lag: 2d 12h since last run (60h — over the 36h threshold, under the 3d stale-routine line)
- Phases that failed: none
- Phases skipped: phase-1.6-project-management (deliberate — run budget went to the buffer refill; `pm_cursor` and `coverage_cursor` deliberately NOT advanced, so G16 is a no-op this run)

**Trigger payload:** `buffer-low: claimable=9/72 — refill planner (mobile-native-gated items dominate; need cloud-landable vectors)`. This run was steered accordingly: every review segment picked was cloud-landable, and `mobile-native-kmp` was deliberately excluded from both review and promotion.

## Shipped
- #2817 gh-issue-2816: remove orphaned `saved_searches` int4 path — deletes the parallel int4 code path left by #2815's overflow hardening
- #2815 widen `portal_saved_searches.match_count` to bigint (migration 00232) — with watermark-advance tests
- #2813 reality-server inquiry-detail returns persisted messages instead of hardcoded `[]`
- #2812 fix(reality-server): stop discarding the saved-search watermark-advance error — adds `saved_search_watermark_advance_tests.rs`
- #2811 gh-issue-2807: route the ai/voice link path through the single-writer voice-device upsert
- #2806 voice webhook HMAC fails closed on an empty secret (see *Watch* — this hardened unreachable code)
- #2805 scope the RUSTSEC-2026-0258 h2 DoS ignore to the h2 0.3.x line
- #2803 gh-issue-2802: bound apt-get network waits in CI so a stalled mirror can't wedge `test-shard`
- #2801 `list_my_reports` returns the true total row count
- #2800 rate-limit anonymous `POST /api/v1/reports` per IP
- #2799 cap `create_review` body length

## Watch
- **The pipeline's real bottleneck is not a shortage of work — it is 215 stranded findings.** `.research/signals/*tier1d*.json` holds 373 distinct `code-review-finding` signals across 168 files. 260 of them had never entered `backlog.json`, `action-list.json`, or `assignments.json` in any form, while the dispatcher reported `honest_claimable=6/72` and could claim nothing. The generator has been manufacturing findings far faster than anything drained them. This run promoted the top 60 (see *Backlog deltas*); 44 remain eligible for the next refill rounds. This is the finding logged at `recurrence=8` in `.research/self-improvement/findings.json`, now measured.
- **`voice_webhooks.rs` churn is being spent on unreachable code.** It is this run's #1 hotspot (4 commits, 3033 lines, `runs_seen=3`), and PR #2806 hardened its HMAC path — but both `alexa_webhook` and `google_actions_webhook` take `RlsConnection` as an *extractor*, which transitively requires a Property Management JWT. Amazon and Google send none, so every genuine delivery is rejected `401` before any of that hardening runs. Re-traced end-to-end during this run (route mount → router layers → extractor chain). Promoted as a plan.
- **PR #2744 has been parked in draft for 8 days** with `mergeable_state=clean` and a reviewer `verdict=approve`. Formal approval is blocked because it is a self-PR, so only a human can mark it ready and merge. It is the single active WIP assignment, so that slot is occupied until someone acts. Filed as `needs-human-judgement`.
- Churn hotspots: `backend/servers/api-server/src/routes/voice_webhooks.rs` (3033 lines / 4 commits, runs_seen=3); `backend/crates/db/src/repositories/llm_document.rs` (2396 / 3 commits, runs_seen=1); `backend/servers/reality-server/src/routes/inquiries.rs` (999 / 3 commits, runs_seen=1)
- Risky-churn: **checked and not emitted.** The PR-file-list heuristic flagged #2806 as a test-free fix touching the top hotspot, but reading the diff shows it does add assertions inside the file's inline `#[cfg(test)]` module. File-name-based test detection is wrong for Rust inline test modules.
- Reverted: none this run.

## PRs stuck in draft despite approval
- #2744 fix(dispatcher): un-wedge oversize-archive push (#1162) + close retry-remint cross-id ghost (#2460) — last update 8d ago (age=08:12). `reviewDecision` is null rather than `APPROVED` because the approve verdict was posted as a comment (self-PR), so the standard query misses it; it is the #539-style merge-gate class all the same.

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segments reviewed: **api-handlers, ppt-web-core, mobile-rn, reality-web** (4 instead of the usual 1 — the buffer-low trigger asked for cloud-landable vectors, and one segment yielding ≤3 findings could not move a buffer sitting at 6/72)
- Reason: churn-aligned (api-handlers, via `voice_webhooks.rs`) + oldest-unreviewed among cloud-landable (ppt-web-core, mobile-rn, reality-web). `ppt-web-ui` was skipped as saturated (3 open findings already); `mobile-native-kmp` was skipped deliberately as structurally unlandable in the cloud runner.
- Experts: rust + security (api-handlers), frontend (ppt-web-core, reality-web), frontend + tester (mobile-rn)
- Findings: 12 (3 per segment)
- Next segment: `api-core` (last reviewed 2026-08-21 by the dispatcher's tier-1d pass) — every segment is now current
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` 106 + `reality/` 47; `mobile/` not yet seeded → 0)
- Drift signals this run: 0 (no merged PR touched a frontend route file — all 11 were backend/CI)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (no screen-doc or route change to gate)

## Backlog deltas
- **New (stranded-signal promotion):** 60 rows recovered from `.research/signals/*tier1d*.json` — 11 at score 3, 49 at score 2, spread across api-core (11), ppt-web-ui (12), reality-web (11), reality-server (10), api-handlers (8), ppt-web-core (7), mobile-rn (1). Gated on: id/stem absent from backlog + action-list + assignments (incl. both archives), not `mobile-native`/KMP, and at least one cited file still present on disk (13 dropped as file-gone).
- **New (this run's review):** [3] Alexa/Google voice webhooks unreachable — `RlsConnection` extractor 401s every real delivery · [3] ppt-web never wires 401 → `refreshToken` · [2] `/dashboard/manager` route has no `ProtectedRoute` and the page has no role check · [2] Home CTA sends `org_admin`/`property_manager` to the resident dashboard · [2] `voice_webhooks` OAuth exchange/refresh leak raw upstream provider errors · [2] mobile `MeterReadingScreen` reports success while online though nothing is POSTed · [2] mobile Settings/WidgetSettings deep links silently land on Dashboard · [2] reality-web `/realtor/[id]` renders `MOCK_AGENT` for every realtor · [2] reality-web sitemap hardcodes `en` as the unprefixed locale but `defaultLocale` is `sk` · [1] `alexa_webhook` leaves the RLS connection to `Drop` on five error exits · [1] mobile hardcoded English on six i18n'd list screens · [1] reality-web price-map hardcodes Slovak for all six locales
- **New (churn):** [1] `llm_document.rs` hotspot · [1] reality-server `inquiries.rs` hotspot
- **New (process):** `dx-stalled-review-pr-2744` — filed `needs-human-judgement`
- **Dropped (deduplicated):** 10 rows collapsed as `[SUPERSEDED by …]`. The rotating review re-finds the same defect under a fresh slug on later passes: 4 ids for the ppt-web 401-unwired defect, 3 for the moderation `window.prompt` cast, and one pair each for the voice-webhook extractor gate, the reality-web inquiry-cancel silent fail, the create-dispute stub, and the SSO negative-cache poisoning. **This duplication is itself a defect in the generator** — it should dedup against `backlog.json` before emitting, or the same finding keeps re-entering the funnel.
- **Decayed:** none (no `open` row was older than 14 days)

## Plans promoted
- `plans/code-review-api-handlers-voice-webhook-jwt-gated.md` — drop the `RlsConnection` extractor from `alexa_webhook` / `google_actions_webhook` and bind RLS from the signature-resolved device, so real Alexa/Google deliveries reach the handler at all · adversarial pass: passed (the extractor→`ValidatedTenantExtractor`→`AuthUser`→401 chain and the absence of any auth-exempting router layer were both re-read from source during this run, not taken from the reviewer's report)
- `plans/code-review-ppt-web-core-no-401-refresh-wiring.md` — pass `onUnauthorized` into `configureApiClient` and check `exp` on session restore, so an expired access token recovers instead of bricking the session · adversarial pass: passed (independently confirmed `configureApiClient({ getToken })` at AuthContext.tsx:337, the guarded-on-null 401 branch at api.ts:219, the comment-without-a-check restore branch at AuthContext.tsx:412, and zero `refreshToken` call sites outside the context and its tests)

## Buffer refill
- `honest_claimable` 6 → 30 (floor 36, target 72, ceiling 120). One `backlog-refill.sh --apply` round promoted 24 rows into `action-list.json` (20 → 44 items, 29 KB — comfortably inside the 64 KiB MCP inline ceiling).
- A second round was deliberately **not** run: the script's 24-per-run cap exists to protect that ceiling (issue #1014), and 44 eligible backlog rows now sit ready for the dispatcher's own Tier-1b refill on its next pass, which is the designed mechanism.
- 215 further stranded findings remain in `.research/signals/`, unpromoted and untriaged.

## Concurrency incident (this run)
- **This run's first commit was lost to a race with the dispatcher and had to be re-landed.** Commit `f56342aa` was pushed to the session branch at 16:45 UTC and `research-land.yml` reported success, but the dispatcher pushed `7d496e63` / `ff997e51` / `55341a9a` to `dev` in the same window (3 tasks claimed, PRs #2818–#2820 captured) and `dev` ended up without the brief, the two plans, or the 75 new backlog rows.
- **Recovered by rebasing rather than re-pushing.** The routine's output was re-applied on top of the dispatcher's current `dev`, and `backlog-refill.sh --apply` was re-run against the dispatcher's *live* `action-list.json` instead of the stale copy — so the 3 tasks it claimed and the 4 rows now in `assignments.json` are preserved intact. Verified before commit: `assignments.json` still holds all 4 live rows, and the dispatcher's own `signals/2026-08-21-ppt-web-core-tier1d.json` is untouched.
- **BLOCKER — this brief could not be landed on `dev`; it is sitting on the session branch `claude/wonderful-gates-fv8pvt`.** The second landing attempt (`research-land.yml` run 645) failed with `! [remote rejected] __dev_landing -> dev (protected branch hook declined)` / `2 of 2 required status checks are expected`. The workflow's own header comment asserts *"GITHUB_TOKEN can push to dev (unprotected — ruleset 11341260 covers only main + release/*)"* — **that assumption is now false**: `dev` has acquired branch protection with required status checks. The workflow also misreports the rejection as `dev moved under us — retrying`, burning all 5 attempts on an error no retry can clear.
- **This will wedge the dispatcher too.** The dispatcher lands its state commits on `dev` by the same direct-push route; every future state commit will hit the same rejection. Operator action needed: either exempt the `github-actions[bot]` push from the `dev` ruleset, or switch `research-land.yml` (and the dispatcher's Phase 6) to the branch → PR → API-merge path already documented in `.research/self-improvement/findings.json` at `recurrence=15`.
- **The routine and the dispatcher have no lockfile between them** (`routine-prompt.md` → *Operational assumptions* → *Concurrent runs* assumes a single writer, which is true per-loop but not across the two loops). The dispatcher runs roughly every 2h; a long routine run overlaps it by construction. Worth a real mutex — or at minimum, the routine re-basing onto `origin/dev` immediately before staging, which is what had to be done by hand here.

## Auto-fix
- `no candidate` — no allowlisted signal met confidence=high **and** backlog score ≥3 this run. `stalled-review-pr-2744` is the only allowlist match and it scores 1, below the bar; it also needs a human decision rather than a bot ping, so it was filed as `needs-human-judgement` instead.

## Self-review findings
- open=34 (high=7, medium=24, low=3); new this run=0; acknowledged=19
- fp-null [recurrence=15] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked, so `PUSH_METHOD` defaults to MCP `push_files` and the 64 KiB guard blocks pushes the environment actually permits; fix: runtime push-capability probe at Phase 1, bypass the size guard when `PUSH_METHOD=git`
- fp-null [recurrence=15] — Phase 6 archive-move produces an archive too large for `mcp__github__push_files` to inline; fix: push to a new branch, open a PR, merge via API (server-side merge carries any size)
- fp-null [recurrence=8] — tier-1d manufactures review signals faster than the planner drains them; none become claimable rows. **This run measured it at 215 stranded findings and drained 60.** The proposed `signals-promote.sh` is still the right durable fix — this run did it by hand.
- fp-null [recurrence=5] — `action-list.json` in-progress rows and `assignments.json` live rows are inverted/decoupled; fix: extend `gc1-reconcile.sh` to restore the bijection, add self-test T32
- fp-null [recurrence=3] — implementer rows claimed with zero branches on origin across two generations still hold `active` slots; fix: escalate to `failed` on reclaim-and-still-branchless, add T33
- … and 2 more (see findings.json)

## Open questions
- **Are the 215 remaining stranded findings worth draining, or should the generator be gated instead?** Draining them all would queue roughly a year of implementer work at the current merge rate; gating tier-1d until the backlog falls below a threshold would stop the pile growing. This run drained the top 60 by score, but the policy call is the user's.
- **Should the stranded findings be re-validated before promotion?** They were emitted between June and August; this run's only staleness gate was "at least one cited file still exists". Some may already be fixed. The implementer will discover that per-task (the `already-landed` escape hatch exists), but a cheap batch grep pass would waste fewer slots.
- **PR #2744 needs a human** to mark it ready-for-review and merge; nothing in the automation can clear a self-PR approval gate.

## Quiet day?
- No
