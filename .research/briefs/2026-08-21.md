# 2026-08-21

## Since last run
- Merged PRs: 10 (range #2799–#2815)
- Open PRs touched: 3 (draft #2817 opened today; #2810 npm-minor-patch, #2809 cargo-minor-patch — both dependabot)
- New / updated issues: 7 (all `follow-up`/`from-merged-review`, all already closed by the paired PRs)
- Commits: ~35 on `dev` since last run (mostly dispatcher chore-commits; 4 substantive PRs shipped — see below)
- Routine lag: 2d 6h since last run (>36h threshold — lag_warning fired; not stale-routine)
- Phases that failed: none — Phase 1.5 delegated to dispatcher tier-1d generator this run (see *Code review slice*); Phase 1.6 intentionally skipped (buffer-refill focus — see *Open questions*); pm_cursor NOT advanced

## Shipped
- #2815 fix(reality-server): widen `portal_saved_searches.match_count` to bigint — closes latent i32 overflow that would wedge long-lived saved searches
- #2813 code-review-reality-server-inquiry-detail-empty-messages — inquiry-detail endpoint now returns persisted messages (was hardcoded `vec![]`)
- #2812 fix(reality-server): stop discarding saved-search watermark-advance error — `let _ = ...` swallow removed, prevents duplicate alert sends
- #2811 gh-issue-2807: route `ai/voice.rs::link_voice_device` through single-writer voice-device upsert — closes the second link-path 23505 unique-violation
- #2806 code-review-api-handlers-voice-webhook-default-secret-retry2 — voice webhook HMAC now fails closed on empty secret
- #2805 gh-issue-2804: scope RUSTSEC-2026-0258 h2 DoS advisory ignore to the h2 0.3.x line only
- #2803 gh-issue-2802: bound apt-get network waits in CI so a stalled mirror can't wedge test-shard
- #2801 code-review-reality-server-list-my-reports-total-page-length — `list_my_reports` returns real total, not page length
- #2800 code-review-reality-server-submit-report-no-rate-limit — anonymous `POST /api/v1/reports` now rate-limited per-IP
- #2799 code-review-reality-server-create-review-body-length-uncapped — `create_review` body length now capped

## Watch
- Stalled review: PR #2744 (dispatcher self-fix, `needs-human-review`) — 8d idle, draft, blocks assignments-archive.json oversize-push wedge (see *Backlog deltas*)
- Stalled reviews (pre-existing, `needs-human-judgement`): #2555, #2558, #2559 — Epic UC-ACC-05 accounting PRs, 24d open, 22d idle
- Reverted: none
- Churn hotspots: `backend/servers/api-server/src/routes/voice_webhooks.rs` (4 in-window commits, runs_seen=3), `backend/servers/reality-server/src/routes/reports.rs` (3, runs_seen=1), `backend/servers/reality-server/src/routes/inquiries.rs` (3, runs_seen=1)

## PRs stuck in draft despite approval
- #2744 fix(dispatcher) un-wedge oversize-archive push — draft, `needs-human-review`, reviewDecision=APPROVED per dispatcher summary but human-gated by label; 8d since last update
- Note: draft #2817 (gh-issue-2816, saved_searches int4 removal) was opened today by the dispatcher fast-path — not stuck, still in normal review flow

## PRs with verdict=changes, no fix-round progress in 24h
- none — dispatcher assignments.json (planning branch) currently shows 2 active rows, no verdict=changes in-window

## Code review slice
- Segment reviewed: **delegated to dispatcher tier-1d generator this run** (reason: 30+ un-ingested code-review-finding signals across api-core / api-handlers / ppt-web-ui / reality-web sat in `.research/signals/2026-08-19..21-*-tier1d.json` — ingesting them beats spawning another dev-review that would just duplicate the pile)
- Segments effectively covered in the last 3 days by the dispatcher: `api-core` (2026-08-21), `api-handlers` / `ppt-web-ui` / `reality-web` (2026-08-20), `reality-server` / `mobile-native-kmp` (2026-08-19)
- Findings ingested this run: 25 landable (backend/frontend/reality) + 2 mobile-native-kmp-gated + 4 already-resolved-in-backlog (added to `seen_signals` to prevent re-emit)
- Next segment: `ppt-web-core` (last cursor 2026-08-17) or `mobile-rn` (2026-08-17) — oldest uncovered non-mobile-native segments after this run
- Skipped: no — this run replaced its own Phase 1.5 pass with dispatcher-fed signal ingestion; `state.review_cursor` advanced to reflect dispatcher coverage

## Screen-map status
- Total `docs/screens/` files: 153 (ppt/=106, reality/=47, mobile/=0)
- Drift signals this run: 0 (merged PRs touched only backend + reality-server + CI + backend deny.toml — no frontend routes)
- Orphan screens this run: 0 (not scanned — routine budget)
- Last `screen-map.yml` CI run on `main`: not queried this run

## Backlog deltas
- **New (landable, backend, score=3):** `code-review-api-core-email-action-stub-drops-mail` — EmailExecutor stub silently drops every workflow email (Epic 94)
- **New (landable, backend, score=3):** `code-review-api-core-notif-action-stub-noop` — NotificationExecutor stub is a no-op + `resolve_target` returns placeholder strings
- **New (landable, backend, score=2):** `code-review-api-core-announcement-roles-legacy-membership` — announcement scheduler resolves target roles via legacy `organization_members` query
- **New (landable, backend, score=2):** `code-review-api-core-osrng-expect-panic-auth-paths` — `.expect()` on OsRng in auth/oauth paths can panic under entropy stall
- **New (landable, backend, score=2):** `code-review-api-core-syndication-stub-never-called` — `process_syndication_job` has zero callers + stubbed body; portal publish silently backlogs forever
- **New (landable, backend, score=2):** `code-review-api-core-voice-contact-mgr-fake-email` — voice `action_contact_manager` speaks fabricated `manager@example.com` to Alexa/Google users
- **New (landable, backend, score=2):** `code-review-api-core-voice-facility-contact-fabricated-success` — voice `book_facility` + `contact_manager` return `success:true` with not-implemented body
- **New (landable, backend, score=2):** `code-review-api-handlers-financial-error-swallow` — unit-ledger handler collapses `Err` into `Ok(...empty transactions...)` — misleading financial data
- **New (landable, backend, score=2, security):** `code-review-api-handlers-list-limit-unclamped` — several list handlers forward user-controlled LIMIT unclamped (DoS + 500 on negative)
- **New (landable, frontend, score=2):** `code-review-ppt-web-core-api-401-onunauthorized-unwired` — `api.ts` onUnauthorized never invoked on 401; refresh queue never drains
- **New (landable, frontend, score=2):** `code-review-ppt-web-ui-ai-chat-delete-swallows-err` — `useAiChat useDeleteSession` swallows non-ok responses
- **New (landable, frontend, score=2):** `code-review-ppt-web-ui-ai-chat-ocr-fetch-no-auth` — OCR fetch is a bare `fetch()` with no auth header
- **New (landable, frontend, score=2):** `code-review-ppt-web-ui-dsa-download-no-onsuccess` — DsaReportsPage download mutation silently fails
- **New (landable, frontend, score=2):** `code-review-ppt-web-ui-moderation-prompt-cast` — ContentModerationPage `prompt()` cast unvalidated
- **New (landable, frontend, score=2):** `code-review-ppt-web-ui-moderation-prompt-cast-unvalidated` — same class in AmlDashboardPage
- **New (landable, reality-web, score=2):** `code-review-reality-web-import-mutations-silent-fail` — Epic 46 agency-import mutations fire without onError
- **New (landable, reality-web, score=2, security):** `code-review-reality-web-sso-callback-error-passthrough` — SSO callback renders raw `error_description` from URL (reflected XSS class)
- **New (landable, backend, score=1):** `code-review-api-core-delay-to-duration-mul-overflow` — DelayConfig::to_duration overflow → wraps to tiny sleep
- **New (landable, frontend, score=1):** `code-review-ppt-web-core-api-default-export-stale-singleton` — stale singleton on mutated baseURL
- **New (landable, frontend, score=1):** `code-review-ppt-web-core-refresh-queue-never-invoked` — AuthContext refresh queue never invoked; concurrent 401 races
- **New (landable, frontend, score=1):** `code-review-ppt-web-ui-ai-chat-querykey-paging` — queryKey ignores limit/offset; pagination collapses cache
- **New (landable, frontend, refactor, score=1):** `code-review-ppt-web-ui-compliance-hardcoded-i18n` — AML/DSA pages hardcode English
- **New (landable, frontend, refactor, score=1):** `code-review-ppt-web-ui-dsa-reports-i18n` — DsaReportsPage status strings hardcoded
- **New (landable, frontend, refactor, score=1):** `code-review-ppt-web-ui-registry-confirm-i18n` — RegistryPage destructive confirmations hardcoded English
- **New (landable, reality-web, refactor, score=1):** `code-review-reality-web-listinggrid-empty-i18n` — search-results grid empty-state hardcoded English
- **New (gated, mobile-native-kmp, score=1):** `code-review-mobile-native-kmp-formatprice-minor-unit-contract` — deferred (unlandable in cloud per #2652)
- **New (gated, mobile-native-kmp, score=1):** `code-review-mobile-native-kmp-ssoinitiation-callback-unencoded` — deferred
- **New (needs-human-judgement, dx, score=1):** `stalled-review-pr-2744` — draft PR w/ `needs-human-review` label, 8d idle, blocks dispatcher assignments-archive wedge
- **Bumped:** none this run (no signals updated existing open rows)
- **Decayed:** none (all 5 pre-existing open items updated within the last 14d)
- **Dropped:** none this run
- **Resolved (done):** none via routine's Phase 2 resolution check; PRs merged this window did not name any open backlog `id` verbatim and evidence-grep scope guard skipped (open items are all mobile-native-kmp, unaffected by the merged Rust/reality-server diffs). Dispatcher had independently marked several backlog rows `done` between routine runs (`inquiry-detail-empty-messages`, `list-my-reports-total-page-length`, `submit-report-no-rate-limit`, `saved-search-watermark-discard`, `create-review-body-length-uncapped`); accepted as-is

## Plans promoted
- `plans/code-review-api-core-email-action-stub-drops-mail.md` — wire `EmailExecutor` to `AppState::email_service` via `ActionRegistry::with_services`, call `send_template_email`, map `EmailError`; add stub-detection unit test + workflow integration test · adversarial pass: passed (source→hypothesis→approach→test chain unbroken; unit test would count zero calls today; 4/4 cited files exist on disk; Mode: cloud-ok)
- `plans/code-review-api-core-notif-action-stub-noop.md` — wire `NotificationExecutor` to `AppState`-owned `NotificationService::send_to_users`; rewrite `resolve_target` for `Role`/`Building` to query real user ids (not placeholder strings); add stub-detection + resolve-target regression tests · adversarial pass: passed (source→hypothesis→approach→test chain unbroken; resolve_target test would fail today because it returns literal `"role:{name}"`; 5/5 cited files exist on disk; Mode: cloud-ok)

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run (all fresh `code-review-finding` signals are confidence=medium; both promoted-to-plan items exceed score≥3 but need real plans — none fits the doc-stub/orphan-remove/lockfile-bump/stalled-review/fixme-in-merged-code allowlist)

## Self-review findings
- none — `.research/self-improvement/findings.json` not present

## Open questions
- Phase 1.6 has been skipped 2 runs in a row (buffer-refill focus). If the dispatcher continues to structurally starve on mobile-native-kmp-gated backlog, the routine's own delivery synthesis is missing; consider whether to change `pm_cursor` policy so the next non-refill run runs `pm-full` once to catch up.
- Dispatcher-fed tier-1d signals under `.research/signals/YYYY-MM-DD-<segment>-tier1d.json` have been quietly piling up unread since 2026-08-01 (~116 files, ~150 signals). This run ingested only the window since 2026-08-19 (25 landable + 2 gated). The 2026-08-18-and-earlier files still hold un-ingested signals (best-effort estimate ≥100 more). Recommend a one-off `text: "deep"` run once the current refill cycle stabilises, or add an explicit "scan `.research/signals/*-tier1d.json` for un-ingested" step to Phase 1.

## Quiet day?
- No — 10 PRs merged, 25 landable new backlog vectors, 2 plans promoted, 1 stalled-review + 3 churn-hotspot process signals emitted
