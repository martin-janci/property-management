# 2026-08-21

## Since last run
- Merged PRs: 10 (range #2799–#2815)
- Open PRs touched: 2 (dependabot #2809/#2810 rolled up; #2744 still draft, needs-human-review)
- New / updated issues: 0 relevant (issue cursor unchanged)
- Commits: 21 on `dev` (mix of dispatcher bookkeeping + 8 substantive merges)
- Routine lag: 2d 0h since last run — over the 36h threshold (trigger payload: `buffer-low: claimable=6/72 — refill planner`)
- Phases that failed: none

## Shipped
- #2815 gh-issue-2814: widen `portal_saved_searches.match_count` to bigint — SMALLINT overflow fix on saved-search counters (reality-server)
- #2813 code-review-reality-server-inquiry-detail-empty-messages: return persisted inquiry messages
- #2812 fix(reality-server): stop discarding saved-search watermark-advance error
- #2811 gh-issue-2807: route ai/voice link path through single-writer voice-device upsert
- #2806 code-review-api-handlers-voice-webhook-default-secret-retry2: voice webhook HMAC fails closed on empty secret
- #2805 gh-issue-2804: scope RUSTSEC-2026-0258 h2 DoS ignore to the h2 0.3.x line
- #2803 gh-issue-2802: bound apt-get network waits in CI so a stalled mirror can't wedge test-shard
- #2801 code-review-reality-server-list-my-reports-total-page-length: list_my_reports returns true total row count
- #2800 code-review-reality-server-submit-report-no-rate-limit: rate-limit anonymous POST /api/v1/reports per IP
- #2799 code-review-reality-server-create-review-body-length-uncapped: cap create_review body length

## Watch
- Stalled review: #2555, #2558, #2559 (all UC-ACC-05.x invoice/PAY-by-square work by @martin-janci — 22d idle now, still `needs-human-judgement` — the accounting-invoice epic is quietly parked).
- Draft-with-CI-green: #2744 (fix(dispatcher) un-wedge oversize-archive push) — approved-in-comment on 2026-08-21T04:30 but formal APPROVE blocked on self-PR; still draft, `needs-human-review` label.
- Churn hotspots: no fresh hotspot scan this run (incremental refill run — full churn analysis deferred to next scheduled run).

## PRs stuck in draft despite approval
- #2744 fix(dispatcher): un-wedge oversize-archive push (#1162) + close retry-remint cross-id ghost (#2460) — reviewer_summary marks approve on 2026-08-21T04:30Z; still draft, `needs-human-review` label — awaits explicit human ready-for-review flip.

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: `api-core` (already picked up by dispatcher earlier — see `signals/2026-08-21-api-core-tier1d.json`; this routine run adopted its three findings into the daily signals + backlog)
- Experts: rust
- Findings: 3 — see Backlog deltas for `code-review-finding` signals
- Next segment: `ppt-web-ui` or `ppt-web-core` will rotate next (both last reviewed 2026-08-16/17; `mobile-native-kmp` also 2026-08-16). Routine will pick the oldest-unreviewed next run.
- Skipped: no — findings adopted from same-day dispatcher tier1d slice

## Screen-map status
- Total `docs/screens/` files: unchanged this run — the merged-PR window touched only backend/CI, not any `frontend/apps/{ppt-web,reality-web}/src/{App.tsx,routes/**,app/**}`.
- Drift signals this run: 0
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (incremental refill scope).

## Backlog deltas
- **New:** [3] api-core EmailExecutor::execute() logs and returns success without invoking EmailService — every workflow email action silently drops
- **New:** [3] api-core NotificationExecutor::execute() is a no-op stub and resolve_target() returns placeholder strings — every workflow notify action silently no-ops
- **New:** [2] api-core action_contact_manager() returns hardcoded manager@example.com placeholders — voice assistant reads fake contact info to residents
- **Status-fix:** `code-review-mobile-native-kmp-inquiries-response-contract` `open → ready` (plan already existed at `plans/code-review-mobile-native-kmp-inquiries-response-contract.md` — status inconsistency corrected so the dispatcher sees it as claimable)
- **Bumped:** none this run
- **Decayed:** none — all open items are within the 14-day window
- **Dropped:** none

## Plans promoted
- `plans/code-review-api-core-email-action-stub-drops-mail.md` — wire real `EmailService.send_template_email` into `EmailExecutor` and thread `Arc<EmailService>` through `ActionRegistry`. adversarial pass: passed
- `plans/code-review-api-core-notif-action-stub-noop.md` — inject `Arc<NotificationService>` + `PgPool` into `NotificationExecutor`, resolve `Role`/`Building` targets to real user ids, then call `send_batch`. adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run (the three api-core findings are `confidence=medium`; the two ready plans go through the manual implementer path)

## Self-review findings
- none

## Open questions
- The three accounting/invoice PRs (#2555, #2558, #2559) are 22d idle in `needs-human-judgement` — the routine is not going to promote them; the human needs to either revive them, close them out, or attach reviewers before the buffer refill helps.

## Quiet day?
- No — 10 merged PRs, 3 new backlog vectors, 2 plans promoted, 1 status-fix; explicit buffer-refill trigger.
