# 2026-08-09

## Since last run
- Merged PRs: 18 (range #2696–#2723; excludes dispatcher-chore commits)
- Open PRs touched: 4 (#2684 QUARANTINED, #2724 draft, #2725 draft, #2726 draft — all under 24h)
- New / updated issues: 3 CLOSED by merges (#2703 by #2710, #2704 by #2707, #2612 by #2714); no new opens
- Commits: 49 on `dev` (14 non-dispatcher; 35 dispatcher-chore) since prior cursor sha `a2c0f53d`; head now `812d00c`
- Routine lag: **2d 1h since last run — `lag_warning` fired** (>36h threshold); daily run appears to have been skipped 2026-08-08
- Phases that failed: none

## Shipped
- **Security fixes (3):**
  - #2710 `gh-issue-2703` — SSRF DNS-rebinding TOCTOU closed in workflow `api_call.rs` via connect-time `SsrfGuardResolver` + redirect-time re-validation. **Author's own follow-up note names `signatures.rs` and `integrations/webhook.rs` as unfixed sibling call sites** — see *Plans promoted* below.
  - #2722 `code-review-api-handlers-community-unauthenticated-reads-retry2` — three anonymous cross-tenant reads (`get_group`, `list_posts`, `get_item`) gated on `RequestPrincipal` + `verify_*_access` helpers; retry-round success after retry1 failed CI.
  - #2707 `gh-issue-2704` — 8 MiB cap on workflow `api_call.rs` response-body read (memory-amplification DoS).
- **Correctness fixes (3):**
  - #2714 `gh-issue-2612-retry1` — scheduled announcement / vote-started / closed-vote notifications decoupled from `published/activated/closed` (added `notified_at` migration `00228`, scheduler retries on transient error).
  - #2708 — workflow condition compare rejects non-finite numeric strings (`NaN`/`inf`) at `compare_numeric`.
  - #2706 — RAG index fails closed on partial `embed_batch` return (was silently dropping surplus chunks).
- **Route wiring / stubs replaced (3):**
  - #2719 — reality-server anonymous inquiry POSTs now go through `InquiriesHandler` (the notifier seam from #2696 was previously never reached on the live path — realtors still got no notification).
  - #2716 / #2717 — `PATCH /api/v1/platform-admin/settings` and `PATCH /api/v1/admin/mobile-config` endpoints added; admin-web Save flows no longer target missing backends (renumbered migrations `00229`, `00230` due to collision with `00228_announcement_vote_notified_at.sql` from #2714).
- **Audit / metrics (2):**
  - #2712 — `DisputeRepository::add_evidence` emits access-audit event on the `AuditRead`-gated stream.
  - #2723 — `AnnouncementRepository::get_fanout_metrics_rls` returns delivered/read/ack per targeting scope with cross-tenant-safe SQL.
- **Refactors / churn dedupe (7):**
  - #2711 (layout tenant), #2713 (layout admin), #2715 (auth handler), #2720 (reports helpers extracted), #2721 (`acquire_public_conn` extracted), #2718 (layout webhook HMAC parity — verification only, no fix required), #2709 (reality-web `ListingForm` extracted into next-intl catalogs, all 6 locales).

## Watch
- **PR #2684 QUARANTINED** — `code-review-api-core-workflow-cond-parse-failopen`, verdict=changes on `926afe8`, backend test-shard(1-4) FAILURE after round-3 clippy fix; `fix_rounds=3` exhausted per dispatcher. Needs human triage before more rounds are spent.
- **PR #2705 closed unmerged** — dispatcher-driven retry to bump `dtolnay/rust-toolchain 1.94.1 → 1.100.0` (dependabot #2385 chain); investigation concluded the pin should stand. Classified as `dep-update-noise`, not backlogged.
- **Three fresh drafts (all day 0)** — #2724 (db-error-leak), #2725 (password-reset transport), #2726 (sso-session-invalidate) — all reality-server retry-1 fixes from the dispatcher's overnight queue. None yet under review.
- Reverted: none.
- Churn hotspots this run: `backend/servers/api-server/src/routes/integrations/webhook.rs` (2799), `backend/servers/api-server/src/routes/reports/mod.rs` (2747), `backend/servers/api-server/src/services/scheduler/mod.rs` (2746). All three are first-entry (`runs_seen=1`) under these paths — the `mod.rs` variants are new after PR #2720's extract.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- `code-review-api-core-workflow-cond-parse-failopen` PR #2684 — QUARANTINED per dispatcher fix_rounds=3 exhausted, last_updated 2026-08-08T10:12:42Z (~22h ago); needs human review before further rounds.

## Code review slice
- Segment reviewed: **api-core** (reason: churn-aligned — `reports/mod.rs` + `scheduler/mod.rs` + `integrations/webhook.rs` all in this segment; AND oldest-unreviewed tied at 2026-08-04)
- Experts: rust
- Findings: **2**
  - `code-review-api-core-ssrf-toctou-signatures` (**+3, HIGH severity, security**) — `signatures.rs::store_signed_document` has the same SSRF DNS-rebinding TOCTOU that PR #2710 just fixed in `api_call.rs`. Router mounted at `/api/v1/signature-requests/webhook/{provider}` (`lib.rs:169`) → **LIVE production vulnerability**. Confidence upgraded medium→high after in-context verification of code paths + router mount.
  - `code-review-api-core-ssrf-toctou-webhook` (**+2 base + 1 churn-overlap stack, medium**, security) — same SSRF class in `integrations/webhook.rs::test_webhook`, but the endpoint is UNMOUNTED per its router docstring (`ROADMAP(PAP-122)`). Fix ports the pattern before Epic-61 mounts the CRUD.
  - Expert also verified `workflow_executor.rs` (which PR #2710's note listed as an unfixed sibling) does NOT construct its own reqwest client — dispatch is delegated to the already-hardened `ApiCallExecutor`. The PR-note inclusion of that file appears inaccurate.
- Next segment: `mobile-native-kmp` (oldest at 2026-08-04, unchanged this run)
- Skipped: no

## Screen-map status
- Total `docs/screens/` files: unchanged (no PR in this run's window touched `docs/screens/**`; `mobile/` still not seeded → 0)
- Drift signals this run: 0 (no PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or non-API paths under `frontend/apps/reality-web/src/app/**`)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (per-workflow REST call skipped for budget)

## Backlog deltas
- **New:**
  - [3] `code-review-api-core-ssrf-toctou-signatures` — LIVE production SSRF DNS-rebinding TOCTOU in the e-sign webhook handler (`signatures.rs`); same pattern PR #2710 just fixed elsewhere. **Promoted to a plan this run.**
  - [3] `code-review-api-core-ssrf-toctou-webhook` — same SSRF class in the currently-unmounted `integrations/webhook.rs::test_webhook`; score composed from `+2` code-review finding + `+1` churn-overlap co-signal on the same file (evidence documents the stack). **Promoted to a plan this run.**
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-integrations-webhook-rs` — first-entry hotspot (2799 lines); overlaps the SSRF finding above.
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-reports-mod-rs` — first-entry hotspot (2747 lines) after PR #2720's `helpers.rs` extraction.
  - [1] `churn-hotspot-backend-servers-api-server-src-services-scheduler-mod-rs` — first-entry hotspot (2746 lines) after PR #2714's fire-once decouple.
- **Bumped:** none.
- **Decayed:** none — the sole `open` row at cutoff (`code-review-mobile-native-kmp-portfolio-analytics-caps-100`, updated 2026-08-04) is within 14 days.
- **Dropped:** none this run.
- **Resolved:** all 18 merged PRs correspond to backlog rows the dispatcher already flipped `done`/`dropped` in earlier commits — Phase 2's resolution-check found no additional open items to flip.

## Plans promoted
- `plans/security-ssrf-toctou-signatures-rebinding.md` — port the `SsrfGuardResolver` + `resolve_and_validate_host` pattern from `api_call.rs` into a shared `common::http_client` helper and apply it to `routes/signatures.rs::store_signed_document`; add IG3 regression test using a stateful hermetic DNS resolver. · adversarial pass: passed
- `plans/security-ssrf-toctou-integrations-webhook.md` — same fix applied to `routes/integrations/webhook.rs::test_webhook` (currently unmounted; lands before Epic-61 mounts the CRUD to prevent silent re-emergence). Depends on the signatures plan for the shared helper extraction. · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run (Phase 5 kill switch not set; env-check clean). The two SSRF findings are code changes with non-trivial blast radius; they go through the manual-implementer path with adversarial-reviewed plans.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present)

## PM analysis (rotating: `pm-qa`)
- **Sprint:** Epic 6, 7A, 8A & 10A — Announcements, Documents, Notifications & OAuth. `3/5` epics done.
- **Shipped since last run (via PM digest):** 18 PRs, largest by domain footprint = #2714 (scheduler decouple, 5 files across db+api-server), #2716 (platform-admin settings, 7 files incl. migration `00229`), #2723 (announcement fan-out metrics, 4 files in db).
- **Next up (top 3 for the manual implementer):**
  1. Finish 84-1 direct-to-S3 upload wiring in ppt-web — api-client binding + `UploadDocument` integration + regression test (owner: `pm-frontend`).
  2. Build signer-facing document-sign page (84-2) in ppt-web against shipped signing API; verify signature-request email end-to-end (owner: `pm-frontend`).
  3. Human triage of quarantined PR #2684 (workflow_cond_parse_failopen) — CI test-shards 1-4 red after 3 respawns; classify as flake vs real regression (owner: `pm-tech-lead`).
- **Blockers:** PR #2684 quarantine; 84-1 + 84-2 long-standing partials (prior implementer attempts failed no-PR).
- **Role focus today:** `pm-scrum-master`, `pm-qa`.
- **Coverage upkeep:** `epic-80` re-checked (3 stories; PR #2712 evidence added to `80-1`); bonus evidence rows for `6-1` (PR #2723 fan-out metrics) and `84-5` (PR #2706 RAG). `scan_kind=upkeep`, `coverage_cursor` 5→6 (next epic: `epic-81`).
- **`action-list.json` buffer refilled to 49 open** (target ≥36; well above the "buffer-low" threshold that triggered this run). 7 items advanced to `done`, 1 to `failed`, 3 stay `in-progress` (the three fresh reality-server drafts).

## Open questions
- The trigger payload `buffer-low: claimable=1/72` referred to the dispatcher's ready-to-implement queue (`action-list.json` open rows). Phase 1.6's PM pass refilled that to 49 open. In parallel, Phase 3 added 2 `ready` security plans + 3 `open` refactor rows to `backlog.json` — the plan-side buffer refills to ~3 claimable after Phase 6 lands.
- **PR-note inaccuracy noted (audit hygiene):** PR #2710's Notes section named `workflow_executor.rs` as an unfixed SSRF sibling, but the file does not construct its own reqwest client (dispatch flows through the hardened `ApiCallExecutor`). The two real sibling call sites are `signatures.rs` and `integrations/webhook.rs`, both now promoted to plans. No corrective PR-body edit needed; recording here so future audits don't chase the phantom.

## Quiet day?
- **No** — 18 PRs merged, 5 new backlog vectors, 2 plans promoted (both security — one covering a LIVE production vulnerability), pm-qa role + Scrum Master ran, coverage upkeep advanced, action-list buffer refilled 2→49. This is the opposite of a quiet day.
