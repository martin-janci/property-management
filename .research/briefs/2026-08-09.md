# 2026-08-09

## Since last run
- Merged PRs: 21 (range #2706–#2726)
- Open PRs touched: 1 (#2684 workflow-cond-parse-failopen — still awaiting review)
- New / updated issues: 3 (all closed: #2704, #2703, #2612)
- Commits: 52 on `dev`
- Routine lag: 2d 3h since last run (last_run_iso=2026-08-07T06:24:00Z, threshold=36h — routine missed the 2026-08-08 daily fire)
- Phases that failed: none

## Shipped
- #2726 reality-server SSO — surface `invalidate_session` failure in `sync_session` (was silently swallowed; portal session survived PM revocation)
- #2725 reality-server password-reset — wire real email transport + "not-configured" guard (was stub)
- #2724 reality-server — route raw DB errors through `util::errors::db_error` (stops leaking Postgres detail to callers)
- #2723 announcement fan-out — delivered/read/ack metrics per targeting scope (data)
- #2722 api community read handlers — gate on principal + tenant (was unauth-readable)
- #2721 api-server layout tenant — extract `acquire_public_conn` helper (refactor of churn hotspot)
- #2720 api-server reports — extract reports helpers into `helpers.rs` (refactor of churn hotspot)
- #2719 reality-server inquiries — route anon POSTs through `InquiriesHandler` (route-wiring gap)
- #2718 layout webhook HMAC — parity verify + pin body-binding test (security)
- #2717 admin-web mobile-config — add PATCH/GET `/api/v1/admin/mobile-config`
- #2716 platform-settings — add PATCH/GET `/api/v1/platform-admin/settings`
- #2715 api-server auth handlers — dedupe error boilerplate (refactor of churn hotspot)
- #2714 scheduler — decouple scheduled notification dispatch from publish/activate/close (closes #2612)
- #2713 api-server layout admin — dedupe handler boilerplate (refactor)
- #2712 disputes — emit `add_evidence` access-audit event (IDOR fix follow-up)
- #2711 api-server layout tenant — dedupe tenant-override handlers (refactor, second slice)
- #2710 workflow `api_call.rs` — close SSRF DNS-rebinding TOCTOU (closes #2703)
- #2709 reality-web `ListingForm` — i18n via next-intl catalogs (sk/cs/de/en/hu/pl)
- #2708 workflow_executor — reject non-finite numbers in condition compare (`"NaN"` / `"inf"` no longer slip past)
- #2707 workflow `api_call.rs` — cap unbounded response-body read at 8 MiB (memory DoS, closes #2704)
- #2706 `ai/llm.rs` — fail closed on partial RAG embedding batch (silent-truncation guard)

## Watch
- Stalled review: #2559 — 11d, martin-janci (PAY-by-square QR endpoint UC-ACC-05.8)
- Stalled review: #2558 — 11d, martin-janci (invoice PDF render UC-ACC-05.9)
- Stalled review: #2555 — 11d, martin-janci (invoice lifecycle UC-ACC-05.17)
- Reverted: none this window
- Closed-not-merged: #2705 (dependabot rust-toolchain 1.94.1 → 1.95.0; 2nd retry failed — #2385-pattern persists)
- Churn hotspots: `backend/servers/api-server/src/routes/reports/mod.rs` (2747 lines / 2 commits, runs_seen=1), `.../services/scheduler/mod.rs` (2746 / 2, runs_seen=1), `.../routes/auth/mod.rs` (2283 / 2, runs_seen=1). All three are fresh module-mod paths after the `auth`/`reports` file→module refactors; historical `routes/auth.rs`/`routes/reports.rs` hotspot entries (5/4 runs_seen) will not attach without a rename-aware history merge (not automated).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: skipped (dispatcher already produced 5 tier1d slices covering api-core ×2, mobile-native-kmp, ppt-web-ui, reality-web today; running an additional routine-side pass would be duplicative for this run)
- Experts: rust, kotlin, frontend (via dispatcher tier1d generator)
- Findings: 30 new tier1d `code-review-finding` signals folded into the backlog (2026-08-08 backlog was 8 unfolded signals + 2026-08-09 was 22; today's routine catches up both days at once because 2026-08-08 had no routine run)
- Next segment: `mobile-rn` (oldest-unreviewed after this run; last reviewed 2026-08-06)
- Skipped: yes — dispatcher's demand-driven Tier-1d generator already ran under buffer-starved conditions today

## Screen-map status
- Total `docs/screens/` files: unchanged (mobile/ still not seeded)
- Drift signals this run: 1 (`screen-map-drift-pr-2718-reality` — PR #2718 touched `frontend/apps/reality-web/src/app/api/layout-revalidate/route.test.ts` without touching `docs/screens/reality/`; heuristic caveat noted — the file is an API-route test, not a page)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (skipped for token budget)

## Backlog deltas
- **New:** 35 items (30 tier1d code-review findings from 2026-08-08 + 2026-08-09, 3 churn-hotspot rows for the fresh mod.rs paths, 1 closed-not-merged-pr row for #2705, 1 screen-map-drift row for #2718 — see backlog.md)
- **Highest new:** [3] `code-review-api-core-rls-context-pool-not-conn` — deprecated `require_rls_context` middleware still re-exported; latent cross-tenant leak if adopted
- **Bumped:** 0 (no re-scored existing rows this run)
- **Decayed:** 0 (only pre-existing open item is 5 days old — well inside the 14-day window)
- **Dropped:** 0
- **Resolved:** 0 (none of the 21 merged PRs referenced an open backlog id verbatim — they all closed prior-run items already marked done by the dispatcher path)

## Plans promoted
- `plans/code-review-api-core-rls-context-pool-not-conn.md` — deprecate the pool-scope RLS middleware + narrow the re-export so future adopters get a compile-time warning; the correct path is the `RlsConnection` extractor which sets RLS on the same connection it hands the handler · adversarial pass: passed

Cap was 2; only one item this run met the readiness gate (`score >= 3` OR security-fast-track). Buffer-low signal from the trigger payload noted — no other item cleared the bar, and lowering the bar would ship a hand-wavy plan.

## Auto-fix
- `no candidate` — screen-map-drift-pr-2718-reality is the only allowlist-eligible signal, but its confidence is `medium` (heuristic) and the underlying PR touched an API-route test, not a page; a stub `docs/screens/reality/<slug>.md` write would encode a false positive into the repo. Bar not met, skipped intentionally.

## Self-review findings
- none (`.research/self-improvement/findings.json` not consulted this run — kept out of scope for token budget)

## Open questions
- Should the routine's Phase 1.5 explicitly *defer* to the dispatcher's tier1d generator on days the dispatcher already ran multi-segment reviews, or keep both mechanisms and dedupe post-hoc? Today's run chose defer; the routine-prompt currently says "run Phase 1.5 unless Phase 1 failed", so this needs a small spec update if we want defer to be the persistent policy.
- Churn hotspot bookkeeping does not track file→module renames, so `routes/auth.rs → routes/auth/mod.rs` starts back at runs_seen=1. Worth adding a rename-aware normalization step before the next run's `repeated-churn` derivation, or the same hot module will look like a fresh signal every time the churn migrates paths.

## Quiet day?
- No (21 PRs merged, 35 new backlog rows, 1 plan promoted, 30 tier1d findings folded)
