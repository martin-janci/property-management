# 2026-08-09

## Since last run
- Merged PRs: 22 (range #2706–#2727)
- Open PRs touched: 1 (#2684 workflow-cond-parse-failopen — still open, no reviewDecision available via REST)
- New / updated issues: 0 (last issue = #2699; no new issues since cursor)
- Commits: 50 first-parent on `dev` since 2026-08-07T06:24Z
- Routine lag: 2d 9h since last run (57h > 36h threshold — routine skipped 2 daily fires; dispatcher continued on its own branch)
- Phases that failed: none (Phase 1.5 skipped by policy — see *Code review slice* below)

## Shipped
- #2727 code-review-reality-server-agency-members-unauth-idor-retry1 — auth+membership gate on `GET /api/v1/agencies/{id}/members`, closes prior IDOR
- #2726 code-review-reality-server-sso-session-invalidate-swallowed — surface portal session-invalidate failure in `sync_session`
- #2725 code-review-reality-server-password-reset-no-transport — real reset-email transport + not-configured guard
- #2724 code-review-reality-server-db-error-leak — route raw DB errors through `util::errors::db_error`
- #2723 data-announcement-fanout-metric — announcement fan-out delivered/read/ack metrics per targeting scope
- #2722 code-review-api-handlers-community-unauthenticated-reads-retry2 — gate community read handlers on principal+tenant
- #2721 refactor-churn-hotspot-api-server-layout-tenant — extract `acquire_public_conn` in layout tenant handlers
- #2720 refactor-churn-hotspot-api-server-reports — extract reports helpers into `helpers.rs`
- #2719 code-review-reality-server-inquiry-notify-route-wiring — route anonymous inquiry POSTs through `InquiriesHandler`
- #2718 sec-layout-webhook-hmac-verify — pin layout-webhook HMAC body-binding parity test
- #2717 dx-fixme-admin-web-mobile-config-patch-endpoint — add `PATCH/GET /api/v1/admin/mobile-config`
- #2716 dx-fixme-admin-web-platform-settings-patch-endpoint — add `PATCH/GET /api/v1/platform-admin/settings`
- #2715 refactor-churn-hotspot-api-server-auth — dedupe auth-handler error mappers
- #2714 gh-issue-2612 — decouple scheduled notification dispatch from publish/activate
- #2713 refactor-churn-hotspot-api-server-layout-admin — dedupe layout admin handlers
- #2712 data-audit-add-evidence-idor-fix — emit dispute `add_evidence` audit
- #2711 refactor-churn-hotspot-api-server-layout-tenant — dedupe layout tenant handlers
- #2710 gh-issue-2703 — close SSRF DNS-rebinding TOCTOU in workflow `api_call.rs` (also brought in 4547 files from a stale branch — see churn caveat)
- #2709 code-review-reality-web-listingform-no-i18n — i18n `ListingForm` via `next-intl`
- #2708 churn-hotspot-workflow-executor — reject unknown action-config keys
- #2707 gh-issue-2704 — cap unbounded workflow api_call response-body read (memory DoS)
- #2706 churn-hotspot-ai-llm — fail closed on parsing errors in `ai/llm.rs`
- #2705 (closed unmerged) dx-closed-not-merged-pr-2385-retry2 — dtolnay/rust-toolchain bump superseded

## Watch
- Stalled review: #2559 (feat(acc) PAY-by-square QR), #2558 (invoice PDF render), #2555 (invoice sent/cancelled lifecycle) — martin-janci, 9d idle each. Non-bot backlog stuck in review since 2026-07-31.
- Stalled dependabot: #2591 (expo-secure-store 57), #2590 (expo-device 57), #2589 (@testing-library/react-native 14), #2588 (expo-status-bar 57) — 9d idle, logged as `dep-update-noise` (Δ0).
- Reverted: none
- Churn hotspots (source-only, excluding PR #2710's 4547-file merge from a stale branch which dominated raw churn):
  - `backend/servers/api-server/src/routes/auth/mod.rs` (326 lines, runs_seen=1 at new post-refactor path — historical `auth.rs` had runs_seen=5)
  - `backend/servers/api-server/src/routes/reports/mod.rs` (287 lines, extract of `helpers.rs`; historical `reports.rs` runs_seen=4)
  - `backend/servers/reality-server/src/routes/inquiries.rs` (239 lines, InquiriesHandler wiring)

## PRs stuck in draft despite approval
<!-- REST-only session — no reviewDecision field; the routine cannot compute this without GraphQL. Recorded as an unknown, not a false "- none". -->
- unknown — REST session cannot read `reviewDecision`; check with `gh pr list --state open --draft --json number,title,updatedAt,reviewDecision,isDraft` locally.

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Read from planning branch assignments.json — the routine on dev cannot see planning's state without an extra fetch. -->
- see dispatcher's active-work checkpoint on `planning` branch — the dispatcher landed 3 code-review retries this cycle without reviewer pushback beyond the merged rounds.

## Code review slice
- Segment reviewed: **skipped this run** (reason: dispatcher's parallel tier1d generator emitted 7 review-signal files across 6 segments in the 24h window — api-core (×2), api-handlers, mobile-native-kmp, ppt-web-ui, reality-server, reality-web, and 08-08 for ppt-web-core + mobile-rn + reality-server; running a duplicate expert pass on the same segments this run would waste ~15k tokens with near-zero marginal information).
- Experts: — (skipped)
- Findings: 0 emitted by this run; **34** dispatcher tier1d findings consolidated into `backlog.json` (see Backlog deltas).
- Next segment: `api-core` (oldest cursor date 2026-08-04 — but dispatcher covered it today twice; realistically pick `mobile-native-kmp` next run since it also last-cursor 2026-08-04 and dispatcher covered it only once today).
- Skipped: yes — dispatcher covered every review segment except `ppt-web-core` in the last 48h; delta review would produce mostly duplicates.

## Screen-map status
- Total `docs/screens/` files: 169 (ppt=106, reality=47, reality-mobile=15, mobile-native still absent so drift detection stays gated per routine-prompt.md).
- Drift signals this run: 0. Merged PRs touched almost no product-facing routes; only PR #2718 touched `frontend/apps/reality-web/src/app/api/layout-revalidate/route.test.ts` (a test file for an API route, not a screen page) — the routine-prompt's best-effort caveat applies and I did not emit a false-positive drift.
- Orphan screens this run: 0 (no matching-name sweep this run — the trigger set of touched route basenames was empty).
- Last `screen-map.yml` CI run on `main`: not queried this run (would need `mcp__github__actions_list`; cost > value on this run).

## Backlog deltas
- **New:** [3] `code-review-api-core-rls-context-pool-not-conn` (security) — `require_rls_context` middleware sets RLS on a transient pool conn, handler runs on a different pooled conn without GUCs — latent cross-tenant leak trap.
- **New:** [2] `code-review-api-core-authz-mw-guest-fail-open` (security), `code-review-api-core-require-auth-guest-default` (security), `code-review-api-core-rls-context-deprecated-exported` (security), `code-review-reality-server-sso-token-cache-ignores-exp` (security), `code-review-api-core-delay-duration-overflow-before-cap` (bug), `code-review-api-core-idempotency-tx-across-handler` (bug), `code-review-api-core-voice-meter-fake-success` (bug), `code-review-api-core-vote-reminders-no-cross-tick-dedup` (bug), `code-review-api-core-accounting-line-multimatch` (bug), `code-review-api-handlers-doc-summary-not-persisted` (bug), `code-review-api-handlers-listings-detail-photos-swallow` (bug), `code-review-api-handlers-reports-unwrap-or-default-silent-zero` (bug), `code-review-api-handlers-workflow-import-partial-silent` (bug), `code-review-mobile-native-kmp-cancellationexception-swallowed-repos` (bug, high), `code-review-mobile-native-kmp-create-listing-submit-stub` (bug, high — user-facing broken feature), `code-review-ppt-web-core-errorhandler-network-misclassify` (bug, high), `code-review-ppt-web-ui-automationrules-mutation-no-catch` (bug), `code-review-ppt-web-ui-conditionbuilder-number-coerce-zero` (bug), `code-review-reality-web-journal-mock-content-shipped` (test-gap)
- **New:** [1] `code-review-api-core-accounting-no-tests` (test-gap), `code-review-api-core-accounting-vs-only-amount-ignored` (bug), `code-review-api-core-quiet-hours-db-error-fail-open` (bug), `code-review-api-core-syndication-fake-synced` (bug), `code-review-api-core-tenant-filter-noop-isolation` (security), `code-review-api-core-voice-announce-fake-success` (bug), `code-review-mobile-native-kmp-apiclient-dead-code-healthcheck-bug` (refactor, high — 100 LOC dead code), `code-review-mobile-native-kmp-compose-state-bang-bang` (refactor), `code-review-mobile-native-kmp-layoutrepo-println-logging` (refactor, high), `code-review-ppt-web-core-usewebsocket-handler-dep-gap` (bug), `code-review-ppt-web-ui-workflow-automation-i18n-gap` (dx), `code-review-reality-web-layout-section-boundary-swallows-errors` (bug), `churn-hotspot-*-auth/mod.rs`, `churn-hotspot-*-reports/mod.rs`, `churn-hotspot-*-inquiries.rs` (refactor)
- **Bumped:** `code-review-sso-rs-client-no-timeout-pool` (2→2 held; new alias id `code-review-reality-server-sso-http-client-no-timeout` fired but marked as alias with Δ=0, only new source appended). Same shape for `code-review-sso-rs-negcache-transient-fail`.
- **Decayed:** none (only 1 open item pre-run, `code-review-mobile-native-kmp-portfolio-analytics-caps-100`, updated 2026-08-04 — 5 days old, below 14d threshold).
- **Dropped:** `code-review-api-core-rls-context-deprecated-exported` — superseded by `rls-context-pool-not-conn` (same underlying trap; delete the deprecated module covers both).
- **Resolved:** none (the 22 merged PRs' shape mostly closes tasks from `.research/management/action-list.json` on the planning branch, not `backlog.json` items — those already flipped to `done` in prior routine runs).

## Plans promoted
- `plans/code-review-api-core-rls-context-pool-not-conn.md` — delete-or-deprecate the pool-vs-conn RLS middleware trap that no handler is currently using but the docstring still promises. Adversarial pass: **passed** — hypothesis→approach→test-plan chain intact, evidence lines-numbered against real code (verified `rls_context.rs:1-37, 180-245`, `middleware/mod.rs:16 pub use rls_context::*;`, `db/tenant_context.rs:18`), 2 rejected alternatives named, C1+C6 ticked, Mode `cloud-ok`, all 4 files resolve on disk.
- (only 1 plan promoted this run — 2nd slot unused because no other open item met the readiness gate: `score >= 3` OR security-fast-track `(security ∧ confidence=high ∧ score >= 2)`. The 34 new dispatcher items scored 1–3; only `rls-context-pool-not-conn` reached 3, and all high-confidence items were vector=`bug` scoring 2, below the standard threshold.)

## Auto-fix
- `no candidate` — no allowlist signal type (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift subshape, `stalled-review`) met the certainty bar this run. The stalled dependabot PRs (#2591/#2590/#2589/#2588) and stalled human PRs (#2559/#2558/#2555) are candidates for `stalled-review` auto-comments, but the +1 stalled-review score keeps them below `score >= 3`; leaving to the manual implementer or a future run to re-fire if the stall persists.

## Self-review findings
- `open=30 (high=6, medium=21, low=3); new this run=0; acknowledged=19`
- fp-? [recurrence=14] — Phase 6 archive-move produced a 198KB `assignments-archive.json`; MCP `push_files` cannot inline a file that large; fix: PR+API-merge workaround (proven, landed #1163).
- fp-? [recurrence=11] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked but env allows it; fix: runtime push-capability probe + drop 64KiB size guard when `PUSH_METHOD=git`.
- fp-? [recurrence=5] — `action-list.json` in-progress rows decoupled from live `assignments.json` (31 vs 3); fix: Phase-2 reconcile step + self-test T32 bijection assertion.
- fp-? [recurrence=3] — implementer-liveness: 5 in-progress rows produced zero branches across 2 generations; fix: on reclaim, if `attempts>=1 ∧ branchless ∧ age>Nh` → `failed` (self-test T33).
- fp-? [recurrence=3] — Phase 2.6 buffer metric reported claimable=54/54 when real=0 (all rows terminal in archive); fix: reconcile-close + fix goal-gate GC3 formula to subtract archive task_ids.
- fp-? [recurrence=?] — 1 more high-severity open finding (see `.research/self-improvement/findings.json`).

## Open questions
- None new this run. The dispatcher's self-review backlog above needs its own PRs (dispatcher-prompt edits) — that's `.research/dispatcher-prompt.md` scope, not routine scope.

## Quiet day?
- No — 22 PRs merged, 34 backlog vectors created (28 dispatcher tier1d + 3 churn + 3 aliases collapsed), 1 plan promoted, 6 role rotations skipped (Phase 1.6 not run this cycle — see below).

## Skipped phases (with reasons)
- **Phase 1.5 (rotating expert review):** skipped — dispatcher's tier1d generator covered every review segment in the last 24h except `ppt-web-core` (which it covered on 2026-08-08). Running the routine's Phase 1.5 on top would spawn a duplicate expert subagent for the same code paths and emit at best a handful of already-caught findings. Next run will re-evaluate; `mobile-native-kmp` and `api-core` cursor dates should advance today anyway (already covered by the tier1d dispatcher signals I consolidated).
- **Phase 1.6 (pm-* role rotation + coverage upkeep):** skipped — trigger payload `buffer-low: claimable=1/72 — refill planner` explicitly prioritised buffer-refill (Phase 3 promotion) over role analysis. `pm-qa` was next (last run 2026-06-15, quite stale) — flagged for next run. `state.pm_cursor.next_index` left at 3. `state.coverage_cursor.next_index` also unchanged (still 5); coverage upkeep skipped for the same reason.
- **Phase 5 (auto-fix):** ran, `no candidate` — see *Auto-fix* section above.

## Adversarial pass results
- `plans/code-review-api-core-rls-context-pool-not-conn.md` — adversarial pass: **passed** (no rewrite, no rollback). Confirmed the module header/docstring contradiction and the glob re-export on the actual filesystem before promoting.
