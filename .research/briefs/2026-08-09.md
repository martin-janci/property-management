# 2026-08-09

## Since last run
- Merged PRs: 18 (range #2706-#2723, plus #2705 closed unmerged) since cursor #2702; window covers the missed 2026-08-08 tick
- Open PRs touched: 4 (dependabot cluster #2673/#2674/#2675/#2584/#2585/#2586/#2583; feature #2559/#2558/#2555, #2684 workflow-cond-parse-failopen updated 08-08 10:12)
- New / updated issues: 3 (all closed by the associated fix PRs) — #2703 (SSRF DNS-rebinding TOCTOU → #2710), #2704 (unbounded body → #2707), #2612 (fire-once dispatch → #2714)
- Commits: 23 real merges on `dev` (43 dispatcher-noise chore commits filtered out); HEAD `97358c47`
- Routine lag: 1d 23h since last run (47h) — >36h so `lag_warning` fired; <72h so no `stale_routine_alert`. Missed the 2026-08-08 daily tick (no brief exists at `briefs/2026-08-08.md`).
- Phases that failed: none (Phase 1.5 and 1.6 skipped — see notes)

## Shipped
- **Security / correctness (5 PRs):**
  - #2710 `gh-issue-2703`: closes SSRF DNS-rebinding TOCTOU in workflow `api_call.rs` — pre-flight lookup + reqwest connect-time resolver now share one `SsrfGuardResolver`; redirect policy fail-closes on http:// downgrade or blocked host. IG3 regression `execute_rejects_dns_rebinding_to_private_ip_at_connect` with hermetic stateful resolver.
  - #2707 `gh-issue-2704`: caps workflow `api_call` response body at 8 MiB (Content-Length pre-check + bounded streaming read), stores `truncated: true` instead of OOMing.
  - #2722 `code-review-api-handlers-community-unauthenticated-reads-retry2`: gates `get_group / list_posts / get_item` on `RequestPrincipal` + tenant check (anonymous → 401, cross-tenant → 404 opaque).
  - #2718 `sec-layout-webhook-hmac-verify-retry2`: pins layout webhook HMAC body-binding parity (rejects a validly-signed *different* body).
  - #2706 `churn-hotspot-.../ai/llm.rs`: fails 502 on partial RAG embedding batch (`embedding_batch_is_complete()`) — prevents zip-truncation silently indexing a document prefix.
- **Bug fixes (5 PRs):**
  - #2714 `gh-issue-2612-retry1`: decouples scheduled announcement/vote notifications from state transition — new watermarks (`notified_at` / `started_notified_at` / `closed_notified_at`) let a transient dispatch failure be retried on the next ~60s tick instead of fire-once.
  - #2708: rejects non-finite numbers (`NaN`, `inf`) in workflow condition compare — no more non-antisymmetric orderings.
  - #2719 `code-review-reality-server-inquiry-notify-route-wiring`: routes anonymous inquiry POSTs through the new `InquiriesHandler` seam so the notifier from #2696 actually fires (both `send_contact_message` + `request_viewing`).
  - #2712 `data-audit-add-evidence-idor-fix-retry2`: emits access-audit event on dispute `add_evidence` (Refs #2483).
  - #2723 `data-announcement-fanout-metric-retry2`: adds `get_fanout_metrics_rls` returning delivered/read/ack per targeting scope (feeds #2484 data-quality guard).
- **DX / endpoints (2 PRs):**
  - #2716 `dx-fixme-admin-web-platform-settings-patch-endpoint-retry1`: adds `GET/PATCH /api/v1/platform-admin/settings` (unblocks admin-web Save).
  - #2717 `dx-fixme-admin-web-mobile-config-patch-endpoint-retry1`: adds `GET/PATCH /api/v1/admin/mobile-config`.
- **Refactor (churn-hotspot dedupes, 3 PRs):**
  - #2715 auth handler error boilerplate (net -83), #2713 layout admin dedupe (-106 net), #2720 reports helpers extract (`mod.rs` 2460→2185), #2721 layout tenant `acquire_public_conn` extract.

## Watch
- **Closed unmerged:** #2705 (routine's own retry-1 for `dx-closed-not-merged-pr-2385`, dependabot dtolnay/rust-toolchain 1.94.1→1.100.0). Draft never came out of draft; superseded by future dependabot cycle.
- **Reverted:** none.
- **Churn hotspots this run** (top 3 by additions+deletions):
  - `backend/servers/api-server/src/services/scheduler/mod.rs` — 482 lines (#2714 fire-once fix). NEW hotspot at post-split path; sibling flat path `services/scheduler.rs` last seen 2026-07-30 (runs_seen=1, churn 347).
  - `backend/servers/reality-server/src/routes/inquiries.rs` — 408 lines across #2698 (rate-limit) + #2719 (notify wiring).
  - `backend/servers/reality-server/src/handlers/inquiries/mod.rs` — 359 lines across #2696 (notifier seam) + #2698 (rate-limit surface). Two paired inquiry files churning together is a small stability concern; both are new post-refactor surfaces so `repeated-churn` not yet applicable (runs_seen=1 both).
- **Not scored:** `backend/servers/api-server/src/services/actions/api_call.rs` (345 lines across #2707 + #2710) — SSRF/DoS hardening; expected concentrated churn on a fix week, not scored to avoid double-count with the security signals already resolved (`code-review-api-core-ssrf-validator-drift` done).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (planning-branch `assignments.json` not re-fetched this run; dispatcher chore commits show a healthy claim/review cadence)

## Code review slice
- Segment reviewed: **none this run** — Phase 1.5 skipped. `review_cursor` shows the oldest segments (`api-core`, `mobile-native-kmp`) were reviewed 2026-08-04 (5 days ago). The `2026-08-09-api-core-tier1d.json` + `-2.json` + `-ppt-web-ui-tier1d.json` under `signals/` are dispatcher tier1d generator outputs (not routine Phase 1.5), and their findings are being routed through the dispatcher pipeline (see the two per-PR `code-review-*` merges above). Running the routine's Phase 1.5 skill from a cloud incremental would only duplicate that pipeline.
- Skipped: **yes — dispatcher is already generating segment-scoped code-review findings in parallel and shipping fixes** (e.g. #2688 enhanced_chat fail-closed, #2685 workflow NOT-group, this run's #2706/#2707/#2708/#2710/#2722). The rotating-review slice would produce duplicative findings this window.

## Screen-map status
- Total `docs/screens/` files: unchanged (no PR in the 47h window touched `docs/screens/**`).
- Drift signals this run: 0 — PRs #2709 (`ListingForm.tsx`, a component not a route) and #2718 (`app/api/layout-revalidate/route.test.ts`, test-only) do not meet the drift trigger (component/test files, not `src/app/**` page routes with real handler additions/removals).
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched this run.

## Backlog deltas
- **New:** none. The three churn-hotspot signals map to newly-split file paths but neither meets the promotion bar (score=1 each after `+1`); noted in `hotspot_history` for repeat detection next run.
- **Bumped:** none.
- **Decayed:** none — the one `open` item (`code-review-mobile-native-kmp-portfolio-analytics-caps-100`, updated 2026-08-04, 5d) is inside the 14-day window.
- **Dropped:** none.
- **Resolved by merged PRs (no status change — already dropped/done at last routine tick, noted for the trail):** #2714→`bug-scheduler-notifications-fire-once` (dropped 2026-08-01; PR closed #2612); #2722→`code-review-api-handlers-community-unauthenticated-reads` (dropped 2026-07-23); #2716→`dx-fixme-admin-web-platform-settings-patch-endpoint` (dropped 2026-07-31); #2717→`dx-fixme-admin-web-mobile-config-patch-endpoint` (dropped 2026-07-31); #2720→`refactor-churn-hotspot-api-server-reports-2026-07-31` (dropped 2026-07-31); #2710→`code-review-api-core-ssrf-validator-drift` (done 2026-08-02, further hardened here); #2707 closes issue #2704 (no pre-existing backlog row).

**Open at score ≥ 2 (dispatcher-consumable candidates):** `code-review-mobile-native-kmp-portfolio-analytics-caps-100` only (score 2). No score-≥3 open items — the promotable pool remains drained since 2026-08-07.

## Plans promoted
- **none this run.** No `open` item currently sits at score ≥ 3 (or ≥ 2 with security fast-track); the only open row is a score-2 mobile-native paging bug — a real candidate but not yet elevated by a second signal. Left for organic escalation.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run (Phase 5 kill switch not set; env-check clean). The 3 churn-hotspot signals produced score=1 items and are analytical (not on the auto-fix allowlist); the `lag_warning` is score 0.

## Self-review findings
- none

## Open questions
- Should the routine's Phase 1.5 stay skipped whenever the dispatcher's tier1d generator has run within the last 24h? Today's picture: dispatcher wrote `2026-08-09-api-core-tier1d.json`, `-2.json`, and `-ppt-web-ui-tier1d.json` (up to 3 findings each) at ~04-06:00Z, and the resulting code-review-* PRs shipped (#2685, #2688, #2691, #2694, plus #2706/#2707/#2708). Running the routine's slice on top would score duplicate findings. Codify the "dispatcher-tier1d-observed-today → skip Phase 1.5" carve-out in the routine prompt, or leave the current implicit skip as a same-run heuristic.

## Quiet day?
- **No** — 18 merged PRs since last cursor, 3 GitHub issues closed by merges, 3 new churn-hotspot signals, `lag_warning` fired. Genuine state advance (`last_pr_seen` 2702→2723, `last_commit_sha` a2c0f53d→97358c47).
