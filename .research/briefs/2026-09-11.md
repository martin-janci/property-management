# 2026-09-11

## Since last run
- Merged PRs: 33 (range #2583–#2956) — most since #2921 (`last_pr_seen`); nearly all merged in the 2026-09-01→2026-09-08 window
- Open PRs touched: 15 (12 dependabot; 2 draft `needs-human-review` — #2744, #2902; 2 stalled accounting features — #2555, #2559)
- New / updated issues: 15 (10 closed, 5 open — 3 with `blocked` / `security` labels)
- Commits on `dev`: 100 (only 9 non-dispatcher; last real code merge was 2026-09-08)
- Routine lag: 10d (state.last_run_iso=2026-09-01T18:38:58Z → today 2026-09-11 UTC). Fires missed for 10 days — the cloud routine schedule appears to have been paused between runs. Dispatcher (separate loop) kept ticking; state.json on this branch fell behind.
- Phases that failed: none this run. Phase 1.5 (Rotating Expert Review) SKIPPED — the dispatcher's Tier-1d flow already produced fresh findings for every segment in the 10-day window (`.research/signals/2026-09-*-tier1d*.json`), so this run ingests those 102 findings into backlog rather than duplicating the read. Phase 1.6 (Project Management) SKIPPED — heavy multi-agent flow deferred; last run 2026-08-31 for `pm-devops` already produced the exact `pm-devops-unblock-mobile-native-cloud-builds` action-list row this brief mirrors for api-server.

## Shipped
- #2955 Purge NFC credential material from SecureStore on session change — mobile-rn logout hardening (closes #2953)
- #2956 Harden `queryKeys.test.ts` auto-discovery — fixes suite-crash risk + glob-depth gap (closes #2954)
- #2950 Purge NFC access log, QR scan history & offline feedback on session change — mobile-rn tenant isolation (closes #2947)
- #2952 Auto-discover feature-local `*Keys` factories in logout-purge coverage test (closes #2948)
- #2941 Validate JWT `exp` at cold boot in `AuthContext` — resolves tier1d finding `code-review-ppt-web-core-authctx-no-boot-expiry-check`
- #2942 Single-flight refresh + replay on 401 in `frontend/apps/ppt-web/src/lib/api.ts` — resolves `code-review-ppt-web-core-api-401-no-request-replay`
- #2943 Enforce logout-purge allowlist coverage in queryKeys — resolves `code-review-ppt-web-core-logout-purge-allowlist-fragile`
- #2938 Add invoice PDF renderer + handler test coverage (closes #2937)
- #2940 Externalize `ConfirmationDialog` loading label via i18n (`code-review-ppt-web-ui-confirm-dialog-i18n`)
- #2932 E2E 403 test for agent-review self-review guard (closes #2930)
- #2931 Report_summary snapshot-consistency test now fails on pre-fix code (closes #2929)
- #2919 Reject self-review on the agent-review endpoint (`code-review-reality-server-agent-review-self-review`)
- #2928 Snapshot-consistent `report_summary` counts + entries (closes #2924)
- #2927 Cover the update-error i18n path in `EmergencyContactDirectoryPage` tests (closes #2923)
- #2925 Route compliance audit-log count error through `db_error` (regression close of #2920)
- #2922 Typed error → HTTP status for saved-search endpoints (`code-review-reality-server-saved-search-error-string-status`)
- #2926 Remove decommissioned FCM legacy send path (`code-review-api-core-fcm-legacy-dead-endpoint`)
- #2921 Cap pagination offset on reality-server list endpoints (`code-review-reality-server-pagination-offset-overflow`)
- #2920 Sanitize DB errors in compliance handlers (`code-review-api-handlers-compliance-raw-db-error-leak`)
- #2918 i18n notification-settings strings (`code-review-ppt-web-ui-notif-settings-hardcoded-errors`)
- #2917 Localize meter dates to active i18n language (`code-review-ppt-web-ui-meters-dates-no-locale`)
- #2913 Localize `EmergencyContactDirectoryPage` error copy (`code-review-ppt-web-ui-emergency-dir-raw-err-message`)
- #2911 Sweep `clamp_limit` across remaining api-server list handlers (closes #2909)
- #2910 Bound in-memory pending SSO session map (`churn-hotspot-reality-server-sso`)
- #2916 Report real GDPR data-export counts instead of stub zeros — `[WIP]` merged
- #2915 Return true user-audit-log count in `get_user_audit_logs` — `[WIP]` merged
- #2914 Auto-refresh FCM HTTP v1 OAuth2 access token
- #2912 Reduce complexity/churn in `quiet_hours_drain.rs`
- #2583/#2585/#2586/#2673/#2935 dependabot minor-patch group bumps merged 09-04

## Watch
- Stalled review: #2555 (73d idle, martin-janci — `feat(acc): wire sent/cancelled invoice lifecycle UC-ACC-05.17`); #2559 (73d idle, martin-janci — `feat(acc): PAY by square QR endpoint UC-ACC-05.8`); #2902 (draft, 8d idle, `screen-map-drift-pr-2894-reality`); #2744 (draft, 29d idle, `fix(dispatcher): un-wedge oversize-archive push`)
- Reverted: none this window
- Churn hotspots: skipped (Phase 1.5 dispatcher-flow already covered the top segments with 102 findings across the 10-day window; this run ingests those rather than re-scanning)

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher was idle — 0 claims / 0 reviews across 8+ ticks on 2026-09-10 because the buffer was starved, not because reviews stalled)

## Code review slice
- Segment reviewed: none — this run's own Phase 1.5 was skipped in favour of ingesting the dispatcher's Tier-1d output.
- Ingested from dispatcher Tier-1d: 102 code-review findings covering 8 segments (api-core=4, api-handlers=14, mobile-native-kmp=6, mobile-rn=8, ppt-web-core=22, ppt-web-ui=18, reality-server=11, reality-web=19).
- Experts: dispatcher-side rotation (rust / kotlin / frontend / completeness / security / tester) — see individual signal `.expert` fields in `.research/signals/2026-09-*-tier1d*.json`.
- Landability breakdown of ingested findings: 78 landable-in-cloud (reality-server/reality-web/ppt-web-core/ppt-web-ui/mobile-rn — jest tests aside), 24 blocked (api-server crate blocked by #2949; mobile-native-kmp blocked by #2652).
- Next segment for the routine's OWN Phase 1.5: `ppt-web-core` — oldest cursor entry among landable segments (`state.review_cursor.ppt-web-core = 2026-08-29`; api-core/reality-server also stale but api-server-side is currently unlandable).
- Skipped: yes — dispatcher pre-emption + 10d lag catch-up. See `Since last run > Phases that failed`.

## Screen-map status
- Total `docs/screens/` files: 153 (across `ppt/` + `reality/` + `reality-mobile/`; `mobile/` still not seeded → 0).
- Drift signals this run: 0 (no merged PR in the window touched a route file without an accompanying screen-map update — the `screen-map.yml` CI has been holding).
- Orphan screens this run: 0 (no `docs/screens/*/*.md` slug lost its route in the window).
- Last `screen-map.yml` CI run on `main`: not queried this run (no drift/orphan signals to warrant it).

## Backlog deltas
- **New (open):** 99 tier-1d code-review findings ingested (score 1–2, confidence medium). Highlights (all landable-in-cloud unless flagged blocked):
  - [2] `code-review-reality-web-additionalinfo-hardcoded-en-us-price` — hardcoded `Intl.NumberFormat('en-US', …)` on public listings section
  - [2] `code-review-reality-web-agency-period-chart-unwired` — period selector state exists but chart never re-fetches
  - [2] `code-review-reality-web-home-sections-null-on-error` — home sections render `null` on API error instead of empty-state
  - [2] `code-review-ppt-web-core-mfa-verify-hardcoded-path` — hardcoded post-MFA redirect path bypasses `RETURN_TO`
  - [2] `code-review-ppt-web-ui-facilities-ismanager-stub-true` — `isManager` stub always true, bypasses permission gate
  - [2] `code-review-ppt-web-core-authed-query-roots-drift` — auth'd query roots list drifted from feature modules
  - [2] `code-review-reality-server-suggestions-hardcoded-sk-region` — public autocomplete suggestions Slovak-only
  - [2] `code-review-reality-server-import-jobs-total-page-len` — pagination `total = page.len()` misreports counts
  - [2] `code-review-api-handlers-voting-cast-no-unit-ownership` — HIGH confidence security finding (blocked by #2949; recorded but not promoted)
  - [3 blocked] `code-review-mobile-native-create-listing-stub` — HIGH confidence bug (blocked by #2652)
- **New (open, non-tier1d):**
  - [3] `security-portfolio-analytics-idor-org-scope` — Issue #2946 cross-tenant IDOR, blocked by #2949 (api-server cloud-build gate)
  - [4] `pm-devops-unblock-api-server-cloud-builds` — Issue #2949; the operator/infra unblock that would let #2944/#2945/#2946-class fixes land in cloud
- **Bumped:** none this run (all tier1d signals are first-emission).
- **Decayed:** `code-review-mobile-native-kmp-portfolio-analytics-caps-100` (2026-08-19 updated → 23d idle → −1 → 0 → dropped); `code-review-mobile-native-kmp-cancellation-swallowed` (2026-08-25 updated → 17d idle → −1 → 0 → dropped). Both were mobile-native-gated and unlandable in cloud regardless.
- **Dropped:** the 2 decayed items above.
- **Done (resolution check hits):** `code-review-ppt-web-core-authctx-no-boot-expiry-check` (PR #2941), `code-review-ppt-web-core-api-401-no-request-replay` (PR #2942), `code-review-ppt-web-core-logout-purge-allowlist-fragile` (PR #2943) — all three merged in the 09-06 batch and matched their tier1d ids verbatim in the PR title.

## Plans promoted
- `plans/code-review-reality-web-login-open-redirect-backslash.md` — the reality-web email/password login page misses the `/\evil.com` shape in its `redirect` param guard while the OAuth callback rejects all three shapes (has a tested regression); extract `isSafeInternalRedirect()` and use in both. Landable cloud-ok (reality-web). · adversarial pass: passed (source→hypothesis→approach→test-plan chain re-checked; the gap is verified against the current file at login/page.tsx:72 and the tested guard at callback/page.tsx:122-124; test would fail on `main` per IG3).
- `plans/code-review-reality-web-realtor-profile-mock-unwired.md` — the public `/realtor/[id]` route renders three hardcoded mocks, discards the route param, and its contact-form submit is a no-op; reality-server already exposes `GET /api/v1/realtors/{id}/profile`, so this is a straight wiring + i18n job. Score stacks to 3 with sibling `code-review-reality-web-realtor-profile-hardcoded-slovak` (+1) on the same file. Landable cloud-ok. · adversarial pass: passed (both stacked signals confirmed via `grep MOCK_AGENT` and `grep '"Byty"' | grep realtor`; reality-server endpoint verified at `realtors.rs:86`).

## Auto-fix
- `no candidate` — Phase 5 skipped: the two eligible allowlist categories this run were (a) the security IDOR at #2946 which is api-server-blocked so no "safe shape" auto-fix path exists, and (b) the stalled-review comment on #2555/#2559 which have already been left open by the author intentionally (accounting features awaiting a downstream schema decision — a routine ping would be noise). No `screen-map-drift`, `fixme-in-merged-code`, or `dep-update-noise/lockfile-drift` matched the certainty bar.

## Self-review findings
- none — `.research/self-improvement/findings.json` unchanged in this window (last run 2026-09-01 wrote no new findings; no findings-file exists on this branch as of today).

## Open questions
- Does the operator want the routine to bulk-open the ~78 landable tier1d findings as GitHub issues (mirroring the existing `code-review-*` PR pattern), or continue letting the dispatcher's backlog-refill promote them one at a time from `backlog.json`? Current default is the latter — no action required unless the buffer starves again.
- Should `pm-devops-unblock-api-server-cloud-builds` (score 4, dx vector, high confidence) skip the normal promotion path and become a dedicated hotfix plan this cycle, given it strands three cross-tenant IDOR fixes (#2944 closed, #2945 closed, #2946 open)? Awaiting operator judgement — the routine surfaces but does not promote operator/infra work.

## Quiet day?
- No — 33 merged PRs, 102 tier1d signals ingested, 2 landable plans promoted, 3 signals resolved against merged PRs, 1 open cross-tenant IDOR security finding surfaced. This run's whole purpose is refilling the dispatcher's buffer per the `buffer-low: claimable=8/72` fire payload.
