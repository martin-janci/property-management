# 2026-09-11

## Since last run
- Merged PRs: 17 net-new via GitHub API (range #2922–#2956); dev git log shows only 2 real code commits (`db08b23` gh-issue-2954, `65fe7f1` gh-issue-2953) — the rest are dispatcher `.research/` churn that was rewound out of dev by `research-land.yml` replays. Cursor advanced to #2956.
- Open PRs touched: 6 dependabot (#2957–#2961, #2939) + 3 long-stalled human PRs (#2555, #2559, #2744) + 1 stalled draft screen-map-drift (#2902).
- New / updated issues: 15 (range #2909–#2954). Most are `follow-up`/`from-merged-review` and were closed same-week by the dispatcher fast-path. Cursor advanced to #2954.
- Commits: 49 on `dev` since last cursor — 47 dispatcher metadata commits, 2 real code merges (see above).
- Routine lag: **9d ~9h since last run (2026-09-01T18:38:58Z)** — cloud routine appears paused while the dispatcher/planner loop keeps running.
- Phases that failed: none (Phase 1.5 + Phase 1.6 skill invocations deferred — see Auto-fix / Open questions).

## Shipped
- **#2956** gh-issue-2954: harden `queryKeys.test.ts` auto-discovery — suite-crash + glob-depth gap in the logout-purge coverage test (feature area: ppt-web-core test infra).
- **#2955** gh-issue-2953: purge NFC credential material from SecureStore on session change — mobile-rn `NFCCredentialManager` + `resetLocalData` (security: cross-tenant residue on shared devices).
- **#2952** gh-issue-2948: auto-discover feature-local `*Keys` factories in logout-purge coverage test.
- **#2950** gh-issue-2947: purge NFC access log, QR scan history & offline feedback on session change.
- **#2943** code-review-ppt-web-core-logout-purge-allowlist-fragile — enforce logout purge allowlist coverage.
- **#2942** code-review-ppt-web-core-api-401-no-request-replay — single-flight refresh + replay on 401.
- **#2941** code-review-ppt-web-core-authctx-no-boot-expiry-check — validate JWT `exp` at cold boot.
- **#2940** code-review-ppt-web-ui-confirm-dialog-i18n — externalize ConfirmationDialog loading label.
- **#2938** gh-issue-2937: invoice PDF renderer + handler test coverage.
- **#2935** dependabot npm-minor-patch group (40 updates).
- **#2932** gh-issue-2930: e2e 403 test for agent-review self-review guard.
- **#2931** gh-issue-2929: report_summary snapshot-consistency test that fails on pre-fix code.
- **#2928** gh-issue-2924: snapshot-consistent report_summary counts + entries.
- **#2927** gh-issue-2923: cover update-error i18n path in EmergencyContactDirectoryPage tests.
- **#2926** code-review-api-core-fcm-legacy-dead-endpoint — remove decommissioned FCM legacy send path.
- **#2925** code-review-api-handlers-compliance-raw-db-leak-regression — route compliance audit-log count error through `db_error`.
- **#2922** code-review-reality-server-saved-search-error-string-status — typed error enum for saved-search HTTP status.

## Watch
- **Open security IDOR:** #2946 (bug/security) `Cross-tenant IDOR (read+write): portfolio_analytics property metrics computed org_id then discarded` — still OPEN since 2026-09-06. Companion issues #2944/#2945 closed via dispatcher fast-path; #2946 wasn't picked up.
- **Blocked infra:** #2951 (mobile jest suite cannot run in cloud runner — jest-expo@56 ↔ react-native@0.87 rot) and #2949 (api-server cannot build in cloud — utoipa-swagger-ui egress-blocked) both OPEN + `blocked` since 2026-09-07.
- Stalled review: **#2555** `feat(acc) UC-ACC-05.17 wire sent/cancelled invoice lifecycle` — 45d open, 43d idle.
- Stalled review: **#2559** `feat(acc) UC-ACC-05.8 PAY by square QR endpoint` — 45d open, 43d idle.
- Stalled review: **#2744** draft `fix(dispatcher): un-wedge oversize-archive push + close retry-remint cross-id ghost` — 29d idle.
- Stalled draft: **#2902** `screen-map-drift-pr-2894-reality: reconcile saved-searches screen-map` — 11d idle.
- Churn hotspots: none this window (only 2 real code commits, 5 files total — below the top-3 hotspot threshold).

## PRs stuck in draft despite approval
- none (no APPROVED + isDraft PRs found in current open list).

## PRs with verdict=changes, no fix-round progress in 24h
- none — dispatcher `assignments.json` on `planning` branch not inspected this run (routine deferred Phase 1.6 due to catch-up scope; see Open questions).

## Code review slice
- Segment reviewed: **skipped** — Phase 1.5 (`ppt-dev-review` skill) deferred this catch-up run.
- Experts: n/a.
- Findings: 0 from routine this run (dispatcher-driven Tier-1d reviews landed independently — see recent `chore(research): dispatcher YYYY-MM-DD — Tier-1d dev-review signals` commits; those signals are in the dispatcher's own state on the planning branch, not merged into this backlog).
- Next segment: `mobile-native-kmp` (reviewed 2026-09-10 by dispatcher; next routine slice picks the oldest unreviewed — currently `api-handlers` / `reality-server` @ 2026-08-31, or `ppt-web-core` @ 2026-08-29).
- Skipped: yes — catch-up run, deferred to next routine invocation.

## Screen-map status
- Total `docs/screens/` files: unchanged — `ppt/` + `reality/` doc counts not re-scanned this run (mobile still not seeded → 0).
- Drift signals this run: 0 (the 2 real code commits touched `frontend/apps/ppt-web/src/lib/queryKeys.test.ts` + `frontend/apps/mobile/src/**` — none are route files; no `docs/screens/` update needed).
- Orphan screens this run: 0 (no route-file removals).
- Last `screen-map.yml` CI run on `main`: not queried this run.

## Backlog deltas
- **New:** none.
- **Bumped:** none.
- **Decayed:** `code-review-mobile-native-kmp-portfolio-analytics-caps-100` — 1 → 0 (no new signals in 14d, open since 2026-08-19) → dropped. `code-review-mobile-native-kmp-cancellation-swallowed` — 1 → 0 (no new signals in 14d, open since 2026-08-25) → dropped.
- **Dropped:** the two decayed rows above.

## Plans promoted
- None this run — no `open` item met the readiness gate (`score >= 3` + concrete source + concrete files + no active plan with same stem). The one score-4 item (`code-review-mobile-native-kmp-create-listing-not-wired`) is blocked by mobile-native's cloud-runner AGP-egress issue (#2652); the KMP task lacks a landable path from cloud right now.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlisted signal type this run. The two emitted signals (`lag-warning`, `stale_routine_alert`) are process/visibility signals, not allowlisted for auto-fix.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present in this repo checkout).

## Open questions
- **Cloud routine paused for 9d — is this intentional?** The dispatcher/planner loop is running (see many `dispatcher YYYY-MM-DD HH:MM` commits) but the research routine's `state.last_run_iso` hasn't advanced since 2026-09-01. If the cron is broken, this catch-up run pushes state forward but the next cadence needs a human to re-enable the trigger.
- **Phase 1.5 (`ppt-dev-review`) and Phase 1.6 (`ppt-project-management`) deferred** — the dispatcher already runs Tier-1d dev-reviews on its own cadence, and its findings live on the `planning` branch (not merged into `.research/backlog.json` on `dev`). Should this routine catch up by folding those dispatcher signals into the backlog, or leave them dispatcher-side?
- **Backlog decay dropped 2 real KMP bugs** — `portfolio-analytics-caps-100` (score 1, medium confidence) and `cancellation-swallowed` (score 1, medium confidence) were both real code-review findings that decayed because no fresh signal re-fired for 14d. If a KMP dev-review re-surfaces them, they'll come back; if not, the routine's decay just discarded them. Consider whether medium/high-confidence code-review findings should decay more slowly than the default.

## Quiet day?
- Yes — catch-up run. 17 merged PRs since the last brief were all already handled by the dispatcher loop (they don't generate net-new backlog work); the 2 emitted signals are process-visibility markers with `score_delta: 0`. `state.stats.quiet_days` bumped 3 → 4.
