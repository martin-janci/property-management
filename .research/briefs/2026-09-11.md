# 2026-09-11

_Recovery run after a 9-day routine silence (2026-09-01 → 2026-09-11). Dispatcher (separate cron on planning branch) kept ticking throughout — this brief resyncs the routine's state cursors, applies overdue decay to open items, and records the buffer-low observation delivered by the trigger payload. Phase 1.5 (rotating expert review) and Phase 1.6 (project-management) are deliberately skipped this run: the dispatcher's Tier-1d generator has been producing per-segment code-review signals daily through 2026-09-11 (see `.research/signals/2026-09-1{0,1}-*-tier1d.json`), so re-invoking `ppt-dev-review` here would double-count. Full PM synthesis defers to the next scheduled run._

## Since last run
- Merged PRs: **17** (range #2922–#2956; excludes #2929 which is an issue). Highest merged: #2956.
- Open PRs touched: 11 (10 dependabot + 1 draft #2902 screen-map-drift reconciliation, unchanged since 2026-09-03).
- New / updated issues: 17 (14 closed follow-ups, 3 still open: #2946, #2949, #2951).
- Commits: 101 on `dev` (mix of ~35 dispatcher chore commits + ~15 dev-review PR merges + backfill).
- Routine lag: **9d 13h** since last run (>36h threshold; >=3d = stale_routine_alert also fired). Cloud routine cron likely paused between 2026-09-02 and 2026-09-10.
- Phases that failed: none — Phase 1.5 and 1.6 skipped by design for this recovery run (not failure); all other phases succeeded.

## Shipped
- #2956 gh-issue-2954 — harden `queryKeys.test.ts` auto-discovery (suite-crash risk + glob-depth gap).
- #2955 gh-issue-2953 — purge NFC credential material from SecureStore on session change.
- #2952 gh-issue-2948 — auto-discover feature-local *Keys factories in logout-purge coverage test.
- #2950 gh-issue-2947 — purge NFC access log, QR scan history & offline feedback on session change.
- #2943 code-review-ppt-web-core-logout-purge-allowlist-fragile — enforce logout purge allowlist coverage.
- #2942 code-review-ppt-web-core-api-401-no-request-replay — single-flight refresh + replay on 401.
- #2941 code-review-ppt-web-core-authctx-no-boot-expiry-check — validate JWT exp at cold boot.
- #2940 code-review-ppt-web-ui-confirm-dialog-i18n — externalize ConfirmationDialog loading label via i18n.
- #2938 gh-issue-2937 — add invoice PDF renderer + handler test coverage.
- #2935 chore(deps) — npm-minor-patch group across 1 directory (40 updates).
- #2932 gh-issue-2930 — add e2e 403 test for agent-review self-review guard.
- #2931 gh-issue-2929 — make report_summary snapshot-consistency test fail on pre-fix code.
- #2928 gh-issue-2924 — snapshot-consistent report_summary counts + entries.
- #2927 gh-issue-2923 — cover update-error i18n path in EmergencyContactDirectoryPage tests.
- #2926 code-review-api-core-fcm-legacy-dead-endpoint — remove decommissioned FCM legacy send path.
- #2925 code-review-api-handlers-compliance-raw-db-leak-regression — route compliance audit-log count error through `db_error`.
- #2922 code-review-reality-server-saved-search-error-string-status — derive saved-search HTTP status from a typed error enum.

## Watch
- Stalled review: #2902 draft (screen-map-drift-pr-2894-reality reconciliation) — untouched since 2026-09-03 (~8d). Not yet ready-for-review; branch remains contaminated per 2026-09-01 note.
- Reverted: none this window.
- Churn hotspots: routine skipped churn-hotspot analysis this run (recovery mode — dispatcher already surfaced Tier-1d hotspots per-segment; adding routine hotspots would double-count). Dispatcher-heavy `.research/` churn dominates commit log.

## PRs stuck in draft despite approval
- #2902 — draft, not approved (dispatcher-owned, branch contaminated). Not a drafted-yet-approved case.
- #2744 fix(dispatcher) — unchanged from prior briefs (self-approval blocker).
- otherwise none.

## PRs with verdict=changes, no fix-round progress in 24h
- planning branch's `assignments.json`: 2 rows both in `review` state (checked via `gh api ... /assignments.json?ref=planning`); neither has `verdict=changes`.
- none.

## Code review slice
- Segment reviewed: **none this run** — deliberately skipped for the recovery pass. Dispatcher's own Tier-1d generator has been producing per-segment findings daily:
  - `signals/2026-09-10-mobile-rn-tier1d.json`, `-ppt-web-core-tier1d.json`, `-reality-server-tier1d.json`, `-reality-web-tier1d.json`
  - `signals/2026-09-11-api-handlers-tier1d.json`
- Experts covered by dispatcher: rust (api-handlers, reality-server), frontend (ppt-web-core, reality-web), react-native (mobile-rn).
- Findings this run: 0 new routine-emitted `code-review-finding` signals (see Backlog deltas for the dispatcher-authored code-review items already landed on `dev`).
- Next segment: routine will re-enter its rotation from `api-core` at the next scheduled run (last-reviewed 2026-09-01, oldest not covered by dispatcher this window).
- Skipped: yes — recovery run, dispatcher's parallel review stream is sufficient coverage.

## Screen-map status
- Total `docs/screens/` files: unchanged (ppt + reality; mobile still 0).
- Drift signals this run: 0 (recovery run — no per-PR file walk; the 17 merged PRs did not touch route files that would drift screen docs, per label + title inspection).
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched this recovery run.

## Backlog deltas
- **New:** none this run (recovery run defers signal-to-backlog promotion to next scheduled routine).
- **Bumped:** none.
- **Decayed:** 2 items (both `code-review-mobile-native-kmp-*`):
  - `code-review-mobile-native-kmp-portfolio-analytics-caps-100` — score 1→0 → `dropped` (updated_at 2026-08-19, 23d old).
  - `code-review-mobile-native-kmp-cancellation-swallowed` — score 1→0 → `dropped` (updated_at 2026-08-25, 17d old).
- **Dropped:** the 2 above transitioned via decay-to-zero → `dropped`.
- **Resolved:** none this run (no matching PR bodies / evidence-gone paths; dispatcher's PR-body-match resolutions have already landed in `backlog.json` earlier).

## Plans promoted
- **0 new plans this run.** Only `score >= 3 && confidence == high && status == open && !plan` candidate remains `code-review-mobile-native-kmp-create-listing-not-wired` (score 4, high). Mobile-native/KMP is cloud-unlandable per issue #2652 (AGP dl.google.com egress blocked); promoting an unlandable plan won't refill the dispatcher's *landable* buffer. Consistency with 2026-09-01 decision preferred.

## Auto-fix
- `no candidate` — recovery run; no signals in the allowlist (screen-map-drift, screen-map-orphan, fixme-in-merged-code, lockfile-bump, stalled-review) surfaced this run. The 10 dependabot PRs open on `dev` are legitimate group-updates, not the lockfile-drift sub-shape auto-fix targets.

## Self-review findings
- none (`.research/self-improvement/findings.json` absent).

## Open questions
- **Routine cron paused ~9 days.** Root cause unknown from this session's vantage (routine is on `main`; dispatcher on `planning`). Trigger payload confirms scheduler is armed *now* (this run fired), but the 2026-09-02 → 2026-09-10 gap needs operator inspection: check cloud routine's crontab and `research-land.yml` action status.
- **Buffer-low with all opens cloud-blocked.** Trigger payload said `claimable=8/72`; after this run's decays, non-terminal opens are 3 items, all mobile-native/KMP. Structural fix requires either (a) unblocking cloud KMP builds (AGP egress allowlist — see #2652), or (b) routine's Phase 1.5 running on non-KMP segments to refill non-KMP work. Consider re-enabling Phase 1.5 explicitly next run.

## Quiet day?
- **No** — 17 merged PRs, 2 backlog decays-to-dropped, 15 signals emitted (2 process signals + 11 dep-update-noise + decay-batch + buffer-low observation). Recovery run rather than incremental run, but not quiet.

## Goal violations
- **G10 (pre-existing, healed this run):** on-disk `backlog.md` at HEAD (2026-09-01 regen) had drifted 11 rows from `backlog.json` — dispatcher's `backlog-reconcile.sh` had updated statuses in JSON without regenerating the rendered view. This run's re-render fixes that. Root cause: dispatcher lacks a post-reconcile render step; routine's next run enforces byte-identity so drift auto-heals.
- No new goal violations this run — G1-G17 checks embedded in `signals/2026-09-11.json.goal_checks[]` after Phase 6.
