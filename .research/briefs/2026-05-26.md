# 2026-05-26

## Since last run
- Merged PRs: 7 (range #503–#511; #507/#509/#512 not merged)
- Open PRs touched: 16 (#509, #513–#515, #529–#540)
- New / updated issues: 13 (#516–#528, all `follow-up`/`from-merged-review` from the post-merge review of PR #529)
- Commits: derived from the 7 merged-PR diffs (squash-merges + CI version bumps on `dev`)
- Routine lag: ~13h since last run — on schedule
- Phases that failed: none

## Shipped
- #503 gap-10b-4 — system announcements admin dashboard (admin-web SystemAnnouncementsPage + api-client + sitemap); shipped *with* a new screen doc `docs/screens/ppt/admin-system-announcements.md`
- #504 gap-6-4 — PATCH `/api/v1/announcements/{id}/pin` + composite sort index migration (POST kept for back-compat)
- #505 gap-6-3 — POST/GET announcement comments API; routes threaded comments through the RLS connection + adds a `#[cfg(test)]` module (sanitize_markdown / comment validation)
- #506 gap-80-2 — dispute state machine (filed→under_review→resolved) with transition enforcement + 422 on illegal transitions — **no test file** (→ backlog `test-gap-dispute-fsm-no-tests`)
- #508 pm-qa — IDOR fix: `respond_to_inquiry` now enforces realtor ownership (reality-server realtors.rs). Left the sibling `mark_inquiry_read` route unscoped (→ new security item)
- #510 pm-qa — `.github/workflows/security-test-gate.yml`: blocks `security`-labelled non-draft PRs that ship no test file
- #511 gap-6-4 — pinned-announcements sticky band (ppt-web + mobile) **and** the App.tsx dispute-route wiring (DisputeDetailPage + `/disputes/:id/mediation`); updated `docs/screens/ppt/{announcements,dispute-detail}.md`

## Watch
- Stalled review: none (all open PRs created within the last 7 days)
- Reverted: none
- Churn hotspots: `frontend/apps/ppt-web/src/App.tsx` (318 lines this run, runs_seen=2); `backend/servers/api-server/src/routes/announcements.rs` (167 lines, runs_seen=2); `frontend/apps/mobile/src/screens/announcements/AnnouncementsScreen.tsx` (139 lines, runs_seen=1, one-time feature add — no item)

## Code review slice
- Segment reviewed: reality-server (reason: churn-aligned / issue #519 lead — verified the post-merge-review claim against the code)
- Experts: rust + security
- Findings: 2 — `code-review-reality-server-realtors-mark-inquiry-read-idor` (+3, security) and `code-review-api-core-dispute-fsm-no-tests` (+2, test-gap)
- Next segment: reality-web | mobile-rn | mobile-native-kmp (all `null` — oldest-unreviewed, highest selection priority next run)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 106 (ppt 60 + reality 46; `mobile/` not yet seeded → 0)
- Drift signals this run: 0 (the two route-touching products updated their docs — #511 touched `ppt-web/App.tsx` and updated `docs/screens/ppt/*.md`; #503 is admin-web, outside the drift trigger set, and added a screen doc anyway)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: unknown — `gh` is unavailable in the cloud sandbox (no GitHub remote configured for it); status not fetched this run

## Backlog deltas
- **New:** [3] IDOR: reality-server realtors `mark_inquiry_read` flips any realtor's inquiry by ID with no owner scoping — security, **promoted**
- **New:** [2] Dispute state machine (PR #506) shipped with no tests + no org predicate on update_status — test-gap
- **Bumped:** [5→6] Add regression tests for inquiry mark_as_read IDOR (PR #497) — PR #507 (the regression test) was closed unmerged; gap still open
- **Bumped:** [1→2] Reduce App.tsx route-aggregator coupling — repeated-churn (PR #511, runs_seen=2)
- **Bumped:** [1→2] announcements.rs churn-hot — repeated-churn (PR #504/#505, runs_seen=2)
- **Decayed:** none (no open item older than 14 days)
- **Dropped:** none

## Plans promoted
- `plans/security-realtors-mark-inquiry-read-idor.md` — bind `principal` + call the existing scoped `mark_inquiry_read_for_realtor`, return 404 on non-owned rows; regression test extends `tests/inquiry_idor_tests.rs` · adversarial pass: passed (source→hypothesis→approach→test chain verified: handler discards principal → unscoped UPDATE → fix calls scoped sibling → B→404 test fails on `main`, passes after)

## Auto-fix
- `no candidate` — no allowlisted signal type (screen-map-drift/orphan, fixme-in-merged-code, dep lockfile-drift, stalled-review) fired; the realtors IDOR is a code change and goes to the manual plan path

## Open questions
- Dispute `update_status` (PR #506) runs `WHERE id=$2 AND status=$3` on the raw pool with no `organization_id` predicate (issue #520). Is the `disputes` table covered by an RLS policy on this path? If not, a manager in org A may transition org B's dispute — escalate `test-gap-dispute-fsm-no-tests` to a security vector once confirmed.
- Phase 1.6 deep PM role-rotation was deferred this run (cloud budget); only the observe→decide→promote core ran. `state.pm_cursor` left unchanged; management artifacts untouched.

## Quiet day?
- No
