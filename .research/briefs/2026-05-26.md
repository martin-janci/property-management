# 2026-05-26

> Second run of the day (evening, 22:37Z). Overwrites the morning 03:12Z brief per the same-day-rerun convergence rule. Window: merged PRs #513–#562 since `last_run_iso` 2026-05-26T03:12:00Z.

## Since last run
- Merged PRs: 21 (range #513–#562)
- Open PRs touched: 6 (#555 mediation-ui, #557/#556 document UI drafts, #558 CLAUDE.md docs, #539/#540 mobile drafts)
- New / updated issues: 0 new; ~13 follow-up issues (#516–#528) closed — resolved by the cleanup PRs #548/#549/#552/#553/#554
- Commits: ~21 squash-merges on `dev` plus version-bump + dispatcher commits
- Routine lag: 19h since last run — on schedule
- Phases that failed: none (`state.last_commit_sha` was a squash artifact absent from this fresh branch; churn recovered from per-PR file stats — no cursor stalled)

## Shipped
- #548 backend cleanup (12 follow-ups) — **resolved two backlog items**: realtors `mark_inquiry_read` now binds principal + scoped query, and added `inquiry_mark_read_idor_tests.rs`; also added `dispute_cross_tenant_tests.rs`
- #532 e-signature config fail-closed — panics on restart if `ESIGN_TOKEN_SECRET`/`ESIGN_WEBHOOK_SECRET` unset (4 inline tests added; deploy reminder surfaced as backlog item)
- #538 Airbnb channel sync backend + #534/#544 Booking.com channel sync/OTA — **introduced the install.rs BOLA flagged below**
- #515 push-fanout worker (new file); #514 dispute mediation API; #513 e-signature UI; #550/#551 document download/preview + folder API; #547 report execution-history UI; #541 contextual help UI
- #559–#562 dispatcher hardening (auto-promote drafts, sandbox reclaim, spec-gap fixes); #554 reality-mobile screen-maps now pass `/screens validate`

## Watch
- Stalled review: none (all 6 open PRs created today)
- Reverted: none this run
- Churn hotspots (this run, after exclusions): `integrations/install.rs` (877 lines, runs_seen=1 — active Booking/Airbnb feature work), `admin-web/SystemAnnouncementsPage.tsx` (869, runs_seen=1 — planned #521 split refactor), `services/push_fanout.rs` (689, runs_seen=1 — brand-new #515 file). All three are active-development churn, not instability → logged, no backlog item.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: api-handlers (reason: churn-aligned — 3/5 churn files live under api-server routes/services)
- Experts: rust + security
- Findings: 3 (see Backlog deltas; the install.rs Airbnb BOLA, the in-file authz inconsistency, the OAuth-state debug log)
- Next segment: reality-web | mobile-rn | mobile-native-kmp (all still `null` — oldest-unreviewed, top priority next run)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 119 (ppt 61 + reality 46 + reality-mobile 12; `mobile/` RN app still not seeded → 0)
- Drift signals this run: 1 (PR #547 added a report execution-history route without a ppt screen doc; PR #549 fired but was suppressed as a false positive — App.tsx auth-context edit, no route change)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: completed success (2026-05-23)

## Backlog deltas
- **New:** [5] BOLA: Airbnb/Booking integration handlers in install.rs missing tenant check — **promoted to a plan**
- **New:** [2] Screen-map drift: PR #547 report execution-history route without a ppt screen doc (review candidate)
- **New:** [2] e-signature fail-closed (#532): ESIGN secrets must be set in staging/prod or api-server panics on restart (deploy reminder)
- **New:** [1] Integrations install.rs logs the OAuth state (CSRF) token at debug level (minor)
- **Done:** Add regression tests for inquiry mark_as_read IDOR (test-gap, was score 6) — resolved by #548 (`inquiry_mark_read_idor_tests.rs`); plan archived
- **Done:** IDOR realtors mark_inquiry_read (security, score 3) — resolved by #548 (principal bound + scoped query); plan archived
- **Updated (partial):** Dispute FSM no-tests — cross-tenant half now covered by #514/#548 tests; residual is the pure `is_valid_transition` table unit test (still none), narrowed to a low-value test-gap
- **Bumped / Decayed / Dropped:** none (no open item older than 14 days this run)

## Plans promoted
- `plans/security-integrations-install-bola.md` — fix the cross-tenant BOLA on the Airbnb/Booking integration handlers in `install.rs` (add the `auth.tenant_id == path.org_id` guard the same file already uses at :1128) · adversarial pass: passed

## Auto-fix
- `no candidate` — the only allowlisted signal (screen-map-drift-pr-547-ppt) is confidence=medium with backlog score 2, below the high-confidence + score≥3 certainty bar

## Open questions
- **Deploy ops:** are `ESIGN_TOKEN_SECRET` (≥32 bytes) and `ESIGN_WEBHOOK_SECRET` actually provisioned in staging + production? If not, the next api-server restart after #532 panics by design (fail-closed). Not verifiable from the repo — needs an operator check.
- **Screen-map:** does an existing `docs/screens/ppt/` reports screen doc already cover the new schedule-detail / execution-history view (PR #547), or is a new entry warranted?
- **Pre-existing plan format:** `plans/p0-10-mock-route-wiring.md` (hand-authored, dev-team-review) uses a custom YAML-frontmatter format rather than the routine's 15-section template — it's untouched this run so G7 (new-plan-only) doesn't gate it, but it would fail the template heading check if re-promoted. Leave as-is or reformat?

> Gate summary: G1–G17 all pass. No application code touched (G8=0). No secrets/Telegram token (G9/G17=0). 1 plan promoted, 0 auto-fixes.

## Quiet day?
- No
