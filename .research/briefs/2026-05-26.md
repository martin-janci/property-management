# 2026-05-26

> **Same-day incremental rerun.** The 03:12Z run covered merges #503–#511 and
> promoted the realtors-IDOR plan. This run (≈22:31Z) processes the rest of
> the day's merges (#513–#562) — overwriting today's brief/signals to converge.
> The headline: the dispatcher's cleanup PRs (#548/#549/#552/#553/#554) closed
> ~25 follow-up issues, resolving three open backlog items including both
> `ready` plans.

## Since last run
- Merged PRs: 21 (range #513–#562; #507/#509/#512 still unmerged)
- Open PRs touched: 6 (#539, #540, #555, #556, #557, #558 — all <7 days old)
- New / updated issues: 0 new; ~25 follow-up issues (#438–#528) **closed** by the cleanup PRs
- Commits: ~21 squash-merges on `dev` + CI version bumps (→ 0.2.765)
- Routine lag: ~19h since the 03:12Z run — on schedule (same day)
- Phases that failed: none

## Shipped
- #513 gap-84-2 — e-signature request flow + status badge in ppt-web lease detail
- #514 gap-80-3 — `PATCH /disputes/{id}/resolve` + `/mediation-notes` (+ `dispute_cross_org_idor_tests.rs`)
- #515 gap-8a-3 — push-notification fanout worker (Epic 8A-3); BLPOP drain deferred → new backlog item
- #532 fix(security) — e-sig config fail-closed (panics in prod without `ESIGN_TOKEN_SECRET`), webhook 503 + constant-time compare, real `EmailService` wired into scheduler; 4 inline IG3 tests
- #534/#538/#544 — Booking.com + Airbnb channel sync (listing push, reservation pull, OTA push-availability/rates, webhook handler); webhook idempotency TODO → new backlog item
- #541 gap-10b-7 — contextual help tooltip/sidebar in admin-web (+ `docs/screens/ppt/admin-contextual-help.md`)
- #547 gap-81-2 — report execution-history table in ScheduleDetailPage (→ screen-map drift item)
- #548 fix(backend) — **closes 12 follow-ups**: OAuth revoke filter, realtors `mark_inquiry_read` IDOR, pin TOCTOU, dispute org-tenancy, WS JWT expiry, e-sig HMAC, audit hash, voice-device precheck, bounded notification dispatch, principal lint + tests
- #549 fix(ppt-web/admin-web) — **closes 7 follow-ups**: FolderTree toasts, Docker API origin, ProtectedRoute role-from-JWT (+9 tests), DocumentSharePanel UUID/confirm, central auth-fetch seam, dispute no-op handlers, SystemAnnouncementsPage split (867 LoC → 9 files, 16 tests)
- #550/#551 gap-7a — document download/preview presigned URLs + folder API
- #552 fix(infra/ci) — Caddy shared-worktree `/api/*` upstream, security-test-gate hardening + self-tests, dispatcher commit-scope guard
- #553 fix(mobile) — iOS per-env bundle/versioning, RN env sentinel, iOS deep-link/SSO hardening (nonce + host allow-list)
- #554 fix(docs) — reality-mobile screen-maps now pass `/screens validate` (12 docs)
- #559/#560/#561/#562 — dispatcher/research-routine hardening (auto-promote drafts, stacked-PR refusal, scope guard, 5 operator-feedback spec gaps)

## Watch
- Stalled review: none (all 6 open PRs created within the last 7 days)
- Reverted: none
- Churn hotspots this run (all one-time feature build-outs, runs_seen=1, no items): `integrations/install.rs` (877, channel-sync), `admin-web/SystemAnnouncementsPage.tsx` (869, #549 split), `services/push_fanout.rs` (689, new worker). `App.tsx` touched again by #547+#549 (continued coupling — evidence added to `refactor-app-tsx-route-coupling`, not re-scored: below top-3 this run).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher actively cycling — last commit 22:13Z)

## Code review slice
- Segment reviewed: mobile-native-kmp (reason: churn-aligned — #553/#522 landed fresh iOS deep-link/SSO code into a never-reviewed segment)
- Experts: swift/kotlin + security
- Findings: 0 actionable — `consumeSsoState` uses a single-use 32-byte `SecRandomCopyBytes` CSRF nonce with empty-state rejection; `DeepLinkHandler` enforces a universal-link host allow-list; `Route.requiresAuth` gates entry. Solid.
- Next segment: reality-web | mobile-rn (both still `null` — oldest-unreviewed)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 119 (ppt 61 + reality 46 + reality-mobile 12; `mobile/` RN app still not seeded → 0)
- Drift signals this run: 1 real (#547 report-history route, no ppt screen doc) + 1 suppressed (#549 App.tsx = useCallback wrappers, not new screens)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: completed/success @ 2026-05-23T09:20Z

## Backlog deltas
- **New:** [2] Airbnb webhook at-least-once delivery enqueues duplicate SYNC_EXTERNAL jobs — bug (PR #538, webhook.rs:1028)
- **New:** [2] PushFanoutWorker BLPOP queue-drain deferred — Redis path is a logging no-op — dx (PR #515, push_fanout.rs:621)
- **New:** [2] Screen-map drift: report execution-history route added without a ppt screen doc — test-gap (PR #547)
- **Resolved (done):** [3] realtors `mark_inquiry_read` IDOR — PR #548 (scoped call + `inquiry_mark_read_idor_tests.rs`); plan archived
- **Resolved (done):** [6] inquiry mark_as_read IDOR regression tests — PR #548 added `inquiry_mark_read_idor_tests.rs`; plan archived
- **Resolved (done):** [2] dispute FSM no-tests — PR #514 + #548 (cross-org/cross-tenant tests + org-tenancy on `update_status`)
- **Evidence-only (no rescore):** `refactor-app-tsx-route-coupling`, `refactor-announcements-rs-hot`, `refactor-ppt-web-untranslated-strings`
- **Decayed:** none (no open item older than 14 days)
- **Dropped:** none

## Plans promoted
- none this run — the three high-value items were *resolved*, not promoted; both prior `ready` plans (`security-realtors-mark-inquiry-read-idor`, `test-gap-inquiry-idor-regression`) moved to `plans/_archive/`. The three new items sit at score 2 (below the promotion bar); the webhook-dup bug is the strongest candidate but needs a second signal or a manual bump first.

## Auto-fix
- `no candidate` — the eligible allowlist signals this run were `fixme-in-merged-code` (#538/#515, confidence=high but item score 2 < 3) and `screen-map-drift` (#547, confidence=medium). None cleared the confidence=high + score≥3 bar; all stay on the manual-implementer path.

## Open questions
- The Airbnb webhook dup-job TODO (#538) is a real idempotency gap but only score 2. Worth a manual bump to promote a dedup plan now, or wait for it to recur? It's pre-GA channel-sync code, so blast radius is currently low.
- Phase 1.6 PM role-rotation deferred again this run (cloud budget; same-day rerun). `state.pm_cursor` / `coverage_cursor` left unchanged; management artifacts untouched. Worth a dedicated `pm-full` run now that channel-sync (Epic 83) and disputes (Epic 80) have landed substantial scope.

## Quiet day?
- No
