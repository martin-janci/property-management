# 2026-07-27

## Since last run
- Merged PRs: 30 (numbers > `last_pr_seen=2512`, range #2514–#2552; +2 older PRs — #2440, #2502 — also landed in the window but were already known by number)
- Open PRs touched: 7 (all authored by `martin-janci`, dispatcher-driven)
- New / updated issues: 2 opened (#2528 booking-webhook hardening, #2532 layout event emission); 7 closed (#2529/#2530/#2531/#2533/#2534/#2536/#2537 — post-merge-review follow-ups landed / superseded)
- Commits: 50 on `dev` (35 non-research, 15 dispatcher-chore)
- Routine lag: **2d 71h** since last run — `lag_warning-2026-07-27` emitted (+0). Cloud routine cadence has drifted; `stale_routine_alert` fires as well (last_run_iso ≥ 3d).
- Phases that failed: none

## Shipped
- **#2544** feat(UC-14): instrument full signup funnel, not just onboarding tour — closes #2530
- **#2548** fix(UC-37): close join_group/leave_group cross-tenant IDOR — closes the #2529 finding
- **#2547** fix(api-server): schedule support_tooling_events retention prune (closes #2531) — **ships WITHOUT an api-server test** → `hotfix-no-test-pr-2547` +2 this run
- **#2546** fix(api-client): surface orphaned S3 object on failed direct upload (closes #2534)
- **#2545** gh-issue-2536: fix screen-map extractEpics catalog source so epic refs validate
- **#2550** feat(db): back dispute KPIs with SQL + structural status transitions — closes #2533
- **#2551** test(api-server): add cross-tenant IDOR + error-branch tests for lease-abstraction import — closes #2537
- **#2552** test(api-server): restore messaging_happy_paths seed scope (BIT-658)
- **#2541** feat(reality-web): add `listing.viewed` analytics event with view-source, filter-state, session context
- **#2526** feat(epic-10A): OAuth token-usage analytics data layer
- **#2515** feat(admin-web): instrument onboarding/signup funnel analytics (10B.6)
- 15+ test-un-quarantine PRs (BIT-565/566/567/570/571/573/574/585/588/609/610/630/647): #2502/#2507/#2508/#2510/#2511/#2514/#2516/#2518/#2519/#2520/#2522/#2525/#2539/#2540/#2542/#2543
- Doc / infra tidy: #2523 (nextest partition runbook), #2524 (support-staff read audit schema), #2527 (admin-web layout-editor extract-styles), #2535 (dependabot cargo-minor-patch runbook), #2538 (verify-gate adoption check-in)
- **Note on PR #2511** — the un-quarantine commit is unusually large (4374 files / 1.17M insertions in one commit). Per-line FIXME/TODO derivation was suppressed for that commit only; would otherwise produce a huge false-positive noise wave (see `signals/2026-07-27.json` notes).

## Watch
- Stalled review: **none** — every open PR has been touched inside the last 6 days (#2433 at 6d is the closest to the 7d threshold, still under). No `stalled-review` signal fired.
- Reverted: none — no revert PR in the window.
- Churn hotspots (top-3 by lines-changed this window):
  - `backend/servers/api-server/src/routes/reports.rs` (3329 lines, `runs_seen=3` after this run) — repeated-churn +1 (**new** at this file; base churn-hotspot re-emit is cumulative-only +0).
  - `backend/crates/integrations/src/booking/mod.rs` (3185 lines, `runs_seen=2`) — **new** churn-hotspot +1.
  - `backend/servers/api-server/src/routes/auth.rs` (2950 lines, `runs_seen=4`) — cumulative re-emit +0 on both churn-hotspot AND repeated-churn (both signal-ids already in `seen_signals`).
- No risky-churn: fix PRs #2546/#2547/#2548 did not intersect the hotspot set.

## PRs stuck in draft despite approval
- **#2549** — `gh-issue-2532: wire layout publish/webhook/revalidate event emission + sink` — approved 2026-07-24T22:16Z (pm-backend verdict=approve), still `draft: true`. Last update **~53h ago**. Blocker per the review body: a pre-existing base-branch biome lint red (`apps/reality-web/src/components/listings/ListingDetailContent.tsx` unused `t`) — needs a base-branch fix / rebase before it can leave draft. Not counted as `stalled-review` (has approval; not the missing-`reviewDecision` case the signal targets).

## PRs with verdict=changes, no fix-round progress in 24h
- none — all `status=review` rows in `.research/management/assignments.json` carry `verdict=approve` (PR #2482, PR #2549) or `reviewer_summary=n/a` (PR #2553, freshly minted).

## Code review slice
- Segment reviewed: **mobile-native-kmp** (reason: oldest-unreviewed at 2026-07-16 — 11d stale). Churn hotspots this run were all backend, so the churn-aligned pick would have been `api-handlers` or `api-core` — both were reviewed within the last week, so oldest-unreviewed won selection.
- Experts: kotlin (single expert; segment is KMP-shared + Android + iOS)
- Findings: 2 — one **High** (Android SSO deep-link missing CSRF state), one **Low** (PortfolioAnalytics inquiriesTrend drops zero-view days). Both surface as `code-review-finding` signals in `signals/2026-07-27.json`.
- Next segment: `reality-web` (last reviewed 2026-07-17, will be oldest after this run advances `mobile-native-kmp`).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: 153 (ppt/=106, reality/=47; mobile/ still 0 — deliberately excluded from drift detection until seeded).
- Drift signals this run: **0** — no merged PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**` in the window.
- Orphan screens this run: 0 (heuristic did not flag any).
- Last `screen-map.yml` CI run on `main`: not queried (no drift signals to root-cause; skipped to save API budget).

## Backlog deltas
- **New:** [3] `code-review-mobile-native-kmp-android-sso-deeplink-missing-csrf-state` (security, promoted to plan this run) — see `backlog.md`
- **New:** [2] `refactor-churn-hotspot-api-server-reports-2026-07-27` — reports.rs repeated-churn (runs_seen 2→3)
- **New:** [2] `bug-hotfix-no-test-pr-2547` — scheduler retention prune fix without a test
- **New:** [1] `code-review-mobile-native-kmp-portfolio-analytics-drops-view-zero-days` — trend-axis set-difference bug
- **New:** [1] `refactor-churn-hotspot-integrations-booking-mod-rs-2026-07-27` — new booking-mod churn spike
- **Bumped:** none — no existing open item overlapped with a scoring signal this run (auth.rs previous item is closed; cumulative signals delta=0).
- **Decayed:** none — every open item has `updated_at >= 2026-07-20` (< 14d threshold).
- **Dropped:** none — no items hit score 0 this run.

## Plans promoted
- `plans/code-review-mobile-native-kmp-android-sso-deeplink-missing-csrf-state.md` — Android SSO deep-link needs a CSRF `state` nonce to match iOS parity; concrete file:line evidence, ADB-injectable repro, instrumented test would fail on `dev`. · adversarial pass: passed
- No second plan this run: the remaining ready candidate at score 2 (`bug-hotfix-no-test-pr-2547`, `refactor-churn-hotspot-api-server-reports-2026-07-27`, etc.) is below the score=3 promotion bar (security fast-track only helps the security-vector item, which is exactly the one already promoted).

## Auto-fix
- **no candidate** — the sole promoted plan is `vector=security` requiring an Android device (C5, local-only); the allowlist covers only `screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / `dep-update-noise` (lockfile-drift) / `stalled-review`, and none of those types had a qualifying signal with `confidence=high` AND `score >= 3` this run.

## Self-review findings
- open=21 (high=4, medium=13, low=4); new this run=0; acknowledged=19
- fp-mcp-push-cannot-inline-large-archive [recurrence=10] — dispatcher archive-push exceeds mcp push_files inline limit → merged-total undercount; fix: adopt push-to-branch → PR → server-side merge as the primary large-file push mechanism in Phase 6
- fp-hardcoded-proxy-403-push-assumption [recurrence=6] — dispatcher assumes `git push` is proxy-blocked → forces MCP path even where direct push works; fix: runtime probe at Phase 1, set PUSH_METHOD=git on success, bypass the 64KiB inline guard
- fp-action-list-open-count-blind-to-archive [recurrence=3] — Phase 2.6 buffer-guard subtracts only active assignment ids (not archived terminal ids), so buffer nears starvation while metric reports healthy; fix: gc1-reconcile auto-close open items terminal-in-archive AND fix GC3 formula to subtract archive ids
- fp-tier2-planner-auth-swallow [recurrence=2] — Tier-2 401/403 from planner kick folded into generic "kick failed" log; fix: branch on status, treat auth failures as OPERATOR-CONFIG escalation surfaced loudly in Phase 7

## Open questions
- **PR #2549 (layout webhook emit + sink) is approved but stuck in draft** because of a pre-existing base-branch biome lint red (`apps/reality-web/src/components/listings/ListingDetailContent.tsx` unused `t`). Should the routine open a follow-up `auto-fix` PR for the base lint fix, or leave it for the manual implementer? — the drift is in one line and the fix is a mechanical `_t`/removal; but it's outside the current auto-fix allowlist.
- **`docs/screens/mobile/` still empty.** Should we bootstrap it (unblocks drift detection on the RN mobile app) or leave it deferred? — no user signal yet.

## Quiet day?
- No. 30 PRs merged, 5 new backlog vectors, 1 plan promoted, 2 code-review findings surfaced, `hotfix-no-test-pr-2547` flagged, and a **stale-routine alert** for the 3d cloud-routine gap. Not close to quiet.
