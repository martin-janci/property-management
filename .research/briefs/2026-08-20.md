# 2026-08-20

## Since last run
- Merged PRs: 8 (#2796, #2799, #2800, #2801, #2803, #2805, #2806, #2811)
- Open PRs touched: 4 (#2812 draft — saved-search watermark; #2813 draft — inquiry-detail messages; dependabot #2808/#2809/#2810 fresh)
- New / updated issues: 4 (#2802 CI apt-hang, #2804 h2 RUSTSEC scope, #2807 voice-device 23505 follow-up, #2794 device-dedup follow-up — all closed)
- Commits: 40 on `dev` since last run (heavy dispatcher tempo + 8 substantive PRs, listed above)
- Routine lag: 1d 33h since last run — under 36h threshold, no lag_warning
- Phases that failed: none — Phase 1.6 intentionally skipped (buffer-refill focus, same rationale as 2026-08-19; pm_cursor untouched)

## Shipped
- #2811 fix(voice/link): route ai/voice link path through single-writer voice-device upsert — closes #2807 23505 unique-index violation on re-link
- #2796 fix(voice OAuth device): enforce single active device per (org,user,platform) at DB level (partial UNIQUE index + upsert) — closes #2794
- #2806 fix(voice webhook HMAC): fail closed on empty VOICE_WEBHOOK_SECRET (was keying HMAC with empty key)
- #2801 fix(reality-server): list_my_reports returns true total row count (was returning page length)
- #2800 fix(reality-server): rate-limit anonymous POST /api/v1/reports per IP — shipped from the 2026-08-19 promoted plan
- #2799 fix(reality-server): cap create_review body length at 5000 chars
- #2803 fix(backend CI): bound apt-get network waits so a stalled mirror can't wedge test-shard — closes #2802
- #2805 fix(cargo-deny): scope RUSTSEC-2026-0258 ignore to h2 0.3.x with a [bans] half-range guard — closes #2804

## Watch
- Stalled review: #2744 (fix(dispatcher): un-wedge oversize-archive push) — 7d idle, approved verdict but needs-human-review label; dispatcher already tracks it as human-gated, `stalled-review-pr-2744` signal recorded for audit trail
- Reverted: none
- Churn hotspots: `backend/servers/api-server/src/routes/voice_webhooks.rs` (3 commits this run, runs_seen=3), `backend/servers/reality-server/tests/suites/reports_tests.rs` (2, first entry to hotspot_history), `backend/servers/reality-server/src/routes/reports.rs` (2, runs_seen=5)

## PRs stuck in draft despite approval
- #2813 (inquiry-detail-empty-messages, draft) — reviewer_summary is null in assignments.json (fast-path review pending); not stuck-in-draft yet
- #2812 (saved-search-watermark-discard, draft) — same as above; both are today's implementer output
- #2744 remains draft; approved 2026-08-13 with needs-human-review label — human-gated by design, not a merge-gate bug

## PRs with verdict=changes, no fix-round progress in 24h
- none — the 3 active review rows (gh-issue-2743/PR#2744 approve+human-gated; #2812 + #2813 fresh, no reviewer_summary yet) all sit on `verdict=approve` or null

## Code review slice
- Segment reviewed: none this run (Phase 1.5 skipped) — the routine absorbed 14 dispatcher-tier1d findings queued from 2026-08-19 (api-core, ppt-web-core, ppt-web-ui, mobile-native-kmp segments) and 2026-08-20 (ppt-web-ui), so a fresh Phase-1.5 slice would compete with unabsorbed backlog rather than add new value. Independently verified 6 of the highest-impact tier1d claims against the current tree via one Explore subagent (2 stale — signals emitted score_delta=0)
- Experts: n/a (no fresh slice); verifier used Explore
- Findings: 14 tier1d absorbed (2 verified-fixed on tree — score suppressed to 0); 3 verified real became fresh code-review-finding items with confidence=high (osrng-expect-panic-auth-paths, announcement-roles-legacy-membership, ppt-web-core-api-default-export-stale-singleton); 1 stacked to score 4 and was promoted (moderation-prompt-cast); 8 verified-medium stay as review candidates in backlog
- Next segment: `api-handlers` (last 2026-08-18, oldest landable; churn hint aligned — voice_webhooks.rs is under handlers)
- Skipped: yes — buffer-refill routine focused token budget on absorbing tier1d + one adversarially-verified promotion

## Screen-map status
- Total `docs/screens/` files: 153 (ppt/=106, reality/=47, mobile/=0)
- Drift signals this run: 0 (merged PRs touched voice/reports/CI/deny.toml — no ppt-web / reality-web route files)
- Orphan screens this run: 0 (not scanned in a buffer-refill run)
- Last `screen-map.yml` CI run on `main`: unknown (no session gh CLI; MCP has no run-listing granularity for a single workflow name)

## Backlog deltas
- **New:** [2] `code-review-api-core-announcement-roles-legacy-membership` (confidence high, verified) — scheduler roles arm queries legacy organization_members while the live path uses user_memberships
- **New:** [2] `code-review-api-core-osrng-expect-panic-auth-paths` (confidence high, verified) — `.expect()` on OS RNG inside AuthService::generate_token() panics from live auth request paths
- **New:** [2] `code-review-ppt-web-ui-moderation-prompt-cast` (confidence high, verified) — moderation page casts window.prompt output to action union unvalidated (stacked to score 4 with the 2026-08-19 -unvalidated signal)
- **New:** [1] `code-review-ppt-web-core-api-default-export-stale-singleton` (confidence high, verified) — api.ts default export binds a stale unconfigured axios singleton
- **New:** [1] `code-review-ppt-web-ui-dsa-reports-i18n` (confidence high, verified) — DsaReportsPage has no useTranslation import; hardcoded English + en-GB locale
- **New:** [1] `code-review-ppt-web-core-refresh-queue-never-invoked` (medium, unverified — review candidate)
- **New:** [1] `code-review-ppt-web-ui-compliance-hardcoded-i18n` (medium, unverified — review candidate)
- **New:** [1] `code-review-ppt-web-ui-registry-confirm-i18n` (medium, unverified — review candidate)
- **New:** [1] `code-review-api-core-delay-to-duration-mul-overflow` (medium, unverified — review candidate)
- **New:** [1] `code-review-mobile-native-kmp-formatprice-minor-unit-contract` (medium; mobile-native-gated in cloud)
- **New:** [1] `code-review-mobile-native-kmp-ssoinitiation-callback-unencoded` (medium; mobile-native-gated in cloud)
- **Bumped:** [2→4] `code-review-ppt-web-ui-moderation-prompt-cast` — the 2026-08-19 -unvalidated signal stacks onto this run's exact-same-finding signal
- **Suppressed (verified-fixed):** `code-review-ppt-web-core-api-401-onunauthorized-unwired` (already wired at api.ts:196/219-221 today), `code-review-ppt-web-ui-dsa-download-no-onsuccess` (hook-level onSuccess exists in `useDownloadDsaReportPdf`) — signals recorded with score_delta=0, IDs added to seen_signals, no backlog item created
- **Decayed:** none — no open items past the 14d threshold this run
- **Dropped:** none this run
- **Resolved (done):** all 8 merged PRs' matching backlog rows were already status=done prior to this run (dispatcher's daily reconciler landed the transitions between routine runs); recorded 2 explicit `resolution` signals for PR #2800 (plan shipped) and PR #2801 (row already reconciled)

## Plans promoted
- `plans/code-review-ppt-web-ui-moderation-prompt-cast.md` — allowlist-guard the window.prompt output before mutating on ContentModerationPage's `handleTakeAction` / `handleDecideAppeal`, drop the compile-only union cast, add an RTL regression test that fails today when the user submits garbage · adversarial pass: passed (source→hypothesis→approach→test chain unbroken; RTL assertion would return no-mutation-call once the guard lands and would fire mutate('deletee') today; no placeholder rot; ContentModerationPage.tsx exists on disk; Mode: cloud-ok; 2 capability ticks C1+C6)

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. `stalled-review-pr-2744` is comment-only-eligible but the dispatcher already logs the PR's human-gated state every run (redundant ping would tick the user for no delta); moderation-prompt-cast is high-confidence + score 4 but not in the safe-shape allowlist (needs a real plan, which was promoted)

## Self-review findings
- open=34 (high=7, medium=24, low=3); new this run=0; acknowledged=19 (findings.json unchanged since 2026-08-16 — file not re-scanned this buffer-refill run)
- fp-mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=15] — dispatcher-prompt still hard-assumes push is proxy-403-blocked; fix: replace hard-coded assumption with runtime probe
- fp-mcp-push-cannot-inline-large-archive [recurrence=15] — Phase 6 archive-move produced a 198KB assignments-archive.json; fix: chunked push-files fallback landed but the underlying prompt guidance still assumes single-shot inline push
- fp-tier1d-signals-accumulate-no-promotion-to-backlog [recurrence=8] — this run partially resolves: 14 tier1d signals from the 2026-08-19/20 window absorbed to backlog and one promoted; recurrence expected to hold or decrement next run if the pattern stays cleared
- fp-stale-in-progress-action-list-decoupled-from-assignments [recurrence=5] — action-list.json rows drift from assignments.json; fix: extend gc1-reconcile.sh
- fp-implementer-sandbox-zero-pushed-branch-multigen [recurrence=3] — implementers claim then produce zero branches; fix: liveness escalation on reclaim
- … and 2 more (see findings.json)

## Open questions
- **Phase 1.6 skipped again.** Second consecutive buffer-refill run to skip PM rotation. If the operator wants at least the Scrum Master synthesis on every routine run (even during payload-driven buffer-refills), say so and the next revision will always run the pm-scrum-master leg even when other roles rotate off. `pm_cursor.next_index` remains 3 (`pm-qa` next).
- **20 plans in `plans/` reference backlog rows already `status: done | dropped`.** They're stale-active by file location while backlog canon says otherwise. Sample: `security-realtors-mark-inquiry-read-idor.md`, `code-review-mobile-native-kmp-android-sso-deeplink-missing-csrf-state.md`, `code-review-reality-server-submit-report-no-rate-limit.md` (just merged as #2800). The routine spec is silent on when to move these to `plans/_archive/`; the dispatcher's `archive-reconcile.sh` only handles `assignments.json`. Should the routine add a lightweight `plan-archive.sh` step for done/dropped plans? Flagging without acting to avoid scope creep — G13's "archive only grows" invariant would still hold with such a move.

## Quiet day?
- No — 8 substantive merges, 11 new backlog vectors from previously-queued tier1d slices, 1 landable plan promoted with an independent-verifier adversarial pass (directly addresses the buffer-low payload signal).
