# 2026-08-20

## Since last run
- Merged PRs: 0 (last merged into dev was #2798 on 2026-08-19T04:23:59Z, just before the previous routine run)
- Open PRs touched: 5 (#2796 draft — voice OAuth device DB uniqueness; #2799/#2800/#2801 draft — reality-server body-cap / rate-limit / pagination-total; #2803 draft — CI apt-mirror bound)
- New / updated issues: 2 (#2802 bug/infra/dispatcher — backend test-shard 90-min apt-mirror hang; #2804 follow-up — scope RUSTSEC-2026-0258 h2 advisory-ignore to h2 0.3.x only). Both already labeled → no `untriaged-issue` signals fire.
- Commits: 17 on `dev` since last run — all dispatcher/research chore commits touching only `.research/`; no application-code churn to score.
- Routine lag: 22h 30m since last run — on schedule (under the 36h threshold, no lag_warning)
- Phases that failed: none — Phase 1.5 + Phase 1.6 intentionally skipped (see *Open questions*); no cursor withheld

## Shipped
- none — 0 merges in this window

## Watch
- Stalled review: none new since last run (existing #2555 / #2558 / #2559 / #1179 / #988 signals already fired in prior runs; dependabot PRs #2673 / #2749 / #2750 / #2751 have not been updated since 2026-08-13 — outside the since-last-run touched-open filter)
- Reverted: none
- Churn hotspots: not re-computed this run — the only commits on `dev` since the last routine run touch `.research/` (dispatcher/research chore commits); no application-code churn to derive hotspots from
- CI blocker in flight: PR #2803 (gh-issue-2802) fixes the backend `test-shard` apt-mirror hang that wedges the merge pipeline; dispatcher hardened both apt steps (ripgrep + fonts) with a per-call `sudo timeout 90` inside a 3× retry loop + `Acquire::ForceIPv4=true` + step timeout-minutes:6, awaiting CI + review. Every other approved PR (#2796 / #2799 / #2800 / #2801) is blocked on the same shared infra flake.

## PRs stuck in draft despite approval
- session cannot query `reviewDecision=APPROVED` for open drafts (list_pull_requests result omits the field); the dispatcher's own commit log identifies #2796 / #2799 / #2800 / #2801 as reviewer-approved but CI-red on the shared apt-mirror flake, and #2803 as approved-but-CI-still-shaking-out on its own head. All five stay draft because self-authored PRs cannot receive a formal approving review from a single account — treat this as an operational cap on the draft/self-review workflow, not a per-PR bug.
- #2744 remains draft (>10d) with `needs-human-review` label — human-gated by design; no routine action.

## PRs with verdict=changes, no fix-round progress in 24h
- none in current window — dispatcher assignments.json (planning branch) reports 0 stale verdict=changes rows this window; the previously flagged #2796 clippy round-2 has landed (7186176 → 4a425f3, fmt+clippy green).

## Code review slice
- Segment reviewed: skipped this run
- Experts: n/a
- Findings: 0
- Next segment: `api-core` / `mobile-native-kmp` (both tied at 2026-08-16, oldest landable) — will fire next run
- Skipped: yes — no fresh code churn to align a slice to (no application-code commits since last run) and every landable segment was reviewed within the last 4 days. Rotating deeper this run would burn tokens for near-zero fresh signal. `review_cursor` unchanged.

## Screen-map status
- Total `docs/screens/` files: 153 (ppt/=106, reality/=47, mobile/=0)
- Drift signals this run: 0 (no merged PRs → nothing to check)
- Orphan screens this run: 0 (not scanned — nothing to align against)
- Last `screen-map.yml` CI run on `main`: unknown (session cannot query Actions runs)

## Backlog deltas
- **New:** none
- **Bumped:** none
- **Decayed:** none — oldest open row is `code-review-mobile-native-kmp-cancellation-swallowed` (updated 2026-08-11, 9d ago); nothing has crossed the 14d decay line yet
- **Dropped:** none
- **Resolved (done):** none — no merges to cross-reference against open items

## Plans promoted
- none — the two open items with `score ≥ 3` (`code-review-mobile-native-kmp-inquiries-response-contract`, `code-review-reality-server-submit-report-no-rate-limit` via security fast-track) already have plans on disk in `plans/`

## Auto-fix
- `no candidate` — no signals emitted this run, so nothing met the certainty bar (confidence=high + score≥3 + allowlist shape)

## Self-review findings
- open=34 (high=7, medium=24, low=3); new this run=0; acknowledged=19 (per prior run's snapshot — file not re-scanned; the routine loop has been reviewing the same six recurring failure modes for a week)
- fp-mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=16] — dispatcher-prompt still hard-assumes push is proxy-403-blocked; fix: replace hard-coded assumption with runtime probe
- fp-mcp-push-cannot-inline-large-archive [recurrence=16] — Phase 6 archive-move keeps producing ~200KB assignments-archive.json; fix: chunked push-files fallback landed but the underlying prompt guidance still assumes single-shot inline push
- fp-tier1d-signals-accumulate-no-promotion-to-backlog [recurrence=9] — Tier1d generator has produced findings the routine has not fully promoted; today's dispatcher log shows two more landable frontend findings written to `.research/signals/` that this quiet-day run did NOT promote (deferred — see *Open questions*)
- fp-stale-in-progress-action-list-decoupled-from-assignments [recurrence=5] — action-list.json rows drift from assignments.json; fix: extend gc1-reconcile.sh
- fp-implementer-sandbox-zero-pushed-branch-multigen [recurrence=3] — implementers claim then produce zero branches; fix: liveness escalation on reclaim
- … and 2 more (see findings.json)

## Open questions
- **Phase 1.5 skipped this run.** Every landable segment was reviewed within the last 4 days (oldest: `api-core` / `ppt-web-ui` / `mobile-native-kmp` at 2026-08-16). Firing another slice on 4-day-old ground would waste budget on a truly quiet day. If the operator prefers the routine always run a slice regardless of freshness, say so and the next revision will drop the freshness guard.
- **Phase 1.6 skipped this run.** No merges + no code activity means the delivery synthesis has nothing new to report vs yesterday. `pm_cursor.next_index` was NOT advanced (still 3 → `pm-qa`), so tomorrow's rotation resumes cleanly. G16 is a no-op when Phase 1.6 doesn't run.
- **Dispatcher Tier1d signals not promoted this run.** The dispatcher's 2026-08-19 18:37 and 2026-08-20 02:32 commits wrote frontend code-review findings into `.research/signals/` (`api-401-onunauthorized-unwired`, `refresh-queue-never-invoked`, `api-default-export-stale-singleton`, `automation-mutation-errors-swallowed`, `workflow-automation-i18n-gap`) intended for the routine to promote into `backlog.json`. This run did NOT wire that ingestion — the promoter path is not yet part of Phase 1/Phase 2 and doing it inline on a quiet day would silently expand the routine's contract. Flagging for a separate, explicit change to the routine.

## Quiet day?
- Yes — 0 merged PRs since last run, 0 new backlog vectors, 0 plans promoted, 0 Phase 1.5 findings, 0 auto-fix actions. `state.stats.quiet_days` bumped 2 → 3.
