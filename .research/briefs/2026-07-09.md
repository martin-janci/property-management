# 2026-07-09

## Since last run
- Merged PRs: 7 (range #2186–#2194)
- Open PRs touched: 21 total open — 13 draft (12 dispatcher-driven auto-impls landed 20:11–20:37 UTC, plus long-lived #1797), 8 non-draft
- New / updated issues: 10 (all `follow-up` / `from-merged-review` — issues #2195–#2204, opened by `ppt-review-merged` skill on the merged PRs #2177 / #2179 / #2180 / #2181 / #2182 / #2183 / #2184 / #2185 / #2186 / #2173)
- Commits: 27 on `dev` since 5877ade (mix of feature merges + dispatcher chore commits)
- Routine lag: 19h since morning run (2026-07-09T02:37Z → 22:14Z) — well under the 36h threshold; this is a same-day incremental sweep
- Phases that failed: none — Phase 1.5 (`ppt-dev-review`) and Phase 1.6 (`ppt-project-management`) intentionally skipped this run (same-day rerun after morning full pass on `api-handlers` + PM-frontend); cursors not advanced so G16 is a no-op; resumes on next incremental run

## Shipped
- #2186 churn-hotspot booking OAuth CSRF tests — strengthen booking/airbnb OAuth CSRF coverage (test-only, `post-merge-reviewed`; follow-up issue #2203 filed against the CSRF replay/single-use branch gap)
- #2187 churn-hotspot booking OAuth routes tests retry1 — strengthen booking OAuth token-exchange route coverage (test-only, `post-merge-reviewed`)
- #2188 gap-screens link UC-10 — link UC-10 (Person-Months) to `ppt/person-months.md` frontmatter
- #2191 gap-screens link UC-40 — link UC-40 (Budget & Planning) to `ppt/financial-budgets.md` frontmatter
- #2192 gap-screens link UC-33 — link UC-33 (Tenant Screening) to `ppt/lease-application*.md` frontmatter (two screens)
- #2193 gap-screens link UC-29 — added new screen-map `docs/screens/ppt/rentals-dashboard.md` (carries UC-29 + UC-30, Epic 18)
- #2194 gap-83-2 property-mapping wizard — Booking.com channel card + Supply-XML property-mapping wizard wired into `IntegrationsPage.tsx` (813-line add), + `api-client/integrations/api.ts` moved from `apiRequest` → `authenticatedFetchJson` (5 calls) so Booking hooks correctly attach the bearer token. Follow-up #2202 already flagged the sibling Airbnb OAuth flow's discoverability / i18n / CSRF-state gaps.

## Watch
- Stalled review: none matched the criteria (>7d, non-draft, non-dependabot). #1797 (`fix(api-server): auth on OCR endpoints + manager-gate rental guest PII reads`) is still a long-lived draft (last updated 2026-07-05, 4d 18h old).
- Reverted: 0 in window.
- Churn hotspots (19h window, after excluding lockfiles / generated / docs / .research / .claude): almost entirely dispatcher-authored feature landings — no repeated-churn signals.
  - `frontend/packages/sitemap/src/json/sitemap.json` — 1500 lines add+del (aggregate JSON regenerated from route + screen-map deltas in the 5 gap-screens-* / gap-83-* PRs; not a real hotspot)
  - `frontend/apps/ppt-web/src/features/settings/integrations/IntegrationsPage.tsx` — 520 lines (single-PR expansion for #2194 Booking wizard; not repeated-churn)
  - `backend/servers/api-server/tests/booking_oauth_routes_tests.rs` — 217 lines (single-PR expansion for #2187)
  - Auth-hot files from the morning brief (`booking/mod.rs`, `oauth_integration_tests.rs`, `auth.rs`) did NOT re-churn this window — the morning's plans (`bug-revoke-all-sessions-cookie-blindness`, `security-forgot-password-no-rate-limit`) are already being executed by the dispatcher on drafts #2190 / #2205 respectively.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     Not queried this run (would require per-PR API round trips for reviewDecision; keeping the same-day rerun cheap). -->
- none surfaced this run (per-PR `reviewDecision` not fetched — same-day rerun cost discipline)

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json (planning branch) where
     reviewer_summary starts with "verdict=changes" AND (now - last_updated) > 24h AND status == "review". -->
- none surfaced this run (planning-branch assignments.json not deep-scanned)

## Code review slice
- Segment reviewed: none this run (same-day rerun after morning `api-handlers` full pass — deferring to next incremental run to avoid double-review)
- Experts: —
- Findings: 0 (see morning-run brief for the 3 `code-review-api-handlers-*` findings already promoted)
- Next segment: `mobile-native-kmp` (oldest-unreviewed after morning run — last reviewed 2026-06-06)
- Skipped: yes — same-day rerun (Phase 1.5 already executed on `api-handlers` at 02:37Z; running again same-day would double-count against the token budget)

## Screen-map status
- Total `docs/screens/` files: 150 (`ppt/` 103, `reality/` 47, `mobile/` 0 — mobile not yet seeded)
- Drift signals this run: 0 (none of the 7 merged PRs touched `App.tsx` / `routes/**` / `reality-web/src/app/**` without matching screen-doc updates. #2194 touched `IntegrationsPage.tsx` under `features/`, which is not in the drift trigger set.)
- Orphan screens this run: 0 (not scanned — same-day rerun cost discipline)
- Last `screen-map.yml` CI run on `main`: not queried this run

## Backlog deltas
- **New:** — none (no new signals crossed the score threshold this window; all 10 new follow-up issues were already claimed by the dispatcher via PRs #2213 / #2214 / #2215 / #2216 / #2217 / #2218 / #2219 / #2220 / #2221 — surfacing them again as backlog vectors would duplicate active work.)
- **Bumped:** — none
- **Decayed:** — none (all 5 open + 2 ready items have `updated_at = 2026-07-09` — created this morning; nothing crosses the 14-day decay window)
- **Dropped:** — none

## Plans promoted
- none (the two ready items promoted this morning — `plans/bug-revoke-all-sessions-cookie-blindness.md` and `plans/security-forgot-password-no-rate-limit.md` — are already claimed by the dispatcher on draft PRs #2190 and #2205; no new candidates crossed the readiness gate this window)

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + Phase-5 allowlist this run (no new signals emitted, and none of the ready items are on the allowlist — both are `bug` / `security` code fixes routed to the manual implementer / dispatcher path)

## Self-review findings
- none (`.research/self-improvement/findings.json` not present in the tree)

## Open questions
- Are the 10 `follow-up` / `from-merged-review` issues (#2195–#2204) intended to route through the dispatcher's default gap pipeline (as they now are — 9 already claimed on draft PRs), or should the routine start surfacing them as `code-review-finding` signals too? Right now they're double-counted if the routine does that on top of the dispatcher's own capture — leaving them as dispatcher-only for now unless the operator asks otherwise.

## Quiet day?
- Partially — 7 PRs merged and 10 new follow-up issues opened, but none required new backlog rows or plan promotions. The morning routine run already consumed the material `code-review-finding` findings this window would surface. `stats.quiet_days` bumped to 2 to reflect the low-decision-density of this incremental sweep.
