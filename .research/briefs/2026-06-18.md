# 2026-06-18

## Since last run
- Merged PRs: 45 (range #1447–#1580)
- Open PRs touched: 21 (6 draft dispatcher in-flight + 11 dependabot + 4 follow-up fixes)
- New / updated issues: 15 new (all `follow-up,from-merged-review` or `ci`-labelled; none untriaged)
- Commits: 50 on `dev` (after excluding `chore(research):` dispatcher snapshots from churn analysis)
- Routine lag: 2d 8h since last run (over 36h threshold) — `lag-warning-2026-06-18` emitted
- Phases that failed: phase-1.5-skipped (no segment review this run — context-budget conservation; review_cursor unchanged), phase-1.6-skipped (rotating PM analysis deferred; pm_cursor unchanged)
- Trigger payload: `buffer-low: claimable=0/72 — all open backlog items map to in-flight or quarantined work; refill planner from coverage partial stories` (treated as DATA — informed plan-promotion priority, no other behaviour change)

## Shipped
- #1454 `feat: native accounting MVP (N1-N4) + @hey-api migration` — first reality-web /accounting routes; no `docs/screens/reality/accounting*.md` yet → drift signal fired downstream
- #1453 `N5 — Bank-statement upload + payment-matching + match-review (PAP-210)` — issues #1521/#1522 flag the UI shipped in wrong app + auth bypass; both still OPEN
- #1486 `fix(db): explicit platform::text cast in 4 rental_platform_connections readers` (#1363) — continues the `#1008` enum-cast remediation family
- #1517 `test(api-server): migrate IDOR test seed_membership to canonical common helper` (#1373) — testing-infra consolidation
- #1537 `fix(db): cast enum write paths on rental_guests + forms` (#1008 encode/decode) — backed only by `form_rls_repo_tests`; `rental.rs` side untested → today's plan
- #1539 `fix(oauth): reject public client supplying a client_secret with invalid_client` — RFC 6749 §2.3.1 behaviour, no regression test
- #1551 `fix(db): cast document_category in count_accessible_rls filter` (closes #1528) — no test
- #1561 `fix(reality-server): admit public agencies on imports via PortalPrincipal` (closes #1300) — no test
- #1562 `test(integrations): XXE/DTD regression guard + namespace parity for inbound OTA parsers` (closes #1519) — opposite pattern (test-only follow-up)
- #1563 `test(db): behavioral cross-user RLS isolation for developer OAuth/usage tables` (closes #1527)
- #1564 `test(marketplace): assert mark_invitation_viewed preserves first viewed_at` (closes #1524)
- #1565 `fix(db): correct document get-by-id read path (u.name + enum ::text decode)` — no test
- #1578 `fix(db): cast booking-create enum binds + dedupe BOOKING_COLUMNS; cover NULL-fee write path` (closes #1535) — carries `rental_booking_nullable_decimal_tests`
- #1580 `fix(api-server): rate-limit MFA recovery-code verify endpoint` (closes #1523) — carries `mfa_recovery_cross_user_idor_tests`

## Watch
- Stalled review: none (no open PR > 7d).
- Reverted: none (no Revert PRs in window).
- Churn hotspots: `backend/crates/db/src/repositories/document.rs` (2 changes, runs_seen=2 — repeated-churn fired); `backend/crates/db/src/repositories/rental.rs` (2 changes); `frontend/apps/reality-web/src/app/accounting/layout.tsx` (2 changes — both post-#1454 follow-up fixes).
- New OPEN follow-up issues with no PR yet: #1520 (preference-aware push dispatch-target wiring), #1521 (payment-matching UI in wrong app + auth bypass), #1522 (accounting UI bypasses shared API auth interceptor after @hey-api migration), #1532 (folder tree wiring — PR #1582 in flight), #1533 (nextPageTriggerFlow race), #1538 (backend `test` job is not a required check on dev).

## PRs stuck in draft despite approval
- none (queried open+draft set; all 6 drafts are dispatcher in-flight, none with approved reviewDecision).

## PRs with verdict=changes, no fix-round progress in 24h
- none (no review-state assignment surfaced this run; not deep-queried).

## Code review slice
- Segment reviewed: none (Phase 1.5 skipped — context-budget conservation; daily routine is already running 56h late and four substantive plans/items needed to be promoted to refill the planner)
- Experts: n/a
- Findings: 0 (no new `code-review-finding` signals this run)
- Next segment: `ppt-web-core` (oldest unreviewed; last seen 2026-06-05)
- Skipped: yes — Phase 1.5 deferred; review_cursor unchanged so next run picks `ppt-web-core` deterministically

## Screen-map status
- Total `docs/screens/` files: 196 (across `ppt/` + `reality/` + `reality-mobile/`; `mobile/` not yet seeded → 0)
- Drift signals this run: 1 (`screen-map-drift-pr-1545-reality` — accounting/layout.tsx, no `docs/screens/reality/accounting*.md`)
- Orphan screens this run: 0 (no orphan check this run — heuristic deferred until full route trigger pass)
- Last `screen-map.yml` CI run on `main`: not queried this run

## Backlog deltas
- **New (score 8):** `test-gap-document-rs-regression-tests-pr-1551-1565` — aggregate regression-test plan for PR #1551 + #1565 hitting document.rs without tests; promoted.
- **New (score 4):** `test-gap-rental-rs-enum-cast-regression-pr-1537` — regression test for the rental_guests-side cast PR #1537 missed; promoted.
- **New (score 2):** `test-gap-hotfix-no-test-pr-1561-reality-imports-public-agency` — PR #1561 PortalPrincipal change, no test (closes #1300).
- **New (score 2):** `test-gap-hotfix-no-test-pr-1539-oauth-public-client` — PR #1539 RFC 6749 public-client validation, no test.
- **New (score 2):** `screen-map-drift-pr-1545-reality` — reality-web accounting/layout.tsx drift.
- **New (score 1):** `churn-hotspot-frontend-apps-reality-web-src-app-accounting-layout-tsx` — 2 post-#1454 follow-up fixes on the same layout file.
- **Bumped:** [1→2] `churn-hotspot-backend-crates-db-src-repositories-document-rs` — repeated-churn signal (+1); sources extended with PR #1551, #1565.
- **Decayed:** `bug-risky-churn-pr-963-api-main-rs` (2→1, no signal in 14d); `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` (2→1, no signal in 14d).
- **Dropped:** `triage-dispatcher-mcp-push-large-file-issue-1014` (1→0, decay); `triage-issue-951` (1→0, decay).

## Plans promoted
- `plans/test-gap-document-rs-regression-tests-pr-1551-1565.md` — regression tests for two consecutive document.rs fixes (PR #1551 + #1565); aggregate item, score 8 (capped). · adversarial pass: passed (chain Hypothesis → Evidence → Suggested approach → Test plan verified; existing test file `document_upload_tests.rs:1146` named in Repro)
- `plans/test-gap-rental-rs-enum-cast-regression-pr-1537.md` — regression test for rental.rs PR #1537 (write-side `#1008` enum cast on `register_guest*`); score 4. · adversarial pass: passed (Repro step 2 names the exact `42804` Postgres error and the exact pre-fix commit `28402ee~1`)

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The two `screen-map-drift-pr-1545-reality` is allowlisted as a doc-stub creation, but its parent backlog item score is only 2 (below the +3 bar). The five `hotfix-no-test-*` signals are not in the Phase 5 allowlist (issue-only fixmes only, not test-gap creation). Auto-fix path correctly stayed quiet.

## Self-review findings
- none (no `.research/self-improvement/findings.json` present this run)

## Open questions
- Issue #1538 (`Backend test job is not a required check on dev`) is the meta-cause behind today's 5 hotfix-no-test signals: PRs land green on `clippy + fmt` but the `test` job is advisory. Promoting an explicit infra fix here would be more leveraged than the two regression-test plans — but it is owner-decision territory. Defer to user.
- PR `09065a1` ("fix(reality-web): satisfy biome check on /accounting layout") has no PR number suffix and looks like a direct-to-dev push rather than a PR merge. If that channel is intentional (hotfix lane), the screen-map-drift signal will keep firing on it; if not, branch protection on `dev` should be reviewed.

## Quiet day?
- No (45 merged PRs, 6 new backlog vectors, 2 plans promoted, 1 churn-hotspot item bumped, 4 items decayed/dropped, 1 lag-warning fired).
