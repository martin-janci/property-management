# 2026-06-18

## Since last run
- Merged PRs: **106** (#1440–#1600) — 37 fix, 16 chore (12 dependabot/research), 15 test, 14 feat, 3 docs, 2 refactor.
- Open PRs touched: **9** (mostly dependabot; #1554 mobile build-config + #1560 OTA push-flow have COMMENTED reviews).
- New / updated issues: **80** (#1519–#1594) — almost entirely `follow-up,from-merged-review` rows that close within a day or two; **#1538 (open, `ci`) remains live** ("Backend `test` job is not a required check on `dev`").
- Commits on `dev`: 100 since `4829015b` → `f799a497`.
- Routine lag: **~62h since last run (~2d14h)** — emits `lag_warning-2026-06-18`. Cloud cron missed the 2026-06-17 slot.
- Phases that failed: **phase-1.5 (Phase 1.5 skipped — context-budget pressure from buffer-low handling; review_cursor not advanced)**, **phase-1.6 (Phase 1.6 skipped — buffer-low payload demands local `/ppt-project-management scan`, not a one-role rotation; pm_cursor and coverage_cursor deliberately NOT advanced this run)**.

## 🚨 Operator-action required — buffer-low

Routine was fired with payload `buffer-low: claimable=0/72`. The dispatcher reports it has **zero claimable tasks** because `coverage.json` (last scanned 2026-06-16) is stale: all 20 `partial` stories now map to merged or active assignments. The cure is **not** something this routine can do — the routine prompt explicitly forbids `scan` mode in Phase 1.6 ("the authoritative full rebuild is the on-demand local `/ppt-project-management scan`"). The user needs to run `/ppt-project-management scan` from a local checkout to refill the buffer.

Backlog item created: `dx-pm-coverage-stale-2026-06-18` (score 3, confidence high, vector `dx`). Not promoted to a plan — it's an operator-action ticket, not an implementer task.

## Shipped
- **Backend test-job unblock (#1440, #1469, #1481, #1495, #1486, #1492, #1499, #1507, #1512, #1537)** — schema-drift hotfix sweep finishing what #1437 surfaced on 2026-06-16. Enum-cast adds across rental connection readers, document/form/user repos, accounting clippy; budget.rs reserve-fund duplicate retired.
- **BIT follow-ups closed in flight** — #1457 (BIT-75 add_evidence/add_comment org scope), #1458 (BIT-77 publish-leg replay), #1460 (BIT-76 FORCE RLS on developer_oauth tables), #1467 (BIT-78 mfa.rs RLS-enforce + IDOR test), #1472 (Booking.com encrypt-at-rest), #1473 (BIT-85 OAuth upsert), #1485 (BIT-110 Booking.com Retry-After + timeout), #1491 (mark_invitation_viewed idempotent), #1539 (oauth public-client invalid_client), #1551 (count_accessible_rls document_category cast), #1552 (OTA token-exchange manager gate), #1561–#1565 (PortalPrincipal harden, board_meetings u.name, others).
- **Dispatcher self-test fixes (#1444, #1448, #1483)** — T21 epic_prefix broadened, depends_on populated in pm-analysis/gap-scan emit, Tier-2 kick payload corrected (closes #1151 — interestingly the same buffer-low symptom recurred).
- **Code-review-finding plans landed:** #1418 closed last run; this window saw `#1555` (faults wiring) and `#1559` (announcement comments) ship for ppt-web — both touched `routes/groups/*.tsx` without screen-doc updates → drift signals fired (see below).
- **Dependabot batch (#1566–#1576)** — 12 PRs across compose-bom, tower-http, dtolnay/rust-toolchain, expo-secure-store, @types/node, @babel/core (major), i18next (major). Two are still open (#1572 expo-secure-store, #1574 cargo-minor-patch group).
- **Dispatcher state PRs** — ~50 of the 106 are bot-driven `.research/management/{assignments,action-list,post-merge-review}.json` updates. Excluded from churn detection.

## Watch
- **#1538 (open, `ci`)** — Backend `test` job is not a required check on `dev`; carry-over from PR-1437 era. Still open after Phase 1.6 pm-devops surfaced it on 2026-06-16.
- **No reverts this run.** The schema-drift sweep was patched forward; no revert PRs in window.
- **Churn hotspots this run (top 3, real code only):**
  - `backend/crates/integrations/src/booking.rs` (3 commits, runs_seen 1→2 → `repeated-churn` stacked +1).
  - `backend/crates/db/tests/api_ecosystem_developer_force_rls_tests.rs` (3 commits — new test churn from BIT-76 / #1527 follow-ups).
  - `backend/Cargo.toml` (2 commits — workspace dep churn from #1568/#1574).
- **Stalled review / draft:** **none** detected in window — `prs_stuck_draft_approved=0`, `stalled_review_prs=[]` per the gh_activity scan. Older long-open drafts (#988, #1179, #1197, #1316) were outside the touched-since-cursor window; will be re-checked next run.
- **Closed-not-merged this run:** 26 PRs closed without merging — vast majority dispatcher state PRs superseded by newer state PRs (one signal batch only, score_delta 0).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (assignments file inspected — both `review`-status rows are <24h since `last_updated`: feat-build-configuration #1554 (~7h), feat-booking-integration #1560 (~7h)).

## Code review slice
- Segment reviewed: **skipped** (context-budget reservation for buffer-low handling).
- Experts: n/a.
- Findings: **0**.
- Next segment: **ppt-web-core** (last reviewed 2026-06-05 — oldest unreviewed at 13d stale). Will be picked up by the next routine run.
- Skipped: **yes — context-budget reservation: buffer-low payload required deeper Phase 1 / Phase 6 work**.

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0 — not yet seeded; `reality-mobile/` placeholder dir exists).
- Drift signals this run: **3** — `screen-map-drift-pr-1545-reality` (PR #1545 reality-web `app/accounting/layout.tsx` prerender fix), `screen-map-drift-pr-1555-ppt` (PR #1555 ppt-web `routes/groups/faults.tsx` + test wiring), `screen-map-drift-pr-1559-ppt` (PR #1559 ppt-web `routes/groups/announcements.tsx` optimistic comments). All three are heuristic — #1545 is a layout fix (not a new screen) and #1555/#1559 update existing routes; review-candidate only, not promotion-ready.
- Orphan screens this run: **0** (no scan — routes-touched-this-run union is small and no slug pattern jumped out).
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New:**
  - **[3] `dx-pm-coverage-stale-2026-06-18`** — Refresh coverage.json — dispatcher buffer empty (claimable=0). Score 3, confidence high. Surfaces the routine-fire payload.
  - [2] `test-gap-screen-map-drift-pr-1545-reality` — reality-web accounting layout prerender fix (heuristic).
  - [2] `test-gap-screen-map-drift-pr-1555-ppt` — ppt-web faults wiring (heuristic; productive change).
  - [2] `test-gap-screen-map-drift-pr-1559-ppt` — ppt-web announcement comments wiring (heuristic; productive change).
  - [1] `churn-hotspot-backend-crates-db-tests-api-ecosystem-developer-force-rls-tests-rs` — 3 commits; healthy test-churn from BIT-76 follow-ups.
  - [1] `churn-hotspot-backend-Cargo-toml` — 2 commits; dependabot/group-bump churn.
  - [1] `repeated-churn-backend-crates-integrations-src-booking-rs` — runs_seen 1→2 across 2026-06-12 and 2026-06-18; Booking.com OTA push-flow + Retry-After landings stacking up.
- **Bumped:** none — every other signal was already in `seen_signals` (cumulative-type re-emits applied score_delta=0 per the routine prompt's cumulative-allowlist rule).
- **Decayed:** `bug-risky-churn-pr-963-api-main-rs` (2→1), `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` (2→1) — both updated_at 2026-06-03, no signals in 15d.
- **Dropped:** `triage-dispatcher-mcp-push-large-file-issue-1014` (decayed to 0), `triage-issue-951` (decayed to 0).
- **Resolved (PR-body match):** `refactor-churn-hotspot-mobile-announcements` → done (PR #1543, evidence not independently verified — inconclusive grep), `refactor-churn-hotspot-mobile-announcements-test` → done (PR #1543, inconclusive). Note: a deeper code-state resolution scan across all 57 open items was deferred this run (context budget); next routine run picks it up.

## Plans promoted
- **0** — buffer-low handling consumed the run budget; existing 6 ready plans are unchanged and continue to await the implementer.

## Auto-fix
- `no candidate` — three `screen-map-drift` signals met the allowlist shape but only at `confidence=medium` (the heuristic caveat is required), all below the `confidence=high + score>=3` bar; no `fixme-in-merged-code`, no `lockfile-drift` dep-update sub-shape, no `stalled-review` in window.

## Self-review findings
- `.research/self-improvement/findings.json` absent — none

## Open questions
- **🚨 Operator-action required: run `/ppt-project-management scan` locally** to refill the dispatcher buffer (`claimable=0/72`). The routine cannot do this itself — Phase 1.6 explicitly forbids `scan` mode. Until this runs, the dispatcher will continue idle.
- **Phase 1.5 + 1.6 deliberately skipped this run** to honor the buffer-low payload's priority. `review_cursor` unchanged; `pm_cursor.next_index` stays at 5 (pm-security still owes a deep-analysis pass — last_run 2026-05-27, now **22d stale**); `coverage_cursor.next_index` stays at 12. Next normal run picks them up.
- **#1538 (CI gate) remains open** 2 days after pm-devops flagged it. Worth a manual nudge: should backend `test` become a required check on `dev` before the next regression-pipeline rebuild?
- **Two open feat PRs in review** (#1554 mobile build-config, #1560 OTA push-flow) — both COMMENTED, last_updated ~7h ago. Likely converging; not yet stalled.

## Quiet day?
- **No.** 106 merged PRs (~50 application-code, 12 dependabot, ~44 dispatcher state), 7 new backlog vectors + 2 resolutions + 2 dropped, lag_warning fired (62h), buffer-low signal demands operator action. `stats.runs` 40 → 41, `vectors_created` 173 → 180, `plans_created` 12 (unchanged), `quiet_days` 1 (unchanged).
