# 2026-06-18

## Since last run
- Merged PRs: 51 (range #1447–#1581)
- Open PRs touched: 18 (11 dependabot, 7 feature/draft)
- New / updated issues: 15 (6 new untriaged: #1520, #1521, #1522, #1532, #1533, #1538)
- Commits: 100+ on `dev`
- Routine lag: 2d 10h since last run (>36h threshold — `lag_warning` emitted)
- Phases that failed: none

## Shipped
- #1454 native accounting MVP (N1-N4) + @hey-api migration — new accounting page on ppt-web; two follow-up issues opened against it (#1521, #1522).
- #1453 N5 — Bank-statement upload + payment-matching + match-review (PAP-210) — landed `reality-web/app/accounting/page.tsx` without screen-map docs.
- #1539 fix(oauth): public client rejects client_secret with 401 invalid_client — closes a #1331 / #1332 follow-up; no new test diff (existing `test_public_client_wrong_secret_rejected_at_token` covers).
- #1545 fix(reality-web) unbreak prod build — `/accounting` QueryClient prerender + favorites pl/hu i18n keys.
- #1561 fix(reality-server) admit public agencies on imports via PortalPrincipal (closes #1300) — new extractor wired into 11 imports.rs handlers.
- #1565 fix(db) document get-by-id read path — `u.name` + enum `::text` decode; **residual** `SELECT d.*` in `get_current_version_rls` at `document.rs:1976` flagged as plan candidate.
- #1537 fix(db) cast enum write paths on rental_guests + forms — closes the #1008 encode surface.
- #1580 fix(api-server) rate-limit MFA recovery-code verify endpoint — closes #1523.
- #1562 test(integrations) XXE/DTD regression guard for inbound OTA parsers — closes #1519.
- #1563 test(db) behavioral cross-user RLS isolation for developer OAuth/usage tables — closes #1527.
- #1564 test(marketplace) idempotency test for `mark_invitation_viewed` — closes #1524.

## Watch
- Stalled review: none (no open PR >7d with no reviewDecision this run).
- Reverted: none.
- Churn hotspots:
  - `backend/crates/db/src/repositories/rental.rs` — 8 changes (enum-cast cascade #1486/#1492/#1507/#1537/#1578), `runs_seen=1`.
  - `backend/crates/db/src/repositories/document.rs` — 8 changes (#1551/#1565), `runs_seen=2` (cumulative re-emit, score not double-counted).
  - `backend/servers/api-server/tests/booking_oauth_routes_tests.rs` — 5 changes, `runs_seen=2`.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: `ppt-web-core` (reason: oldest-unreviewed at 13 days)
- Experts: Frontend
- Findings: 3 (`code-review-ppt-web-core-accounting-invoice-stub-handler` +2, `…-auth-init-no-expiry-check` +2, `…-accounting-auth-headers-cast` +1 consolidated into the stub-handler item)
- Next segment: `mobile-native-kmp` (oldest unreviewed after this run — 2026-06-06)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 127 (ppt: 80, reality: 47; mobile: 0 — not yet seeded)
- Drift signals this run: 4 (PRs #1453, #1454, #1545, #1555 — all `confidence: medium`, review-candidate per routine-prompt heuristic caveat)
- Orphan screens this run: 0 (no orphan check this run — best-effort grep against trigger files only)
- Last `screen-map.yml` CI run on `main`: not checked this run

## Backlog deltas
- **New:** [3] `bug-document-repo-enum-decode-residual` — residual `SELECT d.*` at `document.rs:1976` (post-#1565)
- **New:** [3] `bug-ppt-web-accounting-invoice-page-incomplete` — AccountingInvoiceManagementPage stub handler + cast leaks (cross-refs issues #1521/#1522 and Phase 1.5 accounting-auth-headers-cast finding)
- **New:** [3] `bug-rental-guest-reports-enum-decode-residual` — DEFERRED: deeper inspection showed `rental_guest_reports.status` is VARCHAR, not enum; residual `::guest_registration_status` casts at `rental.rs:1779/1829` are latent but `RETURNING *` decode is correct. Promotion deferred — left at open for next-run signal.
- **New:** [2] `code-review-ppt-web-core-auth-init-no-expiry-check` — AuthContext treats stored accessToken as valid without exp check
- **New:** [4] `screen-map-drift-pr-1453-reality` — consolidated with #1545 (both `reality-web/app/accounting/*`); review-candidate not promoted
- **New:** [2] `screen-map-drift-pr-1454-ppt` — ppt-web `routes/groups/accounting.tsx` no doc
- **New:** [2] `screen-map-drift-pr-1555-ppt` — `routes/groups/faults.tsx` no doc
- **New:** [2] `test-gap-hotfix-no-test-pr-1539-oauth-public-client`, `test-gap-hotfix-no-test-pr-1545-reality-web-prod-build`, `test-gap-hotfix-no-test-pr-1561-reality-imports-portal-principal` — fixes without test diffs (existing upstream tests cited in PR bodies, confidence `medium`)
- **New:** [2] `refactor-fixme-commit-c75cde482-accounting-payment-matching` — TODO at `backend/servers/api-server/src/services/accounting.rs` ("Date proximity, IBAN match") — payment-matching heuristic incomplete
- **New:** triage rows #1520, #1521, #1522, #1532, #1533, #1538 — see `IDEAS_TRIAGE.md`
- **Done:** `refactor-churn-hotspot-mobile-announcements` (+ `-test` variant) — resolved by PR #1543
- **Decayed:** `bug-risky-churn-pr-963-api-main-rs` 2→1, `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` 2→1 (no signals in 14d)
- **Dropped:** `triage-dispatcher-mcp-push-large-file-issue-1014`, `triage-issue-951` (decayed to 0)

## Plans promoted
- `plans/bug-document-repo-enum-decode-residual.md` — audit `document.rs` for `SELECT d.*` residue (smoking gun at `:1976`), mirror PR #1565's projection; add regression test parallel to `test_upload_metadata_roundtrips_through_get_handler`. · adversarial pass: passed
- `plans/bug-ppt-web-accounting-invoice-page-incomplete.md` — wire `onViewInvoice` to a real route or hide it, add `isError` UI on the two `useQuery`s, replace four `as unknown as { Authorization }` casts with the typed @ppt/api-client auth helper; cross-refs issues #1521/#1522 (with the cross-app and shared-interceptor parts explicitly out of scope). · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The four `screen-map-drift` signals would qualify by score/allowlist but stayed at `confidence: medium` per the routine prompt's heuristic-caveat for slug-vs-route basename matching. No `fixme-in-merged-code`, `screen-map-orphan`, lockfile-drift, or stalled-review candidates fired.

## Self-review findings
- none — `.research/self-improvement/findings.json` not present.

## Open questions
- Issue #1538 (backend `test` job not a required check on `dev`) is governance-grade but enters the routine as `vector: triage` per protocol. The pm-security rotation flagged it explicitly. Should this graduate to a `dx` / `security` vector with a forced score bump? (Routine prompt is conservative — leaving in triage for now; the pm-security action-list addresses it.)
- The two `risky-churn` signals (rental + document) both reflect the #1008 enum-decode/encode pattern that's been the dominant theme of the last 3 runs. Worth a meta-plan: "introduce a typed `Document.Category` enum at the model layer so projections don't need `::text AS …`"? Tracked but out of scope.

## Quiet day?
- No — 51 PRs merged, 2 plans promoted, 3 code-review findings, 6 new triage rows, multiple churn signals. Telegram digest sent in Phase 6.

## Phase 1.6 — pm-security (rotation)
Three findings worth surfacing here (full detail in `.research/management/roles/pm-security.md`):
1. **`dev` branch protection requires only the generic `check` context** — auth/RLS regressions can land silently (issue #1538). Action-list-tracked.
2. **OAuth refresh-token revocation guard restored at `session.rs:55` but no integration test asserts it** — issue #481 still open.
3. **WebSocket auth still passes JWT in `?token=` query string** (`ws_notifications.rs:122`) — structural log-exposure surface (issue #480).
