# 2026-06-18

## Since last run
- Merged PRs: **49** (#1197, #1316, #1420, #1421, #1447, #1450, #1453, #1454, #1456, #1464, #1486, #1492, #1502, #1507–#1518, #1526, #1529, #1530, #1536, #1537, #1539–#1553, #1561–#1565) — range #1197–#1565. Includes 8 dispatcher state-PRs (chore(research)) and the resolution of the previous run's #1437 dev-compile blocker (closed 2026-06-16T08:24 — fix shipped via #1511→#1512 sequence + downstream).
- Open PRs touched: 7 drafts (all `auto-impl/*`, all opened 2026-06-18: #1554–#1560). Each is a follow-up implementer attempt for an action-list item whose prior PR already merged (#1464, #1420, #1421, #1450, #1502, #1454, …).
- New / updated issues: **15 new** (#1519–#1538) + 35 updated existing. All 15 new are labeled (zero `untriaged-issue` signals). 8 still open (notable: **#1538 [ci] — backend `test` job not a required check on `dev`; red-test PRs merge freely**).
- Commits on `dev`: 100+ since `4829015b` → `09065a1d` (top conv-commit scopes by mention: `research` 27, `db` 16, `api-server` 15; ≥3 mobile-native, ≥3 reality-server/web).
- Routine lag: **~55h since last run (>36h)** → `lag-warning-2026-06-18` emitted. Routine missed 2026-06-17 entirely.
- Phases that failed: **none**. Phases 1.5 (code-review slice) and 1.6 (PM rotation + coverage upkeep) **deliberately skipped this run** under buffer-low payload + token-budget — cursors NOT advanced; next run owes ppt-web-core (oldest unreviewed, 13d) and pm-security (overdue 22d).

## Shipped (highlights, full list above)
- **Dev pipeline unstuck (#1437 resolved).** PR #1511 closed-not-merged (replaced by #1512), then #1486 / #1492 / #1537 (enum-cast batch on `rental_*`/`forms`), #1507 (test-seed restore + IDOR principal fix), #1517 (IDOR seed_membership migration), #1551 (`document_category` cast in `count_accessible_rls`), #1565 (document get-by-id read-path: `u.name` + enum `::text` decode). Backend CI is green again.
- **Stranded approved PRs drained.** #1197 (OAuth authz-server integration tests, 7d stale) and #1316 (verify-document-folder-organization-backend-promote, 3d stale) **both merged**. The previous brief's *Open questions* explicitly tracked these.
- **Coverage progress (action-list items whose tracked PR merged):**
  - `feat-folder-organization-document-mobile-implementation-mobile` → PR #1464 ✅
  - `feat-booking-integration-ota-xml-parsing-generation-backend` → PR #1420 ✅
  - `feat-booking-integration-rate-availability-push-flow-backend` → PR #1421 ✅
  - `pm-qa-allowed-pet-types-enum-decode-audit` → PR #1549 ✅
  - `pm-qa-tenant-aware-rls-helper` → PR #1510 ✅
  - `pm-devops-confirm-eas-workflows-healthy` → PR #1526 ✅
  - `pm-devops-pre-push-fmt-gate-scope` → PR #1529 ✅
  - `pm-devops-security-test-gate-required-check` → PR #1530 ✅
  - `pm-devops-app-tsx-merge-queue-confirm` → PR #1540 ✅
  - `pm-qa-dispute-draft-autosave-coverage` → PR #1544 ✅
  - `pm-qa-rls-write-download-coverage` → PR #1456 ✅
  - **assignments.json reflects status `review`/`in-progress` for some of these because the dispatcher respawned sibling implementers (#1554–#1560) before reconciling the merged PRs.** That's a dispatcher-side reconcile gap; the routine just observes.
- **Accounting MVP:** #1453 (N5 PAP-210 bank-statement upload + matching), #1454 (native accounting N1–N4 + @hey-api migration), #1512 (post-#1454 clippy follow-up), #1545 (unbreak reality-web prod build — `/accounting` QueryClient prerender + favorites pl/hu i18n).
- **Follow-up issues created + many closed same window:** #1519 → closed by #1562 (XXE/DTD test guard); #1525 → #1552 (OTA manager-gate); #1527 → #1563 (developer OAuth cross-user RLS); #1528 → #1551 (`document_category` cast); #1534 → #1537 (rental enum write-paths cast); #1524 → #1564 (marketplace idempotency preserved-timestamp test). Six post-merge-review gaps shut in this window.
- **Mobile (KMP + RN):** #1447 (KMP infinite scroll AC-4 evidence), #1450 (push-pref dispatch-target), #1464 (mobile folder org slice), #1514 (reality-mobile 82.1 screen-map), #1515 (epic-82 story-vs-impl mapping), #1516 (reality-mobile deep-linking + URL schemes confirmed), #1541 (FavoritesService session gating + screen-map sync), #1536 (post-#1447 spotless fix).

## Watch
- **#1538 (open, [ci])** — *Backend `test` job is not a required check on `dev`. Red-test PRs merge freely and re-red the merge pipeline.* This is the same shape as the #1437 incident (botched merge slipped past CI). Labeled & scoped; not yet on the action-list. Strong candidate for a follow-up.
- **Closed-not-merged this run: #1511** — `fix(api-server): resolve clippy lints blocking dev fmt-clippy gate` superseded same-day by #1512. Logged as `closed-not-merged-pr-1511` (vector=dx, score 1).
- **7 same-day draft PRs (#1554–#1560)** all opened 2026-06-18 by martin-janci as auto-impl respawns for action-list items whose prior PR already merged. None are stalled yet (<24h), no `stalled-review` signal — but if they linger past 7d the dispatcher needs to reconcile assignments.json against the merged PRs first.
- **Churn:** `backend/servers/api-server/src/routes/forms.rs` re-emit (`runs_seen` 2→3, cumulative; PR #1456 extended RLS write/create coverage). No new hotspots derived from `files` data this run — commit listing returns metadata-only; precise file churn would need per-commit `gh api .../commits/{sha}` fan-out, deferred under token budget.
- **Reverts:** **none** this run.

## PRs stuck in draft despite approval
- none (all 7 open drafts <24h old, no approvals yet)

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **none — Phase 1.5 skipped this run** (reason: token budget + buffer-low refill context). `state.review_cursor` unchanged.
- Experts: n/a
- Findings: **0**
- Next segment: **ppt-web-core** (last reviewed 2026-06-05 → 13d stale; oldest unreviewed by 1d over mobile-native-kmp at 12d).
- Skipped: yes — token budget; cursors preserved so the rotation resumes exactly here next run.

## Screen-map status
- Total `docs/screens/` files: **128** (ppt: 80, reality: 48 — +1 from #1514 added `82-1.md`; mobile: 0 not yet seeded).
- Drift signals this run: **0** derived (per-PR file fan-out deferred under token budget; no obvious drift in screen-touch PRs #1514/#1541, both of which paired code + doc).
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New:** [1] `closed-not-merged-pr-1511` — `fix(api-server) clippy lints` closed unmerged, superseded same-day by #1512.
- **Bumped:** none (re-emit of `churn-hotspot-…-forms-rs` is cumulative, `score_delta=0`).
- **Decayed:**
  - `bug-risky-churn-pr-963-api-main-rs` — 2→1 (no signal in 14d, last 2026-06-03)
  - `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` — 2→1
- **Dropped (decay to 0):**
  - `triage-dispatcher-mcp-push-large-file-issue-1014` — 1→0
  - `triage-issue-951` — 1→0
- **Resolved:**
  - `refactor-churn-hotspot-mobile-announcements-test` → `done` (PR #1543 *refactor-churn-hotspot-mobile-announcements-test: stabilize AnnouncementsScreen date test by pinning TZ=UTC* — verbatim id-match in PR title; test instability resolved).

## Plans promoted
- **none** — top open items at score ≤2; security fast-track unmet (no `security`-vector item at confidence=high in window). 6 plans already in `plans/` carry the ready/active load; the implementer pipeline is saturated.

## Auto-fix
- `no candidate` — `closed-not-merged-pr-1511` (score 1, below ≥3 bar); no `screen-map-drift`/`screen-map-orphan`/`fixme-in-merged-code`/`lockfile-drift dep-update` this run; `stalled-review` not eligible (no PR >7d).

## Self-review findings
- none (`.research/self-improvement/findings.json` absent)

## Open questions
- **Dispatcher-side reconcile gap (the buffer-low payload's actual root cause).** The routine-fire payload reported `claimable=0/72`; under the hood, **all 13 open action-list items are tracked in assignments.json as in-progress/review/quarantined, but 11 of them have merged PRs already** (#1464, #1420, #1421, #1450, #1502, #1454, #1510, #1526, #1529, #1530, #1540, #1544, #1456, #1549) — the dispatcher hasn't flipped `status: done` and respawned them as #1554–#1560 instead. **Decision needed:** reconcile assignments.json against merged PRs (dispatcher Phase 2), OR run `/ppt-project-management scan` to rebuild action-list from coverage.json and pick up the 20 `partial` stories that have no claim yet. Best-effort suggestion: re-run dispatcher with a hard reconcile pass, then refresh action-list — this run's routine deliberately did NOT touch action-list/assignments to stay in the lane.
- **#1538 — make backend `test` a required check on `dev`.** Filed as an issue; not yet on the action-list. Recommend pm-devops slot it ahead of further security/RLS work — the #1437 incident shows the cost of not having this gate.

## Quiet day?
- **No.** 49 merged PRs, 1 backlog row resolved, 4 items decayed, 1 new vector, dispatcher-buffer reconciliation context surfaced. `stats.runs` 40→41, `vectors_created` 173→174, `quiet_days` unchanged (1).
