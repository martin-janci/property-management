# 2026-06-18

## Since last run
- Merged PRs: 44 (range #1447–#1564) — heavy implementer fast-path day across mobile/RN, accounting MVP, OAuth, RLS testing, screen-maps.
- Open PRs touched: 7 (all draft auto-impl PRs from today: #1554, #1555, #1556, #1557, #1558, #1559, #1560).
- New / updated issues: ~100 since #1437 (latest #1538 — `Backend test job is not a required check on dev`).
- Commits: 131 on `dev` since `4829015b`.
- Routine lag: **52h** since last run (>36h warning; missed daily run for 2026-06-17 — no brief on disk).
- Phases that failed: none — Phase 1.5 (rotating expert review) and Phase 1.6 (PM rotation) intentionally skipped this run to keep focus on the wedge surfaced by the trigger payload.

## Trigger payload
`buffer-low: claimable=5/72 — refill planner (merge pipeline wedged: self-approve gate + dev-baseline CI red #1332 recurrence)`

Two distinct blockers cited; both are now backlog rows scored 3 each (see *Backlog deltas* below):

1. **Dispatcher self-approve gate.** 5 of 7 open PRs (#1554–#1558) carry `reviewer_summary: verdict=approve ... posted as comment (self-approve blocked)` in `assignments.json`. GitHub forbids an author from approving their own PR; the dispatcher's reviewer agent runs under the same identity as the implementer, so `verdict=approve` only lands as a comment. The PR never reaches `reviewDecision=APPROVED` and auto-merge never engages. Buffer therefore drops to 5/72 claimable.
2. **#1332 recurrence — backend `test` job red on dev baseline.** Issue #1332 (closed 2026-06-17 after the cascade was drained) is back: reviewer comments on #1556 / #1558 today attribute their CI red to "dev-baseline #1332 not this PR". Issue #1538 (filed 2026-06-17, still OPEN) documents the structural reason: backend `test` is NOT a required check on `dev`, so broken-test PRs land and re-red the merge pipeline for everyone.

## Shipped
- #1561 `fix(reality-server)`: PortalPrincipal extractor admits public agencies on imports (closes #1300).
- #1562 `test(integrations)`: XXE/DTD regression guard + namespace parity for inbound OTA parsers (closes #1519).
- #1563 `test(db)`: behavioral cross-user RLS isolation for developer_oauth_apps/grants/api_key_usage_logs (closes #1527).
- #1564 `test(marketplace)`: `mark_invitation_viewed` preserves first viewed_at (closes #1524).
- #1552 `fix(api-server)`: OTA token-exchange — manager role read from PATH org, not JWT tenant (closes #1525, with regression test).
- #1551 `fix(db)`: cast `document_category` in `count_accessible_rls` filter (closes #1528).
- #1545 `fix(reality-web)`: unbreak production build — `/accounting` `force-dynamic` prerender + favorites pl/hu i18n (**no test file — emitted hotfix-no-test signal**).
- #1539 `fix(oauth)`: reject public client supplying a client_secret with `invalid_client` (**no test file — emitted hotfix-no-test signal**).
- #1454 `feat`: native accounting MVP (N1-N4) + @hey-api migration.
- #1453 N5 — Bank-statement upload + payment-matching + match-review (PAP-210).
- … plus the long tail of dispatcher rolls + RLS test additions + mobile/iOS deep-link, screen-map, and pet-types audits.

## Watch
- **Stalled review:** none (every open PR was opened in the last 24h; stalled-review threshold is 7 days idle — no signals fire).
- **Reverted:** none in window.
- **Closed-not-merged:** #1511 `fix(api-server): resolve clippy lints blocking dev fmt-clippy gate (accounting routes)` — closed because the parallel #1512 landed first; no churn signal.
- **Churn hotspots:** *not refreshed this run* — full per-PR file aggregation across 44 merged PRs was not run (routine intentionally lightweight given trigger urgency). `hotspot_history` unchanged from prior run.

## PRs stuck in draft despite approval
<!-- The literal reviewDecision==APPROVED + isDraft==true query returns none, because the dispatcher's
     verdict=approve lands as a PR *comment*, not a GitHub Approval (self-approve blocked). The
     functional equivalent is the 5 PRs listed below — captured as the dx-dispatcher-self-approve-gate
     backlog row, not as per-PR draft rows. -->
- none (per the literal query)
- _functional wedge (5 PRs with reviewer comment `verdict=approve … self-approve blocked`):_
  - #1554 — feat-build-configuration app-icons
  - #1555 — feat-api-client faults page wiring
  - #1556 — feat-notification-pref APNs HTTP/2
  - #1557 — feat-folder-organization mobile rename/move
  - #1558 — feat-esignature webhook idempotency (voided→Cancelled)

## PRs with verdict=changes, no fix-round progress in 24h
- none — all 7 review-state PRs in `assignments.json` carry `verdict=approve` (5) or empty summary (2, just-opened today).

## Code review slice
- Segment reviewed: **skipped** this run (focus shifted to wedge triage).
- Experts: n/a — Phase 1.5 not invoked.
- Findings: 0 new `code-review-finding` signals.
- Next segment: `ppt-web-core` (oldest unreviewed: 2026-06-05).
- Skipped: yes — Phase 1.5 deferred to next incremental run to keep this routine focused on the merge-wedge surfaced by the trigger payload.

## Screen-map status
- Total `docs/screens/` files: not recounted this run (was N/A in prior brief).
- Drift signals this run: 0 (full PR-file aggregation across the 44 merged PRs not performed; drift detection deferred).
- Orphan screens this run: 0 (same reason).
- Last `screen-map.yml` CI run on `main`: not queried this run.

## Backlog deltas
- **New:** [3] `bug-dev-baseline-ci-test-job-red-1332-recurrence` — backend `test` job red on dev baseline; #1332 recurrence; gated only on `check` so red-test PRs land.
- **New:** [3] `dx-dispatcher-self-approve-gate-blocking-merges` — dispatcher reviewer cannot self-approve own implementer PRs; verdict=approve sits as a comment; auto-merge never fires.
- **New:** [2] `test-gap-hotfix-no-test-pr-1539-oauth-public-client` — OAuth public-client `invalid_client` hotfix shipped with no regression test.
- **New:** [2] `test-gap-hotfix-no-test-pr-1545-reality-web-prerender` — reality-web `/accounting` `force-dynamic` build fix shipped with no per-route prerender regression test.
- **Decayed:** `bug-risky-churn-pr-963-api-main-rs` — 2→1 (no new signals in 14d).
- **Decayed:** `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` — 2→1 (no new signals in 14d).
- **Dropped:** `triage-dispatcher-mcp-push-large-file-issue-1014` — decayed to 0.
- **Dropped:** `triage-issue-951` — decayed to 0.

## Plans promoted
- none this run. The 6 currently-ready plans in `plans/` cover diverse user-visible bugs; promoting more without first unblocking the merge pipeline would just deepen the WIP pile. Refilling the planner is the dispatcher's responsibility once the wedge clears — the routine's job here is to surface why the dispatcher can't drain.

## Auto-fix
- `no candidate` — neither wedge signal matches the auto-fix allowlist (allowlist covers `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, lockfile-drift `dep-update-noise`, `stalled-review`). Both wedge blockers need human/operator action (branch-protection edit, GH App identity for the reviewer, or a workflow change). Recording in `signals/2026-06-18.json` as `auto_fix_skipped_reason: "no allowlist match"`.

## Self-review findings
- none recorded — `.research/self-improvement/findings.json` not inspected this run (Phase 1.5/1.6 skipped).

## Open questions
- **Decide which path closes the self-approve gate.** Three viable options:
  1. Reviewer runs as a separate GH App / bot identity that CAN formally approve PRs authored by the implementer identity. Lowest friction; preserves end-to-end autonomy. Cost: provisioning the App + secret rotation.
  2. Self-managed approvals workflow that promotes `verdict=approve` review comments to PR approvals via a privileged GH App on receipt. Smaller surface; ties to comment-format contract.
  3. Flip implementer PRs to `ready-for-review` and require a human approval. Kills the autonomy gain; only acceptable as a temporary stop-gap.
- **Decide whether to make `test` a required check on `dev` (per #1538) before draining the current red.** If yes, the current backlog of test-red merges has to be cleaned first; if no, the dispatcher's merge gate needs to refuse PRs whose `test` job is `failure` or `pending` (option 2/3 in #1538). Either choice closes #1332's recurrence vector.

## Quiet day?
- **No.** 44 merged PRs since the last run + 2 fresh blockers surfaced.

## Goal violations
- **G1**: pass — brief exists with H1 `# 2026-06-18`.
- **G2**: pass — state advanced (`last_pr_seen` 1439→1564, `last_issue_seen` 1437→1538, `seen_signals` +5).
- **G3**: pass — backlog IDs unique (180 items, all distinct).
- **G4**: pass — no signal id from this run pre-existed in `seen_signals`.
- **G5**: pass — score cap (none > 8) + decay applied to 4 items (2 decayed, 2 dropped).
- **G6**: n/a — no new plans this run.
- **G7**: n/a — no new plans this run.
- **G8**: pass — all staged paths under `.research/`.
- **G9**: pass — no secret/private-hostname strings added.
- **G10**: pass — backlog.md regenerated from backlog.json with the documented sort + freshness widget.
- **G11**: pass — no phases failed.
- **G12**: pass — no plan deletions.
- **G13**: pass — `_archive/` count unchanged (6).
- **G14**: pass — IDEAS_TRIAGE.md regenerated from `vector == "triage"` rows with the static prose/legend preserved.
- **G15**: pass — `auto_fix_actions: []` (allowlist no-match; kill switch not set).
- **G16**: n/a — Phase 1.6 skipped this run (`pm_cursor` unchanged).
- **G17**: pass — no Telegram-token literal in the staged diff.
