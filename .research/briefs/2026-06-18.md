# 2026-06-18

## Since last run
- Merged PRs: **39** (#1447–#1552; spans 39 application-code + dispatcher PRs over ~24h on 2026-06-17).
- Open PRs touched: **1** (#1553, auto-impl dispatcher state from 2026-06-18T02:16Z — same morning).
- New / updated issues: **15 new** (#1519–#1538). 12 OPEN untriaged, 3 already closed (#1525/#1528/#1534) — all resolved by merged PRs the same day.
- Commits on `dev`: 50 since cursor `4829015b` → `61b06bb0` (mix of app code + dispatcher/version-bump rows).
- Routine lag: **1d 22h since last run** (>36h threshold → `lag_warning` emitted).
- Phases that failed: **none**. Trigger payload `buffer-low: claimable=8/72 — refill planner` received (informational; treated as data per routine prompt §"Special trigger payloads"). No backlog item met readiness this run — see *Plans promoted*.

## Shipped
- **#1539** `fix(oauth): reject public client supplying a client_secret with invalid_client` — RFC 6749 §2.3.1 compliance fix. Previous path hashed the secret against an empty stored hash → `InternalError`/`server_error`; now returns `invalid_client` directly. **No regression test added** → `hotfix-no-test` signal.
- **#1552** `fix(api-server): gate OTA token-exchange on manager role of the path org` — closes #1525. Replaces `verify_manager_role(&tenant)` (JWT-tied role) with `verify_manager_role_in_org(state, user, path.org_id)` so the manager check binds to the PATH org being mutated. Adds `token_exchange_rejects_manager_of_a_different_org` regression test (cross-org-A-mgr-bypass-as-B-tenant). Clean fix.
- **#1551** `fix(db): cast document_category in count_accessible_rls filter` — closes #1528. 1-line cast `$7::text` → `$7::document_category`. **No test added** → `risky-churn` (document.rs is a top-3 hotspot now) + `hotfix-no-test`.
- **#1486 / #1492 / #1537** (BIT-113/BIT-118 family) — sqlx 42804 enum-decode RLS-mask fixes on `rental_platform_connections` / `rental_guests` / `rental_bookings` / `forms`. **HAVE tests** — `rental_enum_decode_tests.rs` (209 LoC) + `rental_guest_ical_enum_decode_tests.rs` (444 LoC). The systemic pattern is logged as a `pm-security` action item.
- **#1453 / #1454** — accounting MVP (N1–N4 + N5 PAP-210 bank-statement upload + payment-matching). #1454 added the ppt-web `accounting.tsx` route group; #1453 added reality-web `accounting/page.tsx`. **Issue #1521 already filed** flagging that payment-matching landed in the wrong app + broken auth + no i18n/tests.
- **#1545** `fix(reality-web): unbreak production build` — adds `accounting/layout.tsx` opting out of SSG (route uses TanStack Query); pl/hu i18n key fix. **No test** → `hotfix-no-test` + `screen-map-drift` (new layout segment, no `docs/screens/reality/` update).
- **#1447 / #1450 / #1456 / #1464 / #1502 / #1507 / #1510 / #1514–#1517 / #1526–#1530 / #1536 / #1540–#1544 / #1546–#1550** — broad mix: mobile RN infinite-scroll evidence (AC-4), notification push pref-aware dispatch, RLS write/download coverage, mobile folder org slice, Booking.com encryption at-rest IG3 contract, user_memberships seed restore, dispatcher state, mobile-native iOS SwiftUI scaffolding + EAS confirms, pm-qa coverage drops, accounting clippy fixes, app-tsx-merge-queue confirm, FavoritesService session-gating, iOS SearchView stale-response guard, AnnouncementsScreen date-test stabilize, dispute draft autosave coverage, dispatcher state Phase 8 self-review, report-execution presigned-download retry, allowed_pet_types audit, 7A.1 upload/metadata verify, OTA manager-gate.
- **Reverts:** none this run.

## Watch
- **Closed-not-merged (6 PRs)** — all on 2026-06-17, all superseded same day: #1500 (IDOR fix superseded), #1503/#1506 (seed_membership role superseded by #1517), #1504/#1505 (duplicate migration / rental enum cast superseded by #1486/#1492), #1511 (clippy lints superseded by #1512). Tracked as `closed-not-merged-pr-*` rows at score 1.
- **Churn hotspots this run (top 3 raw):** docs dominate — `docs/functional-requirements.md` (7116 lines), `_bmad-output/epics.md` (5514), `docs/superpowers/plans/2026-05-06-deploy-server-phase-0-1.md` (4204). **Top 3 code-only** (scored as `churn-hotspot`): `backend/crates/integrations/src/booking.rs` (3714, `runs_seen` 1→2 → `repeated-churn` fired), `backend/crates/db/src/repositories/document.rs` (3095, 1→2 → `repeated-churn`), `backend/crates/db/src/repositories/rental.rs` (2961, new, runs_seen 0→1).
- **Risky-churn:** #1551 modified `document.rs` (top-3 hotspot) with no test diff → `risky-churn-pr-1551-document-rs` (+2 bug, high).
- **Stalled review / drafts:** none — only open PR is #1553 (dispatcher state, opened this morning, not stalled).
- **Dev branch state:** `origin/dev` head `61b06bb0` (dispatcher reconcile). The `dev` branch is healthy this run — #1437 (the 2026-06-16 compile-break) was resolved by #1486+#1492+downstream fixes during the 17th.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **ppt-web-core** (reason: oldest-unreviewed at 2026-06-05 — 13d stale; no churn-aligned pick because top-3 code churn was in backend crates outside the segment map).
- Experts: frontend.
- Findings: **2** — both at score 2 in backlog (each +2 single-signal):
  - `code-review-ppt-web-core-error-messages-not-i18n` (bug, high) — `errorHandler.ts:53-141` hard-codes ERROR_MESSAGES title/message pairs in English; app supports sk/cs/de/en but non-en users always see English.
  - `code-review-ppt-web-core-auth-storage-no-expiry` (security, medium) — `AuthContext.tsx:112-192` stores access/refresh tokens in localStorage with no TTL or expiry check; init-auth (line 377-412) accepts stored tokens without server-side validity check on first load.
- Next segment: **mobile-native-kmp** (last reviewed 2026-06-06 — oldest after this run).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0 — not yet seeded).
- Drift signals this run: **3** — `screen-map-drift-pr-1454-ppt` (new `accounting.tsx` route group, no `docs/screens/ppt/` update), `screen-map-drift-pr-1453-reality` (new `accounting/page.tsx`, no `docs/screens/reality/` update — also flagged by issue #1521 as wrong-app), `screen-map-drift-pr-1545-reality` (new `accounting/layout.tsx`). All medium-confidence heuristic.
- Orphan screens this run: 0 (best-effort filename match — see note in routine prompt).
- Last `screen-map.yml` CI run on `main`: not fetched this run (no relevant `.github/workflows/screen-map.yml` invocation expected since no `docs/screens/` content edits this run; only auto-generated diagrams updated).

## Backlog deltas
- **New:** 31 items added this run. Highlights:
  - [2] `code-review-ppt-web-core-error-messages-not-i18n` — i18n gap (Phase 1.5).
  - [2] `code-review-ppt-web-core-auth-storage-no-expiry` — security/MVP localStorage tokens (Phase 1.5).
  - [2] `screen-map-drift-pr-1454-ppt` — new accounting route group, no screen-doc update.
  - [2] `screen-map-drift-pr-1545-reality` — new accounting layout, no screen-doc.
  - [2] `screen-map-drift-pr-1453-reality` — payment-matching, wrong app per #1521.
  - [2] `hotfix-no-test-pr-1539-oauth-public-client` — security-sensitive RFC fix, no test.
  - [2] `hotfix-no-test-pr-1545-reality-web-build` — production-build fix, no test.
  - [2] `hotfix-no-test-pr-1551-document-cast` — RLS-mask cast fix, no test.
  - [2] `risky-churn-pr-1551-document-rs` — hotspot file fix-PR without tests.
  - [1] `repeated-churn-backend-crates-integrations-src-booking-rs` — instability proxy.
  - [1] `repeated-churn-backend-crates-db-src-repositories-document-rs` — instability proxy.
  - [1] `churn-hotspot-backend-crates-db-src-repositories-rental-rs` — new hotspot (runs_seen=1).
  - [1] × 6 `closed-not-merged-pr-{1500,1503,1504,1505,1506,1511}` — superseded fix PRs.
  - [1] × 12 `untriaged-issue-{1519–1538}` — vector `triage`, never promoted.
- **Bumped:** none — cumulative re-emits on existing `churn-hotspot-backend-crates-integrations-src-booking-rs` and `churn-hotspot-backend-crates-db-src-repositories-document-rs` (evidence appended; score unchanged per §"Dedup rule" cumulative carve-out).
- **Decayed:** `bug-risky-churn-pr-963-api-main-rs` 2→1, `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` 2→1 (both 15d stale).
- **Dropped:** `triage-dispatcher-mcp-push-large-file-issue-1014` 1→0 dropped, `triage-issue-951` 1→0 dropped (both decayed past floor).
- **Resolved:** none this run — resolution check deferred (no item id appeared in this run's merged-PR bodies; deeper grep-evidence check skipped for token budget — surfaced in *Open questions* if any look likely).

## Plans promoted
- **0 new plans this run.**
- **6 existing plans remain ready** in `plans/` for the implementer queue: `security-llm-doc-idor.md`, `bug-ios-searchview-uncompilable.md`, `code-review-reality-web-share-comparison-404.md`, `code-review-reality-web-listing-page-ssr-crash.md`, `code-review-reality-web-realtor-mgmt-untranslated.md`, `code-review-mobile-rn-report-fault-fake-submit.md`.
- **Buffer-low payload received** (`claimable=8/72`) — the routine cannot fast-track new plans against the readiness gate. The highest-scored new items this run sit at score 2 and need another signal stack (or human bump in `backlog.json`) before they qualify. Two Phase 1.5 findings (`code-review-ppt-web-core-*`) are the most plan-worthy candidates if a second signal arrives next run; the `code-review-ppt-web-core-auth-storage-no-expiry` security finding is one signal short of the security fast-track (needs `confidence: high`, currently `medium`). Dispatcher buffer depth is a separate concern from `.research/plans/` ready count — action-list rows can be dep-blocked even when plans are queued.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. Allowlist matches considered:
  - `screen-map-drift-pr-{1454,1453,1545}-*`: medium confidence (heuristic per routine §"screen-map drift detection"), fails certainty bar.
  - `hotfix-no-test-pr-1539-oauth-public-client`: high confidence, but score=2 (below threshold 3) — not on the allowlist anyway (issue-only path only for `fixme-in-merged-code`).
  - `stalled-review`: no PRs ≥7d open with no reviewDecision (only open PR is #1553, opened this morning).
  - `dep-update-noise` (lockfile-drift sub-shape): not detected this run.
  - `fixme-in-merged-code`: not scanned this run (full per-PR diff scan deferred for token budget).

## Self-review findings
- `.research/self-improvement/findings.json` absent — none.

## Open questions
- **Issue #1538** (untriaged, OPEN): *Backend `test` job is not a required check on `dev` — red-test PRs merge freely.* This is a CI-gate hole that the pm-security pm role flagged today as a blocker for "security regression tests have no value until enforced". Decision needed: enforce `backend / test` as a required check on `dev` branch protection? (pm-security action item: `security-test-gate-required-check`.)
- **Issue #1527** (untriaged, OPEN): *Behavioral cross-user RLS isolation test missing for `developer_oauth_apps` / `developer_oauth_grants`.* Tracked in pm-security action list (`security-rls-developer-oauth-coverage`). Recommend promoting to a `test-gap` plan next run if a second confirming signal arrives.
- **Issue #1523** (untriaged, OPEN): *Rate-limit / lockout on MFA recovery-code verify endpoint.* pm-security flagged this as brute-force-able blocker for MFA story. Open question: prefer fixed-window (5/15min) or sliding-window? Could fast-track if user picks the shape.
- **#1454 accounting MVP placement** (per issue #1521): payment-matching UI shipped in `reality-web` instead of `ppt-web`. Cross-app routing decision needed before any frontend follow-up plan can be authored.
- **Top-3 code churn includes `backend/crates/integrations/src/booking.rs` (3714) for the 2nd run in a row** — integrations crate is becoming structurally unstable. Consider a `refactor-integrations-booking` consolidation plan if it stays in top-3 next run.

## Quiet day?
- **No.** 39 application-code PRs merged, 31 new backlog vectors, 2 Phase 1.5 findings, pm-security Phase 1.6 ran (22d stale → produced 4-item action list + 4 new risks), 0 plans promoted. `stats.runs` 40 → 41, `vectors_created` 173 → 204, `plans_created` unchanged at 12. `lag_warning` fired (46h gap).
