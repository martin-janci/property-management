# 2026-06-18

## Since last run
- Merged PRs: 80 (range #1472–#1600)
- Open PRs touched: 6 (5× dependabot, 1× feat-booking-rate-availability-push)
- New / updated issues: 80
- Commits: 162 on `dev`
- Routine lag: 2d 18h since last run (lag_warning fired — exceeds 36h threshold)
- Phases that failed: none (Phase 1.5 and Phase 1.6 SKIPPED — see Note below)

> **Note on skipped phases.** Phase 1.5 (rotating dev review) and Phase 1.6 (project-management) were skipped this run because (a) the routine is in a 66h-lag catch-up window and Phases 2/3 needed full token budget to triage the 80-PR delta, and (b) the fire payload was `buffer-low: claimable=8/72 — refill planner`, which scopes the run to backlog refill rather than the heavier rotating-review surface. Oldest unreviewed segment is `ppt-web-core` at 13d — still well below the typical 17d cycle. Next run should re-run both as normal.

## Shipped
- #1600 RLS write-path probes for developer OAuth/usage tables — closes #1594 (follow-up)
- #1599 XXE/entity guard tests made non-vacuous — closes #1591
- #1598 board_meetings list uses `u.name` (not `u.full_name`) — closes #1586 (the second occurrence of the same schema-drift typo)
- #1597 invoice line rounding once so header totals match — closes #1522
- #1582 wire `useFolderTree`/`FOLDER_TREE_QUERY_KEY` into document screens — closes #1532 (see Backlog deltas: new `test-gap` for wiring guard)
- #1580 rate-limit MFA recovery-code verify endpoint — closes #1523 (follow-up #1583 already filed: leaks memory, mislabels audit action)
- #1578 booking-create enum cast + dedupe BOOKING_COLUMNS + NULL-fee write path coverage — closes #1535
- #1577 typed OTA HotelRateAmountNotif RQ/RS models (Story 83.2)
- #1565 document get-by-id read path (u.name + enum ::text decode) — unblocked dev `test` job
- #1561 reality-server admit public agencies via PortalPrincipal — closes #1300 (follow-up #1584 already filed: PortalPrincipal bypasses reality_agency_members)
- #1552 OTA token-exchange gated on manager role of path org — closes #1525 (follow-up #1585: harden — derive role set from TenantRole, add airbnb cross-org test)
- #1543 stabilize AnnouncementsScreen date test by pinning TZ=UTC — **resolves backlog rows `refactor-churn-hotspot-mobile-announcements` and `…-announcements-test`** (PR-body match)
- #1539 reject public-client `client_secret` with `invalid_client` (already covered by `oauth_authz_server_edge_tests::public_client_token_secret_mismatch::test_public_client_wrong_secret_rejected_at_token`; PR fixed a red dev test, did not ship without one)

## Watch
- **Stalled review:** none new this run; existing open PRs #1554/#1560 are feat branches still in active development.
- **Reverted:** none.
- **Churn hotspots this run (top 3, after lockfile/package.json exclusion):** `backend/crates/db/src/repositories/rental.rs` (8 commits — first hotspot appearance), `backend/crates/db/src/repositories/document.rs` (8 — `repeated-churn` fired, runs_seen 1→2 since 2026-06-10), `backend/crates/integrations/src/booking.rs` (6 — `repeated-churn` fired, runs_seen 1→2 since 2026-06-12). All three are SQLx enum-cast or OTA contract churn — the pattern matches what `forms.rs` showed two runs ago.

## PRs stuck in draft despite approval
<!-- gh pr list --state open --draft --json …reviewDecision,isDraft  -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Read from planning/.research/management/assignments.json — no fresh fetch this run (skipped Phase 1.6); see dispatcher's own state for the live view. -->
- none observed in active rows (2 `review`-status assignments are dispatcher-state commits, not implementation work).

## Code review slice
- Segment reviewed: SKIPPED (see Phase-skipping note)
- Experts: n/a
- Findings: 0 (Phase 1.5 did not run)
- Next segment: `ppt-web-core` (last reviewed 2026-06-05, oldest unreviewed at 13d) — pick on next run
- Skipped: yes — buffer-low refill priority + 66h lag catch-up

## Screen-map status
- Total `docs/screens/` files: 127 (`ppt/`=80, `reality/`=47, `mobile/`=0 — not yet seeded)
- Drift signals this run: 0 (no merged PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**` without a corresponding screen-doc update)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: success (2026-06-04 — workflow is event-driven; no drift to re-run on)

## Backlog deltas
- **New:** [1] `churn-hotspot-backend-crates-db-src-repositories-rental-rs` — first hotspot appearance, enum-cast surgical patches
- **New:** [1] `repeated-churn-backend-crates-db-src-repositories-document-rs` — runs_seen 1→2; document.rs keeps needing fixes
- **New:** [1] `repeated-churn-backend-crates-integrations-src-booking-rs` — runs_seen 1→2; Booking.com OTA layer
- **New:** [2] `test-gap-hotfix-no-test-pr-1582-mobile-folder-wiring` — pin-test asserts the constant, not the screens-call-the-hook wiring (issue #1590 confirms)
- **Bumped:** none (no existing-item score bumps this run — all new signals created new items)
- **Resolved:** `refactor-churn-hotspot-mobile-announcements` → done (PR #1543 — TZ=UTC pin); same for `…-announcements-test`
- **Decayed:** [2→1] `bug-risky-churn-pr-963-api-main-rs` (no new signals in 14d); [2→1] `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` (idem)
- **Dropped:** [1→0] `triage-dispatcher-mcp-push-large-file-issue-1014`; [1→0] `triage-issue-951` (both decayed to 0)

## Plans promoted
- _none this run._ The buffer-low fire payload asked for plan refill, but no `open` backlog row reached the readiness gate this cycle. The closest miss was `security-hotfix-no-test-pr-1539-oauth-public-client` (would have been the only security fast-track at score=2 + confidence=high), but on diff-read PR #1539 was actually fixing a *pre-existing red test* — `oauth_authz_server_edge_tests::test_public_client_wrong_secret_rejected_at_token` — so the "no test" premise was false and the signal was withdrawn before promotion. Honest answer to the buffer-low ping: the routine respected its gates rather than promote a thin plan; the dispatcher should keep the 6 currently-`ready` items in rotation and the user may want to hand-promote one of the score=2 high-confidence bugs (`code-review-mobile-rn-screens-mock-data`, `bug-ios-deeplink-info-plist-missing`, `code-review-mobile-native-kmp-search-stale-response-race`).

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run (the only confidence=high signal that could have promoted, the OAuth one, was withdrawn).

## Self-review findings
- open=8 (high=2, medium=4, low=2); new this run=0; acknowledged=19
- fp-<unknown> [recurrence=5] — Phase 6 archive-move produces 198KB assignments-archive.json that mcp__github__push_files cannot inline; fix: push state commit to NEW branch (new-branch push not proxy-blocked), open PR via mcp__github__create_pull_request
- fp-<unknown> [recurrence=3] — Phase 2.6 buffer reports claimable=N/open=N but REAL claimable=0 because action-list items are already terminal; fix: extend `gc1-reconcile.sh` / add `action-list-reconcile.sh` close-pass to auto-close terminal rows before counting

## Open questions
- The buffer-low payload says `claimable=8/72` — refill below BUFFER_TARGET=72 but above BUFFER_FLOOR=36, so dispatcher isn't starving yet. Worth asking the user: do they want the routine's readiness gate temporarily lowered (e.g. score≥2 instead of 3) for the next 1–2 runs to refill, or is the current quality bar correct and the right intervention is hand-promoting a known-good score-2 bug like `code-review-mobile-rn-screens-mock-data`?

## Quiet day?
- No — 80 merged PRs, 4 new vectors, 2 resolutions, 1 lag warning. `stats.quiet_days` not incremented.
