# 2026-06-18

## Since last run
- Merged PRs: **46** (#1197, #1316, #1420–#1421, #1447, #1450, #1453–#1454, #1456, #1460, #1464, #1467, #1470, #1473, #1476, #1486, #1491–#1492, #1501–#1502, #1507–#1518, #1526, #1529–#1530, #1536–#1545, #1547–#1552) — bulk dispatcher-driven story coverage + a wave of `from-merged-review` follow-up fixes (OAuth, RLS, enum casts, screen-maps).
- Open PRs touched: **0** newly active against `dev` — implementer queue is fully drained.
- New / updated issues: 50 in window — all labeled (`follow-up`, `from-merged-review`, `bug`). 12 still **open** (#1519–#1524, #1527, #1531–#1533, #1535, #1538). No `untriaged-issue` signals fired (every new issue has a label).
- Commits on `dev`: 100 since `4829015b` → `2ddabfe3` (real-code + dispatcher state commits, all the merged PR landings).
- Routine lag: **~45h** since last run — `lag_warning` emitted (>36h, <3d so not yet stale).
- Trigger payload (informational, not a directive): `buffer-low: claimable=14/72 (after closing 21 shipped-but-open leaks gc1/dev-reconcile missed) — refill backlog from coverage.json gaps`. Logged here; the routine works on observed signals, not coverage refill. 20 partial-status stories remain in `coverage.json` (epics 6/7a/8a/79/80/81/82/83/84/85) — the dispatcher's coverage upkeep owns those, not this routine.
- Phases that failed: **none**.

## Shipped
- **#1552 / #1551 / #1549 / #1548 / #1550** — final close-out wave on `from-merged-review` follow-ups merged within the last 4 hours of the window: OTA token-exchange manager-gate (closes #1525), `document_category` cast in `count_accessible_rls` (closes #1528), `allowed_pet_types` enum decode audit (#1363, #1366), UC-81 presigned-download retry coverage, 7A.1 upload metadata round-trip + promotion.
- **#1545** — `fix(reality-web): unbreak production build` — `/accounting` QueryClient prerender + favorites `pl`/`hu` i18n. Production build was red; this stabilizes it.
- **#1454 / #1453** — `feat: native accounting MVP (N1-N4) + @hey-api migration` and `N5 — Bank-statement upload + payment-matching + match-review (PAP-210)`. Lands native accounting end-to-end across ppt-web + reality-web — but no `docs/screens/{ppt,reality}/` updates → three `screen-map-drift` signals (heuristic, see Backlog deltas).
- **#1539** — `fix(oauth): reject public client supplying a client_secret with invalid_client` — RFC 6749 §2.3.1 conformance gap closed.
- **#1537 / #1486 / #1492 / #1422** — `rental_*` table enum-cast and nullable-Decimal-decode follow-ups closing GH #1008 schema-drift work. Five touches on `backend/crates/db/src/repositories/rental.rs` → new churn-hotspot signal.
- **#1473 / #1467 / #1460** — BIT-85 (cross-tenant OAuth upsert hazard + manager gate), BIT-78 (`mfa.verify_recovery_code` RLS-enforce + cross-user IDOR test), BIT-76 (FORCE RLS on `developer_oauth_apps/grants/api_key_usage_logs`).
- **#1447** — `feat-home-and-search-screens-infinite-scroll-evidenced-mobile: evidence AC-4 infinite scroll (reality-mobile/KMP)`. Story 82.3 evidence pass.
- **#1543** — `refactor-churn-hotspot-mobile-announcements-test: stabilize AnnouncementsScreen date test by pinning TZ=UTC`. Closes both `refactor-churn-hotspot-mobile-announcements` and `refactor-churn-hotspot-mobile-announcements-test` backlog rows (verbatim id match → resolved).
- **#1316 / #1197** — late re-merges of `verify-document-folder-organization-backend-promote` and `test-oauth-authorization-server-integration` (PR numbers predate the cursor; merged dates land in window).

## Watch
- **Buffer pressure**: dispatcher trigger payload says `claimable=14/72` after gc1/dev-reconcile closed 21 shipped-but-open leaks. Routine acted on observed signals; coverage-gap refill is the dispatcher's job (`scan` mode against `coverage.json`). 20 `partial` stories remain in `coverage.json` across 10 epics.
- **Churn hotspots this run (top 3 after exclusions):**
  - `backend/crates/db/src/repositories/rental.rs` — 5 touches (NEW). Rental enum cast + Decimal decode hot-fix rounds.
  - `backend/servers/api-server/tests/booking_oauth_routes_tests.rs` — 4 touches (re-emit cumulative, `hotspot_history.runs_seen` now 2).
  - `_bmad-output/implementation-artifacts/sprint-status.yaml` — 3 touches (sprint tracker, not code — no signal).
- **Stalled reviews:** none open against `dev` this run.
- **Reverts:** none in window.
- **Closed-not-merged:** none in window.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **skipped** — Phase 1.5 deferred this run.
- Experts: n/a.
- Findings: 0.
- Next segment: `ppt-web-core` (last reviewed 2026-06-05 — oldest unreviewed after the 2026-06-15 `api-core` and 2026-06-16 `mobile-rn` slices).
- Skipped: yes — 45h-lag catch-up run prioritised observation/PR-bookkeeping over the rotating review. `review_cursor` unchanged (no advance).

## Screen-map status
- Total `docs/screens/` files: 88 (ppt + reality + reality-mobile; `mobile/` not yet seeded → 0).
- Drift signals this run: **3** — `screen-map-drift-pr-1545-reality`, `screen-map-drift-pr-1453-reality`, `screen-map-drift-pr-1454-ppt` (all `medium` confidence — basename heuristic).
- Orphan screens this run: 0 (no orphans detected on the touched route set).
- Last `screen-map.yml` CI run on `main`: not queried this run (skipped for time discipline).

## Backlog deltas
- **New:**
  - [1] `churn-hotspot-backend-crates-db-src-repositories-rental-rs` — rental.rs top churn (5 touches; enum-cast + Decimal-decode rounds — #1422/#1486/#1492/#1537).
  - [2] `screen-map-drift-pr-1545-reality` — PR #1545 touched reality-web `/accounting/layout.tsx` with no `docs/screens/reality/` update.
  - [2] `screen-map-drift-pr-1453-reality` — PR #1453 (N5 bank-statement upload) touched reality-web `/accounting/page.tsx` with no `docs/screens/reality/` update.
  - [2] `screen-map-drift-pr-1454-ppt` — PR #1454 (native accounting MVP) touched 4 ppt-web routes (`AppRoutes`, `groups/accounting`, `groups/rentals`) with no `docs/screens/ppt/` update.
- **Cumulative re-emit (no score change):** `churn-hotspot-backend-servers-api-server-tests-booking-oauth-routes-tests-rs` — +4 touches this run (booking OAuth test rounds).
- **Resolved (→ done via PR-body match):**
  - `refactor-churn-hotspot-mobile-announcements` — PR #1543 (verbatim id match; code patterns not independently verified — narrative evidence row).
  - `refactor-churn-hotspot-mobile-announcements-test` — PR #1543 (verbatim id match in PR title).
- **Decayed (−1, no signal in 14d):**
  - [2→1] `bug-risky-churn-pr-963-api-main-rs`.
  - [2→1] `test-gap-hotfix-no-test-pr-959-reality-listings-pagination`.
- **Dropped (decayed to 0):**
  - `triage-dispatcher-mcp-push-large-file-issue-1014`.
  - `triage-issue-951`.

## Plans promoted
- none — no open items at `score >= 3` this run (the 6 ready items already have plans, all 3 drift signals are `medium` confidence + heuristic, churn signals stay at score 1).

## Auto-fix
- `no candidate` — every eligible signal type either fell below the certainty bar (`screen-map-drift` ×3 are `medium` confidence per heuristic), or doesn't sit in the allowlist (`churn-hotspot`, `lag_warning`). Kill switch (`ROUTINE_AUTOFIX_DISABLED`) not set this run.

## Self-review findings
- none

## Open questions
- Should the buffer-low refill route through this routine, or stay exclusively with the dispatcher's `scan` mode against `coverage.json`? Current design has the routine working on observed signals only; refill from coverage gaps is the dispatcher's responsibility, but the payload comes through the routine fire endpoint. If we want the routine to synthesize backlog items from `coverage.json` partial-status stories on buffer-low fire, the routine prompt needs a new "Special trigger payload" entry for `buffer-low:*`. (Surfacing the question, not changing behavior this run.)

## Quiet day?
- No — 46 PRs merged, 4 new backlog items, 3 drift signals, 2 resolutions, 4 decay rows, lag warning. Definitely not quiet.
