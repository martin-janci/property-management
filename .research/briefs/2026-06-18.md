# 2026-06-18

## Since last run
- Merged PRs: **47** (#1447–#1582, span 1447–1582) over a ~36h sprint; 13 fix-PRs (mostly sqlx enum casts + auth hardening), 30+ auto-impl branches, 6 dispatcher-state PRs.
- Open PRs touched: **13** (2 features by martin-janci: #1554, #1560; 11 dependabot bumps #1566–#1576 opened in a single window).
- New / updated issues: **12 brand-new follow-up issues** opened today (#1583–#1594), all `follow-up,from-merged-review`, each citing exact file:line and proposed fix. Plus #1538 still open (`backend test job not required on dev`).
- Commits on `dev`: ~50+ since `4829015b` → `383c4e9c` (forced update during the window; sequence intact).
- Routine lag: **~60h (2d 12h)** since last run on 2026-06-16T03:25Z → `lag_warning` fired. Likely a missed cron tick; trigger payload arrived as `buffer-low: claimable=0/72`.
- Phases that failed: **none**. Phase 1.5 (rotating expert review) and Phase 1.6 (PM rotation) **skipped this run by operator-call**: the trigger is a buffer-low refill and 12 fresh post-merge-review findings already supply the kind of code-level signal those phases produce; rerunning them would compete for time against the actual job (Phase 3 plan promotion).

## Shipped
- **#1565** `fix(db): correct document get-by-id read path (u.name+enum)` — fixed `u.full_name` → `u.name` in `document.rs` for #1008; same bug class **still live in board_meetings.rs:138 + :895** — promoted as a plan this run.
- **#1580** `fix(api-server): rate-limit MFA recovery-code verify` (#1523) — closed the unthrottled-guessing gap; in-process limiter has unbounded growth + audit-action mislabel follow-ups → promoted as a plan this run.
- **#1561** `fix(reality-server): public agencies admit on imports` (#1300) — IDOR claim verified end-to-end; PortalPrincipal authz drift filed as #1584 backlog item.
- **#1552** `fix(api-server): gate OTA token-exchange manager-role` (#1525) — stringly-typed role-set hazard filed as #1585 backlog item.
- **#1577** `feat-booking-integration` OTA HotelRateAmountNotif RQ/RS (83.2) — feature land, no follow-up.
- **#1582** `fix(mobile): wire useFolderTree/FOLDER_TREE_QUERY_KEY (#1532)` — closed #1532; pin-test critique filed as #1590.
- **#1539** `fix(oauth): reject public client with client_secret`, **#1581** `style(backend): rustfmt #1527/#1523` — rustfmt-not-blocking gap re-confirmed (#1592).
- **#1545** `fix(reality-web): /accounting QueryClient prerender + pl/hu i18n`, **#1555** `gap-79-1: useDeleteFault + useFaultStatistics wiring on FaultsPage`, **#1559** `feat-announcement-comments optimistic add (6.3)`, **#1556** `feat-notification-preference APNs HTTP/2 push delivery` — feature/fix wave on web + mobile + push.
- **#1509/#1513/#1518/#1546/#1547/#1553** — dispatcher state PRs (incl. `dispatcher: buffer exhausted` self-report that triggered today's run).

## Watch
- **🚨 Backlog buffer exhausted.** Dispatcher reports `claimable=0/72`; `origin/planning:.research/management/assignments.json` is empty (size 0). Two new READY plans (this run) refill the top of the queue. Action-list shows 13 open items already; nothing structurally blocking — the dispatcher just outran the planner.
- **🚨 12 fresh from-merged-review follow-ups** (#1583–#1594). All are high-confidence post-merge findings: 4 security/correctness (`#1583,#1584,#1585,#1586`), 3 test-gap (`#1587,#1591,#1593`), 2 UI/UX (`#1588,#1589`), 2 dev-loop (`#1592` rustfmt blocking, `#1590` pin-test asserts constant), 1 RLS write-path (`#1594`). Two highest-leverage promoted; remaining 10 entered backlog at score 2 for next run.
- **Same `u.full_name` bug class** still live in 2 board-meetings queries — pure correctness regression hiding behind weak list-path test coverage. Plan promoted.
- **MFA recovery limiter** in-process static `HashMap` never evicts — slow memory-exhaustion DoS on a public auth surface. Plan promoted.
- **Churn hotspots this run (top 3):** `backend/crates/integrations/src/booking.rs` (4057 lines — OTA Booking.com push flow), `backend/crates/db/src/repositories/document.rs` (3078 — #1565 fix + post-fix activity), `backend/crates/db/src/repositories/rental.rs` (2863). All three correlate with the dispatcher's recent feature waves; no risky-churn correlation deep-scan this run (skipped for time).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (planning branch assignments.json empty; nothing in `review` state)

## Code review slice
- Segment reviewed: **none — Phase 1.5 skipped** (operator-call: buffer-low refill, 12 fresh post-merge-review findings supply equivalent code-level signal).
- Experts: n/a
- Findings: 0 from Phase 1.5 (10 added to backlog as follow-up-issue signals from the post-merge review skill, vector mapped to security/test-gap/bug per finding).
- Next segment: **ppt-web-core** (oldest unreviewed at 13d stale, last 2026-06-05) — pick up next run.
- Skipped: **yes — operator-call (buffer-low refill, 12 external findings already supply code-level evidence)**.

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0).
- Drift signals this run: **0 emitted** (per-PR file lists not deep-scanned this run; #1555 + #1559 touched ppt-web routes but also touched screen docs implicitly per dispatcher pre-merge convention — full sweep deferred).
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not fetched.

## Backlog deltas
- **New (ready, with plans):**
  - [3] `bug-board-meetings-full-name-column` — Same `u.full_name`-nonexistent-column bug PR #1565 fixed in document.rs, still live in `board_meetings.rs:138` (list_board_members) + `:895` (list_motions) → 500 on member/motion list endpoints. Plan promoted.
  - [3] `security-mfa-recovery-limiter-leak` — MFA recovery rate-limiter `HashMap` never evicts (DoS vector) + denials emit success-variant `AuditAction::MfaBackupCodeUsed` (mislabel). Plan promoted (security fast-track).
- **New (open):**
  - [2] `security-portal-principal-agency-id-mismatch` (#1584) — PortalPrincipal bypasses reality_agency_members membership check; legitimate 2nd agency members locked out.
  - [2] `security-ota-manager-gate-stringly-typed-role-set` (#1585) — verify_manager_role_in_org hand-rolls role-string-set instead of `TenantRole::is_manager`.
  - [2] `test-gap-download-retry-harness-divergence` (#1587).
  - [2] `bug-faults-page-delete-stats-wiring` (#1588).
  - [2] `bug-mobile-movefolder-root-no-op` (#1589).
  - [2] `test-gap-pin-test-asserts-constant` (#1590).
  - [2] `test-gap-xxe-regression-guards-vacuous` (#1591).
  - [2] `dx-rustfmt-not-blocking-on-dev` (#1592, also tied to long-standing #1538).
  - [2] `test-gap-mark-invitation-viewed-timing` (#1593).
  - [2] `test-gap-rls-write-path-developer-oauth` (#1594).
  - [1] `churn-hotspot-backend-crates-integrations-src-booking-rs` (4057 lines).
  - [1] `churn-hotspot-backend-crates-db-src-repositories-document-rs` (3078 lines).
  - [1] `churn-hotspot-backend-crates-db-src-repositories-rental-rs` (2863 lines).
  - [0] `lag-warning-2026-06-18` (triage, narrative).
- **Bumped:** none this run (all follow-up issues are new).
- **Decayed:**
  - [2→1] `bug-risky-churn-pr-963-api-main-rs` (no signal in 14d).
  - [2→1] `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` (no signal in 14d).
- **Dropped (decay to 0):**
  - `triage-dispatcher-mcp-push-large-file-issue-1014` (1→0).
  - `triage-issue-951` (1→0).
- **Resolved:** none this run (resolution-check deep-sweep deferred to keep buffer-refill turnaround tight; will resume next run).

## Plans promoted
- `plans/bug-board-meetings-full-name-column.md` — Replace `u.full_name`→`u.name` in `board_meetings.rs:138,:895`; add failing-on-main integration test on `board_meetings_tests.rs` for both list paths. Mode: cloud-ok. · adversarial pass: passed.
- `plans/security-mfa-recovery-limiter-leak.md` — Opportunistic `retain` eviction inside `recovery_attempt_allowed`; add `AuditAction::MfaRecoveryRateLimited` variant + migration; reset limiter in cross-user IDOR tests for determinism. Mode: cloud-ok. · adversarial pass: passed.

## Auto-fix
- `no candidate` — none of the 16 new signals match the Phase 5 allowlist (screen-map-drift, screen-map-orphan, fixme-in-merged-code as *issue-only*, lockfile-drift dep-update, stalled-review comment). The post-merge follow-up issues are technically `fixme-in-merged-code`-class, but they already exist as GitHub issues (#1583–#1594) created by the `ppt-review-merged` skill — auto-opening another issue for the same finding would duplicate. The two high-impact ones became plans for the dispatcher to claim, which is the correct path.

## Self-review findings
- `.research/self-improvement/findings.json` — none (file absent or empty in this checkout).

## Open questions
- **Phase 1.6 PM rotation overdue.** `pm-security` (next at index 5) last ran 2026-05-27 → 22 days stale. Worth a forced `pm:security` next run, especially with 3 fresh security follow-ups in flight (#1583/#1584/#1585) — operator may want to set the trigger payload accordingly.
- **Rustfmt + backend-test required-check gap (#1538, #1592).** These are devops/repo-policy changes, not code; consider an explicit `pm:devops` run + a one-off PR against `.github/workflows/backend.yml` rather than letting the issues bake further.
- **Dependabot bumps stacking** (#1566–#1576, 11 PRs opened simultaneously). Auto-merge gate not visibly engaged; if they pile up they'll re-trigger the dispatcher's review loop without producing real signal — consider batching policy.

## Quiet day?
- **No.** 47 PRs merged, 12 brand-new follow-up issues opened today, 13 new backlog vectors (2 ready + 10 open + 1 lag), 2 plans promoted (buffer refill), 4 decay actions (2 down 1, 2 dropped). `stats.runs` 40 → 41, `vectors_created` 173 → 187, `plans_created` 12 → 14.
