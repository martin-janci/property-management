# 2026-06-07

> Trigger: `buffer-low` — not a documented special trigger; treated as a normal incremental run. The trigger payload aligns with issue #1151 (dispatcher claimable=0 despite metric=53); see *Watch* and the new `triage-issue-1151` row.

## Since last run
- Merged PRs: 18 (window 2026-06-06T03:06Z → 2026-06-07T00:15Z; range #1009, #1123, #1126, #1134, #1136, #1138–#1150)
- Open PRs touched: 1 (#988 — Epic E2E framework, still draft)
- New / updated issues: 12 (1 new: #1151; 7 closed today: #950, #952, #979, #980, #1014, #1061, #1069, #1070, #1071, #1137; #1008 commented + remains open per author's choice)
- Commits: 50 on `dev` (auto version-bumps + merge commits)
- Routine lag: on schedule (~21h since last run)
- Phases that failed: none (Phase 1.5 and Phase 1.6 deliberately skipped — see *Notes*)

## Shipped
- #1009 fix(voting): delegation/enum schema-drift in vote.rs (#1008) + eligibility & ballot-cast regression tests (#971) — closes voting half of schema-drift
- #1123 gap-82-1: Reality iOS — XcodeGen manifest for `iosApp.xcodeproj` (story 82.1)
- #1126 gap-82-5: Reality mobile inquiries list + account screen wired to reality-server stats
- #1134 chore(deps): bump npm-minor-patch group (21 updates) — dependabot
- #1136 refactor-oauth-integration-tests-hot — consolidate OAuth test helpers (-300 LOC net)
- #1138 test-gap-79-1-api-client-integration-coverage — pin announcements/faults client wiring
- #1139 triage-dispatcher-mcp-push-large-file-issue-1014 — bound action-list.json size
- #1140 triage-issue-755 — fix Epic 8A notification RLS GUC mismatch
- #1141 triage-issue-769 — validate worktree path params (deploy-server) — closes #769
- #1142 triage-issue-789 — reject mismatched organization_id in list_buildings — closes #789
- #1143 triage-issue-950 — fix CI false-red image builds from trigger-deploy 403 — closes #950
- #1144 triage-issue-952 — fix Reality SSO callback URL (`api.<apex>` not bare apex) — closes #952
- #1145 chore(research): dispatcher state update — `.research/management/*.json`
- #1146 test(api-server): real PKCE assertion with RFC 7636 vector — closes #1137 (resolves the tautology backlog item)
- #1147 chore(research): dispatcher self-review 2026-06-06
- #1148 fix(announcements): GIN-indexable targeting via `@>` containment — closes #1070
- #1149 fix(messaging): trim participant display name trailing space — closes #1071
- #1150 fix(api): correct document version/download contract drift in TypeSpec (#974.4)

## Watch
- Stalled review: #988 (Epic Playwright E2E framework) — open since 2026-06-02, no `reviewDecision`, **not draft** (~5 days); below the 7-day threshold for stalled-review signal, but worth eyes if it doesn't move tomorrow
- Reverted: none
- Churn hotspots: no new top-3 code-file hotspots this run. Largest non-test/non-config code change is `backend/crates/db/src/repositories/vote.rs` at only 58 LOC (PR #1009 ships +652 LOC of new tests alongside, which is the right shape). Top file-level churn this run is test/config (oauth_integration_tests.rs 878 LOC, vote test files 374+278, iosApp/project.yml 208) — correctly excluded.
- **Dispatcher buffer-stale (#1151):** new high-severity operational issue — Phase 2.6 reports claimable=53 but the Phase 3 claim predicate finds 0 actionable. Root causes (per issue body): (1) `gc1-reconcile.sh` stem-join misses flat ids → ~44 leaks; (2) tier-1 refill re-adds previously-FAILED archived task_ids; (3) `DISPATCHER_URL` misconfigured (Anthropic API URL instead of planner webhook → HTTP 400). Tracked as `triage-issue-1151` in backlog; not a research-routine fix.

## PRs stuck in draft despite approval
- none (queried open+draft+approved set this run — empty)

## PRs with verdict=changes, no fix-round progress in 24h
- none (no `verdict=changes` rows in `.research/management/assignments.json` head)

## Code review slice
- Segment reviewed: **skipped this run** (buffer-low priority is plan-promotion, not new review findings; segment cursor unchanged)
- Experts: n/a
- Findings: 0
- Next segment: `reality-server` (oldest unreviewed: 2026-05-26, 12d) — first candidate for next run
- Skipped: yes — explicit time-budget choice; routine prompt allows skipping when buffer-low pressure is on output rather than analysis

## Screen-map status
- Total `docs/screens/` files: 95 (ppt + reality; `mobile/` not seeded → 0)
- Drift signals this run: 0 (no merged PR this run touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: success at 2026-06-04T22:19Z

## Backlog deltas
- **New:** [1] `triage-issue-1151` — dispatcher claimable buffer is stale (true claimable=0, metric=53); root causes: gc1-reconcile stem-join + tier-1 refill of failed archives + misconfigured DISPATCHER_URL
- **Resolved (7 → done):**
  - `bug-schema-drift-runtime-sql-issue-1008` — PR #1009 (voting+scheduler) + PR #1040 (users.name sweep); #1008 author confirms fixed in comment
  - `code-review-issue-1137-pkce-test-tautology` — PR #1146 (real verify_pkce assertion); #1137 closed
  - `triage-issue-769` — PR #1141; #769 closed
  - `triage-issue-789` — PR #1142; #789 closed
  - `triage-issue-950` — PR #1143; #950 closed
  - `triage-issue-952` — PR #1144; #952 closed
  - `triage-issue-1061-dispatcher-archive-corruption` — restoration commit 8f154b2; #1061 closed
- **Bumped:** none
- **Decayed:** none above 0 (the announcer-stale-message decay below dropped it)
- **Dropped:** `bug-announcer-stale-message` — score decayed 1 → 0 (no new signals since 2026-05-24, 14d window crossed)

## Plans promoted
- **none this run · adversarial pass: n/a**

  After resolving the schema-drift item, the only score-3 promotion candidate from yesterday is gone. The top remaining open items sit at score=2:
  - `code-review-mobile-native-kmp-deeplink-token-not-url-decoded` (bug, high) — needs 3 for promotion; vector ≠ security so no fast-track
  - `code-review-mobile-native-kmp-search-stale-response-race` (bug, high) — same
  - 5 × `test-gap-screen-map-drift-pr-*` (medium) — same
  - `bug-risky-churn-pr-963-api-main-rs` (medium) — same
  - `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` (high, test-gap) — same

  The `buffer-low` trigger flags that the implementer side has run out of claimable work; the *plan* buffer also has 1 ready (`security-llm-doc-idor`) which is unchanged today. The honest read is that the backlog needs **fresh evidence accumulating** on the score-2 items (or a re-vector to `security` where applicable) before more plans can promote without lowering the quality bar.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist (`screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / lockfile-drift `dep-update-noise` / `stalled-review`) this run. Drift = 0, orphans = 0, no stalled-review at >7d, no new TODO/FIXME in merged-PR diffs that aren't already tracked, dependabot PR #1134 was a clean group bump with lockfile in sync.

## Self-review findings
- open=7 (high=2, medium=3, low=2); new this run=0; acknowledged=19
- fp-high-1 — Phase 6 archive-move produced 198KB `assignments-archive.json`; MCP push cannot inline a file that large; proven workaround documented (auto-checkout branch push, then PR merge)
- fp-high-2 — Phase 2.6 buffer metric overcounts because flat-id terminal items aren't drained from `action-list.json`; fix: extend `gc1-reconcile.sh` exact-id terminal closure (same root cause #1151 documents)

## Open questions
- **Promotion threshold under buffer-low pressure:** should the routine lower the score-≥3 gate to score-≥2 *only* when `trigger == "buffer-low"`, or hold the line and let evidence accumulate naturally? Today's instinct: hold the line — lowering the bar to clear a single-run pressure trade-off is exactly how the dispatcher's `action-list.json` got 44 stale-terminal leaks per #1151 root-cause 2 ("re-emit previously-failed archived task_ids"). The plan-promotion pipeline shouldn't repeat the dispatcher's mistake.
- **Schema-drift recurrence guard:** the durable fix (#1008 proposal 3 — generate `.sqlx` offline metadata) was rejected in the author's 2026-06-06 status comment because all drifted sites use *runtime* `sqlx::query`/`query_as` strings (not the compile-checked `query!` macro). Is the right durable fix (a) migrate the runtime strings to `query!` macros at the drift-prone sites, or (b) cover each affected repo with one `#[sqlx::test]` read so column drift surfaces in tests? Owner suggested splitting this into its own scoped issue.

## Quiet day?
- No — 18 PRs merged, 7 backlog items resolved to done, 1 new triage item added, 1 dropped via decay. The brief is full because the work happened, just not on the routine's plan-promotion path.
