# 2026-07-18

## Since last run
- Merged PRs: 27 (range #2372–#2421)
- Open PRs touched: 11 (mostly dependabot; #2377 draft, #2332 draft, both <7d — no stalled review)
- New / updated issues: 23 returned (11 new #2399–#2418, all closed same-day by dispatcher follow-up PRs — none untriaged)
- Commits: 30+ on `dev` (dispatcher + application code, tip = `27a838e`)
- Routine lag: 2d 1h since last run (last: 2026-07-16T03:04:00Z) — `lag_warning` fires (>36h); `stale_routine_alert` does not (<3d)
- Phases that failed: **phase-1.5-rotating-expert-review (skipped)**, **phase-1.6-project-management (skipped)** — see rationale under *Code review slice*

## Shipped
27 merged PRs since 2026-07-16 03:04Z. Highlights (grouped):
- **Security / IDOR (3 PRs)** — #2416 (cross-tenant IDOR in `government_portal` connection get/update/delete/test), #2421 (guard cross-org `/rag/migrate` under NOBYPASSRLS role), #2378 (strip signing token from `/sign` URL + suppress Referer).
- **Scheduler-RLS + panic recovery (3 PRs)** — #2340 (RLS context for scheduler worker), #2419 (fix migration-number comments 00216 → 00218), #2420 (`code-review-api-core-scheduler-mutex-poison`: recover poisoned scheduler metrics mutex).
- **Reality-web / listing-detail hardening (2 PRs)** — #2373 (consolidate listing-detail shape validation), #2379 (Decimal-as-string decode guard on sibling surfaces).
- **Mobile RN (5 PRs)** — #2372 (login clears whole query cache — cross-tenant stale-data fix), #2383 (upload allow-list drift + photo MIME), #2410 (iOS HEIC/HEIF → JPEG transcoding), #2412 (purge persisted AsyncStorage on session change), #2399/#2400.
- **Reality-server (2 PRs)** — #2375 (`/agencies/me` resolves only active memberships + tests), #2376 (`portal_webhook_events` dedup net for idempotent inquiry retries).
- **Screen-map / i18n / DX (7 PRs)** — #2380 (validate synthesized candidate ids in `scanCandidates`), #2381 (sync category enabled badge), #2382 (AC-5 favorite price-alert toggle + optimistic OFF), #2384 (create-schedule i18n), #2395/#2415 (drop orphan `packages` i18n namespace), #2407 (sitemap.json drift guard), #2411 (create-schedule form localization), #2414 (IdSchema at screen-map init write boundary).
- **Churn-hotspot fold-in from routine 2026-07-16** — #2395 / #2397 landed the two `refactor-churn-hotspots-*-2026-07-16` items from the last brief.
- **Booking gap** — #2398 (`gap-83-2-rate-availability-push-integration-deferred`: persist Booking.com push sync state on connect).
- **Sitemap CJS resolution** — #2396 (`@ppt/sitemap` default export for CJS).

## Watch
- Stalled review: **none** — open PRs are dependabot (age ≤48h) plus 2 old-ish drafts (#2332 age ~3d, #2377 age ~2d — neither carries `reviewDecision=APPROVED`); no PR crosses the 7-day threshold.
- Reverted: 0
- Churn hotspots this run (raw, after exclusions):
  - `backend/servers/api-server/src/routes/reports.rs` — **+3329 lines** (runs_seen: 2 → 3 after this run; **`repeated-churn` fires** — third-in-a-row)
  - `backend/crates/integrations/src/booking/mod.rs` — **+3185 lines** (runs_seen: 1 → 2; below the repeated-churn threshold until next appearance)
  - `backend/servers/api-server/src/routes/auth.rs` — **+2950 lines** (runs_seen: 3 → 4; **`repeated-churn` fires**)
- Runner-ups in code churn (informational): `oauth_integration_tests.rs` (+2719), `financial.rs` (+2411), `buildings.rs` (+2347), `lease.rs` (+2303), `integrations/sync.rs` (+2293).

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     Two open drafts (#2332 age ~3d, #2377 age ~2d) but neither carries reviewDecision=APPROVED. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json (planning branch) where reviewer_summary starts with
     "verdict=changes" AND (now - last_updated) > 24h AND status == "review". Planning branch state is
     tracked separately; today's routine did not fetch it since dispatcher runs ticked twice (#2419/#2420)
     inside this window and cleared any change-requested rows. -->
- none

## Code review slice
- Segment reviewed: **skipped by the routine** (Phase 1.5 skipped)
- Reason: the dispatcher's Tier-1d generator produced **21 fresh `code-review-finding` signals** in `signals/2026-07-16..2026-07-18-*-tier1d.json` covering 6 segments (api-core, mobile-native-kmp, mobile-rn, ppt-web-ui, reality-server, reality-web). None had landed in `backlog.json` — the "buffer-low: claimable=0/72" fire from the caller. This run **ingests those 21 tier1d signals into backlog** rather than reviewing a fresh segment; a routine-side pass would duplicate the dispatcher's work.
- Findings folded in: 21 (all `code-review-finding`, `score_delta ∈ {1,2,3}`, `confidence ∈ {medium, high}`).
- Next segment (when routine resumes rotating review): the segment whose `review_cursor` is oldest (`api-core` @ 2026-07-13 by the routine's own cursor — dispatcher touched it 07-15/16/17/18 but the routine cursor is authoritative for Phase 1.5).
- Skipped: yes — ingest-only mode this run.

## Screen-map status
- Total `docs/screens/` files: 168 (106 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` still not seeded → 0)
- Drift signals this run: **deferred** (27 PRs, per-PR file-list fetch not run — same rationale as 2026-07-16 catch-up)
- Orphan screens this run: **deferred**
- Last `screen-map.yml` CI run on `main`: not queried this run

## Backlog deltas
- **New (24):**
  - **Score-3 (3):**
    - [3] security · `code-review-api-core-apicall-ssrf-redirect` — reqwest client follows 302 → cloud-metadata; `validate_external_url` only vets initial URL · confidence=medium (upgraded to high in plan via file confirm) · **promoted → plan**
    - [3] bug · `code-review-mobile-native-kmp-navigation-create-listing-stub` — `Navigation.kt:476` wires `onSubmit → Result.failure(NotImplementedError)`; feature silently broken · confidence=high · **promoted → plan**
    - [3] bug · `code-review-ppt-web-ui-portfolio-mock-data` — portfolio dashboard renders mock/stub data · confidence=medium · not promoted (2-plan cap)
  - **Score-2 (10):** `code-review-api-core-api-call-ssrf-hostname`, `code-review-api-core-idempotency-lock-cancel-leak`, `code-review-api-core-sched-notify-swallow`, `code-review-api-core-voice-cmd-fabricated-success`, `code-review-mobile-rn-offline-queue-silent-dataloss`, `code-review-ppt-web-ui-invoicelist-noop-view`, `code-review-ppt-web-ui-sessions-unknown-cast`, `code-review-reality-server-sso-redirect-backslash`, `code-review-reality-web-listings-no-error-branch`, plus churn: `refactor-churn-auth-rs-2026-07-18` (churn+repeated), `refactor-churn-reports-rs-2026-07-18` (churn+repeated).
  - **Score-1 (8):** `code-review-api-core-authz-require-perm-fail-open`, `code-review-api-core-workflow-cond-fail-open`, `code-review-mobile-native-kmp-listingrepo-partial` (vector=test-gap), `code-review-ppt-web-ui-dispute-evidence-retry`, `code-review-ppt-web-ui-dsareports-i18n`, `code-review-ppt-web-ui-filedispute-t-shadow-i18n`, `code-review-reality-server-login-sso-account-enum`, `code-review-reality-web-profile-hardcoded-locale`, `code-review-reality-web-saved-search-hardcoded-i18n`, plus churn: `refactor-churn-booking-mod-rs-2026-07-18` (churn only, no repeated yet).
- **Bumped:** none (all backlog rows were terminal at start of run)
- **Decayed:** none (0 non-terminal items at run start)
- **Dropped:** none

## Plans promoted
- `plans/code-review-api-core-apicall-ssrf-redirect.md` — disable default reqwest redirect on the workflow `api_call` client so `validate_external_url` is not bypassed via a 302 to `169.254.169.254` · adversarial pass: passed
- `plans/code-review-mobile-native-kmp-navigation-create-listing-stub.md` — add `createListing()` to shared `ListingRepository`+`ApiClient` and wire `Navigation.kt` onSubmit callback (Android; iOS parity called out as follow-up) · adversarial pass: passed

## Auto-fix
- `no candidate` — the 21 tier1d `code-review-finding` signals are not in the Phase-5 allowlist (allowlist covers `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, lockfile-drift `dep-update-noise`, `stalled-review`). Churn / repeated-churn are analytical (not on the list). No `screen-map-drift` fired this run (drift detection deferred).

## Self-review findings
- open=40 (existing count from dispatcher self-review 2026-07-18 02:15 — see `.research/self-improvement/findings.json`); new this run=0; acknowledged=0
- Two recently-added: `review-cursor-stale-rotation` (dispatcher's tier1d slices don't advance `state.review_cursor`, so the routine's next-segment decision goes stale — surfaced by the mobile-rn slice's own selection reason), and `failed-task-dedup-blocks-open-issue-reingest`. See dispatcher self-review commit `8e114c1`.

## Open questions
- The `review_cursor` finding above (`review-cursor-stale-rotation`) is real: the routine authored `review_cursor` for its own Phase 1.5 rotation, but the dispatcher's Tier-1d slices consume the same segment names without updating that cursor — so the routine keeps re-picking segments the dispatcher already reviewed. Should the routine start ingesting dispatcher-generated `signals/<date>-<segment>-tier1d.json` files to update its own cursor? (This run does that manually as a one-shot; the general fix belongs in a plan the routine can promote from a `dx` vector.)
- The 3 score-3 items promoted / eligible-but-deferred: `code-review-ppt-web-ui-portfolio-mock-data` will need promotion next run under the 2-plan cap.

## Quiet day?
- **No** — 27 merged PRs since last run, 21 fresh tier1d code-review findings ingested, 3 churn hotspots (2 with repeated-churn), 2 plans promoted.

## Goal violations
- **G2 — advance state:** passes (last_pr_seen 2356 → 2421; last_issue_seen 2370 → 2418; last_commit_sha 1006a4c → 27a838e; seen_signals grew by 27; hotspot_history grew by 3; stats.runs 45 → 46).
- **G11 — phase-failure honesty:** Phase 1.5 and Phase 1.6 were skipped (not "failed"); `review_cursor` and `pm_cursor` intentionally not advanced. See *Code review slice* above.
- **G16 — management artifacts:** Phase 1.6 skipped → gate no-op.
- No G8 / G9 / G17 blocking violations expected — routine commit is `.research/` only.
