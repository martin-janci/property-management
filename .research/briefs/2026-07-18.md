# 2026-07-18

## Since last run
- Merged PRs: 26 (range #2371–#2416)
- Open PRs touched: 14 (2 drafts #2332/#2377 unchanged; 9 dependabot #2386–#2394 all 2d old; 3 fresh drafts #2419/#2420/#2421 from dispatcher tier1d — all <25h old)
- New / updated issues: 23 (all labeled follow-up/from-merged-review/security — 0 untriaged)
- Commits: 68 on `dev` (28 application code + 40 research/dispatcher chore)
- Routine lag: **2d 0h since last run** (last: 2026-07-16T03:04:00Z) — cloud cron missed one daily invocation; dispatcher (separate branch/loop) kept ticking
- Phases that failed: **phase-1.6-project-management (skipped)** — deferred in continuing catch-up mode; pm_cursor + coverage_cursor unchanged
- Trigger payload: `buffer-low: claimable=0/72 — refill planner` (dispatcher's action-list has 5 non-terminal rows, all `in-progress`/`review`/`quarantined` — no new claimable work for it to pull; this run's job is to feed the pipeline)

## Shipped
- **Follow-up sweep from PR #2357–#2370 (14 PRs #2371–#2384)** — landed all 14 issues opened by the 2026-07-16 post-merge review as PR fixes:
  - `#2371` KMP MyListings + ListingAnalytics wired to portal API (mobile-native retry)
  - `#2372` mobile login clears whole query cache (cross-tenant stale data eviction) — UC-14
  - `#2373` reality-web listing-detail SSR shape normalizer consolidated into `parseListingDetail()`
  - `#2374` deny-path regression test for GET /ai/chat/escalated role gate
  - `#2375` `/agencies/me` resolves only active memberships + backend tests
  - `#2376` `portal_webhook_events` dedup net for idempotent inquiry retries
  - `#2378` strip signing token from `/sign` URL + suppress Referer
  - `#2379` guard Decimal-as-string decode trap on sibling reality-server surfaces
  - `#2380` validate every synthesized candidate id against IdSchema in `scanCandidates`
  - `#2381` sync category enabled badge on optimistic trigger toggle
  - `#2382` cover AC-5 favorite price-alert toggle + optimistic OFF state
  - `#2383` guard mobile upload allow-list drift + enforce photo MIME
  - `#2384` localize create-schedule report labels + backend error toast
- **Churn-hotspot refactors (from 2026-07-16 brief)** — `#2395` dropped orphaned delegation/packages i18n namespaces from `ppt-web/messages/en.json`; `#2397` made generated `sitemap.json` deterministic to stop cross-run diff churn; `#2398` `gap-83-2` persist Booking.com push sync state on connection; `#2396` `@ppt/sitemap` default export for CJS E2E DX (dx-stalled-review-pr-988-retry2).
- **Second follow-up sweep from PR #2385–#2412 (7 PRs)** — `#2399` purge persisted AsyncStorage caches on mobile session change (extension of #2372 cache clear); `#2400` iOS HEIC/HEIF → JPEG transcode; `#2401` reconcile favorites client to real reality-server DTO; `#2402` `/sign` reload regression + token param strip; `#2403` localize create-schedule form + map remaining error codes; `#2404` guard src/data ↔ sitemap.json drift; `#2405` delete orphaned `packages` i18n namespace from cs/de/hu/pl/sk (Closes #2405).
- **Dispatcher Tier-1d generator fold (Post-merge review)** — commit `983107b` scanned 5 recent PRs, filed 2 new follow-up issues (#2417 scheduler-RLS migration-number doc drift, #2418 cross-org /rag/migrate RLS test-vacuously-passes). Both routed as `follow-up/from-merged-review` action-list rows; #2419 (fix migration doc) and #2421 (NOBYPASSRLS role guard) are drafts as of 2026-07-18 01:09 UTC.
- **Cross-tenant IDOR fix** — `#2416` `gh-issue-2413` government_portal connection get/update/delete/test unscoped → owner-scoped.
- **Code review** — `#2420` `code-review-api-core-scheduler-mutex-poison` recover poisoned scheduler metrics mutex (dispatcher fast-path claimed 2026-07-18 00:06).

## Watch
- **Stalled review: none** — no open PR crosses the 7-day threshold. Oldest: draft #2332 at 3 days (`fix(ai-chat)` for #2317).
- **Reverted:** 0
- **Closed unmerged:** #2385 (`bump dtolnay/rust-toolchain 1.94.1→1.100.0`) — closed by author as rejected upgrade path (`closed-not-merged-pr` signal at score 1).
- **Churn hotspots this run (raw, after exclusions):**
  - `frontend/packages/sitemap/src/json/sitemap.json` — **+4595 lines** (`runs_seen: 1 → 2` after this run; almost entirely from #2397 deterministic-sitemap landing)
  - `frontend/apps/ppt-web/messages/en.json` — **+4110 lines** (`runs_seen: 1 → 2`; churn from #2395 orphaned-namespace drop + #2415 delete-`packages` follow-up)
  - `backend/servers/api-server/src/routes/reports.rs` — **+3361 lines** (`runs_seen: 2 → 3`; **`repeated-churn` fires this run** — see Backlog deltas)
- **Fix-PRs without test diff (`risky-churn`):** 0 fired — the merged fix PRs (#2372 mobile cache clear, #2401 favorites reconcile, #2402 /sign reload, #2416 gov-portal IDOR, #2384 localize, etc.) all include test files. Bookkeeping: title-prefix regex is `^(fix|hotfix|bugfix|revert)`; repo convention uses `gh-issue-N:` so raw title-match yields 0, but every merged fix in this window includes a `_test.rs` or `.test.ts` companion — no cross-check via label matched either. Baseline for the routine.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     3 drafts in the open list: #2332 (age 3d, no reviewDecision), #2377 (age 1d, no reviewDecision),
     #2419/#2420/#2421 (all <1d old drafts opened by implementer). None APPROVED-but-draft. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json where reviewer_summary starts with "verdict=changes"
     AND (now - last_updated) > 24h AND status == "review".
     Current active rows:
       - gh-issue-2360 (quarantined, PR #2377) — 24h+ but quarantined, not "review"; dispatcher owns
       - code-review-api-core-scheduler-mutex-poison (review, PR #2420) — <25h old
       - gh-issue-2417 (review, PR #2419) — <25h old
       - gh-issue-2418 (review, PR #2421) — <25h old
     No row meets both conditions. -->
- none

## Code review slice
- Segment reviewed: **api-core** (reason: oldest-unreviewed at 2026-07-13 — 5d stale; no churn-hotspot alignment since reports.rs is in api-handlers scope which was reviewed 2026-07-14)
- Experts: Explore subagent (Rust static analysis, 233k tokens, 72 tool_uses, 6m45s)
- Findings: 1 high-confidence (Medium severity, +2) — `code-review-api-core-ssrf-validator-drift` (see Backlog deltas + Plans promoted)
- Next segment: **reality-server** (last reviewed 2026-07-13 — tied for oldest before this run; will be oldest unreviewed after api-core advances today)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 168 (106 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` still not seeded → 0)
- Drift signals this run: **3** — PR #2411 (ppt reports.tsx localize), PR #2384 (ppt reports.tsx localize), PR #2373 (reality listing-detail SSR normalizer). All heuristic-caveat: matching route file basename to screen doc slug is best-effort; localization-only and refactor-only changes may not require doc updates. Backlog rows created at score 2 (test-gap, medium confidence).
- Orphan screens this run: 0 (no `docs/screens/**` entry whose slug lost its route)
- Last `screen-map.yml` CI run on `main`: not queried this run (cheap-mode)

## Backlog deltas
- **New:**
  - [2] test-gap · `screen-map-drift-pr-2411-ppt` — PR #2411 touched `ppt-web/src/routes/groups/reports.tsx` w/o doc update · confidence=medium · heuristic caveat noted
  - [2] test-gap · `screen-map-drift-pr-2384-ppt` — PR #2384 touched same route (localize labels) w/o doc update · confidence=medium · heuristic caveat noted
  - [2] test-gap · `screen-map-drift-pr-2373-reality` — PR #2373 touched reality-web listing-detail SSR helpers w/o doc update · confidence=medium · heuristic caveat noted
  - [1] refactor · `repeated-churn-backend/servers/api-server/src/routes/reports.rs` — runs_seen crossed 2 threshold this run (+3361 lines from #2340 scheduler-RLS + #2384/#2411 localize follow-ups) · confidence=medium · instability proxy
  - [2] security · `code-review-api-core-ssrf-validator-drift` — `ApiCallExecutor` in `backend/servers/api-server/src/services/actions/api_call.rs:152` uses a local SSRF validator that drifts from the canonical `common::url_validation::validate_external_url` (missing `http://` prod block, IPv4 multicast, and redirect-policy override) · confidence=high · Phase 1.5 finding · **security fast-track → promoted to plan this run**
- **Bumped:** none (all backlog items were terminal at start of run)
- **Decayed:** none (0 open items at start of run → nothing to decay)
- **Dropped:** none
- **Notable non-creation:**
  - `churn-hotspot-frontend/packages/sitemap/src/json/sitemap.json` re-emitted with `score_delta=0` (cumulative signal, already in `seen_signals`). hotspot_history bumped runs_seen 1 → 2, recent_churn=4595.
  - `churn-hotspot-frontend/apps/ppt-web/messages/en.json` re-emitted with `score_delta=0` (cumulative). hotspot_history bumped runs_seen 1 → 2, recent_churn=4110.
  - `churn-hotspot-backend/servers/api-server/src/routes/reports.rs` re-emitted with `score_delta=0` (cumulative). hotspot_history bumped runs_seen 2 → 3, recent_churn=3361.

## Plans promoted
- `plans/code-review-api-core-ssrf-validator-drift.md` — collapse workflow ApiCallExecutor's local SSRF validator into `common::url_validation::validate_external_url` + set `reqwest::Client` `Policy::none()` to close the 302-to-internal-target bypass · adversarial pass: passed

## Auto-fix
- `no candidate` — the promoted `code-review-api-core-ssrf-validator-drift` plan is high-confidence + score=2 but the auto-fix allowlist does NOT include multi-file security fixes with test authoring. Deferred to the manual implementer path.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0
- fp-mcp-push-cannot-inline-large-archive [recurrence=8] — `mcp__github__push_files` can't inline the >64 KiB `assignments-archive.json`; fix: adopt PR + API-merge as the dispatcher's primary push path for the archive (proven this env — landed PR #1163 that way)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=4] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend `gc1-reconcile.sh` to auto-close any exact-id-terminal row (including status=failed)

## Goal violations
- **G2 — advance state:** passed (last_pr_seen 2356 → 2416; last_issue_seen 2370 → 2418; last_commit_sha 1006a4c → 75ac735; seen_signals grew; hotspot_history grew; stats.runs 45 → 46).
- **G10 — backlog.md matches JSON:** passes after this run's regen (fixes an outstanding drift where the 2026-07-16 backlog.md still showed `open` for the two churn-hotspot rows the dispatcher's fast-path reconcile flipped to `done` after commit; this run's render aligns the two).
- **G11 — phase-failure honesty:** Phase 1.6 was skipped (not "failed"); `pm_cursor` and `coverage_cursor` intentionally not advanced.
- No G8/G9/G17 blocking violations expected — routine commit is `.research/` only, no secrets/hostnames/telegram tokens in added lines.

## Open questions
- **Buffer refill:** the dispatcher's `claimable=0/72` means it's completely dry — all 5 non-terminal action-list rows are `in-progress`/`review`/`quarantined`, no fresh work to claim. The routine can't refill the dispatcher's action-list directly from Phase 2 signals; that path lives in Phase 1.6 (PM skill) which was skipped this run. **Recommendation:** un-skip Phase 1.6 next run (pm-security last-run is 2026-05-27 — 52d stale in rotation), or run `text: "full"` once to reset the rotation cadence. In the meantime the dispatcher's own Tier-1d + Phase-8 self-review paths remain the main refill source.
- Should the routine adopt the dispatcher's Tier-1d review outputs so its `review_cursor` reflects the newer reviews the dispatcher performs? Currently the two systems diverge: dispatcher just filed 2 new issues from post-merge review of #2411–#2416, while the routine's cursor still lags.

## Quiet day?
- **No** — 26 merged PRs since last run, 23 new labeled issues, 4 new backlog vectors, 3 hotspot re-emits, 1 first-fire `repeated-churn`. The dispatcher's fast-path handled the actionable work in real time; this brief documents what would have surfaced across two daily runs.
