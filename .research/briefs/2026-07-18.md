# 2026-07-18

## Since last run
- Merged PRs: 29 (range #2371–#2421)
- Open PRs touched: 11 (8 dependabot chore-deps; 2 drafts by user #2377/#2332; 1 renamed #2391)
- New / updated issues: 23 (all labeled `follow-up`/`from-merged-review` or `security` — every single one is already CLOSED, resolved by the dispatcher's merge cycle)
- Commits: 50 on `dev` (16 application-code merges + 34 dispatcher artifact commits)
- Routine lag: **2d 5h since last run** (last: 2026-07-16T03:04:00Z) — lag_warning threshold (>36h) hit; the cloud cron missed the 2026-07-17 daily invocation while the dispatcher (separate loop, separate branch) kept ticking
- Phases that failed: **phase-1.5-rotating-expert-review (skipped)**, **phase-1.6-project-management (skipped)** — both intentionally deferred (rationale below)

## Shipped
29 merged PRs across the 53h window — every PR is a dispatcher-fulfilled follow-up issue or code-review finding. Highlights:
- **Cross-tenant IDOR / security (5 PRs)** — #2416 (government_portal connection get/update/delete/test — owner-bypassed RLS + missing org filter), #2374 (deny-path regression for `GET /ai/chat/escalated` role gate), #2378 (strip signing token from `/sign` URL + suppress Referer), #2408 (fix `/sign` reload regression + strip only token param), #2421 (guard cross-org `/rag/migrate` under a NOBYPASSRLS role — fixes vacuous superuser-run test).
- **Mobile RN quality (4 PRs)** — #2372 (mobile login clears whole query cache), #2412 (purge persisted AsyncStorage caches on session change), #2410 (transcode iOS HEIC/HEIF to JPEG in DocumentUploadScreen), #2383 (guard mobile upload allow-list drift + enforce photo MIME).
- **Reality-web bug fixes (4 PRs)** — #2409 (reconcile favorites client to reality-server DTO), #2373 (parseListingDetail normalizer), #2379 (Decimal-as-string decode trap on sibling surfaces), #2382 (favorite price-alert AC-5 toggle + optimistic OFF state).
- **Reports & scheduling (3 PRs)** — #2384 (localize create-schedule report labels + error toast), #2411 (localize form + map remaining error codes), #2419 (fix scheduler-RLS comments citing wrong migration 00216 → 00218).
- **Screen-map / sitemap upkeep (3 PRs)** — #2395 (drop orphaned `delegation`/`packages` i18n from en.json), #2415 (delete `packages` from cs/de/hu/pl/sk), #2397 (deterministic sitemap.json generation), #2407 (guard src/data ↔ sitemap.json drift), #2414 (validate IdSchema at screen-map init write boundary).
- **KMP mobile-native + booking (2 PRs)** — #2371 (wire KMP MyListings + ListingAnalytics to portal API), #2398 (persist Booking.com push sync state on connection).
- **Portal webhook (2 PRs)** — #2376 (portal_webhook_events dedup net for idempotent inquiry retries), #2380 (validate synthesized candidate ids against IdSchema in scanCandidates).
- **Infra / smaller (5 PRs)** — #2375 (/agencies/me active-membership resolution), #2381 (category enabled-badge sync on optimistic trigger toggle), #2396 (@ppt/sitemap default export for CJS resolution), #2420 (recover poisoned scheduler metrics mutex).

## Watch
- Stalled review: **none** — no open PR crosses the 7-day threshold. Two user-authored drafts (#2332 age 3d, #2377 age 2d), neither in review yet.
- Reverted: 0 in window.
- Churn hotspots this run (raw, after exclusions):
  - `backend/servers/api-server/src/routes/reports.rs` — **+3329 lines** (runs_seen 2 → 3; cumulative `repeated-churn` re-emit — no fresh score)
  - `backend/crates/integrations/src/booking/mod.rs` — **+3185 lines** (first slash-form appearance; dashed-form id was in older seen_signals; +1 refactor scored)
  - `backend/servers/api-server/src/routes/auth.rs` — **+2950 lines** (runs_seen 3 → 4; cumulative `repeated-churn` re-emit — no fresh score, prior cluster row already resolved by PR #2274)
- Runner-ups (informational, unscored): `oauth_integration_tests.rs` (+2719), `financial.rs` (+2411), `buildings.rs` (+2347), `lease.rs` (+2303), `integrations/{sync,install,webhook}.rs` cluster (+2293/+2283/+2240), `llm_document.rs` (+2225), `vote.rs` (+2198). The reports + integrations cluster remains the strongest signal, matching last run's read.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     Two open drafts (#2332 age 3d, #2377 age 2d) — neither carries an APPROVED reviewDecision; both are dispatcher-owned draft-first flows. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json where reviewer_summary starts with "verdict=changes"
     AND (now - last_updated) > 24h AND status == "review".
     Dispatcher summary at 2026-07-18 06:10 UTC reports 1 active + 0 failed — no stalled verdict=changes rows visible. -->
- none

## Code review slice
- Segment reviewed: **skipped by the routine (dispatcher-owned)**
- Reason: the dispatcher's Tier-1d `dev-review` ran twice in the past 24h — `mobile-rn` and `api-core` (see `.research/signals/2026-07-18-mobile-rn-tier1d.json`, `.research/signals/2026-07-18-api-core-tier1d.json`). The dispatcher cursor is ahead of the routine cursor on those segments. Running a routine-side pass this run would duplicate cost with no fresh coverage.
- Findings: 0 (routine skipped) — dispatcher Tier-1d tier signals persisted separately.
- Next segment (when routine resumes): `api-core` or `reality-server` (both last routine-reviewed 2026-07-13 — the two oldest cursors on the routine side; dispatcher touched `api-core` today so `reality-server` is likely first).
- Skipped: yes — dispatcher tier1d coverage is already ahead of the routine cursor on the two candidate segments.

## Screen-map status
- Total `docs/screens/` files: 168 (106 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` still not seeded → 0)
- Drift signals this run: **2** (`screen-map-drift-pr-2411-ppt`, `screen-map-drift-pr-2409-reality`) — both heuristic (i18n / test-only change), review-candidate not promotable (score capped at 2, below the 3-point promotion threshold)
- Orphan screens this run: 0 (spot-check of the two touched route trees against `docs/screens/{ppt,reality}/` filenames returned no orphan slugs)
- Last `screen-map.yml` CI run on `main`: not queried this run (no route/screen-doc change since the last CI-visible commit)

## Backlog deltas
- **New:**
  - [1] refactor · `refactor-churn-hotspots-booking-2026-07-18` — churn hotspot: `backend/crates/integrations/src/booking/mod.rs` (+3185 lines, first slash-form appearance) · confidence=high · analytical, not instability — driven by PR #2398 gap story
  - [2] test-gap · `screen-map-drift-pr-2411-ppt` — PR #2411 touched `routes/groups/reports.tsx` without updating `docs/screens/ppt/reports.md` · confidence=medium · heuristic (i18n change, not surface)
  - [2] test-gap · `screen-map-drift-pr-2409-reality` — PR #2409 touched `app/[locale]/favorites/page.test.tsx` without updating `docs/screens/reality/favorites.md` · confidence=medium · heuristic (test-only DTO fix)
- **Bumped:** none
- **Decayed:** none (backlog had 0 non-terminal items at start of this run — every prior open row was closed, done, or dropped by the dispatcher cycle)
- **Dropped:** none
- **Notable non-creation:** `churn-hotspot-…/reports.rs` re-emitted with `score_delta=0` (cumulative, already in `seen_signals`); `churn-hotspot-…/auth.rs` likewise (`repeated-churn-…/auth.rs` prior signal already in `seen_signals`; the cluster row `refactor-churn-hotspots-api-server-auth-2026-07-12` was resolved by PR #2274). Hotspot_history bumps: reports.rs 2 → 3, auth.rs 3 → 4, booking/mod.rs new (1).

## Plans promoted
- none — the 3 new backlog rows score at most 2; none clear the promotion threshold. No security fast-track hits this run (all new items are refactor / test-gap heuristics).

## Auto-fix
- `no candidate` — no signal met `confidence=high` + `score≥3` + allowlist this run. The two `screen-map-drift` signals are `confidence=medium`; the `churn-hotspot` signals are analytical (not in the auto-fix allowlist).

## Self-review findings
- Findings file lives on the dispatcher branch (`.research/self-improvement/findings.json`) — reachable from the last dispatcher self-review commit `8e114c1` (2026-07-18 02:15 UTC): open=40, +2 new this run (`review-cursor-stale-rotation`, `failed-task-dedup-blocks-open-issue-reingest`). No high-severity open findings surfaced against the routine this run.
- none new for the routine itself.

## Open questions
- **The cloud routine cron missed the 2026-07-17 daily invocation.** The previous brief flagged a 3-day gap that was resolved by a manual catch-up; this run reports another 53h gap. Whether the schedule is intermittently missing wakeups on the Anthropic side, or a specific failure is silently killing the run, is still an open operator question — the dispatcher's cron is on a separate loop and continues ticking.
- The routine's `review_cursor` still trails the dispatcher's Tier-1d cursor by 3–5 days on every segment (routine oldest `api-core`/`reality-server` at 2026-07-13; dispatcher touched `api-core` and `mobile-rn` today). The de-duplication decision from 2026-07-16 stands — but the two cursor systems remain separate systems of truth.

## Quiet day?
- **No** — 29 merged PRs since last run, 3 new backlog vectors, 2 screen-map drift signals recorded. Not a quiet day; the Telegram delivery digest send condition is met (Phase 6 will re-evaluate).

## Goal violations
- **G2 — advance state:** passed (last_pr_seen 2356 → 2421; last_issue_seen 2370 → 2418; last_commit_sha 1006a4c → 27a838e; seen_signals grew by 4; hotspot_history bumped 3 files; stats.runs 45 → 46; stats.vectors_created 185 → 188).
- **G11 — phase-failure honesty:** Phase 1.5 and Phase 1.6 were skipped (intentionally deferred, not "failed"); `review_cursor` and `pm_cursor` intentionally not advanced. No cursor was silently advanced on skipped work.
- No G8/G9/G17 blocking violations expected — routine commit is `.research/` only, no secrets/hostnames/telegram tokens in added lines.
