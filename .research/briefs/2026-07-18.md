# 2026-07-18

## Since last run
- Merged PRs: 27 (range #2372–#2421)
- Open PRs touched: 10 (mostly 8 dependabot rollups on `chore(deps)`; all <2d old)
- New / updated issues: 23 (all `follow-up`/`from-merged-review`, all closed by the merged PRs; 0 untriaged)
- Commits: 16 code commits + 34 dispatcher/research chore commits on `dev`
- Routine lag: **2d 2h since last run** (last: 2026-07-16T03:04:00Z, threshold 36h) — `lag_warning` emitted
- Phases that failed: none

## Shipped
Grouped by theme (27 PRs):

- **Security / RLS / IDOR (6):** #2416 fix cross-tenant IDOR in `government_portal` connection get/update/delete/test; #2421 guard cross-org `/rag/migrate` RLS under a NOBYPASSRLS role (previously vacuous under superuser); #2378 strip signing token from `/sign` URL + suppress Referer; #2408 fix `/sign` reload regression + strip only the token param; #2419 scheduler-RLS doc-comment migration number fix (00216→00218); #2420 recover poisoned scheduler metrics mutex (from code-review finding).
- **Mobile RN (4):** #2372 mobile login clears whole query cache (cross-tenant stale data); #2383 guard mobile upload allow-list drift + enforce photo MIME; #2410 transcode iOS HEIC/HEIF to JPEG in DocumentUploadScreen; #2412 purge persisted AsyncStorage caches on mobile session change.
- **Reality-web / data integrity (5):** #2373 consolidate reality-web listing-detail shape validation into `parseListingDetail()`; #2379 guard Decimal-as-string decode trap on sibling reality-server surfaces; #2409 reconcile favorites client to real reality-server DTO; #2380 validate synthesized candidate ids against IdSchema in scanCandidates; #2414 validate IdSchema at screen-map init write boundary.
- **Backend / data correctness (3):** #2375 `/agencies/me` resolves only active memberships + backend tests; #2376 `portal_webhook_events` dedup net for idempotent inquiry retries; #2398 persist Booking.com push sync state on connection.
- **i18n (4):** #2384 localize create-schedule report labels + backend error toast; #2411 localize create-schedule form + map remaining error codes; #2395 drop orphaned delegation/packages i18n namespaces from ppt-web/en.json; #2415 delete orphaned `packages` i18n namespace from cs/de/hu/pl/sk.
- **UI/UX (2):** #2381 sync category enabled badge on optimistic trigger toggle; #2382 cover AC-5 favorite price-alert toggle + optimistic OFF state.
- **DX / CI (3):** #2396 `@ppt/sitemap` default export for CJS resolution; #2397 make generated `sitemap.json` deterministic to stop churn; #2407 guard against `src/data ↔ sitemap.json` drift.

## Watch
- Stalled review: **none** (10 touched open PRs all <2d old; 8 are dependabot rollups).
- Reverted: 0.
- Churn hotspots this run (raw, after exclusions; orphan re-import commit `9e3bf9b` filtered):
  - `frontend/packages/sitemap/src/json/sitemap.json` — 868 lines (down from 3727; PR #2397 determinism fix); runs_seen 1→2 → `repeated-churn` fires (delta=+1, low confidence, declining trend).
  - `backend/servers/api-server/tests/rag_migrate_pgvector_tests.rs` — 204 lines (single test file added by PR #2421); new hotspot; likely one-shot.
  - `frontend/apps/ppt-web/messages/en.json` — 168 lines (down from 3926; PR #2395 orphan namespace cleanup); runs_seen 1→2 → `repeated-churn` fires (delta=+1, low confidence, declining trend).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none — the planning-branch `assignments.json` has exactly 1 entry (`quarantined`, not `review`). The one action-list open item (`gh-issue-2360`) was administratively marked `done` this run because its GitHub issue is already closed (PR #2377 quarantined after 3 failed fix rounds — see decisions.md).

## Code review slice
- Segment reviewed: **reality-server** (reason: oldest-unreviewed tied at 2026-07-13 with api-core + ppt-web-ui; picked reality-server since dispatcher Tier1d already re-covered api-core and ppt-web-ui multiple times since 07-13; reality-server had lagged)
- Experts: Rust (primary)
- Findings: **3** — 2 HIGH-severity security (cross-agency invitation escalation chain: `create_invitation` no-membership-check + `accept_invitation` no-email-binding), 1 Medium bug (pagination `total = rows.len()` regression across 4 sites in reports.rs / imports.rs / agency_imports.rs). Both security findings verified via `Read` on the cited file:line — cross-agency escalation is **live in production reality-server code today**.
- Next segment: `api-core` (tied oldest at 2026-07-13 with `ppt-web-ui`; alternate on next run)
- Skipped: no

## Screen-map status
- Total `docs/screens/` files: 96 (across `ppt/`, `reality/`, `reality-mobile/`; `mobile/` not yet seeded → 0)
- Drift signals this run: **2** — PR #2411 (ppt-web routes without docs/screens/ppt update) and PR #2409 (reality-web app without docs/screens/reality update); both heuristic best-effort, marked as review candidates.
- Orphan screens this run: **0** (skipped — see routine caveat about false-positives; orphan scan not run this cycle)
- Last `screen-map.yml` CI run on `main`: not queried this run (routine's own CI is on `research-land.yml`)

## Backlog deltas
- **New:** [3] `security-reality-server-agency-invitation-escalation` — cross-agency invitation escalation (combined chain of 2 findings). Vector=security. Promoted to `plans/security-reality-server-agency-invitation-escalation.md` on the security fast-track (vector=security + confidence=high + score≥2).
- **New:** [1] `bug-reality-server-total-as-page-length-regression` — pagination `total = rows.len()` across 4 handlers.
- **New:** [2] `test-gap-screen-map-drift-pr-2411-ppt` — heuristic; ppt-web routes touched without docs/screens update.
- **New:** [2] `test-gap-screen-map-drift-pr-2409-reality` — heuristic; reality-web app touched without docs/screens update.
- **New:** [1] `refactor-repeated-churn-sitemap-json` — declining trend after PR #2397; low confidence.
- **New:** [1] `refactor-repeated-churn-messages-en-json` — declining trend after PR #2395; low confidence.
- **Bumped:** none — backlog had 0 open items pre-run; all deltas above are net-new.
- **Decayed:** none — no open items to decay (all 188 pre-run items were terminal `done`/`dropped`/`closed`).
- **Dropped:** none.

## Plans promoted
- `plans/security-reality-server-agency-invitation-escalation.md` — Cross-agency invitation escalation on reality-server (create + accept chain); vector=security, score=3, confidence=high · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist match this run. The two HIGH-severity security findings do NOT match the auto-fix allowlist (which is `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile drift, `stalled-review`) — real code fixes are not allowlisted for the routine's auto-PR path; they go to the manual plan path via `plans/`.

## Self-review findings
- self-improvement findings.json exists — read on demand; not summarized this run (dispatcher owns that surface — see `.research/self-improvement/`).

## Open questions
- Was `create_invitation` shipped with the missing membership check from initial implementation, or introduced by a recent refactor? Need `git blame` to scope the exposure window before the incident-audit query runs. Deferred to the plan implementer.
- Is reality-server `list_members` (agencies.rs:345) intentionally public/no-auth (agency directory) or a second IDOR? Filed as separate `pm-security-list-members-idor-audit` action item — do not bundle into the primary hotfix.
- Should the fix land as a `hotfix/*` branch from `main` (per the hotfix workflow in `docs/git-workflow.md`) or as a normal `feature/*` branch to `dev`? Decision-needed logged in `decisions.md` — recommendation: hotfix given confirmed live-exploitability.

## Quiet day?
- **No.** 27 merged PRs, 6 new backlog vectors, 1 promoted plan, 3 code-review findings (2 HIGH-sev security), 7 open action-list items refilled. Buffer starvation partially unblocked.
