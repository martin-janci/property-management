# 2026-07-18

## Since last run
- Merged PRs: 28 (range #2371–#2420, on `dev`)
- Open PRs touched: 12 (10 dependabot bumps + #2377 quarantined-with-PR + #2421 in-flight; #2332 draft AI-chat still parked)
- New / updated issues: 23 (22 follow-ups closed; #2418 the only open one, already claimed by dispatcher)
- Commits: 73 on `dev` since 2026-07-16T03:04Z (routine last cursor `1006a4c` → head `8e114c1`)
- Routine lag: 2d 1h since last run (>36h threshold — logged as `lag-warning-2026-07-18`)
- Phases that failed: none
- **Payload:** `buffer-low: claimable=0/72 — refill planner` — this run adds 10 open items + promotes 2 plans to unstarve the dispatcher.

## Shipped
- Post-merge follow-up cluster: 22 `gh-issue-*` PRs (#2371–#2416) landed the small fixes surfaced by the last two post-merge review passes — i18n gaps, IdSchema validation, IDOR closures, favorites-DTO reconcile, HEIC transcode, AsyncStorage cache purge, RLS scheduler wiring.
- #2419/#2420 landed the dispatcher's Tier 1b/1c fixes — scheduler-RLS migration-number comment fix + scheduler-metrics mutex poison recovery.
- New RLS follow-up #2421 opened (draft) for cross-org `/rag/migrate` test that was passing vacuously as superuser.

## Watch
- **PR #2421** — the RAG-migrate NOBYPASSRLS role fix — is open, drafted 2026-07-18. Already tracked as `gh-issue-2418` in action-list; no separate signal.
- **PR #2377** (portal view-webhook dedup) is quarantined; the fix strategy needs a re-plan before it can be reclaimed. Not surfaced as a new signal.
- Churn hotspots (top-3 code, this run): `backend/servers/api-server/src/routes/reports.rs` (+3329, runs_seen=3), `backend/crates/integrations/src/booking/mod.rs` (+3185, runs_seen=2), `backend/servers/api-server/src/routes/auth.rs` (+2950, runs_seen=4). All three are already-addressed refactors (scheduler-RLS migration, Booking.com push, auth cluster) — no fresh backlog rows minted; hotspot_history bumped for future decay tracking.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     Example:
       gh pr list --repo martin-janci/property-management --state open --draft \
         --json number,title,updatedAt,reviewDecision,isDraft \
         --jq '.[] | select(.reviewDecision=="APPROVED" and .isDraft==true)'
     Compute age-in-hours from updatedAt. If none: emit a single line "- none".
     This catches the merge-gate bug class (#539-style): PR approved, CI green,
     but stuck in draft because a human gate wasn't cleared. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json (planning branch) where
     reviewer_summary starts with "verdict=changes" AND
     (now - last_updated) > 24h AND status == "review".
     Example (read planning's assignments via gh):
       gh api repos/martin-janci/property-management/contents/.research/management/assignments.json?ref=planning \
         --jq '.content' | base64 -d | jq '.assignments[]
         | select(.status=="review" and (.reviewer_summary // "" | startswith("verdict=changes")))
         | {task_id, pr_number, last_updated, reviewer_summary}'
     If none: emit "- none". -->
- none

## Code review slice
- Segment reviewed: adopted from Tier-1d dispatcher slices — api-core (2026-07-17), ppt-web-ui (2026-07-17), reality-web (2026-07-17), mobile-rn (2026-07-18). No fresh routine-side Phase 1.5 run this cycle.
- Experts: rust (api-core), frontend (ppt-web-ui, reality-web, mobile-rn)
- Findings: 10 (see Backlog deltas)
- Next segment: `api-handlers` (last routine-side review 2026-07-14 per `state.review_cursor`; dispatcher tier1d is churning through segments daily out of band)
- Skipped: yes — the dispatcher's Tier-1d generator has already emitted 10 fresh signals from 4 recent slices; folding them in this run gives the same visibility without a duplicate rotating review.

## Screen-map status
- Total `docs/screens/` files: `ppt/` + `reality/` (mobile not yet seeded)
- Drift signals this run: 0 promoted (marginal candidates on PR #2411 + #2409 were test-file-only touches; heuristic left off to avoid noise)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run: not queried this run (frontend CI hasn't been the bottleneck; deferred to keep the run within cache budget)

## Backlog deltas
- **New:** [3] `code-review-api-core-voice-cmd-fabricated-success` — voice-assistant handlers action_check_balance/action_report_fault ship as stubs with fabricated success on the live webhook path (severity-upgraded to High after opening the file).
- **New:** [3] `code-review-reality-web-listings-no-error-branch` — reality-web `/listings` silently renders empty state on API failure (severity-upgraded to High after opening the file).
- **New:** [2] `code-review-api-core-api-call-ssrf-hostname` — validate_external_url only guards literal IPs; hostname resolution can bypass the private-IP block.
- **New:** [2] `code-review-ppt-web-ui-invoicelist-noop-view` — invoice rows styled clickable but no click handler wired.
- **New:** [2] `code-review-mobile-rn-offline-queue-silent-dataloss` — processQueue drops terminally-failed mutations after 3 retries; no dead-letter store.
- **New:** [1] `code-review-api-core-workflow-cond-fail-open` — evaluate_conditions returns `Ok(true)` on unrecognised condition JSON (fail-open).
- **New:** [1] `code-review-ppt-web-ui-dsareports-i18n` — DsaReportsPage validation + error strings hardcoded English.
- **New:** [1] `code-review-ppt-web-ui-dispute-evidence-retry` — failedEvidence threaded into router state but no retry UI on DisputeDetailPage consumes it.
- **New:** [1] `code-review-reality-web-saved-search-hardcoded-i18n` — saved-searches filter summary hardcodes For Sale/For Rent/€… labels.
- **New:** [1] `code-review-reality-web-profile-hardcoded-locale` — agency/realtor public profile pages render hardcoded Slovak text on other locales.
- **Bumped:** none this run (all new signals are fresh IDs).
- **Decayed:** none (no `open` items pre-run — decay pass was a no-op).
- **Dropped:** none.

## Plans promoted
- `plans/code-review-api-core-voice-cmd-fabricated-success.md` — repository-back the two shipped voice stubs; add integration test; assert no `Uuid::new_v4()` fabricated ticket ids · adversarial pass: passed (opened `voice_commands.rs:235-310` — "In a real implementation" comments confirmed, dispatch table wires the two intents without a feature-flag, three webhook entry points reach the stubs; hypothesis → suggested approach → test plan chain verified against the file).
- `plans/code-review-reality-web-listings-no-error-branch.md` — destructure `isError`/`error` from `useListings`, render explicit error branch mirroring favorites/inquiries pattern · adversarial pass: passed (opened `app/[locale]/listings/page.tsx:145,337-341` — grep confirmed zero `error`/`isError`/`catch` tokens in the file; sibling pages each have 4+; hypothesis chain intact).

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlisted-action-shape (screen-map-drift / screen-map-orphan / fixme-in-merged-code / dep-update-noise-lockfile / stalled-review) this run. The two promoted plans are Phase 3 manual-implementer work.

## Self-review findings
- none

## Open questions
- Phase 1.6 (project-management skill) was skipped this run to keep the token budget tight given the 49h routine lag + the buffer-refill directive. The last pm-security role read was 2026-05-27 (52 days stale) — surfacing here so it can be caught up next incremental run or via `text: "pm:security"`.
- The dispatcher's Tier-1d generator is running independently from the routine's `state.review_cursor`. Consider adopting the dispatcher's dated signal filenames as the source of truth for review recency (see 2026-07-18 tier1d selection-reason note in `.research/signals/2026-07-18-mobile-rn-tier1d.json`).

## Quiet day?
- No — 28 PRs merged, 10 new backlog vectors, 2 plans promoted, 0 auto-fix. Buffer refill directive served.
