# 2026-07-18

## Since last run
- Merged PRs: 12 (range #2407–#2421, all auto-impl follow-up fixes; author=martin-janci)
- Open PRs touched: 10 (9 of them dependabot; the 10th is draft PR #2377 — gh-issue-2360 portal view-webhook dedup)
- New / updated issues: 11 (#2399–#2406, #2413, #2417, #2418 — all `follow-up` / `from-merged-review`, several closed on merge of the corresponding fix PR)
- Commits: 30 on `dev` (12 merge commits + 18 `chore(research)` dispatcher heartbeats; last shipping code sha = c8eb68e7 = PR #2421)
- Routine lag: 2d 13h since last run (lag_warning fired; not stale — <72h)
- Phases that failed: none

## Shipped
- #2407 gh-issue-2404: guard against src/data ↔ sitemap.json drift
- #2408 gh-issue-2402: fix /sign reload regression + strip only the token param
- #2409 gh-issue-2401: reconcile favorites client to real reality-server DTO (fix no-op optimistic toggle)
- #2410 gh-issue-2400: transcode iOS HEIC/HEIF to JPEG in DocumentUploadScreen photo picker
- #2411 gh-issue-2403: localize create-schedule form + map remaining error codes
- #2412 gh-issue-2399: purge persisted AsyncStorage caches on mobile session change
- #2414 gh-issue-2406: validate IdSchema at screen-map init write boundary
- #2415 gh-issue-2405: delete orphaned `packages` i18n namespace from cs/de/hu/pl/sk
- #2416 gh-issue-2413: fix cross-tenant IDOR in government_portal connection get/update/delete/test (security)
- #2419 gh-issue-2417: fix scheduler-RLS comments citing wrong migration number (00216 → 00218)
- #2420 code-review-api-core-scheduler-mutex-poison: recover poisoned scheduler metrics mutex
- #2421 gh-issue-2418: guard cross-org /rag/migrate RLS under a NOBYPASSRLS role (security)

## Watch
- Buffer starved: dispatcher reports `claimable=0/72` — the coverage list is 47/49 done and every open action-list row already terminal. Today's routine adds 10 new backlog rows (9 from dispatcher-produced Tier-1d code-review findings + 1 for a closed-unmerged dependabot PR), which begin refilling the pool.
- Reverted: none
- Stalled review: none (no open PR >7 days without a review decision)
- Churn hotspots: not computed this run (per-PR file lists over 12 merged PRs would blow the MCP token budget; the dispatcher's Tier-1d segments — api-core, mobile-rn, ppt-web-ui, reality-server — implicitly cover the recent churn surface)

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher's `.research/management/assignments.json` shows 1 active row, 0 with `verdict=changes`)

## Code review slice
- Segment reviewed: 4 segments (api-core, mobile-rn, ppt-web-ui, reality-server) — Tier-1d work done by the dispatcher earlier today; the routine folds those signals into `signals/2026-07-18.json` and the backlog
- Experts: rust + frontend + completeness
- Findings: 9 (`code-review-finding` — 3 security in reality-server, 3 bug in ppt-web-ui, 2 bug in api-core, 1 bug in mobile-rn)
- Next segment: whichever segment has the oldest `review_cursor` date at next run — currently reality-web (2026-07-17) and mobile-native-kmp (2026-07-16) are the two least-recently reviewed
- Skipped: no — reviewed via dispatcher hand-off

## Screen-map status
- Total `docs/screens/` files: not counted this run (drift signals would require per-PR file lists; none of the 12 merged PRs are known route-file changes — all are follow-up fixes to feature paths rather than new routes)
- Drift signals this run: 0
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not checked this run

## Backlog deltas
- **New:** [3] reality-server create_invitation missing membership check — broken-access-control priv-esc → promoted to plan
- **New:** [2] reality-server accept_invitation does not verify caller email matches invite target
- **New:** [2] api-core idempotency middleware leaks pg_advisory_lock on request-cancel
- **New:** [2] mobile-rn offline queue silently drops non-permanent 3x-retry failures — data loss
- **New:** [2] ppt-web dashboard Action Queue is fully mocked on a shipped surface
- **New:** [2] ppt-web ContentModerationPage window.prompt+`as` cast bypasses action_type enum
- **New:** [1] reality-server list_members has no principal extractor — unauthenticated roster read
- **New:** [1] api-core require_permission() falls back to Guest — latent fail-open helper
- **New:** [1] ppt-web SessionsPage uses `as unknown as` cast — hides SDK contract drift
- **New:** [1] Dependabot PR #2385 (rust-toolchain 1.94.1→1.100.0) closed unmerged — verify supersession
- **Bumped:** none (no signal re-fired against an existing item)
- **Decayed:** none (no open items existed prior to this run — buffer was fully drained)
- **Dropped:** none

## Plans promoted
- `plans/code-review-reality-server-invite-authz.md` — reality-server missing agency-membership gate on `POST /agencies/{id}/invitations` + unfiltered `role` field enables invite→accept privilege escalation to any agency · adversarial pass: passed

## Auto-fix
- no candidate — none of today's signals matched the Phase-5 allowlist (allowlist covers screen-map-drift, screen-map-orphan, fixme-in-merged-code, dep-update-noise lockfile-drift sub-shape, and stalled-review; the 9 `code-review-finding` security/bug rows require the manual-implementer path, and the `dep-update-noise` rows this run are open dependabot PRs not lockfile-drift sub-shape)

## Self-review findings
- open=21 (high=4, medium=13, low=4); new this run=0; acknowledged=19
- fp-1 [recurrence=10] — Phase 6 archive-move produced a 198KB assignments-archive.json; mcp__github__push_files cannot inline files that large, so the archive can't be pushed via that path; fix: use the PR+API-merge workaround (new-branch push, `create_pull_request`, `merge_pull_request` squash) as the primary large-file push
- fp-2 [recurrence=5] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked, so PUSH_METHOD defaults to MCP push_files with a 64KiB inline guard that would false-positive; fix: add a Phase-1 runtime `git push` probe + commit-author-email guard, and bypass the size-guard when push=git
- fp-3 [recurrence=3] — Phase 2.6 buffer reported healthy while all 54 open action-list rows were already terminal in archive; fix: extend gc1-reconcile to auto-close open items whose exact task_id is terminal anywhere (active OR archive, including status=failed), and subtract archive ids from the GC3 claimable formula
- fp-4 [recurrence=2] — Tier-2 upstream planner kick returned http=401; DISPATCHER_TOKEN/DISPATCHER_URL rejected — buffer refill safety net is broken by config; fix: operator rotates DISPATCHER_TOKEN + verifies URL; dispatcher branches on 401/403 as OPERATOR-CONFIG escalation instead of folding into generic "kick failed"

## Open questions
- Phase 1.6 (`ppt-project-management` skill) was skipped this run — coverage is 47/49 done and `pm_cursor.next_index=5` (pm-security, last run 2026-05-27, 52d stale) but running the sub-skill here would exceed this session's token budget for the marginal signal. `pm_cursor`/`coverage_cursor` intentionally NOT advanced. Recommend a targeted `text: "pm:security"` or `text: "full"` fire soon to refresh the delivery synthesis and unblock coverage upkeep.
- Should the buffer-low payload (`claimable=0/72`) trigger a `text: "deep"` follow-up? A 30-day catch-up sweep would surface unchecked-TODOs / fixme-in-merged-code / hotfix-no-test signals across the merged-PR history not covered by the ~2d incremental window.

## Quiet day?
- No (12 shipped PRs since last run, 10 new backlog vectors, 1 plan promoted, 9 code-review findings folded in).
