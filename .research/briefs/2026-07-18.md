# 2026-07-18

## Since last run
- Merged PRs: 26 (range #2371–#2416)
- Open PRs touched: 11 (9 dependabot; 2 draft author work: #2332 #2377)
- New / updated issues: 23 (11 with `follow-up`/`from-merged-review`; 2 still open: #2417 #2418)
- Commits: 68 on `dev` (28 non-research application-code + 40 research/dispatcher artifacts)
- Routine lag: **2d 0h since last run** (last: 2026-07-16T03:04:00Z)
- Phases that failed: **phase-1.5-rotating-expert-review (skipped)**, **phase-1.6-project-management (skipped)** — dispatcher tier1d and post-merge-review already covered the window (per prior run's rationale; pm-cursor stays)

## Shipped
26 merged PRs (all `gh-issue-*` follow-up work + churn-hotspot refactors). Highlights (grouped):

- **Cross-tenant security / IDOR** (#2413→#2416 government_portal connection unscoped get/update/delete/test — cross-org IDOR fix; #2357 → #2374 escalated-AI role gate regression coverage; #2363 → #2378 strip signing token from /sign query + Referer)
- **Post-merge follow-ups (from-merged-review)** (#2405→#2415 orphaned `packages` i18n namespaces purged; #2406→#2414 IdSchema validation at screen-map init write; #2404→#2407 sitemap.json ↔ src/data drift guard; #2403→#2411 create-schedule form localization + error-code map; #2402→#2408 /sign reload regression fix; #2401→#2409 favorites client DTO reconciliation; #2400→#2410 iOS HEIC/HEIF → JPEG transcode; #2399→#2412 AsyncStorage purge on mobile session change)
- **Scheduler / reports / RLS** (#2318 → #2340 scheduler worker RLS context; #2358 → #2376 portal_webhook_events dedup net; #2370 → #2384 create-schedule report-label localization; #2369 → #2381 category badge sync on optimistic toggle)
- **Reality-server / listings** (#2362 → #2373 listing-detail shape validation into parseListingDetail; #2364 → #2379 Decimal-as-string decode guard on sibling surfaces; #2361 → #2372 mobile login clears whole query cache; #2359 → #2375 /agencies/me active-membership resolution)
- **Portal integrations** (#2320 → #2339 org-scoped file_key + signed Content-Length; #2367 → #2380 IdSchema on synthesized candidate ids; #2368 → #2383 mobile upload allow-list drift + photo MIME; #2365 → #2382 favorite price-alert AC-5 toggle regression; **gap-83-2** → #2398 persist Booking.com push sync state)
- **Churn refactors (hotspot follow-ups)** (`refactor-churn-hotspots-en-json-2026-07-16` → #2395; `refactor-churn-hotspots-sitemap-json-2026-07-16` → #2397; `dx-stalled-review-pr-988-retry2` → #2396 @ppt/sitemap CJS default export)
- **Post-merge review (from ppt-review-merged skill)** filed 2 new follow-up issues: **#2417** (scheduler-RLS doc citing wrong migration 00216→should be 00218) and **#2418** (rag/migrate cross-org RLS test passes vacuously — runs as superuser, bypassing FORCE RLS) — both `follow-up`/`from-merged-review`, both already claimed `in-progress` by dispatcher

## Watch
- Stalled review: **none** — no open PR crosses the 7-day threshold. Two author-drafts still open: **#2332** (ai-chat gate escalated feedback, 3d1h stale) and **#2377** (portal view-webhook dedup, 1d10h). Neither carries an APPROVED reviewDecision, so no draft-approval stuck-state.
- Reverted: 0
- Churn hotspots this run (raw code, after exclusions — docs/data/lockfile filtered):
  - `backend/servers/api-server/src/routes/reports.rs` — **+3329 lines** (runs_seen: 2 → 3; `repeated-churn` fires fresh +1 — 5 PRs edited scheduling/RLS: #2318 #2340 #2376 #2384 #2411)
  - `backend/crates/integrations/src/booking/mod.rs` — **+3185 lines** (runs_seen: 1 → 2; next re-emission will fire repeated-churn — Booking push sync state work in gap-83-2/#2398)
  - `backend/servers/api-server/src/routes/auth.rs` — **+2950 lines** (runs_seen: 3 → 4; already-tracked repeated-churn, cumulative delta=0)
- Runner-ups (informational, below top-3): `oauth_integration_tests.rs` (+2719 — already runs_seen=3), `financial.rs` (+2411), `buildings.rs` (+2347), `lease.rs` (+2303), `integrations/{sync,install,webhook}.rs` (+2293/+2283/+2240), `booking_oauth_csrf_tests.rs` (+2205 not in this run), reality-server `documents.rs` (+2213).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **skipped by the routine (catch-up + dispatcher-coverage)**
- Reason: dispatcher's Tier 1d ran `ppt-web-ui` (2026-07-17), `api-core` (2026-07-17), and `reality-web` (2026-07-17); post-merge review (Tier 1d generator fold) scanned 5 PRs and filed #2417 #2418. Routine-side pass would duplicate.
- Findings: 0 (routine skipped) + dispatcher tier1d files persisted at `.research/signals/2026-07-17-*.json`
- Next segment (when routine resumes): `reality-server` (last routine cursor 2026-07-13; dispatcher touched it 2026-07-16)
- Skipped: yes — dispatcher tier1d coverage is ahead of routine cursor across ≥4 segments

## Screen-map status
- Total `docs/screens/` files: 168 (106 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` still not seeded → 0)
- Drift signals this run: 0 (no PR touched a route file without an accompanying `docs/screens/{ppt,reality}/*.md` — the two frontend-heavy PRs #2414 `screen-map/init-write.ts` + #2407 `sitemap.json` guard both stayed in `frontend/packages/screen-map` and `frontend/packages/sitemap` respectively, not the `App.tsx`/`routes/**` trigger set)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (behavior-preserving PRs since the last check)

## Backlog deltas
- **New:**
  - [2] security · `security-rag-migrate-rls-vacuous-test` — RAG /rag/migrate cross-org RLS test passes vacuously (superuser bypass) · Issue #2418 · confidence=high · **dispatcher already claimed `in-progress` (gh-issue-2418)** — not promoted to plan
  - [1] bug · `bug-scheduler-rls-migration-doc-numbers` — scheduler-RLS doc/test cite migration 00216 (should be 00218) · Issue #2417 · confidence=high · **dispatcher already claimed `in-progress` (gh-issue-2417)** — not promoted to plan
  - [1] refactor · `refactor-churn-hotspots-reports-rs-2026-07-18` — churn hotspot: routes/reports.rs (3329 lines, runs_seen: 3 fires repeated-churn +1) · confidence=high · below promotion threshold
  - [1] refactor · `refactor-churn-hotspots-booking-mod-2026-07-18` — churn hotspot: booking/mod.rs (3185 lines, first repeat) · confidence=high · below promotion threshold
- **Bumped:** none
- **Decayed:** 0
- **Dropped:** none
- **Notable non-creations:** `churn-hotspot-backend/servers/api-server/src/routes/auth.rs` re-emitted with `score_delta=0` (cumulative, `seen_signals` already contains id). `repeated-churn-auth.rs` also re-emitted with delta=0.

## Plans promoted
- **none** — both high-scoring candidates (#2418 security-fast-track at score 2, #2417 bug at score 1) are already claimed `in-progress` by the dispatcher (assignments.json rows `gh-issue-2418`, `gh-issue-2417`). Slug-stem uniqueness + "no existing active plan references the same `sources`" forbid promotion. The two churn-refactor rows sit at score 1, below the 3-point promotion threshold. **Buffer-low resolution:** the dispatcher's `action-list.json` (5 open) plus the assignments (4 in-progress + 1 quarantined) already contain the routine's freshest observations; today's routine adds evidence but not new plans. The next-run promotion path opens once these merge and hotspot scores compound.

## Auto-fix
- `no candidate` — the two `code-review-finding` signals score ≥2 with `confidence=high`, but the routine's Phase-5 allowlist accepts only `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` (lockfile-drift only), `stalled-review`. RLS test-refactor / doc-comment-fix are not in the allowlist — they go via the manual/dispatcher plan path. The dispatcher already picked both up.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — `mcp__github__push_files` can't inline the >64 KiB `assignments-archive.json`; fix: adopt PR + API-merge as dispatcher's primary push path for the archive
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer/GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids + extend `gc1-reconcile.sh` to auto-close any exact-id-terminal row (incl. status=failed)

## Open questions
- none

## Quiet day?
- No — 26 merged PRs, 4 new backlog vectors, 2 post-merge-review follow-up issues actionable; but 0 plans promoted (dispatcher claimed the promotable candidates).

## Goal violations
All 17 gates pass. G8/G9/G17 (blocking): 0 non-research paths / 0 secrets / 0 telegram tokens. G10/G14 (rendered views): both byte-identical to canonical regen. G11: Phase 1.5 + 1.6 skipped, `review_cursor` and `pm_cursor` unchanged. G15: no auto-fix candidate matched the allowlist.
