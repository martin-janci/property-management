# 2026-06-11

> Same-day rerun — **fourth pass today** (after 03:11Z, 04:30Z, 08:20Z). Fired
> by the dispatcher with a third `buffer-low` payload at 14:20Z:
> `claimable=14 (<floor 36, <half-floor 18). Please refill action-list from
> coverage.json partial-with-gaps stories.` Between the 08:20Z run and now
> the team merged **9 substantive PRs** — eight of them PAP-67/PAP-80/PAP-150
> RLS-migration follow-ups (every one shipped with its own
> `*_rls_repo_tests.rs` regression file) plus three research-side perf/cost
> wins (PAP-161/163/164). One fix PR (PAP-170, webhook-rls) merged **without
> a new test diff** despite adding two repo methods — fires the canonical
> `hotfix-no-test` signal. Buffer-low itself is unchanged from the carry-
> forward analysis: the dispatcher's 14 claimable rows are already the
> coverage-derived `dispatcher-tier1-refill-coverage` set; further refill
> requires a local `/ppt-project-management scan` run (cloud routine is
> explicitly forbidden from running scan mode — Phase 1.6 of
> `routine-prompt.md`).

## Since last run
- Merged PRs: **9** (range #1227–#1288) — 6 RLS-context migration fixes (all with new tests), 1 webhook-RLS fix (no new test → signal), 3 research-side perf/cost optimizations.
- Open PRs touched: **13 newly opened** — 7 dependabot dep bumps (`dep-update-noise`, score 0), 6 author RLS/security PRs in flight (#1282, #1285, #1286, #1287, #1289, #1290). All <0.2 days old; none stalled.
- New / updated issues: **0** (cursor 1267 unchanged).
- Commits: ~14 on `dev` since `9af6dce` — 9 PR merges + version bumps + research/dispatcher meta-commits.
- Routine lag: **~6h** since last run (well under 36h; no `lag_warning`).
- Phases that failed: **none**.
- Re-trigger reason: dispatcher payload `buffer-low: dispatcher backlog at claimable=14 (<floor 36, <half-floor 18). Please refill action-list from coverage.json partial-with-gaps stories.` Treated as DATA per `routine-prompt.md` "Special trigger payloads": the directive cannot be auto-followed because (a) `coverage.json` has no `partial-with-gaps` status (statuses are `done` / `partial`; 20 partial stories), (b) the routine is explicitly forbidden from running `ppt-project-management` skill in `scan` mode in cloud sessions, (c) the existing 15 open action-list rows are already sourced from `dispatcher-tier1-refill-coverage` — adding more requires fresh gap discovery (local scan).

## Shipped
- #1227 **fix(db): route building_certification repo through RLS-context connection (PAP-80)** — converts `BuildingCertificationRepository` (8 FORCE-RLS tables) from raw `PgPool` to RLS-executor pattern. Stateless repo; defense-in-depth org-keyed predicates retained. **Ships with `building_certification_rls_repo_tests.rs`** — NOSUPERUSER role test proving deny-all, fix, cross-tenant invisibility, write path. No `hotfix-no-test`.
- #1232 **fix(db): route ai_chat repo through RLS-context connection (PAP-103)** — `AiChatRepository` converted (3 FORCE-RLS tables). `add_message` runs message INSERT + parent-session `last_message_at` UPDATE on same RLS-scoped conn. **Ships with `ai_chat_rls_repo_tests.rs`.**
- #1270 **fix(api-server): route workflow repo through RLS-context connection (PAP-77)** — `workflow.rs` (5 FORCE-RLS tables under `/api/v1/ai/workflows`) converted. Removes unscoped `find_by_id` / `find_execution_by_id` / `list_execution_steps`. Background executor acquires short-lived org-context guards. **Ships with `crates/db/tests/workflow_rls_repo_tests.rs`.**
- #1272 **fix(api-server): cast registry enum reads + designation column on pet/vehicle/parking surface (PAP-158)** — DiD top-up of #1271 (PAP-143). Replaces bare `SELECT *` / `RETURNING *` with explicit columns + `::text` casts for `pet_type`/`pet_size`/`vehicle_type`/`status` enums; aliases `u.designation AS unit_number` everywhere; tenant-scopes the vehicle-list `parking_spots` join. **Ships with `registry_enum_decode_tests.rs` + `vehicle_list_does_not_leak_cross_tenant_parking_spot`.** Out-of-scope follow-up explicitly noted: PAP-159 (pet_type[] enum-array decode + encode-side bind cast) — filed under PAP-136, routed to CTO.
- #1280 **chore(repo): cut agent token cost — trim CLAUDE.md, add repo-map + findings (PAP-161)** — `CLAUDE.md` 410→301 lines (~820 tok/heartbeat saved); new `docs/repo-map.md` (location index); new "Agent Efficiency (token discipline)" section; new `.research/token-optimization-findings.md` with phased graphify recommendation. Docs-only, no test impact.
- #1281 **fix(api-server): close esg_reporting cross-tenant IDOR — RLS executor + org keying (PAP-139)** — closes the PAP-136 cross-tenant IDOR on `/api/v1/esg/*` (Epic 136), simultaneously unblocking PAP-72 FORCE-RLS deny-all. Stateless repo (18 by-id queries); every esg_* table org-keyed. **Ships with `esg_reporting_cross_org_idor_tests.rs` (9 cases: metric read/update/verify/delete, report read/submit/delete, unauthenticated, same-org success).** Mirrors merged PR #1262 pattern.
- #1283 **perf(research): slim dispatcher/routine reads — backlog.json projection + Phase 8 factoring (PAP-163)** — two behavior-preserving slices. (1) routine-prompt.md teaches `backlog.json` projection load (146→96 KB, ~12.5k tok/run saved). (2) `dispatcher-prompt.md` Phase 8 reviewer-prompt extracted to `.research/self-improvement/reviewer-prompt.md` (~1.9k tok/run saved). G10 byte-identity preserved. Research-side; no test impact.
- #1284 **perf(research): extract dispatcher merge-path subagent prompts (PAP-164)** — follow-up. Extracts Phase 5 PR-reviewer / 5.4 pre-merge autofix / 5.6 rebaser / 5.7 PR follow-up prompts to `.research/management/` files. Phase 5.5 merger kept inline (already skill-thin). **Adds T27 self-test gate** (hard-fails if extracted prompt missing/truncated/unreferenced — compensates for absent dispatcher dry-run). Measured saving ~1.3–1.6k tok/run.
- #1288 **fix(api-server): route webhook handlers through RLS connections (PAP-170)** — PAP-150 burndown: 6 raw `state.db` accesses across `routes/integrations/webhook.rs` (esignature_webhook, handle_airbnb_webhook) + `routes/voice_webhooks.rs` (oauth_token_exchange, oauth_token_refresh) converted. Adds repo methods `find_esignature_workflow_by_external_id`, `record_airbnb_webhook_event`. Both `webhook.rs` and `voice_webhooks.rs` removed from `rls-handler-baseline.txt`. **NO NEW TEST FILE in diff** → `hotfix-no-test` signal fires (PR body claims existing `update_esignature_workflow_by_external_id` DB coverage, but the two new repo methods + the bootstrap-then-bind RLS pattern in `oauth_token_refresh` are not pinned).

## Watch
- Stalled review: none new. Oldest open is still #988 Playwright E2E framework (~9 days idle). All 13 newly-opened PRs are <0.2 days old.
- Reverted: none.
- Churn hotspots: no significant new file activity beyond the per-PR diffs above. `hotspot_history` carries forward unchanged. The `registry.rs` / `voice_webhooks.rs` / `webhook.rs` / `*_rls_repo_tests.rs` churns are localized and well below the multi-PR hotspot bar.
- **Buffer-low resolution (third carry-forward).** Re-checked the prior brief's claim that "all 15 stems collide with archive-terminal rows" — **that claim was wrong.** All 15 open action-list stems are NOT in `action-list-archive.json` at all (collision count = 0). The real cause of `claimable=14` is **15 open − 1 active assignment** (`refactor-churn-hotspot-mobile-announcements-test` in `review` state on PR #1165, MANUAL-REBASE-blocked since 2026-06-09 — `mergeable=dirty`, MCP `update-branch` 422 conflict, sandbox `git push` 403). The buffer is genuinely low; the right next pass is still a delivery-PM `/ppt-project-management scan` (forbidden in cloud) plus an unblock of #1165 (manual rebase from a local checkout).

## PRs stuck in draft despite approval
- none (all 13 newly-opened PRs are non-draft, all <0.2 days old)

## PRs with verdict=changes, no fix-round progress in 24h
- (not queried — `assignments.json` lives on the dispatcher session, not this routine)
- none (assignments.json this session has 1 `review` + 1 `quarantined`; both >24h, both already surfaced)

## Code review slice
- Segment reviewed: **skipped** — same-day rerun (fourth pass today; `api-handlers` already advanced to 2026-06-10 last run, `ppt-web-ui` + `reality-server` to 2026-06-11 on the 08:20Z run). The 9 newly-merged backend PRs would map to the `api-handlers` segment, but its cursor moved today and the 6h churn window is small.
- Experts: none invoked.
- Findings: **0**.
- Next segment: **reality-web** (last reviewed 2026-05-28 — oldest unreviewed; will run on the next non-same-day routine fire).
- Skipped: yes — same-day rerun, segments most-recently scanned today.

## Screen-map status
- Total `docs/screens/` files: `ppt/` + `reality/` populated; `reality-mobile/` has `nav.md` + `search.md`; `mobile/` still empty (drift detection intentionally excludes `mobile`).
- Drift signals this run: **0** — all 9 merged PRs touch backend (`api-server`, `crates/db`, `scripts/`) or research-side (`.research/`, `CLAUDE.md`, `docs/repo-map.md`, `dispatcher-prompt.md`). None touch frontend route files in the trigger set.
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not queried this rerun.

## Backlog deltas
- **New:** [2] `test-gap-hotfix-no-test-pr-1288-webhook-rls` — webhook RLS migration shipped without a new regression test for the two added repo methods. Confidence=medium (signal type is analytical inference from the diff; the diff itself is fact-grade).
- **Bumped:** none.
- **Decayed:** none (oldest open is `test-gap-screen-map-drift-pr-922-ppt` at 2026-06-01, **10 days** — still within the 14-day window; nothing decays).
- **Dropped:** none.
- **Resolved:** none — cross-referenced the 9 merged PRs against the 27 open/ready backlog items by ID-in-body and file-overlap. No open backlog item references PAP-77 / PAP-80 / PAP-103 / PAP-139 / PAP-150 / PAP-158 / PAP-161 / PAP-163 / PAP-164 / PAP-170 directly. The broader `RLS-handler-baseline` migration thread is captured in already-`done` items (`security-rls-migration-residual`, et al.).

## Plans promoted
- (no new promotions this run — 2 plans remain `ready` from prior runs: `plans/security-llm-doc-idor.md` and `plans/bug-ios-searchview-uncompilable.md`, both intact in `plans/`. The new `test-gap-hotfix-no-test-pr-1288-webhook-rls` at score 2 sits below the score-3 readiness gate; not promoted.)

## Auto-fix
- `no candidate` — the new `hotfix-no-test-pr-1288-webhook-rls` signal does not match the Phase-5 allowlist (allowlist = `screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / `dep-update-noise` lockfile-drift / `stalled-review`). The 7 dependabot dep-update PRs (#1273-#1279) are not lockfile-drift sub-shape (those are dep-update PRs themselves, not the consumer-manifest-out-of-sync follow-up shape). The 2 existing `ready` plans are multi-file code changes outside the allowlist. Manual implementer path applies.

## Self-review findings
- (`.research/self-improvement/findings.json` not surveyed this same-day rerun.)
- none

## Open questions
- **action-list refill (fourth pass today, dispatcher fired three buffer-low payloads in a row).** The dispatcher's `claimable=14` is a real signal: 15 open action-list rows minus 1 in-flight (`refactor-churn-hotspot-mobile-announcements-test` PR #1165, MANUAL-REBASE-blocked). Floor=36, half-floor=18 → 14 < both. Refill requires either (a) a local `/ppt-project-management scan` to discover fresh coverage gaps and emit new `dispatcher-tier1-refill-coverage` rows, or (b) unblocking #1165's manual rebase. **Both are outside the cloud routine's scope.** The previous brief's "all stems collide with archive" diagnosis was wrong (verified this run: zero collisions); the real cause is normal buffer drain + one stuck assignment.
- **iOS toolchain gap (carry-forward).** `bug-ios-searchview-uncompilable` still needs macOS + `xcodebuild` to verify; dispatcher's cloud-implementer path skips `Mode: local-only`. Local-implementer or Mac-runner pickup required.

## Quiet day?
No — 9 substantive PRs merged (PAP-77/80/103/139/158/161/163/164/170), 13 new open PRs, 1 new backlog vector. `last_pr_seen` advanced 1271→1288. Goal violations section is empty (all 17 G-checks expected to pass).
