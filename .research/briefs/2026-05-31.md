# 2026-05-31

## Since last run
- Merged PRs: 6 (#862–#866 + #869 — note: gap at #867/#868 because #867 is a new tech-debt issue and #868 is an open draft PR)
- Open PRs touched: 1 — #868 (sqlx-migration CI gate, draft, opened 2026-05-31T00:28Z by martin-janci); #854 (data-retention privacy doc, draft, dispatcher recorded `verdict=approve` in commit db67ad7) and #745 (e-sig nonce replay v2, draft, ~2d) carried over from prior run with no Phase-1-eligible activity in this window
- New / updated issues: 1 OPEN new issue (#867); 9 issues closed by merged PRs in this window (#806/#808/#817/#824/#835/#836/#845/#849/#799)
- Commits: 10 on `dev` since cursor 8bd4d44 (now 773afb6) — 6 squashed security/fix PRs + 4 dispatcher research-chore checkpoints
- Routine lag: on schedule (~6h since 2026-05-30T20:44Z, no lag_warning)
- Phases that failed: phase-1.5 (skipped — `ppt-dev-review` skill not invoked in this one-shot routine pass; `review_cursor` not advanced), phase-1.6 (skipped — `ppt-project-management` skill not invoked; `pm_cursor` + `coverage_cursor` not advanced)

## Shipped (6 merged PRs — 5 security, 1 production-routing fix)

**Security hardening (5, all labeled `security`):**
- #862 fix(security): require auth on all IoT sensor endpoints (closes #817, #824, #845) — 16 of 22 handlers under `/api/v1/iot/sensors` had no `RequestPrincipal` extractor; PR adds `principal: RequestPrincipal` + `let _tenant_id = require_tenant_id(&principal)?;` to every previously-open handler (create_sensor, get_sensor, update_sensor, delete_sensor, list_readings, add_reading, add_batch_readings, get_aggregated_readings, list_thresholds, create_threshold, update_threshold, delete_threshold, resolve_alert, list_correlations, delete_correlation, apply_template). Adds `iot_auth_tests.rs` (16 cases × 401 assertion).
- #863 fix(security): block SSRF in workflow `call_webhook` action (closes #835) — `WorkflowExecutor::execute_webhook` was accepting attacker-influenced URLs from workflow action JSON and issuing `reqwest` requests with no private-IP/metadata blocking. PR adds `common::url_validation::validate_external_url(&webhook_config.url)` before the request (mirrors `routes/integrations/webhook.rs`). Adds 5 regression tests in `workflow_webhook_ssrf_tests.rs` (cloud-metadata 169.254.169.254, loopback, localhost, RFC1918, non-http scheme).
- #864 fix(security): require manager role for building create/archive/bulk-import (closes #799) — adds `building_manager_rbac_tests.rs` (RLS-only auth was the concern flagged by the code review of Epic 3).
- #865 fix(security): gate org-wide notification role-defaults behind manager (closes #808) — `update_role_defaults`, `delete_role_defaults`, `apply_role_defaults` previously used only `AuthUser` + `TenantExtractor`; PR adds `is_manager()` capability check. Adds `granular_notif_role_defaults_rbac_tests.rs`.
- #869 fix(security): require auth on board-meeting endpoints (closes #849) — 32 handlers under `/api/v1/board-meetings/*` (member CRUD, meeting lifecycle, agenda items, motions incl. `cast_vote`, minutes, action items, documents) had no `AuthUser` extractor. PR adds `_user: AuthUser` to each previously-open handler so unauthenticated requests are rejected with 401 before the handler runs. Adds `board_meetings_auth_tests.rs` (15 cases × 401). PR body explicitly notes follow-up: per-resource org / board-member ownership scoping (e.g. binding `cast_vote` to caller's `board_members` row, cross-org checks) is deferred — closes the P0 unauthenticated-access only.

**Production-routing fix (1, no label):**
- #866 fix(api-server): register 5 routers missing from the production binary (closes #836) — `backend/servers/api-server/src/main.rs` was building the production router from a hand-maintained `.nest(...)` chain separate from `lib.rs::create_router` (used by `TestApp` / integration tests). 5 routes that `create_router` exposed — `push-tokens`, `mfa/recovery-codes`, `pricing`, `property-valuations`, `data-residency` — passed tests but were unreachable in prod. Adds the 5 `.nest()` calls to `main.rs`. **No test added** for route-set parity — the divergence will recur the next time a route lands on only one side. Author opened follow-up tech-debt issue #867 calling out exactly that, with a proposed fix (have `main()` build from `lib.rs::create_router` and layer prod-only middleware on top, OR extract `fn app_router(state) -> Router` shared by both, plus a parity test). Both signals fired this run.

## Watch
- Stalled review: none new (#745 still draft at ~2d, no review request; #854 has `verdict=approve` from dispatcher reviewer at commit db67ad7 — author may want to flip out of draft)
- Reverted: none in window
- Closed-unmerged: none in window
- In-flight security: `security-llm-doc-idor` (score 3, plan ready since 2026-05-29) — still untouched. Re-verified at `routes/ai.rs:2624/2645/2872` — handlers continue to discard `_principal`. No PR in this window touched `ai.rs` or `llm_document.rs`. **This is now the #1 unaddressed P0 in the open-row queue** — every other security row at score ≥ 2 with confidence=high has been merged in the last 48h.
- Churn hotspots this run: micro-window (6h sub-day), 6 small patches. Source-file churn: `routes/board_meetings.rs` +32, `routes/iot.rs` +32, `routes/buildings.rs` (mod), `routes/granular_notifications.rs` (mod), `routes/leases.rs` (mod), `crates/integrations/src/workflow_executor.rs` +8. All landing sites for verified security fixes, not unstable hotspot patterns — score-suppressed (mirrors 2026-05-30 precedent). No additions to `hotspot_history`.
- New tech-debt issue #867 raised by the same PR (#866) author calls out a recurring structural problem (main.rs ↔ lib.rs::create_router divergence). Treat as both a `test-gap` (the parity test that would have caught #836 before merge) AND a `triage` row (#867 awaiting human label).

## PRs stuck in draft despite approval
- #854 pm-data-support-data-retention-privacy — dispatcher reviewer recorded `verdict=approve` (commit db67ad7) but PR is still draft (last update 2026-05-31T00:23Z, ~3h ago). Author needs to flip out of draft for merge.

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: none (phase-1.5 skipped — `ppt-dev-review` skill not invoked in this one-shot routine pass)
- Experts: n/a
- Findings: 0
- Next segment: `api-handlers` (oldest unreviewed — 2026-05-23, 8 days ago)
- Skipped: yes — `ppt-dev-review` skill not invoked

## Screen-map status
- Total `docs/screens/` files: 108 (`ppt/` 62 + `reality/` 46; `mobile/` not yet seeded → 0) — count unchanged this run
- Drift signals this run: 0 (no merged PR touched a frontend route file in the trigger set — all 6 PRs are backend-only)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not re-checked this run

## Backlog deltas
- **New (test-gap):** [2] api-server main.rs vs lib.rs::create_router diverge silently (5 routes unreachable in prod, no test asserts parity) — `test-gap-router-set-parity-tests` — files `backend/servers/api-server/src/main.rs`, `backend/servers/api-server/src/lib.rs`; sources PR #866, issue #867, issue #836
- **New (triage):** [1] Issue #867 (no labels, OPEN): Tech debt: api-server main.rs duplicates lib.rs::create_router — routers diverge silently — `triage-issue-867`
- **Bumped:** none
- **Decayed:** none (oldest open non-triage item `dx-portfolio-dashboard-stubs` updated 2026-05-20 = 11d < 14d threshold)
- **Dropped:** none
- **Resolved → done:**
  - `triage-issue-836` — PR #866 closed issue #836 (Code review: Epic 2B-C — Mobile push & device registration). Marked done with PR reference; underlying device-token registration concern lives on under the `test-gap-router-set-parity-tests` row.
  - `triage-issue-845` — PR #862 closed issue #845 (Code review: Epic 14 — IoT alerts, correlations, thresholds). Marked done with PR reference.
  - `triage-issue-849` — PR #869 closed issue #849 (Code review: Epic 10B+143 — Admin impersonation, Help, Board meetings auth). Marked done with PR reference.
- **Resolution checks (open items re-verified, unchanged this run):**
  - `security-llm-doc-idor` (score 3, ready) — files `routes/ai.rs` (lines 2624, 2645, 2872) + `llm_document.rs` re-grepped; handlers still discard `_principal`. No PR in window touched either file. Stays `ready`.
  - `bug-webhook-airbnb-dup-sync-jobs` (score 2, open, [partial]) — no PR in window touched the inner-loop dedup TODO at `webhook.rs:1073`. Stays `open`.
  - `test-gap-booking-push-validation-untested` (score 2, open) — no PR added tests for #607 batch-cap / non-negative guards. Stays `open`.

## Plans promoted
- none this run. The only new non-triage signal (`hotfix-no-test-pr-866`) seeded a backlog row at score=2 — below the score≥3 readiness threshold AND not eligible for the security fast-track (vector=test-gap, not security). The new triage signal (`untriaged-issue-867`) is excluded by the vector=triage gate. `security-llm-doc-idor` remains the sole `status=ready` plan from prior runs.

## Auto-fix
- `no candidate` — no signal met the Phase 5 certainty bar + allowlist this run.
  - `hotfix-no-test-pr-866` is **not in the allowlist** (Phase 5 only handles `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review`). The right shape would be a real refactor of `main.rs` / `lib.rs` plus a parity test — that's a manual-plan path, not a safe-shape auto-fix.
  - `untriaged-issue-867` is `vector=triage` — out of allowlist by design.

## Self-review findings
- open=11 (high=3, medium=4, low=3) + 1 critical; new this run=0; acknowledged=9; source: dispatcher Phase-8 self-review (`.research/self-improvement/findings.json`), not routine-generated
- `subagent-race-on-dev-push` [critical, rec=1] — fix landed in PR #814 (2026-05-30); re-verify status on next dispatcher run
- `no-run-level-lock-parallel-overlap` [high, rec=4] — PR #814 added Phase 0.5 run-level advisory lock; re-verify acknowledged status on next dispatcher run
- `goal-check-gc1-orphan-action-list-items` [high, rec=1] — GC1 referential-integrity orphan-action-list-items; fix (Phase 2.6 cascade prune/relink) not yet landed
- `goal-check-gc3-buffer-overshoot` [high, rec=1] — PR #814 confirmed GC3 buffer cap landed; re-verify

## Open questions
- The triage-issue cluster keeps decreasing (now 43 OPEN, down from 52 prior run after 3 PR-close-driven resolutions today). Most remaining rows are "Current dev review" / "Dev review rounds" / "Code review: Epic …" — dispatcher / dev-team output. Same question as 2026-05-30 brief: should the routine apply a default label (e.g. `dev-review`, `from-routine`) so they stop firing `untriaged-issue` signals, or is hand-triage the intent? They continue to dominate `IDEAS_TRIAGE.md`.
- PR #866's tech-debt follow-up (#867) advocates either (a) `main()` calls `lib.rs::create_router(state)` and layers prod middleware on top, or (b) `fn app_router(state) -> Router` shared by both. (a) is the simpler refactor; (b) needs a careful read of what middleware is currently prod-only in `main.rs` (TraceLayer, CORS, request-id, etc.) to decide which layer should own it. A human should pick — once chosen, this is a clear-shape vector that can be promoted with score ≥ 3 once a second piece of evidence (e.g. another "test passes, prod missing" PR) lands.
- PR #854 (data-retention/privacy doc) has `verdict=approve` but is still draft. Author action item: flip out of draft to land.

## Quiet day?
- No — 6 PRs merged in the 6-hour window (5 security + 1 prod-routing), 2 new backlog rows (1 test-gap + 1 triage), 3 backlog items resolved to `done`, 3 open items re-verified unchanged. Phase 1.5 + Phase 1.6 were skipped (no skill invocation in one-shot routine pass) and are listed under "Phases that failed".
