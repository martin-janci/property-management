# 2026-07-07

## Since last run
- Merged PRs: 14 (range #2014, #2020, #2100, #2111–#2120, #2130) — 12 substantive + 2 dep updates
- Open PRs touched: 5 (#2131 opened today by martin-janci; #2089 updated 2026-07-05; #1797 / #1812 / #2007 sitting at draft with `needs-human-review` label — pre-existing)
- New / updated issues: 24 in window — all `from-merged-review` follow-ups (labeled → not `untriaged-issue`)
- Commits: 70 on `dev` (mostly dispatcher heartbeats + 14 merge commits)
- Routine lag: 1d 23h since last run (`lag_warning` — over 36h threshold; still under 3d, no `stale_routine_alert`)
- Phases that failed: none
- Phase 1.5 (rotating expert review): **skipped this run** — token budget on catch-up; oldest unreviewed segment remains `ppt-web-core` (2026-06-05, ~32d). Cursor left untouched so the next run picks it up.
- Phase 1.6 (project-management): **skipped this run** — token budget on catch-up; `pm_cursor` + `coverage_cursor` left untouched so rotation resumes at `pm-security` / epic-index 12 next run.

## Shipped
- **#2100** — churn refactor: split `backend/crates/db/src/repositories/rental.rs` (1779 LOC monolith) into cohesive sub-modules (`mod.rs`, `bookings.rs`, `oauth.rs`, `guests.rs`, `connections.rs`, `reports.rs`, `calendar.rs`) mirroring the merged `document.rs` / `subscription.rs` / `sensor.rs` splits. **Byte-for-byte identical method bodies** per PR body; original file **deleted**.
- **#2111** — gh-issue-2106: gate `backend/deny.toml` behind code-owner review (CODEOWNERS entry).
- **#2112** — docs(db): refresh stale BIT-351 quarantine docstrings after un-quarantining the soft-delete unread test.
- **#2113** — fix(db): drive currency/country enum-sync guard off the enum variant set rather than a hand list (touches `backend/crates/db/src/models/multi_currency.rs` — churn hotspot #2 this run).
- **#2114** — fix(research): wire action-list reconcile + MCP-push size guard into dispatcher Phase 6 (turned out to have a placement bug — see #2130).
- **#2115** — gh-issue-2108: fix ListingDetailViewModel resetting to spinner on auth-token change; add hoisted-path test.
- **#2116** — gh-issue-2109: cover `include_system=true` tie-break in template pagination test.
- **#2117** — gh-issue-2103: replace positional 15-arg `SlovakAccountingExport::new` constructor with named-field input (silent-transposition guard).
- **#2118** — code-review-mobile-rn-screens-mock-data: wire mobile Meters / MeterDetail / Leases / LeaseDetail / Forms / Threads / ThreadDetail / PersonMonths screens to the api-server. **Partial resolution** for the backlog row of the same slug — BuildingsScreen.tsx and NotificationsScreen.tsx still ship `MOCK_*` arrays (verified `NotificationsScreen.tsx:36` still declares `MOCK_NOTIFICATIONS`); left row `open` with a `[partial]` evidence line.
- **#2119** — gh-issue-2110: restore executable bit on `.claude/skills/ppt-implement/scripts/check-ignore-reason.sh` + add a recurrence gate.
- **#2120** — gh-issue-2107 (**security-adjacent**): outage mutations now authorize via `RlsConnection::role().is_manager()` (DB-validated role) instead of the JWT `role` claim, which the production login flow never emits — so real managers had been 403'd for every mutation while raw-token tests stayed green. Fix + real-login regression tests (`create_outage_via_real_login_succeeds`, `full_lifecycle_via_real_login_transitions_status`) added in `outages_happy_path_tests.rs`.
- **#2130** — gh-issue-2126: the MCP-push size guard added in #2114 was placed AFTER `git commit` in Phase 6, where `git diff --cached` is empty — silently a no-op each run. Moved the invocation to right after the final `git add`, added `.research/test-mcp-push-size-guard.sh` behavioral smoke test, and wired it into the dispatcher-self-test workflow.
- **#2014** — dependabot: `axum-test` in `/backend`. Noise.
- **#2020** — dependabot: `rust_xlsxwriter` 0.95.0 → 0.96.0. Noise.

## Watch
- **Reverted:** none this window.
- **Churn hotspots (top 3, after file-existence + exclusion filter):**
  - `backend/crates/db/src/repositories/rental/oauth.rs` — 594 lines (new sub-module from PR #2100 split — one-time move-churn; `runs_seen=1`)
  - `backend/crates/db/src/models/multi_currency.rs` — 532 lines (PR #2113 enum-sync guard refactor; `runs_seen=1`)
  - `backend/crates/db/src/repositories/rental/bookings.rs` — 527 lines (new sub-module from PR #2100 split; `runs_seen=1`)
  - _Note:_ raw #1 by numbers was `rental.rs` @ 2933 lines — that's the PR #2100 split's *deleted* file; excluded from hotspot list because it no longer exists. Backlog row `refactor-churn-hotspot-...-rental-rs` marked `done` (resolved by PR #2100).
- **Stalled reviews:** #1797, #1812, #2007 — draft PRs sitting `needs-human-review` since 2026-06-23 / 2026-06-24 / 2026-07-02. Not fresh this run (no `stalled-review` signal — cursor already advanced past their update time), but flagging for visibility.

## PRs stuck in draft despite approval
- none (queried via `list_pull_requests` state=open; no PR combines `isDraft==true` and `reviewDecision==APPROVED` in the current open set — the three long-open drafts are `needs-human-review` labelled, not approved)

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher `assignments.json` on this branch shows all in-flight rows either `claimed`/`in-progress` or freshly reviewed within the last 24h)

## Code review slice
- Segment reviewed: **none this run (Phase 1.5 skipped — token budget on catch-up)**
- Experts: N/A
- Findings: 0 (see Backlog deltas — no `code-review-finding` signals emitted)
- Next segment: `ppt-web-core` (oldest unreviewed at 2026-06-05, ~32d)
- Skipped: yes — token budget

## Screen-map status
- Total `docs/screens/` files: 2 dirs populated (`ppt/`, `reality/`); `mobile/` unseeded (0 files, drift excluded per routine spec).
- Drift signals this run: 0 (no merged PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**` — mobile RN wiring in #2118 lives under `frontend/apps/mobile/src/screens/**`, which is excluded).
- Orphan screens this run: 0 (no route removals detected on the touched trigger surface).
- Last `screen-map.yml` CI run on `main`: not queried this run (skipped for time).

## Backlog deltas
- **New:** [1] `refactor-churn-hotspot-backend-crates-db-src-repositories-rental-oauth-rs` — churn from PR #2100 split.
- **New:** [1] `refactor-churn-hotspot-backend-crates-db-src-models-multi-currency-rs` — churn from PR #2113 enum-sync guard refactor.
- **New:** [1] `refactor-churn-hotspot-backend-crates-db-src-repositories-rental-bookings-rs` — churn from PR #2100 split.
- **Resolved (done):** `refactor-churn-hotspot-backend-crates-db-src-repositories-rental-rs` — PR #2100 split deleted the original file.
- **Partial:** `code-review-mobile-rn-screens-mock-data` — PR #2118 wired Meters/Leases/Forms/Threads (+ their detail variants) + PersonMonths; Buildings + Notifications still ship `MOCK_*`. Row left `open`, `[partial]` evidence appended.
- **Bumped:** none this run.
- **Decayed:** none — every open row was `updated_at=2026-07-05`, inside the 14-day window.
- **Dropped:** none this run.

## Plans promoted
- **none this run** — 0 open items reached the readiness bar (all 14 open + 2 newly-partially-resolved rows carry `score=1`; the 6 rows at `score>=3` are already `ready` with plans authored).

## Auto-fix
- `no candidate` — the run's signals are all analytical (`churn-hotspot`, `dep-update-noise`, `lag_warning`); none of the Phase 5 allowlist types (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift sub-shape, `stalled-review`) fired with `confidence=high` + backlog `score>=3`.

## Self-review findings
- `.research/self-improvement/findings.json` not present in the repo — none

## Open questions
- Should Buildings + Notifications mobile RN screens spawn their own follow-up row now that Meters/Leases/Forms/Threads were wired in PR #2118? Current handling keeps them under the original `code-review-mobile-rn-screens-mock-data` row with a `[partial]` evidence line; if you want them tracked as a fresh issue instead, say so and the next run will split them out.
- Three of the four `test-gap-hotfix-no-test-pr-129x-*` open rows have been sitting at `score=1` since the 19-day catch-up; consider a batched "add regression coverage for the PAP-16{7,8,9} RLS-migration hotfixes" plan if the team prefers one PR over four.

## Quiet day?
- No — 12 substantive PRs shipped (including one security-adjacent authz fix, a large repo split, and a dispatcher self-fix), plus 3 new churn-hotspot rows and one partial resolution.
