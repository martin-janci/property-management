# 2026-07-07

## Since last run
- Merged PRs: 22 (range #1979–#2130)
- Open PRs touched: 13
- New / updated issues: 25 (all follow-up + 1 new security advisory issue #2141)
- Commits: 75 on `dev` (majority dispatcher research chores; ~20 product PR merges)
- Routine lag: 2d 3h since last run (well over 36h — missed the 2026-07-06 daily slot; not stale, dispatcher was ticking normally in the interval)
- Phases that failed: none. Phase 1.5 (`ppt-dev-review`) and Phase 1.6 (`ppt-project-management`) intentionally skipped this run to keep the routine tractable inside the cloud budget after a 48h gap; they resume on the next incremental run

## Shipped
- #2130 gh-issue-2126: fix MCP-push size guard dead no-op (run before commit) — dispatcher hook now runs the MCP-push size guard *before* the commit lands, not after
- #2120 gh-issue-2107: authorize outage mutations on DB role, not JWT claim — closes the JWT-vs-DB-role mismatch from PR #1979
- #2119 gh-issue-2110: restore executable bit on check-ignore-reason.sh + recurrence gate
- #2118 code-review-mobile-rn-screens-mock-data: wire mobile Meters/Leases/Forms/Threads screens to api-server (1096+/332−) — resolves backlog row of the same id
- #2117 gh-issue-2103: named-field input for SlovakAccountingExport::new — kills same-type positional-arg transposition foot-gun
- #2116 gh-issue-2109: cover include_system=true tie-break in template pagination test
- #2115 gh-issue-2108: ListingDetail auth change resets screen to spinner + hoisted-path tests
- #2114 gh-issue-2102: wire MCP-push size guard + action-list reconcile into dispatcher Phase 6
- #2113 gh-issue-2104: enum-sync guard fails on variant missing from canonical list
- #2112 docs(db): refresh stale BIT-351 quarantine docstrings for un-quarantined unread test
- #2111 gh-issue-2106: gate backend/deny.toml with code-owner review (CODEOWNERS)
- #2100 refactor-churn-hotspot-…-rental-rs: split `rental` repository into cohesive sub-modules — resolves backlog row of the same id
- #2101 triage-dispatcher-mcp-push-large-file-issue-1014: MCP-push fallback state-fidelity analysis (docs)
- #2099 gh-issue-2086: enforce SlovakAccountingExport honesty invariant by construction
- #2098 gh-issue-2083: add enum sync guard for currency/country lists
- #2097 gh-issue-2082: un-quarantine #1771 soft-delete unread invariant
- #2096 gh-issue-2084: enforce quick-xml XXE pin via cargo-deny hard ban (**load-bearing** — quick-xml half-range ban lives in `backend/deny.toml`)
- #2095 gh-issue-2085: extract + unit-cover `assign_fault` recipient guards
- #2094 gh-issue-2087: reuse shared `seed_org` fixture in `favorite_alert_read` idempotency test
- #1979 test(api-server): outages happy-path backfill (Wave 9 batch 3, BIT-414) — first landing of the outages happy-path suite; the JWT-vs-DB-role gap it exposed was fixed the same day by #2120
- #2020 chore(deps): bump `rust_xlsxwriter` 0.95.0 → 0.96.0 (dependabot)
- #2014 chore(deps): bump `axum-test` 20.1.0 → 21.0.0 (dependabot, `axum` group)

## Watch
- Stalled review: **not evaluated this run** — no live `reviewDecision` query was performed inside the tightened cloud budget after the 48h gap. Two draft PRs (#1797 auth on OCR + rental guest PII manager-gate; #1812 `reality_portal` split) remain open ~14d — carry-over from prior brief. Owner still needs to decide merge/close/plan.
- Reverted: 0 in window (no revert-titled PRs, no `Revert` in bodies).
- Churn hotspots (2026-07-05 → 2026-07-07 window, dispatcher-chore commits + mass file-restore commit `dc082671` excluded):
  - `frontend/apps/mobile/src/screens/leases/LeaseDetailScreen.tsx` — 273 lines (runs_seen 0→1; new-code from PR #2118 mobile-screens wire)
  - `frontend/apps/mobile/src/screens/messages/ThreadDetailScreen.tsx` — 215 lines (runs_seen 0→1; new-code from PR #2118)
  - `backend/crates/db/src/models/regional_compliance.rs` — 214 lines (runs_seen 0→1; new file)
  These are new-code additions, not instability. Recorded so a `repeated-churn` fires only if they churn again next run.

## PRs stuck in draft despite approval
<!-- Not queried this run — reviewDecision fetch skipped to keep the routine tractable inside the cloud budget after the 48h gap. Will resume next run. -->
- none surfaced this run

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Not queried this run — planning-branch assignments.json not deep-scanned. Dispatcher is a separate loop and is ticking on schedule; this section resumes next run. -->
- none surfaced this run

## Code review slice
- Segment reviewed: — (Phase 1.5 intentionally skipped this run to protect the 48h-lag catch-up budget)
- Experts: —
- Findings: 0
- Next segment: `ppt-web-core` (oldest-unreviewed after this run — last review 2026-06-05, older than every other segment)
- Skipped: yes — 48h lag; Phase 1.5 resumes on next incremental run

## Screen-map status
- Total `docs/screens/` files: not counted this run (heavy scan skipped)
- Drift signals this run: 0 (per-PR file-list not fetched for drift matching this run; PR #2118 touched `frontend/apps/mobile/src/screens/**` but mobile is intentionally excluded from drift detection until `docs/screens/mobile/` exists — see routine prompt)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run

## Backlog deltas
- **New:** [3] `security-rustsec-2026-0204-crossbeam-epoch` — bump crossbeam-epoch ≥0.9.20 to clear RUSTSEC-2026-0204 (cargo-deny red on `dev`)
- **New:** [1] `refactor-churn-hotspot-frontend-apps-mobile-src-screens-leases-lease-detail-screen-tsx`
- **New:** [1] `refactor-churn-hotspot-frontend-apps-mobile-src-screens-messages-thread-detail-screen-tsx`
- **New:** [1] `refactor-churn-hotspot-backend-crates-db-src-models-regional-compliance-rs`
- **Resolved (done):** `refactor-churn-hotspot-backend-crates-db-src-repositories-rental-rs` ← PR #2100
- **Resolved (done):** `code-review-mobile-rn-screens-mock-data` ← PR #2118
- **Bumped:** none this run
- **Decayed:** 0 (no open item older than 14 days — most-recent open bulk was written 2026-07-05)
- **Dropped:** 0

## Plans promoted
- `plans/security-rustsec-2026-0204-crossbeam-epoch.md` — lockfile-only bump (`cargo update -p crossbeam-epoch --precise 0.9.20`) to clear RUSTSEC-2026-0204; verifies with `cargo deny check advisories` · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met the certainty bar + allowlist this run. The RUSTSEC advisory is high-confidence and score=3 but its action shape (`cargo update -p` on the lockfile) does **not** match the `dep-update-noise` lockfile-drift allowlist entry (which is *consumer-manifest caret bump after a merged dependabot PR*, not a transitive advisory bump). Routed to the manual implementer path via the plan above.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present in this repo layout)

## Open questions
- **Draft-PR carry-over** — #1797 (OCR auth + rental guest PII), #1812 (`reality_portal` split): both draft, both ~14d idle. The prior brief already flagged them; still no owner decision. Merge, close, or convert to plan?
- **`cargo-deny` required-status escalation** — issue #2141 notes cargo-deny is not currently a required check on `dev`, so a red advisory doesn't block merges. Once the crossbeam-epoch bump lands, worth wiring `cargo-deny` in as required so the next advisory can't hide.

## Quiet day?
- No — 22 PRs merged since last run, 2 backlog rows resolved, 4 new backlog rows (1 security promoted to plan), 1 plan promoted.
