# 2026-06-28

## Since last run
- Merged PRs: **96** (range #1567–#1898) — the routine missed 12 days; backlog dominated by the dispatcher's churn-fix, RLS-migration, and Epic-66/72 endpoint sweeps (martin-janci 264 PRs in the wider closed-list, dependabot 12).
- Open PRs touched: not tallied this run (cloud routine lag-recovery; deferred to next run).
- New / updated issues: **50+** in the past two weeks — 47 `follow-up`/`from-merged-review` rows from the dispatcher's post-merge-review pipeline (#1758–#1793, #1826–#1828, #1851–#1854, #1904–#1906), plus dispatcher infra rows (#1680 cron-broken, #1739 systemic blockers, #1747 phantom backlog defects).
- Commits on `dev`: **440** since `4829015b` → `feaedceeb`.
- Routine lag: **12d ~0h since last run — `lag_warning` + `stale_routine_alert` both fired** (logged in `signals/2026-06-28.json`, scored 0). Cloud routine cron is the suspected cause (the dispatcher loop on the planning branch kept ticking).
- Phases that failed: **none**, but Phase 1 was reduced-scope: per-PR file-list fetches across 96 PRs were not attempted (token budget). `screen-map-drift` per-PR matching deferred — `screen-map.yml` CI on `main` covers the same trigger set in the meantime.

## Shipped
- **#1713** `revert(delegations) BIT-213` — reverts #1690 (delegation frontend re-wired ~16 min before CEO ruled delegation stays retired per PAP-33/51/55). Backend delegation surface intentionally untouched (still wired into voting `delegation_id` re-validation). New backlog row `revert-pr-1713` (score 3).
- **#1859** `fix(api-server): implement 23 tenant-migration endpoints (BIT-260)` — Epic 66 stories 66.1-66.4 (templates, import jobs, exports), gated by `require_platform_admin`. Phase 1.5 review uncovered two issues in this PR: list-handler in-memory pagination (`list_templates` fetches all then slices in Rust) and an unsigned `Uuid::new_v4()` token in `download_template`'s URL with no `/file` route to consume it — both new backlog rows below.
- **#1861** `fix(api-server): implement 19 regional-compliance endpoints (Epic 72)` — Slovak/Czech compliance endpoints. Phase 1.5 caught hardcoded stub data in `get_slovak_vote_minutes` / `get_czech_usneseni` masquerading as legally-binding compliance documents. Promoted to plan.
- **#1849 / #1847 / #1853 / #1848 / #1850** — Epic 16 messaging + saved-search alert work (BIT-139 / 140 / 244 / 206; org-scoped favorite alert worker).
- **#1755 / #1757** — duplicate SQLx migration 192 collision resolved + `dup-version` lint guard; unblocked all backend PRs after the 192/192 collision broke `cargo sqlx prepare`.
- **#1741** `fix(api-server): require manager role to list Airbnb reservations` — closes #1667 (guest-PII gap surfaced in earlier dev-review).
- **#1737** `fix(iot): remove duplicate JWT-trusting sensor WS channel` — closes #1668. 245-line deletion of the bypassing channel; converged on the DB-checked handler.
- **#1768 / #1756** — messaging API camelCase wire-contract (BIT-238 / 239) for thread/message/participant + attachment endpoints; React Native compatibility follow-up filed in #1780.
- **#1750** `feat(epic-18): guest ID-document upload + OCR seam (Story 18.2, #1687)` — multipart upload, OCR seam, migration `00192_rental_guest_id_documents.sql`.
- **#1809** `fix(accounting): PAP-321 F1/F2 — 404-not-500 + skip-serializing provider secrets` — small, contained.
- **#1683 / #1796 / #1798 / #1800 / #1810 / #1816 / #1719 / #1718** — eight dispatcher-driven `churn-hotspot-*` resolutions (document.rs split, subscription.rs split, emergency.rs, enhanced_tenant_screening.rs, sensor.rs, reserve_funds.rs, form_rls test split, mobile icon-test split).
- **#1684** `fix(api-core): osrng-expect propagate CSPRNG failure` — closes prior Phase 1.5 finding (status flipped to done by ID-match).
- **#1681** `fix(mobile): wire mobile Building list to real API (was MOCK_*)` — resolves Phase 1.5 finding `code-review-mobile-rn-screens-mock-data`.

## Watch
- **🚨 Live `revert` in window — `revert-pr-1713`**: dispatcher gap-sweep re-admitted retired surface; Atlas explicitly flagged "a gap-sweep does not silently overturn a board decision." Backlog row open (score 3) but **not promoted** — the actionable artifact is the audit rule for the dispatcher, not a code plan; defer to the dispatcher's `gap-scan v3` follow-up (issue #1680 cluster).
- **Reverts:** 1 (PR #1713 above) — score-bumped to 3, vector `bug`, status `open`. No clean test plan today; left in backlog for human-judgement framing.
- **Churn hotspots this run (top 3, 12-day window after exclusions):**
  1. `backend/crates/db/src/repositories/document.rs` — **2940 lines** (re-emit; in `seen_signals`; runs_seen 1→2; resolved upstream by PR #1683 module-split this run).
  2. `backend/servers/api-server/src/routes/aml_dsa.rs` — **2691 lines** (re-emit; in `seen_signals`; runs_seen 1→2; resolved upstream by PR #1708 module-split).
  3. `backend/servers/api-server/src/routes/forms.rs` — **2043 lines** (re-emit; runs_seen 2→3, `repeated-churn` also re-emitted Δ=0; resolved by PR #1700 module-split).
- **Stale drafts / stalled review:** not re-fetched this run (lag-recovery scope reduction). Carry-over `dx-stalled-review-pr-988` decayed to 0 and dropped.
- **Closed-not-merged in window:** 4 (#1720, #1727, #1749, #1858) — not individually scored this run (lag-recovery).

## PRs stuck in draft despite approval
- none (not queried this run — lag-recovery; will catch up next run)

## PRs with verdict=changes, no fix-round progress in 24h
- none (not queried this run — lag-recovery; will catch up next run)

## Code review slice
- Segment reviewed: **api-handlers** (reason: **churn-aligned** — top-3 churn this run is `aml_dsa.rs`, `forms.rs`, plus 4th `emergency.rs` + 6th `migration.rs`, all inside `backend/servers/api-server/src/routes/`).
- Experts: Rust.
- Findings: **3** — `code-review-api-handlers-regional-compliance-stub-minutes` (high, +3 → promoted), `code-review-api-handlers-migration-template-pagination` (medium, +2), `code-review-api-handlers-migration-fake-download-token` (medium, +2 → promoted via security fast-track).
- Next segment: **api-core** (last 2026-06-15) → tied with **reality-server** (2026-06-11) / **ppt-web-ui** (2026-06-11); oldest-unreviewed picker will choose reality-server or ppt-web-ui next run.
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0 — not yet seeded).
- Drift signals this run: **0** (per-PR drift scan deferred this run — see Phases-failed note. `screen-map.yml` CI on `main` is authoritative in the meantime).
- Orphan screens this run: **0** (deferred).
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New:**
  - [3] `revert-pr-1713` — `revert(delegations)` PR #1713 reverts #1690 (board-decision compliance, BIT-213).
  - [3] `code-review-api-handlers-regional-compliance-stub-minutes` — Slovak/Czech vote-minutes handlers return hardcoded stubs as legally-binding compliance docs (Phase 1.5; promoted).
  - [2] `code-review-api-handlers-migration-template-pagination` — `list_templates` fetches all then slices in Rust; `page/per_page` never reach DB (Phase 1.5).
  - [2] `code-review-api-handlers-migration-fake-download-token` — `download_template` emits unsigned `Uuid::new_v4()` token with no `/file` route to validate (Phase 1.5; promoted via security fast-track).
- **Bumped:** none (the 96 merged PRs primarily resolved existing rows; the 3 Phase 1.5 findings and 1 revert opened new rows rather than stacking).
- **Decayed (11):** `code-review-mobile-native-kmp-deeplink-token-not-url-decoded` (2→1), `code-review-mobile-native-kmp-search-stale-response-race` (2→1), `test-gap-screen-map-drift-pr-1085-reality` (2→1), `test-gap-screen-map-drift-pr-1100-ppt` (2→1), `bug-risky-churn-pr-963-api-main-rs` (2→1), `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` (2→1), `bug-ios-deeplink-info-plist-missing` (2→1), `test-gap-hotfix-no-test-pr-1288-webhook-rls` (2→1), `test-gap-hotfix-no-test-pr-1287-rls-llm-sessions` (2→1), `test-gap-hotfix-no-test-pr-1289-api-ecosystem` (2→1), `test-gap-hotfix-no-test-pr-1292-mfa-rls` (2→1) — all 12-day-stale, no fresh signals.
- **Dropped (15):** all decayed 1→0 — `churn-hotspot-{vendors,api_ecosystem,iot,reality_portal,reserve_funds_cross_org_idor,form_rls_repo_tests}`, `triage-issue-{1151,951,1331}`, `triage-dispatcher-mcp-push-large-file-issue-1014`, `closed-not-merged-pr-1274`, `dx-stalled-review-pr-988`, `code-review-mobile-native-kmp-deeplink-android-bypasses-shared-router`, `refactor-churn-hotspot-mobile-documents`, `churn-hotspot-mobile-native-{listing-detail-kt,search-screen-kt}` (counted via grouped name).
- **Resolved (16, by PR-body/title ID-match):** `churn-hotspot-{emergency,enhanced_tenant_screening,document.rs(crates/db),subscription.rs,aml-dsa,booking.rs,reserve-funds,sensor.rs,form-rls-repo-tests,mobile-icon-test-ts}`, `repeated-churn-...forms-rs`, `refactor-churn-hotspot-mobile-{announcements,announcements-test}`, `code-review-api-core-osrng-expect`, `screen-map-drift-pr-1418-ppt`, `code-review-mobile-rn-screens-mock-data`.

## Plans promoted
- `plans/code-review-api-handlers-regional-compliance-stub-minutes.md` — wire `get_slovak_vote_minutes` + `get_czech_usneseni` to real DB participant/tally/meeting data (or 501 + feature flag) so the Slovak/Czech compliance documents the API mints are not fabricated. Mode: cloud-ok (backend-only). · adversarial pass: passed.
- `plans/code-review-api-handlers-migration-fake-download-token.md` — replace `Uuid::new_v4()` in `download_template`'s URL with a server-side `template_download_grants` table (or inline-stream), close the latent bulk-export-without-auth surface before a `/file` route lands. Mode: cloud-ok. · adversarial pass: passed.

## Auto-fix
- `no candidate` — none of this run's signals match the Phase 5 allowlist:
  - `revert-pr-1713` — score 3 + high confidence, but `revert` is not on the allowlist (no safe-shape mechanical action exists for a board-decision revert; this is human-judgement territory).
  - 3 `code-review-finding` signals — `code-review-finding` is not on the allowlist either (the allowlist covers `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review`).
  - `screen-map-drift` per-PR scan deferred this run; no candidate to act on.
  - `stalled-review` not re-queried this run; no candidate.

## Self-review findings
- `.research/self-improvement/findings.json` absent — none

## Open questions
- **Cloud routine lag** — `state.json` `last_run_iso` was 12 days stale. Most likely cause: cloud cron paused, queue stalled, or the daily trigger was disabled. Worth a quick check of the routine's GitHub Action / cloud trigger and the `claude.ai` schedule. This run advances state but does NOT diagnose the cron itself.
- **Dispatcher health** — issues #1680 (cron environment can't run core pipeline), #1739 (systemic blockers, branch-truncation), #1747 (phantom backlog + silent throughput) all `OPEN` / `CLOSED` cluster on 2026-06-23. The dispatcher kept ticking on the planning branch but a few defects are noted. Worth a pass to confirm the dispatcher and the daily routine are not racing the same files.
- **96 merged PRs in 12d** — well above sustainable cadence for the review path. Worth a Scrum-Master / pm-tech-lead retro on whether the dispatcher's pace + post-merge review pipeline is right-sized.
- **`revert-pr-1713`** — promotion not attempted (no clean test plan; the actionable rule is a dispatcher gap-sweep policy, not code). Leave open; the dispatcher refresh on `planning` should address this.

## Quiet day?
- **No.** 96 merged PRs, 16 resolutions, 4 new vectors, 2 plans promoted (both passed adversarial), 3 Phase 1.5 findings, pm-security Phase 1.6 (32-day gap closed; report at `.research/management/roles/pm-security-2026-06-28.md`). `stats.runs` 40 → 41, `vectors_created` 173 → 177, `plans_created` 12 → 14.

## Goal violations
- Will be filled by the post-stage `goal_checks[]` pass; the brief is regenerated with this section only if checks fail.
