# 2026-08-03

## Since last run
- Merged PRs: 1 (#2629 — follow-up wiring for issue #2628, closed on merge)
- Open PRs touched: 0 (nothing updated since 2026-08-02T14:23Z — 14 open PRs, oldest 3d idle)
- New / updated issues: 0 (last_issue_seen stays at #2628)
- Commits: 15 on `dev` since cursor `21a036ee` (1 code merge above + 14 dispatcher chore commits)
- Routine lag: 12h since last run — on schedule
- Phases that failed: none

## Shipped
- #2629 gh-issue-2628 (OAuth token-usage analytics wire-up) — merged 20:07Z. Adds the producer inside `OAuthService` (best-effort issued/refreshed/revoked recording on the token lifecycle), a `GET /api/v1/platform-admin/oauth/token-usage` read route (super-admin + AuditRead), and a daily scheduler prune (90-day default, `OAUTH_TOKEN_EVENTS_RETENTION_DAYS`). Regression test drives the real Axum router through issue→refresh→revoke and asserts the three analytics rows; 4 new unit tests pin the daily-cadence gate. Closes the "data layer without wiring" gap PR #2526 left behind.

## Watch
- Stalled review: none crossed the 7-day threshold yet — oldest opens #2555/#2558/#2559 (accounting-MVP trio) sit at 3d idle, same as prior run.
- Reverted: none this run.
- Churn hotspots: none — the only code merge touched 8 files across `backend/servers/api-server/**` (all under one PR), well below hotspot rank thresholds. `hotspot_history` unchanged.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: `ppt-web-ui` + `reality-web` (both by the dispatcher's tier1d generator earlier today, at 02:14Z and 00:15Z respectively)
- Experts: frontend + completeness (ppt-web-ui); frontend (reality-web)
- Findings: 5 (all folded into today's `signals` array; see Backlog deltas below)
- Next segment: `mobile-rn` (oldest unreviewed at 2026-07-22 — 12d)
- Skipped: yes — routine's own Phase 1.5 invocation skipped because the dispatcher already consumed the effective "one review per day per segment" slot for both segments this UTC day. `review_cursor.ppt-web-ui` and `review_cursor.reality-web` both advanced to 2026-08-03 to reflect the reviews.

## Screen-map status
- Total `docs/screens/` files: 153 (ppt=106, reality=47, mobile=0 — not yet seeded)
- Drift signals this run: 0 (PR #2629 touched only `backend/servers/api-server/**` — no frontend routes)
- Orphan screens this run: 0 (no route files touched)
- Last `screen-map.yml` CI run on `main`: not checked this run (no drift trigger)

## Backlog deltas
- **New:**
  - [2] `code-review-ppt-web-ui-accounting-invoice-silent-fail` — AccountingInvoiceManagementPage: create/delete mutations onSuccess-only → silent financial write failures.
  - [1] `code-review-ppt-web-ui-aria-label-i18n` — 103 hardcoded English `aria-label`s across ppt-web features.
  - [1] `code-review-ppt-web-ui-sessions-double-cast` — SessionsPage `as unknown as SessionRow[]` boundary cast defeats compile-time type-drift catch.
  - [1] `code-review-reality-web-agency-import-i18n` — agency/import feature cluster hardcoded English on multi-locale portal.
  - [1] `code-review-reality-web-protectedroute-i18n` — ProtectedRoute renders untranslated auth-required gate for sk/cs/de.
- **Bumped:** none
- **Decayed:** none (no open items older than 14 days — backlog was fully terminal at start of run)
- **Resolved (to done):** none (gh-issue-2628 is dispatcher-tracked in `.research/management/assignments.json`, not this backlog)
- **Dropped:** none

## Plans promoted
- none — the sole scored-2 candidate (`code-review-ppt-web-ui-accounting-invoice-silent-fail`) is a `bug` vector, not eligible for the security fast-track (score ≥ 2), so it sits at open awaiting a second signal to reach the +3 promotion floor. The four other new items score 1 each and are equally below the threshold.

## Auto-fix
- `no candidate` — no signal met confidence=high + score>=3 + allowlist this run. The 5 code-review-finding signals are `medium` confidence and route to the manual implementer path by design.

## Self-review findings
- open=28 (high=6, medium=19, low=3); new this run=0; acknowledged=19
- fp-null [recurrence=13] — assignments-archive.json too large (198KB) for MCP push_files inline; fix: use PR+API-merge path as primary push mechanism
- fp-null [recurrence=10] — dispatcher hard-assumes proxy-403 for git push; env actually permits it if commit email is @users.noreply.github.com; fix: runtime push-capability probe at Phase 1
- fp-null [recurrence=4] — action-list.json in-progress twin diverges from assignments.json live rows; fix: Phase-2 reconcile step to reset action-list on missing assignment
- fp-null [recurrence=3] — in-progress rows produce ZERO branches on origin across two implementer generations; fix: escalate branchless-after-reclaim to status=failed
- fp-null [recurrence=3] — Phase 2.6 claimable metric ignores archive-terminal ids; fix: extend gc1-reconcile to close open items terminal-in-archive for any status incl. failed
- … and 1 more (see findings.json)

## Open questions
- **Refill trickle.** Dispatcher tier1d generator has taken over the daily rotating-review slot on quiet days — running on segments the routine's own Phase 1.5 would have picked. The routine's response has been to fold those signals into its own artifact and advance the same cursor. This works, but it means the two loops are now writing to overlapping stores (`state.review_cursor` on `dev`, dispatcher's tier1d artifacts) and the routine's own Phase 1.5 hasn't run first-hand in several days. Worth noting for observability, but no action to take.
- Phase 1.6 skipped: `pm_cursor.next_index` stays at 2 (pm-frontend last ran 2026-06-10, 54 days ago). Delivery flux since last run is a single well-tested PR — invoking the full role rotation for one merge would be overkill. The pm-frontend role's staleness is a signal in its own right; if it stretches past 60 days consider forcing it via the `pm:frontend` trigger.

## Quiet day?
- No — 5 new backlog vectors added (all code-review-findings from the segments the dispatcher's tier1d ran today); routine still commits with vectors_created bumped by 5.
