# 2026-08-03

> **Same-day rerun.** Triggered by `buffer-low: claimable=0/72 — backlog+coverage+retry all exhausted; refill planner`. This brief supersedes the 03:09Z version — the earlier 5 backlog vectors all shipped in the meantime; refill via 10 folded tier1d findings + 1 drift signal.

## Since last run
- Merged PRs: 6 (range #2631–#2636) — all shipped in a single 9h burst 07:10Z–12:13Z; every previously-open backlog vector cleared.
- Open PRs touched: 0 since 03:09Z cursor (14 open, oldest #2582–#2591 dependabot at 4d idle; #2555/#2558/#2559 accounting-MVP trio at 4d idle)
- New / updated issues: 1 (#2630 — OAuth token-usage HTTP-test coverage; created + closed same-day, resolved by #2632)
- Commits: 20 on `dev` since cursor `a5fbe4f7` (6 code merges + 14 dispatcher chore commits — one `GC3-buffer-bounds=FAIL` recorded on every dispatcher tick since 04:12Z)
- Routine lag: 11h 7m since last run — on schedule
- Phases that failed: none

## Shipped
- #2631 code-review-ppt-web-ui-accounting-invoice-silent-fail — added onError→parseApiError→showToast on invoice create/delete mutations + regression test asserting the toast fires. Closes the sole ppt-web-ui write path shipping silent failures.
- #2632 gh-issue-2630 — router-driven HTTP coverage for `GET /platform-admin/oauth/token-usage`: 200/camelCase envelope, 403 on both authz walls, 400 INVALID_SINCE, valid `?since=` narrowing. Closes the post-merge follow-up gap that #2629 left.
- #2633 code-review-reality-web-protectedroute-i18n — reality-web ProtectedRoute auth-required gate now uses `next-intl` under `auth.protected.*`, populated in all six locale bundles (en/sk/cs/de/pl/hu).
- #2634 code-review-ppt-web-ui-sessions-double-cast — SessionsPage now typed against `@ppt/api-client` `SessionInfo` instead of `as unknown as SessionRow[]`. Removes the local drift-hazard interface.
- #2635 code-review-ppt-web-ui-aria-label-i18n — 104 hardcoded aria-label strings across 51 ppt-web feature components moved into a shared `aria.*` i18n namespace; six-locale parity test locks the invariant. Larger than scope by design (all six locales, not the 4 named in the task) to avoid re-introducing the bug per locale.
- #2636 code-review-reality-web-agency-import-i18n — reality-web agency/import feature cluster (imports, agency listings/branding, inquiries page) converted to `next-intl useTranslations()` under new `agencyListings`/`agencyBranding`/`agencyInquiries`/`import` namespaces + flat `agency` additions, populated in all six locales.

## Watch
- Stalled review: none crossed the 7-day threshold. Oldest opens still #2555/#2558/#2559 (accounting-MVP trio) at 4d idle.
- Reverted: none.
- Churn hotspots: none scored — the top-3 raw slots this window (14 days) are the six `reality-web/messages/*.json` locale bundles + `oauth_integration_tests.rs` at 3 hits each. Locale bundles are i18n data (co-edited with feature work; not code instability) and the test file grew with #2632 by design. `hotspot_history` unchanged.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **`ppt-web-core` + `mobile-rn`** (both by the dispatcher's tier1d generator this UTC day at 06:16Z and 10:16Z — 5 + 5 findings across two ppt-web-core artifacts + one mobile-rn artifact)
- Experts: frontend (both segments)
- Findings: 10 net-new (all folded into today's `signals[]` and `backlog.json`; see *Backlog deltas*). Prior rerun already folded 5 from ppt-web-ui + reality-web at 03:09Z.
- Next segment: `mobile-native-kmp` (last routine touch 2026-07-27, 7d) or `api-handlers` (2026-07-29, 5d) — pick when routine's own Phase 1.5 runs first-hand next.
- Skipped: yes (routine self-invocation) — dispatcher-run tier1d artifacts already cover the segments the routine would have picked today. `review_cursor.ppt-web-core` and `review_cursor.mobile-rn` advanced to 2026-08-03 to reflect the completed reviews.

## Screen-map status
- Total `docs/screens/` files: 153 (ppt=106, reality=47, mobile=0 — not yet seeded)
- Drift signals this run: 1 (`screen-map-drift-pr-2636-reality` — PR #2636 touched `agency/import/page.tsx` + `agency/inquiries/page.tsx` without touching `docs/screens/reality/agency-*.md`. **Heuristic caveat**: change was i18n-only, no user-visible copy delta in English; screen docs likely need only an Agent Log note, not content changes. Treated as review candidate, not promotable.)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not checked this run (no drift trigger on `main`)

## Backlog deltas
- **New:**
  - [2] `code-review-ppt-web-core-logout-cache-purge-gap` — logout leaves 3 tenant-scoped query roots (predictive-maintenance/sentiment/notification-analytics) NOT in `AUTHED_QUERY_KEY_ROOTS`; shared-workstation user B sees user A's cache until 5m staleTime lapses.
  - [2] `code-review-ppt-web-core-route-error-boundary-gap` — 90 lazy route chunks render inside `<Suspense>` with no per-outlet `ErrorBoundary`; post-redeploy stale-index.html crashes app tree to blank screen instead of prompting reload.
  - [2] `code-review-ppt-web-core-mutation-no-onerror` — 6 useMutation hooks in rentals + financial wrappers omit onError (checkIn/checkOut/createConnection/syncPlatforms + payment matchMutation/autoMatchMutation). Same shape #2631 just fixed for invoice page.
  - [2] `code-review-ppt-web-core-rentals-auth-nonnull-mutation` — rentals mutations non-null-assert `useRentalsAuth()`; mid-session token loss → uncaught `TypeError` → blank screen instead of re-auth prompt.
  - [2] `code-review-mobile-rn-offline-sync-false-complete` — `useOfflineSupport.processQueue` reports `isComplete:true` after head-of-line-blocked (halted) sync cycle; sync-status indicators lie during a stuck queue.
  - [2] `code-review-mobile-rn-thread-send-silent-fail` — `ThreadDetailScreen.handleSend` mutation has onSuccess only; failed sends silent, input already cleared.
  - [2] `code-review-mobile-rn-offline-terminal-drop-dataloss` — transient (5xx/network) queued offline actions permanently DROPPED after 3 retries with only console.error; silent loss of offline-created content (fault reports, thread messages, community posts).
  - [2] `screen-map-drift-pr-2636-reality` — mechanical drift signal for PR #2636 (i18n-only route-page changes without screen-doc updates); low-value review candidate.
  - [1] `code-review-ppt-web-core-notfound-i18n-gap` — 8 route-wrapper 'entity not found' fallbacks hardcode English JSX; sk/cs/de users see untranslated errors on missing entities.
  - [1] `code-review-mobile-rn-hardcoded-empty-error-i18n` — ~18 mobile-rn screens hardcode English empty/error/loading state text despite react-i18next being wired.
  - [1] `code-review-mobile-rn-push-debug-console-log` — `usePushNotifications` has `console.log('[push] registering ...')` on the prod register/unregister path; cleanup item.
- **Bumped:** none
- **Decayed:** none (no open items older than 14 days — full backlog was terminal at start of rerun; new items are all created today)
- **Resolved (to done):**
  - `code-review-ppt-web-ui-aria-label-i18n` — PR #2635 title-match + evidence-gone (grep for hardcoded aria-label under `frontend/apps/ppt-web/src/features` → 0 hits).
- **Dropped:** none

## Plans promoted
- none — no open item reaches the score>=3 promotion floor. All 11 new items are new signals at their initial delta (10 code-review-findings at +1/+2 and 1 drift at +2, all `medium` confidence). Security fast-track (score≥2 + `high` confidence + `security` vector) doesn't apply — none are `security`. Refill was via *count*, not *scored-through-promotion*: the dispatcher's implementer path can pick these up as `code-review-finding` action items directly without a routine plan file.

## Auto-fix
- `no candidate` — all new/updated signals this run are `medium` confidence (either code-review analytical findings or the drift heuristic); allowlist requires `high`. `ROUTINE_AUTOFIX_DISABLED` not set. `state.auto_fix_history` unchanged.

## Self-review findings
- open=28 (high=6, medium=19, low=3); new this run=0; acknowledged=19
- fp-null [recurrence=13] — assignments-archive.json too large (198KB → now 512KB) for MCP push_files inline; fix: use PR+API-merge path as primary push mechanism
- fp-null [recurrence=10] — dispatcher hard-assumes proxy-403 for git push; env actually permits it if commit email is @users.noreply.github.com; fix: runtime push-capability probe at Phase 1
- fp-null [recurrence=4] — action-list.json in-progress twin diverges from assignments.json live rows; fix: Phase-2 reconcile step to reset action-list on missing assignment
- fp-null [recurrence=3] — in-progress rows produce ZERO branches on origin across two implementer generations; fix: escalate branchless-after-reclaim to status=failed
- fp-null [recurrence=3] — Phase 2.6 claimable metric ignores archive-terminal ids; fix: extend gc1-reconcile to close open items terminal-in-archive for any status incl. failed
- … and 1 more (see findings.json)

## Open questions
- **Buffer starvation is real, not a metrics artifact.** The dispatcher trigger payload (`claimable=0/72`) matches the routine's own picture: `assignments.json = {}`, `action-list.json.items = []`, backlog top score = 2 (below promotion floor). The refill this run adds 11 items (10 signals + 1 drift) but none crosses score≥3. Options for restoring throughput:
  - Add a router-side rule: `code-review-finding` signals at score 2 are directly claimable by the dispatcher (skip the +3 promotion floor for this vector, since they arrive already file-anchored). This would immediately mark the 7 new score-2 findings as claimable next dispatcher tick.
  - Or lower the routine's promotion threshold for `bug` vectors originating from an expert-review artifact to 2 (mirroring the security fast-track shape); would generate up to 2 plans this run, filling the pipeline for one implementer cycle.
  - Or invoke Phase 1.5 first-hand on `mobile-native-kmp` (7d stale) or `api-handlers` (5d stale) to generate more high-quality signals in previously-unchecked segments. Deferred to the next daily run to keep this rerun tight.
- Phase 1.6 rerun: not invoked. `pm_cursor.next_index` stays at 2 (pm-frontend last ran 2026-06-10, 54d ago — this is now uncomfortable). Delivery flux since last run was substantial (6 merges in 9h) but the dispatcher's daily synthesis at 14:09Z is fresh (`management/` files re-generated). If pm-frontend hasn't run by 2026-08-06 (60d cutoff), force `pm:frontend` on the next trigger.

## Quiet day?
- No — 6 PRs merged, 11 new backlog vectors added (10 folded code-review-findings + 1 drift), 1 item resolved to done. `stats.vectors_created` +11, `stats.runs` +1.
