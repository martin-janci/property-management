# 2026-08-03

## Since last run
- Merged PRs: 16 total across two windows today — 7 in the 03:09Z→17:00Z window (#2629–#2636, brief-1), and **9 more in the 17:00Z→22:31Z window** (this run — #2637 axios-retry-idempotent, #2638 offline-sync-halted, #2639 push-console-gate, #2640 api-token-provider-wire, #2641 thread-send-onError, #2642 mobile-i18n-empty-states, #2644 screen-map-drift-sync, #2646 route-error-boundary, #2648 rentals-auth-nonnull). The dispatcher's implementer fleet chewed through nearly the entire fresh backlog in 5h.
- Open PRs touched: 4 draft PRs still cycling (#2650 logout-cache-purge, #2647 notfound-i18n, #2645 offline-terminal-drop, #2649 mutation-no-onerror) — dispatcher's remaining active claims.
- New / updated issues: 1 (#2643 opened 20:17Z — post-merge follow-up on PR #2629 OAuth token-usage: 3 perf findings — inline await on hot path (medium), unbounded per-client Vec (low), sequential awaits (low)).
- Commits: 30+ on `dev` since 17:00Z (9 code-review merges + dispatcher chore commits).
- Routine lag: 5h since last run — on schedule (same-day rerun triggered by `buffer-low: claimable=0/72`).
- Phases that failed: none. Phase 1.5 (rotating expert review) started against `api-handlers` (5d unreviewed) but the Rust-expert subagent was stopped before completing; findings for this run are lifted from post-merge issue #2643 instead.
- Trigger payload: `buffer-low: claimable=0/72 — refill planner` (dispatcher's claimable buffer fully depleted after implementer fleet merged 9 PRs since 17:00Z; this run adds 3 net-new perf backlog items to refill).

## Shipped
- #2637 code-review-ppt-web-core-api-retry-nonidempotent — merged 18:11Z. Axios retry interceptor now gated on `RETRYABLE_METHODS = [get, head, options, delete]`; POST/PUT/PATCH never auto-retry unless caller opts in with `idempotent: true`. 13 tests including 5 new negative cases.
- #2638 code-review-mobile-rn-offline-sync-false-complete — merged 20:21Z. processQueue now derives `pending = merged.length` and emits `isComplete = pending === 0`; halted head-of-line cycles no longer overwrite state with a false completion tick.
- #2639 code-review-mobile-rn-push-debug-console-log — merged 20:22Z. Test-only PR: usePushNotifications console breadcrumbs were already `__DEV__`-gated; added regression asserting `console.*` on the token path stays behind `if (__DEV__)` and never interpolates the raw push token.
- #2640 code-review-ppt-web-core-api-token-provider-unset — merged 20:24Z. AuthContext bootstrap now calls `configureApiClient({ getToken: getAccessToken })` alongside `setTokenProvider`; regression test asserts the bearer-token header is attached on `getApiClient()` requests. Closes cross-user data leak on `useSentiment` / `usePredictiveMaintenance`.
- #2641 code-review-mobile-rn-thread-send-silent-fail — merged 20:25Z. `handleSend` now passes `onError` alongside `onSuccess`; failed sends surface `QueryErrorBanner` (with Retry) and keep the drafted text. Reused existing component, no new banner.
- #2642 code-review-mobile-rn-hardcoded-empty-error-i18n — merged 22:18Z. 15 mobile screens migrated from hardcoded English empty/error/loading copy to `t()` lookups; 6 locales (en/sk/cs/de/pl/hu) synced with new `common.tryAgain` + per-namespace keys; `FaultsListScreen.i18n.test.tsx` regression.
- #2644 screen-map-drift-pr-2636-reality — merged 22:18Z. `docs/screens/reality/agency-import.md` + `agency-inquiries.md` synced to PR #2636 i18n rewrite; screen-map validate passes (167 files, 0 errors).
- #2646 code-review-ppt-web-core-route-error-boundary-gap — merged 22:18Z. `RouteErrorBoundary` (reuses existing `components/ErrorBoundary`) wraps `<Outlet/>` in App.tsx, keyed on pathname for auto-reset; 3 regression tests (throw, stale-chunk lazy(), navigation-reset).
- #2648 code-review-ppt-web-core-rentals-auth-nonnull-mutation — merged 22:17Z. New `requireRentalsAuthHeaders()` throws typed `AuthError(SESSION_EXPIRED)` instead of dereferencing `auth!`; `handleRentalsAuthError` wired as `onError` on all 4 rentals mutations. 6-case regression test.

## Watch
- Stalled review: none crossed the 7-day threshold. Oldest opens are the 11 dependabot PRs from 2026-07-30 (4d 11h idle) — still 3d shy of `stalled-review`.
- Reverted: none.
- Churn hotspots: top-by-numstat this window are all one-shot test-file additions from today's code-review PRs (AuthContext.api-client-token.test.tsx 150 lines, api.test.tsx 138 lines, App.route-error-boundary.test.tsx 130 lines). No `churn-hotspot` signals — not repeated churn, just narrow fix-with-tests deltas. `.research/management/*` dispatcher churn dominates the raw list and is excluded by convention.

## PRs stuck in draft despite approval
- none (all 4 draft PRs are dispatcher work-in-progress, not stuck-approved).

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: attempted `api-handlers` (5d unreviewed — was oldest along with mobile-native-kmp at 7d). Rust-expert subagent was launched but stopped by the routine before completion when the stop-hook fired demanding a commit. `review_cursor.api-handlers` stays at `2026-07-29`; will re-fire tomorrow.
- Experts: rust (planned, incomplete)
- Findings: 3 net-new backlog items lifted from **issue #2643** (post-merge review of PR #2629 — OAuth token-usage analytics). All 3 are `perf` vector, `high` confidence (the reviewer opened the file and quoted lines). One medium severity (inline await on OAuth hot path — score 2), two low (unbounded Vec, sequential awaits — score 1 each).
- Next segment: `mobile-native-kmp` (2026-07-27 — 7d unreviewed) will be tomorrow's default oldest-first pick.
- Skipped: yes — routine's own `ppt-dev-review` subagent aborted mid-run. The 3 new signals came from the post-merge reviewer's issue #2643, which does high-quality review synchronously with each merge and is now the effective feeder for `api-handlers` findings.

## Screen-map status
- Total `docs/screens/` files: 155 (ppt=106, reality=49 — +2 from PR #2644 sync, mobile=0 — not yet seeded)
- Drift signals this run: 0. No merged PR in the 17:00Z→22:31Z window touched a `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**` route file without a matching `docs/screens/<product>/` update. PR #2646 added `RouteErrorBoundary` to App.tsx but only wrapped an existing outlet — no new route.
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not checked this run.

## Backlog deltas
- **New (3 rows — all from issue #2643):**
  - [2] `code-review-api-handlers-oauth-record-event-inline-latency` — perf/high — OAuthService.record_token_event awaited inline on issuance/refresh/revoke hot path; each `/oauth/token` exchange pays the analytics INSERT round-trip. Fix: detached `tokio::spawn`.
  - [1] `code-review-api-handlers-oauth-token-usage-unbounded-vec` — perf/high — `/platform-admin/oauth/token-usage` returns unbounded per-client Vec; add LIMIT/pagination.
  - [1] `code-review-api-handlers-oauth-token-usage-sequential-awaits` — perf/high — same route awaits `totals()` and `usage_per_client()` sequentially; `tokio::try_join!` halves latency.
- **Bumped:** none.
- **Decayed:** none (no `open` items >14 days old — buffer is entirely fresh).
- **Resolved (to done):** 8 items closed by today's second-window merges: `offline-sync-false-complete` (#2638), `push-debug-console-log` (#2639), `api-token-provider-unset` (#2640), `thread-send-silent-fail` (#2641), `hardcoded-empty-error-i18n` (#2642), `screen-map-drift-pr-2636-reality` (#2644), `route-error-boundary-gap` (#2646), `rentals-auth-nonnull-mutation` (#2648). Sources + evidence lines appended.
- **Dropped:** none.

## Plans promoted
- none. The 3 new perf items are score 1-2; no `security` fast-track. The 4 pre-existing open items (`logout-cache-purge-gap`, `mutation-no-onerror`, `notfound-i18n-gap`, `offline-terminal-drop-dataloss`) all have live dispatcher assignments in `.research/management/assignments.json` (PRs #2650, #2649, #2647, #2645 respectively) — Phase 3's stem-uniqueness rule blocks promotion while a claim is in-flight. Nothing hits the score≥3 floor.

## Auto-fix
- `no candidate` — the 3 new signals are `perf` vector, not on the Phase 5 allowlist (screen-map-drift / screen-map-orphan / fixme-in-merged-code / dep-update-noise / stalled-review). Manual implementer path by design.

## Self-review findings
- open=28 (high=6, medium=19, low=3); new this run=0; acknowledged=19
- fp-null [recurrence=13] — assignments-archive.json too large (198KB) for MCP push_files inline; fix: use PR+API-merge path as primary push mechanism
- fp-null [recurrence=10] — dispatcher hard-assumes proxy-403 for git push; env actually permits it if commit email is @users.noreply.github.com; fix: runtime push-capability probe at Phase 1
- fp-null [recurrence=4] — action-list.json in-progress twin diverges from assignments.json live rows; fix: Phase-2 reconcile step to reset action-list on missing assignment
- fp-null [recurrence=3] — in-progress rows produce ZERO branches on origin across two implementer generations; fix: escalate branchless-after-reclaim to status=failed
- fp-null [recurrence=3] — Phase 2.6 claimable metric ignores archive-terminal ids; fix: extend gc1-reconcile to close open items terminal-in-archive for any status incl. failed
- … and 1 more (see findings.json)

## Open questions
- **Buffer refill effectiveness (still).** The trigger fired at `claimable=0/72` — the dispatcher's target buffer floor of 72 claimable items is not remotely achievable from routine-derived signals alone at current throughput (dispatcher merges ~10 items/day, routine + post-merge-reviewer surface ~3-5 fresh findings/day). Either the dispatcher's buffer-floor metric expects a different signal source (issue backlog? backlog `open+ready` union?) or the floor number needs tuning. Worth confirming with a Phase 1.6 pm-scrum-master synthesis in the morning.
- **Phase 1.5 subagent budget.** Today's attempted `api-handlers` review had to be aborted when the routine's stop-hook demanded a commit. The skill's "spawn one Rust expert" cost isn't budgeted against the same-day-rerun completion window. Options: (a) run Phase 1.5 only on the first daily run, never on same-day reruns, (b) shorten the expert's grep-and-report loop with a tighter cap. Flagging for next daily.
- Phase 1.6 skipped again — `pm_cursor.next_index` stays at 2 (pm-frontend last ran 2026-06-10 — 54d ago). Same-day rerun doesn't warrant a full role rotation; the 09:00Z run tomorrow can pick it up.

## Quiet day?
- No — 3 new backlog vectors added + 8 resolutions to `done`. `stats.vectors_created` bumped by 3, `stats.runs` by 1.
