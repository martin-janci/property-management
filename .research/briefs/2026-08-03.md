# 2026-08-03

## Since last run
- Merged PRs: 1 (#2629 — OAuth token-usage analytics wired: producer + platform-admin read route + 90-day retention prune; well-tested, closes #2628)
- Open PRs touched: 0 (14 open, oldest updated 07-30 — accounting-MVP trio + dependabot cluster still idle, none crossed 7d)
- New / updated issues: 0 (issue cursor #2628 unchanged; #2612, #2575, #2366 remain the oldest open follow-ups)
- Commits: 8 on `dev` since cursor `21a036ee` (2 code merges + 6 dispatcher chore commits including 2 tier1d dev-review generators, 1 dispatcher self-review, 1 dispatcher run with #2628 async-reconcile)
- Routine lag: 11h since last run — on schedule
- Phases that failed: none

## Shipped
- #2629 gh-issue-2628 — wire OAuth token-usage analytics (Epic 10A). `OAuthService.record_token_event` fires best-effort `issued`/`refreshed`/`revoked` rows on the token lifecycle (log-and-ignore on failure); new `GET /api/v1/platform-admin/oauth/token-usage?since=` returns totals + per-client rollup gated by super-admin + `AuditRead`; daily scheduler prune of aged `oauth_token_events` (default 90d via `OAUTH_TOKEN_EVENTS_RETENTION_DAYS`) mirrors the `support_tooling_events`/`layout_change_events` prunes. Ships 3 daily-cadence unit tests + a full lifecycle sqlx integration test that asserts a real `issued+refreshed+revoked` triple lands in the table. No unchecked TODOs.

## Watch
- Stalled review: none crossed 7d (oldest opens #2555/#2558/#2559 accounting-MVP trio + #2581–#2591 dependabot cluster all at ~4d idle — same status as prior run)
- Reverted: none this run
- Churn hotspots: none — 1 merged PR touching 8 files (all under `backend/servers/api-server/`) is insufficient to shift hotspot ranking

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: `reality-web` (Phase 1.5 primary this run, per `.research/signals/2026-08-03-reality-web-tier1d.json`)
- Also absorbing 3 orphan dispatcher tier1d files from 2026-08-02 (`mobile-rn`, `ppt-web-core`, `ppt-web-ui`) — these were produced by the dispatcher's Tier 1d generator during the drained-buffer window at 16:13Z / 18:14Z on 2026-08-02 but had never flowed through the routine's Phase 2/3 pipeline; today's run consumes all 9 findings across the 4 files
- Experts: frontend (rotating segment) + dispatcher-tier1d generator
- Findings: 9 total (see Backlog deltas). In-run confidence upgrades applied to 3 items after direct source read (`useapi-no-refresh-retry`, `nfc-device-clock-expiry`, `portfolio-dash-mock` — all lifted medium→high); 1 vector re-classified (`nfc-device-clock-expiry` bug→security based on the physical-access bypass risk)
- Next segment: `ppt-web-ui` (oldest unreviewed at 2026-07-21 — 13d) — the ppt-web-ui tier1d file consumed here does NOT advance that cursor because it came from the dispatcher's out-of-band generator, not the routine's Phase 1.5 selection
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/`=106, `reality/`=47, `mobile/`=0 — not yet seeded)
- Drift signals this run: 0 (no route files touched by PR #2629 — it only touches `backend/servers/api-server/**`)
- Orphan screens this run: 0 (no route files touched this run)
- Last `screen-map.yml` CI run on `main`: not checked this run (no drift trigger)

## Backlog deltas
- **New:** 9 code-review-finding items absorbed from the 4 pending tier1d signal files:
  - **[2/high/security]** `security-mobile-rn-nfc-device-clock-expiry` — NFC credential expiry validated against user-controlled device clock; rolled-back clock = physical-access bypass (promoted, see *Plans promoted*)
  - **[2/high/bug]** `code-review-ppt-web-ui-portfolio-dash-mock` — entire manager PortfolioDashboardPage is hardcoded mock data (every tenant sees the same fake `Downtown Office Building`)
  - **[2/high/bug]** `code-review-mobile-rn-useapi-no-refresh-retry` — central `apiRequest` throws on 401 with no refresh_token retry; every mobile useQuery/useMutation forces manual re-login on access-token expiry
  - **[2/medium/bug]** `code-review-ppt-web-core-perf-listener-leak` — `usePerformanceMetrics` cleanup only disconnects the PerformanceObserver; window `visibilitychange`+`load` listeners leak on every remount
  - **[2/medium/bug]** `code-review-ppt-web-ui-moderation-prompt-cast` — free-form `window.prompt()` output `as`-cast into enum-typed moderation action with no runtime validation
  - **[1/medium/test-gap]** `test-gap-mobile-rn-nfc-modules-untested` — NFCAccessController+NFCCredentialManager (799 LOC of physical-security code) ship with zero test files
  - **[1/medium/bug]** `code-review-reality-web-agency-import-i18n` — CsvImport/AgencyListings/ListingForm/FeedImport/SyncSchedule/AgencyBranding/CrmConnection/ComparisonView never import next-intl; sk/cs/de realtors see raw English
  - **[1/medium/bug]** `code-review-reality-web-protectedroute-i18n` — ProtectedRoute wraps every gated route yet strings hardcoded English
  - **[1/medium/bug]** `code-review-ppt-web-ui-dialog-i18n-gap` — ppt-web browser-dialog strings hardcoded English across otherwise-translated features
- **Bumped:** none (all 9 items new)
- **Decayed:** none (backlog had 0 open items at run start; nothing to decay)
- **Dropped:** none

## Plans promoted
- `.research/plans/security-mobile-rn-nfc-device-clock-expiry.md` — replace device-clock trust in `NFCAccessController.validateAccess` with a server-anchored `TrustedNow` + explicit `offline_grace_minutes` window; unit-tests the rolled-back-clock case as a failing-on-main regression. Promoted via security fast-track (score 2 + confidence high + vector security satisfies the ≥2 threshold). · adversarial pass: passed (source→hypothesis→approach→test chain is unbroken; failing-on-main test case c in the plan's Test plan exercises the exact rolled-back-clock scenario named in the hypothesis)

## Auto-fix
- `no candidate` — no signal matched the allowlist + `confidence=high` + `score>=3` combo. All 9 new signals are `code-review-finding` (not in the Phase 5 allowlist: `screen-map-drift`/`screen-map-orphan`/`fixme-in-merged-code`/`dep-update-noise` lockfile-drift/`stalled-review`). The security fast-track promotion is a Phase 3 action (plan file), not a Phase 5 auto-PR — auto-fix is only for mechanically-safe shapes.

## Self-review findings
- open=28 (high=6, medium=19, low=3); new this run=0; acknowledged=19 (unchanged since 2026-08-02 — routine did not touch self-improvement this run)
- fp-null [recurrence=13] — assignments-archive.json too large (198KB) for MCP push_files inline; fix: use PR+API-merge path as primary push mechanism
- fp-null [recurrence=10] — dispatcher hard-assumes proxy-403 for git push; env actually permits it if commit email is @users.noreply.github.com; fix: runtime push-capability probe at Phase 1
- fp-null [recurrence=4] — action-list.json in-progress twin diverges from assignments.json live rows; fix: Phase-2 reconcile step to reset action-list on missing assignment
- fp-null [recurrence=3] — in-progress rows produce ZERO branches on origin across two implementer generations; fix: escalate branchless-after-reclaim to status=failed
- fp-null [recurrence=3] — Phase 2.6 claimable metric ignores archive-terminal ids; fix: extend gc1-reconcile to close open items terminal-in-archive for any status incl. failed
- … and 1 more (see findings.json)

## Open questions
- Should the routine's Phase 1.5 selection be aware of the dispatcher's Tier 1d output so it doesn't re-review a segment the dispatcher just covered? Today's absorption of 4 tier1d files (of which 3 came from the dispatcher, not the routine's own Phase 1.5) worked but leaves `state.review_cursor` slightly ambiguous — the routine only advanced `reality-web` to today, even though the dispatcher's tier1d generator effectively reviewed `mobile-rn`/`ppt-web-core`/`ppt-web-ui` on 2026-08-02. Not a blocking issue (dedup on `seen_signals` handles re-emit), but worth deciding whose cursor is authoritative.
- Phase 1.6 skipped this run — management artifacts (`project-state.md`, `action-list.json`, `roadmap.md`) are recent enough (updated 2026-08-01 or later) and the dispatcher's 3 self-review commits on 2026-08-02 kept the delivery view current. `pm_cursor.next_index` stays at 2 (pm-frontend), `coverage_cursor.next_index` stays at 4. G16 is a no-op for this run.

## Quiet day?
- No — 1 new merged PR (#2629), 9 fresh backlog vectors added, 1 plan promoted (security fast-track), tier1d dev-review signals from 3 previously-orphan files absorbed. Buffer refilled from 0 open → 9 open (of which 1 is ready). Progress on state advanced on all cursors that had activity.
