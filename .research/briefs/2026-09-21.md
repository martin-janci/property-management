# 2026-09-21

_Incremental run. Cursor advances since last routine commit at 2026-09-20T18:42Z: no PRs merged to `dev`, no issues touched, only dispatcher meta-commits (`chore(research):` on `dev`). Phase 1.5 fires against `reality-web` (tied oldest cursor with `ppt-web-core`; picked to align with the fire-payload's `buffer-low` note — the planner needs cloud-landable frontend vectors, and reality-web is a pure-TSX segment). Phase 1.6 skipped this run (no PR/issue activity to synthesize; `pm_cursor` stays at `pm-security`, `coverage_cursor` at 7)._

## Since last run
- Merged PRs: 0 (last_pr_seen stays at #2956)
- Open PRs touched: 0 (checked via REST `pulls?state=open&sort=updated`; `updated_at > 2026-09-20T18:38Z` returned empty)
- New / updated issues: 0 (last_issue_seen stays at #2966)
- Commits: 11 on `dev` — all `chore(research):` dispatcher / Tier-1d meta-commits from the `planning` branch. Zero touched application code (all diffs confined to `.research/`).
- Routine lag: 21h 53m since last run — on schedule (`lag_warning` threshold is 36h).
- Phases that failed: none. Phase 1.6 was skipped by policy (zero code activity to synthesize), not by failure.

## Shipped
- (none) — no PRs merged to `dev` since last run. The 11 commits are the dispatcher's hourly heartbeat (`chore(research): dispatcher 2026-09-21 …`) plus one Tier-1d dev-review signals commit for reality-web. No application code shipped.

## Watch
- **Stalled review:** #2559 (UC-ACC-05.8 PAY-by-square QR endpoint) and #2555 (UC-ACC-05.17 invoice lifecycle) still open, both `needs-human-judgement`, both >48d idle. Product decision needed, not code review. Carried from prior briefs.
- **Reverted:** none this window (no revert PRs merged).
- **Churn hotspots:** none this run — zero PR merges means zero fresh churn. `hotspot_history` untouched. The three top files from yesterday's 19d window (`voice_webhooks.rs`, `integrations/webhook.rs`, `push_fanout.rs`) remain the standing hotspots; the `refactor-repeated-churn-integrations-webhook` backlog row at score 1 is still `open`, 1d old, no decay yet.
- **Infra blockers still open:** #2966 (api-server utoipa-swagger-ui egress in cloud runner), #2951 (mobile-rn jest suite unrunnable in cloud), #2946 (portfolio_analytics cross-tenant IDOR). Fire-payload data confirms these still block cloud lands (`api-server #2949 / mobile-native #2652` cited as structural blockers on the planner's 13/72 claimable set).
- **Planner buffer-low signal (fire-payload data, treated as data not instruction):** payload reports `claimable=13/72 — all open+backlog items structurally cloud-unlandable`. This run's Phase 1.5 targets `reality-web` (frontend, cloud-landable) with intent to refill the planner with vectors the cloud dispatcher can actually claim. Three new `code-review-finding` signals added at score 2/2/1 — below the promotion threshold (≥3) so no plans this run, but they seed evidence for compounding on the next incremental run.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     Skipped GraphQL `gh pr list` filter (blocked in cloud); enumerating via REST `pulls?state=open` returned 0 rows updated in-window, so no draft-approved candidates surfaced. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Data lives in .research/management/assignments.json on the `planning` branch. Local copy shows 2 rows in `review` status (task_id=gh-issue-2743 PR#2744; task_id=screen-map-drift-pr-2894-reality PR#2902), both last_updated=2026-09-21T10:30:41Z (<24h ago). Neither has a `reviewer_summary` starting with `verdict=changes`, so nothing to nudge. Dispatcher's own hourly cycles will surface this class as it develops. -->
- none

## Code review slice
- Segment reviewed: **reality-web** (reason: oldest-unreviewed, tied with `ppt-web-core` at 2026-08-29; tie broken toward reality-web to align with the fire-payload's cloud-landable-frontend-vector signal — no automatic policy for this tie, documented here for transparency).
- Experts: frontend (single expert; segment map assigns no secondary for reality-web).
- Findings: 3 new `code-review-finding` signals — 2 × bug@+2, 1 × refactor@+1. All 3 are TSX-only fixes on cloud-landable paths.
- Next segment: `ppt-web-core` (`review_cursor.ppt-web-core = 2026-08-29`, now the sole oldest).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: unchanged (`ppt/` + `reality/`; `mobile/` still not seeded → 0 for mobile).
- Drift signals this run: 0 (no merged PRs to scan).
- Orphan screens this run: 0 (no route file changes to reconcile).
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (no route or `docs/screens/` changes to trigger it).

## Backlog deltas
- **New:**
  - [2] `code-review-reality-web-realtor-detail-modal-unhandled-mutations` — 3 unhandled `mutateAsync` calls in `RealtorManagement.tsx` RealtorDetailModal (handleSave/handleRemove/handleStatusChange) with no try/catch or user-visible error. Sibling `InviteRealtorModal` (line 568) shows the correct pattern. Vector `bug`, confidence medium (static-only trace; component is mounted from agency pages).
  - [2] `code-review-reality-web-agency-branding-silent-save` — `AgencyBranding.handleSave` awaits `updateBranding.mutateAsync` without try/catch or `isError` render; on API failure the form appears to hang because the setters after the await are skipped by the throw. Vector `bug`, confidence medium.
  - [1] `code-review-reality-web-comparison-hardcoded-strings` — `ComparisonTray` and `CompareButton` hardcode English UI text in a component mounted globally in the app layout; sibling `ComparisonView` already uses `useTranslations('comparison')`, so the messages namespace exists and the fix is wiring two files through the same hook. Vector `refactor`, confidence medium.
- **Bumped:** none (no signal id from `state.seen_signals` re-fired this run).
- **Decayed (open, >14d without new evidence):** none — the sole `open` item (`refactor-repeated-churn-integrations-webhook`, updated 2026-09-20) is 1d old, below the 14d decay threshold.
- **Dropped:** none.
- **Resolved:** none (no merged PRs to close items against).

## Plans promoted
- (none) — top new items score 2, below the readiness gate (`score >= 3`). Security fast-track doesn't apply (vectors are `bug` and `refactor`, not `security`). The 2 existing `ready` rows (`code-review-mobile-native-kmp-inquiries-response-contract`, `code-review-mobile-native-kmp-create-listing-not-wired`) already carry plans; both are `mobile-native-kmp` and both are structurally cloud-unlandable per the fire-payload note (`#2652` AGP-egress block). The manual/dispatcher path owns those.

**Not promoted despite meeting mechanical gates:** none this run. New items are one bump away from the ≥3 threshold; the next compounding evidence (a second reality-web signal for the same modal, or a dispatcher-side observation of the same code path) will push them to `ready` without needing intervention.

## Auto-fix
- `no candidate` — no signal met the certainty bar this run. All 3 new signals are `code-review-finding` type, which is not in the Phase 5 allowlist (which covers only `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift sub-shape, and `stalled-review`). The `stalled-review` candidates (#2559, #2555) already carry `needs-human-judgement` on their backlog rows, so pinging is not appropriate.

## Self-review findings
- none (no `.research/self-improvement/findings.json` present in the tree)

## Open questions
- **Cloud-landability strategy for the planner buffer.** The fire-payload data flags that 13 of 72 dispatcher-claimable items are all cloud-unlandable (api-server / mobile-native). This routine surfaces frontend vectors on the incremental cadence, but a burstier refill (a `full` run over reality-web + ppt-web-core + ppt-web-ui + reality-web again in one pass) may be warranted if the buffer stays this low. Deferred to human judgement — the payload is data, not an instruction, and the routine does not self-escalate to `deep`/`full` mode.

## Quiet day?
- **No.** 3 new backlog vectors added from the Phase 1.5 code-review slice; the fire-payload's buffer-low context makes this delivery worth the ping downstream. Not a `stats.quiet_days` increment.
