# 2026-06-15

## Since last run
- Merged PRs: **4** (#1379, #1382, #1383, #1384) — 2 application-code, 2 research/dispatcher.
- Open PRs touched: 0 newly-stalled; 2 carry-over stale drafts (#1316 @ 1.8d, #1197 @ 5.9d).
- New / updated issues: **20** since cursor — 18 post-merge-review follow-ups #1360–#1377 (all labeled `follow-up` + `from-merged-review`), 1 untriaged dispatcher meta-issue #1380, 1 re-touched CI tracker #1332 (state: CLOSED).
- Commits on `dev`: 25 since `48efccc7` → `67c24bde` (4 merge commits + 21 dispatcher/version-bump commits).
- Routine lag: ~23h since last run — well under 36h; no `lag_warning`.
- Phases that failed: **none**.

## Shipped
- **#1379** `fix: unblock dev-wide backend test job (#1332) — 3 production regressions` — `backend/crates/db/src/repositories/form.rs` got `status::text AS status` casts across 9 SQL bodies (sqlx ColumnDecode mismatch on `form_status` ENUM into Rust `String`), `registry.rs` got `COALESCE` on NOT NULL boolean inserts, and `documents/core.rs` got an explicit cross-tenant org guard on `get_download_url`/`get_preview_url` (defense-in-depth for BYPASSRLS/test-pool callers — closes a 503 existence-leak that masqueraded as 404). `form_rls_repo_tests.rs` adds `GRANT SELECT ON users` to the NOSUPERUSER RLS role for the new LEFT JOIN. **Unblocks the dev-wide backend `test` CI job that had been red since 2026-06-12.**
- **#1383** `feat: per-environment app icon variants (DEV/STG badges) (mobile)` — `frontend/apps/mobile/app.config.ts` resolves launcher icon + Android adaptive-icon foreground per `APP_ENV` (development/staging/production), with a fallback to the un-badged production asset when a badged variant PNG is absent so EAS/prebuild never fails on a missing file. New `app.config.icon.test.ts` pins both happy-path and fallback. gap-85-2 (Build Configuration by Environment).
- #1382 / #1384 — dispatcher state PRs (`.research/management/` only). No application code.

## Watch
- **#1378 closed-not-merged** — the DROP-OWNED-BY teardown theory for #1332 was the wrong root cause; superseded by #1379. Backlog row `refactor-closed-not-merged-pr-1378` tracks it (already done).
- **Stale drafts:** #1316 (verify-document-folder-organization-backend-promote, 1.8d), #1197 (test-oauth-authorization-server-integration, 5.9d). Decision needed: promote/rebase/close.
- **Churn hotspots this run (top 3):** `frontend/apps/mobile/app.config.icon.test.ts` (124 lines — NEW co-added test), `frontend/apps/mobile/app.config.ts` (94 — gap-85-2), `backend/crates/db/src/repositories/form.rs` (53 — #1332 unblock). All co-evolved with intentional changes; no repeated-churn from this set.
- **No reverts this run.**

## PRs stuck in draft despite approval
- none (the 2 stale drafts above are `reviewDecision: null`, not APPROVED)

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **api-core** (reason: churn-aligned with #1379 + oldest unreviewed at ~17d stale).
- Experts: rust.
- Findings: **2** — see Backlog deltas for `code-review-api-core-vote-partial-cmp-panic` (medium, +2) and `code-review-api-core-osrng-expect` (low, +1).
- Next segment: **ppt-web-core** (last reviewed 2026-06-05, oldest after this run).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: **142** (ppt: 80, reality: 47, mobile: 0 — not yet seeded).
- Drift signals this run: **0** (the only app-code merge — #1383 — touched `frontend/apps/mobile/*`, which is excluded from drift detection until `docs/screens/mobile/` lands).
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not fetched this run (no `docs/screens/**` edits to trigger).

## Backlog deltas
- **New:**
  - [1] `triage-issue-1380` — Issue #1380 (no labels): Dispatcher stale gap-scan buffer + Tier-2 escalation endpoint misconfigured (triage, never promoted).
  - [1] `refactor-closed-not-merged-pr-1378` — PR #1378 closed not merged (DROP-OWNED-BY theory wrong; status=done — superseded by #1379).
  - [1] `churn-hotspot-frontend-apps-mobile-app-config-icon-test-ts` — 124 lines NEW co-added test (healthy).
  - [1] `churn-hotspot-frontend-apps-mobile-app-config-ts` — 94 lines (gap-85-2 per-env icon resolution).
  - [1] `churn-hotspot-backend-crates-db-src-repositories-form-rs` — 53 lines (#1332 unblock ENUM casts).
  - [2] `code-review-api-core-vote-partial-cmp-panic` — `vote.rs:1765` `partial_cmp().unwrap()` on f64 weighted vote count — NaN propagates from `try_into().unwrap_or(1.0)` → panic on `/votes/{id}/results`. Fix: `total_cmp` or `unwrap_or(Ordering::Equal)`.
  - [1] `code-review-api-core-osrng-expect` — `crypto.rs:127` `SysRng.try_fill_bytes(...).expect()` panics on every integration-credential encrypt if OS CSPRNG read errors.
- **Bumped:** none (no existing-item dedup hits this run).
- **Decayed:** none — earliest open item is `test-gap-screen-map-drift-pr-922-ppt` (2026-06-01, 14d). Eligible next run.
- **Dropped:** none.

## Plans promoted
- `plans/code-review-reality-web-realtor-mgmt-untranslated.md` — wire RealtorManagement.tsx hardcoded English chrome through `useTranslations("agency")`; add 11 new keys to en/sk/cs/de/hu/pl message bundles. · adversarial pass: passed.

## Auto-fix
- `no candidate` — no eligible signal matched the allowlist (no screen-map drift, no fixme-in-merged-code, no lockfile-drift dep-update, no stalled-review against a non-draft PR). Certainty bar moot.

## Self-review findings
- open=8 (high=2, medium=4, low=2); new this run=0; acknowledged=19
- fp-mcp-push-cannot-inline-large-archive [recurrence=5] — Phase 6 archive-move produced large assignments-archive.json; mcp__github__push_files cannot inline; fix: push to a NEW branch (new-branch push is NOT proxy-blocked), open PR via MCP, merge via MCP — landed PR #1163 this way before, T19 was green on dev.
- fp-buffer-metric-blind-to-archived-open-items [recurrence=2] — Phase 2.6 buffer reported claimable=54/open=54 but real claimable=0; fix: extend gc1-reconcile to close any OPEN action-list item whose task_id is terminal in active+archive, OR change the open_count formula to subtract archive task_ids.

## Open questions
- **18 follow-up issues #1360–#1377 lack owner.** All from yesterday's post-merge review. Themes converge on: test gaps (RLS write/download, IDOR, OAuth/CSRF, realtime sync publish, presigned URLs), atomicity bugs (record_payment, record_reserve_transaction), front-end races (dispute draft autosave, iOS SearchView pagination), test-discipline tooling (canonical seed_membership helper, tenant-aware RLS request helper, pre-push fmt/clippy gate). pm-qa role's `next_actions` propose several of these for the dispatcher queue; rest need triage into real backlog or close-as-duplicate.
- **Close #1332?** PR #1379 fixed the dev-wide backend `test` job — confirm green on next main build, then close.
- **Phase 1.6 PM rotation advanced** — pm-qa ran this run (next: pm-devops). Coverage cursor advanced epic-85 → epic-8a.

## Quiet day?
- **No.** 2 application-code PRs merged (#1379 critical CI unblock + #1383 gap-85-2 icons), 7 new backlog vectors, 1 plan promoted, 2 code-review findings from Phase 1.5, full Phase 1.6 PM rotation with pm-qa role. `stats.runs` 38 → 39, `vectors_created` 157 → 164, `plans_created` 10 → 11.
