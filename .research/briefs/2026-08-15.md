# 2026-08-15

## Since last run
- Merged PRs: 10 (range #2745–#2761)
- Open PRs touched: 5 (#2751 npm-minor, #2750 cargo-minor, #2673 ktor, #2749 gradle-minor — all Dependabot; #2744 dispatcher fix draft)
- New / updated issues: 4 (all closed same window — #2752, #2753, #2754 auto-opened by post-merge review then closed by follow-up PRs; #2759 auto-opened by post-merge review of PR #2755 then closed by PR #2761)
- Commits: 51 on `dev` (mix of merged PRs above + dispatcher chore commits on planning branch)
- Routine lag: 2d 4h since last run (last_run_iso=2026-08-12T22:45:00Z; now=2026-08-15T03:11Z) — `lag_warning` fires (> 36h). Below the 3d `stale_routine_alert` threshold.
- Phases that failed: none. Phase 1.6 **skipped this run** (deferred to on-demand local `/ppt-project-management scan` — `pm_cursor` NOT advanced).

## Shipped
- #2761 gh-issue-2759: complete mobile date-formatting consolidation + bare-toLocale* guard test (closes #2759)
- #2760 churn-hotspot websocket disconnect() lifecycle hardening — detach handlers before close, avoid stale-socket reconnect on logout→login
- #2758 code-review-reality-server-sso-backslash-redirect-retry1: fail closed on `/\evil.com`-style backslash-obfuscated open-redirect
- #2757 gh-issue-2754: guard ProtectedRoute common.loading + errors.unauthorized i18n keys + lockstep regression test
- #2756 gh-issue-2752: route MeterDetailScreen reading dates through app i18n locale (closes #2752)
- #2755 gh-issue-2753: consolidate inline `toLocale*` onto format.ts helpers on 9 mobile screens (closes #2753)
- #2748 code-review-api-handlers-voice-alexa-noverify-retry1: fail closed on Alexa `/verify` branch (SECURITY)
- #2747 code-review-api-handlers-marketplace-award-quote-idor-retry1: verify awarded quote belongs to RFQ (IDOR fix + 298-line test suite)
- #2746 code-review-api-core-apicall-ssrf-redirect-retry1: regression test — SSRF redirect policy blocks internal targets
- #2745 closed-not-merged-pr-2705: bump `rust-toolchain 1.94.1 → 1.97.1` (retry of the abandoned 1.100.0 bump — the earlier target didn't exist)

## Watch
- **Dispatcher archive wedge (issue #2743) still open** — PR #2744 is approved+bot-labeled `needs-human-review` (self-modifying dispatcher scripts) and remains draft; every terminal transition on the planning branch risks tripping the 64 KiB MCP push ceiling. No change since last run.
- **PR #2684 quarantined** (`code-review-api-core-workflow-cond-parse-failopen`, CI red on `test-shard 1-4`, fix_rounds=3 exhausted). Age now ~7d — inside quarantine-exit SLA per dispatcher notes.
- **Stalled accounting-cluster reviews** (#2555 / #2558 / #2559) — 15d open, 15d idle now. Signal IDs already in `state.seen_signals` so no re-firing; existing `stalled-review-pr-*` backlog rows still at `needs-human-judgement`.
- **Reverted:** none this window.
- **Churn hotspots (this run, top-3 raw):**
  - `backend/servers/api-server/tests/suites/marketplace_award_quote_idor_tests.rs` — +298 lines, runs_seen=1 (new file, PR #2747 IDOR regression suite)
  - `frontend/apps/ppt-web/src/i18n/protectedRouteI18n.test.ts` — +111 lines, runs_seen=1 (new file, PR #2757 lockstep i18n test)
  - `backend/servers/api-server/src/services/actions/api_call.rs` — +106 lines, runs_seen=1 (PR #2746 SSRF redirect regression)
  - No `repeated-churn` firings — none of the top-3 were in `hotspot_history` with runs_seen ≥ 2.

## PRs stuck in draft despite approval
- #2744 fix(dispatcher): un-wedge oversize-archive push (#1162) + close retry-remint cross-issue blind spot — last update 2026-08-13, still draft with bot approval only + `needs-human-review` label. This is the fix for issue #2743's archive wedge; needs the operator's ack to un-draft.

## PRs with verdict=changes, no fix-round progress in 24h
- code-review-api-core-workflow-cond-parse-failopen PR#2684 — last_updated 2026-08-09 (~6d ago), reviewer note: `verdict=changes`, CI red on `test-shard 1-4`, fix_rounds=3 exhausted → quarantined per dispatcher notes.
- none other (planning-branch assignments.json not read live this run — HTTPS proxy budget)

## Code review slice
- Segment reviewed: **api-handlers** (reason: churn-aligned — `voice_webhooks.rs` +72 lines from PR #2748 — AND oldest-unreviewed cursor 2026-08-10)
- Experts: rust + security
- Findings: 3 (2 medium security + 1 low bug — see Backlog deltas for the `code-review-finding` signals)
- Next segment: `mobile-native-kmp` (oldest-unreviewed after this run: 2026-08-11)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 168 (ppt=106, reality=47, reality-mobile=15; mobile=0 — not yet seeded, drift detection disabled for the mobile trigger set)
- Drift signals this run: 0 (no PR in-window touched `frontend/apps/{ppt-web/src/App.tsx,ppt-web/src/routes/**,reality-web/src/app/**}`)
- Orphan screens this run: 0 (no route-basename churn to trigger the spot-check)
- Last `screen-map.yml` CI run on `main`: (not queried this run — HTTPS proxy budget conservation)

## Backlog deltas
- **New:** [2] `code-review-api-handlers-voice-oauth-refresh-unauthenticated` security (Phase 1.5 finding — promoted, plan written)
- **New:** [2] `code-review-api-handlers-voice-google-verify-fail-open` security (Phase 1.5 finding — promoted, plan written)
- **New:** [1] `code-review-api-handlers-voice-alexa-cert-fetch-no-timeout` bug (Phase 1.5 finding — below promotion floor, stays in backlog)
- **New:** [1] `churn-hotspot-marketplace-award-quote-idor-tests` test-gap (new IDOR test suite; monitor for further churn)
- **New:** [1] `churn-hotspot-protected-route-i18n-test` test-gap (new lockstep i18n test; monitor)
- **New:** [1] `churn-hotspot-api-call-rs` refactor (real code churn from SSRF redirect fix; monitor)
- **Bumped:** none this run (all new signal IDs; no re-scoring of existing IDs).
- **Decayed:** none — no `open` items with `updated_at ≤ 2026-08-01`.
- **Dropped:** none.
- **Resolved to `done`:** none — the 3 open mobile-native-kmp rows still have evidence present on `main` (grepped `PortalListingsRepository.kt:113 limit=100`, `catch (e: Exception)` still present in Listing/InquiryRepository.kt, no SsoServiceTest.kt found), and no merged PR references their IDs.

## Plans promoted
- `plans/code-review-api-handlers-voice-oauth-refresh-unauthenticated.md` — attach AuthUser to voice /oauth/refresh + tests · adversarial pass: passed (chain evidence→hypothesis→approach→tests intact; IG3 test would fail on `main` today; no placeholder rot)
- `plans/code-review-api-handlers-voice-google-verify-fail-open.md` — either implement full JWT verify or remove the placeholder + tests · adversarial pass: passed (chain intact; IG3 forged-bearer test would pass on `main` today ⇒ fails after fix; alternative posture A/B documented; no placeholder rot)

## Auto-fix
- `no candidate` — the 2 new high-confidence security items are score=2 (security fast-track promotes plans at 2, but auto-fix requires score≥3). The 3 churn-hotspot signals score 1 and are outside the auto-fix allowlist. `lag_warning` is not on the allowlist. Stalled-review re-fires suppressed (all prior IDs in `seen_signals`).

## Self-review findings
- none (`.research/self-improvement/findings.json` not present)

## Open questions
- None new. Existing open questions (PR #2744 human ack for archive wedge, PR #2684 human triage) unchanged from prior briefs.

## Quiet day?
- No — 10 PRs merged, 2 security plans promoted, 3 code-review findings emitted, 3 churn hotspots recorded.

---

## Update — 2026-08-15 06:43Z rerun (buffer-low refill)

Fired by the cloud dispatcher's `buffer-low: claimable=4/72 — all remaining claimable
backlog items are mobile-native/KMP (structurally unlandable in cloud runner); need
landable backend/frontend refill vectors` payload. Delta window since 03:11Z routine
commit is only 3h, but a lot happened.

### Shipped in the last 3h
Between the dispatcher's fast-path spawn (04:29–04:59Z) and this rerun (06:43Z), FIVE of the
six spawned implementer PRs merged to `dev` (06:33Z), draining the ready+open landable
queue essentially to zero:

- #2762 fix(api-server): bound Alexa cert fetch with timeouts + shared client (+58/-1, voice_webhooks.rs)
- #2763 test(UC-68): add missing IDOR regressions for marketplace award endpoint (+117 tests; closes 2 authz-branch gaps in load_rfq_for_org path + phantom-quote-id branch)
- #2764 fix(api-server): refuse cross-origin redirects in workflow API-call action (credential-leak SSRF hardening, api_call.rs)
- #2765 fix(api-server): fail-closed Google voice webhook JWT verification (+311/-96, voice_webhooks.rs — RS256 sig + aud/iss/exp + JWKS cache + 5 regression tests)
- #2767 test(ppt-web): cover ProtectedRoute i18n fallback + guard behavior (16→61 tests, runtime i18next resolution + defaultValue↔en parity)

Still open (draft, blocked on CI): **#2766** `code-review-api-handlers-voice-oauth-refresh-unauthenticated` — the last security fix from the 6-PR dispatcher salvo. Backlog row stays `ready`; plan stays live at `plans/code-review-api-handlers-voice-oauth-refresh-unauthenticated.md`.

### Resolution transitions applied (5)
- **done:** `code-review-api-handlers-voice-alexa-cert-fetch-no-timeout` ← PR #2762 (open→done, evidence: shared-client + 5s connect / 10s request timeout landed with 2 regression tests)
- **done:** `churn-hotspot-marketplace-award-quote-idor-tests` ← PR #2763 (open→done)
- **done:** `churn-hotspot-api-call-rs` ← PR #2764 (open→done; cross-origin redirect refusal + hermetic regression test)
- **done:** `code-review-api-handlers-voice-google-verify-fail-open` ← PR #2765 (ready→done; plan archived to `plans/_archive/`)
- **done:** `churn-hotspot-protected-route-i18n-test` ← PR #2767 (open→done)

### Phase 1.5 re-run (rotating expert review)
Re-fired the `ppt-dev-review` skill on `ppt-web-ui` (oldest-unreviewed **landable** segment — mobile-native-kmp was older but the caller's payload explicitly flagged it as structurally unlandable in the cloud runner). Frontend + Completeness experts returned:

- **[1] `code-review-ppt-web-ui-confirm-dialog-i18n-leak` (bug, medium)** — `ConfirmationDialog.tsx:138` renders hardcoded English `Processing...` while the caller-provided i18n-translated `confirmLabel` is suppressed during a mutation; confirmed reachable via `DocumentSharePanel.tsx:344-354` (`isLoading={revokeMutation.isPending}`) — sk/cs/de users see English mid-confirm. Default props `confirmLabel = 'Confirm'` / `cancelLabel = 'Cancel'` at :37-38 are hardcoded English fallbacks (silent leak on any future call-site omission). Distinct from the closed `code-review-ppt-web-ui-aria-label-i18n` (features-only). Below the promotion floor at score=1.

### New churn signal
- **[1] `churn-hotspot-voice-webhooks-rs`** (refactor, high confidence) — `backend/servers/api-server/src/routes/voice_webhooks.rs` churned **+369 additions** across 3 PRs in a 2h window (#2762 +58, #2765 +311, #2766 +86 open). First-run hotspot for this file; watch next run for `repeated-churn` if #2766 lands additional churn. Concurrent-PR risk: #2766 was rebased off pre-#2762/#2765 `dev` — merge conflicts likely on the shared verify blocks.

### Backlog after this rerun
- **open**: 5 (2 landable new refill vectors at score=1: `churn-hotspot-voice-webhooks-rs`, `code-review-ppt-web-ui-confirm-dialog-i18n-leak`; 3 mobile-native/KMP unchanged)
- **ready**: 1 (`code-review-api-handlers-voice-oauth-refresh-unauthenticated`, PR #2766 open)
- **done**: 222 (+5 this rerun)

### Refill assessment against the payload
The caller reported `claimable=4/72 — all remaining claimable backlog items are mobile-native/KMP`. This rerun produced **2 new landable (backend+frontend) rows at score=1**. Neither meets the score≥3 promotion threshold (nor the security fast-track — both are `bug`/`refactor`, not `security`), so they won't auto-promote to plans, but the dispatcher's fast-path can still claim them if it looks below the promotion floor. Deeper refill (score≥3 promotable items) requires either (a) more merges accumulating evidence on these files, or (b) a Phase 1.5 slice on another landable segment (`ppt-web-core`, `reality-web`, `mobile-rn`, `reality-server`, `api-core` are all at 2026-08-12/13 last-review — same-day rerun already spent one landable slice on `ppt-web-ui`).

### Plans promoted this rerun
- none — no ready item met promotion gates.

### Auto-fix this rerun
- `no candidate` — no signal met the allowlist + confidence=high + score≥3 bar. The new hotspot + i18n leak both score=1.

### Phases that failed
- none this rerun. Phase 1.6 skipped (same-day rerun; no new PR delta warrants re-firing PM analysis 3h after last incremental).

### Quiet day?
- **No** — 5 PRs merged, 5 backlog rows resolved, 1 new code-review finding, 1 new churn hotspot, 2 new landable refill vectors.

