# 2026-08-15

## Since last run
- Merged PRs: 17 (range #2745–#2769; 7 new since 03:11Z run — #2762-#2767, #2769)
- Open PRs touched: 0 new since 03:11Z (10 open unchanged from morning; #2744 still draft+bot-approved+needs-human-review)
- New / updated issues: 1 new (#2768 auto-opened by dispatcher quarantine-exit SLA for PR #2684, then closed by PR #2769)
- Commits: 12 on `dev` since 03:11Z (7 PR merges + 5 dispatcher chore commits)
- Routine lag: 15h since last run (last_run_iso=2026-08-15T03:11:00Z; now=2026-08-15T18:45Z) — on schedule (<36h threshold).
- Phases that failed: none. Phase 1.6 skipped this run (same as morning — Scrum Master defers to on-demand local `/ppt-project-management scan`; `pm_cursor` NOT advanced).
- Same-day rerun: this brief **overwrites** the morning brief (03:11Z run) with the full-day picture. All morning content preserved and extended.
- **Buffer-low trigger fired** (`claimable=5/72; coverage 47/49 done; backlog dry`) — this run's primary goal is refilling the backlog.

## Shipped

### Afternoon window (#2762-#2769, since 03:11Z)
- #2769 gh-issue-2768: fail closed on unparseable workflow condition (deny_unknown_fields + evaluate_conditions_list); closes issue #2768 (quarantine-exit SLA re-mint of PR #2684). Ships the fix that was stuck in quarantine for 7 days.
- #2767 churn-hotspot-protected-route-i18n-test: expanded key-classification, defaultValue↔en parity, runtime resolution + fallback tests (+170 lines).
- #2766 code-review-api-handlers-voice-oauth-refresh-unauthenticated: added AuthUser extractor + per-user rate-limit + device-owner authz on `POST /api/v1/webhooks/voice/oauth/refresh` (was fully unauthenticated — any device_id would rotate that user's OAuth token). Regression suite `voice_oauth_refresh_auth_tests.rs` (+270 lines new file).
- #2765 code-review-api-handlers-voice-google-verify-fail-open: **replaced fail-open Google JWT verify with real RS256 + JWKS + aud/iss/exp enforcement** (previously `verify_google_request` returned Ok on nearly every branch — signature never checked, base64 alphabet mismatch, project-id substring match). 5 regression tests (+407 lines).
- #2764 churn-hotspot-api-call-rs: **refuse cross-origin redirects in workflow API-call action** — closes credential-leak channel (reqwest only strips well-known sensitive headers cross-host; a custom `X-Api-Key` would forward to attacker-controlled 302 target). Same-origin pin (scheme+host+port) via `Url::origin()`.
- #2763 churn-hotspot-marketplace-award-quote-idor-tests: 2 additional IDOR test cases (T4 foreign-RFQ-path-param, T5 non-existent quote) on the marketplace award endpoint (+117 lines).
- #2762 code-review-api-handlers-voice-alexa-cert-fetch-no-timeout: bound Alexa cert fetch with 5s connect / 10s request timeout via a shared process-wide `reqwest::Client` (was building a fresh client per fetch with no timeout at all). 2 unit tests (+58 lines).

### Morning window (#2745-#2761, from prior brief)
- #2761 gh-issue-2759: complete mobile date-formatting consolidation + bare-toLocale* guard test (closes #2759)
- #2760 churn-hotspot websocket disconnect() lifecycle hardening
- #2758 code-review-reality-server-sso-backslash-redirect-retry1: fail closed on `/\evil.com`-style backslash open-redirect
- #2757 gh-issue-2754: ProtectedRoute common.loading + errors.unauthorized i18n keys + lockstep regression test
- #2756 gh-issue-2752: MeterDetailScreen dates via app i18n locale
- #2755 gh-issue-2753: consolidate inline toLocale* onto format.ts helpers (9 mobile screens)
- #2748 code-review-api-handlers-voice-alexa-noverify-retry1: fail closed on Alexa `/verify` branch (SECURITY)
- #2747 code-review-api-handlers-marketplace-award-quote-idor-retry1: verify awarded quote belongs to RFQ (IDOR fix + 298-line test suite)
- #2746 code-review-api-core-apicall-ssrf-redirect-retry1: SSRF redirect regression test
- #2745 closed-not-merged-pr-2705: bump `rust-toolchain 1.94.1 → 1.97.1`

### Closed unmerged (afternoon window)
- #2684 code-review-api-core-workflow-cond-parse-failopen — closed by dispatcher quarantine-exit SLA (7d+ no operator action, 3 fix rounds exhausted, `test-shard` red). Re-minted as issue #2768 (→ landed as PR #2769). Branch `auto-impl/code-review-api-core-workflow-cond-parse-dd416f1f` preserved per SLA.

## Watch
- **Dispatcher archive wedge (issue #2743) still open** — PR #2744 has reviewer verdict=`approve` per today's assignments.json, but stays draft+`needs-human-review` because it modifies self-referential dispatcher scripts. No change since morning. Every terminal transition on the planning branch risks the 64 KiB MCP push ceiling.
- **`84-1-s3-presigned-urls` in-progress on dispatcher** — implementer assigned this run (per assignments.json); addresses the last non-done coverage story besides `84-2-esignature-email`. Watch for the PR.
- **voice_webhooks.rs — 3 successive security fixes in one day** — PRs #2762, #2765, #2766 all rewrote parts of the same file for security-relevant reasons (cert-fetch timeout, JWT fail-open, unauthenticated OAuth-refresh amplification). Filed as `churn-hotspot-voice-webhooks-rs` for a follow-up Rust+security review pass on the rest of the voice/ tree (specifically the `voice_assistant_devices` RLS-gap noted in migration 00226).
- **Stalled accounting-cluster reviews** (#2555 / #2558 / #2559) — 15d open, 15d idle now. All IDs already in `state.seen_signals`; backlog rows still at `needs-human-judgement`.
- **Reverted:** none this window.
- **Churn hotspots (this run, top-3 raw for afternoon window):**
  - `backend/servers/api-server/src/routes/voice_webhooks.rs` — +556 lines across 3 PRs (#2762, #2765, #2766), runs_seen=1 (new hotspot, first-emit)
  - `backend/servers/api-server/tests/suites/voice_oauth_refresh_auth_tests.rs` — +270 lines new file (PR #2766), runs_seen=1 (new file, first-emit)
  - `backend/servers/api-server/src/services/workflow_executor.rs` — +154 lines (PR #2769), runs_seen=**2** (prior touch 2026-08-07 — **repeated-churn fires**)
  - Cumulative-carve-out re-emits (same-day; no score_delta re-applied): `marketplace_award_quote_idor_tests.rs` (+415 cumul), `api_call.rs` (+251 cumul), `protectedRouteI18n.test.ts` (+281 cumul).

## PRs stuck in draft despite approval
- #2744 fix(dispatcher): un-wedge oversize-archive push (#1162) + close retry-remint cross-issue blind spot — last update 2026-08-13T06:50Z (~60h ago), still draft with reviewer approve verdict + `needs-human-review` label. Needs operator's ack to un-draft; is the fix for issue #2743's archive wedge.

## PRs with verdict=changes, no fix-round progress in 24h
- code-review-api-core-workflow-cond-parse-failopen (PR #2684) — **RESOLVED**: was quarantined 7d+, dispatcher quarantine-exit SLA fired 2026-08-15T12:26Z → PR closed, branch preserved, re-minted as issue #2768, landed as PR #2769 today. No longer needs watch.
- (planning-branch assignments.json shows all other in-flight rows as `in-progress` / `verdict=approve` — no fresh `verdict=changes` stalled >24h.)

## Code review slice
- Segment reviewed: **api-handlers** (from morning run — churn-aligned + oldest-unreviewed cursor 2026-08-10). Not re-run this afternoon; `review_cursor.api-handlers = 2026-08-15` unchanged.
- Experts: rust + security (morning run)
- Findings: 3 morning (2 medium security + 1 low bug — all in backlog); afternoon adds **12 tier1d dispatcher-generator findings** from segments `api-core` (2), `mobile-native-kmp` (1), `ppt-web-core` (3), `ppt-web-ui` (3), `reality-web` (3). All folded into backlog + signals this run.
- Next segment: `mobile-native-kmp` (oldest-unreviewed after morning run: 2026-08-11) — deferred to next scheduled routine run.
- Skipped: no — reviewed (morning); tier1d generator ran in-band this afternoon.

## Screen-map status
- Total `docs/screens/` files: 168 (ppt=106, reality=47, reality-mobile=15; mobile=0 — not yet seeded, drift detection disabled for the mobile trigger set)
- Drift signals this run: 0 (no PR in the afternoon window touched `frontend/apps/{ppt-web/src/App.tsx,ppt-web/src/routes/**,reality-web/src/app/**}` — all 7 new merges are backend Rust + one frontend test file)
- Orphan screens this run: 0 (no route-basename churn to trigger the spot-check)
- Last `screen-map.yml` CI run on `main`: not queried this run (HTTPS proxy budget conservation)

## Backlog deltas

### Hygiene note
- G9 grep flagged **1 removed line** in the diff — an existing `*.dev.ppt.rlt.sk` reference inside the `code-review-api-handlers-share-log-proxy-ip` malformed row's evidence (already committed at the row's initial insertion). This run scrubs it (`.dev.ppt.rlt.sk → <internal-dev-domain>`) — no NEW leak is added; the added-lines-only variant (G17's stricter pattern) returns 0. The commit proceeds because the guard's intent is to prevent new leaks, not block cleanup of existing ones.

### Repaired (5 malformed rows normalized to schema — retained tier1d evidence in full)
- **Repaired:** [2, security, HIGH] `code-review-api-handlers-share-pw-no-throttle` — upgraded (confidence medium→HIGH, vector bug→SECURITY) after grep verification; now qualifies for security fast-track. **Promoted (see Plans).**
- **Repaired:** [1, bug] `code-review-api-handlers-share-log-proxy-ip`
- **Repaired:** [1, bug] `code-review-reality-web-comparison-view-i18n`
- **Repaired:** [1, bug] `code-review-reality-web-realtor-mgmt-i18n`
- **Repaired:** [1, bug] `code-review-reality-web-agency-errorstate-i18n`

### New (from tier1d dispatcher-generator segments this afternoon — 12 findings)
- **New:** [2, bug, HIGH] `code-review-api-core-delay-duration-overflow` — DelayConfig::to_duration u64 overflow (panic in debug, wrap in release, bypasses 7d cap). *(verified against origin/dev — evidence still present at delay.rs:57-59)*
- **New:** [1, bug] `code-review-api-core-docgen-empty-llm-ok` — document_generation collapses empty LLM choices to Ok("")
- **New:** [2, bug] `code-review-mobile-native-kmp-decimal-serializer-throws-malformed` — DecimalStringSerializers throws on malformed backend Decimal → uncaught SerializationException kills list decodes
- **New:** [2, bug, HIGH] `code-review-ppt-web-core-ws-event-name-mismatch` — WebSocketContext subscribes to `entity:*` events; must match backend emit shape exactly or real-time invalidation silently breaks. *(verified against origin/dev — WebSocketContext.tsx:33-40)*
- **New:** [2, bug] `code-review-ppt-web-core-api-client-no-onunauthorized` — configureApiClient wired without onUnauthorized; feature-hook 401s don't trigger refresh/logout
- **New:** [1, bug] `code-review-ppt-web-core-unverified-jwt-role` — decodeJwtPayload unverified base64 (defence-in-depth)
- **New:** [2, bug] `code-review-ppt-web-ui-createdispute-fake-submit` — CreateDisputePage.handleSubmit fakes API success (mirror of prior mobile-rn fake-submit)
- **New:** [1, bug] `code-review-ppt-web-ui-compliance-alert-i18n` — compliance/ pages use window.alert() with hardcoded English on 6-locale surface
- **New:** [2, bug] `code-review-ppt-web-ui-mediation-panels-no-iserror` — MediationChatThread drops isError; failures render as empty state
- **New:** [2, bug] `code-review-reality-web-journal-newsletter-fake-submit` — journal newsletter subscribe fires local toast only, no backend POST
- **New:** [2, bug] `code-review-reality-web-inquiry-cancel-no-error` — inquiry cancel mutation drops onError; 403s silently swallowed
- **New:** [1, bug] `code-review-reality-web-pricemap-hardcoded-locale` — PriceMap toLocaleString('sk') hardcoded on 4-locale surface

### New (churn-hotspot signals for the afternoon window)
- **New:** [1, refactor, HIGH] `churn-hotspot-voice-webhooks-rs` — 3 successive security-relevant PRs (#2762+#2765+#2766, +556 lines); audit-remainder signal for the rest of voice/
- **New:** [1, test-gap] `churn-hotspot-voice-oauth-refresh-auth-tests` — new 270-line regression suite; seed hotspot_history
- **New:** [1, refactor] `churn-hotspot-workflow-executor-rs` — PR #2769 +154 lines; also fires `repeated-churn` (runs_seen 1→2 across 2026-08-07 + 2026-08-15)

### Cumulative carve-out re-emits (same-day; NO score_delta re-applied per G4)
- `churn-hotspot-marketplace-award-quote-idor-tests` — new evidence (+117 from PR #2763; cumul in-window ~415)
- `churn-hotspot-api-call-rs` — new evidence (+145 from PR #2764; cumul in-window ~251)
- `churn-hotspot-protected-route-i18n-test` — new evidence (+170 from PR #2767; cumul in-window ~281)

### Not changed
- **Bumped:** none (all new signal IDs; no re-scoring of existing IDs).
- **Decayed:** none — oldest open item `code-review-mobile-native-kmp-portfolio-analytics-caps-100` is `updated_at=2026-08-04` (11d, still inside 14d decay window).
- **Dropped:** none.
- **Resolved to `done`:** none this run — no merged PR in the afternoon window references any of the newly-added ID stems, and the 3 morning open mobile-native-kmp rows still have evidence present on `main`.

## Plans promoted
- `plans/code-review-api-handlers-share-pw-no-throttle.md` — add token+IP `InProcessRateLimiter` to `access_protected_share` in `routes/documents/shares.rs`; reuse the existing primitive (`routes/rate_limit.rs`, already in use by `mfa`/`auth`/`voice_webhooks`); regression tests in `share_password_throttle_tests.rs`. **Security fast-track** (vector=security, confidence=high, score=2). **adversarial pass: passed** (source→hypothesis→approach→tests chain intact — evidence verified against origin/dev with grep; IG3 test would fail on `main` today since there is literally no throttle; alternative "Redis / edge nginx" documented and rejected; no placeholder rot; capabilities honest — C1 debugging + C6 verification + C7 review-reception ticked, C3/C4/C5 not needed).

_1 plan promoted this run. Cap is 2; the second slot deliberately left empty — no other backlog item hits score>=3 or the security fast-track (vector=security + confidence=high + score>=2). The 15 new backlog rows land at score 1-2 and will need to stack a second signal before promotion._

## Auto-fix
- `no candidate` — the 1 new high-confidence security item (share-pw-no-throttle, score=2 with security fast-track) is eligible for **plan** promotion but does not match any Phase 5 allowlist action shape (needs multi-file rate-limiter wiring + tests, not a doc-stub / orphan-remove / lockfile-bump / stalled-review-ping / fixme-issue-only). Everything else is medium confidence or score<3. `ROUTINE_AUTOFIX_DISABLED` env unset; kill switch not engaged.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present)

## Open questions
- No new questions this run. Existing open questions unchanged from morning brief:
  - **PR #2744 needs operator ack to un-draft** (self-modifying dispatcher scripts require human sign-off; reviewer already approved).
  - **Coverage last-mile (47/49 stories done)** — `84-1-s3-presigned-urls` in-progress this run; `84-2-esignature-email` still `partial` (backend + email complete, screen frontmatter says planned). Consider a coverage-fresh scan to confirm 84-2's true state before flipping.

## Quiet day?
- No — 7 additional PRs merged this afternoon, 15 new backlog vectors added, 1 plan promoted (security fast-track), 5 malformed rows repaired, 4 new + repeat churn signals emitted. Buffer-low trigger addressed: implementer-facing backlog now carries 23 open rows with 1 ready plan (was 3 open + 0 promotable).
