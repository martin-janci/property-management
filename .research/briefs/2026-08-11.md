# 2026-08-11

## Since last run
- Merged PRs: 30 (range #2703–#2735)
- Open PRs touched: 1 since 2026-08-07T06:24Z (of 16 open total; the rest untouched in-window)
- New / updated issues: 4 in-window — all CLOSED (#2612 #2643 #2703 #2704 resolved by merged PRs #2714 #2732 #2707 #2710)
- Commits: 50 on `dev` since `a2c0f53` (30 PR merges + 20 dispatcher research-chore commits); new head `b30e157d`
- Routine lag: 4d 13h since last run — **`lag_warning` + `stale-routine-alert` emitted** (>36h and ≥3d thresholds both crossed; cloud routine appears to have missed the daily tick for 4 consecutive days)
- Phases that failed: none

## Shipped
Backlog-resolving PRs of interest (30 merges total; grouped):
- **Security fixes:** #2707 workflow api_call response-body cap (memory DoS), #2710 SSRF DNS-rebinding TOCTOU in api_call.rs, #2718 layout webhook HMAC parity, #2722 gate community read handlers on principal+tenant, #2727 require agency membership on GET /agencies/{id}/members, #2731 sanitize vote titles in reports CSV export.
- **Correctness / fail-honestly:** #2706 fail-closed on partial RAG embedding batch, #2708 reject non-finite numbers in workflow condition compare, #2714 decouple scheduled notification dispatch (closes #2612), #2724 route raw DB errors through util::errors::db_error, #2725 wire real password-reset email transport + not-configured guard, #2726 surface portal session invalidation failure in sync_session, #2728 voice check-* fail honestly instead of fabricating empty success, #2729 don't release held notifications on failed quiet-hours drain, #2730 fail-closed on quiet-hours schedule read error, #2732 move OAuth token-usage recording off the hot path (closes #2643).
- **Refactor sweep (dispatcher retry chain):** #2711 layout tenant-override, #2713 layout admin handlers, #2715 auth handler error boilerplate, #2720 reports helpers extract, #2721 acquire_public_conn extract in layout tenant handlers.
- **Reality-portal correctness:** #2709 i18n ListingForm via next-intl, #2719 route anonymous inquiry POSTs through InquiriesHandler.
- **Frontend:** #2716 admin-web platform-settings PATCH/GET, #2717 admin-web mobile-config PATCH/GET, #2733 reconcile ppt error-handling screen-map with PR #2646, #2734 remap carried-forward actions in useOfflineSupport, #2735 regression tests for rentals API↔UI mappers.
- **Cross-cutting:** #2712 emit dispute add_evidence access-audit event, #2723 announcement fan-out delivered/read/ack metrics per targeting scope.

## Watch
- No revert PRs merged in-window. No hotfix-titled merges without tests observed in the 30-PR set.
- Draft/CI blockers: **1 quarantined assignment** — `code-review-api-core-workflow-cond-parse-failopen` (PR #2684, fix_rounds=3 exhausted, CI red on 926afe8 with test-shard 1-4 FAILURE; fmt/clippy green). Quarantine sibling blocks stem-collision for its retry.
- Churn hotspots (top-3, this window's raw churn after exclusions):
  - `frontend/packages/sitemap/src/json/sitemap.json` (4207 lines) — bulk regen from screen-map churn; **data file, noise**.
  - `frontend/apps/ppt-web/messages/en.json` (3984 lines) — i18n catalog bulk updates; **data file, noise**.
  - `backend/servers/api-server/tests/suites/oauth_integration_tests.rs` (3481 lines) — OAuth test-suite churn during dispatcher refactor sweep; genuine but expected (test evolution).
  - No file with `hotspot_history.runs_seen ≥ 2` is in this run's top-3; **no `repeated-churn` fires** this run.

## PRs stuck in draft despite approval
- none — 0 open PRs match `isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h`.

## PRs with verdict=changes, no fix-round progress in 24h
- `code-review-api-core-workflow-cond-parse-failopen` PR#2684 — reviewer_summary `verdict=changes` since 2026-08-08 12:08Z (72h); **quarantined** at 3 fix_rounds. Manual adjudication needed; no further respawn.

## Code review slice
- Segment reviewed: **skipped this run** (routine's Phase 1.5 delegate `ppt-dev-review`); the dispatcher's tier1d generator has already reviewed 6 segments across 2026-08-10/08-11: `api-handlers`, `api-core`, `reality-server`, `ppt-web-core`, `ppt-web-ui`, `reality-web`, `mobile-native-kmp`, `mobile-rn`.
- Experts: dispatcher-generator (segment-scoped static review, single expert per segment).
- Findings: **31 fresh dispatcher tier1d signals consumed** into `backlog.json` as new `open` rows (score 1–2, mixed medium/high confidence, vectors bug/test-gap/completeness/quality).
- Next segment: `reality-server` (last reviewed 2026-08-10 tie-with `api-handlers`/`api-core`; ppt-web-ui/core and mobile-* also within 5 days). Nothing overdue.
- Skipped: yes — the routine's Phase 1.5 slot was implicitly served by the dispatcher's continuous tier1d review across the 4d lag window. Re-running the skill's segment-picker on top would double-count.

## Screen-map status
- Total `docs/screens/` files: unchanged (ppt + reality directories present; mobile still 0 — excluded from drift detection).
- Drift signals this run: 0 (PR #2733 already reconciled the ppt error-handling drift for PR #2646).
- Orphan screens this run: 0 (no orphan scan run in this budget-constrained cycle).
- Last `screen-map.yml` CI run on `main`: not fetched this run (per-workflow REST call skipped for budget).

## Backlog deltas
- **New:** 37 items added (31 dispatcher tier1d findings folded in + 3 churn-hotspot rows + 3 stalled-review rows). Highlights (score ≥ 2, open, non-mobile-native-gated):
  - `[2 high bug]` `code-review-mobile-native-kmp-create-listing-notimpl-stub` (gated — mobile-native)
  - `[2 med bug]` `code-review-api-handlers-webhook-ssrf-loopback-failopen` — SSRF loopback fail-open branch in `webhook.rs` (production-guard-only)
  - `[2 med bug]` `code-review-api-handlers-residency-compliance-failopen` — `.unwrap_or(true)` fails OPEN on missing verification
  - `[2 med bug]` `code-review-ppt-web-core-auth-401-refresh-unwired` — reactive 401 hook wired but no refresh call
  - `[2 med bug]` `code-review-ppt-web-core-api-onunauthorized-unwired` — `configureApiClient` never passed `onUnauthorized`
  - `[2 med bug]` `code-review-ppt-web-ui-facilities-booking-silent-errors` — approve/reject catch is console.error only
  - `[2 med bug]` `code-review-ppt-web-ui-registry-silent-mutation-errors` — every mutation failure swallowed
  - `[2 med bug]` `code-review-ppt-web-ui-content-mod-unvalidated-prompt-cast` — `window.prompt()` cast to typed union unchecked
  - `[2 med bug]` `code-review-reality-server-agent-review-self-review` — realtor can review their own profile
  - `[2 med bug]` `code-review-reality-web-listings-page-no-error-state` — TanStack Query `isError` dropped
  - `[2 med bug]` `code-review-reality-web-layoutsections-dup-key` — dup React key on `layout.sections`
  - `[2 med bug]` `code-review-mobile-rn-widget-apirequest-swallows-errors` — widget fetcher `return {} as T` on ANY failure
  - Plus 12 score-1 findings (mostly test-gap / i18n gap / low-severity fail-open).
- **Bumped:** 0.
- **Decayed:** 0 (only 1 prior `open` item, `code-review-mobile-native-kmp-portfolio-analytics-caps-100`, updated 2026-08-04 = 7d old; not yet at 14d cutoff).
- **Dropped:** 0.
- **Resolved (open → done):** 0 by PR-body match (the 30 merged PRs all resolve pre-existing dispatcher-owned action-list rows, not routine-backlog rows).

**Buffer status:** open items 1 → 38. `real-claimable` in payload was 0; after this refill and mobile-native gate filter, ≥13 ungated score-2 items are new eligible refill candidates for the dispatcher's next `backlog-refill.sh` pass. Payload's "please refill planner/coverage backlog" satisfied on the planner side.

## Plans promoted
- **none this run.** No open item currently sits at score ≥ 3, and no `security` vector item at score 2 + `confidence=high` qualifies for the security fast-track (the 3 security-adjacent items — webhook SSRF fail-open, residency compliance fail-open, voice OAuth fail-open — are all `vector=bug` + `confidence=medium`). The 37 new items are freshly seeded at score 1–2 and need either a corroborating signal (Phase 2 stack) or a manual confidence upgrade before Phase 3 promotion.

## Auto-fix
- `no candidate` — no signal met `confidence=high + score≥3 + allowlist` this run (Phase 5 kill switch not set; env-check clean). The one high-confidence signal (`create-listing-notimpl-stub`, score 2) is a mobile-native/Kotlin code change requiring app implementation — outside the doc-stub/orphan-remove/lockfile-bump allowlist.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present or unpopulated this cycle)

## Open questions
- Should the routine's Phase 1.5 formally delegate to the dispatcher's tier1d generator when it detects fresh tier1d signals for the same day? Right now the routine skips silently and the dispatcher fires the skill separately; the two mechanisms are complementary but the routine only records "skipped" in the brief. A pointer under `state.review_cursor` (e.g. `-tier1d-fed` mark) would make this legible.
- The quarantined `code-review-api-core-workflow-cond-parse-failopen` (PR #2684) needs human adjudication — 3 respawn rounds could not pass the test-shard suite. Not routine-actionable; surface to operator.

## Quiet day?
- **No.** 30 merges landed, 37 new backlog vectors added, cursor advanced (`last_pr_seen` 2702→2735; `last_commit_sha` a2c0f53→b30e157). Genuine state advance.

## Goal violations
- (populated after Phase 6 gate run; see `signals/2026-08-11.json > goal_checks[]`)
