# 2026-08-11

## Since last run
- Merged PRs: **26** (range #2706–#2731) — all dispatcher-driven backlog resolutions; zero human-authored feature PRs in the window
- Open PRs touched: 1 (#2684 code-review-api-core-workflow-cond-parse-failopen — updated 2026-08-08)
- New / updated issues: 2 (#2703 SSRF DNS-rebinding TOCTOU CLOSED via #2710; #2704 unbounded response-body CLOSED via #2707)
- Commits: 26 PR-merge commits + ~40 dispatcher research-chore commits since `a2c0f53d`; new dev HEAD `b403a1a8`
- Routine lag: **3d 93h since last run** (`lag_warning` and `stale-routine-alert` both fired — cloud routine schedule skipped 2026-08-08/09/10 while the dispatcher on the planning branch kept ticking through those days)
- Phases that failed: none
- **Phases skipped this run (deferred, not failed):**
  - Phase 1.5 (rotating expert code review) — dispatcher's Tier-1d generator produced 4 fresh code-review signal files today/yesterday (`2026-08-10-*-tier1d.json`, `2026-08-11-ppt-web-ui-tier1d.json`); running the routine's ppt-dev-review skill in the same window is redundant. `review_cursor` intentionally not advanced.
  - Phase 1.6 (project-management skill) — `pm_cursor`/`coverage_cursor` intentionally not advanced. **pm-qa role is stale (last run 2026-06-15 — 57 days)** — recommend triggering `pm:qa` on the next routine wake so the QA lens catches up before the ~30 backlog-item merges above accumulate untested surfaces.

## Shipped
- **26 PRs merged, all dispatcher-generated backlog resolutions.** Highlights (full list in `git log --oneline origin/dev ^a2c0f53d`):
  - **Security-adjacent (5):** #2710 SSRF DNS-rebinding TOCTOU (closes #2703); #2707 unbounded response-body read (closes #2704); #2718 layout-webhook HMAC parity + body-binding pin; #2722 gate community read handlers on principal; #2727 require agency membership for member listing
  - **Fail-closed correctness fixes (5):** #2706 partial-RAG embedding fail-closed; #2708 workflow_executor non-finite number reject; #2728 voice check-announcements/check-meter fail honestly; #2729 quiet-hours drain does not release held on failed delivery; #2730 quiet-hours schedule read error fails closed
  - **Reality-server hardening (5):** #2719 anonymous inquiry POST → InquiryNotifier; #2724 DB errors through `util::errors::db_error`; #2725 real password-reset email transport; #2726 surface SSO portal session invalidation failure; #2727 agency members auth
  - **Layout/reports refactors (4):** #2711/#2721 layout tenant-override dedup + `acquire_public_conn` extraction; #2713 layout admin dedup; #2720 reports helpers extraction
  - **DX/data (5):** #2712 dispute add_evidence audit; #2714 decouple scheduled notification dispatch (closes #2612); #2715 auth handler error dedup; #2716 platform-settings PATCH/GET; #2717 mobile-config PATCH/GET; #2723 announcement fan-out metrics
  - **Also today:** #2731 (`code-review-api-handlers-reports-csv-injection`) merged 2026-08-11T04:08Z — closes the last open high-signal item from the 2026-08-10 backlog

## Watch
- **Open PRs (16 total)** — 0 draft, 0 approved-and-stuck, but three accounting feature PRs are **stalled at 13 days** with no reviewDecision:
  - #2555 `feat(acc): wire sent/cancelled invoice lifecycle (UC-ACC-05.17)` — 13d idle, last update 2026-07-30
  - #2558 `feat(acc): invoice PDF render endpoint (UC-ACC-05.9)` — 13d idle
  - #2559 `feat(acc): PAY by square payment QR endpoint (UC-ACC-05.8)` — 13d idle
- Reverted: none this window
- Churn hotspots (top-3, fresh entries in `hotspot_history`):
  - `backend/servers/reality-server/src/services/email.rs` — 368 lines churn (real password-reset transport + several email path fixes)
  - `backend/servers/api-server/src/services/notification_pipeline.rs` — 181 lines (quiet-hours drain + schedule fail-closed)
  - `backend/servers/api-server/src/services/voice_commands.rs` — 134 lines (voice-action fail-honest)
- **Risky-churn (score +2 each):** two `fix(*)` PRs touched top-3 churn files with **no test diff**:
  - #2730 `notification_pipeline.rs` fix (quiet-hours schedule read error)
  - #2728 `voice_commands.rs` fix (voice check-announcements/check-meter)

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none observed on `dev`; dispatcher planning-branch assignments not re-queried this run

## Code review slice
- Segment reviewed: **skipped** — dispatcher Tier-1d generator produced code-review-finding signals in `.research/signals/2026-08-10-*-tier1d.json` and `2026-08-11-ppt-web-ui-tier1d.json`, which covers the same ground; the routine's Phase 1.5 would double-count. `review_cursor` unchanged.
- Experts: n/a this run
- Findings: 0 from Phase 1.5 (see dispatcher tier1d signal files for today's findings)
- Next segment: `mobile-native-kmp` (last routine-side review 2026-08-04, oldest)
- Skipped: yes — dispatcher tier1d overlap + routine catch-up budget

## Screen-map status
- Total `docs/screens/` files: `ppt/` + `reality/` populated; `mobile/` not yet seeded (0)
- Drift signals this run: 0 (no merged PR touched a frontend route file this window)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run

## Backlog deltas
- **Done:** `code-review-api-handlers-reports-csv-injection` — resolved by PR #2731 (merged 2026-08-11T04:08Z, verbatim id match in PR title)
- **New (from this run's signals):**
  - `[2] bug risky-churn-pr-2730-notification_pipeline-rs` — `notification_pipeline.rs` touched by fix PR without test
  - `[2] bug risky-churn-pr-2728-voice_commands-rs` — `voice_commands.rs` touched by fix PR without test
  - `[1] refactor refactor-churn-hotspot-…-email-rs` (368 churn)
  - `[1] refactor refactor-churn-hotspot-…-notification_pipeline-rs` (181 churn)
  - `[1] refactor refactor-churn-hotspot-…-voice_commands-rs` (134 churn)
  - `[1] triage stalled-review-pr-2555 / -2558 / -2559` (three accounting PRs at 13d)
  - `[1] dx closed-not-merged-pr-2705` (dependabot rust-toolchain retry closed)
- **Bumped:** none this run
- **Decayed:** none this run (only open item pre-run, mobile-native portfolio, is 7 days old)
- **Dropped:** none

## Plans promoted
- **none** — no `open` item reached readiness (score ≥ 3, or security fast-track score ≥ 2 + confidence=high).
- Buffer-refill note: the fire payload flagged `buffer-low: claimable=1/72`. The routine cannot manufacture readiness — the only open item at score ≥ 2 (mobile-native portfolio-analytics-caps-100) is confidence=`medium` (needs a bump to score 3 or an upgrade to confidence=`high`+security to fast-track). Refill is expected from the dispatcher's Tier-1d generator, which produced 3 api-core code-review findings on 2026-08-10 already; those flow into the backlog via the dispatcher's own path, not this routine.

## Auto-fix
- `no candidate` — 0 eligible signals matched confidence=high + score≥3 + allowlist this run (Phase 5 kill switch NOT set; simply nothing eligible).

## Self-review findings
- none (no `.research/self-improvement/findings.json` in tree)

## Open questions
- **Cloud routine schedule stalled 2026-08-08 → 2026-08-10** (three routine ticks missed). Dispatcher on planning branch kept ticking. Operator: check the cloud cron for the routine (not the dispatcher) — evidence in `state.last_run_iso` gap.
- Buffer-low refill is dispatcher-side, not routine-side — the only lever for the routine to add promotable rows would be Phase 1.5's rotating expert review producing a High-severity finding. Consider triggering `text: "deep"` on the next fire to catch up scoring on the 26 recently-merged PRs.

## Quiet day?
- No.

