# 2026-08-12

## Since last run
- Merged PRs: not fetched — cloud `gh` unauthenticated this run (last_pr_seen=2702 pinned)
- Open PRs touched: not fetched
- New / updated issues: not fetched
- Commits: not fetched
- Routine lag: **5d 22h** since last run (2026-08-07T06:24Z → 2026-08-12T04:15Z) — **stale_routine_alert fired**; the daily routine has been dark while the dispatcher kept ticking on `planning`
- Phases that failed: phase-1-gh-observation (unauthenticated GH_TOKEN in cloud sandbox — see Phase 5.6 in `docs/routine-cloud-ops.md` if it existed; there is no auto-heal for this). All PR/issue/commit cursors were **not** advanced (per Phase-4 rule); `seen_signals`/`stats` did advance because Phase 2 ingest is cursor-independent.

## Shipped
- Nothing new observed this run (see *Since last run*). The dispatcher's own commits on `dev` since 2026-08-07 are known via `git log` but not attributed to research signals — they are dispatcher-internal (`chore(research): dispatcher …`) and out of scope for the routine's Phase 1 audit.

## Watch
- **Buffer-low fire (this run's trigger):** dispatcher reported `claimable=4/72 — backlog exhausted (coverage 47/49 done, backlog.json 0 fresh)`. That prompted this manual research run. The root cause turned out to be a 40-signal ingestion backlog (see *Backlog deltas*), not a genuine dry-well — dispatcher's tier1d generator kept emitting signals to `.research/signals/` daily, but the daily routine wasn't running to fold them into `backlog.json`.
- **Stale routine:** `state.last_run_iso` had been `2026-08-07T06:24Z`. Five daily cron slots missed. Cloud cron path likely paused; needs an operator look. Dispatcher on `planning` was healthy across the same window.
- Reverted: not fetched.
- Churn hotspots: not computed (requires PR-file fetches).

## PRs stuck in draft despite approval
- unknown — GH fetch skipped

## PRs with verdict=changes, no fix-round progress in 24h
- unknown — planning-branch `assignments.json` snapshot not fetched this run (would need to `gh api` the `planning` ref; cloud unauth). Local snapshot on `HEAD` shows 3 active rows: `code-review-api-core-workflow-cond-parse-failopen` (quarantined, PR#2684), `code-review-ppt-web-ui-offline-ind-i18n` (in-progress, no PR), `code-review-ppt-web-ui-protectedroute-i18n` (in-progress, no PR). Age data not computable without a fresh fetch.

## Code review slice
- Segment reviewed: **skipped** this run (Phase 1.5) — the daily rotating review already ran today via the dispatcher's tier1d generator (`ppt-web-ui`, 2 findings ingested with the rest).
- Experts: n/a — Phase 1.5 skipped
- Findings: **40 code-review signals ingested from dispatcher tier1d output (2026-08-08 → 2026-08-12) that had accumulated while the daily routine was dark.** See *Backlog deltas* for the full list.
- Next segment: `ppt-web-core` (oldest unreviewed after this run — `state.review_cursor.ppt-web-core = 2026-08-06`).
- Skipped: yes — daily slot spent on the tier1d ingestion instead of a fresh 3-expert pass.

## Screen-map status
- Total `docs/screens/` files: not recomputed this run (unchanged since 2026-08-07: `ppt/` + `reality/`; `mobile/` still empty)
- Drift signals this run: 0 (no PR observation)
- Orphan screens this run: 0 (no PR observation)
- Last `screen-map.yml` CI run on `main`: unknown — GH fetch skipped

## Backlog deltas
- **New (40 rows ingested from tier1d generator signals, all score 1-3, all `open`):**
  - **api-core (4):** `rls-context-pool-not-conn` (3), `require-auth-guest-default` (2), `idempotency-tx-across-handler` (2), `listings-detail-swallows-db-err` (2)
  - **api-handlers (4):** `webhook-ssrf-loopback-failopen` (2), `residency-compliance-failopen` (2), `residency-chain-valid-failopen` (2), `voice-oauth-link-failopen` (1)
  - **mobile-native-kmp (16):** `cancellationexception-swallowed-repos` (2), `httpclient-no-timeout` (2), `create-listing-notimpl-stub` (2), `create-listing-stub` (2), `create-listing-submit-stub` (2), `createlisting-notimpl-stub` (2), `mylistings-truncates-100-no-paging` (2), `nav-onfailure-empty-swallow` (2), `portfolio-unbounded-fanout` (2), `decimal-serializer-nfe-crash` (2), `decimal-wire-test-no-malformed` (1), `apiclient-dead-code-healthcheck-bug` (1), `compose-state-bang-bang` (1), `layoutrepo-println-logging` (1), `ssoservice-no-test` (1), `ssoservice-no-unit-test` (1)
  - **mobile-rn (2):** `widget-apirequest-swallows-errors` (2), `widget-storage-unguarded-jsonparse` (2)
  - **ppt-web-core (3):** `errorhandler-network-misclassify` (2), `auth-401-refresh-unwired` (2), `usewebsocket-handler-dep-gap` (1)
  - **ppt-web-ui (6):** `facilities-booking-silent-errors` (2), `automationrules-mutation-no-catch` (2), `conditionbuilder-number-coerce-zero` (2), `registry-silent-mutation-errors` (2), `registry-review-approve-only` (1), `workflow-automation-i18n-gap` (1)
  - **reality-web (3):** `listings-page-no-error-state` (2), `layoutsections-dup-key` (2), `maint-fallback-i18n` (1)
  - **reality-server / sso-rs (2):** `sso-rs-negcache-transient-fail` (2), `sso-rs-client-no-timeout-pool` (2)
- **Bumped:** none (all 40 were fresh IDs)
- **Decayed:** none (no `open` rows had `updated_at` older than 2026-07-29; the resolution check + decay gate found nothing to touch)
- **Dropped:** none from decay
- **Duplicate-flags for the dispatcher (documented, not deduped here — cross-tenant stem match should be caught at claim time):**
  - `mobile-native-kmp-cancellationexception-swallowed-repos` (this run) **overlaps** `mobile-native-kmp-cancellation-swallowed` (2026-08-11, open)
  - `mobile-native-kmp-{ssoservice-no-test,ssoservice-no-unit-test}` (this run) **overlaps** `mobile-native-kmp-ssoservice-untested` (2026-08-11, open)
  - `mobile-native-kmp-{create-listing-notimpl-stub, create-listing-stub, create-listing-submit-stub, createlisting-notimpl-stub}` — four IDs for the same "create-listing screen is a stub" finding across four consecutive tier1d runs
  - Dispatcher's slug-stem uniqueness gate (see `dispatcher-prompt.md` Phase 3) will keep only the highest-priority variant active

## Plans promoted
- `plans/code-review-api-core-rls-context-pool-not-conn.md` — deprecate `require_rls_context` middleware (docstring lies about tenant isolation; RLS GUCs set on a pooled connection don't survive to the handler's own connection; module header already flags it deprecated but symbol is still glob re-exported) · adversarial pass: passed

(Cap is 2 per run. Only 1 backlog row met the readiness gate this run — `score >= 3` is the bar, and the RLS-context row was the sole score-3 candidate; several score-2 fail-open findings in api-handlers/sso-rs qualify as security-relevant but were classified `candidate_vector: "bug"` by the tier1d generator and don't qualify for the security fast-track (`vector=security` + `confidence=high` + `score>=2`). Deliberate under-promotion to preserve quality-over-volume.)

## Auto-fix
- `no candidate` — no allowlist signal type (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, lockfile-drift, `stalled-review`) fired this run, and none could fire without live PR observation. Kill switch (`ROUTINE_AUTOFIX_DISABLED`) not set.

## Self-review findings
- Not queried this run (self-improvement file inspection is a cheap check but tangential to the buffer-low emergency)
- none

## Open questions
- Should the `mobile-native-kmp-cancellation*-swallowed*` triplet be manually collapsed to one canonical ID before promotion? Same for the ssoservice-no-test triplet and the create-listing-notimpl-stub quadruplet. Currently they'll consume 8 backlog slots for 3 distinct issues. Slug-stem dedup at claim time protects the dispatcher, but the backlog readability suffers.
- Why did the daily routine stall from 2026-08-08 through 2026-08-11? Operator action needed on the cloud cron (out of routine scope).

## Quiet day?
- No — 40 new backlog rows, 1 plan promoted, buffer-low emergency responded to.

## Goal violations
- None — all 17 goal checks passed (see `signals/2026-08-12.json` `goal_checks[]`). G16 (management artifacts) is a no-op because Phase 1.6 was skipped this run (deferred to the next healthy cloud tick — running the pm role rotation subagents inside a catch-up run would blow the token budget without materially helping the buffer-low emergency).
