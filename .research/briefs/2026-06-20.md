# 2026-06-20

## Since last run
- Merged PRs: **89** (#1440–#1615) — a 4-day catch-up window. Of those, **~63 application-code** PRs + ~17 dependabot / dispatcher state + 9 follow-up coverage tests. New peaks: epic-83 OTA integrations (#1454 N1-N4 native accounting, #1577 OTA HotelRateAmountNotif, #1560 combined rate/availability push, #1601 airbnb cross-org gate), MFA hardening burst (#1467 verify_recovery_code RLS, #1580 rate-limit, #1602 evict stale entries), enum-cast cluster on `rental.rs` (#1486 #1492 #1565 #1578), Booking.com OAuth (#1473 PAP-47), iOS SwiftUI listing-detail polish (#1541), mobile folder-organization (#1557 #1582 #1609 #1613).
- Open PRs touched: **6** newly active — #1606 (accounting list_contacts manager-gate PAP-281, 1d old), #1554 (RN app icons all sizes — 2d, needs-human-review label), #1567/#1568/#1569/#1574 (dependabot, 2d). No new stalled-review fires since the prior `dx-stalled-review-pr-988` and `dx-stalled-review-pr-1179` rows already track the older offenders.
- New / updated issues: **30 new** (#1438–#1614) + 53 updated existing. Notable opens: **#1607** (research-routine label — dispatcher stalled: stale coverage.json + 14 quarantined rows), **#1604** (Dispatcher backlog exhausted — coverage.json 2026-06-16 stale), **#1538** (Backend `test` job is not a required check on dev — red-test PRs merge freely), **#1522** (accounting UI bypasses shared API auth interceptor after @hey-api migration), **#1521** (payment-matching UI shipped in wrong app, broken auth + no i18n), **#1584** (PortalPrincipal bypasses reality_agency_members — admits any kind).
- Commits on `dev`: **49** since last run (`4829015b` → `8c598e88`); 34 application-code + 15 dispatcher state / version bumps / merge commits.
- Routine lag: **~100h (4d) since last run — `lag_warning` and `stale_routine_alert` fired**. Cloud cron likely skipped 4 daily slots; check the scheduler. Dispatcher (separate loop on planning branch) kept ticking — its commits dominate the `chore(research)` prefix this window.
- Phases that failed: **none** (Phase 1.5 + 1.6 intentionally skipped this catch-up run — see *Code review slice* and *Self-review findings*).

## Shipped
- **Epic-83 OTA integrations** — #1454 (N1-N4 native accounting MVP + @hey-api migration), #1453 (N5 bank-statement upload + payment matching PAP-210), #1577 (typed OTA HotelRateAmountNotif RQ/RS for Booking.com Story 83.2), #1560 (combined OTA rate/availability push with partial-failure outcome), #1556 (APNs HTTP/2 delivery wiring for OS push), #1502 (BIT-98 IG3 Booking.com credential encryption-at-rest contract test), #1473 (BIT-85 cross-tenant OAuth upsert hazard + manager gate PAP-47), #1539 (oauth: reject public client supplying client_secret with invalid_client), #1552 (OTA token-exchange gated on manager role of path org), #1601 (OTA manager-gate derived from TenantRole + airbnb cross-org test), #1615 (DB-backed Airbnb linked-listing list route).
- **MFA hardening cluster** — #1467 (verify_recovery_code RLS-enforce + cross-user IDOR test, BIT-…), #1580 (rate-limit MFA recovery-code verify, closes #1523), #1602 (evict stale entries + dedicated audit action, closes #1583).
- **DB enum-cast cluster on rental.rs** — #1486 (4 readers cast platform::text BIT-…), #1492 (cast remaining GUEST/CALENDAR_BLOCK/ICAL_FEED_COL readers), #1537 (cast enum write paths on rental_guests + forms #1008), #1565 (correct document get-by-id read path with enum ::text decode), #1578 (booking-create enum binds + dedupe BOOKING_COLUMNS + NULL-fee write path, closes #1535), #1551 (count_accessible_rls cast document_category, closes #1528), #1597 (round invoice lines once so header totals match items, #1522), #1598 (board_meetings list joins use u.name not u.full_name, closes #1586). #1599 made XXE/entity guards non-vacuous (closes #1591), #1562 added inbound OTA XXE/DTD namespace parity (closes #1519).
- **RLS / IDOR clean-ups** — #1460 (FORCE RLS on developer_oauth_apps/grants/api_key_usage_logs, BIT-76 #1404), #1517 (migrate IDOR test seed_membership to canonical common helper), #1507 (restore user_memberships seed + fix platform principal for IDOR), #1501 (handler-level IDOR regression for imports/feeds GH #1300), #1480 (list building/unit-scoped docs for members + unify preview gate), #1476 (RLS dev-tooling + test-suite cleanup GH #1340/#1341/#1366), #1470 (DSA report download self-ref regression guard GH #1201), #1561 (admit public agencies on imports via PortalPrincipal — flagged at review, see #1584), #1603 (pin PortalPrincipal admits-any-kind is still owner-scoped, #1584 finding 3), #1563 (behavioral cross-user RLS isolation for developer OAuth/usage), #1600 (write-path RLS probes for developer OAuth/usage tables, closes #1594).
- **Faults / documents / accounting front-end** — #1555 (UC-03 gap-79-1 wire useDeleteFault + useFaultStatistics into FaultsPage), #1610 (fix faults: confirm delete + use closed_count + refresh stats, closes #1588), #1559 (UC-06 Story 6.3 optimistic add for announcement comments), #1557 (mobile folder rename/move UI), #1582 (wire useFolderTree / FOLDER_TREE_QUERY_KEY into mobile document screens, closes #1532), #1609 (single source of truth for folder-tree flatten utils #1589 finding 2), #1613 (allow detaching a folder to root via PUT parent_id=null, closes #1589), #1611 (cover ScheduleDetailPageRoute download/retry toast wiring, closes #1587), #1545 (unbreak reality-web production build — /accounting QueryClient prerender), #1612 (refactor accounting: queryKeys factory + drop console.log stub #1522 finding 2), #1572 (mark_invitation_viewed idempotent, closes #1301), #1564 (assert mark_invitation_viewed preserves first viewed_at, closes #1524), #1608 (deterministic COALESCE-preservation guard for viewed_at, closes #1593).
- **iOS / mobile-native** — #1514 (82.1 SwiftUI project-setup screen-map), #1515 (epic-82 SwiftUI vs implementation mapping), #1516 (verify navigation routing deep-linking URL schemes), #1541 (iOS listing-detail favorites SwiftUI polish — FavoritesService session-gate), #1542 (iOS UI test for SearchView stale-response guard), #1536 (remove unused import failing spotless on dev post-#1447), #1450 (notification-pref-sync mobile push preference-aware), #1447 (home + search screens infinite scroll evidence AC-4).
- **DevOps / CI** — #1605 (ci backend: enforce rustfmt in required check job, closes #1592), #1531 (rustfmt #1527/#1523 fix red lint job), #1512 (clippy fixes in accounting routes post-#1454), #1526 (pm-devops confirm EAS workflows healthy), #1529 (ADR — pre-push fmt/clippy gate scope), #1530 (document required-check verification for security tests), #1540 (confirm app-tsx-merge-queue.yml actively gating).
- **Dispatcher follow-ups (closed-not-merged):** #1273 (rust-toolchain bump 1.94→1.100 superseded), #1500 (dispute/violation add_evidence IDOR — fixed elsewhere?), #1503 (booking_oauth_routes_tests seed_membership role), #1504 (duplicate migration 00181 force_developer_oauth_rls), #1505 (rental enum cast PAP-158 — superseded by #1486+#1492), #1506 (booking-oauth non-manager test seed role), #1511 (clippy accounting routes — superseded by #1512).

## Watch
- **🚨 #1607 (open, research-routine)** — *Research dispatcher stalled: stale coverage.json + 14 quarantined rows need cleanup.* Dispatcher is on a separate planning branch loop; this routine is the cloud routine. Filed by the dispatcher itself surfacing its own pipeline blockage. **#1604** (Dispatcher backlog exhausted — coverage.json 2026-06-16 stale) is the upstream cause. Action: human triage of `.research/management/coverage.json` + dispatcher quarantine queue.
- **#1538 (open, ci)** — *Backend `test` job is not a required check on dev — red-test PRs merge freely.* #1605 (ci backend: enforce rustfmt in required check) lands part of the gate-tightening but `cargo test` itself is still advisory. Risk: another #1426-class breakage can land unnoticed.
- **#1522 (open, follow-up)** — *Accounting UI bypasses shared API auth interceptor after @hey-api migration.* Cross-cutting auth wiring bug from the big #1454 migration. #1612 (refactor accounting queryKeys factory) addressed finding 2 only.
- **#1521 (open, follow-up)** — *Payment-matching UI shipped in wrong app, with broken auth + no i18n/types.* Functional gap in #1453 N5 bank-statement upload deliverable.
- **#1584 (open, follow-up, partial-fix)** — PortalPrincipal admits any kind on reality_agency_members reads; #1603 added the pin-test for owner-scoping but the broader rebind path is open.
- **Reverts:** none this run.
- **Churn hotspots this run (top 3):**
  - `backend/crates/integrations/src/booking.rs` — 3 PRs, ~4365 lines (4330+/35-); `runs_seen=1→2` → `repeated-churn` fired (+1, total score 1→2); healthy feature churn (epic-83 OTA scaffold + XXE guard).
  - `backend/servers/api-server/src/routes/mfa.rs` — 3 PRs, 1383 lines (1377+/6-); first-run hotspot; security-area hardening burst (#1467/#1580/#1602).
  - `backend/crates/db/src/repositories/rental.rs` — 2 direct PRs (#1486/#1492) + related sister fixes (#1505 superseded, #1565, #1578), 2883 lines (2860+/23-); repeated per-call-site enum-cast pattern suggests a missing helper.
- **Stale drafts / stalled review carried over:** #988 (epic, 13d → ~17d now, already in `dx-stalled-review-pr-988`), #1179 (already tracked as `dx-stalled-review-pr-1179`), #1197 / #1316 (older drafts — no fresh signal).
- **Closed-not-merged this run:** 7 — #1273 superseded by later rust-toolchain bump; #1500 (dispute/violation IDOR — likely superseded by other PAP work); #1503/#1506 (booking-oauth seed_membership role — superseded by #1517 + #1507); #1504 (duplicate migration superseded by clean rename); #1505 (rental enum cast PAP-158 — superseded by #1486 + #1492); #1511 (clippy accounting — superseded by #1512). All look like dispatcher-fast-path collisions; no new backlog rows needed.

## PRs stuck in draft despite approval
- none — not surveyed this catch-up run (would require live `gh pr list --draft` query against open queue; deferred to next normal run).

## PRs with verdict=changes, no fix-round progress in 24h
- none — not surveyed this catch-up run (would require dispatcher `assignments.json` read on `planning` branch; deferred to next normal run).

## Code review slice
- Segment reviewed: **none — Phase 1.5 skipped this run.**
- Experts: n/a
- Findings: **0**
- Next segment: **ppt-web-core** (last reviewed 2026-06-05 → 15d stale; oldest in `review_cursor`). Churn-aligned candidate would be `api-handlers` (mfa.rs hot) or `api-core` (booking.rs / rental.rs hot), but both reviewed within 5-10 days.
- Skipped: **yes** — catch-up run with 4d lag; expert review deferred so the brief stays focused on the 4d activity sweep. `review_cursor` unchanged.

## Screen-map status
- Total `docs/screens/` files: **127** unchanged (ppt: 80, reality: 47, mobile: 0 — not yet seeded).
- Drift signals this run: **4** — `screen-map-drift-pr-{1611,1610,1559,1555}-ppt`. PRs #1610 (faults confirm-delete UX), #1555 (faults useDeleteFault + statistics wiring) and #1559 (announcements UC-06 comment thread) are real route surface changes deserving screen-doc updates; #1611 (reports route test-only) is the heuristic false-positive caveat in action.
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not fetched this run (no `docs/screens/**` edits in this catch-up window).

## Backlog deltas
- **New:**
  - [2] `churn-hotspot-backend-crates-integrations-src-booking-rs` (was [1], booking.rs repeated-churn fired +1; cumulative — re-emit base churn-hotspot at +0).
  - [2] `screen-map-drift-pr-1611-ppt` — test-file only, heuristic.
  - [2] `screen-map-drift-pr-1610-ppt` — faults confirm-delete + closed_count UX without screen-doc.
  - [2] `screen-map-drift-pr-1559-ppt` — UC-06 announcement comments without screen-doc.
  - [2] `screen-map-drift-pr-1555-ppt` — gap-79-1 fault delete + stats wiring without screen-doc.
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-mfa-rs` — MFA security-hardening burst (#1467/#1580/#1602).
  - [1] `churn-hotspot-backend-crates-db-src-repositories-rental-rs` — repeated enum-cast pattern; suggests missing helper.
- **Bumped:** [1→2] `churn-hotspot-backend-crates-integrations-src-booking-rs` — runs_seen=1→2 across 2026-06-12/2026-06-20 (`repeated-churn` signal stacked +1; original `churn-hotspot` re-emitted with +0; same convention as the 2026-06-16 `forms.rs` entry).
- **Decayed (6, 2→1):** `code-review-mobile-native-kmp-deeplink-token-not-url-decoded`, `code-review-mobile-native-kmp-search-stale-response-race`, `test-gap-screen-map-drift-pr-1085-reality`, `test-gap-screen-map-drift-pr-1100-ppt`, `bug-risky-churn-pr-963-api-main-rs`, `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` — all hit the 14-day no-signal window.
- **Dropped (7, 1→0):** `churn-hotspot-mobile-native-listing-detail-kt`, `churn-hotspot-mobile-native-search-screen-kt`, `code-review-mobile-native-kmp-deeplink-android-bypasses-shared-router`, `refactor-churn-hotspot-mobile-announcements`, `refactor-churn-hotspot-mobile-documents`, `triage-dispatcher-mcp-push-large-file-issue-1014`, `triage-issue-951`.
- **Resolved:** `refactor-churn-hotspot-mobile-announcements-test` → `done` (PR #1543 merged 2026-06-17 — `refactor-churn-hotspot-mobile-announcements-test: stabilize AnnouncementsScreen test`, explicit PR-body ID match). Decay first dropped it to 0; resolution promoted to `done` for accurate history.

## Plans promoted
- **none this run** — no open backlog item reached score ≥ 3 (or score ≥ 2 on the security fast-track) AND `status == "open"` AND lacked an existing plan. The 4d activity bumped only `booking.rs` (score 2, refactor — not enough on its own; benign feature churn). All security/bug score-3 ready items already have plans from prior runs (`security-llm-doc-idor`, `bug-ios-searchview-uncompilable`, 4× `code-review-reality-web-*`, `code-review-mobile-rn-report-fault-fake-submit`).

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The 4 `screen-map-drift` signals are medium-confidence heuristic (review candidates per the routine rules); the 3 `churn-hotspot` signals are analytical, not actionable (cumulative-allowlist excludes them from Phase 5 entirely); no `fixme-in-merged-code` fired (catch-up run did not scan per-PR diff bodies for TODO additions — deferred); no lockfile-drift `dep-update-noise` matched the action shape; no fresh `stalled-review` candidate (the carry-over rows are already in `seen_signals`).

## Self-review findings
- `.research/self-improvement/findings.json` absent — none.

## Open questions
- **#1607 — dispatcher stalled.** Routine surfaced it but cannot triage the dispatcher's coverage.json staleness or the 14-row quarantine queue from inside its own commit budget. **Human action needed** on the dispatcher loop (planning branch, `.research/management/assignments.json` quarantine cleanup + coverage.json refresh via `/ppt-project-management scan`).
- **Cloud cron missed 4 daily slots** between 2026-06-16 03:25 UTC and 2026-06-20 08:18 UTC. The routine itself can't restart the scheduler — operator should check `code.claude.com` routine config + last error log.
- **rental.rs enum-cast pattern** — five PRs over two runs all add per-call-site `::text` casts. Is a single sqlx custom-type or a shared `cast_platform_enum()` helper preferable to per-query fixes? Not promoting yet, but a refactor plan after 1–2 more enum-cast cycles would be defensible.
- **Backend `test` job required-check escalation (#1538)** — #1605 made `rustfmt` required; should `cargo test` follow? #1426-class breakages keep landing because the gate is advisory. PM-devops candidate.
- **Skipped Phase 1.5 + 1.6 this run.** Both cursors (`review_cursor`, `pm_cursor`, `coverage_cursor`) unchanged. Next normal run picks up `ppt-web-core` (oldest at 15d) for review and `pm-security` (index 5; last 2026-05-27, 24d stale) for the rotating role.

## Quiet day?
- **No.** 89 PRs merged in the 4d window, 30 new issues, 1 routine `lag_warning`, 8 new backlog vectors (including a repeated-churn bump), 1 resolution, 0 plans promoted (no fresh ready candidate), Phase 1.5 + 1.6 intentionally deferred. `stats.runs` 40 → 41, `vectors_created` 173 → 179, `plans_created` unchanged at 12, `auto_fix_count` 0.
