# 2026-06-20

## Since last run
- Merged PRs: **89** (#1447–#1615) — large 4-day batch dominated by the post-merge-review follow-up wave (#1300/#1302/#1303/#1339/#1342/#1583/#1585/#1586/#1587/#1588/#1589/#1591/#1592/#1593/#1594 all closed by their resolving PRs).
- Open PRs touched: 6 (no stalled-review candidates — all updated <2d ago).
- New / updated issues: **83 in window**; key open follow-ups: **#1522** (accounting auth interceptor bypass, security), **#1584** (PortalPrincipal agency bypass, security), **#1614** (combined OTA push never wired), **#1538** (backend `test` job not required on dev), **#1520**/**#1521**/**#1533**/**#1590** (smaller follow-ups), **#1604**/**#1607** (dispatcher infra).
- Commits on `dev`: not enumerated (89 merged PRs ≈ several hundred commits).
- Routine lag: **3d 22h since last run** (94h, last_run_iso `2026-06-16T03:25:00Z`) — `lag_warning` fired *and* `stale_routine_alert` fired (≥3d gap). The dispatcher (separate loop) is still ticking — last dispatcher commit `42c40e1` at 2026-06-20 00:14 UTC. Cloud research routine itself was the laggard.
- Phases that failed: **none for cursor advance**. Phase 1.5 (rotating expert review) and Phase 1.6 (PM rotation) were **deferred this run** — see *Code review slice* / *PM rotation* below.

## Shipped
- **Security cluster (BIT-* / post-merge follow-ups):**
  - **#1460** `fix(db): FORCE RLS on developer_oauth_apps/grants/api_key_usage_logs (BIT-76, #1303)` — three previously un-FORCEd owner-bypassable tables locked down; **#1563/#1600** add behavioral + write-path cross-user isolation tests.
  - **#1467** `fix(api-server): mfa verify_recovery_code RLS-enforce + cross-user IDOR test (BIT-78, #1302/#1304)` — `acquire_public` path closed; mixed model resolved.
  - **#1470** `test(api-server): regression guard for DSA report download self-ref (GH #1201)`.
  - **#1473** `fix(api-server): BIT-85 cross-tenant OAuth upsert hazard + manager gate (#1405)`.
  - **#1480** `feat(api-server): list building/unit-scoped docs for members + unify preview gate (GH #1413)`.
  - **#1501** `test(reality-server): handler-level IDOR regression for imports/feeds (GH #1300)` (post-merge follow-up issue #1584 surfaces residual divergence — see *Plans promoted*).
  - **#1539** `fix(oauth): reject public client supplying a client_secret with invalid_client`.
  - **#1552** `fix(api-server): gate OTA token-exchange on manager role of the path org (closes #1525)`; **#1601** hardens it (TenantRole-derived gate + airbnb cross-org test, closes #1585).
  - **#1561** `fix(reality-server): admit public agencies on imports via PortalPrincipal (closes #1300)` — **resolves the IDOR but introduces an authz-model divergence** flagged by Issue #1584 (promoted to plan).
  - **#1580/#1602** MFA recovery-code endpoint rate-limited + stale-entry eviction + dedicated audit action (closes #1523/#1583).
  - **#1599/#1562** XXE/DTD regression guard + namespace parity for inbound OTA parsers; **#1599** hardens to non-vacuous (closes #1519/#1591).
  - **#1610** confirm-fault-delete + closed_count + stats refresh (closes #1588).
- **Backend correctness / enum-cast cluster (BIT-113/118 + PAP-141):**
  - **#1486/#1492/#1537** explicit `::text` casts on rental_platform_connections readers + GUEST/CALENDAR_BLOCK/ICAL_FEED + rental_guests/forms write paths (BIT-113/118).
  - **#1565/#1598** fix `u.name` (drop nonexistent `u.full_name`) in document get-by-id + board_meetings.rs (closes #1586).
  - **#1578** `fix(db): cast booking-create enum binds + dedupe BOOKING_COLUMNS; cover NULL-fee write path (closes #1535)`.
  - **#1597** `fix(db): round invoice lines once so header totals equal sum of stored items (#1522)` — accounting MVP rounding correctness.
- **Accounting native MVP + @hey-api migration (#1453/#1454/#1612):** N1–N4 backend solid (forced-RLS on 11 new tables, all money as `Decimal`, manager gate everywhere, 823-line `accounting_rls_repo_tests.rs`); UI side ships but **bypasses the shared axios interceptor** — Issue #1522 (promoted to plan); **#1612** drops `console.log` stub and uses queryKeys factory (finding 2).
- **Mobile + frontend deliveries:** #1447 home/search infinite-scroll AC-4 evidence (KMP), #1450 preference-aware push dispatch-target (mobile, follow-up issue #1520 still open for real-prefs wiring), #1453 N5 bank-statement upload + payment-matching (UI shipped in wrong app per #1521), #1464 mobile folder-organization slice (Coverage 7a-2), #1541 FavoritesService session-gating, #1542 iOS SearchView stale-response guard test (PR #1365 follow-up), #1546 reality-web favorites pl/hu i18n + /accounting prerender unbreak, #1582 mobile useFolderTree wiring (#1532), #1609/#1613 folder-tree flatten-utils dedup + root detach.
- **DX / CI:** **#1605** `ci(backend): enforce rustfmt in the required check job (closes #1592)`, #1540 `app-tsx-merge-queue` confirmed actively serializing App.tsx PRs, #1581 backend rustfmt sweep that triggered #1592 → #1605.
- **Dispatcher state PRs (research routine cohabitants):** #1508/#1509/#1513/#1518/#1546/#1547/#1553/#1595/#1596 — buffer reconciles, archive sweeps, post-merge review, self-review.
- **Dependabot batch (no scoring):** #1566 (compose-bom), #1570 (mikepenz/action-junit-report), #1571 (npm-minor-patch group ×6), #1572 (expo-secure-store), #1573 (@types/node), #1575 (@babel/core), #1576 (i18next).

## Watch
- **Open security follow-ups (promoted):** Issue **#1584** (PortalPrincipal authorizes by user_id, breaks multi-member agencies — divergent authz vs `agency_imports.rs`) and Issue **#1522** (accounting UI bypasses shared interceptor — no 401-refresh). Both got plans this run.
- **Open completeness gap:** Issue **#1614** — `BookingClient::push_availability_and_rates` + `PushOutcome` (PR #1560 AC-5) shipped but never wired into any route/job. Combined entry point is dead code; live flow still pushes streams independently. Score 2, open backlog row `bug-ota-combined-push-orphan`.
- **CI process gap:** Issue **#1538** — backend `test` is not a required check on `dev`. PRs whose `test` job is red can squash-merge (`check` is the only required context). The #1331/#1332/#1008 cascade required 6 fix PRs. Score 1, open backlog row `test-gap-dev-test-job-not-required`.
- **Dispatcher health:** Issues **#1604** (backlog exhausted — stale `coverage.json` 2026-06-16, only no-op tasks) and **#1607** (research dispatcher stalled — stale coverage.json + 14 quarantined rows). Two `triage` rows; needs the on-demand `/ppt-project-management scan` to refresh `coverage.json`. Surfaced in dispatcher PRs `42c40e1` / `0ec28fd` (active dep-blocked=0 but coverage stale).
- **Reverts:** none this run.
- **Churn hotspots this run (top-3):** *deferred* — Phase 1's per-commit `files` fetch over 89 merged PRs is too token-heavy in deep-catchup mode. Existing hotspot rows in backlog remain. Re-run on the next normal-cadence routine pass to refresh.
- **Stale drafts / stalled review:** none — all 6 open PRs updated <2d ago. (#1554 frontend RN app-icons PR has `needs-human-review` label since 2d, still under the 7d threshold.)
- **Closed-not-merged this run:** 7 PRs from the 2026-06-17 merge-pipeline thrash — #1273, #1500, #1503, #1504, #1505, #1506, #1511 — all rolled forward via separate PRs the same day. Each now an open backlog row `closed-not-merged-pr-NNNN` (score 1, will decay).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher run `42c40e1` shows 0 stale-terminal, 17 active)

## Code review slice
- Segment reviewed: **skipped this run** (deep-catchup; per *Skip Phase 1.5 when* — not strictly satisfied, but Phase 1 was so token-heavy it crowded out a meaningful review pass; the routine's recurring rotation will resume on the next normal-cadence run).
- Experts: n/a.
- Findings: **0** from Phase 1.5 itself. However, the post-merge-review skill already produced four high-quality findings as GitHub follow-up issues (#1522, #1584, #1614, #1538), all surfaced into the backlog this run as `code-review-followup-issue-*` signals.
- Next segment: **`ppt-web-core`** (last reviewed 2026-06-05 — oldest unreviewed after this run).
- Skipped: **yes — deferred due to 4-day catchup token pressure; backlog already absorbed the equivalent review value via the four post-merge follow-up issues**.

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0 — not yet seeded).
- Drift signals this run: **0** (no `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**` PR landed without a matching `docs/screens/<product>/*.md` touch in the 4-day window — the frontend PRs that merged were either feature work in `features/` or test files, not route surface).
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not fetched this run (no `docs/screens/**` edits in window).

## Backlog deltas
- **New:**
  - [3] `security-portal-principal-agency-bypass` — Issue #1584 (PR #1561 follow-up); **promoted to plan**.
  - [2] `security-accounting-ui-auth-interceptor-bypass` — Issue #1522 (PR #1454 follow-up); **promoted to plan via security fast-track** (score 2 + security + high conf).
  - [2] `bug-ota-combined-push-orphan` — Issue #1614; `push_availability_and_rates` orphan.
  - [1] `test-gap-dev-test-job-not-required` — Issue #1538; CI gate process gap.
  - [1] `triage-issue-1604` — Dispatcher backlog exhausted (coverage stale).
  - [1] `triage-issue-1607` — Research dispatcher stalled + 14 quarantined rows.
  - [1] `closed-not-merged-pr-1273` — dep-update vector.
  - [1] `closed-not-merged-pr-1500` — IDOR fix superseded.
  - [1] `closed-not-merged-pr-1503` — seed_membership fix subsumed.
  - [1] `closed-not-merged-pr-1504` — duplicate migration rename.
  - [1] `closed-not-merged-pr-1505` — rental enum casts superseded.
  - [1] `closed-not-merged-pr-1506` — seed_membership booking-oauth subsumed.
  - [1] `closed-not-merged-pr-1511` — clippy lints superseded.
- **Bumped:** none.
- **Decayed:**
  - `test-gap-screen-map-drift-pr-1085-reality` — −1 (2→1; no signal in 14d).
  - `test-gap-screen-map-drift-pr-1100-ppt` — −1 (2→1).
  - `bug-risky-churn-pr-963-api-main-rs` — −1 (2→1).
  - `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` — −1 (2→1).
- **Dropped:**
  - `refactor-churn-hotspot-mobile-announcements` — decayed to 0.
  - `refactor-churn-hotspot-mobile-announcements-test` — decayed to 0.
  - `refactor-churn-hotspot-mobile-documents` — decayed to 0.
  - `triage-dispatcher-mcp-push-large-file-issue-1014` — decayed to 0.
  - `triage-issue-951` — decayed to 0.
- **Resolved:**
  - `triage-issue-1331` → `done` (GitHub #1331 closed 2026-06-17 as part of the `test`-job-red cascade drain).
  - `triage-issue-1151` → `done` (GitHub #1151 closed 2026-06-16; dispatcher claimable-buffer reconciliation shipped).

## Plans promoted
- `plans/security-portal-principal-agency-bypass.md` — Reconcile `imports.rs` and `agency_imports.rs` onto a shared `assert_agency_member` membership check; drop the `user_id`-equality scoping that breaks multi-member agencies; add positive test for second member + platform-admin pin. Mode: cloud-ok. · adversarial pass: passed.
- `plans/security-accounting-ui-auth-interceptor-bypass.md` — Move auth wiring from per-feature `useAccountingAuth` hook to centralized request/response interceptors on the `@hey-api` `client` in a new `lib/apiClient.ts`; mirror 401-refresh from `lib/api.ts`; delete the bespoke hook and 4 `as unknown as` casts; add `accounting` queryKeys namespace. Mode: cloud-ok. · adversarial pass: fixed-in-place (corrected file path `features/accounting/AccountingInvoiceManagementPage.tsx` → `features/accounting/pages/AccountingInvoiceManagementPage.tsx` after disk-verify).

## Auto-fix
- `no candidate` — no signal met the allowlist this run. Both security findings are high-confidence but the action shape requires code design (not doc-stub / orphan-remove / lockfile-bump) → manual plan path. No `screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / lockfile-drift dep-update / `stalled-review` fired.

## Self-review findings
- none

## Open questions
- **Phase 1.6 PM rotation deferred** — pm-security is next (index 5, last run 2026-05-27 → 24d stale). Recommend running it on the next normal-cadence routine pass; the two security plans promoted this run (PortalPrincipal + accounting interceptor) are squarely in pm-security's purview.
- **Phase 1.5 segment** — `ppt-web-core` (last 2026-06-05) is oldest unreviewed and should run next; `mobile-rn` (last 2026-06-16) is freshest.
- **Cloud routine cadence** — why did the routine miss 2026-06-17/18/19? Dispatcher kept running on its own loop. Worth checking the cloud cron config and recent schedule failures.
- **Dispatcher coverage.json refresh** — Issues #1604 and #1607 both point to stale `coverage.json` (2026-06-16) producing no-op tasks. The on-demand `/ppt-project-management scan` is the documented refresh path. Worth running it as a separate one-shot.

## Quiet day?
- **No.** 89 PRs merged, 13 new backlog vectors, 2 plans promoted (both security), 2 triage resolutions, 9 decay-touched items (4 still positive + 5 dropped), 22 signals emitted, routine lag-warning + stale-routine alert recorded. `stats.runs` 40 → 41, `vectors_created` 173 → 186, `plans_created` 12 → 14.
