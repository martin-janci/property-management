# 2026-06-20

## Since last run
- Merged PRs: **100** (#1469–#1619, span 1469–1619; ~62 application-code + ~30 dispatcher state + 7 dependabot + 1 stale-branch reconcile). Last cursor `last_pr_seen=1439`; new cursor `last_pr_seen=1620`.
- Open PRs touched: **6** newly active (5 dependabot dep-bumps awaiting review: #1567, #1568, #1569, #1574; #1554 RN app icons created 2d ago; #1620 reality-server platform-kind regression test). No new stalled reviews this run.
- New / updated issues: **81** since last run; new cursor `last_issue_seen=1614`. Most are closed follow-ups from merged review cycles; 5 net-new open issues warranting triage (see below).
- Commits on `dev`: ~150 since `4829015b` → `079dda43` (mix of application-code commits + dispatcher state PRs).
- Routine lag: **🚨 ~4d 7h since last run** (last 2026-06-16T03:25Z). `lag_warning` fires. `stale_routine_alert` also fires (>= 3d). Cloud cron likely paused; dispatcher (planning branch) was independently active in window.
- Phases that failed: **none** — but Phase 1.5 (rotating expert review) and Phase 1.6 (PM rotation) **skipped this run** due to lag-mode catch-up priority; rotation cursors unchanged. Next run resumes normal rhythm.

## Shipped
- **#1469–#1499, #1501–#1502, #1507** — 4d firefight around the #1437 dev-compile breakage from PR #1426 (last run's CRITICAL). Pattern: db enum-cast hotfixes (BIT-110/113/114/118, PAP-141 follow-ups), RLS migration coverage fillers (#1471/#1490), Booking.com encrypted-credentials surface (#1472/#1502), and clearing the test seed gap (#1507). Dev backend compile is green again.
- **#1510, #1517** — pm-qa tenant-aware test helper (#1370) and migration of IDOR seed_membership to canonical common helper (#1373) — both Phase 1.6 / Scrum-Master findings from prior runs.
- **#1539, #1580, #1602** — auth/MFA hardening: OAuth public-client `client_secret` rejection (RFC 6749), MFA recovery-code rate-limit (#1523), MFA recovery limiter memory leak + audit action (#1583).
- **#1552, #1601, #1606** — OTA / accounting manager-gate consistency: token-exchange manager-gate (#1525), derive-from-TenantRole hardening (#1585), PAP-281 `list_contacts` manager-only.
- **#1561, #1601, #1603, #1620 (open)** — reality-server `PortalPrincipal` work after #1300/#1584 surface: portal admit + pin tests. Issue **#1584 remains OPEN** — root agency_id=user_id mismatch is not closed by these PRs (see Watch).
- **#1545, #1555, #1610, #1611, #1612, #1613, #1616, #1618** — frontend follow-up cleanup from #1454 (accounting) and the documents/folders refactor: queryKeys factory, @hey-api auth interceptor centralization (closes #1522), Faults page wiring (#1588), reports download/retry coverage (#1587), folder root-detach (#1589), folder-tree wiring pin (#1590).
- **#1577, #1617** — OTA integration: typed `HotelRateAmountNotif` models (Story 83.2) and combined availability+rate push wired into the listing-push route (#1614 follow-up).
- **#1619** — mobile-native infinite-scroll race + transition pin (#1533 follow-up).
- **#1605** — CI: rustfmt enforced in required `check` job on backend (closes #1592). Removes a lint gap surfaced in last run's pm-devops slice.
- **#988, #1179, #1197, #1316** — long-stalled drafts/PRs from prior briefs all **merged** in window (Playwright E2E framework epic, epics catalog backfill, OAuth integration tests, document folder backend promote).
- **#1514, #1515, #1516, #1526, #1529, #1530, #1540, #1542, #1543, #1544, #1548, #1549, #1550, #1556, #1557, #1558, #1559, #1560, #1565, #1566 (deps), #1571 (deps), #1572 (deps), #1573 (deps), #1575 (deps), #1576 (deps), #1578, #1579, #1581, #1582, #1597, #1598, #1599, #1600, #1608, #1609, #1615** — wider mix: SwiftUI Story 82 mapping + screen-map docs, dispatcher state PRs, marketplace deterministic guards, board_meetings u.name fix (#1586), XXE/entity guards non-vacuous (#1591), write-path RLS probes (#1594), and the Airbnb linked-listing list route (#1615).

## Watch
- **🚨 P0 — Research dispatcher itself is stalled.** Issues **#1604** (Dispatcher backlog exhausted, coverage.json 2026-06-16 stale) and **#1607** (research-routine label: 14 quarantined rows need cleanup) both OPEN. Same window the daily routine was stale. Operator action needed to refresh `coverage.json` (run `/ppt-project-management scan` locally) and drain quarantine.
- **#1538 (OPEN, [ci])** — Backend `test` job is **not** a required check on `dev`; red-test PRs merge freely. Direct enabler of last run's #1437 catastrophe (`integrations` crate compile-break). Related triage row `triage-issue-1331` (older variant of the same gap).
- **#1521 (OPEN, follow-up)** — Payment-matching UI shipped to wrong app + broken auth + no i18n/tests (PR #1453 follow-up). Architectural placement bug; needs human triage to decide owning app.
- **#1584 (OPEN, follow-up)** — `PortalPrincipal` bypasses `reality_agency_members` via `agency_id=user_id` mismatch (PR #1561). PR #1603 added coverage; root mismatch unfixed. Security follow-up requiring design call.
- **Reverts:** none this run (the open #1620 stacks on #1584 thread; no rollback path activated).
- **Churn hotspots this run (top 3):** *not computed* — Phase 1 hot-file ranking skipped in lag-mode catch-up (the per-PR `files` walk over 100 PRs would burn budget without proportionate signal). Repeated-churn history unchanged; existing `repeated-churn-backend-servers-api-server-src-routes-forms-rs` row remains at score 1.
- **Stale drafts / stalled review:** none new this run — every prior-brief stalled PR (#988, #1179, #1197, #1316) merged in window. Cleared.
- **Closed-not-merged this run:** 12 — #1488, #1493, #1494, #1496–#1498, #1500, #1503–#1506, #1511. All superseded by other fixes during the #1437 firefight; no scoring (mechanical churn while the team converged on the right hotfix).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (planning branch's `assignments.json` not queried this run — dispatcher itself flagged stalled in #1607)

## Code review slice
- Segment reviewed: **skipped** (reason: lag-mode catch-up — 4-day backlog dominates; Phase 1.5 deferred to next run to avoid stacking review work on top of catch-up signal extraction).
- Experts: n/a.
- Findings: **0** (no new `code-review-finding` signals this run).
- Next segment: **ppt-web-core** (last reviewed 2026-06-05 — oldest unreviewed; will run next normal-rhythm routine).
- Skipped: **yes — lag-mode catch-up**.

## Screen-map status
- Total `docs/screens/` files: **129** (ppt: 80, reality: 47, mobile-rn (RN): 0, mobile-native (SwiftUI): 2 — PR #1514 added 82.1 SwiftUI project-setup; PR #1541 reality-mobile sync).
- Drift signals this run: **0** computed — full per-PR `files`/route walk over 100 PRs was skipped in lag-mode. The route-touching PRs in window (e.g. #1545 reality-web `/accounting` prerender + favorites i18n, #1582 mobile document screens) all *did* touch screen docs or app-config files in the same diff; no obvious drift surfaced from PR-title scan.
- Orphan screens this run: **0** computed (same reason).
- Last `screen-map.yml` CI run on `main`: not fetched (no immediate trigger).

## Backlog deltas
- **New:**
  - [1] `triage-issue-1538` — Backend test job not a required check on dev (CI governance).
  - [1] `triage-issue-1521` — payment-matching UI in wrong app + broken auth + no i18n/tests (follow-up #1453).
  - [1] `triage-issue-1604` — Dispatcher backlog exhausted: coverage.json 2026-06-16 stale (research-routine).
  - [1] `triage-issue-1607` — Research dispatcher stalled: 14 quarantined rows need cleanup (research-routine).
  - [1] `triage-issue-1584` — PortalPrincipal bypasses reality_agency_members on agency_id=user_id (security follow-up).
- **Bumped:** none — Phase 1.5/1.6 skipped; no per-PR signal stacking on existing rows.
- **Decayed (−1, status remains open):**
  - `bug-risky-churn-pr-963-api-main-rs` (2→1)
  - `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` (2→1)
  - `test-gap-screen-map-drift-pr-1085-reality` (2→1)
  - `test-gap-screen-map-drift-pr-1100-ppt` (2→1)
  - `code-review-mobile-native-kmp-deeplink-token-not-url-decoded` (2→1)
  - `code-review-mobile-native-kmp-search-stale-response-race` (2→1)
- **Dropped (decayed to 0):**
  - `triage-dispatcher-mcp-push-large-file-issue-1014` — superseded conceptually by #1604/#1607.
  - `triage-issue-951` — Deploy blocker ESIGN_TOKEN_SECRET — no new evidence in 14d.
  - `refactor-churn-hotspot-mobile-announcements`, `refactor-churn-hotspot-mobile-announcements-test`, `refactor-churn-hotspot-mobile-documents` — single-run churn signals without follow-on.
  - `churn-hotspot-mobile-native-listing-detail-kt`, `churn-hotspot-mobile-native-search-screen-kt` — same.
  - `code-review-mobile-native-kmp-deeplink-android-bypasses-shared-router` — single-run finding without follow-up; decay to 0.
- **Resolved:**
  - `dx-stalled-review-pr-988` → `done` (PR #988 Playwright E2E framework epic merged 2026-06-16; long-stalled draft cleared).
  - `dx-stalled-review-pr-1179` → `done` (PR #1179 epics docs backfill merged 2026-06-16; stalled review cleared).

## Plans promoted
- **none this run.** 6 plans are already active (`ready` status, all score≥3): `code-review-mobile-rn-report-fault-fake-submit`, `code-review-reality-web-{share-comparison-404,listing-page-ssr-crash,realtor-mgmt-untranslated}`, `bug-ios-searchview-uncompilable`, `security-llm-doc-idor`. Promoting more would dilute the implementer queue while the lag-window backlog is unprocessed.
- **Note — stale plan source paths (review candidate, not auto-resolved):** `security-llm-doc-idor` plan still cites `backend/servers/api-server/src/routes/ai.rs`, but that file no longer exists — `ai/` is now a directory (`ai/voice.rs`, `ai/sessions.rs`, `ai/llm.rs`, etc.). The plan's *Files* section needs updating before the implementer picks it up; the IDOR may or may not still be present in the split-out handlers. Left as `ready` pending plan-author rewrite; not auto-flipped to `done` because file-deletion-as-resolution would be wrong here (refactor, not fix).

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The 5 new triage signals are score 1 (untriaged-issue baseline) and `triage` vector is auto-fix ineligible (no allowlist action shape). `lag_warning` and `stalled-review-resolved` are log-only.

## Self-review findings
- `.research/self-improvement/findings.json` absent — none.

## Open questions
- **Decide who runs `/ppt-project-management scan` to refresh `coverage.json`** — `#1604` and `#1607` both confirm the dispatcher is starving on stale 2026-06-16 coverage. Local refresh is the documented remedy (skill explicitly says "do NOT run scan in the cloud routine"). Until then, the implementer-via-dispatcher pipeline produces no-op tasks.
- **#1538 — promote test-job-required-check to a real plan?** Score 1 right now (untriaged), but it's the structural enabler of catastrophe-class events (#1437 last run). On next normal-rhythm run, consider score-bumping after Phase 1.5 corroborates the gap, then promote.
- **#1521 — owning app for payment-matching UI?** Reality vs. PPT decision is a product call; not actionable until the user adjudicates.
- **Should the daily routine cloud cron be re-armed?** 4-day silence suggests the trigger lapsed. If this run is the first after operator manually restarted, schedule check needed.

## Quiet day?
- **No** — explicit. 100 merged PRs in window (single biggest cumulative window since the routine started), 5 new triage signals (4 are operational governance — CI required-check gap, dispatcher starvation, PortalPrincipal mismatch, payment-matching app-placement), 6 backlog rows decayed, 8 dropped, 2 stalled-review rows resolved. `stats.runs` 40 → 41, `quiet_days` unchanged (1), `vectors_created` 173 → 178.

## Goal violations
- **G2** (advance state): passes — `last_pr_seen 1439→1620`, `last_issue_seen 1437→1614`, `last_commit_sha 4829015b→079dda43`, `seen_signals` grew by 9.
- **G5** (decay applied): passes — all open items with `updated_at ≤ 2026-06-06` either lost 1 point or moved to `dropped`.
- **G16** (management artifacts): **n/a** — Phase 1.6 skipped this run; `pm_cursor.next_index` unchanged (still 5, pm-security next).
- Phase 1.5 and Phase 1.6 skips are honest operational tradeoffs in lag-mode catch-up, not gate failures — both are surface-area "skipped" reported in their respective sections.
