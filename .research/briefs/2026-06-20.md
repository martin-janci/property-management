# 2026-06-20

## Since last run
- Merged PRs: **129** (#1440–#1622)
- Open PRs touched: 5 (incl. #1554 RN app-icon all-sizes draft, 4 dependabot PRs)
- New / updated issues: 83 (incl. #1607 dispatcher coverage stale, #1604 dispatcher backlog exhausted, #1584 PortalPrincipal bypass follow-up, #1538 backend test-job not required)
- Commits: 193 on `dev` (#1440 → #1622 squash chain)
- Routine lag: **4d 16h** (112h since 2026-06-16T03:25Z) — `lag_warning` + `stale_routine_alert` emitted; cloud cron missed 3 daily slots — operator check needed
- Phases that failed: none

## Shipped
- **#1622** `feat(reality-server): in-app delivery for saved-search alerts (#983 story 16.3)` — first chunk of epic-16 saved-search delivery.
- **#1621** `fix(ppt-web): relocate payment-matching to ppt-web with correct api-server auth (closes #1521)` — payment-matching now under correct auth scope; touched 2 routes for ppt + 2 for reality (drift signals fired).
- **#1620** `test(reality-server): pin platform-kind caller stays user-scoped on portal imports` — covers #1584 finding #3 (PortalPrincipal bypass follow-up).
- **#1619** `fix(mobile-native): close infinite-scroll in-flight double-fetch race + pin transition (closes #1533)` — KMP race fix.
- **#1617** `fix(integrations): wire combined OTA availability+rate push (closes #1614)` — `hotfix-no-test-pr-1617` (no test diff).
- **#1611** `test(reports): cover ScheduleDetailPageRoute download/retry toast wiring (closes #1587)` — but touched ppt-web routes/, drift signal fired (heuristic — it's a test file).
- **#1610** `fix(faults): confirm delete + use closed_count + refresh stats on delete (closes #1588)`.
- **#1608** `test(marketplace): deterministic COALESCE-preservation guard for viewed_at`.
- **#1606** `feat(accounting): manager-only role gate for list_contacts (PAP-281)` — `unchecked-todo-pr-1606` (1 unchecked).
- **#1565** `fix(db): correct document get-by-id read path (u.name + enum ::text decode)` — no tests.
- **#1561** `fix(reality-server): admit public agencies on imports via PortalPrincipal` — no tests; follow-up issue #1584 already filed by author.
- **#1554** drafted RN app-icon flow (still open — closes the one remaining open action-list item).
- **#1545** `fix(reality-web): unbreak production build — /accounting QueryClient prerender + …` — combined with #1453 (added accounting page) — both fired screen-map-drift.
- **#1454** `feat: accounting routes` — 4 new ppt-web routes; **largest drift signal of run**.
- **#1485** Booking.com client timeout + Retry-After + RateLimited.
- **#1471/#1476/#1477/#1501/#1507** RLS + IDOR test coverage hardening wave (most carry 3–5 unchecked checkboxes — followed up as security backlog rows).
- **Bulk-batch (87 PRs not individually listed)** — full list `gh pr list --state merged --base dev --limit 130 --json number,title,mergedAt`. Highlights: enum-cast hardening across rental/forms (#1469/#1492/#1499/#1537/#1551), dispatcher state PRs, dependabot batch (8 PRs, noise).

## Watch
- **🚨 Dispatcher** — Issue #1607 (open) "Research dispatcher stalled: stale coverage.json + 14 quarantined rows need cleanup"; Issue #1604 "Dispatcher backlog exhausted — coverage.json (2026-06-16) stale, producing only already-shipped no-op tasks". The routine-fire payload received this run (`buffer-low: claimable=0/72, 20 partial coverage stories, remaining gaps mostly human-gated/quarantined; please plan fresh claimable work`) confirms — out of scope for this routine (which only writes `.research/`), but the cloud delivery loop needs operator attention.
- **🚨 Stale-routine alert** — `state.json` `last_run_iso = 2026-06-16T03:25Z`, 4 days old. Cloud cron likely paused.
- **Reverts:** none this run (the entire window is forward-fix flow).
- **Churn hotspots this run (top 3):**
  - `backend/crates/integrations/src/booking.rs` — **1700 lines** (runs_seen=2 → `repeated-churn` fired)
  - `backend/servers/api-server/tests/document_folder_tests.rs` — 1147 (runs_seen=1)
  - `backend/servers/api-server/src/services/push_fanout.rs` — 1010 (runs_seen=2 → `repeated-churn` fired)
- **Stalled-review carry-overs:** #988 (~17d), #1197 (~11d), #1316 (~7d) — no movement this window; not re-emitted (still in `seen_signals`).
- **Closed-not-merged batch (26)** — many are squash-rebase pairs (`#1503`/`#1506`, `#1500`/`#1610`); 24 emitted as `closed-not-merged-pr-*` process rows. Won't impact code health unless an idea is genuinely abandoned — let next rotation triage. Notable: `#1500 fix(api-server): org-scope IDOR gate on dispute/violation add_evidence` — closed unmerged; needs verification that the security gap isn't open.
- **Open follow-up issue #1584** — `PortalPrincipal bypasses reality_agency_members and rides on agency_id=user_id mismatch (PR #1561)` — security follow-up filed by the same PR author; #1561 still landed without tests this run (the test followed in #1620 for finding #3).

## PRs stuck in draft despite approval
- none (the 4 open PRs are `decision=null` not approved)

## PRs with verdict=changes, no fix-round progress in 24h
- none (no live changes-verdict rows on the planning branch this run — dispatcher buffer-exhausted state means no fresh review-state PRs flow through the rotation)

## Code review slice
- Segment reviewed: **ppt-web-core** (reason: oldest unreviewed at 15 days — 2026-06-05; backend churn was in `backend/crates/integrations/` which no segment cleanly owns, so churn alignment didn't override).
- Experts: frontend.
- Findings: **3** — see Backlog deltas for `code-review-ppt-web-core-ws-stale-token-after-refresh` (Medium, +2, high-conf), `code-review-ppt-web-core-errorhandler-i18n` (Medium, +2, high-conf), `code-review-ppt-web-core-lib-api-dead-default-export` (Low, +1, high-conf). None promoted to a plan (max score is 2, below the +3 readiness threshold).
- Next segment: **mobile-native-kmp** (last reviewed 2026-06-06 — oldest after this run).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0 — not seeded).
- Drift signals this run: **9** across 8 PRs (highest count in the routine's life). Hotspots: `accounting` routes — PR #1454 added 4 ppt-web accounting routes, #1453 added the reality-web accounting page, #1545 unbroke its production build, #1611 added test for download/retry, #1621 relocated payment-matching for both products. **No `docs/screens/{ppt,reality}/accounting*.md` exists.** This is the routine's clearest auto-fix candidate (the allowlist covers `screen-map-drift → stub doc + PR`), but the 9 signals would each create a separate stub; per-run cap is 1. **Deferred to next run** with a recommended pick of `#1454 ppt-web accounting` (largest surface).
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New:** 71 — 15 `security|bug|refactor|completeness-unchecked-todo-*` (score 2–3), 15 `test-gap-hotfix-no-test-*` (score 2), 9 `test-gap-screen-map-drift-pr-*` (score 2), 24 `closed-not-merged-pr-*` (score 1), 3 `churn-hotspot-*` (score 1, two also bumped to `repeated-churn` +1), 3 `code-review-ppt-web-core-*` (score 1–2, all high-conf), 2 derived `repeated-churn-*`.
- **Bumped:** none (no signal targeted an existing open backlog id).
- **Decayed:** 14 open items past 14-day cutoff — 6 lost 1 point (now score 1), 8 dropped to 0.
- **Dropped:** `churn-hotspot-mobile-native-listing-detail-kt`, `churn-hotspot-mobile-native-search-screen-kt`, `code-review-mobile-native-kmp-deeplink-android-bypasses-shared-router`, `refactor-churn-hotspot-mobile-{announcements,announcements-test,documents}`, `triage-dispatcher-mcp-push-large-file-issue-1014`, `triage-issue-951` — all decayed to 0.
- **Resolved this run:** 0 (the resolution check found no merged PR body containing a verbatim backlog `id`; the catch-up batch is dominated by team-authored fix PRs, not routine-driven plan landings).

## Plans promoted
- **none this run.** Already 6 ready items with plans on disk (`security-llm-doc-idor`, `bug-ios-searchview-uncompilable`, `code-review-reality-web-{share-comparison-404,listing-page-ssr-crash,realtor-mgmt-untranslated}`, `code-review-mobile-rn-report-fault-fake-submit`); promotion cap is 2 per run and the queue isn't draining. The 10 new score=3 unchecked-TODO rows are confidence=medium and lack concrete hypotheses (a "checkbox left blank" is process noise, not a bug — promote only if the same TODO recurs with code evidence next rotation). The 3 Phase-1.5 findings cap at score 2 (need a second signal to clear the +3 readiness bar). The `accounting screen-map drift` is best handled via Phase 5 auto-fix next run, not a plan.

## Auto-fix
- `no candidate` — `ROUTINE_AUTOFIX_DISABLED` not set, but: (a) the 9 `screen-map-drift` signals are individually high-conf but the per-run cap is 1 PR; choosing one product/slug is judgement work (the affected files don't have an obvious 1:1 slug — payments / accounting / etc. spread across multiple files) and the doc stubs need real *Routes* lists; deferring to next run; (b) no `fixme-in-merged-code` signal fired (the per-PR-diff scan was deferred — see Phase 1 notes); (c) no `dep-update-noise` lockfile-drift sub-shape detected (the 8 dependabot PRs all merged cleanly).

## Self-review findings
- open=8 (high=2, medium=4, low=2); new this run=0; acknowledged=19
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer reported claimable=22/29 while true claimable=2; 29/31 open action-list items were exact-id-present in archive (mostly `status=failed`); fix: two-part PR closing failed-zombie open rows AND fixing the metric formula (still unlanded as of `ce9a2cb`).
- fp-archive-too-big-for-mcp [recurrence=5] — Phase 6 archive-move produced 198KB `assignments-archive.json`; `mcp__github__push_files` cannot inline that size; fix: proven workaround is push state commit to a new branch (new-branch push not proxy-blocked) and open PR.

## Open questions
- Is the cloud routine cron paused or genuinely broken? The 3-day gap is the longest since the routine started; operator should check the scheduler config — without it, the dispatcher's `buffer-low` fire payload may be the only daily heartbeat reaching this loop, which is fragile.
- Should `accounting` be its own product surface in `docs/screens/`, or are the ppt-web and reality-web accounting routes considered separate features? The 4 ppt routes and 1 reality page emerged in parallel with no screen docs. Need a screen-map authoring decision before auto-fix can pick the right slug shape.

## Quiet day?
- **No.** 129 merged PRs, 71 new backlog rows, 3 Phase-1.5 findings — busiest single run in the routine's life.
