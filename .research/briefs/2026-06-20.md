# 2026-06-20

## Since last run
- Merged PRs: **50** (#1544–#1615 sampled across the 4d window — sample-cap of the search API; range covers most/all of the merge window since 2026-06-16)
- Open PRs touched: 5 newly active (latest #1606 pap-281-contacts-gate untouched-since-creation; the four dependabot PRs #1567-#1574 carry the lockfile churn)
- New / updated issues: **30** (#1519–#1614; all labeled — 27 `follow-up,from-merged-review`, 1 `ci`, 1 `needs-human-review`, 1 `research-routine`)
- Commits: 50+ on `dev` since `4829015b` (sample-cap of list_commits)
- Routine lag: **4d 0h since last run** — `lag_warning` fired (threshold 36h) AND `stale_routine_alert` fired (≥3d threshold). Cloud routine appears to have skipped 2026-06-17 / 06-18 / 06-19. Dispatcher loop kept running (see #1607).
- Phases that failed: **none in hard sense — Phase 1.5 (rotating code review) and Phase 1.6 (PM rotation) were deliberately skipped to keep the catch-up run inside budget; cursors for both preserved.** Churn-derived signals (`churn-hotspot`, `repeated-churn`, `risky-churn`, `hotfix-no-test`, `fixme-in-merged-code`, `screen-map-drift`, `screen-map-orphan`) also deferred — per-PR file fetches on 50 merged PRs would have busted the run budget.

## Shipped
A summary at this size is read-only — the dispatcher's per-PR review process is the source of truth. Highlights:
- **Resolution wave on dispatcher follow-up issues**: 22 of the 27 `follow-up,from-merged-review` issues filed during the window were already closed by their fix/test PRs in the same window. Open carry-overs: #1520 (push dispatch-target selection), #1521 (payment-matching UI shipped to wrong app), #1522 (accounting UI bypasses shared API auth interceptor), #1533 (nextPageTriggerFlow race), #1584 (PortalPrincipal scoping), #1590 (pin-test asserts the constant), #1614 (combined OTA push_availability_and_rates never wired into a route).
- **Critical-path security/correctness landings**: #1551 `fix(db): cast document_category in count_accessible_rls`, #1552 `fix(api-server): gate OTA token-exchange on manager role`, #1561 `fix(reality-server): admit public agencies on imports via PortalPrincipal`, #1562 `test(integrations): XXE/DTD regression guard`, #1563 `test(db): cross-user RLS isolation for developer OAuth/usage tables`, #1580 `fix(api-server): rate-limit MFA recovery-code verify`, #1602 `fix(api-server): MFA recovery limiter — evict stale entries + dedicated audit action`, #1605 `ci(backend): enforce rustfmt in the required check job`.
- **Big features inched forward**: #1556 (APNs HTTP/2 delivery), #1558 (eSignature webhook idempotency terminal-state guard), #1560 (combined OTA rate/availability push flow — but see open #1614 for the route-wiring gap), #1577 (typed OTA HotelRateAmountNotif RQ/RS models), #1615 (DB-backed Airbnb linked-listing list route).
- **Dispatcher housekeeping**: #1546 / #1553 / #1595 / #1596 — dispatcher state PRs + archive sweep (43 terminal items archived 2026-06-18).

## Watch
- **🚨 #1607 (open, `research-routine`)**: *Research dispatcher stalled: stale coverage.json + 14 quarantined rows need cleanup* — filed 2026-06-19 by the dispatcher routine itself. The dispatcher cannot self-resolve. **This routine's catch-up run does not address it.** Operator action needed.
- **🚨 #1604 (open, `needs-human-review`)**: *Dispatcher backlog exhausted — coverage.json (2026-06-16) stale, producing 0 claimable rows*. Same root cause as #1607.
- **#1538 (open, `ci`)**: *Backend `test` job is not a required check on `dev` — red-test PRs merge freely.* Unchanged from 2026-06-17. PR #1605 closed the rustfmt half (lint-job), but the test-job gate remains open.
- **Reverts:** none detected.
- **Closed-not-merged:** not exhaustively checked this run (lag-window catch-up); none surfaced in the search-API sample.
- **Stalled review:** not exhaustively checked this run — #1606 is fresh (1d old); the dependabot PRs (#1567–#1576) are 2d old, healthy.
- **Churn hotspots this run:** not computed — see *Phases that failed* note above.

## PRs stuck in draft despite approval
- none (not exhaustively re-queried this run; carrying forward 2026-06-16's read)

## PRs with verdict=changes, no fix-round progress in 24h
- none (not queried — planning branch state not fetched in catch-up)

## Code review slice
- Segment reviewed: **skipped — lag-catchup**.
- Experts: n/a.
- Findings: 0.
- Next segment: **`ppt-web-core`** (last reviewed 2026-06-05 — oldest unreviewed; will be picked by the next normal-cadence run).
- Skipped: **yes — lag-catchup budget; `state.review_cursor` preserved so the rotation resumes from the same point next run.**

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0 — not yet seeded). Unchanged this run (no `docs/screens/**` touch in the merged set, eyeballed).
- Drift signals this run: **0 derived** (per-PR file-list fetch deferred).
- Orphan screens this run: **0 derived**.
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New:** none (no new signals beyond `lag_warning` / `stale_routine_alert`, both `score_delta: 0`).
- **Bumped:** none.
- **Decayed:**
  - `test-gap-screen-map-drift-pr-1085-reality` — −1 (no signal in 15d, 2→1).
  - `test-gap-screen-map-drift-pr-1100-ppt` — −1 (no signal in 15d, 2→1).
  - `bug-risky-churn-pr-963-api-main-rs` — −1 (no signal in 17d, 2→1).
  - `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` — −1 (no signal in 17d, 2→1).
- **Dropped:**
  - `refactor-churn-hotspot-mobile-announcements` — decayed to 0.
  - `refactor-churn-hotspot-mobile-announcements-test` — decayed to 0.
  - `refactor-churn-hotspot-mobile-documents` — decayed to 0.
  - `triage-dispatcher-mcp-push-large-file-issue-1014` — decayed to 0.
  - `triage-issue-951` — decayed to 0 (deploy-blocker on missing ESIGN_TOKEN_SECRET / ESIGN_WEBHOOK_SECRET; the issue itself remains OPEN on GitHub — drop here just means the routine stops auto-scoring it, not that the underlying gap is resolved).
- **Resolved:** none this run (PR-body cross-reference of 50 merged PRs against 58 open/ready backlog ids yielded 0 verbatim matches; the dispatcher's `task_id` convention diverges from this backlog's `id` convention by design).

## Plans promoted
- **none.** All 6 `ready` items already have plans (`security-llm-doc-idor`, `bug-ios-searchview-uncompilable`, `code-review-reality-web-share-comparison-404`, `code-review-reality-web-listing-page-ssr-crash`, `code-review-reality-web-realtor-mgmt-untranslated`, `code-review-mobile-rn-report-fault-fake-submit`). No new high-confidence vectors reached score ≥ 3 this run.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run (the only new signals were `lag_warning`/`stale_routine_alert`, both informational with `score_delta: 0`, and not in the Phase 5 allowlist).

## Self-review findings
- `.research/self-improvement/findings.json` absent — none.

## Open questions
- **Cloud routine cadence**: state.json was last advanced 4d ago. The dispatcher (separate loop, separate branch) kept ticking — see #1607 / #1604 — but this research routine did not. Is the cloud scheduler healthy? Operator should check the routine trigger config; this run advances the cursors so the *next* trigger sees only normal-window delta again.
- **Skipped phases — are they worth backfilling?** Phase 1.5 (rotating expert review) and Phase 1.6 (PM rotation) were skipped. The cleanest path is "next normal-cadence run picks up the rotation where it was" (cursors preserved). If you'd rather catch up the missed slots, a manual `text: "full"` trigger will run the full PM rotation; the code-review slice catches up automatically over the next 8 daily runs.
- **Issue #1604 / #1607 (dispatcher backlog exhausted)**: still open; outside this routine's auto-fix allowlist. Manual coverage-rebuild needed (`/ppt-project-management scan` per the skill spec — runs locally, never in the cloud).

## Quiet day?
- **No.** 50 merged PRs, 30 issues created/updated, 9 backlog decay actions (5 drops + 4 −1 ticks), 2 informational signals (lag + stale-routine alerts). `stats.runs` 40 → 41. `quiet_days` unchanged.
