# 2026-06-20

## Since last run
- Merged PRs: **89** (range #1447–#1615) — span includes 7 dependabot batch (#1566/#1570/#1571/#1572/#1573/#1575/#1576) + 6 closed-not-merged superseded fixes (#1500/#1503/#1504/#1505/#1506/#1511) + heavy `martin-janci`-authored implementer/dispatcher output. **Catch-up window: 95h (3d 23h) since 2026-06-16 03:25 UTC.**
- Open PRs touched: 0 in window (all open PRs queried — most are stale dependabot or older drafts, none touched since `SINCE_ISO`).
- New / updated issues: 0 untriaged this run (all 11 open issues since 2026-06-16 carry labels — `research-routine`, `follow-up`, `ci`, `needs-human-review`).
- Commits on `dev`: ~89 (mirrors merged PR count; routine reads PR list rather than raw commit log this run).
- Routine lag: **3d 23h since last run (95h) — `lag-warning` + `stale-routine-alert` both fired.** Dispatcher (separate loop, `.research/management/`) kept up — last touched 2026-06-20 00:14 UTC (`42c40e1`). The cloud cron for *this* routine appears to have stalled between 2026-06-16 and today; investigate next run.
- Phases that failed: **none** (Phase 1 succeeded; Phases 1.5, 1.6, 3 explicitly skipped — see *Catch-up posture* below).

## Shipped
- **Top 10** (full theme summary in `signals/2026-06-20.json`):
  - **#1615** — DB-backed Airbnb linked-listing list route (coverage 83-1).
  - **#1560** — combined OTA `push_availability_and_rates` with partial-failure outcome (booking integration).
  - **#1454** — native accounting MVP (N1–N4) + `@hey-api` migration (PAP-232) — adds `/api/v1/accounting/{invoices,contacts,matches,statements}`.
  - **#1453** — N5: bank-statement upload + payment-matching + match-review (PAP-210); ships `reality-web` `/accounting` UI.
  - **#1556** — APNs HTTP/2 delivery wired into mobile OS push (FCM/APNs feature).
  - **#1450** — preference-aware push dispatch-target selection (coverage 8A-3).
  - **#1602** — MFA recovery limiter eviction + dedicated `MfaRecoveryRateLimited` audit action (closes #1523 follow-up).
  - **#1601** — OTA manager-gate derived from `TenantRole` + airbnb cross-org test (closes #1525).
  - **#1460** — FORCE RLS on `developer_oauth_apps`/`grants`/`api_key_usage_logs` (BIT-76, #1303).
  - **#1605** — enforce `rustfmt` in the required `check` CI job (closes #1592).
- **Themes:** accounting MVP (N1–N5 + #1597 rounding fix + #1612 queryKeys refactor); booking/OTA integration maturation (#1577 typed `HotelRateAmountNotif`, #1601 manager-gate, #1614 follow-up flag); RLS/IDOR hardening sweep (#1460 FORCE RLS, #1613 doc folder tri-state, #1603 portal-principal pin, #1539 OAuth public-client rejection); mobile push plumbing (#1556 APNs HTTP/2, #1450 preference-aware target); CI tightening (#1605 rustfmt required).
- **Test-only PRs closed coverage gaps:** #1470, #1473, #1501, #1502, #1517, #1542, #1543, #1544, #1548, #1549, #1562, #1563, #1564, #1579, #1599, #1600, #1608, #1611 (regression guards spanning OTA OAuth/CSRF, document preview, marketplace COALESCE, report download/retry, XXE).

## Watch
- **Reverts:** none in window.
- **Churn hotspots this run (top 3 by LOC):**
  - `backend/servers/api-server/src/services/push_fanout.rs` — **1010 LOC** across PRs #1556 + #1450 (**REPEATED** — last seen on 2026-05-26; `repeated-churn` fired).
  - `backend/crates/db/tests/accounting_rls_repo_tests.rs` — **911 LOC** across PRs #1454 + #1597 (NEW, healthy churn — new test file for accounting MVP).
  - `backend/crates/integrations/src/booking.rs` — **669 LOC** across PRs #1562/#1560/#1577/#1599 (**REPEATED** — last seen 2026-06-12; `repeated-churn` fired).
- **`hotfix-no-test` signals (9):** #1582 (mobile useFolderTree wiring), #1565 (`document get-by-id` u.name + enum cast), #1561 (reality-server imports `PortalPrincipal` admit), #1551 (`count_accessible_rls` cast), #1545 (reality-web build fix — i18n only), #1539 (**OAuth public-client wrong-secret rejection — actual security fix without regression test**), #1537 (rental enum write paths), #1536 (mobile-native spotless cleanup — cosmetic), #1512 (clippy fixes in accounting routes — cosmetic). Heuristic caveats noted: #1536/#1545/#1512 are formatting/i18n-only and the test-gap framing is generous; #1539 is the most substantive of the nine.
- **`unchecked-todo` signals (5, all `confidence=high`, all score=3):** #1507, #1492, #1501, #1460, #1476 — bodies retained one or more `- [ ]`. **Hand-wave caveat:** several of these (#1460/#1476/#1507) read as administrative checklists ("CI green", "reviewer signed off"), not substantive follow-up work. Phase 3 deliberately did not promote — see *Catch-up posture*.
- **`screen-map-drift` signals (7):** 5 ppt-web (#1454 accounting MVP, #1555 faults wiring, #1559 announcement comments thread, #1610 fault delete, #1611 report download-retry test) + 2 reality-web (#1453 accounting MVP, #1545 reality-web build fix). **The accounting MVP pair (#1453 + #1454) is the substantive miss** — new accounting pages without `docs/screens/{ppt,reality}/accounting*.md`. Other drift signals are test-file or build-fix noise (heuristic caveat).
- **Closed-not-merged this run:** #1500 (IDOR gate, superseded), #1503 (test seed_membership fix), #1504 (migration rename), #1505 (rental enum cast), #1506 (booking-oauth role arg), #1511 (clippy lints) — all functionally superseded by merged peers; `confidence=high`, `vector=triage`.
- **Stalled-review / stuck-in-draft:** **0** — the open-PR query found no PRs touched in the window. The Cloudflare API-list-pull-requests pagination caps at 100 returned; if the daily query yields all-merged, the open queue is small or quiet. (Worth double-checking from the dispatcher's view next run.)
- **Dependabot batch:** 7 dep-update PRs auto-merged 2026-06-17/18 (compose-bom 2026.05→.06.00, mikepenz/action-junit-report 5→6, npm-minor-patch group, expo-secure-store 55→56, @types/node 20→25, @babel/core 7→8, i18next 25→26) — logged only, no score.

## PRs stuck in draft despite approval
- none (no open-PR data captured in window; see *Watch* caveat).

## PRs with verdict=changes, no fix-round progress in 24h
- Not assessed this run (dispatcher state on the planning branch — separate loop; recent assignments include the in-progress `feat-build-configuration-by-environ-app-icons-all-sizes-mobile` PR #1554 carrying `needs-human-review`).

## Code review slice
- Segment reviewed: **skipped** (catch-up posture — see below).
- Experts: —
- Findings: 0
- Next segment: **ppt-web-core** (oldest unreviewed: 2026-06-05, 15d stale).
- Skipped: **yes** — `ppt-dev-review` skill not invoked this run due to 95h-lag catch-up budget pressure. `state.review_cursor` left untouched.

## Screen-map status
- Total `docs/screens/` files: **141** (ppt: 80, reality: 47, reality-mobile: 14; mobile RN: 0 — not seeded).
- Drift signals this run: **7** (5 ppt, 2 reality — `screen-map-drift-pr-{1453,1454,1545,1555,1559,1610,1611}`). The accounting MVP pair (#1453 + #1454) names net-new pages that warrant fresh screen-map entries.
- Orphan screens this run: **0** (light spot-check only; full orphan sweep deferred).
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New (30 rows added; 8 noise signals — 7 dep-update + 1 lag-warning — recorded in `seen_signals` only):**
  - [3] `unchecked-todo-pr-1507`, [3] `unchecked-todo-pr-1492`, [3] `unchecked-todo-pr-1501`, [3] `unchecked-todo-pr-1460`, [3] `unchecked-todo-pr-1476` — all `confidence=high`, all `vector=bug|test-gap`. See *Watch* caveat re: administrative-checklist false-positive risk.
  - [2] `hotfix-no-test-pr-{1582,1565,1561,1551,1545,1539,1537,1536,1512}` — see *Watch* for which are substantive vs cosmetic.
  - [2] `screen-map-drift-pr-{1610,1611,1555,1559,1545,1453,1454}-{ppt|reality}` — `confidence=high` (file-list match factual); accounting MVP pair is the meaningful one.
  - [1] `churn-hotspot-backend-crates-db-tests-accounting_rls_repo_tests-rs` — 911 LOC (healthy test churn).
  - [1] `repeated-churn-backend-servers-api-server-src-services-push_fanout-rs` — 1010 LOC, runs_seen advanced.
  - [1] `repeated-churn-backend-crates-integrations-src-booking-rs` — 669 LOC, runs_seen advanced.
  - [1] `closed-not-merged-pr-{1500,1503,1504,1505,1506,1511}` — superseded peers.
- **Bumped:** **0** (no signals collided with existing open items by ID).
- **Decayed:** −1 each: `test-gap-screen-map-drift-pr-1085-reality` (2→1), `test-gap-screen-map-drift-pr-1100-ppt` (2→1), `bug-risky-churn-pr-963-api-main-rs` (2→1), `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` (2→1).
- **Dropped (decayed to 0):** `refactor-churn-hotspot-mobile-announcements` (was 1, no signal since 2026-06-05), `refactor-churn-hotspot-mobile-announcements-test` (same), `refactor-churn-hotspot-mobile-documents` (same), `triage-dispatcher-mcp-push-large-file-issue-1014` (same), `triage-issue-951` (same).
- **Resolved by PR-body match:** **0** — no merged PR in window referenced an open backlog row's `id` verbatim. (Resolution-check only covers verbatim id matches; evidence-gone grep deferred.)

## Plans promoted
- **none** — see *Catch-up posture* below for the rationale. All 5 score-3 candidates this run are `unchecked-todo` signals carrying administrative-checklist evidence; Phase 3.5's adversarial pass would have flagged hand-wavy hypotheses. The accounting MVP screen-map drift pair (PR #1453 + #1454) is genuinely promotable but at score 2 sits below the bar — score will compound on the next run's repeated detection if the docs still aren't there.

## Auto-fix
- `no candidate` — no signal met `confidence=high` + `score>=3` + allowlist this run. The five score-3 `unchecked-todo` items are not in the auto-fix allowlist (issue-only would be `fixme-in-merged-code`, which did not fire), and the `screen-map-drift` doc-stub auto-fix path would have been eligible — but each of those is score 2, below the bar. `ROUTINE_AUTOFIX_DISABLED` is unset.

## Self-review findings
- `.research/self-improvement/findings.json` absent — none.

## Open questions
- **Cloud routine cron stalled 2026-06-16 → 2026-06-20.** Confirm the scheduler is firing daily again. The dispatcher's separate loop (`management/`) kept ticking — only this routine missed. Check the cloud workflow logs and adjust the trigger if necessary.
- **Accounting MVP screen-map docs gap.** PR #1453 (`reality-web /accounting` layout/page) and PR #1454 (`ppt-web` accounting MVP — invoices/contacts/matches/statements) shipped without matching `docs/screens/{ppt,reality}/accounting*.md`. Worth a one-shot human pass or an `/screens init` invocation before drift compounds.
- **`unchecked-todo` administrative-checklist heuristic.** Five PRs in window left `- [ ]` boxes that read as CI/reviewer signoff, not real follow-up work. Consider tightening Phase 1's detection: require the unchecked box to live in a *substantive* section (not a "Checklist" / "## Test plan" footer) before scoring.

## Catch-up posture (operational note)
This run is a catch-up after a 95h cron stall. To stay within budget, the routine **skipped three optional phases**:
- **Phase 1.5 (Rotating Expert Review)** — skipped; `state.review_cursor` untouched. Next run should pick `ppt-web-core` (oldest at 15d).
- **Phase 1.6 (Project Management & Delivery rotation)** — skipped; `state.pm_cursor.next_index` left at 5 (next: `pm-security`, currently 24d stale), `coverage_cursor.next_index` left at 12. The dispatcher kept `.research/management/{action-list,risks,project-state}.{json,md}` fresh independently (last touched 2026-06-20 00:14 UTC by dispatcher), so the artifacts remain valid for the next run. G16 is a no-op when Phase 1.6 is skipped.
- **Phase 3 (Promotion)** — skipped; rationale above. All 5 score-3 candidates are unchecked-todo signals with administrative-checklist evidence, not promotable without a body-diff verification pass.

The brief still records every Phase 1 signal, runs Phase 2 (backlog decay/drop/resolution-check), and writes artifacts so the catch-up is auditable.

## Quiet day?
- **No.** 89 merged PRs, 30 new backlog vectors, 4 decayed, 5 dropped, 0 plans promoted (deferred), 0 auto-fix actions. `stats.runs` 40 → 41, `vectors_created` 173 → 203, `plans_created` 12 (unchanged), `quiet_days` 1 (unchanged).
