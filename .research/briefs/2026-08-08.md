# 2026-08-08

## Since last run
- Merged PRs: 16 (range #2706–#2721) since 2026-08-07T06:24Z; all authored by martin-janci (dispatcher-driven refactor/security landing chain)
- Open PRs touched: 1 (#2684 workflow-cond-parse-failopen — merged at 10:12 into `dev`; already visible in commit stream)
- New / updated issues: 3 (#2703 SSRF DNS-rebinding closed by #2710; #2704 workflow api_call unbounded-body closed by #2707; #2612 scheduler-notifications-fire-once closed by #2714 retry1)
- Commits: 40 on `dev` since prior cursor `a2c0f53d` → HEAD `11d2df9d`; 15 feature/fix commits + 25 dispatcher research-chore commits
- Routine lag: ~34h since prior 2026-08-07 06:24Z run — routine skipped one daily tick (single-day lag, no `lag_warning` fires at >36h threshold)
- Phases that failed: none (Phase 1.5 and 1.6 skipped by design — see sections below)

## Shipped
- **Security landings (Tier1d issue → merged):**
  - #2707 `gh-issue-2704`: cap unbounded workflow `api_call` response-body read (memory DoS). Enforces content-length + streaming cap; regression test asserts over-limit body rejected.
  - #2710 `gh-issue-2703`: close SSRF DNS-rebinding TOCTOU in workflow `api_call.rs`. Pins validated IP through connect via custom resolver; IG3 regression test asserts private-IP flip on 2nd resolution is rejected.
  - #2714 `gh-issue-2612-retry1`: decouple scheduled announcement/vote notification dispatch from publish/activate. Adds `notified_at` migration 00228 + retry scheduler tick; transient error no longer permanently drops the notification.
  - #2718 `sec-layout-webhook-hmac-verify-2026-07-23-retry2`: verify layout webhook HMAC parity + body-binding regression test.
- **Reality-server churn (inquiry seam refactor):**
  - #2696 (in prior run's commit stream) + #2719 `code-review-reality-server-inquiry-notify-route-wiring`: route anonymous inquiry POSTs through `InquiryNotifier` seam. State layout adjusted for injectable notifier.
- **API-server refactor churn (layout+auth+reports dedupe):**
  - #2711 layout tenant dedupe (retry1), #2713 layout admin dedupe, #2715 auth handler error-boilerplate dedupe, #2720 reports helpers extract, #2721 layout tenant `acquire_public_conn` extract.
- **DX/admin PATCH endpoints:**
  - #2716 admin-web platform-settings PATCH/GET (new migration 00229 + repo/route/tests); #2717 admin-web mobile-config PATCH/GET (new migration 00230).
- **Frontend i18n:**
  - #2709 `code-review-reality-web-listingform-no-i18n`: ListingForm strings routed through next-intl catalogs (cs/de/en/hu/pl/sk); regression tests added.
- **Data audit:**
  - #2712 `data-audit-add-evidence-idor-fix-2026-07-23-retry2`: emit dispute `add_evidence` access-audit event.

## Watch
- Stalled reviews (>7d, no `reviewDecision`, non-dependabot): **#2555, #2558, #2559** — all UC-ACC-05 invoice trio, 9.4d idle. Self-COMMENT-review by author (owner cannot self-approve), Copilot bot quota-exhausted, no outside reviewer requested. Not blocking dispatcher (feature branches, not `dev`-gating).
- Reverted: none.
- Churn hotspots: `backend/servers/api-server/src/routes/layout/tenant.rs` (3×; runs_seen bumps 1→2), `backend/servers/reality-server/src/routes/inquiries.rs` (3×, NEW), `backend/servers/reality-server/src/state.rs` (3×, NEW). Layout-tenant refactor is dispatcher-driven cleanup, not organic instability.
- Dependabot stalled: #2588–#2591 expo 56→57 chain (9.3d idle) — `dep-update-noise`, score 0, no backlog rows.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h. -->
- none (open PRs at this run: 16 total; only #2684 was actively touched and merged; no draft-approved deadlocks)

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json (planning branch) where reviewer_summary starts with "verdict=changes" AND (now - last_updated) > 24h AND status == "review". -->
- none — planning-branch assignments.json not fetched this run (per prior brief's carve-out; dispatcher's fast-path cascades already handled #2720/#2721 verdict=changes rounds within-window)

## Code review slice
- Segment reviewed: **4 segments folded in from today's Tier1d dispatcher passes** (api-core ×2, api-handlers, reality-server, mobile-native-kmp) — 10 code-review-finding signals adopted from `signals/2026-08-08-{api-core-tier1d,api-core-tier1d-2,api-handlers-tier1d,reality-server,mobile-native-kmp-tier1d}.json`.
- Experts (composite across segments): rust, security, kotlin, tester.
- Findings: **10 net-new** — 2 in api-core (accounting) + 2 in api-core (delay + notification), 1 in api-handlers (reports .unwrap_or_default), 2 in mobile-native-kmp (CreateListing stub + ApiClient dead code), 2 in reality-server (SSO neg-cache + no-timeout client). All folded into backlog as `open` rows.
- Next segment (oldest-unreviewed after this run's fold-in): `ppt-web-core` OR `mobile-rn` (both at 2026-08-06 = oldest tied).
- Skipped: **yes — routine's own Phase 1.5 slice skipped**; the 4 tier1d passes today already covered 4 of the 8 segments. Running another slice would only add noise.

## Screen-map status
- Total `docs/screens/` files: unchanged from prior run (ppt/ + reality/ only; mobile/ still unseeded → 0 doc files).
- Drift signals this run: **0** (no merged PR touched frontend route entrypoints or `frontend/apps/reality-web/src/app/`; #2709 touched `components/realtor/ListingForm.tsx` only; #2718 touched `app/api/layout-revalidate/route.test.ts` only).
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (per-workflow REST call skipped for budget).

## Backlog deltas
- **New (12 rows):**
  - [2 bug/high] `code-review-mobile-native-kmp-create-listing-submit-stub` — CreateListing screen submit hardcoded to NotImplementedError; production nav reaches it.
  - [2 bug/med] `code-review-api-core-accounting-line-multimatch` — duplicate suggested matches when 2+ open invoices share a Variable Symbol.
  - [2 bug/med] `code-review-api-core-delay-duration-overflow-before-cap` — 7d cap runs after unchecked *86400 multiply; release-profile wrap bypasses guard.
  - [2 bug/med] `code-review-api-handlers-reports-unwrap-or-default-silent-zero` — 4 manager reports return HTTP 200 with silently-zeroed data on transient DB error.
  - [2 bug/med] `code-review-sso-rs-negcache-transient-fail` — 5xx from PM cached as active=false for 60s; users logged out post-recovery.
  - [2 bug/med] `code-review-sso-rs-client-no-timeout-pool` — reqwest::Client::new() per SSO helper call; hang risk if PM stalls.
  - [1 bug/med] `code-review-api-core-accounting-vs-only-amount-ignored` — VS-only match at 0.8 crosses 0.5 threshold; amount ignored.
  - [1 bug/med] `code-review-api-core-quiet-hours-db-error-fail-open` — .ok() swallows Err → push during user's quiet window.
  - [1 test-gap/med] `code-review-api-core-accounting-no-tests` — accounting.rs (339 lines) has zero tests.
  - [1 refactor/high] `code-review-mobile-native-kmp-apiclient-dead-code-healthcheck-bug` — dead ~100-line class with .toString() body bug.
  - [1 refactor/high] `churn-hotspot-backend-servers-reality-server-src-routes-inquiries-rs` — 3 changes in this run.
  - [1 refactor/high] `churn-hotspot-backend-servers-reality-server-src-state-rs` — 3 changes in this run.
- **Bumped:** none (no existing item stacked new signals — all 10 tier1d findings are net-new IDs).
- **Decayed:** none (all open items updated within the last 14 days).
- **Dropped:** none.

**Open at score 2 (dispatcher-consumable score-2 candidates):** 7 rows — kmp `create-listing-submit-stub` (conf=high — highest quality), api-core `accounting-line-multimatch`, api-core `delay-duration-overflow`, api-handlers `reports-unwrap-or-default`, sso-rs `negcache-transient-fail`, sso-rs `client-no-timeout-pool`, mobile-native-kmp `portfolio-analytics-caps-100` (prior run, unchanged). **Zero rows at score >= 3** → **zero promotable to plans this run** (see Plans promoted below).

## Plans promoted
- **none this run.** No `open` item currently sits at score >= 3, and no score-2 item has `vector == "security"` for the fast-track (all new bugs are `bug`/`test-gap`/`refactor` — no security fast-track path applies). The 10 new tier1d code-review-findings entered as score 1–2, matching signal weights. Buffer refill via routine-promoted plans is blocked on: (a) a second signal stacking on any of the 7 score-2 open rows (`score_delta` bump to 3 triggers promotion), or (b) hand-authored plan for the highest-confidence mobile-native `create-listing-submit-stub` bug (conf=high, concrete fix path — user can bump manually).

## Auto-fix
- `no candidate` — no signal met the certainty bar this run (confidence=high + score>=3 + allowlist type). The 3 issue → PR chains (#2703 → #2710, #2704 → #2707, #2612 → #2714) were already-in-flight dispatcher work, landed before this routine ran; those are not routine-eligible auto-fix shapes. Kill switch `ROUTINE_AUTOFIX_DISABLED` not set.

## Self-review findings
- none

## Open questions
- **Buffer refill for dispatcher.** Trigger payload flagged `claimable=0/72 — only mobile-native-gated + quarantined work remains`. This routine surfaced 10 net-new bug rows but none reach the score-3 promotion threshold. Consider: hand-authoring a plan for `code-review-mobile-native-kmp-create-listing-submit-stub` (conf=high, user-facing broken feature, concrete fix path — either wire `createListing` repo method or hide the route until endpoint lands) to give the dispatcher one high-quality claimable task without waiting for score compounding.

## Quiet day?
- No — 15 feature commits, 3 issues closed, 10 net-new code-review-findings adopted, 12 new backlog rows, 3 churn hotspots. Not quiet.
