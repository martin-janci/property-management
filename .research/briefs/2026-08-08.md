# 2026-08-08

## Since last run
- Merged PRs: 18 (range #2706–#2723)
- Open PRs touched: 1 (#2684, code-review-api-core-workflow-cond-parse-failopen — 2d old, not stalled)
- New / updated issues: 3 (all closed follow-ups: #2612, #2703, #2704)
- Commits: ~30 on `dev` (mostly dispatcher `chore(research)` + merge commits)
- Routine lag: 1d 15h since last run — lag_warning fired (>36h threshold)
- Phases that failed: none

## Shipped
- #2723 announcement fan-out delivered/read/ack metrics per targeting scope (data)
- #2722 gate community read handlers on principal + tenant (security, IDOR fix)
- #2721 extract acquire_public_conn in layout tenant handlers (refactor, churn dedupe)
- #2720 extract reports helpers into helpers.rs (refactor, churn dedupe)
- #2719 route anonymous inquiry POSTs through InquiriesHandler (bug, notifier seam wiring)
- #2718 verify layout webhook HMAC parity + pin body-binding test (security)
- #2717 add PATCH/GET /api/v1/admin/mobile-config (dx, missing endpoint)
- #2716 add PATCH/GET /api/v1/platform-admin/settings (dx, missing endpoint)
- #2715 dedupe auth handler error boilerplate (refactor, churn dedupe)
- #2714 decouple scheduled notification dispatch (bug, Closes #2612 — resolves scheduled-notifications-fire-once)
- #2713 dedupe layout admin handler boilerplate (refactor, churn dedupe)
- #2712 emit dispute add_evidence access-audit event (data, audit trail)
- #2711 dedupe layout tenant-override handlers (refactor)
- #2710 close SSRF DNS-rebinding TOCTOU in workflow api_call.rs (security, Closes #2703)
- #2709 i18n ListingForm via next-intl catalogs (i18n)
- #2708 reject non-finite numbers in condition compare (bug)
- #2707 cap unbounded workflow api_call response-body read (security, Closes #2704)
- #2706 fail closed on partial RAG embedding batch (bug)

## Watch
- Open PR #2684 (code-review-api-core-workflow-cond-parse-failopen): quarantined per dispatcher (fix_rounds=3 exhausted, CI test-shards red); 2d old, not yet stalled by routine's 7d threshold — dispatcher owns it.
- Churn hotspots (top-3 this run):
  - `backend/servers/api-server/src/services/scheduler/mod.rs` (482 lines, runs_seen=1)
  - `backend/servers/api-server/src/services/actions/api_call.rs` (345 lines, runs_seen=1) — two SSRF/DoS fixes landed on it (#2710 + #2707); expected concentration
  - `backend/servers/api-server/src/routes/layout/admin.rs` (330 lines, runs_seen=2) — repeated-churn threshold reached; will fire repeated-churn on the next observation
- Dispatcher throughput: 18 merges in the window is the routine's highest-throughput day since the run started; buffer refill payload confirms it drained the queue.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- #2684 code-review-api-core-workflow-cond-parse-failopen — quarantined by dispatcher (fix_rounds=3 exhausted); not in verdict=changes 24h+ state per assignments (still `quarantined`, not `review`).

## Code review slice
- Segment reviewed: **skipped as a routine subagent spawn this run** — the dispatcher's Tier-1d dev-review generator already produced 9 net-new `code-review-finding` signals across 5 segments today (api-core ×2, api-handlers ×1, mobile-native-kmp ×2, ppt-web-core ×2, reality-server ×2). Rather than double-review, this run folds those signals into `backlog.json` (Phase 2) and advances the routine's `review_cursor` for the 5 covered segments to today.
- Experts: (dispatcher tier1d generator: rust, kotlin, frontend)
- Findings: 9 folded (see Backlog deltas — code-review-* rows created)
- Next segment: `mobile-rn` or `reality-web` or `ppt-web-ui` (all now oldest-unreviewed at 2026-08-06 / 2026-08-07)
- Skipped: yes — dispatcher Tier-1d already produced 9 findings across 5 segments today; no marginal value in a second review pass this run

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` 106 + `reality/` 47; `mobile/` not yet seeded → 0)
- Drift signals this run: 0 — only frontend/apps/reality-web/src/app touch in the merged range was PR #2718's `route.test.ts` (test-only, no user-facing route change; deliberately skipped to avoid false-positive per heuristic caveat)
- Orphan screens this run: 0 (not scanned this run — routine budget; would need full route-tree walk)
- Last `screen-map.yml` CI run on `main`: not checked this run

## Backlog deltas
- **New:** [2] `code-review-api-core-delay-duration-overflow-before-cap` — delay.rs u64 overflow before cap (bug/medium)
- **New:** [1] `code-review-api-core-quiet-hours-db-error-fail-open` — notification_pipeline.rs quiet-hours DB error fail-open (bug/medium)
- **New:** [2] `code-review-api-handlers-reports-unwrap-or-default-silent-zero` — reports/mod.rs unwrap_or_default swallows DB errors, serves silently-zero data (bug/medium)
- **New:** [2] `code-review-mobile-native-kmp-create-listing-submit-stub` — CreateListingScreen onSubmit stubbed (bug/high)
- **New:** [1] `code-review-mobile-native-kmp-apiclient-dead-code-healthcheck-bug` — ApiClient never instantiated (refactor/high; vector normalized from `cleanup`→`refactor`)
- **New:** [2] `code-review-ppt-web-core-errorhandler-network-misclassify` — parseApiError isNetworkError branch ordering misclassifies HTTP errors (bug/high)
- **New:** [1] `code-review-ppt-web-core-usewebsocket-handler-dep-gap` — useWebSocket effect handler dep gap (bug/medium)
- **New:** [2] `code-review-sso-rs-negcache-transient-fail` — introspect_pm_token neg-caches transient failures for 60s (bug/medium)
- **New:** [2] `code-review-sso-rs-client-no-timeout-pool` — SSO helpers construct fresh `reqwest::Client::new()` per call, no timeouts (bug/medium)
- **New:** [1] `churn-hotspot-backend-servers-api-server-src-services-scheduler-mod-rs` (refactor/high)
- **New:** [1] `churn-hotspot-backend-servers-api-server-src-services-actions-api_call-rs` (refactor/high)
- **New:** [1] `churn-hotspot-backend-servers-api-server-src-routes-layout-admin-rs` (refactor/high; will fire `repeated-churn` next run — pre-run runs_seen was 1)
- **Bumped:** none this run
- **Decayed:** none (only pre-existing open item is 4d old)
- **Dropped:** none

## Plans promoted
- none — no open item reached score≥3 this run (buffer refill is intentionally cold-start; new items start at 1–2 and need signal-stacking or human bump before promotion). Security fast-track (score≥2, vector=security, confidence=high) also did not apply — none of the 12 new items carry `vector=security`.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run (allowlist covers only `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review`; none fired)

## Self-review findings
- open=30 (high=6, medium=21, low=3); new this run=0; acknowledged=19
- fp-high [recurrence=?] — Phase 6 archive-move produced 198KB assignments-archive.json; MCP push_files cannot inline that large; fix: PROVEN workaround — push to new branch + gh PR + API-merge bypasses inline limit
- fp-high [recurrence=?] — dispatcher hard-assumes `git push` proxy-403 blocks it and defaults PUSH_METHOD=mcp push_files; fix: replace with runtime capability probe at Phase 1, bypass 64KiB guard when git push succeeds
- fp-high [recurrence=?] — action-list.json holds 31 in-progress rows vs assignments.json's 3; fix: add Phase-2 reconcile in gc1-reconcile.sh + T32 bijection self-test
- fp-high [recurrence=?] — 5 in-progress rows generated 0 branches on origin across 2 implementer generations; fix: sandbox-reclaim escalation to `failed` if branchless after N grace windows + T33 self-test
- fp-high [recurrence=?] — Phase 2.6 buffer reported claimable=54 but real=0 (all terminal in archive); fix: extend gc1-reconcile to auto-close open items terminal in archive + fix Phase 2.6/GC3 formula to subtract archive ids
- fp-high [recurrence=?] — Tier-2 planner kick returned http=401 (DISPATCHER_TOKEN rejected); fix: rotate token + escalate 401/403 as OPERATOR-CONFIG in Phase 7, not fold-into generic "kick failed"

## Open questions
- The dispatcher's Tier-1d generator has produced 9 code-review-finding signals today (via `.research/signals/2026-08-08-*-tier1d.json` and `.research/signals/2026-08-08-reality-server.json`) that were NOT folded into `backlog.json` until this run. This is the intended "generator → refill" path, but the coupling is by convention rather than by a hard gate. If the routine ran with `paused: true`, those signals would sit indefinitely. Consider: should the dispatcher's Tier-1d generator write directly to `backlog.json` via a targeted append rather than to a side signals file the routine has to scoop? (Filed as an open question — no plan promoted this run.)

## Quiet day?
- No — 18 PRs merged since last run, 12 new backlog vectors created, buffer refilled from 1→13 open items (dispatcher's `buffer-low: claimable=1/72` payload actively addressed).
