# 2026-08-08

## Since last run
- Merged PRs: 6 (range #2706–#2712 on `dev`; range straddles a dispatcher pre-spawn commit `1f2a351`)
- Open PRs touched: 0 (all 20 open PRs' `updated_at` was ≤ prior `last_run_iso 2026-08-07T06:24Z`; no fresh reviewer activity)
- New / updated issues: 2 (#2703 OPEN, `follow-up`, SSRF DNS-rebinding — being fixed by draft PR #2710; #2704 CLOSED 16:19Z by #2707)
- Commits: many on `dev` since prior cursor `a2c0f53d…` — 6 PR merges + several dispatcher research-chore commits; head now `1f2a35151df4e8d2632655dc4cef902878e211c4`
- Routine lag: 19h since last run — on schedule (dispatcher `buffer-low` payload triggered this run; last routine run was the 06:24Z rerun window on 2026-08-07)
- Phases that failed: none

## Shipped
- **#2706** `churn-hotspot-…llm-rs`: `POST /rag/index` now fails closed on partial embedding batches — `embedding_batch_is_complete(chunk_count, embedding_count)` predicate + 502 response before the `chunks.iter().zip(embeddings.iter())` write loop can silently drop the surplus chunks. New regression test proves the zip-truncation hazard.
- **#2707** `gh-issue-2704` (memory-amplification DoS): `services/actions/api_call.rs` now caps upstream buffering at `MAX_RESPONSE_BODY_BYTES = 8 MiB` via `read_capped_body` (Content-Length pre-check + streaming bounded read); marks body truncated with `RESPONSE_TRUNCATION_MARKER`. Closes #2704.
- **#2708** `churn-hotspot-…workflow_executor-rs`: `compare_numeric` routed through `coerce_finite_f64` — `"NaN"`/`"inf"`/`"infinity"` string coercion now returns `None` and the comparator errors uncomparable instead of a non-antisymmetric ordering. 4 new unit tests.
- **#2709** `code-review-reality-web-listingform-no-i18n`: `ListingForm.tsx` uses `useTranslations('listingForm')`; all 11 field labels, 4 validation strings, 5 property-type options and 2 transaction-type options moved to `listingForm` namespace across all 6 next-intl catalogs.
- **#2711** `refactor-churn-hotspot-api-server-layout-tenant` (dedupe): `routes/layout/tenant.rs` extracts `require_org_admin` (nil-org sentinel + admin gate) and `load_screen_config` (get_config + 404) — pure churn/complexity reduction on the PR #2478 hotspot; no route contract change.
- **#2712** `data-audit-add-evidence-idor-fix-…-retry2`: `DisputeRepository::add_evidence` emits a `resource_created`/`dispute_evidence` row on `audit_logs` (existing enum value, no migration) so a support engineer can trace evidence attachment across tenants. Regression test `add_evidence_writes_access_audit_event`.

## Watch
- **Stalled reviews (new this run):** #2559, #2558, #2555 — accounting MVP-loop trio, 9d idle each, no reviewDecision (`stalled-review-*` signals fired, Phase 1.5 pm-qa also flagged as reviewer-starvation escalation).
- **Draft-with-red-CI:** #2705 (rust-toolchain dependabot bump 1.94→1.100 — CI red since opened, invalid pin — dispatcher verdict=changes 4.5h ago, still fresh <24h).
- **Draft PRs from dispatcher this run:** #2710 (SSRF DNS-rebinding fix), #2713 (layout admin dedupe retry).
- **Churn hotspots (top-3 this run):** `services/actions/api_call.rs` (139 lines, NEW), `services/workflow_executor.rs` (91 lines, runs_seen 1→2 — `repeated-churn` fired), `components/realtor/ListingForm.tsx` (77 lines, NEW). All three are the direct product of this window's fixes, not organic instability — but the `workflow_executor.rs` two-run streak is worth flagging.
- **Reverted:** none.
- **Dep-update-noise (log-only):** #2591/#2590/#2589/#2588 (expo-* 56→57.0.1, `@testing-library/react-native` 13.3.3→14.x — dependabot, 8d, no reviewDecision — noise, score_delta=0).

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND age>24h. -->
- none (drafts #2705/#2710/#2713 not yet APPROVED — #2705 has dispatcher verdict=changes, #2710 waiting on green CI, #2713 was opened by the dispatcher fast-path within the window).

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: assignments.json rows where reviewer_summary starts with "verdict=changes" AND status=="review" AND age(last_updated) > 24h. -->
- none — the sole verdict=changes row (`dx-closed-not-merged-pr-2385-retry2`, PR #2705) was last updated 2026-08-07T22:17Z (~4.5h ago, well under 24h).

## Code review slice
- Segment reviewed: **api-core** (reason: churn-aligned — both `services/actions/api_call.rs` and `services/workflow_executor.rs` are under api-core; tied for oldest-unreviewed with `mobile-native-kmp` at 2026-08-04, churn broke the tie)
- Experts: rust (no secondary — api-core matrix)
- Findings: 2 (both new `code-review-finding` signals)
  1. **`code-review-api-core-email-notif-actions-noop`** — score 3, confidence **high**, vector `bug`: `EmailExecutor::execute` / `NotificationExecutor::execute` are silent no-ops (log + return `ActionResult::success()` unconditionally), yet registered as the live production handlers via `ActionRegistry::new()` for the `send_email` / `send_notification` action types. Any workflow using those actions reports COMPLETED with zero delivery. **Meets score≥3 readiness ⇒ promoted to plan this run.**
  2. **`code-review-api-core-delay-rs-duration-overflow`** — score 2, confidence medium, vector `bug`: `DelayConfig::to_duration` computes `duration * 86400` on raw `u64` before the 7-day cap check, so `{"duration": 144115188075855872, "unit":"days"}` wraps to 0 and collapses to a 0-second delay in release, panics in debug — overflow defeats the very bound the check exists to enforce.
- Next segment: `mobile-native-kmp` (oldest unreviewed at 2026-08-04 after this advance)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 170 (`ppt/`: 106 · `reality/`: 47 · `mobile/`: 0 — not yet seeded, drift detection excludes mobile per routine rules)
- Drift signals this run: 0 (no merged PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`; #2709's `components/realtor/ListingForm.tsx` is not in the trigger set)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (per-workflow REST call skipped for budget; see prior brief for method)

## Backlog deltas
- **New:**
  - [3] `bug-api-core-email-notif-actions-noop` — Phase 1.5 high-severity finding; promoted to plan below.
  - [2] `bug-api-core-delay-rs-duration-overflow` — Phase 1.5 medium finding; stays in backlog (score < 3).
  - [1] `dx-stalled-review-pr-2559` / `pr-2558` / `pr-2555` — accounting-trio 9d stall.
  - [1] `refactor-churn-hotspot-api-server-services-actions-api_call-rs` — PR #2707 churn.
  - [1] `refactor-churn-hotspot-reality-web-listingform-tsx` — PR #2709 churn.
  - [1] `refactor-repeated-churn-workflow_executor-rs-2026-08-08` — runs_seen 1→2 (PR #2685 + PR #2708).
- **Bumped:** none (no prior open item matched by id or overlapping sources).
- **Decayed:** none — all open items have `updated_at ≥ 2026-07-25` (cutoff = today − 14d).
- **Dropped:** none.
- **Resolved (open/ready → done):** none this run — this run's merged PRs (#2706/#2707/#2708/#2709/#2711/#2712) each resolve backlog items that had *already* been flipped `done` in prior runs (see PR-body match in earlier briefs); no new resolution transitions in `backlog.json`.

**Open items at score ≥ 2 (dispatcher-consumable candidates):** `bug-api-core-email-notif-actions-noop` (score 3, promoted this run), `code-review-mobile-native-kmp-portfolio-analytics-caps-100` (score 2, carried over), `bug-api-core-delay-rs-duration-overflow` (score 2, new). The trigger payload `buffer-low: claimable=5/72 — coverage+backlog exhausted, refill planner` reflects the dispatcher's ready-to-implement queue drain — this run replies with **1 new score-3 promotion + Phase 1.6 action-list refill 6→23** (see project-state.md; honest shortfall vs the 36 target because the coverage-map's true gap pool is only 5 items — see Phase 1.6 anomaly note below).

## Plans promoted
- `plans/bug-api-core-email-notif-actions-noop.md` — wire `EmailExecutor` / `NotificationExecutor` to the existing `EmailService::send_template_email` / `NotificationService::send_to_users`; add `ActionRegistry::with_services(...)` constructor; propagate errors to `ActionResult::failure`; IG3 regression tests. · adversarial pass: passed

## Auto-fix
- `no candidate` — the single ready item this run (`bug-api-core-email-notif-actions-noop`) is a code-change spanning 4 files and does NOT match any Phase 5 allowlist shape (doc-stub, orphan-remove, lockfile-bump, fixme-in-merged-code issue-only, stalled-review comment-only). Kill-switch not set. The stalled-review signals (`stalled-review-pr-2559`/`pr-2558`/`pr-2555`) match the comment-only allowlist shape *type* but the per-run cap is **1 comment** and the trio is one operator-level policy issue (pm-qa flagged it as a decision, not three independent stalls) — pinging one of three would be arbitrary; better to leave for pm-tech-lead's roadmap action.

## Self-review findings
- open=30 (high=6, medium=21, low=3); new this run=0; acknowledged=19
- fp-mcp-push-cannot-inline-large-archive [recurrence=14] — dispatcher archive files > 64KB MCP inline ceiling not persisted; fix: PR + `merge_pull_request` (squash) bypasses both the dev-push proxy and the MCP inline limit — PROVEN, adopt as primary push mechanism
- fp-git-push-blocked-by-proxy-assumption [recurrence=11] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked; fix: runtime capability probe at Phase 1 (@users.noreply.github.com email + dry-run) sets PUSH_METHOD=git on success, bypass 64KB size-guard
- fp-action-list-status-decoupled-from-assignments [recurrence=5] — 31 in-progress action-list rows vs 3 live assignments; fix: gc1-reconcile bidirectional reset + self-test T32 asserting bijection
- fp-sandbox-branchless-implementer-generations [recurrence=3] — reclaimed implementers never produced branches on origin (2 generations, ~6h); fix: escalate to status=failed after N reclaim_attempts + no origin branch (self-test T33)
- fp-buffer-metric-ignores-archive-terminals [recurrence=3] — Phase 2.6 reports claimable=54 while REAL=0 (all 54 items terminal in archive); fix: gc1 auto-close open items whose exact id is terminal in archive, GC3 formula subtracts archive too

## Open questions
- The Phase 1.6 pm-qa run flagged 3 stalled accounting PRs (#2555/#2558/#2559) as a **reviewer-starvation escalation** — an operator/policy issue, not a code fix. The dispatcher can't unblock it; the pm-tech-lead action queued this run is the correct owner. Should the routine's Phase 5 grow a "stalled-review nudge" auto-comment for organizationally-stuck PRs, gated by a per-author cap? Log for future consideration; do not auto-fix this run.
- Phase 1.6 anomaly: `sprint-status.yaml` epic-80 rollup shows `stories_completed: 1` while `coverage.json` + per-story `development_status:` show all 3 dispute stories `done` since 2026-06-25 → 07-15. pm-scrum-master raised this as a decision (out-of-scope to hand-edit `sprint-status.yaml` from a routine). Operator: reconcile epic-80's `stories_completed` to `3` in the next epic-status sweep.

## Quiet day?
- **No** — 6 merged PRs, 8 new backlog rows (1 promoted to plan), 2 code-review findings including a high-confidence score-3 bug, 3 stalled-review signals surfaced, buffer-low trigger honestly answered with one promotion + Phase 1.6 action-list refill 6→23. Genuine state advance (`last_pr_seen` 2702→2712, `last_issue_seen` 2699→2704, `last_commit_sha` a2c0f53d→1f2a3515).
