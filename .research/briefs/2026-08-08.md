# 2026-08-08

## Since last run
- Merged PRs: 20 (numeric range #2690–#2721 with cursor-tail entries #2690, #2696, #2698 also landing in-window; contiguous #2706–#2721 in the ~35h since 06:24Z 2026-08-07)
- Open PRs touched: 2 (#2684 workflow-cond-parse-failopen — updated 10:12Z with no reviewDecision; #2722 opened 16:44Z draft — community unauth reads retry2 by the dispatcher fast-path)
- New / updated issues: 3 (#2703 SSRF DNS-rebinding, #2704 unbounded response-body — both filed by the dispatcher during a signals-persistence outage on the earlier tick, both now CLOSED by PRs #2710 and #2707 respectively; #2612 CLOSED by #2714 decoupled-scheduler landing)
- Commits: 46 on `dev` (~14 PR merges + ~32 dispatcher research-chore commits); head now `a82eeb68`
- Routine lag: 1d 11h since last run — on schedule (under the 36h `lag_warning` threshold; scheduler-tick + one buffer-low fire in the middle)
- Phases that failed: none
- Same-day rerun: no — this is the first routine invocation on 2026-08-08. Phases 1.5 (rotating code review) and 1.6 (PM role rotation) were **not** run standalone this tick: Phase 1.5 was already covered by the dispatcher's Tier1d reviews across 5 segments (`api-core`, `api-handlers`, `mobile-native-kmp`, `mobile-rn`, `reality-server`), whose signals are folded here; Phase 1.6 remains deferred (pm_cursor untouched at 3 → G16 no-op).
- Trigger: `buffer-low: claimable=1/72 — refill planner (coverage exhausted 47done/2partial; backlog+retry dry)`. Payload observed and acted on — 10 tier1d review findings + 3 churn hotspots + 3 stalled-review process signals now folded into `backlog.json` for the planner to pick up.

## Shipped
- **Security fixes (2):**
  - #2710 `gh-issue-2703`: SSRF DNS-rebinding TOCTOU closed in `services/actions/api_call.rs` — pinned validated IP through `url_validation.rs` + regression test for public/private DNS flip. Closes issue #2703 (dispatcher-filed).
  - #2707 `fix(api-server): cap workflow api_call response-body read` — bounded upstream response with truncate-on-limit marker; closes issue #2704 (dispatcher-filed).
- **Reliability / correctness (5):**
  - #2714 `fix(api-server) decouple scheduled notification dispatch` — new `notified_at` column + separate scheduler pass; transient dispatch error no longer permanently drops publish/vote-start/vote-close notifications. Closes issue #2612 (post-merge finding from PR #2608).
  - #2706 `fix(api-server): fail closed on partial RAG embedding batch in /rag/index` — no more silent partial inserts on a failed embedding row.
  - #2708 `fix(api-server): reject non-finite numbers in workflow condition compare` — NaN/±Inf no longer bypasses numeric comparators.
  - #2719 `code-review-reality-server-inquiry-notify-route-wiring` — anonymous inquiry POSTs now go through `InquiriesHandler` (rate-limited + tenanted); route→handler seam wired.
  - #2718 `test(reality-web) HMAC parity` — pinned layout-revalidate webhook body-binding regression.
- **Feature completions (2):**
  - #2717 `dx-fixme-admin-web-mobile-config-patch-endpoint` — `PATCH/GET /api/v1/admin/mobile-config` + migration 00230.
  - #2716 `dx-fixme-admin-web-platform-settings-patch-endpoint` — `PATCH/GET /api/v1/platform-admin/settings` + migration 00229.
- **Refactor / hotspot shrinks (5):**
  - #2721 layout/tenant.rs — extracted `acquire_public_conn` helper.
  - #2720 reports/mod.rs — extracted helpers.rs (routes/reports hotspot addressed; runs_seen=4 in prior history).
  - #2715 auth/mod.rs — deduped auth handler error boilerplate.
  - #2713 layout/admin.rs — deduped layout admin handler boilerplate.
  - #2711 layout/tenant.rs — deduped tenant-override handlers (predecessor of #2721).
- **Frontend i18n:** #2709 realtor ListingForm SK/CS/DE/EN/HU/PL strings routed through next-intl catalogs.
- **Data / audit:** #2712 `feat(pm-data): emit dispute add_evidence access-audit event`.
- **Reality-server: 2 anonymous-inquiry landed:** #2696 injectable notifier seam + #2698 rate-limiting wire.

## Watch
- Stalled review: #2555, #2558, #2559 — three feat(acc) PRs (invoice lifecycle / PDF render / PAY-by-square QR) open **9 days** with no reviewDecision, all self-authored by martin-janci. Fires `stalled-review-pr-<num>` process signals (+1 each). Suggested next step: someone-not-the-author needs to review, or convert to draft.
- Reverted: none.
- Churn hotspots (this run, top-3 code):
  - `backend/servers/api-server/src/services/scheduler/mod.rs` — 482 lines (#2714), runs_seen=1.
  - `backend/servers/reality-server/src/routes/inquiries.rs` — 408 lines (#2698 + #2719), runs_seen=1.
  - `backend/servers/reality-server/src/handlers/inquiries/mod.rs` — 359 lines (#2696), runs_seen=1.
  - Caveat: PR #2690 (auth-restore squash-merge, 4543 files/+1.2M insertions) is a rebase artefact — excluded from churn ranking to avoid poisoning the hotspot signal.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none — dispatcher activity is fresh (planning-branch `assignments.json` not fetched this run; the 32 research-chore commits confirm active turnover).

## Code review slice
- Segment reviewed: **none this tick** — Phase 1.5 slot was already spent by the dispatcher's Tier1d passes (5 segments today: `api-core` twice, `api-handlers`, `mobile-native-kmp`, `mobile-rn`, `reality-server`).
- Experts: n/a (dispatcher-driven); the folded findings came from `rust` + `security` + `kotlin` + `tester` experts (per per-segment signal files).
- Findings: 10 (see Backlog deltas — 10 new `code-review-finding` items).
- Next segment: `ppt-web-core` or `ppt-web-ui` (both last seen 2026-08-06, oldest-unreviewed after this run's cursor bump for the 5 dispatcher-covered segments).
- Skipped: yes — dispatcher already covered a broader Tier1d slice than a single-segment Phase 1.5 would have.

## Screen-map status
- Total `docs/screens/` files: 176 (`ppt/` 111 + `reality/` 50 + `reality-mobile/` 15; `mobile/` not yet seeded → 0). No changes this run.
- Drift signals this run: 0 (only #2718 touched a `frontend/apps/reality-web/src/app/**` path, and it was `route.test.ts` under the `api/layout-revalidate/` webhook path — not a user-facing screen; the heuristic exception is documented in the routine prompt as expected).
- Orphan screens this run: 0 (no exhaustive scan; spot-check clean).
- Last `screen-map.yml` CI run on `main`: not fetched this tick (budget).

## Backlog deltas
- **New (16 items):**
  - [2] `code-review-mobile-native-kmp-create-listing-submit-stub` (bug, high) — Create-Listing screen wired to a hardcoded `NotImplementedError`; every submit fails in prod. Ships broken.
  - [2] `code-review-api-core-delay-duration-overflow-before-cap` (bug, medium) — `DelayConfig::to_duration` u64 overflow before cap on attacker-controlled duration.
  - [2] `code-review-api-core-accounting-line-multimatch` (bug, medium) — payment auto-matcher's inner loop emits multiple `PaymentMatch` rows per payment.
  - [2] `code-review-api-handlers-reports-unwrap-or-default-silent-zero` (bug, medium) — four report handlers serve HTTP 200 with silently-zeroed data on DB error.
  - [2] `code-review-sso-rs-negcache-transient-fail` (bug, medium) — `introspect_pm_token` negatively caches any non-2xx as inactive for 60s; PM 5xx logs users out for a full minute after recovery.
  - [2] `code-review-sso-rs-client-no-timeout-pool` (bug, medium) — SSO OAuth helpers build `reqwest::Client::new()` per call: no timeout, no pool; hung upstream blocks workers.
  - [1] `code-review-api-core-quiet-hours-db-error-fail-open` (bug, medium) — quiet-hours DB lookup fails open on transient error; notifications bypass user schedule.
  - [1] `code-review-api-core-accounting-vs-only-amount-ignored` (bug, medium) — Variable-Symbol match alone crosses 0.5 threshold ignoring amount.
  - [1] `code-review-api-core-accounting-no-tests` (test-gap, medium) — 339-line payment matcher module has zero tests.
  - [1] `code-review-mobile-native-kmp-apiclient-dead-code-healthcheck-bug` (refactor, high) — `ApiClient` class is unreferenced dead code; buried `.toString()` bug in `healthCheck()`.
  - [1] `churn-hotspot-backend/servers/api-server/src/services/scheduler/mod.rs` (refactor, high).
  - [1] `churn-hotspot-backend/servers/reality-server/src/routes/inquiries.rs` (refactor, high).
  - [1] `churn-hotspot-backend/servers/reality-server/src/handlers/inquiries/mod.rs` (refactor, high).
  - [1] `stalled-review-pr-2555` (dx, high) — feat(acc) sent/cancelled invoice lifecycle, 9d idle.
  - [1] `stalled-review-pr-2558` (dx, high) — feat(acc) invoice PDF render, 9d idle.
  - [1] `stalled-review-pr-2559` (dx, high) — feat(acc) PAY-by-square QR, 9d idle.
- **Bumped:** none (all new; no re-emit of existing IDs).
- **Decayed:** none — cutoff 2026-07-25; all remaining `open` items were within 14 days.
- **Dropped:** none.

**Buffer view for the planner:** open pool now at 17 items (was 1). Score distribution: 6× at score 2, 10× at score 1. None reach the score≥3 promotion bar this run; all sit as ready-to-compound buffer for future ticks.

## Plans promoted
- **none this run.** Score-≥3 bar not met by any item (top new items sit at 2). No `security` vector with high confidence + score≥2 to fast-track. This is expected: the tier1d review scoring caps each finding at `Medium(+2)`; a second signal needs to fire on the same ID for promotion. The next daily tick's Phase 1 (churn re-emit) or a follow-up review may compound.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run (Phase 5 kill switch not set; env check clean). Stalled-review comments were candidate-eligible on the allowlist but their backing items sit at score=1, below the ≥3 auto-fix certainty bar.

## Self-review findings
- none

## Open questions
- Should the routine's Phase 3 promotion bar be **lowered to 2** when the trigger is `buffer-low` and the planner is truly starved? Today's payload said `claimable=1/72`, yet the routine cannot promote any of the 6 fresh score-2 items because the gate is score≥3. If the operator wants a mechanically-refilled buffer, either the tier1d signals need `High(+3)` scores more often, or the promotion bar needs a buffer-low carve-out. **No action taken this run** — the gate stands as designed.
- 3 `feat(acc)` PRs (#2555/#2558/#2559) have sat 9 days without review. Author is martin-janci; nobody else is on the review loop. Consider assigning a reviewer or converting to draft to move them out of the stalled bucket.

## Quiet day?
- **No** — 20 PRs merged, 3 issues closed, 16 new backlog items, 46 commits on `dev`, buffer refill delivered per trigger payload. `state.last_pr_seen` advanced 2702→2721, `state.last_issue_seen` advanced 2699→2704, `state.last_commit_sha` advanced.
