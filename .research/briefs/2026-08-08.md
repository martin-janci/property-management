# 2026-08-08

## Since last run
- Merged PRs: 7 (range #2706–#2713)
- Open PRs touched: 4 (#2714 new draft, #2710 draft, #2696 ready, #2684 ready)
- New / updated issues: 3 (#2703 open SSRF, #2704 closed→resolved by #2707, #2612 closed→PR #2714 in flight)
- Commits: 30 on `dev` (7 code merges + 23 dispatcher chores)
- Routine lag: 21h since last run — on schedule
- Phases that failed: none

## Shipped
- #2713 refactor(api-server): dedupe layout admin handler boilerplate — mirrors #2711 tenant dedupe (330 lines churn)
- #2712 feat(pm-data): emit dispute add_evidence access-audit event — refs #2483 (writes to audit_logs stream)
- #2711 refactor(api-server): dedupe layout tenant-override handlers — extract `require_org_admin` / `load_screen_config`
- #2709 fix(reality-web): i18n ListingForm strings via next-intl catalogs — 6-language catalog updates
- #2708 fix(api-server): reject non-finite numbers in workflow condition compare — plugs `"NaN"`/`"inf"` bogus-ordering hole
- #2707 fix(api-server): cap workflow api_call response-body read (memory DoS) — 8 MiB stream cap; closes #2704
- #2706 fix(api-server): fail closed on partial RAG embedding batch in /rag/index — 502 on count mismatch

## Watch
- Reverted: none this window
- Churn hotspots this run:
  - `backend/servers/api-server/src/routes/layout/admin.rs` — 330 lines (PR #2713 dedupe); runs_seen 1→2 (repeated-churn — but the refactor IS the fix, score_delta=0 per G4)
  - `backend/servers/reality-server/src/routes/inquiries.rs` — 169 lines (PR #2698 rate-limit rewiring); first-time hotspot
  - `backend/servers/reality-server/src/handlers/listings/mod.rs` — 287 lines (all deletion — orphan handler removed by #2697; cleanup, not instability)
- Stalled reviews (10 days, ready-not-draft): #2559 UC-ACC-05.8 QR-payment, #2558 UC-ACC-05.9 invoice PDF, #2555 UC-ACC-05.17 invoice lifecycle — all reviewer-starved (auto-review loop skips accounting UC PRs)

## PRs stuck in draft despite approval
- none (no approved-draft PR observed in this window; only #2710/#2714 are draft, both unreviewed)

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher's Phase 5.7 already respawned #2710 SSRF changes-request; PR was updated today per #2710 timeline)

## Code review slice
- Segment reviewed: mobile-native-kmp (reason: oldest-unreviewed by state cursor; dispatcher Tier-1d covered api-handlers this same cycle so routing to a different segment)
- Experts: kotlin
- Findings: 2 (see Backlog deltas — httpclient-no-timeouts +2, profile-stats-serial-fetch +1)
- Additional Tier-1d finding (dispatcher-generated pre-routine): api-handlers reports/mod.rs `.unwrap_or_default()` silent-zero on secondary queries (+2)
- Next segment: api-core or mobile-native-kmp (both had cursor before this run — mobile-native-kmp advances to 2026-08-08; api-core still 2026-08-04)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: (unchanged; ppt/ + reality/; mobile/ still not seeded)
- Drift signals this run: 0 (no PR touched the trigger route files in this window; #2709 touched a ListingForm component but not the route entry files)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not fetched (webhook data unavailable via MCP; see `.github/workflows/screen-map.yml` for the last automatic run)

## Backlog deltas
- **New:** [2] `code-review-mobile-native-kmp-httpclient-no-timeouts` — every mobile API call can hang indefinitely (Ktor 3.x has no default HttpTimeout, no plugin installed)
- **New:** [1] `code-review-mobile-native-kmp-profile-stats-serial-fetch` — Account screen open latency ~3× baseline (three serial GETs instead of `awaitAll`)
- **New:** [2] `code-review-api-handlers-reports-unwrap-or-default-silent-zero` — four report handlers serve HTTP 200 with silently-zeroed data on transient DB error
- **Bumped:** none (no signal ID stacked onto an existing item this run)
- **Decayed:** none (both open items are within 14 days)
- **Dropped:** none this run

## Plans promoted
- 0 plans promoted. All 3 new items scored 1–2; none crossed the +3 promotion bar and none qualify for the security fast-track (they're `bug`/`perf`, not `security` at high confidence). Adversarial pass: N/A.

## Auto-fix
- `no candidate` — no signal met (confidence=high + score≥3 backlog item + allowlist). Stalled-review signals are allowlist-eligible for comment-only but do not have backing backlog items at score≥3 this run.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present in this branch)

## Open questions
- **Buffer starvation continues** — canonical backlog has only 2 open items (portfolio-caps-100 @2, inquiry-email-stub @1) plus 3 new this run (all ≤ 2). Dispatcher's `buffer-low: claimable=6/72` trigger reflects a real supply-demand gap: `code-review-finding` signals seed items at their initial `score_delta` and rarely re-fire on the same ID (dispatcher's Tier-1d rotates segments too). Refill lives at Tier-1c/1d generators, not this routine — the routine cannot fabricate +3 items from +2 signals.
- Should the routine promote `httpclient-no-timeouts` despite score=2 given its cross-fleet blast radius? (Currently blocked by Phase 3's score>=3 gate; security-fast-track is `security`+`high`+2, doesn't extend to `bug`.)
- Are `#2707` (memory-DoS body-cap) and `#2712` (add_evidence audit) truly missing regression tests per pm-qa's read, or are those tests inline `#[cfg(test)] mod tests {}` blocks not caught by the filename heuristic? (pm-qa proposed backfill; check before landing.)

## Quiet day?
- No — 7 PRs merged, 3 new backlog items, 2 code-review findings folded, coverage_cursor advanced (epic-80 evidence bumped via #2712), pm_cursor advanced (pm-qa role slot filled after 54-day stale).
