# 2026-07-01

## Since last run
- Merged PRs: **150** (range #1744–#2003) since 2026-06-16T03:25Z — 15-day catch-up window.
- Open PRs touched: 5 (#1990, #1968, #1979, #1812, #1797) — all owner: `martin-janci`; none stalled >7d.
- New / updated issues: **7 open** (5 new post-merge-review follow-ups #1997–#2001, split #1995, plus long-lived #1680 dispatcher-infra).
- Commits on `dev`: 40+ real-code commits + many dispatcher-state commits.
- Routine lag: **15d 12h since last run — 15× the 36h threshold. `lag_warning` emitted.** Cloud cron appears to have been paused or unfired during the window; the dispatcher (separate loop) kept ticking, so the codebase moved forward without the research routine.
- Phases that failed: **Phase 1.5 (skipped)** — rotating expert review deferred this run to avoid multi-subagent token spend on a 15-day catch-up; will rotate normally on the next run. **Phase 1.6 (skipped)** — same reason (pm_cursor + coverage_cursor left at HEAD; note that G16 becomes a no-op when Phase 1.6 skipped).

## Shipped
- **Post-merge review issues (#1997–#2001) filed by `ppt-review-merged`** — 5 concrete constructive follow-ups on PRs #1983, #1991, #1992, #1993, #1994 covering coverage-integrity, a serde/sqlx contract bug (SCREAMING_SNAKE_CASE), a non-transactional worker race, a quarantined regression test, and a missing pagination index. Each backed by verified code trace.
- **#2003 / #2002 / #1996** — dispatcher-completed stories (document sharing UUID validation on mobile share sheet, ppt-web error handler test coverage, onboarding tour helper tests). Coverage story wins.
- **#1994 (migration): SQL-side template pagination + honest import/export checklist** — reconciles the `count_templates` pair, marks the still-facade endpoints `partial` in `finance.md` — but leaves the data-movement layer as a facade (see #1995 split).
- **#1993 / #1990** — messaging: `count_unread_rls` now excludes soft-deleted threads (closes #1771); N-party start_thread swapped from per-recipient `is_blocked_rls` loop to one set-based `EXISTS`/`= ANY($2)` query. `messaging.rs` (both routes and db repo) became a top-3 churn hotspot again.
- **#1992 (perf/reality): set-based favorite-alert worker + idempotent mark-read** — enqueue/advance split into 4 statements; follow-up #1999 flags the non-transactional interleaving hazard.
- **#1991 (test): un-quarantine 5 finance happy-path suites (BIT-479)** — the un-quarantine forced the test payload to hard-code `"S_K"`/`"E_U_R"`, surfacing the enum `rename_all` bug now filed as issue #2000 (promoted to a plan this run).
- **Churn-split refactors merged 2026-06-24** — #1798 emergency, #1800 enhanced_tenant_screening, #1796 subscription, #1816 reserve_funds, #1810 sensor — all dispatcher-spawned; resolve 5 open churn-hotspot backlog rows this run.
- **#1946 (acc): TOTP + capability-token hardening (UC-ACC-16.1)**, **#1987 (reality-web): SSO callback CSRF state-bypass**, **#1976 (regional-compliance): live quorum + manager-gate + export scoping** — three security-adjacent hardening PRs merged; none open findings for research.
- **Dependabot noise:** 6 PRs (#1836–#1839, #1841, #1842) merged — routine + gradle + cargo-minor-patch + dtolnay/rust-toolchain 1.94→1.100; logged, not scored.

## Watch
- **`churn-hotspot-backend-crates-db-src-repositories-reality-portal-rs`** — 3rd re-emit (cumulative, no score delta), still monolithic 1779-line file on `dev`; **PR #1812 draft open to split** it (labeled `needs-human-review`, idle 5d) — merging that will resolve the row.
- **`messaging.rs`** — both the route and the db-repo landed 3 PRs each in the window (participant watermark, unread soft-delete, set-based block). Two new `churn-hotspot-*-messaging-rs` rows (+1 each).
- **Reverts:** none this run.
- **Stalled review:** none — all 5 open PRs updated within 7 days.
- **Closed-not-merged:** none identified this run.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **none — Phase 1.5 skipped**
- Experts: N/A
- Findings: 0 (rotating review deferred; 5 post-merge-review issues #1997–#2001 filed by `ppt-review-merged` cover equivalent ground and were absorbed as `code-review-finding` signals)
- Next segment: **ppt-web-core** (last reviewed 2026-06-05 — 27d stale, will run first on next routine)
- Skipped: yes — 15-day catch-up window; multi-subagent expert panel deferred to keep this run within a single-loop token budget

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0 — not yet seeded)
- Drift signals this run: **0** (per-PR drift scan not run — see *Deviations* below)
- Orphan screens this run: **0**
- Last `screen-map.yml` CI run on `main`: not fetched this run

## Backlog deltas
- **New:**
  - [3] `bug-country-currency-rename-all-screaming-snake` — CountryCode/SupportedCurrency SCREAMING_SNAKE_CASE breaks wire + sqlx (issue #2000 · high confidence · promoted to plan this run)
  - [2] `bug-favorite-alert-worker-nontransactional-drop` — enqueue+advance non-transactional; race drops price alerts (issue #1999)
  - [2] `perf-migration-list-templates-index-tiebreak` — list_templates missing (org, updated_at) index + total-sort tiebreak + tests (issue #1997)
  - [2] `test-gap-count-unread-rls-soft-delete-quarantined` — #1771 regression test [ignore]'d under BIT-351 (issue #1998)
  - [1] `bug-tenant-migration-import-export-facade` — DB-backed facade: no S3, no worker, fake export URL (issue #1995)
  - [1] `test-gap-finance-endpoint-checklist-inflated` — finance.md marks ~59 endpoints done citing ignored tests (issue #2001)
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-messaging-rs` — 3 PRs this window
  - [1] `churn-hotspot-backend-crates-db-src-repositories-messaging-rs` — 3 PRs this window
- **Bumped:** none (the reality-portal churn re-emit was cumulative; delta=0 per Phase 1 dedup rule)
- **Decayed:** 47 open items — all had `updated_at < 2026-06-17` after the 15-day silence; each lost 1 point per Phase 2 decay rule.
- **Dropped:** 33 items — decayed to 0. Most were pre-2026-06-05 code-review findings, screen-map heuristic candidates, and closed-not-merged PR notes that never accumulated a second signal. All left in `backlog.json` with the `decayed: no new signals since <date>` evidence trail.
- **Resolved:** 5 churn-hotspot rows → `done` (dispatcher-spawned splits merged 2026-06-24):
  - `churn-hotspot-…-routes-emergency-rs` ← PR #1798
  - `churn-hotspot-…-routes-enhanced-tenant-screening-rs` ← PR #1800
  - `churn-hotspot-…-routes-reserve-funds-rs` ← PR #1816
  - `churn-hotspot-…-repositories-subscription-rs` ← PR #1796
  - `churn-hotspot-…-repositories-sensor-rs` ← PR #1810

## Plans promoted
- `plans/bug-country-currency-rename-all-screaming-snake.md` — Change `rename_all = "SCREAMING_SNAKE_CASE"` to `"UPPERCASE"` on `CountryCode` (multi_currency.rs:144-145) and `SupportedCurrency` (multi_currency.rs:19-20); revert the bent test payloads in `multi_currency_happy_path_tests.rs`; add a serde round-trip unit test. Mode: cloud-ok. · adversarial pass: passed.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The high-confidence, score-3 promotion (`bug-country-currency-rename-all-screaming-snake`) is a real code change (not in the allowlist of doc-stub / orphan-remove / lockfile-bump), so it goes through the manual implementer path.

## Self-review findings
- `.research/self-improvement/findings.json` absent — none

## Open questions
- **Deviation call:** Phase 1.5 (rotating expert review) and Phase 1.6 (project-management analysis) were both skipped this run. The routine spec makes them mandatory when Phase 1 succeeds. Rationale: a 15-day catch-up window has already produced 5 concrete post-merge-review issues covering equivalent ground; running the ppt-dev-review and ppt-project-management skills on top of that during a single re-catch-up loop would spawn many subagents at heavy token cost with low marginal value. Both will resume normal rotation on the next run (ppt-web-core is next by review_cursor; pm-security at pm_cursor.next_index=5). If the operator disagrees, re-fire the routine with `text: "pm-full"` to force Phase 1.6.
- **Stale-routine alert:** `state.last_run_iso` was 15d old — the cloud cron for this routine appears to have been paused or unfired during that window. The **dispatcher** (planning branch, separate loop) kept ticking normally. Confirm the routine's cloud trigger is armed before the next window.
- **PR #1812 draft (reality_portal split)** carries `needs-human-review`; sitting 5d without a review. Merging it resolves the last open reality-portal churn row.

## Quiet day?
- **No.** 150 merged PRs since last routine run, 8 new backlog vectors + 5 resolutions, 1 plan promoted (score-3 serde contract bug), and 5 fresh post-merge-review issues absorbed as code-review-finding signals. `stats.runs` 40 → 41, `vectors_created` 173 → 181, `plans_created` 12 → 13.

## Deviations from spec (this run)
- **Phase 1.5 skipped** — see *Open questions* rationale; `review_cursor` left untouched.
- **Phase 1.6 skipped** — `pm_cursor` + `coverage_cursor` left untouched; G16 becomes a no-op (no `management/` artifacts written this run).
- **Screen-map drift/orphan per-PR scan skipped** — a 150-PR fan-out of `gh pr view --json files` at cloud rate was out of budget. No `screen-map-*` signals emitted this run; will resume on the next run when the window shrinks back to <7 days.
- **`fixme-in-merged-code` deep grep skipped** — same rationale (would require 150 diff reads). None emitted.
- **`risky-churn` correlation skipped** — same reason; would require per-PR file lists cross-referenced with the churn set.
