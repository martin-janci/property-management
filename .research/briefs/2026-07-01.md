# 2026-07-01

## Since last run
- Merged PRs: **34** merged into `dev` (range span #1758–#2003), via 50 commits on `dev` (17 real-code + 33 dispatcher / consolidation). Full list of application-code PRs preserved in `signals/2026-07-01.json`.
- Open PRs touched: **7** (this run only spot-samples the newest 30 open; #1797 and #1812 flagged stalled — see Watch).
- New / updated issues: **not queried this run** (catch-up mode — issue cursor not advanced; `last_issue_seen` unchanged at 1437).
- Commits on `dev`: **50** (`4829015b` → `368d715`).
- Routine lag: **15d 8h** since last run — **`lag_warning` fired**. The `stale_routine_alert` signal also fired: state.json cursor 15d stale; the cloud research cron is likely paused. The dispatcher (separate loop, `planning` branch) is still ticking — `assignments.json` was touched 2026-07-01 20:06. Operator: check the research routine trigger.
- Phases that failed: **1.6 skipped** (catch-up run — no failure, deliberate). `pm_cursor` and `coverage_cursor` NOT advanced this run; management/ artifacts remain dispatcher-authored (Jun 30 → Jul 1 refresh).

## Shipped
Top signal-carrying PRs (application-code, chronological within this window):

- **#1946** — `fix(acc): harden TOTP + capability-token security (UC-ACC-16.1 / 05.11)` — TOTP + capability token hardening.
- **#1955** — `integration: consolidate 22 reviewed/compiling PRs onto one branch (merge-then-f)` — mass consolidation PR; this single squash landed the bulk of the BIT-268 test-backfill wave (webhook / llm / mfa regression tests). Resolves 4 open backlog items (see Backlog deltas → **Resolved**).
- **#1966** — `fix(api-server): enforce length floors in preflight env check` (#1758).
- **#1967** — `fix(api-core): restore structured token_type/raw_kind logs + pin JwtService access gate` (#1782, #1761).
- **#1970** — `test(iot): sensor WS authz regression tests (non-member 403 before upgrade)` (#1786, #1763).
- **#1972** — `fix(db): correct delegation scopes enum-array SQL casts` (BIT-407).
- **#1973** — `fix(rentals): manager-gate guest/booking PII read paths` (#1766).
- **#1974** — `fix(faults): emit notifications on triage + confirm transitions` (#1793).
- **#1976** — `fix(regional-compliance): live quorum lookup + manager-gate writes + export scoping` (#1906).
- **#1977** — `fix(messaging): per-participant read watermark for N-party threads` (#1773).
- **#1987** — `fix(reality-web): close SSO callback CSRF state-bypass` (#1826).
- **#1989** — `fix(accounting): symmetric 404 in confirm_match + drop ToSchema on secret-bearing model` (#1827).
- **#1992** — `perf(reality): set-based favorite-alert worker + idempotent mark-read`.
- **#1993** — `fix(messaging): exclude soft-deleted threads from unread count + set-based block check` (#1771, #1776, #1789).
- **#1994** — `fix(migration): SQL-side template pagination + honest import/export checklist` (#1905).
- **#1996 / #2002 / #2003** — mobile RN screen-map + coverage stories (Story 10B.6 onboarding tour helpers; 79.3 error-handler unit tests; 7A.5 document sharing UUID guard).
- Massive test backfill: **BIT-268 waves 4–9** (#1892, #1895, #1896, #1921, #1923, #1925, #1930, #1931, #1932, #1934, #1937, #1944, #1953, #1957, #1960, #1961, #1962, #1963, #1964, #1980, #1983, #1991), plus BIT-360 quarantine re-enable (#1957, #1964) and BIT-440 test-gate unblock (#1984, #1985).

## Watch
- **Stalled reviews (open >7d, no reviewDecision):**
  - `#1797` — 8d open, draft, `fix(api-server): auth on OCR endpoints + manager-gate rental guest PII reads` (#1772, #1766). No reviewer engagement since 2026-06-24.
  - `#1812` — 7d open, draft, `needs-human-review` label, `churn-hotspot-backend-crates-db-src-repositories-reality-portal-rs: split reality_portal repository into cohesive sub-modules`. This IS the response to the reality_portal churn hotspot the routine flagged 2026-06-12; sitting waiting for reviewer decision on the module-split pattern.
- **Reverts:** none this run.
- **Churn hotspots this run (top 4, real code, ≥3 changes in the 15-day window):**
  - `backend/servers/api-server/src/routes/messaging.rs` (3 changes)
  - `backend/crates/db/src/repositories/reality_portal.rs` (3 changes, **runs_seen 1→2 → `repeated-churn` fired**; pending refactor PR #1812)
  - `backend/crates/db/src/repositories/messaging.rs` (3 changes)
  - `backend/crates/db/src/repositories/financial.rs` (3 changes — cross-references the Phase 1.5 `code-review-backend-financial-payment-race` finding; promoted this run)
- **Test-backfill artifact churn (excluded from scoring):** 9 `tests/*_happy_path_tests.rs` files each with 4 churns — coordinated BIT-268 backfill, not instability.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **api-core** (reason: churn-aligned — `routes/messaging.rs`, `repositories/financial.rs`, `repositories/reality_portal.rs`, `repositories/messaging.rs` all in the top-4 hotspots).
- Experts: rust.
- Findings: **3** — see Backlog deltas for `code-review-backend-financial-payment-race` (high, +3, **promoted**), `code-review-backend-messaging-race-block-check` (medium, +2), `code-review-backend-reality-watermark-ordering` (medium, +2).
- Next segment: **mobile-native-kmp** (last reviewed 2026-06-06 — oldest after this run).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: **161** (up from 127 last run — heavy screen-map backfill in the window via #1907/#1909/#1911–#1918 batch adding ppt + reality screens).
- Drift signals this run: **0** (no screen-touching PR merged without a matching `docs/screens/<product>/*.md` update — the recent wave of screen-map PRs *is* the doc update).
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New:**
  - [3] `code-review-backend-financial-payment-race` — `allocate_existing_payment` race allows over-allocation of a payment (**promoted**).
  - [2] `code-review-backend-messaging-race-block-check` — `send_message` per-recipient `is_blocked` loop; batch-query alternative exists.
  - [2] `code-review-backend-reality-watermark-ordering` — `favorite_alerts` worker enqueue+advance not isolated per org.
  - [1] `dx-stalled-review-pr-1797` — OCR auth + rental PII manager-gate PR open 8d.
  - [1] `dx-stalled-review-pr-1812` — reality_portal.rs refactor PR open 7d.
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-messaging-rs` — 3 changes this window.
  - [1] `churn-hotspot-backend-crates-db-src-repositories-messaging-rs` — 3 changes this window.
  - [1] `churn-hotspot-backend-crates-db-src-repositories-financial-rs` — 3 changes this window.
- **Bumped:**
  - [1→2] `churn-hotspot-backend-crates-db-src-repositories-reality-portal-rs` — `repeated-churn` +1 (runs_seen 1→2 across 2026-06-12/2026-07-01).
- **Decayed (updated_at ≤ 2026-06-17, −1 this run):** 10 items — mostly older screen-map heuristic drift + code-review findings that didn't attract new signals (see `signals/2026-07-01.json` → `decayed`).
- **Dropped:** 37 items whose scores hit 0 during decay. All had `updated_at ≤ 2026-06-17` and no new evidence in 15 days. Full list in `signals/2026-07-01.json` → `dropped`; every dropped item retains its evidence in `backlog.json` for historical audit. Nothing high-value was silently lost — the top-scored drops were 1-point churn-hotspot rows for files that stopped churning + 1-point triage rows already addressed by hand.
- **Resolved (open → done):**
  - `test-gap-hotfix-no-test-pr-1288-webhook-rls` — webhook regression tests landed via #1955 consolidation (webhook_repo_dedup, airbnb_webhook_dedup, airbnb_webhook_routes, esignature_webhook_idempotency).
  - `test-gap-hotfix-no-test-pr-1287-rls-llm-sessions` — llm/sessions/integrations regression tests landed (llm_document_rls_repo, integration_rls_repo, ai_llm_cross_tenant_idor, ai_llm_workflow_batch5).
  - `test-gap-hotfix-no-test-pr-1292-mfa-rls` — mfa regression tests landed (mfa_enrollment, admin_mfa_disable/recovery/step_up).
  - `bug-risky-churn-pr-963-api-main-rs` — `backend/servers/api-server/tests/security_headers_tests.rs` exists and asserts the middleware fires via the real `create_router` chain end-to-end (#954/#963 follow-up), with a companion static guard in `router_single_source_tests.rs`.

## Plans promoted
- `plans/code-review-backend-financial-payment-race.md` — Add `SELECT … FOR UPDATE` on the payment row inside the tx of `allocate_existing_payment`; author `financial_allocation_concurrency_tests.rs` with 2 tokio tasks + Barrier proving one-Ok/one-Err/sum-equals-amount. Mode: cloud-ok. · adversarial pass: passed.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. `code-review-backend-financial-payment-race` is high-confidence + score 3 but its action shape (SQL-lock refactor + concurrency test) is not in the Phase 5 allowlist (only doc-stub / orphan-remove / lockfile-bump / fixme-issue / stalled-review-comment). Left for the manual implementer via `plans/code-review-backend-financial-payment-race.md`. Two stalled-review signals could have produced comments but the routine deliberately holds off — the drafts are actively worked on by the same author (martin-janci) and a routine ping adds noise.

## Self-review findings
- `.research/self-improvement/findings.json` — none surveyed this run (Phase 1.6 skipped)

## Open questions
- **Should the research routine cron be re-armed?** 15d gap → `stale_routine_alert` fired. Operator to check the trigger.
- **PR #1812 (reality_portal.rs split) is the routine's own churn-hotspot response** — it was queued 2026-06-12 as `refactor` action-list-item and shipped a draft on 2026-06-24, then sat 7d with no reviewer. Human call: merge or reject the split pattern; both `messaging.rs` and `financial.rs` repositories are now candidates for the same treatment, and having a precedent unblocks them.

## Quiet day?
- **No — very much not.** 34 PRs merged into `dev`, 12 new signals (2 info + 4 churn + 1 repeated-churn + 2 stalled + 3 code-review), 8 new backlog rows, 4 resolutions, 1 plan promoted, 10 decayed / 37 dropped (natural consequence of the 15-day gap). `stats.runs` 40 → 41, `vectors_created` +8, `plans_created` 12 → 13. `state.last_pr_seen` 1439 → 2003, `last_commit_sha` 4829015b → 368d715.
