# 2026-07-01

## Since last run
- Merged PRs: **379** (range #1440–#2005) — 15-day catch-up window since 2026-06-16.
- Open PRs touched: 7 on latest page (recent 2 days).
- New / updated issues: 96 new + 69 updated (min #1438, max #2001); all new issues carry labels — no untriaged-issue signals fired.
- Commits on `dev`: at least 300 in-window (subagent hit 3-page cap; local shallow clone shows 50).
- Routine lag: **15d 0h since last run** — well over the 36h threshold; `lag_warning` + stale-routine alert both emitted. Cloud cron likely paused between 2026-06-16 and today.
- Phases that failed: **none** (Phase 1.5 rotating expert review + Phase 1.6 PM rotation both **skipped** this run — see *Open questions*).

## Shipped
Full 15-day window; only headliners called out here. Full list of #1440–#2005 in the merged-PR JSON. Top-impact merges:
- **#1946** `fix(acc): harden TOTP + capability-token security (UC-ACC-16.1 / 05.11)` — merged 2026-06-29; high-severity account-security hardening. Emitted `security-totp-capability-hardening-pr-1946` for post-merge test/coverage audit.
- **#1473** `fix(api-server): BIT-85 cross-tenant OAuth upsert hazard + manager gate (#1405)` — merged 2026-06-17; closes cross-tenant upsert hazard on OAuth handlers. Emitted `security-cross-tenant-oauth-upsert-pr-1473` for broader OAuth-handler audit follow-up.
- **#1621** `fix(ppt-web): relocate payment-matching to ppt-web with correct api-server auth (closes #1521)` — merged 2026-06-20; auth-context fix on payment matching.
- **#1629** `fix(api-client): send auth from the financial client (#975)` — merged 2026-06-20; auth-header regression on the generated financial client.
- **#1713** `revert(delegations): remove re-added delegation frontend (#1690) — reconcile with retirement (BIT-213)` — merged 2026-06-22; **only revert in window**. Second revert cycle on delegations confirms retirement per BIT-213. Emitted `revert-pr-1713` → new backlog item `refactor-delegations-frontend-retirement-confirmed` (score 3, open) to prevent a third re-add.
- **#1961 / #1963 / #1978 / #1981** (BIT-407/408/258/415) — governance + finance test-backfill waves; several 100s of new happy-path tests landed in `backend/servers/api-server/tests/*_happy_path_tests.rs`.
- **#1994** `fix(migration): SQL-side template pagination + honest import/export checklist (#1905)` — merged 2026-07-01; corrects overstated coverage in the import/export checklist.
- **#2002 / #2003 / #2005** — dispatcher-driven UC coverage close-outs (79.3 error toasts, 7a-5 doc sharing UUID validate, 82-3 KMP screen-map status).

Resolutions applied to backlog (5 items → `done` via PR-body match):
- `code-review-reality-web-share-comparison-404` → PR #1676
- `code-review-reality-web-listing-page-ssr-crash` → PR #1677
- `code-review-reality-web-realtor-mgmt-untranslated` → PR #1678
- `code-review-mobile-rn-report-fault-fake-submit` → PR #1679
- `code-review-mobile-rn-screens-mock-data` → PR #1681

Plans moved to `plans/_archive/` accordingly: `code-review-reality-web-share-comparison-404`, `code-review-reality-web-listing-page-ssr-crash`, `code-review-reality-web-realtor-mgmt-untranslated`, `code-review-mobile-rn-report-fault-fake-submit`.

## Watch
- **Stalled draft: #1812** — `churn-hotspot-backend-crates-db-src-repositories-reality-portal-rs` (repo module split). Open 6+ days as draft, no reviewDecision, labelled `needs-human-review`. Blocks resolution of the `reality_portal.rs` churn hotspot (repeated-churn re-fired this run). Signal `stalled-review-pr-1812` emitted.
- **Reverts:** #1713 (delegations frontend — second revert of re-add; see *Shipped*).
- **Churn hotspots this run (production code, tests excluded):**
  - `backend/servers/api-server/src/routes/messaging.rs` — 3 shallow-window touches, first-time hotspot.
  - `backend/crates/db/src/repositories/reality_portal.rs` — 3 touches, runs_seen=2 → `repeated-churn` fired.
  - `backend/crates/db/src/repositories/messaging.rs` — 3 touches, first-time.
  - `backend/crates/db/src/repositories/financial.rs` — 3 touches, first-time; parallels the finance test-backfill wave.
- **Caveat:** local git is a shallow clone (50 commits total on `origin/dev`); churn window covers only 2026-06-29 → 2026-07-01. GitHub-side counts (from Phase 1 subagent) are the authoritative activity totals; deeper churn analysis over the full 15-day window would need `git fetch --unshallow` and is deferred.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     `mcp__github__list_pull_requests` does not expose reviewDecision, so this section is best-effort.
     Sample of 7 open PRs touched this window shows 3 drafts (#2006, #2004, #1812, #1797); none with reviewDecision surfaced.
     Recommend re-running with per-PR `pull_request_read` if this class becomes actionable. -->
- none surfaced (reviewDecision not exposed by the list tool this run)

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query would target `.research/management/assignments.json` on the planning branch.
     Skipped this run — planning-branch fetch not in scope; dispatcher runs the assignments loop. -->
- none surfaced (deferred to dispatcher loop)

## Code review slice
- Segment reviewed: **skipped** (catch-up run; the ppt-dev-review skill was not invoked because Phase 1 already ballooned the run's token budget).
- Experts: n/a
- Findings: 0
- Next segment: `ppt-web-core` (last reviewed 2026-06-05 — oldest cursor; churn is on backend messaging/reality_portal, so `api-handlers` (2026-06-10) is a churn-aligned alternative).
- Skipped: yes — catch-up prioritisation. Re-scheduled for next run.

## Screen-map status
- Total `docs/screens/` files: **162** (ppt: 100, reality: 47, reality-mobile: 15; `docs/screens/mobile/` still not seeded — mobile RN routes remain excluded from drift detection per the routine's exclusion rule).
- Drift signals this run: **0** (drift detection needs per-PR file lists; the subagent skipped diff fetches under the stop-condition, so drift can't be computed cheaply for the 379-PR window — deferred to next incremental run).
- Orphan screens this run: 0 (same reason).
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New (created this run):**
  - `[3] refactor-delegations-frontend-retirement-confirmed` — confirm delegations frontend stays retired after 2nd revert (source: PR #1713)
  - `[3] security-totp-capability-hardening-pr-1946` — verify TOTP/capability-token hardening has regression coverage
  - `[3] security-cross-tenant-oauth-upsert-pr-1473` — audit remaining OAuth handlers for cross-tenant upsert hazard
  - `[2] refactor-backend-crates-db-src-repositories-reality-portal-rs` — churn hotspot (also fires `repeated-churn`)
  - `[1] refactor-backend-servers-api-server-src-routes-messaging-rs` — churn hotspot
  - `[1] refactor-backend-crates-db-src-repositories-messaging-rs` — churn hotspot
  - `[1] refactor-backend-crates-db-src-repositories-financial-rs` — churn hotspot
  - `[1] process-stalled-review-pr-1812` — reality_portal repo split PR stalled 6+ days
- **Resolved (→ done via PR-body match):** 5 items (see *Shipped* — 4 code-review-* + 1 code-review-mobile-rn-screens-mock-data). Their plans moved to `plans/_archive/`.
- **Decayed:** 52 open items lost −1 each for `updated_at < 2026-06-17`. Items reaching 0 were flipped to `dropped` (subset in `signals/2026-07-01.json` under `decayed[]`).
- **Dropped:** several rows now at score 0 after decay — see `backlog.json` for the full list.

## Plans promoted
- **none** — the 3 new score-3 items are audit-shaped (verify-coverage / cross-handler audit / retirement confirmation) rather than surgical code changes. A promotion here would produce a hand-wavy plan (Quality Gate 11 → placeholder rot). Deferred until a concrete file:line finding accumulates on top. · adversarial pass: n/a

## Auto-fix
- **no candidate** — no signal met the confidence=high + score≥3 + allowlist bar this run. The two `security-*` items are high-confidence and score 3 but their action shape ("verify TOTP coverage", "audit OAuth handlers") is analytical, not on the allowlist (doc-stub / orphan-remove / lockfile-bump / issue-only / comment-only). The `stalled-review-pr-1812` item is allowlisted (`comment-only`), but the PR is `needs-human-review`-labelled — pinging the author would duplicate the existing review request; skipped.

## Self-review findings
- open=8 (high=2, medium=4, low=2); new this run=0; acknowledged=0
- `fp-mcp-push-cannot-inline-large-archive [recurrence=5] — MCP push_files inline ceiling blocks 198KB assignments-archive.json; fix: session-branch push + PR + MCP squash-merge (proven workaround; adopt as primary in Phase 6)`
- `fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 subtract only ACTIVE assignment ids, missing archived-terminal open action-list rows; fix: extend gc1-reconcile + adjust buffer/GC3 formulas to subtract archive task_ids`

## Open questions
- **Should Phase 1.5 be run as a follow-up this session?** Skipping it means all 8 review cursors carry an extra day of stale. Recommend the next routine tick invokes `ppt-dev-review` with churn-aligned `api-handlers` (or the oldest `ppt-web-core`) segment.
- **Should Phase 1.6 be run as a follow-up?** `pm_cursor.next_index=5` → `pm-security` is next AND it hasn't run since 2026-05-27 (35 days). Recommend invoking `/ppt-project-management pm:security` before the next incremental run so security has a delivery read against the OAuth/TOTP fix wave.
- **Local git is a shallow clone (50 commits total).** For accurate churn attribution over multi-day catch-up windows, deepen the clone (`git fetch --unshallow`) or drive churn from GitHub commit file-lists via a bounded subagent.

## Quiet day?
- **No.** 15-day catch-up: 379 merged PRs, 165 issue events, 300+ commits, 1 revert, 4 fresh churn hotspots, 5 backlog resolutions, 52 decays, 8 new backlog items. Any of these alone would have taken the day out of "quiet".

## Goal violations
- **G2 caveat:** the `.claude/wonderful-gates-qs58tt` branch's `HEAD` is a dispatcher commit (`65eaa53`, from `dev`), not the previous *routine* commit. G2 compares vs `HEAD` on the routine's landing branch — the check will still pass because cursors/`seen_signals`/`hotspot_history` all advanced, but the diff is unusually large because the compare-base is a non-routine commit. Recorded here for the audit trail.
