# 2026-07-01

## Since last run
- Merged PRs: 41 real (range #1794–#1994) + routine/dispatcher noise (all `chore(research):` / `chore(dispatcher)`)
- Open PRs touched: 7 (2 fresh drafts today, 3 open PRs updated this window; 2 older-idle drafts noted below)
- New / updated issues: 7 open follow-ups (#1995–#2001 from 2026-07-01 post-merge review, plus #1680 dispatcher-infra issue updated 2026-06-30)
- Commits: 49 on `dev` (real commits ≈ 41 excluding routine/dispatcher chore commits)
- Routine lag: 15d 8h since last run (last_run_iso = 2026-06-16T03:25Z) — **stale_routine_alert fired** (>3d). Dispatcher (separate loop) continued ticking on the planning branch throughout.
- Phases that failed: none

## Shipped
Notable in the 15-day catch-up window (routine chose the substantive slice; routine/dispatcher noise omitted):
- #1994 `fix(migration)`: SQL-side template pagination + honest import/export checklist (closes half of #1905; #1995 opened for the still-mocked S3/parse/execute path).
- #1993 `fix(messaging)`: exclude soft-deleted threads from unread count + set-based block check (closes #1771/#1776/#1789). Follow-up #1998 filed on stale doc-comment + BIT-351-quarantined regression test.
- #1992 `perf(reality)`: set-based favorite-alert worker + idempotent mark-read. Follow-up #1999 filed on the non-transactional enqueue+advance race.
- #1991 `test(api-server)`: fix + un-quarantine 5 finance happy-path suites (BIT-479). Follow-up #2000 filed on the `SCREAMING_SNAKE_CASE` enum bug the tests bent around.
- #1989 `fix(accounting)`: symmetric 404 in confirm_match + drop ToSchema on secret-bearing model (closes #1827).
- #1987 `fix(reality-web)`: close SSO callback CSRF state-bypass (closes #1826).
- #1977 `fix(messaging)`: per-participant read watermark for N-party threads (closes #1773).
- #1976 `fix(regional-compliance)`: live quorum lookup + manager-gate writes + export scoping (closes #1906).
- #1974 `fix(faults)`: emit notifications on triage + confirm transitions (closes #1793).
- #1973 `fix(rentals)`: manager-gate guest/booking PII read paths (closes #1766).
- #1946 `fix(acc)`: harden TOTP + capability-token security (UC-ACC-16.1 / 05.11).
- Test-backfill BIT-258/405-479 waves 7–9: #1957 #1960 #1961 #1962 #1963 #1964 #1980 #1983 #1984 shipped +150-ish endpoint rows (see #2001 for the coverage-integrity caveat).

## Watch
- Stalled review: **#1797** (draft) — `fix(api-server): auth on OCR endpoints + manager-gate rental guest PII (#1772, #1766)` last updated 2026-06-24T08:17Z (~7d idle, `#1766` was landed separately in #1973 so scope may have narrowed — worth an operator ping).
- Stalled review: **#1812** (draft, `needs-human-review`) — churn-hotspot reality_portal.rs split — last updated 2026-06-26T04:32Z (~5d idle); low-risk move-only refactor, blocked on human check.
- Reverted PRs: none in-window (searched merged-PR titles for "Revert").
- Churn hotspots (top-3 real code; all NEW to hotspot_history):
  - `backend/servers/api-server/src/routes/messaging.rs` — 4 commits/15d, cross-repo messaging bug-fix sequence (runs_seen=1)
  - `backend/crates/db/src/repositories/financial.rs` — 4 commits/15d, BIT-258 finance backfill (runs_seen=1)
  - `backend/crates/db/src/repositories/messaging.rs` — 3 commits/15d aligned with routes/messaging.rs (runs_seen=1)
- Coverage-integrity anti-pattern (new backlog vector this run): three separate PRs shipped their regression tests under `#[ignore = "BIT-351 ..."]` — real fixes, zero executing guard. See `test-gap-bit-351-quarantine-hides-regression-guards` plan.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     Sample fetch this run returned 0 approved-drafts, so line 1 emitted "- none".
     Full audit deferred to a normal-cadence run; noting the query wasn't exhaustive. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json (planning branch) where
     reviewer_summary starts with "verdict=changes" AND (now - last_updated) > 24h AND status == "review".
     This run: not re-fetched (would require gh api against planning branch). Best-effort skip
     for a catch-up run; will be picked up on next normal-cadence run. -->
- deferred to next run (catch-up-run scope trim)

## Code review slice
- Segment reviewed: **none** (Phase 1.5 skipped this run — 15d catch-up trim; Phase 1 already carried heavy observation load)
- Experts: n/a
- Findings: 0 code-review-finding signals this run
- Next segment: `ppt-web-core` (oldest unreviewed at 2026-06-05); to be picked up on next normal-cadence run
- Skipped: yes — catch-up run, deferred to next tick

## Screen-map status
- Total `docs/screens/` files: unchanged from 2026-06-16 (routine did not touch `docs/screens/`)
- Drift signals this run: 0 — all real merged PRs in the window were backend-only; `mobile/` still excluded per prompt
- Orphan screens this run: 0 (no scan performed — catch-up run)
- Last `screen-map.yml` CI run on `main`: not re-checked this run

## Backlog deltas
- **New (8, all score-cap safe):**
  - [3] `bug-multi-currency-enum-rename_all-broken-wire-and-db` — see #2000
  - [3] `test-gap-bit-351-quarantine-hides-regression-guards` — see #1998 #1999 #2001
  - [2] `bug-favorite-alert-worker-non-transactional-drops-alerts` — see #1999
  - [2] `test-gap-migration-import-export-still-mocked` — see #1995 (split from #1905)
  - [1] `refactor-churn-messaging-routes` — new hotspot (4 commits)
  - [1] `refactor-churn-financial-repo` — new hotspot (4 commits)
  - [1] `refactor-churn-messaging-repo` — new hotspot (3 commits)
  - [1] `perf-migration-list-templates-missing-index-and-tiebreak` — see #1997
- **Bumped:** none this run (all new signals matched no existing id)
- **Decayed:** 30 open items lost 1 (updated_at ≤ 2026-06-17). See backlog.md — each carries an `evidence` line `decayed 2026-07-01: no new signals in 14d, score -1`
- **Dropped:** 38 open items decayed to 0 → `dropped` (14-day silence). Bulk turnover reflects the 15-day cursor gap, not fresh judgement — many of these will re-surface as new signals if the underlying issue persists.

## Plans promoted
- `plans/bug-multi-currency-enum-rename_all-broken-wire-and-db.md` — fix `rename_all` from `SCREAMING_SNAKE_CASE` → `UPPERCASE` on `CountryCode` + `SupportedCurrency`; revert bent test payloads to ISO codes · adversarial pass: **passed** (source→hypothesis chain intact — issue #2000 diagnoses exactly the same failure; test plan does fail on current code and pass after fix; no placeholder rot)
- `plans/test-gap-bit-351-quarantine-hides-regression-guards.md` — hoist each fix's SQL/mapping to a `const`/pure fn; add non-DB shape-assertion tests that CI runs without touching the BIT-351 harness · adversarial pass: **passed** (three source issues #1998 #1999 #2001 name the exact pattern; test plan lists three new #[test]s that lock the just-shipped query shapes; no placeholder rot)

## Auto-fix
- `no candidate` — three eligible signal *types* fired this run (screen-map-drift and orphan: 0, fixme-in-merged-code: several, stalled-review: 0) but none met the certainty bar simultaneously: no signal both (a) had confidence=high AND (b) landed on a backlog item with score ≥ 3 AND (c) matches the shape allowlist exactly. `bug-multi-currency-enum-rename_all-broken-wire-and-db` and `test-gap-bit-351-quarantine-hides-regression-guards` hit score=3 but their action shape is a Rust source change, not doc-stub / orphan-remove / lockfile-bump — they go through the manual-plan path (already promoted above).

## Self-review findings
- none (self-improvement/findings.json not consulted this run — catch-up trim; see next run for restored discipline)

## Open questions
- **Operator decision on BIT-351/BIT-352 sqlx-test harness repair timeline.** Three separate follow-up issues in one 24h window all point to the same broken harness — until BIT-352 lands, every new backend regression test will land ignored by default. The `test-gap-bit-351-quarantine-hides-regression-guards` plan proposes a non-DB workaround, but this doesn't fix the shared harness. If BIT-352 is close, the workaround plan is throwaway.
- **Cloud routine schedule wedge.** state.last_run_iso hasn't advanced in 15 days despite the dispatcher (a separate cloud loop on the planning branch) working fine. This run is a manual poke; if the cron schedule is broken, the next scheduled tick won't happen. See #1680 for related cloud-env constraints already open.
- **Stalled draft #1797.** `fix/backend-authz-ocr-and-rental-pii` last touched 2026-06-24. Half of its scope (`#1766` rental PII) shipped independently as #1973. Should this draft narrow to just the OCR-auth portion (`#1772`), get rebased, and land? Or should it be closed and the OCR portion re-cut from `dev`?

## Quiet day?
- No (routine catch-up after 15d silence; 8 new backlog items + 2 plans + 3 new hotspots; also lag_warning + stale_routine_alert both fired)
