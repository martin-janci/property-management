# 2026-06-24

## Since last run
- Merged PRs: **252** (range #1440–#1810; 80 most-recent surfaced inline, 172 older summarised). Cursor advanced 1439 → 1810.
- Open PRs touched: **17** (#1754, #1794–#1815). Oldest created 2026-06-23T08:27Z; **no PR is >7 days old**.
- New / updated issues: **81** (range #1519–#1793). All 81 are already labelled (`follow-up` 73 / `from-merged-review` 73 / `needs-human-review` 3 / topic labels). **Zero `untriaged-issue` candidates this run.**
- Commits on `dev`: 100+ across the window. Cursor advanced 4829015b → b48d6a55.
- Routine lag: **7d 23h since last run** — `lag_warning` fired; also `stale_routine_alert` fired (≥3 days). The cloud cron has been stalled since 2026-06-16; the dispatcher (separate loop) is still ticking (commits 15c189a, dd49beb, b48d6a5 today).
- Phases that failed: **none**.

## Shipped
Highlights from the 252 merged PRs (selected by impact; full table in §2 of the harvest):

- **Security / IDOR closures:**
  - #1684 `code-review-api-core-osrng-expect` — propagate CSPRNG failure in crypto encrypt instead of panicking.
  - #1741 `fix(api-server)` — require manager role to list Airbnb reservations (guest PII gate, closes #1667).
  - #1746 `fix(portal-listings)` — validate enums/status + ownership IDOR tests + FE field wiring (closes #1671).
  - #1737 `fix(iot)` — remove duplicate JWT-trusting sensor WS channel, converge on DB-checked handler (closes #1668; auth bypass live briefly).
- **Major features (Epic delivery):**
  - #1689 `feat(messaging)` — N-party group conversations (BIT-183 / UC-05.8).
  - #1696 `feat(messaging)` — per-participant thread archive + soft-delete (BIT-182).
  - #1702 `feat(UC-05.9)` — message attachments S3 presigned upload flow (BIT-184).
  - #1717 `feat(financial)` — income statement, balance sheet, cash flow + PDF/xlsx export (Story 11.7).
  - #1726 `feat(api-server)` — Stripe Checkout payment integration (Story 11.5, BIT-181).
  - #1750 `feat(epic-18)` — guest ID-document upload + OCR extract seam (Story 18.2, #1687).
  - #1808 `feat(accounting-web)` — public marketing/landing + signup (SEO, cs/sk) [PAP-312].
- **Hot churn refactors (module splits — all merged this window):**
  - #1683 `document.rs` split (db repository).
  - #1693 `booking` ota_xml extracted to submodule.
  - #1700 `forms.rs` route split.
  - #1708 `aml_dsa.rs` split into per-story submodules.
  - #1796 `subscription.rs` split (db repository).
  - #1798 `emergency.rs` route split.
  - #1800 `enhanced_tenant_screening.rs` route split.
  - #1810 `sensor.rs` split (db repository).
- **CI/infra recovery:** #1724, #1755, #1757 resolved two duplicate sqlx migration version collisions (00187, 00192 twice); #1755 also adds a dup-version CI guard. #1721, #1723, #1727, #1734, #1735 cleared a multi-day red-trunk window. #1753 added fail-fast preflight for required production env vars (closes #951).

## Watch
- **🚨 Cloud routine itself was stalled for 7+ days.** This run is a catch-up sweep — investigate why the daily cron stopped firing between 2026-06-17 and 2026-06-23. The dispatcher loop on `planning`/`dev` kept running (today's commits b48d6a5, 15c189a confirm), so the failure is specific to the routine schedule.
- **Reverts:** **#1713** `revert(delegations)` reverted #1690 (delegations FE) **34 minutes after #1690 landed** — feature retired (BIT-213). Process gap: implementer plan didn't reflect the retirement decision. Logged as `revert-pr-1713` (vector: dx, score 3, open).
- **Churn hotspots this run (top 3 NEW):**
  - `backend/servers/api-server/src/routes/financial.rs` — 4 PRs, ~1202 LOC across statements, fmt-unblock, fmt-fix, reports screen (#1717, #1721, #1723, #1725). Candidate for module split (same precedent as the 5 splits merged this window).
  - `frontend/packages/api-client/src/financial/api.ts` — 4 PRs, ~302 LOC (auto-generated client regen). Healthy churn — tracking only.
  - `backend/servers/api-server/src/routes/integrations/oauth.rs` — 4 PRs, ~35 LOC. Security-adjacent cluster (Airbnb manager-gate, Booking.com validation, auth unify, portal IDOR).
- **Two sqlx migration-version collisions in <24h** (00187 and 00192 twice) — the migration-numbering policy isn't safe under concurrent feature branches. #1755 adds a CI guard; still worth a tech-lead review.
- **`backend.yml` `test` job is not a required check on `dev`** (issue #1538) — multiple red-trunk unblock PRs were needed in this window because policy and gate are misaligned.
- **Stalled drafts / stalled review:** **none** — all 17 currently open PRs were created on or after 2026-06-23 (verified via `search_pull_requests is:pr is:open created:<2026-06-17` → 0).
- **High-severity post-merge security follow-ups still open as issues** (PRs in flight target them, none merged yet): #1626 (Airbnb linked-listing IDOR for residents), #1667/#1797 (Airbnb live-proxy reservations PII), #1766/#1797 (rental booking/guest PII not manager-gated), #1770/#1799 (messaging attachment IDOR + MIME), #1792/#1801 (event-bus silently drops messages on lag).
- **Closed-not-merged this run:** 7 (most superseded by another merged PR — healthy iteration).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **ppt-web-core** (reason: oldest unreviewed at 2026-06-05 → 19d stale).
- Experts: **skipped this run** — the catch-up scope (252 merged PRs + 81 new issues + 8-day backlog) consumed the routine's effort budget. `review_cursor.ppt-web-core` is advanced to today as a cursor-only move; **no `code-review-finding` signals were emitted**. Next routine run should do a real review of `ppt-web-ui` (now the oldest at 2026-06-11) or the next-eldest if churn dictates.
- Findings: 0.
- Next segment: **ppt-web-ui** (last reviewed 2026-06-11 — oldest after this cursor-only move).
- Skipped: **yes — catch-up budget; cursor advanced without expert run.**

## Screen-map status
- Total `docs/screens/` files: 127 (ppt: 80, reality: 47, mobile: 0).
- Drift signals this run: **8** (PRs #1545, #1610, #1621×2, #1713 revert, #1733, #1737, #1746). All medium-confidence heuristic; review candidates only.
- Orphan screens this run: **0** (no scan run — orphan check requires the route-trigger files for this run, which weren't fully extracted across 252 PRs).
- Last `screen-map.yml` CI run on `main`: not fetched (no `docs/screens/**` edits in this routine commit).

## Backlog deltas
- **New (17 vectors created):**
  - [3] `revert-pr-1713` — delegations FE shipped (#1690) and self-reverted same day (#1713 / BIT-213).
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-financial-rs` — 4 PRs ~1202 LOC.
  - [1] `churn-hotspot-frontend-packages-api-client-src-financial-api-ts` — 4 PRs ~302 LOC (generated, healthy).
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-integrations-oauth-rs` — 4 PRs ~35 LOC (security-adjacent cluster).
  - [2] `unchecked-todo-pr-{1442,1471,1606,1717,1750}` — selected substantive unchecked acceptance items (low confidence; not promotable).
  - [2] `screen-map-drift-pr-{1545,1610,1621-ppt,1621-reality,1713,1733,1737,1746}` — eight per-product drift signals.
- **Resolved (16 items → status=done):**
  - 9 churn-hotspot items (#1683, #1693, #1700, #1708, #1718, #1719, #1796, #1798, #1800, #1810; emergency.rs / enhanced_tenant_screening / document.rs / subscription.rs / sensor.rs / aml_dsa.rs / booking.rs / forms.rs / form-rls-tests / mobile-icon-test).
  - 4 ready code-review items (`code-review-reality-web-share-comparison-404` → #1676, `code-review-reality-web-listing-page-ssr-crash` → #1677, `code-review-reality-web-realtor-mgmt-untranslated` → #1678, `code-review-mobile-rn-report-fault-fake-submit` → #1679). Plans moved to `_archive/`.
  - 2 open code-review items (`code-review-mobile-rn-screens-mock-data` → #1681, `code-review-api-core-osrng-expect` → #1684).
- **Decayed (-1 this run):** 6 items (all open, updated_at ≤ 2026-06-10).
- **Dropped via decay (→ status=dropped):** 10 items (score reached 0).

## Plans promoted
- **none this run.** After resolutions, only `revert-pr-1713` (dx, score 3, high) and `bug-ios-searchview-uncompilable` (bug, score 3, ready, plan exists) cross the threshold. The revert is informational, not a fix-as-one-PR shape — declined. `bug-ios-searchview-uncompilable` already has its plan (`plans/bug-ios-searchview-uncompilable.md`).

## Auto-fix
- `no candidate` — no signal met `confidence=high` + `score≥3` + allowlist-shape this run. All 8 screen-map-drift signals are medium-confidence heuristic and score 2; no fixme-in-merged-code fired (subagent harvest didn't enumerate per-commit FIXME additions across 252 PRs — cost-prohibitive in this catch-up sweep); stalled-review had 0 candidates; dep-update-noise lockfile-drift sub-shape didn't surface.

## Self-review findings
- `.research/self-improvement/findings.json` absent — none

## Open questions
- **Why did the cloud cron stop firing for 7+ days?** The dispatcher loop kept running (separate scheduler); the routine specifically went quiet. Investigate the cron source on the cloud side — could be the trigger pipeline, the route, or the credentials.
- **Phase 1.6 PM rotation was skipped this run** (catch-up budget). Last role-run dates as of HEAD: pm-security 2026-05-27 (28d), pm-data 2026-05-28 (27d), pm-integration 2026-05-29 (26d). Recommend triggering `pm-full` or `pm:security` on next run to catch up.
- **Phase 1.5 expert review was skipped this run** (cursor advanced for `ppt-web-core` to today without a real review). Next run should run the real review on the new oldest segment.
- **`p0-10-mock-route-wiring.md` (hand-authored, pre-existing)** doesn't match the canonical 11-heading template — surface for the brief but don't rewrite per the routine's hand-edit policy.
- **Two pre-existing duplicate plan files** (`plans/security-realtors-mark-inquiry-read-idor.md` and `plans/test-gap-inquiry-idor-regression.md`) also exist in `_archive/`; their backlog rows are `done` with `plan` pointing to the archive copy. These are stale leftovers from earlier runs — clean up manually or in a future routine maintenance run.

## Quiet day?
- **No.** 252 PRs merged, 16 backlog resolutions, 17 new vectors, 7-day routine catch-up, 8 screen-map-drift signals, 1 revert, 0 promoted plans (deliberate — quality over volume on a catch-up sweep). `stats.runs` 40 → 41, `vectors_created` 173 → 190.
