# 2026-06-24

## Since last run
- Merged PRs: 95 (range #1567–#1822)
- Open PRs touched: 21 (mostly drafts of post-merge follow-up fixes)
- New / updated issues: 81 new, 69 updated (31 new still OPEN + unlabeled → triage queue)
- Commits: 49 non-merge commits on `dev` (most are dispatcher chore commits)
- Routine lag: **8d 0h since last run** — `last_run_iso=2026-06-16T03:25:00Z`; exceeds 36h. **`stale_routine_alert` fired** (≥3d): cloud cron may be paused. The dispatcher (separate planning branch) is still ticking per recent commits.
- Phases that failed: none

## Shipped
Highlights (full 95-PR list in `scratchpad/phase1_pr_summary.json`):

- #1822 — `79-2-authentication-flow`: reality-web SSO `/auth/callback` e2e integration tests (8 cases). Closes `gap-79-2-auth-callback-e2e`; coverage.json now flips 79-2 to **done**.
- #1821 — Epic ACC (Invoicing & Accounting MVP) initial scaffold + standalone accounting-server on **:8082**.
- Churn-hotspot route/repo split wave (move-only): #1796 (subscription repo), #1798 (emergency route), #1800 (enhanced_tenant_screening route), #1810 (sensor repo), #1813 (vendors route), #1815 (iot route), #1816 (reserve_funds route). PRs #1812 (reality_portal repo) and #1814 (form repo) remain stuck — see Watch.
- Story 11.6 + 11.7 — financial scheduler + reports/PDF/xlsx (#1709, #1717).
- Epic 14 IoT — BIT-145 WebSocket channel + alerts page (#1685, #1737, #1740).
- Epic 13 — Sentiment & Predictive Maintenance dashboards (#1641).
- Epic 18 Story 18.2 — Guest ID-doc OCR seam (#1750, #1703).
- Messaging — camelCase wire flip + N-party plumbing (#1768/#1756/#1802/#1689/#1696/#1702).
- BIT-185 fault notifications (#1705); follow-up gap (#1793) filed for two missing transitions (`triage_fault`, `confirm_fault`).
- #1713 — `revert(delegations)`: remove re-added delegation frontend (#1690) — reconciles with BIT-213 retirement (intentional, closed in backlog).

## Watch
- **Stalled / blocked PRs** —
  - PR #1812 (reality_portal.rs churn-split) — CI failing exit 126; `check-rls-enforcement.sh` lost exec bit on `dev` (100644 vs 100755). Needs manual `chmod +x` + push. Owner: pm-tech-lead.
  - PR #1814 (form.rs churn-split) — modify/delete conflict vs `dev` post-#1781; needs manual rebase. Owner: pm-tech-lead.
- **Reverted:** #1713 reverts #1690 (delegations) — intentional BIT-213 retirement; backlog row closed, no follow-up.
- **Churn hotspots this run (top-3, after exclusions):**
  - `backend/crates/db/src/repositories/document.rs` — 6146 lines (runs_seen=1; mostly PR #1683 repo split).
  - `backend/servers/api-server/src/routes/emergency.rs` — 3362 lines (runs_seen=1; mostly PR #1798 route split).
  - `backend/crates/db/src/repositories/rental.rs` — 2939 lines (runs_seen=1; mostly PR #1750 Story 18.2 OCR).

## PRs stuck in draft despite approval
<!-- list_pull_requests MCP envelope does not carry reviewDecision; this run skips the precise filter. Cross-check on next run when MCP exposes it. -->
- none observed this run (MCP envelope omits `reviewDecision`; not reliably checkable)

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Source: planning branch .research/management/assignments.json. Not fetched this run (saves a planning-branch round-trip). -->
- none observed this run (planning-branch fetch skipped)

## Code review slice
- Segment reviewed: **api-handlers** (reason: churn-aligned — `routes/emergency.rs` + `routes/auth.rs` both in top-7 churn)
- Experts: rust + security
- Findings: 3 (see Backlog deltas for `code-review-finding` signals)
  - `code-review-api-handlers-platform-admin-jwt-only-gate` — **HIGH × security × confidence=high** — `subscriptions.rs:69-132` & `feature_packages.rs:49-112` gate all platform-admin writes on JWT role claim only; revoked super-admin retains power until JWT TTL.
  - `code-review-api-handlers-agencies-unwrap-as-ref` — MEDIUM × bug × medium — brittle `is_none() || X.as_ref().unwrap().field` guards at `agencies.rs:747, 774, 867`.
  - `code-review-api-handlers-role-const-drift` — MEDIUM × security × high — `SUPER_ADMIN_ROLES` / `ADMIN_ROLES` constants drift across `feature_packages.rs:29-34`, `subscriptions.rs:49-54`, `admin/users_lifecycle.rs:140-146`.
- Next segment: **ppt-web-core** (oldest unreviewed, 2026-06-05)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 140 (`ppt/` = 93 · `reality/` = 47 · `mobile/` not yet seeded → 0)
- Drift signals this run: 0 (one candidate from PR #1822 dropped — touched only `reality-web/src/app/[locale]/auth/callback/page.test.tsx`, a test file, not a route; false-positive per heuristic caveat in routine-prompt.md)
- Orphan screens this run: 0 (no orphan scan ran — basename-vs-slug heuristic flagged as best-effort in routine spec)
- Last `screen-map.yml` CI run on `main`: not fetched this run (cloud constraint)

## Backlog deltas
- **New (46 items)**:
  - 1 × revert (`revert-pr-1713` — score 3; closed as intentional retirement)
  - 3 × churn-hotspot (document.rs, emergency.rs, rental.rs — emergency.rs/document.rs already resolved by PR #1798/#1683; rental.rs open at score 1)
  - 8 × unchecked-todo (PRs #1768 #1709 #1717 #1705 #1703 #1685 #1641 #1723 — score 3 each, but **confidence=low** so cannot promote without a follow-up signal)
  - 2 × hotfix-no-test (PRs #1757 dup-migration, #1751 dispatcher — score 2 each)
  - 31 × triage (untriaged-issue: open unlabeled issues #1758–#1793 + #1680)
  - 3 × code-review-finding from Phase 1.5 (see Code review slice)
- **Bumped:** 0 (all signals matched existing items via already-seen IDs cumulatively, OR landed as net-new)
- **Decayed (6):** open items with `updated_at <= 2026-06-10` lost 1 point each.
- **Dropped to 0 / status=dropped (13):** stale items that decayed past 0 — sample: `churn-hotspot-backend-servers-api-server-src-routes-vendors-rs`, `churn-hotspot-backend-crates-db-src-repositories-subscription-rs`, `code-review-mobile-native-kmp-deeplink-android-bypasses-shared-router`, several stale mobile-native + churn rows.
- **Resolved by merged PRs (10):** `churn-hotspot-backend-servers-api-server-src-routes-emergency-rs` (PR #1798), `churn-hotspot-backend-crates-db-src-repositories-document-rs` (PR #1683), `churn-hotspot-backend-crates-integrations-src-booking-rs` (PR #1693), `churn-hotspot-backend-servers-api-server-src-routes-reserve-funds-rs` (PR #1816), `churn-hotspot-backend-crates-db-src-repositories-sensor-rs` (PR #1810), `churn-hotspot-backend-crates-db-tests-form-rls-repo-tests-rs` (PR #1719), `churn-hotspot-frontend-apps-mobile-app-config-icon-test-ts` (PR #1718), `code-review-api-core-osrng-expect` (PR #1684), `repeated-churn-backend-servers-api-server-src-routes-forms-rs` (PR #1700), `code-review-mobile-rn-screens-mock-data` (PR #1681).

## Plans promoted
- `plans/code-review-api-handlers-platform-admin-jwt-only-gate.md` — Replace JWT-claims-only `require_super_admin` in `subscriptions.rs` + `feature_packages.rs` with DB-backed `require_platform_admin` middleware (canonical pattern from `infrastructure.rs` / `marketplace.rs` post-#1741/#1746). · adversarial pass: passed
- Not promoted: `code-review-api-handlers-role-const-drift` (score 2, security, high — passes security fast-track, but its fix is a side-effect of the JWT-only-gate plan above; promoting both would create overlapping file edits / merge conflicts).
- Not promoted: `revert-pr-1713` (intentional retirement — closed as `done` in backlog with no plan).

## Auto-fix
- `no candidate` — no signal this run matched the Phase 5 allowlist (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, lockfile-drift, `stalled-review`). Phase 1.5 findings and untriaged-issue signals go to the manual-implementer path.

## Self-review findings
- open=8 (high=2, medium=4, low=2); new this run=0; acknowledged=19
- fp-? [recurrence=5] — Phase 6 archive-move produced a 198KB assignments-archive.json that exceeded MCP push_files' inline limit; the merged PR#1019 row was dropped from active but its archive record could not be persisted; fix: adopt PR+API-merge path (push to new branch, open PR, merge via API) as primary archive-push mechanism — already validated in PR #1163.
- fp-? [recurrence=3] — Phase 2.6 buffer reported claimable=54/open=54 but real claimable=0; all open action-list items already terminal in active+archive; fix: extend gc1-reconcile to auto-close ANY open id terminal in active+archive incl. status=failed, AND fix the Phase 2.6 buffer-guard + goal-gate GC3 formula to subtract archive ids (T15 self-test needs the same guard).

## Open questions
- The `code-review-api-handlers-role-const-drift` finding overlaps with `platform-admin-jwt-only-gate` (both touch the same SUPER_ADMIN_ROLES constants). Confirm the implementer collapses both fixes into the one PR rather than splitting; the planner has already opted for one plan to avoid merge conflicts.
- Are issues #1758–#1793 (31 open follow-up items now in the triage queue) supposed to be picked up this sprint, or batched into next sprint planning? `pm-scrum-master` flagged the same question.
- ACC Epic (PR #1821, :8082 standalone server) landed scaffold + server with no sprint planning or owner assignment in `sprint-status.yaml`. Owner decision needed before further implementation merges.
- Cloud cron has been stale for 8 days. Is there a known reason (manual disable, infra incident), or is it an unannounced outage that should be escalated to pm-devops today?

## Quiet day?
- **No.** 95 PRs merged, 46 new backlog items, 1 plan promoted (high-confidence security finding), 3 code-review findings, 31 triage items added.
