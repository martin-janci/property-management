# 2026-06-19

## Since last run
- Merged PRs: **104 real** + 9 dispatcher/research + 19 closed-not-merged (range #1440–#1603, 50% squashed since cursor). Window: 2026-06-16T03:25Z → 2026-06-19T02:15Z (~70h, 2d22h).
- Open PRs: **5 total** (1 needs-human-review on `dev` — #1554 RN app icons; 4 dependabot from 2026-06-18).
- New / updated issues: **27 new** (#1519–#1594; 15 closed, 12 still open) + 53 existing-issue updates (mostly close-outs as follow-up PRs landed).
- Commits on `dev`: 51 (origin/dev latest 4324bfdf, was 4829015b).
- Routine lag: **70h** (2d22h) — emitted `lag-warning-2026-06-19`; under 3d threshold so no `stale_routine_alert`.
- Phases that failed: **none**. Phase 1.5 (code review) and Phase 1.6 (PM rotation) **skipped** this run — see *Code review slice* and *Skipped phases* below.

## Shipped
Three-day catch-up — grouped by theme rather than per-PR:

**Backend `test`-job stabilization cascade (RESOLVED #1437):** the schema-drift + RLS-grant + clippy red trunk inherited from PR #1426 was finally cleared by an interlocking sequence: **#1469 / #1481 / #1487 / #1488** (schema-drift SQL repair + reserve-fund retirement + rustfmt), **#1486 / #1492 / #1494 / #1505** (rental enum cast sweep — `RentalGuest`, `CalendarBlock`, `ICalFeed`, platform/status), **#1495 / #1496 / #1498 / #1499** (form RLS grant + form_submission_status / count_accessible cast), **#1537 / #1551** (form/rental_guests/document_category casts), **#1512 / #1581** (post-merge rustfmt+clippy gates), and the final **#1579** un-ignoring the 3 Airbnb reconcile tests. `dev` is green; #1437/#1331/#1332 closed.

**Security hardening — BIT-* cluster:**
- **#1467** (closes #1302, #1304): mfa.rs `verify_recovery_code` RLS-enforce + cross-user IDOR test.
- **#1471**: RLS-routing test coverage — ESG FORCE policy, webhook repo, public platform tables (closes #1305–#1307).
- **#1460**: FORCE RLS on `developer_oauth_apps`/`grants`/`api_key_usage_logs` (BIT-76, closes GH #1303).
- **#1463 + #1480** (BIT-117 / GH #1413): document download/preview building/unit scope gate + listing endpoint.
- **#1457** (BIT-75): org-scope `create_appeal`/`add_evidence`/`add_comment` writes.
- **#1473** (BIT-85): cross-tenant OAuth upsert hazard + manager gate (closes #1405).
- **#1472 / #1502**: Booking.com basic-auth credential encryption-at-rest (BIT-98) + IG3 contract tests (closes #1362).
- **#1552 / #1601** (closes #1525, #1585): OTA token-exchange manager gate now derives from `TenantRole` on the path org (no JWT-tenant trust); Airbnb cross-org test pinned.
- **#1580 / #1602** (closes #1523, #1583): MFA recovery-code verify endpoint rate-limited (per-user 1min + per-IP 30min), stale entries evicted, dedicated `mfa_recovery_throttled` audit action.
- **#1490 / #1482**: `check-rls-enforcement.sh --strict` fails on stale baseline entries (closes #1340).

**IDOR + reality-server:** **#1501** handler-level IDOR regression for imports/feeds (PAP-142); **#1561** PortalPrincipal admits public agencies on imports (closes #1300); **#1603** test pin (closes #1584 finding 3).

**Marketplace + invitations:** **#1491** idempotent `mark_invitation_viewed` (closes #1301); **#1564** test asserts preserved first-view timestamp (closes #1524).

**Atomicity / decimal / DB integrity:** **#1597** invoice line rounding (#1522 finding 3); **#1489** nullable rental_bookings Decimal decode (closes #1422); **#1598** `u.name` (not `u.full_name`) in board_meetings.rs (closes #1586); **#1565** document get-by-id read path.

**Booking.com OTA + Airbnb integration buildout (NEW SURFACE — drives this run's top churn):**
- **#1485**: client timeout + Retry-After + RateLimited reliability (BIT-114).
- **#1560**: combined OTA rate/availability push flow with partial-failure outcome (Coverage 83-2 AC-5).
- **#1577**: typed OTA `HotelRateAmountNotif` RQ/RS models (Story 83.2).
- **#1562 / #1599**: XXE/DTD-safe parser + non-vacuous entity guards (closes #1519, #1591).
- **#1578**: cast booking-create enum binds + dedupe BOOKING_COLUMNS + NULL-fee round-trip (closes #1535).

**Mobile/native:**
- **#1465**: iOS SearchView stale-response guard + staging deep-link host + orphaned Info.plist pruned (closes GH #1267, #1365, #1407–#1410). **Closes legacy backlog rows `bug-ios-searchview-uncompilable` + `bug-ios-deeplink-info-plist-missing`.**
- **#1464 / #1582 / #1557**: mobile folder organization (Coverage 7a-2) — useFolderTree + FOLDER_TREE_QUERY_KEY now wired into document screens (closes #1532).
- **#1541 / #1542 / #1547**: SwiftUI listing-detail polish + iOS UI tests (Story 82-4).
- **#1556 / #1450**: FCM + APNs OS push dispatch (Coverage 8a-3).
- **#1447**: AC-4 infinite scroll evidence on Reality Search (KMP).

**Frontend / ppt-web:**
- **#1454** + **#1453**: native accounting MVP (N1–N4) + bank-statement upload / payment matching (PAP-210) — large new surface area; **issues #1521, #1522 flagged wrong-app placement + auth-interceptor bypass**.
- **#1446 + #1559**: announcement pin/unpin web UI + optimistic comment add (Story 6.4/6.3).
- **#1555**: wire `useDeleteFault` + `useFaultStatistics` into FaultsPage list (gap-79-1).
- **#1468**: disputes — watch subscription + flush-on-unmount + i18n keys (closes #1364, #1404).
- **#1545**: reality-web `/accounting` QueryClient prerender unblocked.

**Process + tooling:**
- **#1452 / #1455**: dev-push compile gate + CI fmt+clippy lint mirror (closes the #1426→#1437 regression class).
- **#1441 / #1517 / #1442 / #1510**: pre-push fmt/clippy local hook + IDOR-test canonical `seed_membership` migration (closes #1373, #1375).
- **#1483 / #1448**: dispatcher Tier-2 kick payload fix + populate `depends_on` (closes parts of #1151, #1380, #583 cluster).
- **#1445 / #1466**: research docs — backlog.json projection sufficiency + extracted merge-path prompt anchor (closes #1309, #1310).

## Watch
- **Churn hotspots (top-3 real-code this run):**
  - `backend/crates/integrations/src/booking.rs` — **4365 lines**, 5 commits, **runs_seen 1→2** (`repeated-churn`). Healthy churn driven by OTA buildout.
  - `backend/crates/db/src/repositories/document.rs` — **3076 lines**, 2 commits, **runs_seen 1→2** (`repeated-churn`). PR #1565 fix shipped without test diff → see `risky-churn-pr-1565-document-rs`.
  - `backend/crates/db/src/repositories/rental.rs` — **2853 lines**, 2 commits (PR #1578 has test diff in `rental_booking_nullable_decimal_tests.rs` — not risky-churn).
- **Risky-churn:** `risky-churn-pr-1565-document-rs` (+2, bug, high) — fix-class PR touched a top-3 hotspot with no test in the diff. The canonical instability pattern.
- **Open follow-ups worth tracking (12 still open):** #1521/#1522 (accounting UI wrong-app + auth bypass — PR #1453/#1454 follow-ups, ~user-visible regression in payment-matching), #1538 (backend `test` job not a required check on `dev` — same gap that let #1426 land red), #1592 (rustfmt `lint` job also not blocking). The dev-push compile gate (#1452) caught some of this but neither test nor lint is a *required* status check yet.
- **Closed-not-merged (19 in window):** all dispatcher-parallel duplicates of merged PRs — no surface to act on individually. Pattern: dispatcher opens multiple attempts at the same fix and the strongest one wins.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **— (skipped)**.
- Experts: none.
- Findings: 0.
- Next segment: **ppt-web-core** (last reviewed 2026-06-05 — 14d stale; oldest unreviewed after this run). Backend churn (booking.rs / document.rs / rental.rs) sits inside `db` and `integrations` crates — neither maps to one of the 8 review segments, so no churn-aligned override.
- Skipped: **yes — 3-day catch-up window (104 real PRs to triage)** consumed the routine budget; next run will pick up `ppt-web-core`.

## Screen-map status
- Total `docs/screens/` files: **144** (ppt: 80, reality: 47, reality-mobile: 13, mobile: 0).
- Drift signals this run: **2** (`screen-map-drift-pr-1559-ppt`, `screen-map-drift-pr-1555-ppt`) — both medium-confidence heuristic, both touching existing route surfaces (announcements / faults) without docs/screens/ppt edits.
- Orphan screens this run: **0** (orphan detection deferred — best-effort scope reduced for this catch-up sweep).
- Last `screen-map.yml` CI run on `main`: not fetched (no `docs/screens/**` edits to trigger on the routine commit).

## Backlog deltas
- **New (18):**
  - [2] `risky-churn-pr-1565-document-rs` — bug; document.rs top-3 hotspot patched without tests.
  - [2] `screen-map-drift-pr-1559-ppt` — test-gap (heuristic).
  - [2] `screen-map-drift-pr-1555-ppt` — test-gap (heuristic).
  - [1] `repeated-churn-backend-crates-integrations-src-booking-rs` — refactor; runs_seen 1→2.
  - [1] `repeated-churn-backend-crates-db-src-repositories-document-rs` — refactor.
  - [1] `churn-hotspot-backend-crates-db-src-repositories-rental-rs` — refactor.
  - [1] × 12 `triage-issue-{1520,1521,1522,1533,1538,1584,1587,1588,1589,1590,1592,1593}` — open follow-up issues.
- **Bumped:** none beyond cumulative re-emits (booking.rs / document.rs churn-hotspots re-emitted with new evidence at score_delta=0).
- **Decayed (open, ≥14d):** `bug-risky-churn-pr-963-api-main-rs` (2→1), `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` (2→1), `test-gap-screen-map-drift-pr-1085-reality` (2→1), `test-gap-screen-map-drift-pr-1100-ppt` (2→1).
- **Dropped (decayed to 0):** `triage-dispatcher-mcp-push-large-file-issue-1014`, `triage-issue-951`, `refactor-churn-hotspot-mobile-announcements`, `refactor-churn-hotspot-mobile-announcements-test`, `refactor-churn-hotspot-mobile-documents`.
- **Resolved (→ done via PR match + evidence-gone):**
  - `bug-ios-searchview-uncompilable` — PR #1465 closes #1267 reclassification (SearchView compile fix + orphaned plist prune). Plan moved to `plans/_archive/`.
  - `bug-ios-deeplink-info-plist-missing` — PR #1465 closes #1267/#1408/#1409/#1410 (xcconfig pruned, deep-link wiring landed).
  - `test-gap-hotfix-no-test-pr-1292-mfa-rls` — PR #1467 closes #1302, #1304 (verify_recovery_code RLS-enforce + IDOR test).
  - `test-gap-hotfix-no-test-pr-1287-rls-llm-sessions` — PR #1471 closes #1305 (ai/sessions + calendar sync RLS-routing tests).
  - `test-gap-hotfix-no-test-pr-1288-webhook-rls` — PR #1471 closes #1306 (RLS-routed webhook repo paths).

## Plans promoted
- **0 plans promoted this run.** Promotion budget intentionally held back during the 3-day catch-up — the 6 existing ready plans (`security-llm-doc-idor`, `code-review-reality-web-{share-comparison-404, listing-page-ssr-crash, realtor-mgmt-untranslated}`, `code-review-mobile-rn-report-fault-fake-submit`) cover the highest-value queue; new candidates (risky-churn-pr-1565-document-rs, screen-map-drift drafts) sit at score 2 below the ready threshold. Next run will reassess after Phase 1.5 fires.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The two `screen-map-drift` signals are medium-confidence (heuristic caveat); `risky-churn` is not in the auto-fix allowlist (bug-class, code-change); `fixme-in-merged-code` was not scanned (3-day window too large for per-PR body scan); no lockfile-drift dep-update detected; no `stalled-review` ≥7d (only 5 PRs open and #1554 is 1d old). Kill switch not set.

## Self-review findings
- `.research/self-improvement/findings.json` absent — none

## Open questions
- **Backend `test` not required (#1538) + rustfmt `lint` not required (#1592)** — same status-check gap that allowed #1426 to land red on dev. The dev-push compile gate (#1452) lands later in the pipeline. Worth promoting to branch protection: add both to required checks before the next release cut.
- **Accounting MVP placement (#1521 / #1522)** — payment-matching UI shipped under the wrong app (`/accounting` route on `reality-web` instead of `ppt-web` per the original Coverage 13/14 spec), with `@hey-api` auth interceptor bypassed. Needs an architectural decision on which app owns native accounting before more N5/N6 work lands.
- **Skipped Phase 1.5 / 1.6** — heavy catch-up consumed budget; next run will rotate through `ppt-web-core` review and `pm-security` PM role (23d stale).

## Quiet day?
- **No.** 104 real PRs + 9 dispatcher PRs landed across a 3-day catch-up window. 18 new backlog vectors + 5 resolutions + 4 decays + 5 drops. 0 plans promoted (queue is full of ready plans already). Stats: runs 40→41, vectors_created 173→191, plans_created 12 (no change), quiet_days 1 (no change).

## Skipped phases (this run)
- **Phase 1.5 — Code review:** skipped to keep budget for catch-up triage.
- **Phase 1.6 — Project Management & Delivery:** skipped this run; `pm_cursor` + `coverage_cursor` left at `next_index=5` (pm-security) / `next_index=12` (epic at index 12). G16 is a no-op when Phase 1.6 is skipped. Next run will run pm-security (23d stale) and rotate the coverage cursor.
