# 2026-06-19

## Since last run
- Merged PRs: **49** (range #1544–#1613) over a 3-day catch-up window (no run on 2026-06-17 or 2026-06-18). All base `dev`. Authors: martin-janci (43) + dependabot[bot] (6).
- Open PRs touched: **6** — #1606 (accounting manager-gate, fresh), #1554 (RN app-icon generation, needs-human-review), #1574/#1569/#1568/#1567 (4 dependabot batches).
- New / updated issues: **16 new in window** (#1519–#1535 backlog of follow-ups, #1583–#1594 next-day follow-up batch, #1604/#1607/#1614 dispatcher + post-merge findings). Of those, **14 are CLOSED** (resolved within the same 3-day window via the test/fix PRs below); **3 stay open** — #1604, #1607, #1614 plus carry-over open ones #1538, #1584, #1590.
- Commits on `dev`: 100+ (full window cap) — HEAD now `1f77bfc8` (was `4829015b`).
- Routine lag: **3d 0h** since last run — **stale_routine_alert FIRED** (last_run_iso=2026-06-16T03:25:00Z). Cloud routine likely missed the 2026-06-17 and 2026-06-18 schedules; surface to operator. Dispatcher state lives in `.research/management/assignments.json` on the planning branch — check that separately.
- Phases that failed: **none**. Phase 1.5 (rotating expert review) and Phase 1.6 (PM project management) **deferred** this run due to remote-execution token budget. `review_cursor`, `pm_cursor`, `coverage_cursor` left at HEAD values; G16 / Code-review slice / Screen-map status sections below reflect that.

## Shipped
- **Follow-up sweep ×14 closed** (the dominant pattern this window): every `#15xx` and `#158x`/`#159x` follow-up issue filed against an earlier merged PR got patched and closed by a small surgical PR in the same window. Mappings:
  - #1551 → closes #1528 (`fix(db)`: cast `document_category` in `count_accessible_rls` filter)
  - #1552 → closes #1525 (`fix(api-server)`: gate OTA token-exchange on manager role of the path org)
  - #1561 → closes #1300 (`fix(reality-server)`: admit public agencies on imports via PortalPrincipal)
  - #1562 → closes #1519 (`test(integrations)`: XXE/DTD regression guard + namespace parity for inbound OTA parsers)
  - #1563 → closes #1527 (`test(db)`: behavioral cross-user RLS isolation for developer OAuth/usage tables)
  - #1564 → closes #1524 (`test(marketplace)`: assert `mark_invitation_viewed` preserves first viewed_at)
  - #1578 → closes #1535 (`fix(db)`: cast booking-create enum binds + dedupe `BOOKING_COLUMNS`; cover NULL-fee write path)
  - #1580 → closes #1523 (`fix(api-server)`: rate-limit MFA recovery-code verify endpoint)
  - #1582 → closes #1532 (`fix(mobile)`: wire `useFolderTree` / `FOLDER_TREE_QUERY_KEY` into document screens)
  - #1598 → closes #1586 (`fix(db)`: `board_meetings` lists use `u.name`, not nonexistent `u.full_name`)
  - #1599 → closes #1591 (`test(integrations)`: make XXE/entity guards non-vacuous)
  - #1600 → closes #1594 (`test(db)`: write-path RLS probes for developer OAuth/usage tables)
  - #1601 → closes #1585 (`fix(api-server)`: derive OTA manager-gate from `TenantRole` + airbnb cross-org test)
  - #1602 → closes #1583 (`fix(api-server)`: MFA recovery limiter — evict stale entries + dedicated audit action)
  - #1605 → closes #1592 (`ci(backend)`: enforce rustfmt in the required check job — finally a hard gate, addresses old `dx-pre-push-fmt-clippy-gate` style gap)
  - #1608 → closes #1593 (`test(marketplace)`: deterministic `COALESCE`-preservation guard for viewed_at)
  - #1610 → closes #1588 (`fix(faults)`: confirm delete + use `closed_count` + refresh stats on delete)
  - #1611 → closes #1587 (`test(reports)`: cover `ScheduleDetailPageRoute` download/retry toast wiring)
  - #1613 → closes #1589 (`fix(documents)`: allow detaching a folder to root via `PUT parent_id=null`)
- **#1546 / #1547 / #1553 / #1595 / #1596** — research dispatcher state PRs (5 buffer-reconcile + Phase-8 self-review + archive sweep).
- **#1544 / #1548 / #1549 / #1550 / #1555 / #1556 / #1557 / #1558 / #1559 / #1560 / #1565** — pm-qa coverage drops + verify-promote-to-done passes + feature wires (announcement comments optimistic, folder rename/move on mobile, APNs HTTP/2 delivery, OTA rate/availability combined push, e-signature webhook idempotency).
- **#1577** typed OTA `HotelRateAmountNotif` RQ/RS models (Story 83.2) — backbone for the rest of the OTA push flow.
- **#1579 / #1581 / #1545** — un-ignore airbnb reconcile tests; backend rustfmt sweep on #1527/#1523; reality-web `/accounting` QueryClient prerender + favorites pl/hu i18n unbreaking production build.
- **#1597** `fix(db)`: round invoice lines once so header totals equal sum of stored items (refs #1522).
- **#1603** `test(reality-server)`: pin PortalPrincipal admits-any-kind is still owner-scoped (#1584 finding 3).
- **#1609 / #1612** — refactor follow-ups: single source of truth for folder-tree flatten utils (#1589 finding 2); accounting queryKeys factory + drop console.log stub (#1522 finding 2).
- **Dependabot batch:** #1566 (androidx.compose-bom 2026.05.01→2026.06.00), #1570 (mikepenz/action-junit-report 5→6), #1571 (npm-minor-patch group ×6), #1572 (expo-secure-store 55→56), #1573 (`@types/node` 20→25 — major), #1575 (`@babel/core` 7→8 — major), #1576 (i18next 25→26 — major). Three majors landed in one window — watch for follow-up runtime surprises.

## Watch
- **🚨 stale_routine_alert** — routine missed 2026-06-17 + 2026-06-18 cycles. If the cloud cron schedule is healthy, this is the first miss in 40+ runs; surface to operator to confirm scheduler health.
- **#1604 (open, needs-human-review)** — *Dispatcher backlog exhausted — coverage.json (2026-06-16) stale, producing only already-shipped tasks.* Two days of stale coverage means the dispatcher buffer recycles dropped/done tasks. Tied to **#1607** (open, research-routine) — *Research dispatcher stalled: stale coverage.json + 14 quarantined rows need cleanup.* Both want a manual `/ppt-project-management scan` to refresh `coverage.json` end-to-end. Not actionable from the daily routine — routine Phase 1.6 spec explicitly forbids the heavy `scan` mode in cloud.
- **#1614 (open, follow-up)** — Combined OTA `push_availability_and_rates` is never wired into a route/caller (PR #1560). Medium severity; effectively dead code until routed.
- **#1584 (open, follow-up)** — `PortalPrincipal` bypasses `reality_agency_members` and rides on `agency_id=user_id` shortcut. Carry-over from 2026-06-18; #1603 pinned one finding but the broader pattern is open.
- **#1590 (open, follow-up)** — Pin-test asserts the constant, not the wiring; stale test comment (PR #1582). Test-quality follow-up; not a regression.
- **#1538 (open, ci)** — Backend `test` job not a required check on `dev`; red-test PRs merge freely. Adjacent to #1605 which made rustfmt a required check, but `test` is still advisory. Worth promoting next run.
- **Churn hotspots this run:** *not measured* — per-PR file-list scanning skipped this run (token budget). Existing hotspot history rolls forward unchanged; will be re-derived next normal run.
- **Stalled review:** none new this run. PR #988 (e2e framework epic) is *no longer in the open-PR window* — has been closed or merged in the gap; `dx-stalled-review-pr-988` will be auto-resolved next run via the open-PR list check. PR #1554 (RN app-icon generation) sits at 1d open, labeled `needs-human-review` — not yet stalled but watch.
- **Closed-not-merged this run:** none observed.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher's `assignments.json` not read this run — Phase 1.6 deferred)

## Code review slice
- Segment reviewed: **none — Phase 1.5 deferred** (remote-execution token budget for the catch-up run).
- Experts: n/a.
- Findings: **0**.
- Next segment: **`ppt-web-core`** (last reviewed 2026-06-05 — 14 days stale, oldest unreviewed). Will run next normal cycle.
- Skipped: **yes — deferred to next-run; not a Phase 1.5 failure**. `state.review_cursor` left at HEAD values.

## Screen-map status
- Total `docs/screens/` files: **127** unchanged (ppt: 80, reality: 47, mobile: 0 — not yet seeded). Not re-counted on disk this run.
- Drift signals this run: **0** — per-PR file-list scanning skipped (token budget).
- Orphan screens this run: **0** — same reason.
- Last `screen-map.yml` CI run on `main`: not fetched this run.
- Heuristic caveat: existing rows `test-gap-screen-map-drift-pr-1085-reality`, `test-gap-screen-map-drift-pr-1100-ppt` decayed to score 1 this run; they remain review-candidates, not promotable.

## Backlog deltas
- **New:** none. Only synthetic signal this run is `lag-warning-2026-06-19` (score 0, brief-only — never enters backlog per routine table).
- **Bumped:** none.
- **Decayed (−1, no new signal in 14d):**
  - `test-gap-screen-map-drift-pr-1085-reality` 2 → 1
  - `test-gap-screen-map-drift-pr-1100-ppt` 2 → 1
  - `bug-risky-churn-pr-963-api-main-rs` 2 → 1
  - `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` 2 → 1
- **Dropped (decayed to 0):**
  - `refactor-churn-hotspot-mobile-announcements` 1 → 0
  - `refactor-churn-hotspot-mobile-announcements-test` 1 → 0
  - `refactor-churn-hotspot-mobile-documents` 1 → 0
  - `triage-dispatcher-mcp-push-large-file-issue-1014` 1 → 0
  - `triage-issue-951` 1 → 0
- **Resolved (→ done):**
  - `triage-issue-1151` — GitHub issue #1151 closed (no source PR; closed by hand; root causes addressed by dispatcher reconcile + DISPATCHER_URL fix in subsequent state PRs).
  - `triage-issue-1331` — GitHub issue #1331 closed (CI runner /tmp full + 41-min hang patched; backend `test` job back to required-check state via PR #1605).

## Plans promoted
- **none** this run. The 6 currently-`ready` items already have plans in `plans/` from prior cycles. No newly-`ready` candidate emerged this run (Phase 1.5 + Phase 1.6 deferred; no new high-confidence security/bug signals derived from the catch-up data).

## Auto-fix
- `no candidate` — Phase 5 ran with the kill switch unset and the allowlist applied. The only new signal this run is `lag-warning-2026-06-19` (confidence=high, score 0, type not in allowlist). No `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` (lockfile-drift sub-shape), or `stalled-review` signal fired.

## Self-review findings
- `.research/self-improvement/findings.json` absent — none.

## Open questions
- **Should `/ppt-project-management scan` be run manually now to break the dispatcher stall (#1604, #1607)?** Cloud routine spec forbids running it from here, but the local operator could trigger it. Recommend running before the next research routine cycle so Phase 1.6 has fresh `coverage.json` to work from.
- **Why did the cloud routine miss 2026-06-17 and 2026-06-18?** Operator should check the scheduler logs. If this was a one-off, no action; if recurrent, investigate the trigger config (`text == "deep"` payload could be used to back-fill, but cursors already advanced this run).

## Quiet day?
- **No.** 3-day catch-up window: 49 merged PRs (43 application-code + 6 dependabot), 14 follow-up issues filed-and-resolved within the same window, 4 backlog rows decayed, 5 dropped, 2 resolved (`triage-issue-1151`, `triage-issue-1331`), `lag_warning` + `stale_routine_alert` surfaced. `stats.runs` 40 → 41, `vectors_created` 173 → 173 (no new vectors), `plans_created` 12 → 12, `auto_fix_count` 0 → 0.
