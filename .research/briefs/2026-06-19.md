# 2026-06-19

## Since last run
- Merged PRs: **79** (range #1440–#1609 — full span includes the 11 closed-not-merged + 8 dependabot; ~71 author-merged)
- Open PRs touched: **6** (2 fresh today — #1606 accounting list_contacts gate + dependabot pending #1567/#1568/#1569/#1574; plus #1554 RN app-icons awaiting human review)
- New / updated issues: **82 in window** (per `since=2026-06-16T03:25Z` query, totalCount=82). 30+ are `follow-up,from-merged-review` items emitted by the dispatcher's post-merge reviewer — **all labeled**, so none qualify for `untriaged-issue`. Notable open follow-ups still to fix: #1520, #1521, #1522, #1533, #1584, #1587, #1588, #1589, #1590. Two routine-self issues: **#1604** (Dispatcher backlog exhausted, coverage.json stale, needs-human-review) and **#1607** (Research dispatcher stalled, research-routine label).
- Commits on `dev`: 80+ since `4829015b` → `9287be4`.
- Routine lag: **3d 12h** since last run — **lag_warning** + **stale_routine_alert** both fired (LAG_HOURS=84, LAG_DAYS=3). Cloud scheduler appears to have missed two daily slots. Dispatcher (separate planning branch) kept running through this window — see #1546/#1547/#1553/#1595/#1596 dispatcher state PRs.
- Phases that failed: **none.**  Phases **skipped** (catchup-mode): **1.5** (rotating expert review), **1.6** (project-management rotation).

## Shipped
- **#1601 / #1602 / #1603 / #1605 / #1608 / #1609** — final pre-routine batch: OTA manager-gate hardening (PAP-285, closes #1585), MFA recovery limiter stale-entry eviction + audit action (PR #1602 closes #1583), PortalPrincipal owner-scope regression pin (#1603 finding 3 of #1584), CI rustfmt promotion to required check (closes #1592), deterministic COALESCE-preservation guard for `viewed_at` (closes #1593), folder-tree flatten refactor (#1589 finding 2).
- **#1580 / #1581 / #1597 / #1598 / #1599 / #1600** — MFA recovery rate-limit (closes #1523), rustfmt unbreak after #1527/#1523 stack landed unformatted, invoice header-totals rounding (closes #1597 — single-round-trip invariant), board_meetings `u.name` cast fix (closes #1586), XXE/entity-guard non-vacuous strengthening (closes #1591), write-path RLS probes for developer OAuth/usage tables (closes #1594).
- **#1577 (booking) / #1578 (db) / #1579 (un-ignore)** — typed OTA HotelRatePlanRQ parsing & generation; booking-create enum cast + dedupe BOOKING_COLUMNS + NULL-fee NaN-bind round-trip; un-ignore airbnb reconcile tests now that enum-cast bugs are fixed on dev.
- **#1551 / #1552** — `document_category` cast in `count_accessible_rls` (closes #1528), OTA token-exchange gated on manager role of the path-org (closes #1525).
- **#1543** — stabilized `AnnouncementsScreen` date tests; closes the two `refactor-churn-hotspot-mobile-announcements*` items in this routine (resolution detected via PR-body grep — see *Backlog deltas* below).
- **#1582** — wires `useFolderTree` / `FOLDER_TREE_QUERY_KEY` into document screens (closes #1532).
- **8 dependabot landings:** #1449 (npm-minor-patch), #1566 (Compose BOM 2026.05→2026.06), #1570 (mikepenz/action-junit-report 5→6), #1571 (frontend npm-minor-patch group), #1572 (expo-secure-store 55→56), #1573 (@types/node 20→25), #1575 (@babel/core 7→8), #1576 (i18next 25→26).
- **Dispatcher state PRs:** #1546 / #1547 / #1553 / #1595 / #1596 — dispatcher kept rotating + archive-sweeping (43 terminal rows archived 2026-06-18). The routine itself missed slots; the dispatcher's loop is separate and continued.

## Watch
- **Stalled review:** #988 (state 13+ days, already tracked as `dx-stalled-review-pr-988`); #1179 (already tracked as `dx-stalled-review-pr-1179`). #1554 (RN app-icons) is 1d old — not yet stalled. No fresh stalled-review fires this run.
- **Reverts:** **0** in window (the breakage chain around PR #1426 was patched forward).
- **Churn hotspots:** **not derived this run** (catchup mode skipped per-commit file analysis). Existing 14 open `churn-hotspot-*` rows carry over unchanged (none qualified for decay).
- **Routine-self issues:** **#1604** (dispatcher backlog exhausted, coverage.json 2026-06-16 stale) and **#1607** (research dispatcher stalled — stale coverage + 14 quarantined rows). Both labeled, neither qualifies for `untriaged-issue`. Surface for human attention.
- **Catchup-mode caveat:** Phase 1.5 (rotating code review) + Phase 1.6 (PM rotation) **skipped** to keep this run bounded — `review_cursor` + `pm_cursor` + `coverage_cursor` left untouched. Next normal-cadence run resumes both.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **none — Phase 1.5 skipped (catchup-mode)**.
- Experts: n/a
- Findings: **0** (no Phase 1.5 invocation this run).
- Next segment: **ppt-web-core** (oldest unreviewed at 2026-06-05 — 14 days stale; will be picked next normal run).
- Skipped: **yes — catchup mode**, 3.5d lag prioritized cursor advance + resolution-check + decay over deep review.

## Screen-map status
- Total `docs/screens/` files: same as last run (drift detection skipped this run — would require per-PR file-list fetch across 79 merges).
- Drift signals this run: **0** (not derived — catchup mode).
- Orphan screens this run: **0** (not derived — catchup mode).
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **Resolved (done):** `refactor-churn-hotspot-mobile-announcements` (was open, score 1) + `refactor-churn-hotspot-mobile-announcements-test` (was open, score 1) — PR #1543 merged 2026-06-17, title verbatim match against item slugs (PR-body grep hit).
- **Decayed (−1, still open):** `test-gap-screen-map-drift-pr-1085-reality` 2→1; `test-gap-screen-map-drift-pr-1100-ppt` 2→1; `bug-risky-churn-pr-963-api-main-rs` 2→1; `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` 2→1.
- **Dropped (decayed to 0):** `refactor-churn-hotspot-mobile-documents` (score 1→0, no new signals in 14d); `triage-dispatcher-mcp-push-large-file-issue-1014` (1→0); `triage-issue-951` (1→0).
- **New:** none — catchup-mode synthetic signals (`lag-warning-2026-06-19`, `stale-routine-alert-2026-06-19`, `catchup-window-pr-1440-to-1609-2026-06-19`) carry `score_delta: 0` and don't create backlog rows.
- **Net counts:** open 52→47 (−2 resolved, −3 dropped), ready 6→6, done 110→112, dropped 7→10.

## Plans promoted
- **none this run.** No open item meets the readiness gate (`score >= 3`, `confidence == high`) that isn't already promoted. The 6 currently-`ready` items (5 code-review findings + iOS SearchView uncompilable + LLM-doc IDOR) all have plans in `plans/` waiting for the manual implementer; promoting more would saturate the plan-output cap without serving the implementer who's already 6 deep.

## Auto-fix
- **no candidate** — no signal met confidence=high + score≥3 + allowlist this run (the 3 synthetic catchup-mode signals are score-0 audit-trail only; no `screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / `dep-update-noise` lockfile-drift / `stalled-review` derived).

## Self-review findings
- open=8 (high=2, medium=4, low=2); new this run=0; acknowledged=19
- fp-mcp-push-cannot-inline-large-archive [recurrence=5] — Phase 6 archive-move 198KB exceeds `mcp__github__push_files` inline ceiling; fix: PROVEN WORKAROUND — push state commit to a new branch, open PR via `mcp__github__create_pull_request`, land via `mcp__github__merge_pull_request` (squash); already used for PR #1163.
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer reports claimable=54 but REAL=0 (all 54 open action-list rows are archive-terminal duplicates); fix: extend gc1-reconcile to auto-close ANY open id terminal in archive (including `failed`), and subtract archive task_ids from the GC3 claimable formula + dispatcher-self-test T15 guard.

## Open questions
- **Cloud scheduler health:** state.json hasn't advanced in 3.5d despite the dispatcher continuing to commit. Is the routine scheduler paused, broken, or just slow? This run resumes manually — recommend the operator verify the routine's scheduled trigger.
- **Catchup-mode policy:** when lag ≥ 3d, should Phase 1.5 + 1.6 run in degraded form (single segment only, no full PM rotation) instead of skipping entirely? Current "skip both" leaves `review_cursor` + `pm_cursor` static across the lag window — next normal run picks up where the cursors left off, which is correct, but means the operator gets no PM digest until the routine catches up.

## Quiet day?
- **No** — 79 PRs merged in window, 2 items resolved, 3 items decayed-to-dropped, 4 items score-decayed, 3 synthetic signals emitted, cursors advanced significantly (last_pr_seen 1439→1609, last_issue_seen 1437→1607, last_commit_sha 4829015b→9287be4). Telegram digest send gated on `PM_DIGEST_QUIET=0` accordingly.
