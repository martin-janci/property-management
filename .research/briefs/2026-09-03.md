# 2026-09-03

## Since last run
- Merged PRs: 5 (range #2922–#2928)
- Open PRs touched: 2 (#2931, #2932 — both draft, both closing today's follow-up issues)
- New / updated issues: 2 (#2929, #2930 — both `follow-up`/`from-merged-review`, both already have draft PRs)
- Commits: 25 on `dev` (many are hourly dispatcher tick commits)
- Routine lag: 33h since last run (below the 36h `lag_warning` threshold — on schedule)
- Phases that failed: none

## Trigger payload
- `buffer-low: claimable=8/72 — cloud-landable backlog starved (all open items mobile-native/KMP-gated); refill planner with cloud-landable vectors` — this run explicitly targets cloud-landable vector generation via a `reality-server` code review + security fast-track promotion.

## Shipped
- #2922 code-review-reality-server-saved-search-error-string-status — derive saved-search HTTP status from a typed enum discriminant (adds `SavedSearchError::LimitReached`/`NotFound`/`Db`; kills `err.to_string().contains(...)` branches)
- #2925 code-review-api-handlers-compliance-raw-db-leak-regression — route compliance audit-log count error through `db_error`; adds `no_handler_bubbles_raw_db_error_to_client` grep-guard test
- #2926 code-review-api-core-fcm-legacy-dead-endpoint — remove decommissioned FCM legacy `/fcm/send` path from `push_fanout.rs` (169 lines churn; also drops `FCM_SERVER_KEY` env var)
- #2927 gh-issue-2923 — cover the `updateEmergencyContact` error path in `EmergencyContactDirectoryPage.errorI18n.test.tsx` (raises `err.message` vs translated copy)
- #2928 gh-issue-2924 — wrap `DataExportRepository::report_summary` in a REPEATABLE READ, READ ONLY transaction so aggregate counts + entries share one snapshot

## Watch
- Stalled review: #2555 / #2558 / #2559 (accounting UC-ACC-05 trio, ~36 d old, `needs-human-judgement`); #2744 dispatcher fix ~20 d draft.
- Reverted: none.
- Churn hotspots: `backend/servers/api-server/src/services/push_fanout.rs` (169, runs_seen=2 → repeated-churn fires), `backend/servers/reality-server/src/routes/saved_searches.rs` (106), `backend/servers/api-server/tests/suites/push_delivery_receipt_tests.rs` (99).

## PRs stuck in draft despite approval
- none  <!-- 4 open draft PRs (#2932, #2931, #2902, #2744) — none carry an APPROVED reviewDecision + updatedAt > 24h. #2931/#2932 opened today, #2902 opened 2d ago, #2744 opened 20d ago. -->

## PRs with verdict=changes, no fix-round progress in 24h
- none  <!-- assignments.json (this branch) has 2 `review` rows (`gh-issue-2743`, `screen-map-drift-pr-2894-reality`); neither carries a `reviewer_summary` starting with `verdict=changes`. -->

## Code review slice
- Segment reviewed: `reality-server` (reason: churn-aligned — `saved_searches.rs` was top-3 hotspot this run; cloud-landable, last reviewed 2026-08-31)
- Experts: rust
- Findings: 2 (see Backlog deltas for `code-review-finding` signals)
- Next segment: `mobile-native-kmp` (oldest unreviewed, 2026-08-25) — but pipeline is starved for CLOUD-LANDABLE work so next churn-aligned pick may bump; check `state.review_cursor` next run
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` 106 + `reality/` 47 + `mobile/` 0)
- Drift signals this run: 0 (no merged PR this run touched `frontend/apps/{ppt-web/src/{App.tsx,routes/**},reality-web/src/app/**}`)
- Orphan screens this run: 0 (no route-basename sweep triggered this run)
- Last `screen-map.yml` CI run on `main`: not queried (MCP `actions_list` interface changed; will resume next run)

## Backlog deltas
- **New:** [2] `code-review-reality-server-imports-raw-err-leak` — vector=security, confidence=high, 6 leaking `map_err` sites in `imports.rs` + `agencies.rs` — see backlog.md (promoted this run via security fast-track)
- **New:** [1] `code-review-reality-server-osrng-expect` — vector=bug, confidence=medium, `SysRng.expect()` in SSO login + password-reset paths (`sso.rs:708`, `handlers/users/mod.rs:558`)
- **Decayed:** `code-review-mobile-native-kmp-portfolio-analytics-caps-100` — −1 → 0 → status=dropped (updated_at 2026-08-19, >14d, no new signals)
- **Bumped:** none this run
- **Dropped:** `code-review-mobile-native-kmp-portfolio-analytics-caps-100` (decayed to 0)

## Plans promoted
- `plans/code-review-reality-server-imports-raw-err-leak.md` — route the six leaking `map_err` sites in `imports.rs` + `agencies.rs` through `db_error` to stop the raw sqlx-text leak · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The one high-confidence promotion (`code-review-reality-server-imports-raw-err-leak`) is a multi-file code fix, not on the allowlist shape (which is doc-stub / orphan-remove / lockfile-bump / fixme-issue / stalled-review); it's routed via the manual-implementer plan path instead.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present)

## Open questions
- pm-security asked: which merged PR(s) actually closed `#2485` (layout webhook replay) and `#2483` (dispute `add_evidence` IDOR)? Both risks were closed this run on code-level verification (`webhook.rs` + `layout-revalidate/route.ts` carry the timestamp+HMAC pair; `disputes.rs:860-884` is org-scoped) without citing a PR — surface for the operator to backfill the `source` field in `risks.json` when they know.
- pm-security also asked whether `frontend/apps/mobile` (React Native — `LAYOUT_CACHE_KEY` / SSO CSRF risks in `risks.json`) is cloud-landable or falls under the mobile-native exclusion. Currently the trigger set excludes both (per the routine-prompt's mobile-drift carve-out); worth clarifying next PM tick.

## Quiet day?
- No — 5 PRs merged, 2 new backlog vectors, 1 plan promoted, 2 code-review findings, 2 risks closed. Telegram digest will send.
