# 2026-07-02

## Since last run
- Merged PRs: **32+ visible** (window enumerated in the ~06-29 → 07-02 slice; the earlier 06-16 → 06-28 window is truncated — see *Notes / caveats* below). Highest seen: **#2008**. Enumerable merges span #1945 → #2008 with heavy runtime-hardening, test-un-quarantine, and screen-map-doc waves.
- Open PRs touched: **~30** new activity (per open-PR pull). Notable stale drafts: **#1797** (8d idle, no reviewDecision — see *Watch*), **#1812** (6d idle, `needs-human-review`, reality_portal.rs split), **#2007** (draft, dispatcher marked approved — needs ready-for-review flip).
- New / updated issues: **7 new opens** (#1995 tenant-migration data movement; #1997/#1998/#1999/#2000/#2001 follow-up cluster; **#2009** cargo-deny RUSTSEC advisory blocker); ~20 closed by matching PR fixes.
- Commits on `dev`: 100 visible in the returned page (2026-06-28 → 07-02); the 06-16 → 06-28 slice is truncated. Top authors: martin-janci (99), dependabot[bot] (1). No `revert:` / `hotfix:` in commit messages.
- Routine lag: **~16d** since last run (2026-06-16T03:25Z → 2026-07-02T06:15Z). `lag_warning` + `stale_routine_alert` emitted; cloud cron likely missed cycles.
- Phases that failed: **none** — Phase 1 succeeded with data-window truncation caveat; Phase 1.5 (rotating expert review) and Phase 1.6 (PM delivery snapshot) were **skipped this run** for catch-up efficiency (they resume next cycle). Per G16, Phase 1.6 skip is a no-op gate.

## Shipped
- **#1946** `fix(acc): harden TOTP + capability-token security (UC-ACC-16.1/05.11)` — swaps skeleton UUID crypto for RFC-6238 TOTP + SHA-256-hashed share tokens / recovery codes; migration 00204 (`token`→`token_hash`) with capability-read RLS policy.
- **#1966 / #1967** — env-check length floors; api-core JWT logging & access-gate restore.
- **#1973** `fix(rentals): manager-gate guest/booking PII read paths` — closes the tail of the #1766 rental-guest PII authz hole (four routes gated with `TenantRole::is_manager`).
- **#1974 / #1970 / #1969** — faults triage notifications, IoT sensor WS authz regressions, migration-collision guard hardening.
- **#1976** `fix(regional-compliance): live quorum lookup + manager-gate writes + export scoping (#1906)` — includes migration 00210 (Czech §1209/§1210 statute fix) and closes a client-supplied `organization_id` PII bypass.
- **#1977 / #1993 / #1968** — messaging N-party sweep: per-participant read watermark (migration 00208), unread soft-delete leak, set-based block check. Reconciles #1771 / #1776 / #1789.
- **#1984 (+ #1985 mini-fix)** `test(backend): quarantine 9 dev-hostage suites (BIT-440)` — silenced 9 suites to unblock the test gate; #1985 patched a duplicate `#[ignore]` typo left behind. Neither PR added a regression test for the gate itself → seeds `test-gap-bit-440-fragile-test-gate` (score 2).
- **#1986 / #1992 / #1991** — perf/index sweeps: payment-reminder partial index, set-based favorite-alert worker + idempotent mark-read, finance 5-suite un-quarantine (BIT-479).
- **#1987** `fix(reality-web): close SSO callback CSRF state-bypass` — plugs the "missing `state` short-circuits nonce check" pattern.
- **#1988 / #1989** — accounting-web a11y + login TODO closure; accounting `confirm_match` symmetric 404 + ToSchema drop on secret model.
- **#1994** `fix(migration): SQL-side template pagination + honest import/export checklist (#1905)` — corrects the pagination path and downgrades over-claimed checklist rows from `done` → `partial`.
- **#1996 / #2002 / #2003 / #2004 / #2005 / #2006 / #2008** — screen-map + verify-to-done stream: onboarding-tour helpers (Story 10B.6), ppt-web error-handler unit coverage, mobile share-sheet UUID validation, report-execution-history status pin, KMP Home/Search build-status pins, KMP inquiry-reply/scheduling doc fix, Booking.com dual-auth model doc.

## Watch
- **🚨 CRITICAL — Issue #2009 (open, [security] [dependencies])**: cargo-deny RUSTSEC advisory on `ammonia v4.1.2` **blocks every backend PR merge** that runs `cargo-deny check advisories`. Reproduced on PR #2007 (a 5-line doc-only change). Fix is one line (`cd backend && cargo update -p ammonia`) — promoted to `plans/security-cargo-deny-ammonia-advisory-bump.md` this run.
- **Reverts:** none in the visible window; #1985 is a *mini-hotfix* for #1984's duplicate `#[ignore]` typo, no `revert:` / `hotfix:` commit prefix seen.
- **Churn hotspots this run (top 3):** `backend/servers/api-server/src/routes/messaging.rs` (3 PRs — #1977, #1993, #1983), `backend/crates/db/src/repositories/messaging.rs` (2 PRs, ~116 lines + migration 00208), `backend/servers/api-server/src/routes/regional_compliance.rs` (1 PR, 77 lines + 5 manager-gates + org-id PII fix).
- **Stalled review:** **#1797** (draft, 8d idle, no reviewDecision — auth on OCR + manager-gate rental PII, blocks closure of #1772 / #1766). Also #1812 (draft, 6d, `needs-human-review`).
- **Closed-not-merged this run:** #1958, #1982, #1802, #1956, #1795, #1799, #1804, #1814, #1823 (batch supersessions).

## PRs stuck in draft despite approval
- **#2007** — `83-3-portal-webhooks: align portal-webhook receivers with Story 83.3 (UC-32.9)`, draft, created 2026-07-02, dispatcher recorded as `approve` in commit `5fa3f24` but the PR itself is still `draft=true`. Needs a manual ready-for-review flip before Phase 5 or the auto-merge chain can act on it.

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **none** (Phase 1.5 skipped this run).
- Reason: 16-day routine lag → prioritise incremental catch-up (backlog decay + signal ingestion) over the expert-review slice this cycle.
- Findings: 0.
- Next segment: **ppt-web-core** (last reviewed 2026-06-05 — oldest after this run, 27d stale as of today; will run next incremental cycle).
- Skipped: **yes — catch-up run, deferring segment review to next cycle**.

## Screen-map status
- Total `docs/screens/` files: **~127** (unchanged since 2026-06-16; no new inits this run — the merged `82-3/82-5/81-2` PRs edited existing files only).
- Drift signals this run: **3** (`screen-map-drift-pr-1987-reality` — SSO callback route touched with no `docs/screens/reality/auth-callback.md`; `screen-map-drift-pr-1977-ppt` + `screen-map-drift-pr-1993-ppt` — messaging unread semantics changed on route with no `docs/screens/ppt/messaging-*` update). Heuristic caveat applies (routes ≠ 1:1 screen slug).
- Orphan screens this run: **0** (spot-check only; no exhaustive scan).
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New:** 19 items — 13 with concrete `score_delta > 0` created rows:
  - [3] `security-cargo-deny-ammonia-advisory-2009` — RUSTSEC advisory on ammonia; blocks all backend PR merges (issue #2009). *Promoted to plan.*
  - [2] `screen-map-drift-pr-1987-reality` — SSO callback CSRF fix, no `auth-callback.md`.
  - [2] `screen-map-drift-pr-1977-ppt` — messaging unread watermark, no messaging screen-doc update.
  - [2] `screen-map-drift-pr-1993-ppt` — messaging soft-delete + block set fix, no messaging screen-doc update.
  - [2] `test-gap-bit-440-fragile-test-gate` — quarantined 9 suites without a gate-hardening test (#1984 + #1985).
  - [1] `stalled-review-pr-1797` — 8-day-idle draft, no reviewDecision.
  - [1] `dx-followup-issue-growth-2026-07-02` — 5 new `from-merged-review` issues in one day (#1997-#2001).
  - [1] each × 3 `churn-hotspot-*-messaging-rs`, `-messaging-rs` (repo), `-regional-compliance-rs`.
  - [1] each × 9 `closed-not-merged-pr-*` — #1958, #1982, #1802, #1956, #1795, #1799, #1804, #1814, #1823.
- **Bumped:** 0 (no re-scored existing items — all novel signal IDs).
- **Decayed:** 14 — pre-window items with `updated_at < 2026-06-18` lost 1 point (code-review mobile-native-kmp deeplink + search race, screen-map drift #1085/#1100 + #1418, risky-churn #963, hotfix-no-test #959 / #1287-#1289 / #1292, ios deeplink info.plist, mobile-rn mock data + deeplink init).
- **Dropped:** 38 — decayed from 1 → 0. Big cleanup: legacy churn hotspots (backend routes + mobile-native + frontend mobile app.config), triage rows (#1151, #1331, #1380, #951, dispatcher-mcp-push #1014), old stalled-review (#988, #1179), old closed-not-merged (#1425, #1274), old repeated-churn (forms.rs), and stale code-review rows (osrng-expect, mobile-native deeplink-android-bypasses, mobile-native listing-detail/search-screen).
- **Resolved this run:** 0 (no open item's `id` appeared verbatim in a merged-PR body/title in the visible window; evidence-gone grep skipped this run for capacity — deferred to next cycle).

## Plans promoted
- `plans/security-cargo-deny-ammonia-advisory-bump.md` — `cargo update -p ammonia` to `>=4.1.3` to clear the RUSTSEC advisory blocking every backend PR (#2009). Vector=security, Score=3, Confidence=high. Mode: cloud-ok (no browser/ADB). · adversarial pass: passed (Source → Hypothesis → Suggested approach → Test plan chain verifiable; `cargo deny check advisories` deterministically fails today per #2009 evidence and passes after the bump; no placeholder rot; both cited files exist on disk).

## Auto-fix
- `no candidate` — the promoted `security-cargo-deny-ammonia-advisory-2009` matches "high confidence + score ≥ 3" but its signal type (`unchecked-todo` proxy) is NOT in the Phase 5 allowlist (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift shape, `stalled-review`). The `stalled-review-pr-1797` matches the comment-only shape and confidence bar, but the item's score is only 1 (below the ≥3 threshold). No `screen-map-orphan`, `fixme-in-merged-code`, or lockfile-drift signals fired this run. The dep-update to bump ammonia is a *code* change (Cargo.lock delta), not a manifest caret bump against a merged dependabot PR — outside the auto-fix safe-shape allowlist. Correct outcome: manual implementer picks up the plan.

## Self-review findings
- open=8 (high=2, medium=4, low=2); new this run=0; acknowledged=19.
- (high, open) — `Phase 6 archive-move produced a 198KB assignments-archive.json; mcp__github__push...` [recurrence=5] — fix: PROVEN WORKAROUND — push the dispatcher state commit …
- (high, open) — `Phase 2.6 buffer reported claimable=54/open=54 but REAL claimable=0; all 54 open…` [recurrence=3] — fix: Two-part, both still unlanded as of ce9a2cb6a: (1) CLOSE ste…

## Open questions
- **cargo-deny ammonia bump** — is `>=4.1.3` semver-compatible with our `ammonia::Builder` usage? If the transitive bump introduces a breaking API change, the plan's fallback is one of the advisory's alternative ranges (`>=4.0.2, <4.1.0` or `>=3.3.2, <4.0.0`) with an explicit `[dependencies]` pin. Implementer confirms during the change.
- **Data-window truncation** — the 06-16 → 06-28 slice was not enumerable from the returned dumps (both PRs and commits pages truncated). Should the next run do a `text: "deep"` sweep to fill the gap, or is the visible 06-29 → 07-02 evidence enough forward motion? Recommend: accept the gap, note in state, do not force a deep sweep — the routine's job is to advance, not to be complete.
- **Phase 1.5 + 1.6 skip** — segment review + PM rotation deferred to next cycle. Not a policy violation (G16 no-ops), but if the routine keeps missing 16-day windows the PM cursor will drift further; consider a manual `pm-full` invocation.

## Quiet day?
- **No.** 32+ application-code PRs merged (visible window), 1 critical repo-wide CI blocker filed (#2009) + plan promoted, 19 new backlog vectors + 38 decayed drops (huge cleanup), 3 fresh churn hotspots on messaging + regional_compliance, 5 new post-merge review follow-up issues. `stats.runs` 40 → 41, `vectors_created` 173 → 192, `plans_created` 12 → 13, `quiet_days` 1 → 1.

## Notes / caveats
- **Data-window truncation** — the `list_pull_requests state=closed` dump was capped at 50 most-recently-updated rows, revealing only PRs updated 06-29 → 07-02. Merges in 06-16 → 06-28 with no later touch (roughly #1440 → #1945) are not enumerated. Commits page was also 100-row-truncated. This run advances cursors to the highest seen (`last_pr_seen=2008`, `last_issue_seen=2009`) — items in the untraversed window remain unsurveyed. Recommend next incremental run tolerates this; a targeted `deep` catch-up is optional.
- **Phase 1.5 + Phase 1.6 skipped** — catch-up run: prioritise closing the 16d lag over expert-review + PM slice this cycle. Both resume next incremental run under normal rotation.
