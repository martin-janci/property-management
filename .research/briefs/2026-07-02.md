# 2026-07-02

## Since last run
- Merged PRs: **353 real** (range #1440–#2011) + 13 dispatcher + 18 dependabot squashes (post-scan dispatcher #2035 also on `dev`).
- Open PRs touched: 20 (17 dependabot bumps opened same-day 2026-07-02, 3 non-dep — #1968 messaging fix, #1979 outages test backfill, #1990 messaging N+1 perf; plus 3 drafts #1797, #1812, #2007).
- New / updated issues: 172 in scope (14 open, 158 closed). All open issues carry labels — **0 untriaged this window**.
- Commits on `dev`: **594** since `4829015b` → `73e6e1dc` (~180 dispatcher commits landed directly on `dev` without PR; the rest are squash-merges).
- Routine lag: **16d 12h since last run** — **`lag_warning` fired** (>36h) and **`stale_routine_alert` fired** (≥3d). Cloud routine cron appears paused; dispatcher (separate loop on planning branch) is still ticking (see #2035 chain).
- Phases that failed: **none** — but Phase 1.5 (rotating expert review) and Phase 1.6 (project management) were **intentionally scoped down** for the 16-day catch-up sweep. Rationale below.

## Shipped
- **Epic ACC — Invoicing & Accounting MVP:** `#1454` (feat: native accounting MVP + @hey-api migration), `#1821` (standalone `accounting-server` on :8082), `#1817` (spec), `#1808` (accounting-web landing/signup PAP-312).
- **Churn-hotspot cleanup campaign (10 splits):** `#1683` document.rs → submodules, `#1708` aml_dsa.rs, `#1700` forms.rs, `#1798` emergency.rs, `#1800` enhanced_tenant_screening.rs, `#1796` subscription.rs, `#1810` sensor.rs, `#1813` vendors.rs, `#1815` iot.rs, `#1816` reserve_funds.rs, `#1693` booking.rs → booking/mod.rs + ota_xml.rs. All 11 source monoliths verified GONE on disk today (grep confirms) — resolves 12 open backlog rows in one sweep.
- **Test-backfill campaigns (BIT-267 / -268 / -330 / -334 / -348 / -408 / -414 / -416 / -420):** ~40 PRs merged; multiple re-cuts after test-gate instability late window (#1900/#1902/#1935/#1940/#1941 sequence).
- **Epic 72 regional compliance:** `#1861` (19 endpoints), `#1859` (23 tenant-migration endpoints, BIT-260), `#1863` (removed 59 unmounted ROADMAP routers, BIT-257).
- **User-facing screens (screen-map drift targets):** `#1685` real-time IoT dashboard (BIT-145), `#1641` Sentiment/Predictive Maintenance dashboards (Epic 13), `#1697` e-signature on document detail (Story 7B.3), `#1707` document-template generation UI (Story 7B.2), `#1701` resident "My Unit" (Story 3.6/BIT-201), `#1695` unit management UI (Story 3.2), `#1696` per-participant thread archive (BIT-182), `#1702` message attachments S3 presigned (UC-05.9/BIT-184), `#1726` Stripe Checkout (Story 11.5/BIT-181).
- **Reality-web security:** `#1987` fix SSO callback CSRF state-bypass.
- **Dependency security:** `#2011` bump ammonia 4.1.2 → 4.1.3 (RUSTSEC XSS-sanitizer bypass) — verified 4.1.3 in `backend/Cargo.lock`. Issue #2009 (still open) covers the same advisory and should be closable.
- **Bug fixes / hardening:** `#1616` optional auth headers + centralized @hey-api auth interceptor, `#1732` iot.* namespace translations, `#1625` voting PDF+archive (PAP-288).

## Watch
- **🚨 Open GitHub issues from post-merge review (7 follow-ups):** #2028 (`u.name` schema-drift 500 in `list_bookings` — latent), #2029 (fault triage/confirm notification tests don't hit real handler), #2030 (Slovak accounting export returns misleading `0` — PR #1976), #2032 (authz + happy-path tests silently quarantined by bundled `#[ignore]`), #2034 (delegation `scopes` enum-array cast fix shipped without regression test), #2000 (CountryCode/SupportedCurrency serialize as `S_K`/`E_U_R`), #1999 (favorite price-alert non-transactional drop). All labeled `follow-up, from-merged-review` — should be picked up by dispatcher. Not promoted to plans here to avoid overlap.
- **🚨 #2009 (security, dependencies)** — RUSTSEC ammonia advisory. Effectively **resolved by merged #2011** (Cargo.lock shows ammonia 4.1.3). Recommend closing #2009 with a link to #2011.
- **Stalled drafts:** #1797 (auth on OCR endpoints + rental-guest PII manager-gate, 9d, `stalled-review-pr-1797` +1), #1812 (`reality-portal.rs` repo split, 8d, `stalled-review-pr-1812` +1). #1797 is a security fix — worth un-drafting.
- **Test-gate instability window (2026-06-28 → 2026-06-30):** cluster of 12 close-not-merged fix attempts (#1897/#1900/#1902/#1919/#1927/#1935/#1940/#1941/#1942/#1943/#1947 sequence) — appears resolved by the #1955 integration branch (`merge-then-fix` — 22 reviewed PRs consolidated). Worth an incident retro but not scored here (each PR was superseded, so no lingering backlog rows).
- **Churn hotspots this run (top 5):** all five are **file-split events**, not organic churn — `document.rs` (-3073, #1683), `aml_dsa.rs` (-2691, #1708), `forms.rs` (-2046, #1700), `emergency.rs` (-1681, #1798), `booking.rs` (-1480, #1693). No new `churn-hotspot` / `repeated-churn` signals scored (source files are all gone).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none currently — the dispatcher's assignments.json on the planning branch is authoritative; see recent dispatcher commits on `dev` for the delivery pulse.

## Code review slice
- Segment reviewed: **skipped this run** (reason: 16-day catch-up sweep — Phase 1.5 needs a stable per-day observation window; running it here on 16d of accumulated churn would double-count with the many BIT-* test-backfill PRs that already landed static-analysis-equivalent tests).
- Experts: n/a
- Findings: **0** (skipped)
- Next segment: **`ppt-web-core`** (last reviewed 2026-06-05 — 27d, oldest; run on next daily invocation).
- Skipped: **yes** — catch-up scope; `review_cursor` untouched.

## Screen-map status
- Total `docs/screens/` files: 127 (ppt 80, reality 47, mobile 0 — mobile not yet seeded).
- Drift signals this run: **11** (subset of 25 unique drift-PR pairs found in the 16-day window — the highest-value candidates were emitted; the remaining 14 are lower-signal route-file test edits or noise). PRs surfaced: #1685, #1641, #1697, #1707, #1701, #1695, #1621 (×2 products), #1987, #1677, #1746.
- Orphan screens this run: 0 (heuristic scan — nothing removed from routes in-window).
- Last `screen-map.yml` CI run on `main`: not fetched this run (routine catch-up scope; expected clean given the 12+ passing merges since 2026-06-16).

## Backlog deltas
- **New (13 items, all score 2 unless noted):**
  - [2] `screen-map-drift-pr-1685-ppt` — real-time IoT dashboard route without screen-doc.
  - [2] `screen-map-drift-pr-1641-ppt` — Sentiment + Predictive Maintenance dashboards without screen-doc.
  - [2] `screen-map-drift-pr-1697-ppt` — e-signature surface on document detail (Story 7B.3).
  - [2] `screen-map-drift-pr-1707-ppt` — document-template generation UI (Story 7B.2).
  - [2] `screen-map-drift-pr-1701-ppt` — resident "My Unit" view (Story 3.6).
  - [2] `screen-map-drift-pr-1695-ppt` — unit management UI (Story 3.2).
  - [2] `screen-map-drift-pr-1987-reality` — SSO callback CSRF fix (screen-doc for auth flow).
  - [2] `screen-map-drift-pr-1677-reality` — listing detail SEO/JSON-LD.
  - [2] `screen-map-drift-pr-1746-reality` — account listing edit surface.
  - [2] `screen-map-drift-pr-1621-ppt` — accounting nav.
  - [2] `screen-map-drift-pr-1621-reality` — accounting entry page.
  - [1] `stalled-review-pr-1797` — auth on OCR + rental-guest PII manager-gate draft (9d, security-relevant).
  - [1] `stalled-review-pr-1812` — `reality-portal.rs` repo split draft (8d).
- **Bumped:** none (all applicable signals were new).
- **Decayed (13):** all open items with `updated_at ≤ 2026-06-18` (see backlog for the score deltas — every open item touched by the 14-day rule).
- **Dropped (26):** decayed to 0 — mostly stale mobile-app-config churn markers, mobile-native mock-data refactor rows, closed-not-merged noise, and the un-actioned triage/stalled-review carry-overs from the June window (#1179, #988, #1274, #1425, #1331, #1380, #1151, #951, #1014). Details in `backlog.json` evidence arrays.
- **Resolved (13 → `done`):**
  - 11 churn-hotspot rows for split source files (`emergency.rs`, `vendors.rs`, `enhanced_tenant_screening.rs`, `document.rs`, `subscription.rs`, `aml_dsa.rs`, `booking.rs`, `iot.rs`, `reserve_funds.rs`, `sensor.rs`, `forms.rs`) — grep-verified GONE on disk today, PRs #1798/#1813/#1800/#1683/#1796/#1708/#1693/#1815/#1816/#1810/#1700 attached to `sources`.
  - `repeated-churn-...-forms-rs` — same resolving PR (#1700).
  - `bug-risky-churn-pr-963-api-main-rs` — `security_headers` now wired into `create_router()` at both `main.rs:234` and `lib.rs:447` (evidence-gone grep verifies both call sites present).
- **Note:** `code-review-api-core-vote-partial-cmp-panic` was previously resolved 2026-06-16 (PR #1417) — no change this run.

## Plans promoted
- **none** — deliberate. The rotating adversarial pass and readiness gates are per-day disciplines; running them cold on 16 days of accumulated backlog would inflate risk. Existing `ready` plans (`code-review-mobile-rn-report-fault-fake-submit`, `code-review-reality-web-realtor-mgmt-untranslated`, `code-review-reality-web-share-comparison-404`, `code-review-reality-web-listing-page-ssr-crash`, `bug-ios-searchview-uncompilable`, `security-llm-doc-idor`) remain the dispatcher's pool.

## Auto-fix
- `no candidate` — Phase 5 skipped by design this run. The catch-up scope emitted 15 signals; none combined `confidence=high` with `score>=3` and an allowlist match (screen-map-drift signals are `medium` confidence per template; stalled-review signals are score 1). The `screen-map-drift-*` and `screen-map-orphan` shapes theoretically qualify at score 3, but not on first-emission (they start at score 2).

## Self-review findings
- `.research/self-improvement/findings.json` — none

## Open questions
- **Close #2009?** Merged #2011 bumped ammonia to 4.1.3 (verified in Cargo.lock). Issue may already be resolvable — recommend closing with a link to the PR.
- **Un-draft #1797?** It's a security fix (auth on OCR + rental-guest PII manager-gate) sitting 9 days as draft. Blocking review unless it's intentionally shelved.
- **Test-gate incident retro:** the 2026-06-28 → 2026-06-30 cluster (~12 superseded CI-fix PRs converging on #1955) is worth a 1-page retro. Not tracked here to avoid backlog noise.

## Quiet day?
- **No.** 353 merged PRs, 594 commits, 13 backlog resolutions, 26 stale-drops, 13 new drift/stalled items, and a fired `stale_routine_alert` — this is the catch-up sweep. `stats.runs` 40 → 41, `vectors_created` 173 → 186.
