# 2026-07-02

## Since last run
- Merged PRs: 41 landed on `dev` (range #1946–#2008 + #2011)
- Open PRs touched: 23 (all fresh; 2 drafts >5d — #1797 auth-on-OCR, #1812 reality_portal repo split)
- New / updated issues: 11 open (#1680, #1995, #1997–#2001, #2009, #2028–#2030)
- Commits: 51 on `dev` (mix of squash merges + dispatcher bookkeeping)
- Routine lag: **16d 10h since last run** (last_run_iso = 2026-06-16T03:25Z) — cloud cron was paused; this run is a compressed catch-up
- Phases that failed: none

## Shipped (top 12 of 41)
- #1946 — auth: capability-token replay + TOTP verification hardening (UC-ACC-16.1 / 05.11)
- #1973 — rentals: manager-gate guest/booking PII read paths (#1766)
- #1974 — faults: emit notifications on triage + confirm transitions (#1793) — follow-up #2029 (tests don't drive real handler)
- #1976 — regional-compliance: live quorum lookup + manager-gate writes + export scoping (#1906) — follow-up #2030 (Slovak export shows 0)
- #1977 — messaging: per-participant read watermark for N-party threads (#1773)
- #1987 — reality-web: close SSO callback CSRF state-bypass
- #1989 — accounting: symmetric 404 in confirm_match + drop `ToSchema` off secret model (#1827)
- #1993 — messaging: unread soft-delete leak + set-based block check (#1771/#1776/#1789) — follow-ups #1998 (#1771 test never runs in CI)
- #1994 — migration: SQL-side template pagination + honest import/export checklist (#1905) — follow-up #1997 (index + tiebreak + test)
- #1991 — test(api-server): fix + un-quarantine 5 finance happy-path suites (BIT-479) — surfaced #2000 (CountryCode `S_K` bug)
- #1992 — perf(reality): set-based favorite-alert worker + idempotent mark-read — follow-up #1999 (enqueue/advance race dropping alerts)
- #2011 — dep-bump: ammonia 4.1.2 → 4.1.3 (RUSTSEC XSS-sanitizer bypass) — closes #2009

## Watch
- Stalled review: none >7 days on non-draft PRs
- Reverted: #1713 revert(delegations) — remove re-added delegation frontend (BIT-213) — reconciled
- Churn hotspots: `backend/servers/api-server/src/routes/messaging.rs` (3 touches), `backend/crates/db/src/repositories/messaging.rs` (3), `backend/servers/api-server/tests/multi_currency_happy_path_tests.rs` (3) — N-party messaging is the hottest surface; two follow-ups still open

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **ppt-web-core** (reason: oldest-unreviewed since 2026-06-05; churn was in backend/messaging so no churn-aligned override)
- Experts: frontend
- Findings: 3 (all new backlog rows — see Backlog deltas)
- Next segment: **ppt-web-ui** or **mobile-native-kmp** (both stale; next run will pick the oldest)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: **162** (`ppt/` 100 + `reality/` 47 + `reality-mobile/` 15; `mobile/` still empty)
- Drift signals this run: **0** (both `ppt/` and `reality/` doc-dirs received updates alongside route churn; heuristic PR-level drift detection deferred in this compressed catch-up run)
- Orphan screens this run: 0 (no exhaustive scan)
- Last `screen-map.yml` CI run on `main`: last success 2026-06-04 (workflow only fires on PRs touching `docs/screens/**`)

## Backlog deltas
- **New:** [3] `code-review-ppt-web-core-iot-ws-relative-url` — IoT WS live stream broken (buildWsUrl reads undef `window.__API_BASE_URL__`, WebSocket ctor throws SyntaxError, throw is unhandled in useEffect)
- **New:** [2] `code-review-ppt-web-core-perf-metrics-leak` — `usePerformanceMetrics` leaks `load` + `visibilitychange` listeners on unmount (anonymous fn can never be removed)
- **New:** [1] `code-review-ppt-web-core-sessions-unchecked-cast` — `SessionsPage` double-casts API response to local shape; no runtime validation across auth API boundary
- **New:** [1] `churn-hotspot-backend-servers-api-server-src-routes-messaging.rs` — 3 touches this window (N-party watermark + soft-delete leak)
- **New:** [1] `churn-hotspot-backend-crates-db-src-repositories-messaging.rs` — 3 touches this window (same feature area)
- **New:** [1] `churn-hotspot-backend-servers-api-server-tests-multi-currency-happy-path-tests.rs` — 3 rewrites (BIT-479 un-quarantine surfaced CountryCode `S_K` bug #2000)
- **Decayed:** 52 open items ≤ 2026-06-18 lost 1 point; 31 items reached 0 → `dropped` with evidence "decayed: no new signals in 14 days"

## Plans promoted
- **none** — 6 plans already sit `ready` idle (`security-llm-doc-idor`, `bug-ios-searchview-uncompilable`, 4 code-review-* items), and this compressed catch-up's new signals don't meet the readiness bar (medium-conf code-review-findings + churn-hotspots) with concrete `source: PR #|issue #|commit sha` per Phase 3. The IoT WS finding is the closest candidate for next-run promotion once verified against a real reproducer.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. Closest miss was `code-review-ppt-web-core-iot-ws-relative-url` (score 3, confidence medium — needs high for auto-fix). Kill switch: default (not set).

## Self-review findings
- none (no `.research/self-improvement/findings.json`)

## Open questions
- Should the cloud cron scheduler pause be intentional? 16-day lag broke the routine's daily cadence — surface to operator.
- Should the routine explicitly emit a `bug` signal for known-referenced follow-up issues (#2000, #1999, #1998, #2028–#2030)? Current signal table only has `untriaged-issue` (labels prevent that), so labeled bug/security issues stay only in GitHub, not the backlog.

## Quiet day?
- **No** (41 PRs, 6 new backlog rows, 52 items decayed, catch-up nature). `stats.quiet_days` unchanged.

## Goal violations
- (none surfaced pre-commit; Phase 6 will populate `goal_checks[]` if any fail)

## Catch-up-run caveats
- **Resolution check skipped** — routine's per-item PR-body + evidence-gone grep would need per-item file scan against 51 commits; too expensive for a compressed catch-up. Deferred to next run.
- **Screen-map drift detection compressed** — no per-PR file-list diff was cross-checked against `docs/screens/{ppt,reality}/`; screen-doc dirs got updates alongside route churn so aggregate drift is low, but individual PRs may have skipped their doc.
- **Untriaged-issue signal skipped** — all 11 open issues carry labels (`follow-up`, `from-merged-review`, `security`, `dependencies`, `enhancement`, etc.); none qualify as "no label".
- **Hotfix-no-test heuristic passed** — all recent `fix(*)` PRs mention tests in the body (none warrant `hotfix-no-test` signal).
- Next routine run will resume normal incremental cadence off cursors `last_pr_seen=2008 / last_issue_seen=2030 / last_commit_sha=078b865a`.
