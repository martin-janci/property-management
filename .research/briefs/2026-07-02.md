# 2026-07-02

## Since last run
- Merged PRs: **28 application** (span #1966–#2006 in the sampled window) + 12 older application-code + 9 dispatcher chore commits — total 40 merged-to-`dev` in the 16-day window
- Open PRs touched: 7 (2 new drafts #2007, #2008; 5 carry-overs)
- New / updated issues: 30 (7 new since #1437, 23 updates on existing; all new issues have labels — no `untriaged-issue` signals this run)
- Commits: ~299 on `dev` (169 application + 130 dispatcher chore); newest sha `b5311d1`
- Routine lag: **16d 1h** since last run — routine was cold; cloud cron likely paused (dispatcher on `planning` branch kept running)
- Phases that failed: **none code-side**; **Phase 1.5 (rotating expert review) and Phase 1.6 (PM analysis) skipped this run** — see *Skipped phases* below

## Skipped phases (16-day-catch-up bounded scope)
- **Phase 1.5 (rotating expert review)** skipped: the routine cannot faithfully run a 16-day catch-up + a full expert-team pass in one go without blowing token budget. `state.review_cursor` **not advanced**; oldest segment `ppt-web-core` (last reviewed 2026-06-05) will be picked up next run.
- **Phase 1.6 (project-management)** skipped: the dispatcher on `planning` is the authoritative delivery loop right now (assignments.json / action-list.json updated today by the dispatcher). Running the routine's PM rotation on top would double-book. `state.pm_cursor.next_index` and `state.coverage_cursor.next_index` **not advanced**; next role in rotation is `pm-security` (index 5, last run 2026-05-27).

## Shipped
- #1966 fix(api-server): enforce length floors in preflight env check
- #1967 fix(api-core): restore token_type/raw_kind logs + pin JwtService access gate
- #1969 test(db-migration): harden migration-version collision guard
- #1970 test(iot): sensor WS authz regression tests (non-member 403 before upgrade)
- #1973 fix(rentals): manager-gate guest/booking PII read paths
- #1974 fix(faults): emit notifications on triage + confirm transitions
- #1976 fix(regional-compliance): live quorum lookup + manager-gate writes + export scoping (closes #1906)
- #1977 fix(messaging): per-participant read watermark for N-party threads
- #1983/#1991 test(api-server): finance happy-path backfill (BIT-417/BIT-479) — 234/234 endpoints covered
- #1985 fix(api-server): remove duplicate `#[ignore]` from BIT-440 quarantine — unblocked dev test gate
- #1986 perf(db): partial index + cross-org docs for payment-reminder sweeps
- #1987 fix(reality-web): close SSO callback CSRF state-bypass
- #1989 fix(accounting): symmetric 404 in confirm_match + drop ToSchema on secret model
- #1992 perf(reality): set-based favorite-alert worker + idempotent mark-read (closes #1852)
- #1993 fix(messaging): unread soft-delete leak + set-based block check (closes #1771, #1776, #1789)
- #1994 fix(migration): SQL-side template pagination + honest import/export checklist (closes #1905)
- #1996 test(UC-42.1): onboarding tour pure-helpers unit tests (Story 10B.6)
- #2002 test(ppt-web): errorHandler.ts unit backfill (Story 79.3)
- #2003 fix(mobile): DocumentShareSheet UUID-validate user-ID (closes #485 mobile parity)
- #2004 docs(ppt-web): report execution history — verify-to-done + pin apiStatus reason
- #2005 docs(reality-mobile): track shipped KMP build status on Home + Search screen-maps
- #2006 docs(reality-mobile): correct stale KMP inquiry-reply/scheduling API note

## Watch
- **Revert (governance):** #1713 reverted #1690 (delegation frontend re-added). Root cause = timing: #1690 merged ~16 min before CEO ruled delegation stays retired (BIT-198). Not a code defect — process signal. Tracked as `revert-pr-1713-delegations` (backlog).
- **Stalled draft PRs (long-tail carry-overs from prior brief):** #1812 (churn-hotspot draft, updated 2026-06-26, 6d), #1797 (auth on OCR + rentals guests, updated 2026-06-24, 7d). Neither exceeds the 7-day strict threshold this snapshot; will fire next run if untouched.
- **Churn hotspots this run:** top-3 are all backend/api-server test files churning as part of BIT-417/BIT-479 finance backfill campaign — noise, not instability. Recorded but low-severity (`churn-hotspot-*happy_path_tests.rs` × 3 in backlog).

## PRs stuck in draft despite approval
- none (no approved+drafts in the open snapshot; #2007, #2008 are fresh drafts without decision)

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher assignments checked; no `verdict=changes` rows aged >24h)

## Code review slice
- Segment reviewed: **skipped this run** (see *Skipped phases* above)
- Experts: — (none)
- Findings: 0
- Next segment: `ppt-web-core` (oldest unreviewed after this run — last reviewed 2026-06-05)
- Skipped: yes — 16-day catch-up bounded scope; review budget deferred to next run.

## Screen-map status
- Total `docs/screens/` files: 161 (per validation output in PR #2005 body) across `ppt/` + `reality/` + `reality-mobile/`; `mobile/` (RN) still 0 → drift detection remains gated
- Drift signals this run: 0 (per-PR file-list correlation not run — 16-day catch-up bounded scope; heavy screen-map updates were shipped alongside route changes)
- Orphan screens this run: 0 (spot-check skipped this run)
- Last `screen-map.yml` CI run on `main`: not checked this run

## Backlog deltas
- **Resolved (marked `done` + plan archived this run):**
  - `bug-ios-searchview-uncompilable` — PR #2005 confirms iOS `SearchView.swift` compiles (`func performSearch` + `func scheduleSearch` present; grep count = 2). PR #1357 fix already on dev.
  - `security-llm-doc-idor` — LLM handlers moved to `routes/ai/llm.rs`, now bound via `RlsConnection` + org-scoped repo methods (`publish_description_for_org`, `list_listing_descriptions_for_org`); comments cite issues #766, #816.
  - `code-review-reality-web-share-comparison-404` — `ComparisonUrlHandler.tsx` now fetches `${getApiBase()}/api/v1/listings/${id}`.
  - `code-review-reality-web-listing-page-ssr-crash` — new `jsonLd.ts::buildListingJsonLd(listing: unknown)` validates defensively; page.tsx short-circuits on null.
  - `code-review-mobile-rn-report-fault-fake-submit` — `handleSubmit` now calls `apiRequest<CreateFaultResult>('/api/v1/faults', {method: POST})` with idempotency key + offline queue; no `setTimeout` or "Simulate API".
  - `p0-10-mock-route-wiring` — no Mock component references remain in `App.tsx`.
- **New:** [3] `revert-pr-1713-delegations` — refactor, governance revert (not code-quality)
- **New:** [1] × 3 `churn-hotspot-*happy_path_tests.rs` — refactor, BIT-417/BIT-479 test-backfill churn
- **Decayed −1:** 14 open items (last-updated ≤ 2026-06-18) — see backlog.md `updated_at = 2026-07-02` rows
- **Dropped (decay to 0):** 38 items — bulk cleanup of stale low-score triage / churn / stalled-review signals from May/early-June that never gained follow-on evidence

## Plans promoted
- **0 new plans this run.** Candidates considered:
  - `revert-pr-1713-delegations` (score 3, high conf) — **not promoted:** the PR body itself declares "no code residue to clean"; the underlying issue is a governance/decision-clock process gap, not a code fix — belongs in the pre-merge protocol, not a `plans/` slug.
  - Top-3 churn-hotspot items — score 1 each, below the 3 threshold.
- **Active plans remaining:** 1 (`code-review-reality-web-realtor-mgmt-untranslated`, hardcoded EN in RealtorManagement.tsx — still open in code, confirmed via grep of "Invite Realtor" hardcode at line 86 + 1292).

## Auto-fix
- **no candidate** — no signal met confidence=high + score≥3 + allowlist this run. `stalled-review` signals not emitted (idle-days didn't cross strict >7); no `screen-map-drift`/`orphan`/`fixme-in-merged-code`/`dep-update-noise` derived (per-PR file-list correlation deferred to next run).

## Self-review findings
- open=8 (high=2, medium=4, low=2); new this run=0; acknowledged=19
- (no `high`+`open` finding-slug lookup surfaced this pass — `.research/self-improvement/findings.json` unchanged by this run; refer to that file for the 2 high findings and their proposed fixes.)

## Post-commit signal (dispatcher fire-payload)
- **`planner-buffer-low-2026-07-02`** — dispatcher fire-payload reported `claimable=0/72, coverage exhausted (all 49 stories terminal/active)`. Delivery pipeline is idle until the planner is refilled. This is a project-management signal for the human — the routine records the observation and does **not** invent new plans off it (that would be overreach — the `refill planner with new backlog` action needs product judgement about which epic/story to seed next). Recommend: run `/ppt-project-management scan` (or dispatch the `pm-scrum-master` role by hand) to author the next batch, then let the dispatcher pick it up.

## Open questions
- Cloud routine cron: why did the daily routine miss 16 runs while the dispatcher on `planning` kept ticking? Worth a `.claude/routines/` cron-config audit before the next run to prevent recurrence.
- Planner refill: which epic seeds the next dispatcher batch, given all 49 coverage stories are terminal/active?

## Quiet day?
- **No** — 28 merged PRs (application) + 6 resolved backlog items + 5 new backlog signals + 16d catch-up + delivery-pipeline stall.
