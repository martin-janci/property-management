# 2026-05-24

> Incremental run (~03:20Z), ~9.5h after the prior 2026-05-23T17:47Z run. Real activity this time: 4 PRs merged to `dev` (#436, #437, #440, #446) and PR #435 (the security P0/P1 batch) merged with deferred findings now filed as issues #438/#439. This run resolves the dead-dup-handler backlog item (PR #437), reviews the `ppt-web-ui` segment (3 findings), and — via the PM phase + issue #439 cross-check — confirms and promotes the still-open **P1-05 SSRF** to a plan under the security fast-track.

## Since last run
- Merged PRs: 4 (#436, #437, #440, #446) + #435 (security batch, merged 22:26Z)
- Open PRs touched: 6 (#441–#445, #447 — all opened today, 4 draft; none stalled)
- New / updated issues: 2 (#438, #439 — both labeled `follow-up`/`from-merged-review`, tied to PR #435)
- Commits: feature/version-bump churn on `dev`; no change to tracked route hotspots
- Routine lag: 9h since last run (on schedule)
- Phases that failed: none

## Shipped
- #437 — remove dead AuthHandler/BuildingHandler modules (2495 lines deleted). **Resolves** backlog `refactor-dead-dup-handler-modules` (→ done) and the duplicate-handler divergence risk.
- #436 — Epic 8A code review marked done (8a-1/8a-2/8a-3 → done in sprint-status.yaml).
- #440 — story 6.1 announcement creation review pass done (story already shipped in PR #5).
- #446 — gap-7a-4: PDF.js client-side document preview (react-pdf) in ppt-web DocumentDetail; updated its own `docs/screens/ppt/document-detail.md` (no drift).
- #435 — security P0/P1 auth/security fixes; merged with deferred findings → #438/#439.

## Watch
- **Issues #438/#439** (PR #435 follow-ups): P1-05 SSRF still open, P1-04 `format!("{:?}")` audit-hash domain, P0-12 cookie scope, P1-01 ordering, IG3 test gap. The SSRF is promoted to a plan this run; the rest are tracked as PM actions and need a single owner.
- 6 open PRs (#441–#447) all opened today — review-queue depth is the near-term delivery risk; no reverts, no stalled reviews.
- Churn hotspots (`runs_seen=2`, no new churn this run): `routes/integrations.rs`, `routes/organizations.rs`, `routes/documents.rs`. This run's apparent top-churn (handlers/auth+buildings/mod.rs, PdfPreview.tsx) is a one-time deletion + a new file — not instability, so no churn-hotspot signals.

## Code review slice
- Segment reviewed: ppt-web-ui (reason: oldest-unreviewed — no churn-aligned segment this run; api-handlers/api-core/reality-server were taken on 2026-05-23)
- Experts: frontend + completeness
- Findings: 3 (see Backlog deltas — `code-review-ppt-web-ui-*` signals)
- Next segment: ppt-web-core (oldest-unreviewed after this run; reality-web, mobile-rn, mobile-native-kmp also still null)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 99 (ppt/ 53 + reality/ 46; mobile/ not yet seeded → 0)
- Drift signals this run: 0 (#446 touched the documents feature + updated its own screen doc; no `App.tsx`/`routes/**` route file changed)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not re-checked this run (no route files changed)

## Delivery (Phase 1.6 — PM)
- Rotating role: Scrum Master + `pm-backend` (rotation index 1 → 2).
- Sprint Epic 6/7A/8A/10A: 2/5 epics done (8A closed 3/3, 10A backend done). Coverage upkeep marked gap-7a-4 web slice shipped (PR #446); rotating epic re-checked: epic-10a (unchanged).
- `pm-backend` independently traced the P1-05 SSRF at `integrations.rs:2743` + `signatures.rs:628`, and flagged the 10B silent-stub handlers + missing Epic 81 schedule endpoints.
- Artifacts refreshed: `.research/management/{project-state.md, action-list.json (23, 5 done / 6 in-progress), action-list.md, risks.json (9; dead-handler risk → resolved), decisions.md, roadmap.md, coverage.json, roles/pm-scrum-master.md, roles/pm-backend.md}`.

## Backlog deltas
- **New:** [3] SSRF: signed-document fetch + webhook-test POST issue outbound requests to unvalidated user-controlled URLs — `security`, confidence **high**. Promoted (security fast-track: security + high + score≥2).
- **New:** [2] Latent fail-open: ProtectedRoute role check skipped when `user.role` is falsy — `security`, confidence medium (latent — no requiredRoles call sites yet). Not promoted (medium confidence, score<3).
- **New:** [1] ppt-web status/auth components hardcode English in an i18n'd app — `refactor`.
- **New:** [1] Announcer: untracked clear-then-set timeouts can resurrect a stale screen-reader message — `bug`.
- **Bumped:** none
- **Decayed:** none (oldest open item is 4 days old)
- **Dropped:** none
- **Resolved:** `refactor-dead-dup-handler-modules` → done (code patterns gone — PR #437 deleted the handler modules).

## Plans promoted
- `plans/security-ssrf-outbound-url-validation.md` — extract `validate_external_url` into a shared module and apply it before the two unguarded outbound sinks (`signatures.rs:628`, `integrations.rs:2743`); failing-on-main unit test on the new validator · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal this run is on the Phase 5 allowlist (screen-map-drift/orphan, fixme-in-merged-code, dep lockfile-drift, stalled-review). The promotable finding is a security code fix (SSRF), which is explicitly not auto-fixable; routed to the manual plan path.

## Open questions
- The two SSRF sinks (signed_url fetch, webhook-test POST) are authenticated (org-scoped) — worth a human nod that the shared `validate_external_url` + private-range blocklist is the right shape, and whether a per-provider host allow-list for e-signature providers should land in the same PR.
- Issues #438/#439 bundle 5 findings; only the SSRF is promoted. The Debug-format audit-hash (P1-04) and cookie-scope (P0-12) are one-line-ish fixes — should they ship as a hotfix off `dev` or batch into the next release?

## Quiet day?
- No — 4 PRs merged, 1 backlog item resolved, 4 new backlog vectors (1 promoted to a plan under the security fast-track), and 3 code-review findings from the ppt-web-ui slice.
