# 2026-08-30

## Since last run
- Merged PRs: 3 (range #2876–#2878; all fix-vector implementer follow-throughs from the 2026-08-29 backlog refill)
- Open PRs touched: 3 (dependabot: #2865 cargo minor-patch, #2866 setup-java 5→6, #2867 npm minor-patch/39 updates — unchanged since last run)
- New / updated issues: 0 (last_issue_seen cursor unchanged at #2847)
- Commits: ~10 on `dev` (3 fix-PR merges + 5 dispatcher heartbeats + 2 dispatcher tier1d generator commits)
- Routine lag: **4h since last run** (on schedule — this fire is `buffer-low` trigger, not the daily cadence)
- Phases that failed: none

## Buffer-low context (routine fire trigger)
- Payload: `buffer-low: claimable=13/72 (floor=36, <floor/2) — refill planner`
- Confirmed: 15 open backlog items, 2 ready-with-plan items, **zero items meet the score-3 promotion threshold or the security fast-track**. The 3 top-scored open items are all score=2 medium-or-high-confidence bugs — none has stacked evidence yet.
- Buffer scenario since last routine tick: yesterday's routine folded 13 tier1d dev-review vectors and the dispatcher (5 heartbeats between 00:33 and 02:37 UTC) claimed 6 of them, spawned 6 implementers, and merged 3 (PRs #2876/2877/2878 — see *Shipped*). That drained the fresh non-mobile vectors down to the current 15 open / 2 ready / 0 promotable pool.
- Response this run: fold the 2 dispatcher-generated Tier-1d artifacts for today (reality-server bytelen-vs-charcount + report-contact-unvalidated) and the mobile-native-kmp create-listing-not-wired finding into `backlog.json` (3 net-new rows), and mark the 3 items whose PRs merged today as `status: done` with PR references. Buffer stays low structurally — this is not a "one refill fixes it" problem, it's the mobile-native/KMP cloud-unlandability plus the score-threshold + no-new-signals-in-24h combination.

## Shipped
- #2876 code-review-ppt-web-core-raw-fetch-bypasses-jwt-interceptor — route `useBuildingUnits` through axios client so JWT interceptor stamps the auth header (Bearer token); adds regression test asserting the token is present on the `/units` call (person-months views were silently 401'ing for real users)
- #2877 code-review-ppt-web-ui-create-dispute-page-stub-submit — delete orphaned `CreateDisputePage.tsx` (169 lines) whose `handleSubmit` was a `setTimeout(1000)` no-op that silently discarded submissions; live `/disputes/new` route already uses the real `useCreateDispute` mutation. Adds barrel-guard test so the stub cannot be re-exported.
- #2878 code-review-api-handlers-unbounded-pagination-limit — new shared `routes::pagination::clamp_limit` helper clamps user-supplied `?limit=N` into `[1, MAX_PAGE_LIMIT]=100` (and floors hostile non-positive values before the `as usize` cast); applied at every affected handler boundary in ai/*, vendors, compliance, predictive_maintenance. Closes the cheap-DoS class flagged in the 2026-08-29 tier1d review.

## Watch
- Stalled review: PR #2744 (gh-issue-2743) — now **17d in review**, approved + CI-green but `needs-human-review` + self-PR → dispatcher structurally cannot merge. Operator gate. (Unchanged from yesterday.)
- Reverted: none this run.
- Churn hotspots: no new churn analysis this run — 4h window; hotspot signals already captured under 2026-08-28/29. Files touched by today's 3 fix PRs are all previously-identified hotspots (`routes/ai/*`, `features/disputes/pages/*`, `routes/groups/person-months.tsx`) and the *fix* is exactly what the churn signal was pointing at.

## PRs stuck in draft despite approval
- #2744 gh-issue-2743 — approved + CI green, still `draft` + `needs-human-review` after 17d (self-PR archive-push fix; operator-gated)

## PRs with verdict=changes, no fix-round progress in 24h
- none (only `review` row in `assignments.json` is #2744 which is `approve`+human-gated, not `changes`)

## Code review slice
- Segment reviewed: **mobile-native-kmp + reality-server** (both covered by dispatcher Tier-1d generator artifacts on 2026-08-30 before this routine ran — see `.research/signals/2026-08-30-mobile-native-kmp-tier1d.json` and `-reality-server-tier1d.json`)
- Experts: kotlin (KMP), rust (reality-server) — both static-analysis; findings folded verbatim
- Findings: 3 `code-review-finding` signals folded (1 high-confidence bug + 1 medium-confidence bug + 1 medium-confidence bug — see *Backlog deltas*)
- Next segment: **api-handlers** would be next by oldest-unreviewed (all 8 segments are now within 5 days of today after this run). No pressure to force a Phase-1.5 spawn on this buffer-low run — the tier1d generators already covered the two most-stale segments.
- Skipped: **Phase 1.5 (routine's own review agent) skipped** — the dispatcher's Tier-1d generator already covered both mobile-native-kmp (finding: create-listing-not-wired) and reality-server (findings: bytelen-vs-charcount + report-contact-unvalidated) BEFORE this routine ran (commit 0182df4f at 00:38 UTC). Spawning a second reviewer for the same segments this run would duplicate work.

## Screen-map status
- Total `docs/screens/` files: unchanged (mobile/ still not seeded → 0)
- Drift signals this run: 0 — none of the 3 merged PRs touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**` (#2876 touched `routes/groups/person-months.tsx` — a *feature route file*, not the top-level `App.tsx`/`routes/**` trigger set from `.github/workflows/screen-map.yml`; #2877 deleted a `features/disputes/pages/` file; #2878 is backend-only).
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not checked this run (no route-touching merges).

## Backlog deltas
- **New (3 rows, all vector = bug):**
  - [2] `code-review-mobile-native-kmp-create-listing-not-wired` — bug, confidence **high** — realtor Create-Listing composable's `onSubmit` is a hardcoded `NotImplementedError` stub; `PortalListingsRepository` lacks a `createListing()` method even though the backend `POST /api/v1/my/listings` already exists. 100% non-functional advertised feature. (Cloud-unlandable per issue #2652 — flagged in tier1d artifact for a non-cloud implementer.)
  - [2] `code-review-reality-server-bytelen-charcount-validation` — bug, confidence medium — public write validators (`inquiries.rs`, `articles.rs`, `agent_reviews.rs`, `reports.rs`) measure `str::len()` (bytes) but error strings + docs say "characters". Slovak/Czech/German 2-byte diacritics → advertised 5000-char cap effectively ~2500 for diacritic-heavy input. Single small PR across 4 route files.
  - [1] `code-review-reality-server-report-contact-unvalidated` — bug, confidence medium — `submit_report` binds `reporter_email` and `reporter_phone` directly into INSERT with zero length/format validation on an anonymous-allowed endpoint. Sibling `inquiries.rs` caps both. Row bloat / junk moderator data / 500 on any DB-column-length constraint.
- **Bumped:** none this run (all 3 signals are fresh IDs; none re-emitted from `state.seen_signals`).
- **Decayed:**
  - `code-review-mobile-native-kmp-httpclient-no-timeout` — bug, high confidence — score 2 → 1 (updated_at was 2026-08-16, exactly 14 days ago; qualifies under the ≤ 14-day cutoff). Not dropped (score > 0). Cloud-unlandable per issue #2652 anyway.
- **Dropped:** none this run.
- **Resolved (moved open → done):**
  - `code-review-ppt-web-core-raw-fetch-bypasses-jwt-interceptor` → done via PR #2876
  - `code-review-ppt-web-ui-create-dispute-page-stub-submit` → done via PR #2877
  - `code-review-api-handlers-unbounded-pagination-limit` → done via PR #2878

## Plans promoted
- **none** — no open item meets the promotion bar this run:
  - Highest open score is 2 (`code-review-api-core-workflow-eq-type-strict-no-coercion`, `-docgen-summarize-char-boundary-panic`, `-mobile-native-kmp-create-listing-not-wired`, `-reality-server-bytelen-charcount-validation`); the standard bar is score ≥ 3.
  - Security fast-track (`vector==security AND confidence==high AND score>=2`) does not apply — no `security` items are open (yesterday's `code-review-api-core-client-ip-cf-spoof` was the only recent security row and it's already `ready`+planned + claimed).
  - Result: 2 plans stay `ready` (client-ip-cf-spoof, mobile-native-kmp-inquiries-response-contract), 15 items sit `open`, 0 new plans promoted. Refilling further requires either another tier1d review pass raising an item to score 3, or a stacking signal (churn+risky-churn, revert, or PR-body-referenced follow-up) on one of the existing open score-2 items.

## Auto-fix
- `no candidate` — all 3 tier1d signals this run are `code-review-finding` (analytical, medium/high-confidence but not on the Phase-5 allowlist: screen-map-drift, screen-map-orphan, fixme-in-merged-code, lockfile-drift, stalled-review). Phase 5 is a no-op this run.

## Self-review findings
- none — `.research/self-improvement/findings.json` not touched this run.

## Skipped by design (Phase 1.6)
- Phase 1.6 (ppt-project-management skill) **skipped** for this buffer-low fire. Rationale unchanged from 2026-08-29: buffer starvation is a *cloud-runner capability gap* (KMP AGP egress-blocked → mobile-native items unlandable) plus a *score-threshold artefact* (no open item has stacked evidence to score 3), not a delivery-analysis gap; running pm-devops (next in rotation `pm_cursor.next_index=4`, last-run 2026-06-16) would produce more analysis, not more claimable work. Rotation cursor + `role_last_run` left untouched so the next normal-cadence tick picks up where the daily rotation left off. G16 is a no-op under this skip.

## Open questions
- **PR #2744 (self-PR) — now 17d idle** — needs the operator to either merge manually (clear `needs-human-review` label + un-draft) or explicitly close/rework. Same operator gate as yesterday.
- **Structural buffer floor** — with 15 open items at score 1–2 and no cheap way to stack evidence in a 4-hour window between routine ticks, the claimable buffer will stay below floor until either: a) a tier1d generator surfaces a **new** high-signal item that lands directly at score 3 (needs a `+3` signal type — `revert` or a bug/security-labelled unchecked-todo with candidate_vector matching), or b) an existing item picks up an *additional* signal (churn, risky-churn, revert reference, or explicit PR-body TODO ping). This is a real limitation of the buffer-refill loop, not a bug — surfacing so the operator can decide whether to relax the score threshold or add cheap secondary signals.

## Quiet day?
- **No** — 3 PRs merged, 3 new backlog vectors, 3 items resolved, 1 decayed. Buffer status: unchanged (still below floor; nothing promotable). See *Plans promoted* + *Open questions* for why refill has hit a structural floor and what would unstick it.
