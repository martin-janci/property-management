# 2026-07-14

## Since last run
- Merged PRs: 30 (range #2273–#2315)
- Open PRs touched: 1 (PR #2316 — dispatcher's `auto-impl/gh-issue-2314` cross-tenant IDOR fix in `news_articles`; draft + `needs-human-review`)
- New / updated issues: 1 new (#2314 — cross-tenant IDOR in news_articles, filed by dispatcher Tier-1d review) + 12 closed (post-merge-review follow-ups all resolved by their matching PRs this window)
- Commits: 30 on `dev` (mixed: application-code merges + dispatcher plumbing commits)
- Routine lag: 41h since last run (2026-07-13T02:11:42Z; prior run was a 3h49m quick run — so effective gap to last real activity window is ~day-and-a-half). `lag_warning` emitted; not stale (<3d).
- Phases that failed: none — Phase 1.5 (routine code review) and Phase 1.6 (project management) both skipped by policy, not by failure (dispatcher already ran 5 Tier-1d dev-review passes today across `api-handlers`, `mobile-rn`, `ppt-web-core` and daily PM state is fresh from dispatcher commits).

## Shipped
- #2273 gh-issue-2272 — extract RAG provenance model_filter assertions into own `#[sqlx::test]` (test-gap follow-up)
- #2274 refactor-churn-hotspots-api-server-auth-2026-07-12 — extract `authenticated_user_id()` helper in api-server auth (resolves yesterday's sole open backlog row)
- #2278 gh-issue-2275 — portal inquiry webhook returns 500 on lead persistence failure (backend fix)
- #2280 gh-issue-2277 — reality-web AgencyDashboard error-state fix (misleading "No Agency Found" on any API error)
- #2281 gh-issue-2276 — reality-web listing detail SSR crash on partial 200 body
- #2285 gap-84-3 — ship reality/favorites screen-map baseline (docs/screens/reality/)
- #2286 gap-83-3-replay-protection — add freshness/timestamp window to portal webhook (webhook security)
- #2287 gap-84-3 — price-alert subscription surface in reality-web
- #2288 gap-84-3 — mobile-native price-tracking surface for favorites (KMP)
- #2289 gh-issue-2279 — scope AI chat by-session-id handlers to owning user (within-tenant IDOR; same class as today's #2314)
- #2290 gap-84-4 — route pipeline push through FCM+APNs combined adapter (real iOS transport)
- #2291 gap-84-5 — wire `migrate_embeddings_to_pgvector` to HTTP route
- #2292 gap-81-1-frontend — wire Create-new-schedule flow to backend (ppt-web)
- #2293 gap-84-4 — notification-trigger management UI (ppt-web)
- #2294 gap-83-3 — portal webhook status manager surface (ppt-web)
- #2295 gap-81-1 — recompute report schedule `next_run_at` on cron edit
- #2296 chore(backend) — bump yanked `spin` versions (unblock cargo-deny gate)
- #2297 gap-screens — normalize story-id-style epic refs in screen-map frontmatter
- #2298 gap-tracking — reconcile shipped-story tracking drift
- #2304 gh-issue-2282 — mobile dashboard surfaces load errors instead of silent zeros (RN)
- #2305 gh-issue-2283 — test mobile useApi JWT decode + X-Tenant-ID injection (RN)
- #2306 gap-84-2 — add screen-map for signer-facing e-sign page
- #2307 gh-issue-2299 — tick Tasks/Subtasks in 15 reconciled done story-md files
- #2308 gh-issue-2284 — test mobile DocumentUploadScreen upload/validate/icon paths (RN)
- #2309 gap-84-1 — add presigned PUT endpoint for direct client-to-S3 upload
- #2310 gh-issue-2301 — harden gap-84-4 push wiring (honest metrics, single token fetch, routing guard)
- #2311 gh-issue-2302 — capture letter-suffixed epics in screen-map drift extractor
- #2312 gh-issue-2300 — pgvector back-fill stamps provenance + set-based UPDATE
- #2313 gh-issue-2303 — wire `report_schedules` due-work consumer + unify cadence model
- #2315 gap-83-2 — exercise Booking.com `fetch_property` outbound happy path (test coverage)

## Watch
- Stalled review: none — every recent PR merged or closed within its 24h window
- Reverted: 0
- Churn hotspots: not recomputed this run (30 PRs is too large a batch to re-derive per-file churn from scratch without heavy API calls; dispatcher's Tier-1d generator has already covered the fastest-churning segments — `api-handlers`, `mobile-rn`, `ppt-web-core` — with 13 code-review-finding signals this run; existing hotspot state left untouched)

## PRs stuck in draft despite approval
- #2316 auto-impl/gh-issue-2314 — draft, approved by bot reviewer, `needs-human-review` label (security hot-path, self-approve blocked per dispatcher Phase 5.5). Opened 2026-07-14T16:37Z (age ~4h) — under the 24h age gate but noted here because the PR is in the classic "stuck-in-draft-despite-approval" shape

## PRs with verdict=changes, no fix-round progress in 24h
- none — sole active dispatcher assignment (`gh-issue-2314` / PR #2316) is in `review` with `verdict=approve`; no `verdict=changes` open

## Code review slice
- Segment reviewed: **skipped by the routine**
- Reason: dispatcher already ran 5 Tier-1d `dev-review` generator passes today covering `api-handlers` (1 finding), `mobile-rn` (9 findings across 3 batches), and `ppt-web-core` (3 findings). All 13 signals consolidated into `.research/signals/2026-07-14.json` by this routine run and added to the backlog. No fresh churn arrived after those passes to justify a duplicate routine slice; oldest-unreviewed after today's dispatcher touch is `mobile-native-kmp` (cursor `2026-07-12`).
- Experts: rust (news-articles IDOR) · tester (NFC, voice, onboarding, deeplink-mgr coverage gaps) · frontend/RN (i18n, dashboard-no-err-ui, queue-swallow, meter-console) · frontend/web (units-fetch-noauth, dashboard-unguarded, sessions-cast)
- Findings: 13 (see Backlog deltas below)
- Next segment: `mobile-native-kmp` (oldest-unreviewed after this run)
- Skipped: yes — dispatcher covered

## Screen-map status
- Total `docs/screens/` files: 165 (103 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` for the React Native app still not seeded → 0)
- Drift signals this run: 0 emitted by routine (per-PR file-list enumeration for 30 PRs skipped — routine budget; `screen-map.yml` CI on `main` remains the enforcement path). #2306 explicitly *added* a screen-map for the signer-facing e-sign page, and #2311 fixed the extractor's letter-suffixed-epic bug — so drift-detection tooling itself moved forward this window.
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried (no route-file activity requiring a spot check; PR #2311 shipped the extractor fix so any post-#2311 drift would be caught cleanly)

## Backlog deltas
- **New (13):**
  - [3] `code-review-api-handlers-news-articles-idor` — Cross-tenant IDOR + data-loss in `news_articles` handlers (raw pool, no org scope, RLS ENABLE-only under BYPASSRLS) → status=`deferred` (fix in flight via PR #2316; will flip to `done` on merge)
  - [3] `code-review-mobile-rn-nfc-no-tests` — NFC credential/access modules (security-critical) have zero tests
  - [2] `code-review-mobile-rn-announcements-i18n` — AnnouncementsScreen hardcodes English strings
  - [2] `code-review-mobile-rn-dashboard-no-err-ui` — dashboard silently renders zero data on API errors
  - [2] `code-review-mobile-rn-deeplink-mgr-untested` — DeeplinkManager (auth-token bridging) untested
  - [2] `code-review-ppt-web-core-units-fetch-noauth` — person-months units fetch missing Bearer token
  - [2] `code-review-ppt-web-core-dashboard-unguarded` — manager/resident dashboards routed without ProtectedRoute
  - [1] `code-review-mobile-rn-queue-swallow-error` — offline queue swallows errors on transient failures
  - [1] `code-review-mobile-rn-voice-module-no-tests` — voice module (mutating input surface) untested
  - [1] `code-review-mobile-rn-fault-category-i18n` — fault category labels hardcoded English
  - [1] `code-review-mobile-rn-meter-console-error` — meter screen throws unhandled console.error on invalid input
  - [1] `code-review-mobile-rn-onboarding-untested` — first-run onboarding flow has zero coverage
  - [1] `code-review-ppt-web-core-sessions-cast` — SessionsPage double-cast obscures type-safety
- **Bumped:** none — all 13 signal ids are new; no matching backlog rows to stack onto
- **Decayed:** none — no `open` items older than 14 days on entry (backlog was fully drained yesterday)
- **Dropped:** none

## Plans promoted
- none — the two candidates that meet strict readiness gates (`code-review-api-handlers-news-articles-idor` under security fast-track; `code-review-mobile-rn-nfc-no-tests` under score≥3) are being left for the dispatcher's Tier-2 planner to schedule via `assignments.json` (the primary plan-management path for this repo). The routine's `plans/` directory hasn't been fed new items since 2026-07-11 — that's the current operating pattern.
  - `code-review-api-handlers-news-articles-idor` is already actively implemented in PR #2316 — a routine-promoted plan would duplicate in-flight work; deferred instead.
  - `code-review-mobile-rn-nfc-no-tests` will be handled by the dispatcher's next planner cycle (Tier-2 planner was kicked at 2026-07-14T18:12Z per the last dispatcher commit).

## Auto-fix
- `no candidate` — no signal met the certainty bar this run. All 13 new signals are `code-review-finding` type (not in the Phase 5 allowlist: `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise-lockfile-drift`, `stalled-review`). No stalled-review candidate either — #2316 is fresh and PR-owner has active dispatcher tracking.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — mcp__github__push_files can't inline the >64KiB assignments-archive.json; fix: adopt PR+API-merge as the dispatcher's primary push path for the archive (proven this env)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend gc1-reconcile to auto-close any exact-id-terminal row (including status=failed)

## Open questions
- none — dispatcher has the single security follow-up (PR #2316) fully staged; awaits only the human-review sign-off

## Quiet day?
- No — 30 PRs merged, 13 new backlog vectors, 1 in-flight security fix (PR #2316), 13 code-review findings from dispatcher Tier-1d passes. Not bumping `stats.quiet_days`.
