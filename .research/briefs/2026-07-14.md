# 2026-07-14

## Since last run
- Merged PRs: 19 (range #2273–#2298) — huge burst; the dispatcher drove ~all of them from Tier-1a assignments (gap-* / gh-issue-* fast-path)
- Open PRs touched: 1 (#2304 auto-impl dashboard-load-errors, draft, <1h old)
- New / updated issues: 12 — #2299–#2303 all `follow-up`/`from-merged-review` (post-merge review byproducts), #2282–#2284 all `frontend`/`follow-up` from the dispatcher's mobile-rn Tier-1d dev-review; issues #2275–#2279 are the CLOSED counterparts of the merged bug/IDOR fixes
- Commits: 50 on `dev` since 2026-07-13T02:11Z (19 app-code merges + 31 `chore(research):` dispatcher/routine plumbing)
- Routine lag: 25h since last run — on schedule
- Phases that failed: none

## Shipped
- #2273 test-gap(backend) — extract RAG provenance `model_filter` assertion into its own `#[sqlx::test]` (BIT-351 quarantine still gates execution — regression guard now has its own case rather than being buried inside `rag_retrieval_correctness` section E)
- #2274 refactor(api-server) — extract shared bearer-auth preamble in auth routes (the churn hotspot from yesterday's brief resolved by the intended refactor)
- #2278 fix(api-server) — portal inquiry webhook now 500s on lead persistence failure instead of silently dropping to a false 200 (closes #2275)
- #2280 fix(reality-web) — AgencyDashboard now renders a real error state instead of the misleading "No Agency Found" screen on any API error (closes #2277)
- #2281 fix(reality-web) — listing detail SSR guards address/features deref on partial 200 bodies (closes #2276)
- #2285 docs(reality-web) — ship reality/favorites screen-map baseline (docs-only, gap-84-3)
- #2286 fix(83-3-portal-webhooks) — add replay/freshness (5-min timestamp window folded into HMAC input) to portal webhook receiver; regression tests cover stale/future/missing/non-numeric timestamps + signature-swap attack
- #2287 feat(reality-web) — price-alert subscription UI at `/[locale]/favorites/alerts` (story 84.3)
- #2288 feat(mobile-native) — KMP price-tracking surface + view-model for favorites (story 84.3)
- #2289 fix(api-server) — AI chat by-session-id handlers now scope by user, not just org (closes #2279 IDOR)
- #2290 feat(84-4) — pipeline push now routed through combined FCM+APNs adapter (iOS transport wired for the synchronous dispatch path, no longer dropped as "not yet implemented")
- #2291 feat(84-5-pgvector-rag) — `POST /api/v1/ai/llm/rag/migrate` (platform-admin only) wired for legacy no-provenance row back-fill
- #2292 feat(reports) — Create-new-schedule flow now actually wired (button was inert before; ReportsPageRoute now passes `onCreateSchedule` + report list)
- #2293 gap-84-4 — notification-trigger management UI at `/notifications/triggers` (list/update/reset per channel)
- #2294 gap-83-3 — portal webhook status manager surface at `/settings/portal-webhooks` (read-only syndication dashboard)
- #2295 fix(UC-81) — `update_schedule` now recomputes `next_run_at` from the new cron on edit (was stale after edit; issue #2242 finding)
- #2296 chore(backend) — bump yanked `spin` 0.9.8/0.10.0 → 0.9.9/0.10.1 (unblocks cargo-deny on every PR)
- #2297 docs(ppt-screens) — normalize story-id-style epic refs in 16 ppt screen-map frontmatters (`Epic-6-5` → `Epic-6`, `Epic-10B-7` → `Epic-10B`, etc.)
- #2298 docs(epic-79) — reconcile shipped-story tracking drift for epics 79/82/84/85/9 (story-md Status flips + sprint-status.yaml annotations)

## Watch
- Stalled review: none — sole open PR (#2304) is <1h old
- Reverted: 0
- Churn hotspots this run — three files broke 200+ lines:
  - `backend/servers/api-server/src/routes/integrations/webhook.rs` — 390 lines from #2286 (replay-protection). Already in seen_signals; cumulative re-emit, no score bump; hotspot_history runs_seen bumped to 2 (repeated-churn will fire next run if it churns again).
  - `frontend/apps/ppt-web/src/features/settings/webhooks/PortalWebhooksPage.tsx` — 303 lines from #2294 (fresh surface, first-time hotspot).
  - `frontend/apps/ppt-web/src/features/notification-triggers/pages/NotificationTriggersPage.tsx` — 283 lines from #2293 (fresh surface, first-time hotspot).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none — assignments.json shows two `in-progress` rows (`gap-tracking-reconcile-story-md-statuses-2026-07-13` — already merged as PR #2298 but assignment not yet marked, and `gh-issue-2282` — PR #2304 exists as draft <1h old). Neither has a `verdict=changes` reviewer summary.

## Code review slice
- Segment reviewed: **skipped by the routine**
- Reason: dispatcher already ran a Tier-1d `ppt-dev-review` on `mobile-rn` today (`.research/signals/2026-07-14-mobile-rn.json`, 3 findings — nfc-no-tests, dashboard-no-err-ui, deeplink-mgr-untested). Those findings live on the dispatcher's audit trail, not the routine's `signals[]`, since the two loops keep separate seen-sets.
- Findings: 0 (routine skipped) + 3 (dispatcher Tier-1d, already committed on its own path)
- Next segment: `ppt-web-core` (oldest-unreviewed by routine's own `state.review_cursor`, last touched 2026-07-07 — 7 days)
- Skipped: yes — dispatcher already covered mobile-rn today

## Screen-map status
- Total `docs/screens/` files: 164 (103 `ppt/`, 46 `reality/` (46 not 47 after #2297's dedup), 15 `reality-mobile/`; `mobile/` — the React Native app — still not yet seeded → 0). PR #2285 added `reality/favorites.md`; PR #2297 deduped two settings-two-factor entries.
- Drift signals this run: **4** (PRs #2287, #2281 on the reality side, #2292 and #2294 on the ppt side). All four are the classic "route touched but no matching screen-doc refresh" pattern — best-effort heuristic per routine spec §Best-effort heuristic; **treat as review candidates, not promotable plans**.
- Orphan screens this run: 0 (no screen-doc slugs went stale relative to routes touched this run)
- Last `screen-map.yml` CI run on `main`: not queried this run (no application-code push touched the workflow file directly; the last known green from yesterday's brief stands)

## Backlog deltas
- **New:**
  - [1] `refactor-churn-portal-webhooks-page-2026-07-14` — new ppt-web PortalWebhooksPage.tsx (303-line surface just landed; watch for follow-up churn)
  - [1] `refactor-churn-notification-triggers-page-2026-07-14` — new ppt-web NotificationTriggersPage.tsx (283-line surface just landed; watch for follow-up churn)
  - [2] `screen-map-drift-pr-2287-reality` — `/favorites/alerts` sub-route shipped without a matching `docs/screens/reality/*.md`
  - [2] `screen-map-drift-pr-2281-reality` — listing-detail SSR guard shipped without a `docs/screens/reality/listing-detail.md` refresh
  - [2] `screen-map-drift-pr-2292-ppt` — create-schedule flow wired in reports.tsx without a `docs/screens/ppt/reports*.md` refresh
  - [2] `screen-map-drift-pr-2294-ppt` — new `/settings/portal-webhooks` route added without a `docs/screens/ppt/portal-webhooks.md`
- **Bumped:** none (webhook.rs churn re-emit is cumulative-type; per Phase-1 dedup rule the `score_delta` is not re-applied)
- **Decayed:** none — sole prior-open item `refactor-churn-hotspots-api-server-auth-2026-07-12` already flipped to `done` in the interim (resolved by PR #2274, extracted-authenticated_user_id refactor); no other open item existed to age
- **Dropped:** none

## Plans promoted
- none — the 6 new open items are all at score 1 or 2, below the promotion threshold (≥3). No security fast-track candidates either (nothing is `vector=security` at `confidence=high`). Review-candidate screen-map-drift signals stay in the backlog per the routine's `§Best-effort heuristic` note; they'll compound if the pattern repeats or a human bumps them.

## Auto-fix
- `no candidate` — no signal met `confidence=high` + `score>=3` + allowlist this run. The 4 `screen-map-drift-*` signals ARE on the allowlist but all fire at `confidence=medium` (the heuristic caveat) and land at `score=2` on first emit — the bar keeps them in the manual-implementer / plan-triage path. The two `churn-hotspot-*` signals are outside the allowlist (churn is analytical, not an actionable auto-fix shape).

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — mcp__github__push_files can't inline the >64KiB assignments-archive.json; fix: adopt PR+API-merge as the dispatcher's primary push path for the archive (proven this env)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend gc1-reconcile to auto-close any exact-id-terminal row (including status=failed)

## Open questions
- none

## Quiet day?
- No — 19 merged app-code PRs since last run, 6 new backlog vectors (2 churn hotspots + 4 screen-map drift), 0 plans promoted, 0 auto-fixes. The `PM_DIGEST_QUIET` gate in Phase 6 flips to send (this is not quiet).
