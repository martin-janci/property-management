# 2026-09-02

## Since last run
- Merged PRs: 5 (range #2922–#2928)
- Open PRs touched: 1 (of 20 total open)
- New / updated issues: 2 (#2923, #2924 — both closed in-window by #2927/#2928)
- Commits: 9 on `dev` (5 merge commits + 4 chore/research)
- Routine lag: 0d 8h since last run — on schedule
- Phases that failed: none (`gh pr/issue list` GraphQL 403'd; recovered via `mcp__github__list_pull_requests` REST calls, cursor still advanced)

## Shipped
- #2928 fix(db): snapshot-consistent report_summary counts + entries — GDPR admin-report render now uses one CTE/txn so counts and entries match under concurrent writes; closed follow-up issue #2924 same-window (auto-review loop working).
- #2927 test(UC-62): cover update-error i18n path in EmergencyContactDirectoryPage — added the two missing regression cases (update + reload) so all 5 localized error handlers are pinned; closed follow-up issue #2923.
- #2926 refactor(api-server): remove decommissioned FCM legacy send path — dropped `send_fcm_legacy`, `FCM_SERVER_KEY`, and both fallback call sites; `is_configured()` now requires `FCM_PROJECT_ID` (v1 only). Resolves backlog `code-review-api-core-fcm-legacy-dead-endpoint`.
- #2925 code-review-api-handlers-compliance-raw-db-leak-regression — routed compliance audit-log count error through `db_error()` so no raw sqlx text reaches clients; extended regression suite. Closes the raw-DB-leak reintroduction from PR #2915.
- #2922 refactor(reality-server): derive saved-search HTTP status from typed error enum — replaced fragile error-string comparison with a discriminant enum on `create_saved_search`/`update_saved_search`.

## Watch
- Stalled review: #2555 / #2558 / #2559 — accounting MVP trio (UC-ACC-05.8/05.9/05.17) 35 days idle with no reviewDecision; already tracked (`stalled-review-pr-25{55,58,59}` in seen_signals); pm-scrum-master flags as reviewer-starvation.
- Reverted: none this run.
- Churn hotspots: `backend/servers/api-server/src/services/push_fanout.rs` (169 lines, runs_seen=2 — 1st repeat since 2026-05-26); `backend/servers/reality-server/src/routes/saved_searches.rs` (106 lines, new hotspot); `backend/servers/api-server/tests/suites/push_delivery_receipt_tests.rs` (99 lines, new hotspot — test file).

## PRs stuck in draft despite approval
<!-- Query would need reviewDecision; MCP output too large to slice for that field this run. Safe default. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Dispatcher assignments live on the planning branch; nothing surfaced in the local snapshot. -->
- none

## Code review slice
- Segment reviewed: reality-server (reason: churn-aligned — `saved_searches.rs` fresh hotspot AND tied for oldest-unreviewed at 2026-08-31; backlog had 0 open reality-server findings so not saturated)
- Experts: rust
- Findings: 2 (both high-confidence, both surfaced as new backlog rows at score 2)
  - `code-review-reality-server-sso-callback-leaks-upstream-error` (security, +2) — `sso.rs:300-305 / :740-741 / :756-758` echo raw OAuth upstream text (invalid_grant reason, misconfigured client secret notice, internal URLs) to unauthenticated `/api/v1/sso/callback` callers.
  - `code-review-reality-server-bg-workers-interval-not-floored` (bug, +2) — `saved_search_alerts.rs:133` + `favorite_alerts.rs:73` build `interval(Duration::from_secs(env_int))` with no floor; `SAVED_SEARCH_ALERT_INTERVAL_SECS=0` / `FAVORITE_ALERT_INTERVAL_SECS=0` panic `tokio::time::interval` at tick. Sibling `search_alert_drainer.rs:286-288` documents the trap and floors via `poll_interval()`.
- Next segment: mobile-native-kmp (oldest unreviewed after this run — 2026-08-25)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` 106 + `reality/` 47; `mobile/` not yet seeded → 0)
- Drift signals this run: 0 (no PR this run touched a trigger route file — `App.tsx`, `routes/**`, or `app/**`)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried (network path via `mcp__github__actions_list` unused this run — no drift, no need)

## Backlog deltas
- **New:** [2] code-review-reality-server-sso-callback-leaks-upstream-error — Phase 1.5 finding, security/high.
- **New:** [2] code-review-reality-server-bg-workers-interval-not-floored — Phase 1.5 finding, bug/high.
- **Bumped:** none this run (no existing item accumulated a fresh scoring signal).
- **Decayed:** none crossed the −1 threshold today; portfolio-analytics-caps-100 (score 1 → 0) rolled to dropped below.
- **Dropped:** [1→0] code-review-mobile-native-kmp-portfolio-analytics-caps-100 — decayed: no new signals in 14 days (updated_at 2026-08-19).
- **Resolved (→ done):** code-review-api-core-fcm-legacy-dead-endpoint — PR #2926 removed `send_fcm_legacy`, `FCM_SERVER_KEY`, both fallback sites; grep at `push_fanout.rs` shows only removal-comments remain.
- **Spot-checked (stays open):** code-review-reality-server-agent-review-self-review — routes renamed to `agent_reviews.rs`; `create_review` (:200-290) inserts `(realtor_id, reviewer_user_id=principal.user_id)` with no `principal.user_id != realtor_profiles.user_id` guard. Gap confirmed; already covered by dispatcher assignment PR #2919 (in review), so not re-promoted here.

## Plans promoted
- `plans/code-review-mobile-native-kmp-create-listing-not-wired.md` — wire KMP realtor `CreateListingScreen.onSubmit` to `POST /api/v1/my/listings` (add `createListing` to `PortalListingsRepository`, extend `CreateListingInput` with the three fields the backend requires — `propertyType`, `street`, `postalCode`) · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run (drift/orphan/fixme/dep-lockfile/stalled-review-new all quiet; the two Phase 1.5 findings need real code changes and stay on the manual-plan path).

## Self-review findings
- open=37 (high=9, medium=25, low=3); new this run=0; acknowledged=19
- fp- [recurrence=18] — dispatcher-prompt hard-assumes `git push` is proxy-403'd and defaults PUSH_METHOD to MCP `push_files` with a 64KiB inline guard; recent runs show direct `git push` succeeds once the commit author email is `<user>@users.noreply.github.com`; fix: runtime push-capability probe in Phase 1 and bypass the 64KiB guard when git-push is available.
- fp- [recurrence=18] — Phase 6 archive-move produces >64KiB assignments-archive.json that MCP `push_files` cannot inline; fix: adopt the proven new-branch push + `create_pull_request` + `merge_pull_request` (squash) path as primary large-archive push mechanism.
- fp- [recurrence=12] — Tier-1d generator keeps firing when buffer is drained and `.research/signals/*tier1d*.json` piles up without becoming backlog rows; fix: add signals→backlog promoter invoked by Phase 2.6 when `avail=0`, or gate Tier-1d on unconsumed-signal ceiling.
- fp- [recurrence=6] — action-list.json holds many `in-progress` rows while `assignments.json` has only 3 live rows (twin-status inverted); fix: extend `gc1-reconcile.sh` to reconcile action-list vs live assignments; add T32 self-test asserting bijection.
- fp- [recurrence=4] — PR #2744 batches two unrelated dispatcher fixes and has been unmergeable ~11d on the needs-human-review self-PR gate; fix: split self-remediation to one-fix-per-PR and surface a Phase-7 "remediation-blocked" line so operators see which recurring findings have a staged fix awaiting merge.

## Open questions
- pm-security surfaced a **log-scrubbing policy question** on push_fanout emitting `user_id` in structured `info` logs at ~10 sites — that's a data-classification / observability call, not a defect. Deferred to `.research/management/roles/pm-security.md`; not promoted here.

## Quiet day?
- No (5 PRs merged, 2 new backlog vectors, 1 plan promoted, 2 Phase 1.5 findings; Phase 1.6 rotated to pm-security after 6 weeks; coverage cursor advanced 7→8; stats.quiet_days unchanged at 3).
