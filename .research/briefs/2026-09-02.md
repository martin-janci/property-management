# 2026-09-02

_Scheduled routine fire; trigger payload `buffer-low: claimable=0/72 (honest=8) — all open backlog is mobile-native-gated or in-flight; need fresh landable vectors. refill planner`. Response: run rotating expert review to seed one landable, high-confidence bug plan on top of resolution recording; skip Phase 1.6 to keep this run focused on buffer-refill._

## Since last run
- Merged PRs: **4** (#2922 saved-search typed error enum, #2925 compliance regression, #2926 FCM legacy removal, #2927 EmergencyContactDirectoryPage update-error test coverage). Range #2922–#2927.
- Open PRs touched: 1 (#2928 report_summary snapshot fix — flipped open→auto-merge between `list_pull_requests` and `list_commits` calls; head commit `7f9b02c` present on `dev`).
- New / updated issues: 0.
- Commits: ~10 on `dev` (4 code merges + 5 dispatcher-chore commits + the #2928 auto-merge). origin/dev HEAD `7f9b02c`.
- Routine lag: 6h since last run — on schedule (yesterday's 2nd fire at 18:38Z).
- Phases that failed: none.

## Shipped
- #2927 gh-issue-2923: cover update-error i18n path in EmergencyContactDirectoryPage tests — wired `onEdit` into stubbed EmergencyContactsList and added an update-failure test asserting the translated `emergency.errors.failedToUpdate` copy is shown and the raw backend message is not leaked.
- #2926 code-review-api-core-fcm-legacy-dead-endpoint — removed `send_fcm_legacy`, `FCM_SERVER_KEY` env + `FcmConfig::server_key`, and both fallback call sites; `is_configured()` now requires `FCM_PROJECT_ID`; deleted 2 legacy integration tests + `make_legacy_adapter` and 2 server-key unit tests. Directly resolves the corresponding backlog item.
- #2925 code-review-api-handlers-compliance-raw-db-leak-regression — routed compliance.rs:319 audit-log count error through `db_error`; added a source-level regression guard test (`no_handler_bubbles_raw_db_error_to_client`) that fails on any future `map_err(...e.to_string())` with `INTERNAL_SERVER_ERROR` in the module. Resolves the regression signal filed yesterday.
- #2922 code-review-reality-server-saved-search-error-string-status — introduced typed `SavedSearchError` enum (`LimitReached`/`NotFound`/`Db`) in `reality_portal_repo`; route now maps the discriminant to 400/404/500 without matching error text; unit tests assert status-per-variant.

## Watch
- Stalled review: unchanged from yesterday's brief (#2559 / #2558 / #2555 UC-ACC-05.x — `needs-human-judgement`, 34+ days).
- Reverted: none this window.
- Churn hotspots: nothing new — the 4 PRs each touched a distinct file. The 3-in-6h `compliance.rs` cluster from yesterday closed with #2925.

## PRs stuck in draft despite approval
- #2919 code-review-reality-server-agent-review-self-review — draft, dispatcher-owned, updated 2026-09-01 18:28Z. Approved but held by a cancelled test-shard (dispatcher notes eviction, not a real red). Age ~6h — not yet a stuck-in-draft case.
- #2902 screen-map-drift-pr-2894-reality — unchanged (branch contamination, needs-human-review).
- #2744 fix(dispatcher): un-wedge oversize-archive push — unchanged (self-approval blocker, open since 2026-08-13 → 20d).

## PRs with verdict=changes, no fix-round progress in 24h
- #2902 — unchanged from yesterday's brief.
- otherwise none.

## Code review slice
- Segment reviewed: `reality-web` (reason: oldest-unreviewed landable segment — `mobile-native-kmp` is older at 2026-08-25 but stays gated in cloud per #2652, so a KMP review would add to the parked pile not the claimable pile).
- Experts: frontend.
- Findings: **3** (see Backlog deltas below) — one high-confidence severe bug (score 3, promoted to a plan this run), one high-confidence i18n regression (score 2), one high-confidence test-gap (score 1).
- Next segment: still `mobile-native-kmp` on age; skip again unless the AGP cloud-egress unblock lands, then rotate through the four 2026-08-29 slots (ppt-web-ui/ppt-web-core/mobile-rn — reality-web now moves to `2026-09-02`, off the oldest list).
- Skipped: no — reviewed reality-web.

## Screen-map status
- Total `docs/screens/` files: unchanged (ppt/ + reality/; `mobile/` still not seeded → 0).
- Drift signals this run: 0 — of the 4 merged PRs, none touched a route file in `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`. #2927 touched an emergency-feature test file (not a route), #2925 backend only, #2926 backend only, #2922 backend only.
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched this run.

## Backlog deltas
- **New (3):**
  - [3] `code-review-reality-web-realtor-api-missing-bearer-header` — bug, high; realtor-api.ts `request()` never merges `getAuthHeader()`, whole realtor onboarding + listing publish 401s under form-login. Promoted to plan this run (see Plans promoted).
  - [2] `code-review-reality-web-comparebutton-hardcoded-english` — bug, high; 3 hardcoded English strings in CompareButton (`Remove/Add to comparison`, `Comparing/Compare`, `Maximum 4 properties. Remove one first.`) render on every listing card, sk/cs/de affected.
  - [1] `code-review-reality-web-realtor-api-no-tests` — test-gap, high; realtor-api.ts has no `realtor-api.test.ts` sibling — a fetch-stub test asserting outgoing headers would have caught the sibling bearer bug in the same run.
- **Bumped:** none this run.
- **Decayed:** `code-review-mobile-native-kmp-portfolio-analytics-caps-100` — −1 (updated_at 2026-08-19 exactly 14d old at 2026-09-02); score 1 → 0 → **dropped**. Mobile-native-kmp segment is cloud-gated (#2652); can be re-opened when a local implementer picks it up.
- **Dropped:** `code-review-mobile-native-kmp-portfolio-analytics-caps-100` (decay to 0, per above).
- **Resolved (from merged PRs #2925 + #2926):** **2 items**.
  - `code-review-api-core-fcm-legacy-dead-endpoint` — resolved by #2926. Verbatim id match in PR title + code-evidence-gone (`send_fcm_legacy` fn + `FCM_SERVER_KEY` env + both call sites deleted; grep confirms).
  - `code-review-api-handlers-compliance-raw-db-leak-regression` — resolved by #2925. Verbatim id match + compliance.rs:319 now `map_err(db_error)?`; regression-guard test added to prevent re-emergence.

## Plans promoted
- `plans/code-review-reality-web-realtor-api-missing-bearer-header.md` — realtor-api.ts request() must spread `...getAuthHeader()` into fetch headers so every realtor CRUD write carries the localStorage bearer token; mirrors the pattern already in `auth-api.ts:43`. Plan lists 3 fetch-stub test cases (bearer present, bearer absent for SSR/unauthed, caller-supplied override wins). adversarial pass: passed — source→hypothesis→approach→test-plan chain intact; failing-on-main assertion concrete; file exists on disk; no placeholder rot; Mode: cloud-ok.

## Auto-fix
- `no candidate` — the top new signal is high-confidence + score 3 but outside the auto-fix allowlist (allowlist covers screen-map-drift/orphan, fixme-in-merged-code lockfile-bump, stalled-review; raw code-review-findings are not eligible by design). The score-2 i18n item is high-confidence too but a) below the score-≥3 bar and b) also outside allowlist.

## Self-review findings
- none (`.research/self-improvement/findings.json` absent).

## Open questions
- **Phase 1.6 skipped this run — is that acceptable when management artifacts are already 26h old at HEAD?** The routine skipped Phase 1.6 to keep budget on the reality-web review + plan promotion (buffer-refill priority). `pm_cursor.next_index` unchanged at 5 (`pm-security` still the next role). Management artifacts (`action-list.json`, `risks.json`, `project-state.md`) at HEAD are 26h stale (2026-08-31 03:10Z). Dispatcher's Phase 2 read tolerates this age; the operator should schedule a `pm-full` fire when convenient. G16 becomes a no-op this run (Phase 1.6 didn't advance the cursor), so this doesn't fail any gate — flagging it here for visibility.
- **#2652 AGP egress blocker — still deferred.** Buffer-refill via Phase 1.5 reality-web review demonstrates that non-mobile-native segments still have landable findings; no urgency to escalate the mobile-native unblock this run.

## Quiet day?
- **No.** 4 merged PRs, 3 new signals (one immediately promoted to a plan), 2 backlog resolutions, 1 decay-drop.

## Goal violations
- none — G1-G17 verified in Phase 6 (see `signals/2026-09-02.json.goal_checks[]`).
