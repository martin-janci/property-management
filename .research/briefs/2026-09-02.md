# 2026-09-02

_Buffer-low fire (payload: `claimable=8/72 landable=~0 — all 8 open mobile-native-gated; refill planner with non-mobile-native vectors`). Two non-mobile-native plans promoted this run: `neighbors-raw-db-error-leak` (security, mirrors PR #2925 pattern) and `mobile-rn-refresh-purges-offline-queue` (bug, silent offline-queue data loss)._

## Since last run
- Merged PRs since #2921: **5 PRs** with number > 2921 (#2922, #2925, #2926, #2927, #2928). PR #2919 also merged today (2026-09-02T12:34Z) but its number is ≤ last_pr_seen so the routine's cursor filter skips it; it's still surfaced under **Shipped** as it resolves an open backlog item.
- Open PRs touched: 1 (#2902 draft — screen-map-drift-reality, needs-human-review, branch contaminated).
- New / updated issues: 0 since cursor.
- Commits: ~15 on `dev` (6 code PRs + ~9 dispatcher-chore commits). HEAD `9f46336`.
- Routine lag: 18h 9m since last routine run (2026-09-01T18:38Z → 2026-09-02T12:48Z) — on schedule for a buffer-low fire.
- Phases that failed: none.

## Shipped
- #2928 `fix(db)`: snapshot-consistent `report_summary` counts + entries — one REPEATABLE READ read-only transaction pins both statements to the same snapshot; regression test locks the invariant `total_requests == entries.len()` when `limit >= rows`. Closes #2924.
- #2927 `test(UC-62)`: cover update-error i18n path in EmergencyContactDirectoryPage — completes the i18n error-copy coverage started by PR #2913; asserts translated `errors.failedToUpdate` copy on `handleUpdate` failure. Closes #2923.
- #2926 `refactor(api-server)`: remove decommissioned FCM legacy send path — drops `send_fcm_legacy`, `FCM_SERVER_KEY`, `FcmConfig::server_key`; `is_configured()` now requires `FCM_PROJECT_ID`; two legacy integration tests + `make_legacy_adapter` pruned; log/doc references cleaned.
- #2925 `code-review-api-handlers-compliance-raw-db-leak-regression` — routes compliance audit-log count query through `db_error()`; adds source-level regression guard that fails on any handler bubbling raw DB error via `map_err(... e.to_string())`.
- #2922 `refactor(reality-server)`: typed error enum for saved-search HTTP status — replaces text-matching `sqlx::Error.to_string().contains(…)` with `SavedSearchError { LimitReached, NotFound, Db }`; DB-free unit tests assert status is derived from variant, not message.
- #2919 `fix(reality-server)`: reject self-review on agent review endpoint — adds `subject != reviewer` guard on `POST /api/v1/realtors/{id}/reviews` (pure `reject_self_review` helper) + unit tests. Resolves backlog item `code-review-reality-server-agent-review-self-review`.

## Watch
- Stalled review: unchanged from prior brief (#2559 / #2558 / #2559 UC-ACC-05.x — 30d+, `needs-human-judgement`).
- Reverted: none this window.
- Churn hotspots: dispatcher-heavy `.research/` churn; the only real code hotspots were `backend/crates/db/src/repositories/data_export.rs` (#2928), `backend/servers/api-server/src/routes/compliance.rs` (already patched by #2925 for the leak regression + guard), and `backend/servers/api-server/src/services/push_fanout.rs` (#2914 window — already cooled). No new `churn-hotspot` signal fired (nothing exceeded stale top-3).

## PRs stuck in draft despite approval
- #2902 screen-map-drift-pr-2894-reality — draft, last updated 2026-09-01T02:28Z (~34h ago), branch contamination noted since 2026-08-31; human-gated.
- #2744 fix(dispatcher) un-wedge oversize-archive — self-PR, 20d+; human-gated.
- none other in the approved+draft class this run.

## PRs with verdict=changes, no fix-round progress in 24h
- #2902 — unchanged from prior brief (branch contamination, needs operator close+re-cut per dispatcher note).
- otherwise none.

## Code review slice
- Segment reviewed: **mobile-rn** (reason: oldest-unreviewed tied at 2026-08-29 + landable + not covered by today's dispatcher Tier-1d).
- Experts: frontend.
- Findings: 2 — `code-review-mobile-rn-refresh-purges-offline-queue` (high +3, high confidence, bug — silent offline-queue data loss on transient network refresh failure) and `code-review-mobile-rn-4xx-silent-drop` (medium +2, medium confidence, bug — 4xx-terminated queued actions dropped with only console.error).
- Next segment: `mobile-native-kmp` (review_cursor 2026-08-25 remains oldest overall; will stay deferred while cloud egress is gated on #2652) OR `ppt-web-ui` / `ppt-web-core` / `reality-web` (all 2026-08-29 now that mobile-rn advanced) — the routine will pick the next tie-broken segment on next fire.
- Skipped: no.

## Screen-map status
- Total `docs/screens/` files: unchanged.
- Drift signals this run: 0 (of the 6 merged PRs, only #2927 touches frontend, and it stays inside `frontend/apps/ppt-web/src/features/emergency/pages/` — not a route file; no `App.tsx` / `routes/**` / `reality-web/src/app/**` touched).
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched this run.

## Backlog deltas
- **New:** [3] `code-review-mobile-rn-refresh-purges-offline-queue` — see backlog.md; [2] `code-review-mobile-rn-4xx-silent-drop` — see backlog.md; [3] `code-review-api-handlers-neighbors-raw-db-error-leak` — see backlog.md; [3] `code-review-ppt-web-core-route-groups-no-protectedroute` — see backlog.md.
- **Bumped:** none this run.
- **Decayed:** `code-review-mobile-native-kmp-portfolio-analytics-caps-100` — score 1→0, status open→dropped (no new signal in 14 days).
- **Dropped:** the same portfolio-analytics-caps-100 item (decayed to 0).
- **Done:** `code-review-reality-server-agent-review-self-review` — resolved by PR #2919 (verbatim ID match in PR title, evidence-gone via unit-tested subject != reviewer guard).

## Plans promoted
- `plans/code-review-api-handlers-neighbors-raw-db-error-leak.md` — sanitize the 4 raw sqlx::Error leaks in `neighbors.rs` via a local `db_error(msg, e)` helper mirroring `admin_tenants.rs` / `legal.rs`; add source-level guard test alongside compliance's. · adversarial pass: passed
- `plans/code-review-mobile-rn-refresh-purges-offline-queue.md` — classify errors in `AuthContext.refreshToken()`; only invoke `logout()` (and its `resetLocalData()` queue purge) on real 401/403, not on fetch-rejects (network unreachable); regression test seeds `ppt_offline_queue` and asserts survival across a `TypeError('Network request failed')`. · adversarial pass: passed

## Auto-fix
- `no candidate` — none of the merged-PR signals this run match the Phase 5 allowlist (screen-map-drift, screen-map-orphan, fixme-in-merged-code, dep-update-noise lockfile-drift, stalled-review). The 4 new code-review findings need manual plans, not auto-fixes.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present in this repo tree).

## Open questions
- Should the ProtectedRoute wrapping for the 11 unguarded ppt-web route groups (pm-security finding, score 3 open) promote next run as a single sweep-plan, or split per-group? A single sweep is faster to write but larger to review — deferred for a routine-run judgement after `mobile-rn-refresh-purges-offline-queue` lands.
- pm-security noted the `migration.rs` broad `e.to_string()` surface (~35 sites) but flagged it as low-priority pending an admin/internal reachability audit — needs human confirmation before spending a plan slot on it.

## Quiet day?
- No — 5 merged PRs + 4 new backlog vectors + 2 promoted plans + 1 resolution + 1 decay. Trigger was buffer-low, response is a substantive refill of non-mobile-native landable work.
