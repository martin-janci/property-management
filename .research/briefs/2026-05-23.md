# 2026-05-23

## Since last run
- Merged PRs: ~30 substantive + many version bumps (range #344–#433; #435 open) — the repo adopted `dev` as the default working branch this window (#433)
- Open PRs touched: 1 (#435 — 10-pass dev-team P0/P1 security batch, opened today)
- New / updated issues: 1 (#375, closed — already labeled `bug`, no triage signal)
- Commits: 50 on `dev` since the last run
- Routine lag: 3d 2h since last run (last run 2026-05-20T10:53Z > 36h threshold → `lag-warning-2026-05-23`)
- Phases that failed: none

## Shipped
- #420/#421 — RLS migration completed (dead voting/faults modules removed; market_pricing, notification_preferences, reports routed through RLS). Resolves backlog `security-rls-migration-residual` (already marked `done`).
- #422 — RLS penetration tests now run as a non-superuser so RLS is actually enforced (resolves issue #375).
- #423 — `clap` "env" feature enabled so `ppt-seed` compiles / `cargo check --workspace` is green.
- #427 — CI frees disk before heavy cargo build/test (fixes "No space left on device").
- #431/#433 — caddy/deploy-server `register_route` idempotency + post-merge hardening.
- #385 — mobile-native toolchain upgrade (AGP/Gradle/Kotlin/Compose).
- Dependabot batch: #404 tar, #406 password-hash, #407 thiserror 2.0, #410 commander, #411/#416 vite 8, #415 npm-minor-patch group, #418 jsonwebtoken/argon2, #419 opentelemetry — `dep-update-noise`, not scored.

## Watch
- **Open PR #435** (security P0/P1 batch) explicitly *defers* several large items to follow-up PRs: S3 presigned-URL SigV4 (P0-05), 4 golden-path mock routes in `ppt-web/App.tsx` (P0-10), WS token-in-URL → subprotocol auth (P0-11), refresh-tokens → HttpOnly cookies (P0-12), community-routes IDOR cluster, webhook SSRF, audit-log forgery. These are candidate vectors but live in an unmerged PR — not promoted from PR text alone this run.
- Churn hotspots (2nd consecutive run, `runs_seen=2`): `routes/integrations.rs` (4,327 lines this run), `routes/organizations.rs` (4,018), `routes/documents.rs` (3,553). Next tier: `ai.rs` (3,134), `platform_admin.rs` (2,762), `announcements.rs` (2,722).
- No reverts and no stalled reviews this run (only open PR #435 was opened today).

## Code review slice
- Segment reviewed: api-core (reason: churn-aligned — the top-3 churn files live under `backend/servers/api-server/src/routes/`)
- Experts: rust
- Findings: 1 (see Backlog deltas — `code-review-api-core-voice-device-idor`)
- Next segment: api-handlers (oldest-unreviewed / fallback; all other segments still null)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 99 (ppt/ + reality/; mobile/ not yet seeded → 0)
- Drift signals this run: 0 — route files for both ppt-web and reality-web changed in the window, but `docs/screens/ppt/` and `docs/screens/reality/` were updated alongside them (the window also includes a full screen-map regen). No per-product drift.
- Orphan screens this run: 0 (no route-basename slug went missing this run)
- Last `screen-map.yml` CI run on `main`: not checked (no `gh run` access in this cloud env; verify via Actions UI)

## Backlog deltas
- **New:** [3] IDOR: `unlink_voice_device` deactivates any device by ID with no owner/org scoping — `security`, confidence high (rotating review). Promoted.
- **Bumped:** [1→2] integrations.rs churn-hot — repeated-churn (runs_seen 2)
- **Bumped:** [1→2] organizations.rs churn-hot — repeated-churn (runs_seen 2)
- **Bumped:** [1→2] documents.rs churn-hot — repeated-churn (runs_seen 2)
- **Resolution check:** `dx-integration-marketplace-stubs` — integrations.rs OAuth TODOs gone, but both `IntegrationMarketplacePage.tsx` TODOs remain → stays open (partial note added). `dx-portfolio-dashboard-stubs` — all 3 TODOs still present, unchanged.
- **Decayed:** none (all open items ≤3 days old)
- **Dropped:** none

## Plans promoted
- `plans/security-voice-device-idor.md` — scope the voice-device unlink mutation to the caller (add owner/tenant predicate to the `UPDATE`, stop ignoring `_principal`) · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + the Phase 5 allowlist. The one promotable high-confidence finding (voice-device IDOR) is a security *code* fix, which by policy goes to the manual-plan path, not Phase 5.

## Open questions
- PR #435's deferred backlog (S3 SigV4, mock-route rewiring, community IDOR cluster, audit-log forgery) should be triaged into vectors once #435 merges and the deferred items are confirmed still-open against `dev`. Holding until then to avoid promoting from unmerged PR text.

## Quiet day?
- No
