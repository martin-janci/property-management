# 2026-05-23

> **Branch note.** The routine prompt commits to `main`, but the cloud harness placed this session on `claude/jolly-darwin-E2JIG` with explicit "never push to a different branch without permission". This commit lands on the feature branch; merging into `main` is left to the human so the next routine run picks up the cursor (`last_pr_seen=433`). Same conflict as the 2026-05-20 run — see *Open questions*.
>
> **History note.** `origin/main` was force-updated since the last run (the prior cursor commit `fc8af0a` is no longer an ancestor). `git log --since` churn was polluted by rewritten commit dates, so per-file churn was recomputed from merged-PR file diffs — immune to history rewrites.

## Since last run
- Merged PRs: 66 (range #344–#432 on `main`) + 1 on `dev` (#433)
- Open PRs touched: 0
- New / updated issues: 1 (#375, closed)
- Commits: 31 on `main`
- Phases that failed: none

## Shipped
- #420 chore(api-server): remove dead voting & faults handler modules (−2304 lines of dead code)
- #421 fix(api-server): route market_pricing, notification_preferences & reports DB lookups through RLS — **clears the last 12 live `TODO: Migrate to *_rls` markers** (grep now returns 0; `security-rls-migration-residual` is genuinely done)
- #431 fix(caddy): `register_route` truly idempotent (DELETE-loop-then-POST replaces PUT-with-POST-fallback that silently accumulated duplicate routes across redeploys) + observability + `MAX_DELETE_ITERS`/`SWEEP_DEADLINE` bounds — 7 tests, two dev-team review rounds
- #433 fix(caddy): post-merge hardening — resolved the three deferred P3 items from #431's 20-pass review (merged to `dev`)
- #422 fix(ci): run RLS penetration tests as non-superuser so RLS is actually enforced (resolves issue #375); #369 ran them serially; #427 frees disk space before heavy cargo builds
- #385 chore(mobile-native): toolchain upgrade — AGP 9.2.1 / Gradle 9.4.1 / Kotlin 2.3 / Compose 2026.05; #395 / #379 carried the async-storage v3 + Kotlin 2.3 DSL adaptations
- #364 feat(research): Phase 5 — auto-fix for high-certainty signals; #365 the 2026-05-20 brief
- ~40 dependabot / grouped dependency bumps incl. major jumps: jsonwebtoken 9→10, argon2 0.5→0.6, opentelemetry 0.21→0.32, thiserror 1→2, governor 0.6→0.10, vite 7→8, jsdom 24→29, commander 12→14 (#418, #419, #407, #355, #416, #413, #410, plus the npm/cargo/gradle groups); #398 introduced minor+patch grouping in dependabot config

## Watch
- Stalled review: none — zero open PRs.
- Reverted: none in window.
- Churn hotspots (living code, from PR diffs):
  - `backend/servers/deploy-server/src/infra/caddy.rs` — 656 lines this run (runs_seen=1; healthy feature churn, well-tested, not instability)
  - Prior backend hotspots (`integrations.rs`, `organizations.rs`, `documents.rs`) were **not** touched this window → no repeated-churn.
- **Major-version dependency wave** — auth/crypto/observability bumps (jsonwebtoken 9→10, argon2 0.5→0.6, opentelemetry 0.21→0.32) landed green but are a regression-watch surface for the next run.
- **Caddy operational follow-up** — #431 deferred a one-time manual sweep of pre-`@id` untagged stale routes on staging/prod Caddy (its DELETE-by-id sweep only removes `ppt-deploy-*`-tagged routes). Ops runbook step, not code; flagged so it isn't forgotten on first rollout.

## Screen-map status
- Total `docs/screens/` files: 99 (53 `ppt/` + 46 `reality/`; `mobile/` not yet seeded → 0).
- Drift signals this run: 0 — no PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`.
- Orphan screens this run: 0 — no route files touched, nothing to compare.
- Last `screen-map.yml` CI run on `main`: completed/success at 2026-05-23T09:20:06Z.

## Backlog deltas
- **New:** [1] `refactor-caddy-rs-hot` — top living-code churn this run (656 lines, #431+#433); informational watch, not a refactor ask (file is well-tested and freshly hardened).
- **Bumped:** none — no window PR touched the open `dx-*` stub items or prior hotspot files, so no new scored evidence.
- **Decayed:** none — no open item has `updated_at` older than 14 days (oldest is 2026-05-20).
- **Dropped:** none.
- **Confirmed done:** `security-rls-migration-residual` — #420/#421 cleared all 31 residual TODOs; verified `grep "Migrate to .*_rls"` now returns 0.

## Plans promoted
- (none) — top backlog score is 2; promotion gate is ≥ 3. This was a maintenance/deps-heavy window: the substantive code work (RLS routing, caddy idempotency) shipped fully tested and resolved existing backlog scope rather than opening new promotable vectors.

## Open questions
- **Branch policy conflict (recurring).** The harness keeps placing routine sessions on `claude/jolly-darwin-*` branches and forbids direct pushes to `main`, while the routine cron expects `state.json.last_pr_seen` to live on `main`. **Action needed:** either merge these `.research/`-only commits into `main` after each run, or rewire the harness so the routine can commit to `main` directly. (G8 proves the diff is `.research/`-only.)
- **`main` force-pushed.** The prior cursor commit `fc8af0a` is no longer reachable from `origin/main`. If this is the version-bump workflow rebasing, fine; if not, worth confirming nothing was lost. Cursor now keys off PR number (#433) and the current tip (`f45e2b4`), which are stable regardless.
- **Major-version dep bumps unverified at runtime.** jsonwebtoken/argon2/opentelemetry majors landed green in CI but the routine didn't exercise auth/observability paths. No action unless the next run surfaces a revert or bug tied to them.

## Quiet day?
No — but a quieter, maintenance-shaped window: 66 merged PRs dominated by a ~40-PR dependency wave + version bumps, with only two substantive code threads (RLS completion, caddy idempotency), both of which shipped fully tested and closed existing scope. One new informational backlog item, zero plans promoted, zero process alarms (no reverts, no stalled reviews, no screen-map drift, no untriaged issues, no hotfix-no-test).

## Goal-check summary
All 14 goal checks passed. Detail in `signals/2026-05-23.json` → `goal_checks`.

- G1 ✔ brief exists with `# 2026-05-23` header
- G2 ✔ cursors advanced (PR 343→433, commit fc8af0a→f45e2b4, seen_signals 7→9, hotspots 3→4, runs 1→2)
- G3 ✔ 7 unique backlog ids
- G4 ✔ both new signal ids absent from prior `seen_signals`
- G5 ✔ max score = 2; no item ≥14 days stale, so no decay due
- G6 ✔ vacuously true — 0 plans promoted
- G7 ✔ 0 new plans (≤ 2)
- G8 ✔ all staged paths under `.research/`
- G9 ✔ no secrets / private hostnames in staged diff
- G10 ✔ `backlog.md` regenerates byte-identical from `backlog.json`
- G11 ✔ no phase failures; all cursors advanced
- G12 ✔ no prior-run plans to converge
- G13 ✔ `_archive/` count 0 ≥ 0
- G14 ✔ `IDEAS_TRIAGE.md` regenerates byte-identical from `backlog.json`
