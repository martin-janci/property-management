# 2026-05-23

First routine run on this branch — `.research/state.json` was a fresh scaffold (null cursors), so this was an initial sweep over recent GitHub activity (last ~14 days). The branch checkout is at `fc8af0a` (v0.2.409), behind `main`'s tip `f45e2b48` (v0.2.433); all activity below was read from GitHub via the GitHub MCP, not the local tree. Because the merged-PR backlog is large (39 merged PRs in window), signal derivation focused on the highest-signal recent PRs rather than exhaustively fetching every PR's file list — noted where this limits coverage.

## Since last run
- Merged PRs: 39 (range #354–#434; #433 highest meaningfully merged, #435 still open)
- Open PRs touched: 1 (#435)
- New / updated issues: 3 (all closed, all labeled — no untriaged)
- Commits: 100+ on `main` (page cap hit; window 05-09..05-23, mostly version bumps + dependabot)
- Phases that failed: none

## Shipped
- #421 `fix(api-server): route market_pricing, notification_preferences & reports DB lookups through RLS` — migrated 13 endpoints to `*_rls` and added a cross-tenant IDOR guard in 4 report handlers. **No tests in the diff** (see backlog).
- #431 `fix(caddy): register_route truly idempotent + observability + drift signal` — DELETE-loop-then-POST rewrite + `purge_legacy_untagged_routes` startup reconciliation; 11 inline tests.
- #433 `fix(caddy): post-merge hardening` — UTF-8-safe body truncation, body-in-Err, error-variant pins; same file as #431.
- #422 `fix(ci): run RLS penetration tests as non-superuser (#375)` and #420 `chore: remove dead voting & faults handler modules` — addressed parts of the RLS/security cleanup.
- Large security batch landed 05-19/05-20 (X-Tenant-Context auth bypass, /workflows IDOR, reality-server hardening) — most shipped with companion `test(...)` commits.

## Watch
- Stalled review: none detected (no open PR >7 days without a decision; only #435 open, recently active).
- Reverted: none (no revert-titled PRs in window).
- Churn hotspots: `backend/servers/deploy-server/src/infra/caddy.rs` (~654 lines this run across #431 + #433, runs_seen=1) — actively maintained and well-tested, watching for repeated churn.

## Screen-map status
- Total `docs/screens/` files: 104 (across `ppt/` + `reality/`; `mobile/` not yet seeded → 0)
- Drift signals this run: 0 (none of the sampled merged PRs touched `ppt-web`/`reality-web` route files; detection was sampled over high-signal PRs, not all 39 — best-effort)
- Orphan screens this run: 0 (no route-basename scan triggered — no route files in the sampled PRs)
- Last `screen-map.yml` CI run on `main`: not checked (no `gh`/Actions API access this run)

## Backlog deltas
- **New:** [4] Add RLS/IDOR regression tests for endpoints migrated in PR #421 — see backlog.md
- **New:** [3] Verify caddy register_route idempotency on staging + watch infra/caddy.rs churn — see backlog.md
- **New:** [2] Verify async-storage v3 migration follow-up (PR #395 unchecked box) — see backlog.md
- **Bumped:** none (first run)
- **Decayed:** none (first run)
- **Dropped:** none

## Plans promoted
- `plans/rls-endpoint-regression-tests.md` — endpoint-level RLS/cross-tenant-IDOR regression tests for the three modules PR #421 migrated without coverage, anchored on the existing `workflow_cross_tenant_idor_tests.rs` pattern · adversarial pass: passed

## Open questions
- The score-3 caddy item is intentionally **not** promoted: its open work is a manual staging smoke (open→close→re-open, verify one Caddy route per host) and a dashboard observation — neither is an automatable failing-on-main regression test, and the logic is already covered by passing unit tests. Promote only if a human wants the staging smoke scripted against a real Caddy admin.
- Issue #375 suggests making `security-tests` a required check that runs on all backend PRs (not just title-matched). That is CI-policy work, kept out of the promoted test-gap plan; flag if it should become its own vector.

## Quiet day?
- No.
