# 2026-05-23

> Second routine run today (incremental, ~16:04Z), ~1h after the morning deep-catch-up run (14:56Z). The morning run consumed merged PRs #344–#433 and promoted the voice-device IDOR plan; this run found no new repo activity since 14:56Z and adds one code-review finding (api-handlers segment). Sections below carry the day's cumulative picture; the deltas this run are the Code review slice and the one new backlog vector.

## Since last run
- Merged PRs: 0 since 14:56Z (morning run covered #344–#433; #435 still open)
- Open PRs touched: 0 since 14:56Z (#435 last updated 14:29Z, before the cursor)
- New / updated issues: 0 since 14:56Z
- Commits: 5 on `dev` since 14:56Z — all CI/version-bump/research chore commits (no application churn)
- Routine lag: 1h since last run (on schedule)
- Phases that failed: none

## Shipped
- (Daily record; shipped earlier today and consumed by the morning run)
- #420/#421 — RLS migration completed (dead voting/faults modules removed; market_pricing, notification_preferences, reports routed through RLS). Resolves backlog `security-rls-migration-residual` (`done`).
- #422 — RLS penetration tests now run as a non-superuser so RLS is actually enforced (resolves issue #375).
- #423 — `clap` "env" feature enabled so `ppt-seed` compiles / `cargo check --workspace` is green.
- #427 — CI frees disk before heavy cargo build/test.
- #431/#433 — caddy/deploy-server `register_route` idempotency + post-merge hardening; `dev` adopted as default working branch.
- Nothing new shipped in this run's 1h window.

## Watch
- **Open PR #435** (security P0/P1 batch) defers several large items to follow-ups: S3 presigned-URL SigV4, mock golden-path routes in `ppt-web/App.tsx`, WS token-in-URL → subprotocol auth, refresh-tokens → HttpOnly cookies, community-routes IDOR cluster, webhook SSRF, audit-log forgery. Candidate vectors, but live in an unmerged PR — not promoted from PR text alone. Opened today, not stalled.
- Churn hotspots (`runs_seen=2`, no new churn this run): `routes/integrations.rs`, `routes/organizations.rs`, `routes/documents.rs`.
- No reverts and no stalled reviews this run.

## Code review slice
- Segment reviewed: api-handlers (reason: oldest-unreviewed — churn-aligned api-core was already reviewed in the morning run; api-handlers `handlers/` is adjacent to the churning `routes/` and was still null)
- Experts: rust + security
- Findings: 1 (see Backlog deltas — `code-review-api-handlers-dead-dup-auth-bldg-handlers`)
- Next segment: reality-server (oldest-unreviewed after this run; ppt-web-*, reality-web, mobile-* also still null)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 99 (ppt/ 53 + reality/ 46; mobile/ not yet seeded → 0)
- Drift signals this run: 0 (no route files changed since 14:56Z)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: success (2026-05-23T09:20Z)

## Backlog deltas
- **New:** [2] Dead/duplicate handler modules: `AuthHandler` & `BuildingHandler` unused, routes reimplement inline — `refactor`, confidence medium (rotating review, api-handlers). Not promotable (score 2, refactor).
- **Bumped:** none
- **Decayed:** none (all open items ≤3 days old)
- **Dropped:** none
- **Resolution check:** no new merged PRs to match this run; open items unchanged.

## Plans promoted
- None this run. (The morning run's `plans/security-voice-device-idor.md` remains; the only finding this run is a medium-confidence refactor below the promotion bar.)

## Auto-fix
- `no candidate` — the only signal this run is a medium-confidence refactor (dead/duplicate handler modules), which does not meet the Phase 5 certainty bar (confidence=high + score≥3 + allowlist match).

## Open questions
- Dead `AuthHandler`/`BuildingHandler` modules: confirm they are truly superseded by the inline `routes/` implementations before deleting (a low-risk cleanup, but worth a human glance that no out-of-tree caller depends on them).
- PR #435's deferred backlog should be triaged into vectors once #435 merges and the deferred items are confirmed still-open against `dev`.

## Quiet day?
- No — repo activity was quiet (no new PRs/issues/churn), but the rotating review surfaced one new vector.
