# 2026-05-23

> **Branch note.** The routine prompt commits to `main`, but the cloud harness placed this session on `claude/jolly-darwin-Wyhl3` with explicit "never push to a different branch without permission". This commit lands on the feature branch; merging into `main` is left to the human so the next routine run picks up the advanced cursor. Same standing item as the 2026-05-20 run — see *Open questions*.

## Since last run
- Merged PRs: 64 (range #344–#430)
- Open PRs touched: 0
- New / updated issues: 1 (#375, closed)
- Commits: 68 on `main`
- Phases that failed: none

## Shipped
- **#420** `chore(api-server): remove dead voting & faults handler modules` — deleted `handlers/voting/mod.rs` + `handlers/faults/mod.rs` (−2,304 lines), the 19 dead-code half of the RLS-residual finding.
- **#421** `fix(api-server): route market_pricing, notification_preferences & reports DB lookups through RLS` — RLS-migrated the 12 live markers and closed two genuine cross-tenant IDOR surfaces (market_pricing `find_by_id`, `reports::get_building_name`). **Shipped with no tests** → drove this run's promoted plan.
- **#422** `fix(ci): run RLS penetration tests as non-superuser so RLS is enforced (#375)` — closed issue #375 (RLS pen tests were passing only because the superuser bypassed RLS).
- **#385** `chore(mobile-native): toolchain upgrade — AGP 9.2.1 / Gradle 9.4.1 / Kotlin 2.3 / Compose 2026.05`; **#395** async-storage v3 API adaptation; **#377** dependabot-queue unblock.
- **#416** vite 7→8 (Rolldown), **#418** jsonwebtoken 9→10 + password-hash/argon2, **#419** opentelemetry 0.21→0.32 — major-dep bumps.
- ~40 dependabot bumps merged (actions/*, cargo, npm, gradle/compose/ktor groups) + version-bump CI commits.

## Watch
- Stalled review: none — zero open PRs against `main` this run.
- Reverted: none in window.
- Closed-unmerged PRs: 22, all benign — dependabot version-bump supersessions (grouped/re-issued) and three author PRs (#376/#378/#379) superseded by merged equivalents (#395/#385/#377). Not scored.
- Churn hotspots this run (live code, deletions & `.research/` excluded):
  - `backend/servers/api-server/src/routes/ai.rs` — 420 lines (AI auth / JWT enforcement; paired with `tests/ai_auth_tests.rs` +219)
  - `backend/servers/reality-server/src/util/url_validator.rs` — 302 lines (SSRF hardening)
  - `backend/servers/api-server/src/routes/notification_preferences.rs` — 143 lines (RLS migration #421)
  - Top *raw* churn was the two deleted modules (`handlers/voting/mod.rs` 1,155, `handlers/faults/mod.rs` 1,147) — excluded: a deleted file can't be a refactor hotspot.
  - Last run's big hotspots (integrations/organizations/documents) saw no churn this run; `runs_seen` stays 1.
- Residual RLS marker outside the closed finding's scope: `documents.rs:1630` carries a separate `Migrate restore_version to RLS pattern` note; `documents.rs:2480/2605` are intentionally-public endpoints (no RLS context). Tracked here, not yet a vector.

## Screen-map status
- Total `docs/screens/` files: 99 (53 `ppt/` + 46 `reality/`; `mobile/` not yet seeded → 0).
- Drift signals this run: 0 — no merged PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**` (verified via diff `fc8af0a..HEAD`).
- Orphan screens this run: 0 — no route files touched, so no basenames to diff against (heuristic, see routine note).
- Last `screen-map.yml` CI run on `main`: completed/success @2026-05-22T20:12:51Z.

## Backlog deltas
- **New:** [4] `test-gap-rls-idor-regression-421` — #421 closed two cross-tenant IDOR surfaces with zero tests (hotfix-no-test +2, risky-churn +2). **Promoted to a plan.**
- **New:** [1] `refactor-ai-rs-hot` — informational churn-hotspot watch (AI auth work).
- **New:** [1] `refactor-url-validator-rs-hot` — informational churn-hotspot watch (SSRF hardening).
- **New:** [1] `refactor-notification-preferences-rs-hot` — informational churn-hotspot watch (RLS migration #421).
- **Done:** `security-rls-migration-residual` — #420 (removed 19 dead markers) + #421 (RLS-migrated 12 live markers) resolved all 31 markers across the original 5 files. Verified: `grep 'Migrate to .*_rls'` now returns 0; voting/faults handler dirs gone.
- **Updated:** `dx-integration-marketplace-stubs` — backend half resolved (integrations.rs now 0 TODOs; OAuth-URL generation wired in a PR ≤#343). Narrowed to the two remaining frontend stubs at `IntegrationMarketplacePage.tsx:234/238`. Score unchanged (2).
- **Bumped:** none.
- **Decayed:** none — all open items are ≤3 days old (14-day decay clock not reached).
- **Dropped:** none.

## Plans promoted
- `plans/test-gap-rls-idor-regression-421.md` — add cross-tenant IDOR regression tests for the market_pricing / reports / notification_preferences handlers RLS-migrated in #421, mirroring `workflow_cross_tenant_idor_tests.rs`. · adversarial pass: passed (source→hypothesis→approach→test-plan chain intact; the test-plan includes a revert-discriminator bullet so it provably guards #421 rather than just exercising the happy path).

## Open questions
- **Branch policy conflict (standing).** Harness forbids pushing to `main`; routine cron expects the next run to read the advanced cursor (`last_pr_seen=430`, `last_commit_sha=b9d7188…`) on `main`. **Action:** merge `claude/jolly-darwin-Wyhl3` → `main` (diff is `.research/`-only, G8-proven), or rewire the harness so the routine commits to `main` directly.
- **Routine-prompt drift.** #364 merged a "Phase 5 — auto-fix" extension into `.research/routine-prompt.md` (G15, Phase 6). The instruction set this session was invoked with is the pre-#364 4-phase / G1–G14 version, so this run did **not** run auto-fix. If Phase 5 is meant to be live, the harness needs to invoke the routine with the updated prompt; if it's still draft, no action. Flagging the mismatch, not acting on it.
- **`documents.rs` RLS residual.** Worth confirming whether `restore_version` (documents.rs:1630) is a deliberate deferral or an overlooked marker before opening a follow-up vector.

## Quiet day?
No — 64 merged PRs, one finding closed (`security-rls-migration-residual` via #420+#421), one plan promoted (the IDOR test-gap it exposed), four new vectors. Dependabot-heavy window but with two substantive security closures underneath.

## Goal-check summary
All goal checks run after staging; detail in `signals/2026-05-23.json` → `goal_checks`.
