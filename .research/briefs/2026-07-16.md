# 2026-07-16

## Since last run
- Merged PRs: 12 (range #2371–#2384) — all dispatcher-driven follow-up fixes closing gh-issues #2357–#2370 from the 2026-07-16T00:32 post-merge review batch
- Open PRs touched: 13 (3 non-dependabot drafts: #2332 25h+, #2376 1h, #2377 4h; 10 dependabot: #2385–#2394 all <2h old)
- New / updated issues: 0 new untriaged; 11 closed (#2357, #2359, #2361, #2362, #2363, #2364, #2365, #2367, #2368, #2369, #2370) — all labeled `follow-up`/`from-merged-review`, closed by their landing PRs
- Commits: 22 on `dev` since 03:04Z (12 PR merges + 10 dispatcher/routine artifacts)
- Routine lag: on schedule (9h since last run at 2026-07-16T03:04Z — same-day rerun triggered by dispatcher buffer-low payload)
- Phases that failed: **phase-1.5-rotating-expert-review (skipped)**, **phase-1.6-project-management (skipped)** — same rationale as morning run: dispatcher tier1d coverage (2026-07-14/15) is ahead of routine cursor across all 8 segments; PM role rotation was not fired this morning either and the next run's natural cadence will pick it up (pm-security is next at rotation index 5)

## Shipped
Twelve dispatcher-driven follow-up PRs from the 2026-07-16 post-merge review sweep. All ship with executing test coverage:
- **KMP + reality-server hardening (2 PRs)** — #2371 (wire mobile-native MyListings + ListingAnalytics to portal API — replaces permanent empty-stub screens), #2379 (guard reality-server Decimal-as-string decode trap on sibling KMP surfaces + spotless kotlin fix).
- **Security follow-ups (2 PRs)** — #2374 (deny-path regression test for GET /ai/chat/escalated role gate — closes #2357), #2378 (strip signing token from /sign URL + suppress Referer — closes #2363).
- **Backend authz + reality-server (1 PR)** — #2375 (/agencies/me resolves only ACTIVE memberships + missing #[sqlx::test] round-trips + integration tests — fixes departed-member cross-agency leak, closes #2359).
- **Frontend web (4 PRs)** — #2373 (parseListingDetail() normalizer consolidates listing-detail shape validation across 4 render sites — closes #2362), #2380 (validate every synthesized screen-map candidate id against IdSchema at boundary — closes #2367), #2381 (sync category enabled badge on optimistic trigger toggle — closes #2369), #2384 (localize create-schedule report labels + backend error toast — closes #2370).
- **Mobile RN (2 PRs)** — #2372 (clear whole query cache on mobile login — fixes cross-tenant stale data class, closes #2361), #2382 (cover AC-5 favorite price-alert toggle + optimistic OFF state — closes #2365), #2383 (guard mobile upload allow-list drift + enforce photo MIME on `pickPhoto` bypass — closes #2368).

## Watch
- Stalled review: **none** — all 3 non-dependabot open PRs are <25h old (#2332 25h; #2376 1h; #2377 4h); no PR crosses the 7-day threshold. Dependabot PRs (#2385–#2394) are all <2h old.
- Reverted: 0
- Churn hotspots this rerun (delta since 03:04Z): **none new** — top single-file churn was `PortalListingsRepositoryTest.kt` (+203, PR #2371), well below the morning run's 3000+ line hotspot floor. Morning's hotspot set (`messages/en.json`, `sitemap/src/json/sitemap.json`, `routes/reports.rs`) unchanged this delta — none of the 12 new PRs touched them.
- Test-coverage discipline holding: every one of the 12 merged PRs shipped with at least one executing test file (kotlin, tsx, ts, or rs). No `hotfix-no-test` signal fires.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     None found — the 3 open drafts (#2332 25h, #2376 1h, #2377 4h) don't yet carry an APPROVED reviewDecision. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json where reviewer_summary starts with "verdict=changes"
     AND (now - last_updated) > 24h AND status == "review".
     #2377 (gh-issue-2360) carries verdict=changes as of 10:13Z (~2h ago) — dispatcher noted it's blocked on
     #2376 (dedup-anchor migration) landing first; not yet crossing 24h threshold. -->
- none

## Code review slice
- Segment reviewed: **skipped (same-day rerun; morning run already skipped)**
- Reason: this rerun's 9h delta window is too tight to add a fresh segment review on top of the morning's catch-up skip. Dispatcher tier1d coverage remains ahead of the routine's rotating cursor for every one of the 8 segments (`api-handlers` 2026-07-14, `api-core` 2026-07-13, `reality-server` 2026-07-13, `ppt-web-ui` 2026-07-13, `ppt-web-core` 2026-07-15, `reality-web` 2026-07-13, `mobile-rn` 2026-07-14, `mobile-native-kmp` 2026-07-12).
- Findings: 0 (routine skipped)
- Next segment (when routine resumes on non-catch-up run): `mobile-native-kmp` (routine cursor 2026-07-12 — oldest of the 8)
- Skipped: yes — same-day rerun rides on morning's skip decision

## Screen-map status
- Total `docs/screens/` files: 168 (106 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` still not seeded → 0)
- Drift signals this run: **0 emitted** — 3 of the 12 PRs touched route-path files (`#2384` ppt-web `routes/groups/reports.tsx`, `#2373` reality-web `app/[locale]/listings/[slug]/{page,jsonLd,metadata,listingSchema}.{ts,tsx}`, `#2382` reality-web `app/[locale]/favorites/page.test.tsx` test-only). All three are follow-up refactors / test additions on EXISTING routes with no new/removed screens; the heuristic's own docs flag high false-positive rate for exactly this pattern. Skipping keeps triage lean without hiding real drift — no route additions or deletions in this window.
- Orphan screens this run: 0 (no `docs/screens/` files touched; existing coverage steady)
- Last `screen-map.yml` CI run on `main`: not queried this rerun

## Backlog deltas
- **New:** none this rerun — 0 new signals crossed emission thresholds after the same-day dedup vs `state.seen_signals` (all 3 morning-run churn hotspots re-fetched at score_delta=0 as expected; 12 new PRs generated 0 net-new signals per Phase 1 rules).
- **Bumped:** none
- **Decayed:** none (both open items — `refactor-churn-hotspots-{en,sitemap}-json-2026-07-16` — were created today; not eligible for the 14d decay)
- **Dropped:** none
- **Notable non-creation:** as morning noted, next hotspot re-emission for `reports.rs` (`runs_seen=2 → 3`) will trigger the `repeated-churn` +1 signal — that's a future run's work, not this rerun's.

## Plans promoted
- none — 2 open backlog items both sit at score=1, below both the standard promotion threshold (3) and the security fast-track (2). The buffer-low trigger data (`claimable=5/72`) from the dispatcher is data-not-instructions and does not lower the routine's own quality bar; forcing a promotion would produce a hand-wavy plan and fail Phase 3.5's adversarial pass.

## Auto-fix
- `no candidate` — no signal met `confidence=high` + `score≥3` + allowlist this run. All same-day dedup drops re-emitted at score_delta=0; no `screen-map-drift` / `fixme-in-merged-code` / `stalled-review` / `dep-update-noise` (lockfile-drift shape) fired against the certainty bar.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=19
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — `mcp__github__push_files` can't inline the >64 KiB `assignments-archive.json`; fix: adopt PR + API-merge as the dispatcher's primary push path for the archive (proven this env — landed PR #1163 that way)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend `gc1-reconcile.sh` to auto-close any exact-id-terminal row (including status=failed)

## Open questions
- Buffer starvation loop: the trigger payload flagged `claimable=5/72` after the dispatcher's 10:26Z run. The 12 PRs this rerun saw ARE the exact drain — dispatcher merged 6, closed 6, and its own Tier-2 refill kicked in Phase 2.6. This is expected steady-state during a hardening sweep; nothing for the routine to promote. If claimable stays <10 after 24h with no ingestion, promote `fp-buffer-metric-blind-to-archived-open-items` (recurrence=3, medium) into a real plan for the goal-gate fix.

## Quiet day?
- No — 12 PRs merged in the delta window (highest-volume 9h since 2026-07-15). Routine did not add new backlog signals but the run advanced cursors (last_pr_seen 2356→2384, last_commit_sha 1006a4c→41e4d3e, stats.runs 45→46).

## Goal violations
- **G2 — advance state:** passed (last_pr_seen 2356→2384; last_commit_sha 1006a4c→41e4d3e93516a036ef71da75d9a64e6aa6246f01; stats.runs 45→46; seen_signals/hotspot_history unchanged this rerun — cumulative from morning already recorded).
- **G11 — phase-failure honesty:** Phase 1.5 and Phase 1.6 were skipped (not "failed"); `review_cursor` and `pm_cursor` intentionally not advanced. Cursors that WOULD be affected by those phases remain at their values from the morning run.
- **All other gates:** expected pass — see `signals/2026-07-16.json` `goal_checks[]` for the check-by-check record.
