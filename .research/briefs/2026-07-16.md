# 2026-07-16

## Update — 20:12Z incremental rerun (buffer-low trigger)
Triggered on `buffer-low: claimable=0/72 — refill planner`. The morning run (03:04Z) captured the 03:00Z-cutoff snapshot; this rerun folds in the 13 dispatcher-shipped follow-ups that landed 04–12Z (PRs #2371–#2384 — all `gh-issue-*` fixes closing #2357, #2358, #2359, #2361, #2362, #2363, #2364, #2365, #2367, #2368, #2369, #2370 from PR #2353/#2337/#2345/#2346/#2338/#2344/#2347/#2348/#2350/#2351/#2352 post-merge review). No new signals emit — every new PR is a `gh-issue-*` fix (already-tracked issue closure, not net-new work). The **buffer-refill gap is dispatcher-side**: 3 open post-merge review issues (#2318 RLS worker no-op — HIGH, #2320 direct-to-S3 file_key IDOR + size cap bypass — HIGH, #2366 direct-to-S3 dropped `building_id` — MEDIUM) are not in the routine's backlog nor in `action-list.json`. The routine has no signal type for "post-merge follow-up issue with `follow-up`/`from-merged-review` labels" — they carry labels so `untriaged-issue` doesn't fire, and no other type maps. This is a signal-grammar gap; the immediate mitigation is that the dispatcher's `issue-ingest.sh` should be re-run to pick up #2318/#2320/#2366 as `gh-issue-<N>` action-list rows (mirroring the existing `gh-issue-2360` row). Cursors advanced: `last_pr_seen` 2356→2384, `last_commit_sha` 1006a4c→151e0820, `stats.runs` 45→46.

## Since last run
- Merged PRs: **63** (50 in morning slice #2273–#2356; +13 in 20:12Z rerun #2371–#2384)
- Open PRs touched: 4 in morning (#2332, #2339, #2340, #2371); #2371 has since merged
- New / updated issues: 46 (all labeled `follow-up`/`from-merged-review` or security/frontend/backend — none untriaged). 3 of these (#2318/#2320/#2366) remain open and unbacklogged — see *Update* above
- Commits: **65** on `dev` (49 in morning slice + 16 more between 03:04Z and 18:49Z — a mix of application PRs merging and dispatcher `chore(research)` commits)
- Routine lag: **3d 0h since previous run** (2026-07-13→2026-07-16 03:04Z); this 20:12Z rerun is a same-day incremental, not a scheduled invocation
- Phases that failed: **phase-1.5-rotating-expert-review (skipped)**, **phase-1.6-project-management (skipped)** — both intentionally deferred in catch-up mode (see rationale below). Rerun does not re-invoke either — dispatcher tier1d coverage is still ahead of the routine cursor across all 8 segments.

## Shipped
Large volume — 50 merged PRs across a 72h window. Highlights (grouped):
- **Cross-tenant IDOR / security hardening (7 PRs)** — #2289 (AI chat sessions per-user scope), #2316 (news_articles cross-tenant fix), #2335 (rag/migrate FORCE-RLS + assumed-provenance), #2356 (AI escalated role-gate + owner-scope feedback), plus follow-ups #2336/#2352/#2354.
- **Portal webhook hardening (5 PRs)** — #2278 (inbound inquiry 500-on-failure), #2286 (freshness/timestamp replay window), #2294 (webhook status manager UI), #2353 (retry-on-unmatched syndication), #2354 (per-portal replay protection + dedup).
- **Direct-to-S3 document upload flow (4 PRs)** — #2309 (backend presigned PUT), #2345 (ppt-web wire), #2346 (mobile fail-fast auth + MIME guard), #2347 (signer-facing document-sign page).
- **Report scheduling / cron (5 PRs)** — #2292/#2295/#2313/#2338, plus follow-up #2340 (open — RLS context for scheduler worker).
- **Push / KMP price-tracking (7 PRs)** — #2290 (FCM+APNs adapter), #2310 (honest metrics + routing guard), #2288 (KMP price-tracking surface), #2331/#2350 (Decimal-as-string wire mismatch), #2348 (price-alert Decimal + AC-5 toggle), #2349 (quiet benign push skips).
- **Mobile RN quality (5 PRs)** — #2304 (dashboard error surfaces), #2305 (JWT decode + tenant-id tests), #2308 (DocumentUploadScreen tests), #2334 (QueryErrorBanner), #2352 (JWT tenant-decode consolidation).
- **Reality-web listing / agency (4 PRs)** — #2280 (AgencyDashboard error branch), #2281 (SSR partial-body crash), #2351 (listing detail body validation), #2355 (agency /me endpoint).
- **RAG / pgvector (2 PRs)** — #2291 (wire migrate route), #2312 (provenance-stamped back-fill), #2335 (cross-org RLS + CI gaps).
- **Screen-map / tracking upkeep (5 PRs)** — #2285/#2297/#2298/#2306/#2311/#2344/#2307/#2333.
- **Backend infra (2 PRs)** — #2274 (auth helper), #2296 (yanked spin bump), #2273 (RAG provenance sqlx::test).

## Watch
- Stalled review: **none** — all 4 open PRs are <25h old (#2371 is 1h; #2332/#2339/#2340 are 18–25h). No PR crosses the 7-day threshold.
- Reverted: 0
- Churn hotspots this run (raw, after exclusions):
  - `frontend/apps/ppt-web/messages/en.json` — **+3926 lines** (first hotspot appearance; i18n content churn from many landed frontend PRs — not code instability)
  - `frontend/packages/sitemap/src/json/sitemap.json` — **+3727 lines** (first hotspot appearance; sitemap data updates from route additions)
  - `backend/servers/api-server/src/routes/reports.rs` — **+3333 lines** (runs_seen: 1 → 2 after this run; still one below the `repeated-churn` threshold — repeated-churn will fire next time)
- Runner-ups in code churn (informational): `booking/mod.rs` (+3185), `auth.rs` (+2950), `oauth_integration_tests.rs` (+2719), `financial.rs` (+2411), `buildings.rs` (+2347), `lease.rs` (+2303), `integrations/{sync,install,webhook}.rs` (+2293/+2283/+2250), `llm_document.rs` (+2225), `push_fanout.rs` (+2198), `vote.rs` (+2198), `api_ecosystem.rs` (+2184). Reports + integrations cluster is the strongest signal.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     None found — the two open drafts (#2332 age 25h, #2371 age 1h) don't yet carry an APPROVED reviewDecision. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json where reviewer_summary starts with "verdict=changes"
     AND (now - last_updated) > 24h AND status == "review".
     PR #2371 carries verdict=changes (ci-lint-spotless) as of ~1h ago — dispatcher already pushed ktfmt lint fix
     (commit 1006a4c) and CI is re-running, so no PR crosses the 24h threshold. -->
- none

## Code review slice
- Segment reviewed: **skipped by the routine (catch-up mode)**
- Reason: dispatcher ran Tier-1d `dev-review` on 5 segments during the 3-day routine gap — `mobile-native-kmp` (2026-07-15), `ppt-web-core` (2026-07-14/15, 2 rounds), `ppt-web-ui` (2026-07-15), `reality-server` (2026-07-15), `reality-web` (2026-07-15), `api-core` (2026-07-15), `api-handlers` (2026-07-15) — plus post-merge review of PRs #2357–#2370 (14 issues filed). Duplicating that with a routine-side pass this run is pure noise; the routine's rotating cursor already lags all 8 segments by ≤ 4 days.
- Findings: 0 (routine skipped) + dispatcher tier1d files persisted at `.research/signals/2026-07-14-*.json`, `.research/signals/2026-07-15-*.json`
- Next segment (when routine resumes): `mobile-native-kmp` (last reviewed 2026-07-12 by routine cursor; dispatcher touched it 2026-07-15 but the routine cursor stays)
- Skipped: yes — routine catch-up (dispatcher tier1d coverage is already ahead of the routine cursor across all 8 segments)

## Screen-map status
- Total `docs/screens/` files: 168 (106 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` still not seeded → 0)
- Drift signals this run: **deferred** — per-PR file-list fetch across 50 PRs would burn ~50 API calls for a heuristic detector; deferred to the next non-catch-up run. Individual screen-map upkeep PRs (#2285, #2297, #2306, #2311, #2333) landed inside this window, so drift is likely low.
- Orphan screens this run: **deferred** (same rationale)
- Last `screen-map.yml` CI run on `main`: not queried this run

## Backlog deltas
- **New:**
  - [1] refactor · `refactor-churn-hotspots-en-json-2026-07-16` — churn hotspot: `frontend/apps/ppt-web/messages/en.json` (+3926 lines, first appearance) · confidence=high · analytical — not instability, but the raw hotspot metric fires
  - [1] refactor · `refactor-churn-hotspots-sitemap-json-2026-07-16` — churn hotspot: `frontend/packages/sitemap/src/json/sitemap.json` (+3727 lines, first appearance) · confidence=high · content churn from route additions
- **Bumped:** none
- **Decayed:** none (backlog has 0 non-terminal items after last run's convergence pass — every prior open row was closed or dropped)
- **Dropped:** none
- **Notable non-creation:** `churn-hotspot-backend/servers/api-server/src/routes/reports.rs` re-emitted with `score_delta=0` (cumulative signal, already in `seen_signals`). Hotspot_history bumped `reports.rs` runs_seen 1 → 2; next hotspot re-emission will also fire `repeated-churn` (+1).

## Plans promoted
- none — no ready items (all new backlog rows score 1, below the 3-point promotion threshold; no security fast-track hits)

## Auto-fix
- `no candidate` — no signal met `confidence=high` + `score≥3` + allowlist this run. The only new backlog rows are `refactor`-vector churn hotspots at score=1, and the routine's fresh signals (`lag_warning`, `stale_routine_alert`, three `churn-hotspot`s) are analytical/observational, not in the auto-fix allowlist.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — `mcp__github__push_files` can't inline the >64 KiB `assignments-archive.json`; fix: adopt PR + API-merge as the dispatcher's primary push path for the archive (proven this env — landed PR #1163 that way)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend `gc1-reconcile.sh` to auto-close any exact-id-terminal row (including status=failed)

## Goal violations
- **G2 — advance state:** passed (last_pr_seen 2271 → 2356; last_issue_seen 2272 → 2370; last_commit_sha 1691c9d → 1006a4c; seen_signals grew by 4; hotspot_history grew by 3; stats.runs 44 → 45).
- **G11 — phase-failure honesty:** Phase 1.5 and Phase 1.6 were skipped (not "failed"); `review_cursor` and `pm_cursor` intentionally not advanced. Cursors that WOULD be affected by those phases remain at prior values.
- No G8/G9/G17 blocking violations expected — routine commit is `.research/` only, no secrets/hostnames/telegram tokens in added lines.

## Open questions
- **The cloud routine cron has been silent since 2026-07-13.** This run is a manual catch-up. The dispatcher branch kept running (its cron is separate) — investigate whether the routine schedule was paused, whether Anthropic's cron infra dropped a job, or whether an unrecoverable failure earlier in the week silently killed the schedule.
- Should the routine adopt the dispatcher's Tier-1d review outputs (already persisted as `signals/<date>-<segment>.json` files) so its `review_cursor` reflects the newer reviews the dispatcher performs? Currently the two systems diverge — dispatcher reviewed all 8 segments in the past 4 days while the routine cursor still reports oldest = `mobile-native-kmp` at 2026-07-12.

## Quiet day?
- **No** — 63 merged PRs since previous scheduled run (50 in morning slice + 13 in 20:12Z incremental rerun), 46 new/updated issues, 2 new backlog vectors, 3 churn hotspots recorded. The dispatcher fast-path handled the actionable work in real time; this brief documents the volume the routine would have surfaced across three daily runs.

## Rerun addendum (20:12Z) — buffer-refill next steps
The buffer-low trigger (`claimable=0/72`) is signal-grammar mismatch, not backlog exhaustion. Two follow-ups:
1. **Dispatcher-side (immediate):** re-run `.research/issue-ingest.sh` on the current OPEN issue list. It will pick up #2318 / #2320 / #2366 (currently absent from `action-list.json` — only #2360 is present, and it's `quarantined` in assignments). Once ingested, `claimable` should climb from 0 toward 3+ actionable rows before the dispatcher's next Tier-2 planner spawn.
2. **Routine-side (next scheduled run — 2026-07-17):** add a `followup-review-issue` signal type (or widen `untriaged-issue` to fire on `label ∈ {follow-up, from-merged-review}` when neither the issue nor a closing PR appears in `state.seen_signals`). Score +2 base, +3 if severity=high. This closes the gap where the `ppt-review-merged` skill files real work as GitHub issues but the routine's Phase 1 never sees them.
- No plan is promoted from this rerun — the routine will not act on unbacklogged issues without a signal derivation step. Backlog invariant preserved.
