# 2026-07-16

<sub>Second run today (10:20 UTC) — overwrites the 03:04 UTC catch-up brief per the same-day-rerun idempotency rule. Trigger payload: `buffer-low: claimable=7/72 (WIP-jammed 8/8) — refill planner`.</sub>

## Since last run
- Merged PRs: 8 (range #2339–#2378 — includes #2339/#2340 which were listed "open" in the 03:04 brief and merged at 06:02 UTC)
- Open PRs touched: 10 new drafts (#2376–#2377, #2379–#2386) — all created in this window, all <8h old, all draft (dispatcher `auto-impl/*` branches + 2 dependabot)
- New / updated issues: 5 (#2357, #2359, #2361, #2362, #2363 — all closed follow-ups from post-merge review)
- Commits: 14 on `dev` (7 PR-merge commits + 7 dispatcher chore commits)
- Routine lag: 7h since last run (03:04 UTC) — on schedule
- Phases that failed: **phase-1.5-rotating-expert-review (skipped)**, **phase-1.6-project-management (skipped)** — both intentionally deferred (see rationale below)

## Shipped
Small window (7h), 8 merged PRs — all follow-ups from the 2026-07-15 post-merge review sweep:
- **#2339 (security, HIGH)** — `documents`: enforce org-scoped `file_key` at registration + signed `Content-Length` on presigned PUT (closes 2/3 HIGH findings from #2320; orphaned-object reconcile job deferred to #2320).
- **#2340 (correctness)** — `reports`: give the scheduler worker RLS context so due schedules actually fire (silent no-op fixed — the worker's SELECT ran without GUC, always 0 rows).
- **#2371 (product)** — mobile-native KMP: wire MyListings + ListingAnalytics screens to the merged `/api/v1/my/listings` + `/{id}/analytics` endpoints (replaces the permanent empty stubs in Navigation.kt:440/458 with a real `PortalListingsRepository`).
- **#2372 (tenant-isolation)** — mobile RN: clear the whole TanStack Query cache on login (mirror `logout()`); prior behavior left the prior org's non-tenant-scoped read data (`documents/list`, `buildings/list`, …) visible until refetch.
- **#2373 (refactor)** — reality-web: consolidate 4-way listing-detail shape validation into a single `parseListingDetail()` normalizer at the `getListing` boundary (closes the #2276/#2281/#2341 SSR-500 regression class).
- **#2374 (tests)** — backend: add deny-path (resident → 403) + positive (manager → 200) regression for `GET /api/v1/ai/chat/escalated` role gate that shipped in #2356 without one.
- **#2375 (authz, HIGH)** — reality-server: `/agencies/me` now filters `is_active = TRUE` so a departed member no longer resolves to a former agency's private data + backend `#[sqlx::test]` coverage.
- **#2378 (security, low)** — ppt-web: strip single-use signing token from `/sign` URL via `history.replaceState` + `<meta name="referrer" content="no-referrer">` so it can't be recovered from browser history / leak via Referer.

## Watch
- Stalled review: **none** — all 10 open PRs are <8h old (fresh drafts).
- Reverted: 0
- Churn hotspots this run: **skipped** — top-3 raw churn (`report_schedule_scheduler_rls_tests.rs` +225, `PortalListingsRepositoryTest.kt` +203, `PortalListingsRepository.kt` +158) are all **new-file singleton additions** from a single PR each, not repeated modifications. The `churn-hotspot` signal is an instability proxy; emitting it on green-field additions creates score-1 backlog noise that decays into `dropped` without action. Deferred to future runs that aggregate activity from multiple PRs. `hotspot_history` still tracks their first-appearance for future `repeated-churn` detection if they re-appear.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     All 10 open PRs are <8h old, none carry an APPROVED reviewDecision yet. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json where reviewer_summary starts with "verdict=changes"
     AND (now - last_updated) > 24h AND status == "review".
     Not queried this run (7h window, dispatcher still actively cycling — nothing plausibly crosses 24h). -->
- none

## Code review slice
- Segment reviewed: **skipped by the routine** (7h window, no code-review pass this run)
- Reason: only 7h since the previous routine ran (03:04 UTC catch-up), and that run itself explicitly skipped Phase 1.5 because dispatcher tier1d had already covered every segment in the 2026-07-13→2026-07-15 window (`review_cursor` shows all 8 segments last-reviewed within the past 4 days). Running another routine-side pass on the 8 PRs merged in the last 7h is duplicative — every one of them is a follow-up to a PR the dispatcher's post-merge-review skill already surfaced findings for (#2357–#2370 chain).
- Findings: 0 (routine skipped)
- Next segment (when routine resumes review): `mobile-native-kmp` (routine cursor 2026-07-12; dispatcher touched it 2026-07-15 but the routine cursor is intentionally left as-is per this run's skip decision)
- Skipped: yes — same-day + dispatcher-tier1d-fresh

## Screen-map status
- Total `docs/screens/` files: 168 (106 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` still not seeded → 0)
- Drift signals this run: **1** — `screen-map-drift-pr-2373-reality`: PR #2373 (refactor: `parseListingDetail` normalizer) touched 7 files under `frontend/apps/reality-web/src/app/[locale]/listings/[slug]/*` + 2 under `components/listings/ListingDetailContent` without updating any `docs/screens/reality/*.md`. Flagged as **medium confidence, likely-false-positive per the heuristic caveat** — the change is an internal parse-helper extraction inside the existing listing-detail route, not a route add/remove/rename, and the screen-map docs describe screens, not internal SSR normalizers. Backlog row entered at score=2 for human review; auto-fix bar not met (Phase 5 needs confidence=high + score≥3).
- Orphan screens this run: 0 (no `docs/screens/reality/<listings-detail>.md` slug spot-check triggered a removal — route file basenames unchanged in #2373).
- Last `screen-map.yml` CI run on `main`: not queried this run (best-effort deferral consistent with the 03:04 brief).

## Backlog deltas
- **New:**
  - [2] test-gap · `test-gap-screen-map-drift-pr-2373-reality` — screen-map drift signal from PR #2373; medium confidence, best-effort heuristic (likely false positive); not promotable this run (below score=3 threshold, not security-fast-track vector)
- **Bumped:** none
- **Decayed:** none — the 2 previously-open items (`refactor-churn-hotspots-en-json-2026-07-16`, `refactor-churn-hotspots-sitemap-json-2026-07-16`) were both created this same day; decay applies only to rows with `updated_at ≤ today-14`.
- **Dropped:** none
- **Resolution check:** no open items match today's merged PRs by verbatim id or by evidence-gone grep — nothing transitions to `done`.

## Plans promoted
- **none** — no backlog item meets the readiness bar this run.
  - The 3 open items are all score ≤2: the one new `screen-map-drift` (score=2, medium confidence, test-gap vector — not security-fast-track eligible), plus the two lingering churn hotspots from the 03:04 brief (`en-json`, `sitemap-json`, both score=1).
  - **Buffer-low context.** The trigger payload said `claimable=7/72 (WIP-jammed 8/8) — refill planner`. The routine cannot fabricate ready promotions from thin air: promotion needs a backlog item with `score ≥ 3` + open + concrete source + files + hypothesis + test plan. Today's activity produced only one new signal at score=2. The pipeline that keeps the buffer stocked is **code activity → signal derivation → score accumulation → promotion**, and in a 7h window with follow-up-only PRs (each closing prior review findings cleanly) there are no new observation-grade signals worth promoting. The buffer will re-fill organically when: (a) tomorrow's routine picks up a wider window of activity, (b) the dispatcher's own tier1d code-review adds `code-review-finding` signals at +2/+3, or (c) WIP drains and the dispatcher pulls the 7 already-claimable items.
  - Actionable ask if the buffer stays starved 24h+: consider raising the dispatcher's WIP cap from 8 to 10, or manually promoting one of the deferred dispatcher-tier1d findings that already sit in `.research/signals/2026-07-14-*.json` / `2026-07-15-*.json` into a plan by hand.

## Auto-fix
- `no candidate` — the one new signal (`screen-map-drift-pr-2373-reality`) is in the Phase 5 allowlist (`screen-map-drift → doc-stub PR`) BUT fails two of the certainty bars: `confidence == "high"` requires `high` (this is `medium`, best-effort heuristic), and `score ≥ 3` requires 3 (this is 2). Correct not to fire — auto-generating a stub `docs/screens/reality/*.md` for a refactor-only route touch would be pure noise.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — `mcp__github__push_files` can't inline the >64 KiB `assignments-archive.json`; fix: adopt PR + API-merge as the dispatcher's primary push path for the archive (proven this env — landed PR #1163 that way)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend `gc1-reconcile.sh` to auto-close any exact-id-terminal row (including status=failed)

## Goal violations
- **G2 — advance state:** passed (last_pr_seen 2356 → 2378; last_issue_seen 2370 → 2378; last_commit_sha 1006a4c → 09fc2520; seen_signals grew by 1; stats.runs 45 → 46).
- **G8/G9/G17 — blocking gates:** none expected; routine commit is `.research/` only, no secrets/hostnames/telegram tokens in added lines.
- **G11 — phase-failure honesty:** Phase 1.5 and Phase 1.6 skipped (documented above); `review_cursor` and `pm_cursor` intentionally not advanced.
- **G16 — management artifacts:** no-op this run (Phase 1.6 skipped, so `pm_cursor` did not advance; per G16's own text, the gate is a no-op when Phase 1.6 skipped).

## Open questions
- **Buffer-low policy.** When the routine sees `buffer-low: claimable=7/72 (WIP-jammed 8/8)`, is the expected response to (a) raise the dispatcher's WIP cap so the 8 in-flight can drain in parallel with more claims, (b) leave it alone (WIP is bottleneck, more claimable items don't help), or (c) reach into `.research/signals/2026-07-14-*.json` and promote deferred dispatcher-tier1d findings into plans? The routine currently does (b) by default because promotion needs a score-3+ item and the routine cannot re-score dispatcher signals it didn't emit. This run surfaces the question rather than acting unilaterally.
- The 03:04 UTC brief's open question about the cloud-cron 3-day silence (2026-07-13→2026-07-15) is superseded: today's cron did fire twice (03:04 and now), so the schedule appears healthy again.

## Quiet day?
- **No** — 8 merged PRs, 1 new backlog vector, 10 new open drafts. But: 0 plans promoted (documented above as the buffer-low natural rate — no code-side signal met the promotion bar in this 7h window).
