# 2026-07-16

<sub>Second run of the day. First brief was written at 03:04 UTC covering the 3-day catch-up window (2026-07-13 → 2026-07-16 03:00). This overwrite converges today's picture at 08:15 UTC, adding the 5-hour delta since the earlier brief.</sub>

## Since last run
- **Cumulative for today (2026-07-13 → 2026-07-16 08:15 UTC):** Merged PRs: 54 (range #2273–#2374); Open PRs touched: 6 (#2332 + #2375–#2379); New / updated issues: 46 (all labeled `follow-up`/`from-merged-review` or security/frontend/backend — none untriaged); Commits on `dev`: 55.
- **Delta since 03:04 UTC brief (5h):** 4 new merged PRs (#2371–#2374, all clean follow-ups with tests), 5 new open drafts (#2375/#2376/#2377/#2378/#2379 — dispatcher-produced fixes for gh-issue-2358/2359/2360/2363/2364), 0 new issues (issue cursor stays at 2370). No new stalled reviews (all new drafts <4h old; #2332 age 24-25h).
- Routine lag: **5h 11m since last run** — on schedule (previous run 03:04 UTC).
- Phases that failed: **phase-1.5-rotating-expert-review (skipped — same rationale as earlier brief; dispatcher tier1d coverage still ahead)**, **phase-1.6-project-management (skipped — same rationale: 5h is well inside the daily-rotation cadence and no PM signal changed since 03:04 UTC)**.

## Since-03:04 trigger context
- Fire payload: **`buffer-low: claimable=12/72 — refill planner`**. Dispatcher's claimable-work buffer sits at 12 of a 72-target — the planner is running dry. **Nothing the routine can promote this run:** the current backlog carries 0 items at `score >= 3` (or `>= 2` on the security fast-track). The only two open rows are today's score-1 churn hotspots (`en.json`, `sitemap.json`), both content-churn signals with `candidate_vector=refactor` — below the promotion threshold and (correctly) not backlog-plan material.
- Where the dispatcher will get its next claim work from: the 6 open draft PRs (#2375–#2379 + #2332) are already-claimed items filling `assignments.json` in-progress/review lanes — those come off the buffer as they merge. The refill source is (a) new post-merge-review issues from PR #2371/#2372/#2373/#2374 (dispatcher Phase 2.5, not the routine); (b) any high-signal Phase-1.5 code-review findings on the next routine tick; (c) hand-authored plans landed straight into `.research/plans/`.

## Shipped
Total 54 merged PRs across the 72h window. Earlier brief already documents #2273–#2356; adding the four that landed since:

- **#2371 code-review-mobile-native-kmp-mylistings-analytics-stub-retry1** — KMP `MyListings` + `ListingAnalytics` wired to the portal API (closes the Tier-1d KMP stub finding).
- **#2372 gh-issue-2361** — mobile `AuthContext.login()` clears the whole TanStack query cache on login (was only evicting `['auth','tenant-id']`, so prior-org data survived cross-login on non-tenant-scoped keys — `[buildings|documents|forms|neighbors|faults|announcements|voting|leases]`). Symmetric with the logout path.
- **#2373 gh-issue-2362** — reality-web listing-detail `parseListingDetail()` normalizer: single source of truth for the wrong-typed-`features`/`photos` shape guard that was previously hand-rolled across four sites (`page.tsx`, `metadata.ts`, `jsonLd.ts`, `ListingDetailContent`).
- **#2374 gh-issue-2357** — deny-path regression test for the escalated-AI role gate (`GET /ai/chat/escalated`, resident → 403).

All four followed the fix-with-tests pattern (no hotfix-no-test signal fires).

## Watch
- **Stalled review:** none — all 6 open PRs are ≤25h old:
  - `#2332` (fix/gh-issue-2317, draft) — age 24-25h (created 2026-07-15 01:48 UTC, no review decision yet).
  - `#2375` (gh-issue-2359 /agencies/me active-only) — draft, age <4h.
  - `#2376` (gh-issue-2358 portal_webhook_events dedup migration) — draft, age <4h.
  - `#2377` (gh-issue-2360 view-webhook dedup) — draft, age <4h.
  - `#2378` (gh-issue-2363 signing-token URL/history hygiene) — draft, age <2h.
  - `#2379` (gh-issue-2364 KMP Decimal wire-contract guard) — draft, age <2h.
- **Reverted:** 0 (delta unchanged from earlier brief).
- **Churn hotspots this run (raw, after exclusions) — unchanged from 03:04 UTC brief:** `en.json` (+3926), `sitemap.json` (+3727), `reports.rs` (+3333). The 4 new PRs since 03:04 UTC did not shift any hotspot ranking.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     None found — the 6 open drafts either lack an APPROVED review decision or are <24h old. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json where reviewer_summary starts with "verdict=changes"
     AND (now - last_updated) > 24h AND status == "review".
     Per dispatcher 06:20 log: 3 PRs merged (6 reviewed) since 04:24 UTC; no verdict=changes rows sit in the review lane past 24h. -->
- none

## Code review slice
- Segment reviewed: **skipped by the routine (same-day rerun — dispatcher tier1d coverage is still ahead of the routine cursor across all 8 segments)**
- Reason: identical to earlier brief — dispatcher Tier-1d dev-reviews on 2026-07-14/15 cover all 8 segments; the routine cursor still lags each by ≤ 4 days; re-running Phase 1.5 5h after the earlier skip would produce zero new information.
- Findings: 0 (routine skipped; dispatcher tier1d files at `.research/signals/2026-07-14-*.json`, `.research/signals/2026-07-15-*.json`)
- Next segment (when routine resumes): `mobile-native-kmp` (routine cursor 2026-07-12; dispatcher touched 2026-07-15 but routine cursor stays).
- Skipped: yes — same-day rerun catch-up.

## Screen-map status
- Total `docs/screens/` files: 168 (106 `ppt/`, 47 `reality/`, 15 `reality-mobile/`; `mobile/` still not seeded → 0). Unchanged from 03:04 UTC.
- Drift signals this run: **deferred** (same rationale — per-PR file-list fetch across 50+ PRs would burn API budget for a heuristic detector).
- Orphan screens this run: **deferred** (same rationale).
- Last `screen-map.yml` CI run on `main`: not queried this run.

## Backlog deltas
- **New:** none (this delta) — the 4 PRs merged since 03:04 UTC did not derive new backlog signals (all follow-ups with regression tests, all fix-and-test PRs so `hotfix-no-test` doesn't fire; churn contribution insufficient to displace existing top-3).
- **Bumped:** none.
- **Decayed:** none.
- **Dropped:** none.
- **Cumulative for today (from earlier brief):**
  - [1] refactor · `refactor-churn-hotspots-en-json-2026-07-16` — churn hotspot: `frontend/apps/ppt-web/messages/en.json` (+3926 lines, first appearance) · confidence=high.
  - [1] refactor · `refactor-churn-hotspots-sitemap-json-2026-07-16` — churn hotspot: `frontend/packages/sitemap/src/json/sitemap.json` (+3727 lines, first appearance) · confidence=high.

## Plans promoted
- none — same reason as earlier brief: backlog carries no items above the score-3 promotion threshold, and no security fast-track hits. **The `buffer-low` trigger cannot force a promotion** — quality-over-volume gate.

## Auto-fix
- `no candidate` — same as earlier brief. No signal met `confidence=high` + `score≥3` + allowlist. The two score-1 churn-hotspot rows are `refactor`-vector content churn, not in the auto-fix allowlist. No `screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / stalled `stalled-review` / `dep-update-noise` (lockfile-drift) signals in this window.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — `mcp__github__push_files` can't inline the >64 KiB `assignments-archive.json`; fix: adopt PR + API-merge as the dispatcher's primary push path for the archive (proven this env — landed PR #1163 that way)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend `gc1-reconcile.sh` to auto-close any exact-id-terminal row (including status=failed)

## Goal violations
- **G2 — advance state:** passed (last_pr_seen 2356 → 2374; last_commit_sha 1006a4c → 83bf81c; last_run_iso 2026-07-16T03:04:00Z → 08:15:00Z; stats.runs 45 → 46; seen_signals and hotspot_history unchanged since 03:04 UTC — expected on a 5-hour delta with no new signals).
- **G11 — phase-failure honesty:** Phase 1.5 and Phase 1.6 were skipped (not "failed"), same rationale as earlier brief; `review_cursor` and `pm_cursor` intentionally not advanced.
- No G8/G9/G17 blocking violations expected — routine commit is `.research/` only, no secrets/hostnames/telegram tokens in added lines.

## Open questions
- **Buffer-low will re-fire until the dispatcher's assignment pool drains.** The 6 open drafts (#2332 + #2375–#2379) will refill the merged-PR stream over the next 12-24h; each merge triggers a post-merge review that may create new gh-issue-N assignments. The routine doesn't drive that loop; the dispatcher does. Recommend re-checking the buffer after the next dispatcher tick.
- **The cloud routine cron pause (documented in the 03:04 UTC brief) is still an open question.** This manual same-day rerun is a workaround, not a fix — the operator should inspect the cloud routine's cron schedule and any failure logs from 2026-07-13 → 2026-07-16 03:04 UTC.

## Quiet day?
- **No** — 54 merged PRs cumulative for the day, 46 new/updated issues, 2 new backlog vectors, 3 churn hotspots recorded. The dispatcher fast-path is doing the actionable work in real time; this brief documents the observation the routine would have surfaced.
