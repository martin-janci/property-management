# 2026-07-16

## Since last run
- Merged PRs: **15** since 03:04Z rerun (range #2339–#2384, plus #2371, #2372, #2373, #2374, #2375, #2376, #2378, #2379, #2380, #2381, #2382, #2383, #2384, #2339, #2340) — all follow-up fixes to review findings from PRs #2273–#2356.
- Open PRs touched: 12 (10 dependabot updates #2385–#2394, draft #2377 gh-issue-2360 waiting on dep, stale draft #2332 age ~37h)
- New / updated issues: 12 (all `follow-up`/`from-merged-review`, all closed by the merged fixes)
- Commits: 21 on `dev` (application code + dispatcher chore commits)
- Routine lag: **0d 11h since last run (same-day rerun triggered by buffer-low)** — cloud cron resumed
- Phases that failed: **none** — Phase 1.5 (rotating review) folded in via adoption of dispatcher tier1d signals; Phase 1.6 (project management) intentionally deferred (same-day rerun, no `pm-full` trigger)

## Shipped
15 merged PRs since 03:04Z, all follow-ups to the #2273–#2356 review batch. Every issue (#2357–#2370) surfaced by the post-merge review is now closed on `dev`:

- **Backend hardening (5 PRs):**
  - #2376 (Closes #2358) — `portal_webhook_events` dedup net for idempotent inquiry retries: migration 00219 creates the table + partial unique index, `record_webhook_event` gains `ON CONFLICT DO NOTHING`, `InquiryPersistOutcome::Duplicate` now actually fires; retriable 500 from PR #2353 is now safe.
  - #2339 (Closes #2320) — enforce org-scoped `file_key` + signed `Content-Length` on presigned document PUT (extended to version upload).
  - #2340 (Closes #2318) — reports scheduler worker RLS context: due schedules actually fire.
  - #2374 (Closes #2357) — deny-path regression test for `GET /ai/chat/escalated` role gate (resident 403, manager 200).
  - #2375 (Closes #2359) — `/agencies/me` filters to active memberships + `#[sqlx::test]` round-trips + integration tests for 200/404/401.

- **Frontend polish (6 PRs):**
  - #2373 (Closes #2362) — reality-web `parseListingDetail` normalizer consolidates 4 hand-rolled shape guards.
  - #2384 (Closes #2370) — localize create-schedule report labels + backend error toast (fixes English leaking to 5 non-English locales).
  - #2381 (Closes #2369) — keep category enabled badge in sync on optimistic trigger toggle.
  - #2382 (Closes #2365) — cover AC-5 favorite price-alert toggle + optimistic update.
  - #2378 (Closes #2363) — strip signing token from `/sign` URL + suppress `Referer`.
  - #2372 (Closes #2361) — clear whole query cache on mobile login (mirror logout, tenant-isolation fix).

- **Mobile RN + KMP (3 PRs):**
  - #2383 (Closes #2368) — guard mobile upload allow-list drift + enforce photo MIME (HEIC bypass fix, cross-stack parity test).
  - #2379 (Closes #2364) — Decimal-as-string wire contract test for sibling reality-server surfaces (regression prevention).
  - #2371 — wire mobile-native KMP `MyListings` + `ListingAnalytics` to portal API (`PortalListingsRepository`).

- **Tooling (1 PR):**
  - #2380 (Closes #2367) — validate every synthesized candidate id against `IdSchema` in `scanCandidates` (non-Latin name → schema-invalid id class bug).

## Watch
- Stalled review: **none** — all 12 open PRs are ≤ 37h old. Draft #2332 (age ~37h) is under the 7-day threshold; draft #2377 (age ~6h) is fresh (waits on #2376 dedup migration, which landed 12:19Z — now unblocked).
- Reverted: 0
- Churn hotspots this rerun (raw, after exclusions): no new hotspots this window — the merged follow-up PRs are small-diff fixes (each < 500 lines). The three hotspots from the morning run (`en.json` +3926, `sitemap.json` +3727, `reports.rs` +3333) are unchanged and remain in the backlog at score=1 each.
- Dependabot noise: 10 open dep-update PRs — logged, not scored (`dep-update-noise` signal type).

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     None: the two drafts (#2332 age ~37h, #2377 age ~6h) don't carry APPROVED reviewDecision. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json where reviewer_summary starts with "verdict=changes"
     AND (now - last_updated) > 24h AND status == "review". Dispatcher chore commit at 12:23Z shows
     GC3=FAIL (buffer starved), not a fix-round backlog. -->
- none

## Code review slice
- Segment reviewed: **adopted from dispatcher tier1d** (not the routine's own rotation this rerun)
- Reason: the same-day rerun's job is to refill the dispatcher's actionable buffer (buffer-low trigger: claimable=4/72). The routine's Phase 1.5 was already skipped this morning; running its own review now would duplicate the dispatcher's tier1d work. Instead, this rerun adopts the tier1d findings that landed on disk since the morning run and promotes the two highest-scored to plans.
- Findings adopted this rerun: **15** code-review-finding signals across 7 segments (`api-core`, `api-handlers`, `ppt-web-core`, `ppt-web-ui`, `reality-server`, `reality-web`, `mobile-native-kmp`) — see Backlog deltas.
- Next segment (when routine resumes normal rotation): `api-core` (oldest cursor after this run) — but the routine has now consumed 7 tier1d files, so all cursors sit at `2026-07-16`; next-run rotation will pick by churn or the oldest wall-clock signal.
- Skipped: no — 15 findings adopted directly.

## Screen-map status
- Total `docs/screens/` files: 168 (unchanged since 03:04Z) — merged follow-up PRs touched behavior, not routes.
- Drift signals this rerun: 0 — the frontend PRs that touched `reality-web/src/app/**` (#2373, #2382) landed alongside deliberate no-doc changes (typing fixes + optimistic-update wire, not new/removed routes); the `ppt-web/src/routes/**` touch (#2384) modified an existing schedule route without adding/removing a screen. No drift detector fire.
- Orphan screens this rerun: 0
- Last `screen-map.yml` CI run on `main`: not queried this rerun.

## Backlog deltas
- **New (15 tier1d code-review findings, all confidence ≥ medium):**
  - [3] bug · `code-review-mobile-native-kmp-navigation-create-listing-stub` — Android CreateListing wired to `NotImplementedError` — realtor Publish silently blocked · confidence=high · promoted
  - [3] bug · `code-review-api-handlers-missing-authz-article` — `news_articles` publish/archive/restore/pin/delete/media handlers missing authz (`_user: AuthUser` extracted but never checked) · confidence=medium · promoted
  - [2] bug · `code-review-api-handlers-unenforced-toggle-flags` — toggle/feature flags read but never enforced on gated news paths
  - [2] bug · `code-review-ppt-web-core-iot-ws-unbounded-reconnect` — IoT WebSocket reconnect loop with no backoff cap
  - [2] bug · `code-review-ppt-web-core-auth-stale-token-init` — auth init reads stale refresh token; races with silent-refresh handshake
  - [2] bug · `code-review-ppt-web-ui-paymatch-mut-noerr` — PaymentsMatch mutation has no error branch (silent failure)
  - [2] bug · `code-review-ppt-web-ui-invoice-mut-noerr` — Invoice mutation has no error branch (silent failure)
  - [2] bug · `code-review-ppt-web-ui-portfolio-dash-mock` — portfolio dashboard still renders hardcoded mock (no backend wire)
  - [2] bug · `code-review-reality-web-pricealert-mut-noerr` — PriceAlert subscribe/unsubscribe silent-failure branch
  - [1] bug · `code-review-api-core-scheduler-mutex-poison` — scheduler Mutex poison never recovered
  - [1] bug · `code-review-api-handlers-rng-expect-panic` — `rng().expect()` → process crash on RNG failure
  - [1] bug · `code-review-ppt-web-core-nav-aria-no-i18n` — hard-coded English `aria-label` on nav
  - [1] bug · `code-review-reality-server-view-no-visibility-gate` — view route missing visibility gate
  - [1] bug · `code-review-reality-web-sso-callback-i18n` — SSO callback error strings hard-coded English
  - [1] test-gap · `code-review-mobile-native-kmp-listingrepo-partial` — 5 of 6 `ListingRepository` methods have no MockEngine test
- **Bumped:** none (all 15 are net-new IDs; no existing item matched)
- **Decayed:** none (the two churn hotspots from morning were created today, no 14d decay yet)
- **Dropped:** none

## Plans promoted
- `plans/code-review-mobile-native-kmp-navigation-create-listing-stub.md` — wire Android realtor CreateListing to a real `createListing` on `ListingRepository` + `ApiClient`; add MockEngine test + Compose UI test · adversarial pass: passed
- `plans/code-review-api-handlers-missing-authz-article.md` — restore `is_manager || is_author` gate on publish/archive/restore/pin/delete + add_media/delete_media; add `news_articles_authz_tests.rs` · adversarial pass: passed

Adversarial pass notes: both plans trace every hypothesis claim back to a verified `file:line`; test-plan entries would fail today (methods/tests absent on `dev`); Files bullets all resolve on disk (7/7 for the news_articles plan, 3/3 for the KMP plan). No placeholder rot on grep. Slug stems clear against `plans/`, `_archive/`, `assignments.json` (only `gh-issue-2360` active), and `action-list.json` open rows.

## Auto-fix
- `no candidate` — the 15 new signals are all `code-review-finding` type, which is not in the auto-fix allowlist (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise:lockfile-drift`, `stalled-review`). No screen-map drift detected this window; no stalled review crosses the 7-day threshold.

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=0
- fp-mcp-push-cannot-inline-large-archive [recurrence=7] — `mcp__github__push_files` can't inline the >64 KiB `assignments-archive.json`; fix: adopt PR + API-merge as the dispatcher's primary push path for the archive (proven this env — landed PR #1163 that way)
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer + GC3 formulas subtract only ACTIVE assignments, so terminal-in-archive open rows inflate `claimable`; fix: subtract archive task_ids and extend `gc1-reconcile.sh` to auto-close any exact-id-terminal row (including status=failed)

## Goal violations
- **G2 — advance state:** passed (last_pr_seen 2356 → 2384; last_commit_sha 1006a4c → 6264584; seen_signals grew by 15; stats.runs 45 → 46; stats.plans_created 14 → 16).
- **G6 — promoted plans have adversarial-pass annotations:** passed — both new plans annotated `adversarial pass: passed` in the *Plans promoted* section above.
- **G7 — ≤2 new plans, all sections + files exist:** passed — 2 plans; 4 metadata fields + 11 headings each; capability tick + mode declaration each; 3/3 and 7/7 file paths resolve on disk.
- **G12 — plan convergence:** passed — all prior plans still tracked (either in `plans/` or `plans/_archive/`); no silent deletions.
- **G16 — management artifacts / pm_cursor:** no-op — Phase 1.6 skipped this rerun (same-day rerun, no `pm-full` trigger).
- No G8/G9/G17 blocking violations expected — routine commit is `.research/` only; no secrets, hostnames, or Telegram token literals in added lines.

## Open questions
- Should the routine formally adopt dispatcher tier1d signals every run (bypassing its own rotating Phase 1.5) when a same-day rerun refills the buffer? Today's rerun did this ad-hoc — it would be cleaner as an explicit branch of Phase 1.5.
- The dispatcher's `GC3-buffer-bounds=FAIL` has persisted across the last 6 chore commits (`record-only`) — is this a chronic starvation state that needs a structural fix (planner more aggressive) or is it just a metric that under-counts? Recurring `fp-buffer-metric-blind-to-archived-open-items` (recurrence=3) suggests the metric is undercounting; the fix has been open for 3 runs.

## Quiet day?
- **No** — 15 merged PRs this window, 15 new backlog vectors (13 open + 2 promoted-to-ready), 2 new plans. This rerun directly addresses the `buffer-low: claimable=4/72` trigger by promoting the two highest-scored dispatcher tier1d findings (create-listing stub + news_articles authz gap) and adding 13 more code-review-finding rows to the routine backlog for later promotion.
