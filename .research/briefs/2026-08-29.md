# 2026-08-29

## Since last run
- Merged PRs: 6 (range #2868–#2874; real code-touch on `dev` since b67552b)
- Open PRs touched: 3 new dependabot (#2865/#2866/#2867 opened 2026-08-27); stalled draft #2744 still unmoved; 3 `feat(acc)` PRs #2555/#2558/#2559 still `needs-human-judgement`
- New / updated issues: 2 (#2873 + #2875 — both `cargo-deny chacha20 yanked`; #2873 closed by PR #2874; #2875 opened as a duplicate and closed same-day)
- Commits: 20+ on `dev` (6 real merges + 14 dispatcher chores)
- Routine lag: 23h since last run — on schedule (dispatcher meanwhile ticked every ~2h with active buffer-starvation reporting)
- Phases that failed: none — Phase 1 fetched PRs/issues/commits + churn; Phase 1.5 ran on `reality-web` (2 findings — churn-aligned segments api-handlers/api-core/ppt-web-ui had already been covered by the dispatcher's Tier-1d generator yesterday); Phase 1.6 ran (Scrum Master + `pm-devops` rotation, `pm_cursor` 4→5, `coverage_cursor` 6→7 advanced together); Phase 2 ran; Phase 3 promoted 1 plan (corrective revival — see below); Phase 5 no candidate

## Shipped
- #2874 `fix(backend)` — chacha20 0.10.0→0.10.2 lockfile bump to clear yanked RUSTSEC advisory; `backend/Cargo.lock` (2 lines). Closes #2873; unblocks every backend PR's cargo-deny gate.
- #2872 `refactor(api-server)` — extract shared `enforce_email_verification` seam in `AuthPolicyEnforcer`; `services/auth_policy.rs` (+77/−25) + 2 sqlx tests locking the membership-path unverified-target reject / verified-target-allowed contract.
- #2871 `refactor(api-server)` — dedupe `ModerationCaseResponse` construction across 7 moderation handlers via `ModerationCaseResponse::from_case`; `routes/aml_dsa/moderation.rs` (+203/−184) + 6 unit tests. Behavior-preserving; `now` threaded so a queue page shares one timestamp.
- #2870 `code-review-api-handlers-report-content-unbounded` — bound `report_content` reason length via `MAX_REPORT_REASON_LEN=5000` + `validate_optional_text_field`; `routes/aml_dsa/moderation.rs` + `routes/aml_dsa/shared.rs` (+56) with over-limit/absent/empty/at-cap unit tests. Resolves the corresponding backlog security row.
- #2869 `fix(api-server)` — validate `decide_appeal` decision enum + cap rationale via `MAX_RATIONALE_LEN`; `routes/aml_dsa/moderation.rs` + `routes/aml_dsa/shared.rs` (+50). Fixes a silent-typo class where a wrong-casing "Upheld" was mapped to a rejection.
- #2868 `churn-hotspot ContentModerationPage` — dedupe decision-toast callbacks into `decisionMutationCallbacks` helper for `submitTakeAction` + `submitDecideAppeal`; `features/compliance/pages/ContentModerationPage.tsx` (+40/−38) + `ContentModerationPage.test.tsx` (+93/−1) locking toast + dialog-reset behavior.

## Watch
- Stalled review: #2744 (draft, `needs-human-review`, `fix(dispatcher): un-wedge oversize-archive push` + `retry-remint cross-id ghost`) — 16 days idle, 0 commits since 2026-08-13. Self-PR from operator; routine surfaces as `stalled-review-pr-2744` (needs-human-judgement — only the operator can un-draft or close).
- Reverted: none this run.
- Churn hotspots this run (real code-touch only; lockfiles excluded):
  - `backend/servers/api-server/src/routes/aml_dsa/moderation.rs` — 400 raw lines across PRs #2871/#2870/#2869; `runs_seen=2` (**repeated-churn fires**)
  - `backend/servers/api-server/src/services/auth_policy.rs` — 102 lines from PR #2872; `runs_seen=2` (**repeated-churn fires**)
  - `frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.test.tsx` — 94 lines from PR #2868; first-time hotspot (`runs_seen=1`)

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     PR #2744 is draft but not APPROVED (labeled needs-human-review, awaiting operator un-draft). -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json where reviewer_summary starts with
     "verdict=changes" AND (now - last_updated) > 24h AND status == "review".
     Local assignments.json has none. -->
- none

## Code review slice
- Segment reviewed: `reality-web` (reason: fallback to oldest-cloud-buildable-not-covered-by-dispatcher — churn-aligned candidates api-handlers/api-core/ppt-web-ui were all covered by the dispatcher's Tier-1d generator yesterday and 15 of those findings are still unactualized in the routine's own backlog; the FCM one is revived here — see *Backlog deltas*)
- Experts: frontend
- Findings: 2 (both under `Backlog deltas`)
  - `code-review-reality-web-realtor-account-i18n` (i18n, +2, high confidence) — five realtor / account / SSO-callback pages ship raw English on sk/cs/de/en without importing `useTranslations`
  - `code-review-reality-web-comparison-unsafe-cast` (refactor, +1, medium) — `ComparisonUrlHandler` casts `/api/v1/listings/{id}` response as `ListingSummary` with no runtime shape check; endpoint actually returns `ListingDetail`
- Next segment: `mobile-native-kmp` (oldest not-recently-covered after this run, 2026-08-25 → same age today; findings would be structurally unlandable in cloud but still valuable as backlog rows)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` 106 + `reality/` 47; `mobile/` still 0)
- Drift signals this run: 0 (none of the 6 merged PRs touched `App.tsx`, `routes/**`, or `app/**` — all backend + `features/compliance/` refactors)
- Orphan screens this run: 0 (no route-basename check needed — no route file touched)
- Last `screen-map.yml` CI run on `main`: not queried this run (no `docs/screens/**` changes in window; expected quiet)

## Backlog deltas
- **New:**
  - [3] `code-review-api-core-fcm-oauth-token-stale` — **corrective revival** of dispatcher Tier-1d finding that was in `state.seen_signals` but never actualized into backlog. `FcmHttpAdapter` captures `FCM_OAUTH_TOKEN` once at startup (`push_fanout.rs:229-236`); ~1h TTL then every Android push returns 401 UNAUTHENTICATED but the error classifier only treats device-side statuses as `token_expired` — credential-level failure is silently swallowed as a per-message `tracing::warn`. Bug, medium confidence. Verified by direct read of `push_fanout.rs:229`, `:330-341`, `:362-376`. **Promoted to plan this run** (see *Plans promoted*).
  - [2] `code-review-reality-web-realtor-account-i18n` — 5 realtor/account/SSO-callback pages hardcode English strings; high-confidence i18n gap (see *Code review slice*).
  - [1] `code-review-reality-web-comparison-unsafe-cast` — cross-boundary unchecked cast in ComparisonUrlHandler (see *Code review slice*).
  - [1] `repeated-churn-…aml_dsa/moderation.rs` — top-3 churn 2 runs in a row (400 lines this window); refactor-shape (post large dedupe).
  - [1] `repeated-churn-…services/auth_policy.rs` — top-3 churn 2 runs in a row (102 lines this window); legitimate refactor cadence.
  - [1] `churn-hotspot-…ContentModerationPage.test.tsx` — first appearance; 93 lines locking dedupe helper behavior in PR #2868.
  - [1] `stalled-review-pr-2744` — self-PR from operator, 16 days idle, `needs-human-judgement` immediately (only operator can act).
- **Bumped:** none (no existing backlog row got new corroborating evidence this run).
- **Decayed:** none — the 3 open `mobile-native-kmp` items sit at `updated_at` 2026-08-16 / 08-19 / 08-25 (max 13 days old, one run below the −1 threshold).
- **Dropped:** none.

## Plans promoted
- `plans/code-review-api-core-fcm-oauth-token-stale.md` — cloud-ok, `Mode: cloud-ok`, bug, score 3. Adds a `credential_expired` classification for FCM UNAUTHENTICATED / 401 / 403 responses and introduces a `FcmBearerSource` refresh seam so a follow-up PR can plug in a real google-cloud-auth-backed refresher without touching the send path. Files: `backend/servers/api-server/src/services/push_fanout.rs`. Test plan: `cargo test -p api-server push_fanout` with `wiremock`/`mockito` FCM double stubbing HTTP 401 + `err_status="UNAUTHENTICATED"`. **Directly refills the planner buffer** that the routine trigger flagged as starved (6/72 claimable, all mobile-native/KMP-gated). · adversarial pass: passed

## Auto-fix
- `no candidate` — the one allowlisted stalled-review signal (`stalled-review-pr-2744`) is a self-PR from the operator (`martin-janci`), and the routine's standing convention is not to auto-comment on the operator's own PRs. No `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, or lockfile-drift `dep-update-noise` candidate met confidence=high + score≥3 + allowlist this run.

## Self-review findings
- open=37 (high=20, medium=?, low=?); new this run=0 (routine did not re-run self-review — dispatcher owns that pass); acknowledged=19
- fp-? [recurrence=18] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked and derives fallback wiring from that assumption; fix: replace the hard-coded assumption with a runtime probe at Phase 1
- fp-? [recurrence=18] — Phase 6 archive-move produces oversize `assignments-archive.json`; fix: push oversize commits to a new branch via git-push rather than `mcp__github__push_files` inline
- fp-? [recurrence=12] — buffer drained (0 open backlog rows) but signals kept generating; fix: add an in-repo signals→backlog promoter to close the pump
- fp-? [recurrence=6] — action-list.json holds 31 `status=in-progress` rows but assignments.json has only ~1/8; fix: extend `gc1-reconcile.sh` with an action-list ↔ assignments reconciliation step
- fp-? [recurrence=4] — PR #2744 carries fixes for two recurring defects but has been draft/needs-human-review for 16 days; fix (operator): un-draft/merge to release the batched size-guard + retry-remint fixes

## Open questions
- The dispatcher's Tier-1d generator produced 15 code-review-finding signals on 2026-08-28 (across api-core services/repositories, ppt-web-core, ppt-web-ui, reality-server) — all 15 IDs landed in `state.seen_signals` but only the 3 `api-handlers` findings materialized as backlog rows. Should the routine's Phase 2 add a "revive unactualized dispatcher signals" pass (a canonical corrective refill loop), or is the current one-off manual revival (this run: FCM only) the intended behavior?
- The PM synthesis flagged `RUSTSEC-2026-0258` (h2 empty-DATA-frame DoS, gh-issue-2797) as blocking every backend PR at cargo-deny for >11 days. But 6 backend PRs merged today — the base branch's cargo-deny gate is passing. Verify whether the h2 advisory is a warning (not deny-classified) or whether the finding is stale.

## Quiet day?
- No — 6 merged PRs shipped, 7 new backlog vectors added (1 corrective revival + 2 Phase-1.5 code-review + 3 churn + 1 stalled-review), 1 plan promoted (cloud-ok, refills the starved planner buffer). Digest will send.
