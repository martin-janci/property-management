# 2026-08-29

## Since last run
- Merged PRs: 6 real code (range #2868–#2874); plus multiple dispatcher chore commits
- Open PRs touched: 3 dependabot batches (#2865/#2866/#2867) refreshed 2026-08-27; #2744 self-PR still `needs-human-review`; #2555/#2558/#2559 accounting still parked as `needs-human-judgement`
- New / updated issues: 2 (both cargo-deny chacha20-yanked follow-ups #2873/#2875 — both closed same window)
- Commits: 21 on `dev` (6 real merges + 15 dispatcher chore commits)
- Routine lag: 21h since last run — on schedule
- Phases that failed: none — Phase 1 fetched PRs/issues/commits + churn; Phase 1.5 skipped in favor of the dispatcher's Tier-1d ppt-web-core + ppt-web-ui + reality-server reviews committed 2026-08-28 (9 findings ingested here); Phase 1.6 skipped (no-op — management artifacts still valid, `pm_cursor` and `coverage_cursor` intentionally not advanced per G16 no-op path); Phase 2 ran; Phase 3 promoted 2 plans (security fast-track, score=2 + confidence=high); Phase 5 skipped (no allowlisted signal met the certainty bar)

**Context (buffer-low fire payload):** the dispatcher signalled `claimable=6/72 — all remaining open backlog is mobile-native/KMP (AGP egress-gated, unlandable in cloud). Need fresh landable (backend/frontend/web) coverage vectors.` This run explicitly addresses that: 9 landable code-review findings ingested into backlog (7 web + 2 backend), 2 promoted to `ready`.

## Shipped
- #2868 churn-hotspot `ContentModerationPage.tsx` — dedupe decision toast callbacks (dispatcher-generated refactor).
- #2874 gh-issue-2873 — bump chacha20 0.10.0 → 0.10.2 to clear cargo-deny yanked advisory (unblocked all backend PR merges).
- #2869 code-review-api-handlers-decide-appeal-unvalidated — validate `decide_appeal` decision enum + cap rationale (from last brief's readiness watch).
- #2872 churn-hotspot `auth_policy.rs` — extract shared `enforce_email_verification` seam.
- #2871 churn-hotspot `aml_dsa/moderation.rs` — extract `ModerationCaseResponse::from_case` constructor.
- #2870 code-review-api-handlers-report-content-unbounded — bound `report_content` reason length (MAX_REPORT_REASON_LEN=5000). Closes the 2026-08-28 review finding — resolution captured this run.

## Watch
- Stalled review: #2744 (draft, `needs-human-review`, self-PR archive-push fix; last update 2026-08-13 06:50 UTC — **>16d idle**). Already in `needs-human-judgement` upstream; operator-gated by design.
- Reverted: none this run.
- Churn hotspots this run (real code-touch only; excludes lockfiles, generated, `.research/management/`):
  - `backend/servers/api-server/src/routes/aml_dsa/moderation.rs` — 400 lines this run (PR #2871 dedup refactor); **runs_seen=2** → `repeated-churn` signal fires (bookkeeping trace — churn is intentional cleanup)
  - `backend/servers/api-server/src/services/auth_policy.rs` — 102 lines (PR #2872 seam extraction); **runs_seen=2** → `repeated-churn` bookkeeping trace
  - `frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.test.tsx` — 94 lines (test-side co-motion with #2868); new to hotspot_history (runs_seen=1)

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: `ppt-web-core` + `ppt-web-ui` + `reality-server` (via dispatcher Tier-1d, committed 2026-08-28 06:37 / 08:31 / 14:37 UTC — reasons: `ppt-web-core` was oldest-unreviewed pair with `api-core`; `ppt-web-ui` refreshed alongside; `reality-server` opportunistic during buffer-refill Tier-1d generator run)
- Experts: mixed (dispatcher aggregate — rust + frontend static analysis)
- Findings: 9 landable `code-review-finding` signals ingested this run (see Backlog deltas). Fact-grade evidence (line-cited) allowed routine to upgrade dispatcher-emitted `medium` confidence to `high` on 8 of 9.
- Next segment: `api-core` (last routine-reviewed 2026-08-21; oldest-unreviewed after this run — dispatcher's Tier-1d also last touched api-core 2026-08-28)
- Skipped: no — dispatcher ran three segment reviews in the window; routine ingested their signals without re-running the skill (token discipline; yesterday's brief established this convention)

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` 106 + `reality/` 47; `mobile/` still 0)
- Drift signals this run: 0 (none of the 6 merged PRs touched `App.tsx`, `routes/**`, or `reality-web/app/**`)
- Orphan screens this run: 0 (no route-basename check needed — no route file touched)
- Last `screen-map.yml` CI run on `main`: not queried this run (no `docs/screens/**` changes in window)

## Backlog deltas
- **Resolved:** [2→done] `code-review-api-handlers-report-content-unbounded` — PR #2870 verbatim-ID match + evidence-gone (validate_optional_text_field now in shared.rs).
- **Promoted (Phase 3, security fast-track — score=2 + confidence=high + vector=security):**
  - `code-review-ppt-web-core-init-no-exp-check` — new plan `plans/security-authcontext-stored-token-no-expiry-check.md`
  - `code-review-reality-server-sso-sessions-unbounded` — new plan `plans/security-reality-server-sso-sessions-unbounded.md`
- **New (from dispatcher Tier-1d + routine's own churn analysis):**
  - [2] `code-review-ppt-web-core-refresh-token-localstorage` (security, high) — refresh token in localStorage; XSS-exfiltrable long-lived credential
  - [2] `code-review-ppt-web-core-init-no-exp-check` (security, high) — cold-boot trusts stored token with no exp check → PROMOTED
  - [2] `code-review-ppt-web-core-no-runtime-token-refresh` (bug, high) — axios wired without onUnauthorized; no runtime silent-refresh
  - [1] `code-review-ppt-web-core-retry-ignores-retry-after` (bug, medium) — retry loop ignores server's Retry-After header
  - [1] `code-review-ppt-web-core-ws-jwt-in-url` (security, high) — WS URL puts JWT in ?token=; logged everywhere
  - [1] `code-review-ppt-web-ui-errorboundary-raw-message-prod` (bug, high) — global ErrorBoundary leaks raw error.message in prod (no DEV guard)
  - [1] `code-review-ppt-web-ui-authcallback-decodeuri-throw` (bug, high) — unwrapped `decodeURIComponent(error_description)` throws URIError on malformed %
  - [2] `code-review-reality-server-sso-sessions-unbounded` (security, high) — HashMap<SsoState> with no cap, no sweeper → PROMOTED
  - [2] `code-review-reality-server-sso-reqwest-no-timeout` (bug, high) — `reqwest::Client::new()` with default (no) timeout on OAuth exchange
  - [1] `churn-hotspot-…ContentModerationPage.test.tsx` — 94 lines this window
  - [1] `repeated-churn-…moderation.rs` — runs_seen=2 (bookkeeping trace only — intentional refactor)
  - [1] `repeated-churn-…auth_policy.rs` — runs_seen=2 (bookkeeping trace only)
- **Bumped:** none (no non-cumulative signal double-counted this run)
- **Decayed:** none (no open item is >14d stale — all recent)
- **Dropped:** none

## Plans promoted
- `plans/security-authcontext-stored-token-no-expiry-check.md` — cold-boot expiry-check + reroute to refreshTokenInternal · adversarial pass: passed (source→hypothesis→approach→test chain unbroken; three concrete tests specified; test would fail on `main` today since no expiry check exists; no placeholder rot).
- `plans/security-reality-server-sso-sessions-unbounded.md` — `SsoSessionStore` newtype with TTL + max-cap + tokio sweeper · adversarial pass: passed (evidence, hypothesis, files, root-cause, and test plan all reference the same `state.rs:855`/`:991` sites; alternatives explicitly considered and rejected; no placeholder rot).

## Auto-fix
- `no candidate` — no `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, or `stalled-review` signal met the certainty bar (confidence=high + score≥3 + allowlist match) this run

## Self-review findings
- none (`.research/self-improvement/findings.json` not re-evaluated by this run — dispatcher owns its Phase-8 self-review daily)

## Open questions
- Should the ppt-web-core auth findings converge into a single epic (`epic-auth-frontend-hardening`) rather than three separate PRs? The three security-related findings (refresh-token-localstorage, init-no-exp-check, no-runtime-token-refresh) all touch `AuthContext.tsx` and each depends on the others being consistent; a single reviewer pass across the three would be more efficient than sequential PRs. Not blocking today's promotions — the two promoted plans are cleanly separated (init-no-exp-check is client-only + tiny; reality-server one is backend + unrelated).

## Quiet day?
- No — 6 merged PRs shipped, 1 item resolved open→done, 12 new backlog vectors added (9 landable code-review + 3 churn/repeated-churn), and 2 plans promoted directly addressing the dispatcher's buffer-low fire. Digest will send.
