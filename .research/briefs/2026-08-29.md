# 2026-08-29

## Since last run
- Merged PRs: 6 (range #2868–#2874)
- Open PRs touched: ~7 (3 new dependabot batches #2865/#2866/#2867 opened 2026-08-27; older dependabot pile #2673/#2749/#2808/#2583–#2591 unchanged; #2744 still parked `needs-human-review`)
- New / updated issues: 2 (#2873 and #2875 — both about chacha20 0.10.0 yanked advisory; both closed by #2874 same window)
- Commits: 24 on `dev` (6 real code-touch merges + 18 dispatcher chore commits + 1 today's dispatcher Tier-1d signal commit c5673cc)
- Routine lag: 0d 23h since last run — on schedule (<36h threshold; no `lag_warning` fired)
- Phases that failed: none — Phase 1 fetched PRs/issues/commits + derived churn; Phase 1.5 delegated to dispatcher's Tier-1d api-core review committed today at 02:32 UTC (2 findings ingested); Phase 1.6 skipped (light day, artifacts still valid, `pm_cursor` and `coverage_cursor` intentionally not advanced per G16 no-op path); Phase 2 ran; Phase 3 promoted 0 plans (no candidate met readiness gate — see below); Phase 5 skipped (no allowlisted signal met certainty bar).

## Shipped
- #2868 `churn-hotspot ContentModerationPage` — dedupe moderation decision toast callbacks; extract `decisionMutationCallbacks` shared by `submitTakeAction` + `submitDecideAppeal`; tests lock toast + dialog-reset behavior. Files: `ContentModerationPage.tsx` (+40−38), `ContentModerationPage.test.tsx` (+93−1). Follow-up to #2856 Toast wiring.
- #2869 `fix(api-server) validate decide_appeal decision enum and cap rationale` — closes the `code-review-api-handlers-decide-appeal-unvalidated` backlog row; free-form `req.decision` silently rejected typo/case-mismatch appeals (e.g. `"Upheld"` mapped to rejection). Adds enum validation + `MAX_RATIONALE_LEN` cap. Files: `moderation.rs` (+7), `shared.rs` (+43 incl. inline unit tests).
- #2874 `fix(backend) bump chacha20 0.10.0 → 0.10.2` — precise `Cargo.lock` bump clears yanked-advisory that had been blocking every Rust PR merge; no manifest change, no `deny.toml` exception. Closes #2873 (and duplicate #2875).
- #2872 `refactor(api-server) extract shared email-verification seam in AuthPolicyEnforcer` — DRY-consolidate the byte-identical `require_email_verification` block from `check_membership_grant` + `check_capability_grant_for_user` into one `enforce_email_verification` helper; 2 sqlx tests lock the membership path. Pure DRY, no behavior change. File: `auth_policy.rs` (+77−25).
- #2871 `refactor(api-server) dedupe moderation response construction` — extract `ModerationCaseResponse::from_case`; 7 handlers now route through one constructor; `now` threaded so a queue page shares one timestamp and age math is unit-testable. 6 unit tests lock behavior. File: `moderation.rs` (+203−184). Follow-up to #2856/#2860 churn on this file.
- #2870 `code-review-api-handlers-report-content-unbounded` — closes the report-content backlog row; add `MAX_REPORT_REASON_LEN` (5000) + a `validate_optional_text_field` helper (caps length without requiring non-empty, since reason is optional), reject over-limit input with 400 before touching the DB. Files: `moderation.rs` (+6), `shared.rs` (+50 incl. inline unit tests).

## Watch
- Stalled review: #2744 (draft, `needs-human-review`, `fix(dispatcher) un-wedge oversize-archive push + close retry-remint cross-id ghost` — last update 2026-08-13 06:50 UTC → **16d idle**). Already parked; the operator gate holds the merge — surfaced here for continuity; no ping sent (Phase 5 declined a routine-ping on a human-gated self-PR to avoid noise).
- Reverted: none this run.
- Churn hotspots this run (real code-touch only; excludes `Cargo.lock`, locale JSON, generated files):
  - `backend/servers/api-server/src/routes/aml_dsa/moderation.rs` — 400 lines across 3 PRs (#2869 fix +7, #2870 length-cap +6, #2871 dedupe +387); `runs_seen=2` (`repeated-churn` fires)
  - `backend/servers/api-server/src/services/auth_policy.rs` — 102 lines from #2872 (email-verification DRY extract); `runs_seen=2` (`repeated-churn` fires)
  - `backend/servers/api-server/src/routes/aml_dsa/shared.rs` — 93 lines across 2 PRs (#2869 +43, #2870 +50); `runs_seen=1` (new to hotspot_history)
- Risky-churn this run: two mechanical signals fire on PR #2869 (only fix-prefixed merge with no dedicated test file in diff) intersecting both hotspot files — `risky-churn-pr-2869-moderation-rs` + `risky-churn-pr-2869-shared-rs`. Caveat noted in evidence: inline `#[cfg(test)]` tests exist in the `shared.rs` diff (`validate_decision` + `validate_optional_text_field` regression coverage); the path-based check still fires because no dedicated test *file* was added.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: `api-core` (via dispatcher Tier-1d generator, committed 2026-08-29 02:32 UTC — reason: `oldest-unreviewed` with `api-core` and `ppt-web-core` tied at 2026-08-21; tie broken toward `api-core` for higher security value; sub-files targeted (workflow engine + docgen) explicitly untouched by four earlier api-core signals)
- Experts: rust
- Findings: 2 (both `code-review-finding`, +2 each, medium confidence, vector `bug`) — `code-review-api-core-workflow-eq-type-strict-no-coercion` (Eq/Ne/In/NotIn use strict serde_json equality → string-serialized UI condition values silently never match numeric trigger fields) + `code-review-api-core-docgen-summarize-char-boundary-panic` (byte-index String slice in `summarize_document` can panic on multi-byte UTF-8 at either 200k-byte cut point — realistic for sk/cs/de/en documents)
- Next segment: `ppt-web-core` (oldest unreviewed at 2026-08-21 after this run; api-core cursor now 2026-08-29)
- Skipped: no — dispatcher's Tier-1d output ingested as this routine's Phase 1.5 (mirrors 2026-08-28's approach; avoids double-work on the same segment same day)

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` 106 + `reality/` 47; `mobile/` still 0)
- Drift signals this run: 0 (none of the 6 merged PRs touched `App.tsx`, `frontend/apps/ppt-web/src/routes/**`, or `frontend/apps/reality-web/src/app/**`)
- Orphan screens this run: 0 (no route file touched)
- Last `screen-map.yml` CI run on `main`: not queried this run (no `docs/screens/**` changes in window; expected quiet)

## Backlog deltas
- **New:**
  - [2] `bug-risky-churn-pr-2869-moderation-rs` — fix-prefixed PR touched top-3 churn file without dedicated test file (inline tests exist; caveat noted)
  - [2] `bug-risky-churn-pr-2869-shared-rs` — same PR, second hotspot intersection
  - [2] `code-review-api-core-workflow-eq-type-strict-no-coercion` — strict serde_json equality on Eq/Ne/In/NotIn contradicts coercing ordering/string operators on same field; string-vs-numeric UI condition values silently mis-guard automations
  - [2] `code-review-api-core-docgen-summarize-char-boundary-panic` — byte-index String slice in `summarize_document` panics on multi-byte UTF-8 (sk/cs/de/en) near either 200k-byte cut point
  - [1] `refactor-churn-hotspot-aml-dsa-shared-rs` — 93 lines across 2 PRs; new AML/DSA validation-seam module worth watching for consolidation
  - [1] `refactor-repeated-churn-aml-dsa-moderation-rs-2026-08-29` — runs_seen=2; 400 lines this window despite dedupe #2871 landing (next-run signal will tell whether the dedupe held)
  - [1] `refactor-repeated-churn-auth-policy-rs-2026-08-29` — runs_seen=2; 102 lines via #2872 DRY extract locked by 2 sqlx tests (expected to stabilize)
- **Bumped:** none this run.
- **Decayed:** none this run (only `code-review-mobile-native-kmp-httpclient-no-timeout` at updated 2026-08-16 approaches the 14d decay threshold; still 13d, not yet due).
- **Dropped:** none this run.

## Plans promoted
- none — no open backlog row met the readiness gate this run. The two new `code-review-api-core-*` items (score 2, `vector: bug`) sit below the +3 threshold; security fast-track doesn't apply (`vector != security`). The 4 non-terminal mobile-native/KMP items remain gated (structurally unlandable in cloud runner — AGP egress). The one `ready` item (`code-review-mobile-native-kmp-inquiries-response-contract`) already has `plans/code-review-mobile-native-kmp-inquiries-response-contract.md`.

## Auto-fix
- `no candidate` — no signal met `confidence=high` + `score >= 3` + allowlist this run. The two code-review-finding signals are `confidence: medium` (Tier-1d static-only pass); the risky-churn / repeated-churn / churn-hotspot signals are not on the allowlist; the only `stalled-review` candidate (#2744) is a self-PR labeled `needs-human-review` and a routine ping would add noise to an already-human-gated thread (declined).

## Self-review findings
- open=37 (high=9, medium=25, low=3); new this run=0; acknowledged=19
- (findings.json entries lack stable ids — displaying top-5 high-severity open by index, no `recurrence_count` bumps this run)
- high#1 — dispatcher hard-assumes `git push` is proxy-403-blocked and 64KiB MCP size-guard would false-positive block a git push the env actually permits; fix: Phase-1 push-capability probe + bypass size-guard when PUSH_METHOD=git
- high#2 — 198KB `assignments-archive.json` cannot go through MCP `push_files` inline; fix: adopt PR+API-merge path as primary large-archive push in this env (proven via PR #1163)
- high#3 — Tier-1d generator keeps firing despite backlog buffer drained; unconsumed tier1d signal files pile up (61+); fix: add signals→backlog promoter OR gate Tier-1d when unconsumed count exceeds threshold
- high#4 — `action-list.json` in-progress twin decoupled from `assignments.json` live rows (bijection broken); fix: extend `gc1-reconcile.sh` + self-test T32 asserting bijection
- high#5 — PR #2744 batches TWO recurring fixes (>64KiB push-guard regression + retry-remint ghost-retry) and blocks on `needs-human-review`; fix: operator merge #2744; systemically split self-remediation to one-fix-per-PR; add Phase 7 `remediation-blocked` line

## Open questions
- none this run.

## Quiet day?
- No (6 merges closed 2 in-flight code-review-finding backlog rows and cleared the cargo-deny advisory; 7 new backlog vectors; 2 new api-core code-review findings via Tier-1d; ~1d 0h since last routine run).
