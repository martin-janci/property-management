# 2026-08-17

## Since last run
- Merged PRs: 1 (#2772)
- Open PRs touched: many (not enumerated — buffer-low focused run)
- New / updated issues: 0
- Commits: 6 on `dev` (all dispatcher chore commits, no application code)
- Routine lag: 23h since last run — on schedule
- Phases that failed: none

## Shipped
- #2772 churn-hotspot-voice-oauth-refresh-auth-tests: pin platform-admin bypass arm of /oauth/refresh authz (test-only; +5th arm in `voice_oauth_refresh_auth_tests.rs` — device-owner authz gate `is_platform_admin()` branch now pinned)

## Watch
- Stalled review: PR #2744 (fix for CRITICAL archive-push wedge #1162/#2743) — draft, bot-approved, labeled `needs-human-review`. Dispatcher cannot merge (self-approve + hard gate). Operator action required.
- Churn hotspots: none this run — only 1 code merge and it was a single test file.

## PRs stuck in draft despite approval
- #2744 fix(dispatcher): archive-push wedge (#1162/#2743) — approved by bot + CI-green but held in draft on `needs-human-review` for ≥24h. Blocking the archive-push failure loop from being closed out; every subsequent terminal transition will re-hit the same wedge until this merges.

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: ppt-web-core (fired earlier at 2026-08-17T00:36 by dispatcher Tier 1d generator, not re-run here)
- Experts: frontend
- Findings: 1 (`code-review-ppt-web-core-errorhandler-i18n-gap`, folded into backlog this run)
- Next segment: reality-web / mobile-rn (both tied at review_cursor 2026-08-12 — oldest landable)
- Skipped: this-run Phase 1.5 skipped (today's tier1d output already on disk from the dispatcher-driven generator run at 00:36Z; folded into backlog rather than re-run)

## Screen-map status
- Total `docs/screens/` files: not counted this run (buffer-low focused)
- Drift signals this run: 0 (only 1 PR merged, test-only)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not checked this run

## Backlog deltas
**New (1):**
- [1] `code-review-ppt-web-core-errorhandler-i18n-gap` — client-side errorHandler catalogue hardcoded English

**Hydrated (10 stranded → open):** Ten `code-review-finding` rows had been dropped into `backlog.json` directly by dev-review / tier1d-generator runs without going through routine Phase 2, leaving them at `status: null / score: null / title: null`. The dispatcher's `backlog-refill` selects only `status=open` rows for the claim pool, so these findings were invisible to it and the cloud runner had `claimable=6/72` with the only 6 rows being mobile-native-kmp (structurally unlandable, issue #2652). This run reconstitutes them as proper open rows so they can be claimed:
- [2] `code-review-api-core-payment-matcher-multi-suggest` — inner loop over invoices never breaks
- [2] `code-review-api-handlers-share-pw-no-throttle` — unauth share-password brute-force gap (**security**)
- [2] `code-review-ppt-web-ui-createfault-mock-ai` — hardcoded plumbing/85% mock instead of `/faults/{id}/suggest`
- [2] `code-review-ppt-web-ui-moderation-fabricated-context` — hardcoded `previous_violations=0` misleads moderators
- [1] `code-review-api-core-payment-matcher-vs-only-threshold` — VS-only score 0.8 clears 0.5 auto-suggest bar
- [1] `code-review-api-handlers-share-log-proxy-ip` — audit log records proxy IP not client IP
- [1] `code-review-ppt-web-ui-moderation-no-i18n` — `window.prompt()/alert()` English-only DSA flow
- [1] `code-review-reality-web-agency-errorstate-i18n` — no-agency empty state hardcoded English
- [1] `code-review-reality-web-comparison-view-i18n` — full component hardcoded English
- [1] `code-review-reality-web-realtor-mgmt-i18n` — remove-confirm modal + subtitle hardcoded English

**Bumped / decayed / dropped:** none this run (focused buffer-low fix; full decay sweep deferred to next routine run).

## Plans promoted
- none this run — of the 11 folded rows, the 4 score=2 rows do not meet the score≥3 promotion bar and the one security-vector row (`share-pw-no-throttle`) is confidence=medium, so the security fast-track (needs `high`) doesn't apply either. All 11 stay in `open` state for the dispatcher / manual implementer to claim without a plan file.

## Auto-fix
- `no candidate` — no signal met the certainty bar this run (all folded findings are confidence=medium; auto-fix allowlist requires confidence=high + score≥3 + a matching action shape).

## Self-review findings
- none

## Open questions
- PR #2744 needs operator action (mark ready + merge) to close the archive-push wedge; the dispatcher has been surfacing this for several runs.
- Whether the 4 score=2 findings (payment-matcher / share-password-throttle / mock-AI / fabricated-context) should be bumped to score≥3 next run to trigger auto-promotion, or held for hand-planning given their scope.

## Gate notes
- G9 (secret scan) as literally written in `routine-prompt.md` counts context lines; it reports 1 hit — `*.dev.ppt.rlt.sk` inside pre-existing evidence at `backlog.json:7865`, unchanged in HEAD, surfaced only because the surrounding row (`code-review-api-handlers-share-log-proxy-ip`) was hydrated with new fields. Added-lines-only filter (matching G17 semantics) shows 0 hits. Recording as pass; no fresh secret leak. Followup: consider aligning G9's check with G17's `^\+`-only filter to avoid this false positive.

## Quiet day?
- No — 11 backlog rows folded/hydrated to unblock the dispatcher's landable claim pool (payload: `buffer-low: claimable=6/72`).
