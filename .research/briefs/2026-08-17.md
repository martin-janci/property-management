# 2026-08-17

## Since last run
- Merged PRs: 15 (#2773–#2788, minus still-open #2780 = 14 merged; also #2772 counted by prior run at 03:17 UTC)
- Open PRs touched: 1 (#2780 `code-review-mobile-rn-nfc-revoke-local-persists-offline` — dispatcher `verdict=approve`, mergeable_state=unstable, last update 14:46 UTC)
- New / updated issues: 1 (#2789 — security follow-up on PR #2784 from `ppt-review-merged` skill)
- Commits: 21 on `dev` (14 dispatcher fast-path merges + dispatcher chore commits + this run's forthcoming research commit)
- Routine lag: 0d 11h since last run (03:17 UTC → 14:56 UTC) — same-day rerun triggered by dispatcher `buffer-low` payload
- Phases that failed: none

_Same-day rerun_ — this brief overwrites the 03:17 UTC morning-run brief. Trigger payload: `buffer-low: claimable=6/72 landable=0 (all open items mobile-native-gated) — refill planner with cloud-landable backend/frontend/reality work`. Phase 1.5 (code review) and Phase 1.6 (PM rotation) were skipped this run — both already advanced their cursors at 03:17 UTC (`ppt-web-core` reviewed; `pm_cursor.next_index=3` unchanged). The refill work happens in Phase 2/3 below.

## Shipped
- #2773 `code-review-api-handlers-share-pw-no-throttle` — public protected-share password throttled (`share_access_key(token,ip)` bucket) — **backlog row already `done` last run; noted here for the sweep**
- #2774 `code-review-api-core-payment-matcher-multi-suggest` — matcher surfaces multiple tied suggestions (already `done`)
- #2775 `code-review-mobile-rn-nfc-accesslog-parse-unguarded` — NFC access-log `JSON.parse` guarded (already `done`)
- #2776 `code-review-ppt-web-ui-moderation-fabricated-context` — moderation stops fabricating case context (already `done`)
- #2777 `code-review-reality-web-realtor-mgmt-i18n` — RealtorManagement UI localised (already `done`)
- #2778 `code-review-ppt-web-ui-createfault-mock-ai` — CreateFault stops shipping fake AI triage (already `done`)
- #2779 `code-review-ppt-web-core-errorhandler-i18n-gap` — errorHandler catalogue via react-i18next (already `done`)
- #2781 `code-review-reality-web-agency-errorstate-i18n` — AgencyErrorStates via next-intl — **open → done this run**
- #2782 `code-review-api-core-payment-matcher-vs-only-threshold` — matcher honours IBAN tie-breaker (already `done`)
- #2783 `code-review-ppt-web-ui-moderation-no-i18n` — moderation dashboard localised (already `done`)
- #2784 `code-review-api-handlers-share-log-proxy-ip` — `resolve_client_ip()` unwinds proxy headers — **open → done this run** (⚠ follow-up issue #2789 filed — see *Backlog deltas*)
- #2785 `code-review-reality-web-comparison-view-i18n` — ComparisonView localised — **open → done this run**
- #2786 `code-review-mobile-rn-nfc-securestore-2kb-blob` — NFC creds chunked across SecureStore slots — **open → done this run**
- #2787 `repeated-churn voice_webhooks.rs: don't panic building Google JWKS HTTP client` — instability tracker `repeated-churn-…-voice_webhooks.rs` gains PR #2787 in sources; **tracker stays open** (this is yet another change to this file — churn persists)
- #2788 `code-review-api-core-voice-actions-fabricated-empty-retry1` — voice actions query real announcement/meter data

## Watch
- Stalled review: none new since last run — #2780 (open, verdict=approve, ~6h since open) and #2744 (open, verdict=approve, ~91h) both under the 7-day stalled threshold
- Reverted: none
- Churn hotspots: `backend/servers/api-server/src/routes/voice_webhooks.rs` (now runs_seen≈2 with fresh PR #2787 landing → file received 4 substantive changes in the last ~14 days; tracker stays open until churn subsides). No new hotspots this rerun — churn window unchanged.

## PRs stuck in draft despite approval
- #2744 `fix(dispatcher): un-wedge oversize-archive push (#1162) + close retry-remint cross-id ghost (#2460)` — last update 91h ago (age=03:19); assignments.json marks it `verdict=approve`, but formal GH APPROVE is blocked on self-PR (dispatcher task `gh-issue-2743`, specialist=pm-tech-lead) — carried from morning brief, unchanged
- #2780 `code-review-mobile-rn-nfc-revoke-local-persists-offline` — dispatcher `verdict=approve`, `mergeable_state=unstable`; opened 08:46 UTC, last update 14:46 UTC — still fresh (~6h), watch on next run

## PRs with verdict=changes, no fix-round progress in 24h
- none (all 4 assignments in `review` state are `verdict=approve`; the 1 with empty verdict — `code-review-api-core-voice-actions-fabricated-empty-retry1` PR #2788 — merged during this run's window)

## Code review slice
- Segment reviewed: **skipped this rerun** (`ppt-web-core` already reviewed at 03:17 UTC — `state.review_cursor.ppt-web-core = 2026-08-17`; oldest-unreviewed pair is `reality-web` + `mobile-rn` at 2026-08-12, both untouched this rerun)
- Experts: n/a
- Findings: 0 from Phase 1.5 this rerun (the 1 morning finding — `code-review-ppt-web-core-errorhandler-i18n-gap` — was already merged today as PR #2779, so it's `done` in backlog)
- Next segment: `reality-web` or `mobile-rn` (both stale at 2026-08-12; oldest-tie broken however next run picks)
- Skipped: yes — same-day rerun, cursor already advanced

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/`=106, `reality/`=47, `mobile/`=0 — mobile not yet seeded)
- Drift signals this run: 0 (the 14 merged PRs touched frontend/reality-web + frontend/apps/mobile RN + backend/api-server routes; the frontend touches were localisation/error-state updates in components — not the trigger set `frontend/apps/reality-web/src/app/**` or `frontend/apps/ppt-web/src/{App.tsx,routes/**}`)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: unknown (session cannot query Actions runs)

## Backlog deltas
- **New:** [3] `security-share-ip-spoof-follow-up-2789` — Spoofable proxy-header client IP defeats share brute-force throttle (#2773) + poisons access-log audit (#2784). Security fast-track eligible (vector=security, confidence=high, score≥2 → promoted). See plans/security-share-ip-spoof-follow-up-2789.md
- **Resolved (done):**
  - `code-review-reality-web-comparison-view-i18n` — PR #2785 (score 1)
  - `code-review-reality-web-agency-errorstate-i18n` — PR #2781 (score 1)
  - `code-review-api-handlers-share-log-proxy-ip` — PR #2784 (score 1) — closes the audit-attribution gap; the spoofing gap is now tracked separately as the new security row above
  - `code-review-mobile-rn-nfc-securestore-2kb-blob` — PR #2786 (score 2)
- **Bumped:** none this run
- **Sources added:** `repeated-churn-backend/servers/api-server/src/routes/voice_webhooks.rs` — PR #2787 added to sources (evidence line records the continued churn; tracker stays `open` at score 1)
- **Decayed:** none (no open item older than 14 days this run)
- **Dropped:** none this run

## Plans promoted
- `plans/security-share-ip-spoof-follow-up-2789.md` — trusted-proxy gate on `resolve_client_ip()` + right-to-left XFF walk; migrate `forms/submissions.rs` + `admin/*.rs` naive XFF sites in the same PR. Cloud-landable backend/api-server work — directly refills the dispatcher buffer (payload asked for `cloud-landable backend/frontend/reality work`). **adversarial pass: passed** — source → hypothesis → approach → test plan chain intact; the throttle-bypass regression test would fail against `main` today (rotating spoofed XFF opens fresh buckets); Files section paths all resolve on disk; Mode: cloud-ok; capabilities C1 + C6 ticked; no placeholder rot; G7 template checks pass (4 metadata + 11 headings + ≥1 capability + Mode declared + files exist).

## Auto-fix
- `no candidate` — the new security signal is a code-fix, which the allowlist excludes (only `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, lockfile-drift `dep-update-noise`, and `stalled-review` comments qualify). The kill switch `ROUTINE_AUTOFIX_DISABLED` is unset.

## Self-review findings
- open=34 (high=7, medium=24, low=3); new this run=0; acknowledged=19 (unchanged from morning — self-improvement/findings.json not mutated by rerun)
- fp-mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=15] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked; fix: replace hard-coded proxy-403 assumption with a runtime probe
- fp-mcp-push-cannot-inline-large-archive [recurrence=15] — Phase 6 archive-move produces oversize files that `mcp__github__push_files` can't inline; fix: rotate/shard archives, or restore a scoped `git push` route
- fp-tier1d-signals-accumulate-no-promotion-to-backlog [recurrence=8] — tier1d code-review signals accumulated in `.research/signals/*-tier1d.json` without promotion until an in-repo promoter was added
- fp-stale-in-progress-action-list-decoupled-from-assignments [recurrence=5] — action-list.json holds many status=in-progress rows but assignments.json only reflects a few live ones
- fp-implementer-sandbox-zero-pushed-branch-multigen [recurrence=3] — implementer subagents claimed rows and produced ZERO branches on origin across multiple generations
- … and 2 more (see .research/self-improvement/findings.json)

## Open questions
- The buffer-low condition (`landable=0`) is *structural* until (a) `mobile-native/` verify gate has an egress path for `dl.google.com` (AGP), or (b) the operator opens the `ppt-bridge` MCP route for mobile-native. This rerun refilled the buffer with **one** promotable cloud-landable plan (`security-share-ip-spoof-follow-up-2789`) — enough for the dispatcher to pick up one high-priority security fix. Subsequent refills depend on either (a)/(b) landing or on Phase 1.5 code-review sweeps producing more score-≥3 findings.

## Quiet day?
- No — 14 merged PRs, 4 done transitions, 1 new score-3 security vector, 1 plan promoted. Non-quiet by every criterion.
