# 2026-08-06

## Since last run
- Merged PRs: 25 (range #2653–#2683)
- Open PRs touched: 19
- New / updated issues: 0 (highest issue number unchanged at #2643)
- Commits: 150+ on `dev` (mix of merged-PR merges + dispatcher research-chore commits; last routine-committed sha `e0703d5901ef` is >150 commits back)
- Routine lag: 2d 15h since last run (routine was late — cloud scheduler drifted; not a stale-routine alert yet, but flagged as `lag_warning`)
- Phases that failed: none

## Shipped
- #2683 refactor-churn-hotspot-infra-ops-authz-backfill (BIT-268): strengthen infra_ops authz backfill assertions
- #2682 code-review-reality-server-invite-email-mismatch: enforce invite email match on accept_invitation
- #2681 code-review-api-core-idempotency-lock-cancel-leak: make idempotency advisory-lock release cancellation-safe
- #2680 code-review-api-core-llm-unscoped-idor-footguns: remove unscoped LlmDocumentRepository IDOR-footgun methods
- #2679 code-review-api-core-voice-cmd-fabricated-success: stop fabricating success on voice check-balance
- #2678 code-review-api-core-sched-notify-swallow: propagate announcement target-resolution errors
- #2677 code-review-api-core-api-call-ssrf-hostname: close DNS-hostname SSRF bypass in api_call action
- #2676 code-review-api-handlers-ai-ocr-correction-idor: scope OCR correction org to reading's owning org
- #2672 security-voice-webhook-alexa-signature-not-verified-retry1: verify Alexa voice webhook signatures
- #2671 repeated-churn-api-server-routes-auth-rs-retry1: split auth.rs into per-surface submodules
- #2670 code-review-regional-compliance-hardcoded-vote-minutes: source SK/CZ vote-minutes from real vote data
- #2669 code-review-reality-server-invite-authz: gate agency invitation creation on membership
- #2668 code-review-reality-server-ssrf-ipv4-mapped-ipv6: reject IPv4-mapped IPv6 in SSRF URL guard
- #2667 code-review-api-handlers-voice-webhook-token-authbypass: regression test for absent voice access token
- #2666 code-review-api-core-voice-report-fault-not-persisted: persist voice-reported faults instead of fake id
- #2665 code-review-api-handlers-missing-authz-article: enforce manager/author authz on news article handlers
- #2664 code-review-ppt-web-ui-portfolio-mock-data: wire portfolio dashboard to real API
- #2663 gh-issue-2662: harden voice-webhook auth device lookup (Closes #2662)
- #2661 screen-map-drift-pr-2649-ppt: reconcile financial-payments screen-map with #2649 onError toasts
- #2660 gh-issue-2658: validate voice webhook access token (broken auth)
- #2659 churn-hotspot ThreadDetailScreen.tsx: guard malformed message timestamps
- #2657 screen-map-drift-pr-2648-ppt: reconcile ppt-web screen-map
- #2656 gh-issue-2654: route ThreadDetailScreen send-error banner through i18n
- #2655 gh-issue-2652: gate mobile-native/KMP tasks at claim time (dispatcher)
- #2653 gh-issue-2651: normalise pick-target-epic epic_prefix to lowercase kebab (T21)

The 2026-08-04→08-06 window essentially cleared the code-review + SSRF/IDOR security backlog — the dispatcher acted on almost every high-confidence tier1d finding from the previous run. The dispatcher's `buffer-low` fire that woke this routine (`claimable=1/72 — planner refill requested`) is the mechanical consequence: the queue drained faster than the routine refilled it.

## Watch
- Stalled review: none — the three feature PRs open >7d (#2555, #2558, #2559 acc invoice/QR/lifecycle) are all owned by the ACC epic and appear to be a batched sprint waiting on the epic lead, not a review-decision stall. Left as-is; no `stalled-review` signal emitted.
- Reverted: none this window.
- Churn hotspots: not computed this run — the routine's per-PR file-list fetch requires a per-PR REST call that the session-scoped MCP layer would issue >20× (over budget). The prior run flagged `frontend/apps/mobile/src/screens/messages/ThreadDetailScreen.tsx` as top churn (fixed by #2659) and `backend/servers/api-server/src/routes/auth.rs` (fixed by #2671, split into submodules). Both are resolved.

## PRs stuck in draft despite approval
- none  <!-- List of open PRs where isDraft==true would appear here; the four current drafts (#2684 #2685 #2686 #2687) are all fresh (<24h since update) and reviewDecision is not APPROVED yet. -->

## PRs with verdict=changes, no fix-round progress in 24h
- none  <!-- The dispatcher's assignments.json rows have all been re-armed within the last hour (819e2cd re-armed PR #2684 followup round 1). -->

## Code review slice
- Segment reviewed: **five segments already covered by dispatcher-driven Tier-1d refills earlier today** (api-handlers, mobile-rn, ppt-web-core, reality-server, reality-server-inquiries). No additional Phase 1.5 segment run — the pre-computed tier1d files already saturated the demand.
- Experts: rust (api-handlers), frontend (ppt-web-core), react-native (mobile-rn), rust (reality-server)
- Findings: 11 net-new `code-review-finding` signals (see Backlog deltas). Highest-scoring is `code-review-ppt-web-core-ws-pong-timeout-drop` (score 3, high confidence) — promoted to plan this run.
- Next segment: `mobile-native-kmp` (last reviewed 2026-08-04) and `reality-web` (2026-08-03) are the oldest — priority for the next Phase 1.5 tick that isn't dispatcher-driven.
- Skipped: no — the pre-computed tier1d files were folded in verbatim; skill was not re-invoked because inputs would have been identical.

## Screen-map status
- Total `docs/screens/` files: unchanged from 08-04 baseline (routine did not touch `docs/screens/`; PRs #2657 and #2661 reconciled ppt-web + financial-payments earlier this window).
- Drift signals this run: 0 (the drifting PRs from the prior window — #2648 / #2649 — were both reconciled by the merged #2657 / #2661).
- Orphan screens this run: 0 (no orphan pass done this run — no route files removed in the window).
- Last `screen-map.yml` CI run on `main`: not fetched this run (would require a per-workflow REST call). Green last checked 08-04.

## Backlog deltas
- **New:** [3] `code-review-ppt-web-core-ws-pong-timeout-drop` — ppt-web WS force-closes and reconnects every ~40s because client app-level `{type:'ping'}` gets no `{type:'pong'}` reply.
- **New:** [2] `code-review-api-handlers-enhanced-chat-stub` — `POST /chat/enhanced` returns fabricated confidence/tokens/echo; no LLM call.
- **New:** [2] `code-review-mobile-rn-auth-restore-stale-token` — mobile RN restore-session path uses a stale token cache.
- **New:** [2] `code-review-ppt-web-core-ws-giveup-no-resume` — WS `maxReconnectAttempts` gives up with no auto-resume on network return.
- **New:** [2] `code-review-reality-server-inquiries-no-ratelimit` — public inquiry create endpoint has no rate-limit; spam/abuse vector.
- **New:** [2] `code-review-reality-server-listing-viewcount-hardcoded-zero` — `viewcount` returned as hardcoded 0 instead of the actual counter.
- **New:** [1] `code-review-api-handlers-ai-upstream-error-leak` — raw upstream LLM/OAuth error text is forwarded verbatim into client-facing error body.
- **New:** [1] `code-review-mobile-rn-biometric-prompt-i18n` — biometric prompt copy is hardcoded English.
- **New:** [1] `code-review-reality-server-inquiry-email-stub` — inquiry email dispatch is a stub / never fires.
- **New:** [1] `code-review-reality-server-orphan-listinghandler-stub-detail` — orphan ListingHandler detail path is a stub.
- **New:** [1] `code-review-reality-server-orphan-schedule-viewing-stub` — orphan schedule-viewing route is a stub.
- **Bumped:** none (all 11 tier1d signal ids are net-new).
- **Decayed:** none — the only prior-open item (`code-review-mobile-native-kmp-portfolio-analytics-caps-100`) is 2 days old, well inside the 14-day decay window.
- **Dropped:** none.

## Plans promoted
- `plans/code-review-ppt-web-core-ws-pong-timeout-drop.md` — remove the ppt-web client-side app-level WS ping/pong; rely on protocol-level ping + browser auto-pong for liveness · adversarial pass: passed

Only 1 promotion this run: it is the sole `open` item that clears the standard readiness gate (`score >= 3`, `confidence == high`, concrete file evidence, plausible test plan, unique stem). The next-highest candidates (`code-review-api-handlers-enhanced-chat-stub`, `code-review-reality-server-listing-viewcount-hardcoded-zero`) sit at score 2 with high confidence; they need one more corroborating signal to promote, and neither is a `security` vector so the fast-track doesn't apply. Leaving them at `open` for the next run is intentional — quality over volume.

## Auto-fix
- `no candidate` — no signal met confidence=high + score>=3 + allowlist this run. (The one high-confidence high-score signal, `code-review-ppt-web-core-ws-pong-timeout-drop`, is a code-review finding — not in the Phase 5 allowlist. Allowlisted shapes are `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, `stalled-review` — none fired this run.)

## Self-review findings
- none

## Open questions
- The three ACC-epic feature PRs (#2555 / #2558 / #2559) are all 7.5 days old with no reviewDecision. Not scoring as `stalled-review` because they appear to be a coordinated epic-batch waiting on the ACC lead, but confirm this is intended pacing rather than an accidental review-gap.

## Quiet day?
- No — 25 PRs merged, 11 new backlog vectors, 1 plan promoted.
