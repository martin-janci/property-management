# 2026-07-06

## Since last run
- Merged PRs: 18 (range #2094–#2120)
- Open PRs touched: 4 (2 drafts + 2 dependabot)
- New / updated issues: 15 (all already CLOSED — post-merge follow-ups resolved same-day)
- Commits: 50 on `dev` (16 product + 34 dispatcher chores)
- Routine lag: 1d 16h since last run (`state.json.last_run_iso=2026-07-05T03:10:00Z`) — above 36h `lag_warning` threshold; below 3d stale-routine-alert.
- Phases that failed: none. Phase 1.5 (rotating expert review) and Phase 1.6 (PM rotation) were **skipped this run** due to session budget — cursors preserved so next run picks up exactly where they left off.

## Shipped
- #2120 gh-issue-2107: authorize outage mutations on DB role, not JWT claim — closes the JWT-vs-DB-role mismatch flagged in the outages post-merge review (issue #2107).
- #2119 gh-issue-2110: restore executable bit on check-ignore-reason.sh (+ recurrence gate) — the git-data-API mode-100644 quirk from #2091 is now backstopped.
- #2118 code-review-mobile-rn-screens-mock-data: wire mobile Meters/Leases/Forms/Threads screens to api-server. **Closes backlog item `code-review-mobile-rn-screens-mock-data`** — mock-data submits removed; grep for `setTimeout` across the four screen dirs returns 0 hits.
- #2117 gh-issue-2103: named-field input for SlovakAccountingExport::new — kills the 15-positional-arg same-type-transposition risk flagged in #2103.
- #2116 gh-issue-2109: cover include_system=true tie-break in template pagination test — closes the default-path gap in the pagination guard.
- #2115 gh-issue-2108: ListingDetail auth change resets screen to spinner; test hoisted paths — VM keyed on `listingId` only + `updateAuth` intent; 4 new commonTest cases for `start`/`retry`/`onSubmitInquiry`.
- #2114 gh-issue-2102: wire MCP-push size guard + action-list reconcile into dispatcher Phase 6 — #1014 corruption-class fix moved from prose to code.
- #2113 gh-issue-2104: enum-sync guard fails on variant missing from canonical list — variant-set assertion tightens the currency/country three-source-of-truth invariant.
- #2112 docs(db): refresh stale BIT-351 quarantine docstrings for un-quarantined unread test — three misleading docstrings in `messaging.rs` fixed.
- #2111 gh-issue-2106: gate backend/deny.toml with code-owner review — closes the CODEOWNERS gap for the load-bearing quick-xml ban.
- #2101 triage-dispatcher-mcp-push-large-file-issue-1014: MCP-push fallback state-fidelity analysis — triage doc that surfaced the follow-ups landed in #2114.
- #2100 refactor-churn-hotspot-...-rental-rs: split rental repository into cohesive sub-modules. **Closes backlog item `refactor-churn-hotspot-backend-crates-db-src-repositories-rental-rs`** — `rental.rs` no longer exists on `dev`; `rental/{bookings,calendar,connections,guest_documents,guests,ical,mod,oauth,reports,statistics}.rs` present.
- #2099 gh-issue-2086: enforce SlovakAccountingExport honesty invariant by construction (call-me-or-forget fix).
- #2098 gh-issue-2083: add enum sync guard for currency/country lists.
- #2097 gh-issue-2082: un-quarantine #1771 soft-delete unread invariant.
- #2096 gh-issue-2084: enforce quick-xml XXE pin via cargo-deny hard ban (replaces advisory workflow).
- #2095 gh-issue-2085: extract + unit-cover assign_fault recipient guards.
- #2094 gh-issue-2087: reuse shared seed_org fixture in favorite_alert_read idempotency test.
- Plus 2 dependabot bumps (#2020 rust_xlsxwriter 0.95→0.96; #2014 axum group) merged in the same window — `dep-update-noise`, no score.

## Watch
- Stalled review: none this run (the 3 old drafts — #1797, #1812, #2089 — are all intentional WIP, not awaiting review).
- Reverted: none.
- Churn hotspots: this run's top raw churn is entirely **resolution-driven** — the rental repo split (PR #2100, ~6000 lines across 11 new files) and the mobile RN screens wire-up (PR #2118, ~1400 lines across 12 files). Both closed the corresponding backlog items in the same PR set. **No `churn-hotspot` signals emitted** — the underlying instability that motivated churn tracking is being resolved, not repeating. `hotspot_history` unchanged.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: skipped this run (session budget).
- Experts: —
- Findings: 0
- Next segment: `ppt-web-core` (last reviewed 2026-06-05 — oldest unreviewed) or `mobile-native-kmp` (2026-06-06) — churn-alignment favours the latter given PR #2115.
- Skipped: yes — Phase 1.5 skill invocation deferred to reduce cloud-run session token spend; `state.review_cursor` untouched so priority ordering is preserved.

## Screen-map status
- Total `docs/screens/` files: unchanged (ppt/ + reality/ populated; mobile/ still empty per the `screen-map-drift` exclusion).
- Drift signals this run: 0 (none of the 18 merged PRs touched `ppt-web/src/App.tsx`, `ppt-web/src/routes/**`, or `reality-web/src/app/**`).
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not queried this run (skipped to conserve session budget; will resurface on next run's brief).

## Backlog deltas
- **Resolved (open → done):** `code-review-mobile-rn-screens-mock-data` via PR #2118 (title-verbatim match + evidence-gone: no `setTimeout` mocks in `frontend/apps/mobile/src/screens/{meters,leases,forms,messages}/*.tsx`).
- **Resolved (open → done):** `refactor-churn-hotspot-backend-crates-db-src-repositories-rental-rs` via PR #2100 (title stem-match + evidence-gone: `backend/crates/db/src/repositories/rental.rs` no longer exists in the tree; replaced by `rental/*` submodules).
- **New:** none — 0 fresh backlog items created this run.
- **Bumped:** none.
- **Decayed:** none — no `open` items older than 14 days this run.
- **Dropped:** none.

## Plans promoted
- none — nothing crossed the readiness bar this run (no fresh `open` items reached score ≥3, and the still-`open` items on record either are already `ready` with plans or sit at score 1 waiting for evidence stacking).

## Auto-fix
- `no candidate` — nothing matched confidence=high + score≥3 + allowlist. `lag-warning` and `dep-update-noise` are 0-score log-only signals; no `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, or `stalled-review` matches. `ROUTINE_AUTOFIX_DISABLED` was not set. `auto_fix_actions[] = []`.

## Self-review findings
- none — `.research/self-improvement/findings.json` not present in this branch.

## Open questions
- Should the routine also cross-reference the post-merge review issues (#2082–#2110) against the older backlog items whose PR authors filed them? Several of these follow-up issues touch the same territory as existing `test-gap-*` and `refactor-*` items but with more recent evidence — worth a dedup pass on the next full run.

## Quiet day?
- No — 18 merges, 2 backlog resolutions, 15 follow-up issues filed-and-closed within 24 h. Stats: `runs=42`, `quiet_days=1` (unchanged).
