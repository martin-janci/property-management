# 2026-07-06

## Since last run
- Merged PRs: 18 (range #2094–#2120; plus 2 dependabot #2020 rust_xlsxwriter, #2014 axum-test group)
- Open PRs touched: 3 substantive (#2089 draft lease_units_join test, #1797 draft OCR-auth/rental-PII, #1812 draft reality_portal split) + 2 dependabot (#2016 rust-toolchain, #2018 aes-gcm)
- New / updated issues: 0 new open. 15 follow-up / from-merged-review issues closed by their matching PR merges (#2102–#2110, #2082–#2087).
- Commits: 50 on `dev` (mostly dispatcher chore rows; ~18 substantive product merges).
- Routine lag: 1d 8h since last run — on schedule.
- Phases that failed: none.

## Shipped
- #2120 authorize outage mutations on DB role, not JWT claim — pattern fix that seeds the pm-security sweep.
- #2119 restore executable bit on `check-ignore-reason.sh` + add `script-exec-bit-gate.yml` recurrence guard.
- #2118 wire mobile RN Meters/Leases/Forms/Threads to api-server (partial resolve of `code-review-mobile-rn-screens-mock-data` — Notifications+PersonMonths still hold MOCK_).
- #2117 named-field input struct for `SlovakAccountingExport::new` (kills the 15-arg positional transposition risk from #2103).
- #2116 sibling test for `include_system=true` tie-break path in `import_templates` pagination.
- #2115 key `ListingDetailViewModel` on listingId only + `updateAuth` intent (fixes the auth-reset regression; adds `ListingDetailViewModelTest.kt`).
- #2114 wire MCP-push size guard + action-list reconcile into dispatcher Phase 6 (root-cause for the #1014 corruption class).
- #2113 drive currency/country enum-sync guard off `EnumIter` variant set (closes the "list-omission survives compile" gap from #2104).
- #2112 refresh three stale BIT-351 quarantine docstrings in `messaging.rs`.
- #2111 gate `backend/deny.toml` with CODEOWNERS (secures the load-bearing quick-xml ban from #2096).
- #2100 split `backend/crates/db/src/repositories/rental.rs` into `rental/{mod,bookings,calendar,connections,guest_documents,guests,ical,oauth,reports,statistics}.rs` (~5866 LOC moved).
- #2101 triage doc for issue #1014 (MCP-push fallback state-fidelity) — analysis-only, sibling PR #2114 lands the actual wiring.
- Wave from 2026-07-05T08:07 UTC: #2094 (fixture reuse), #2095 (`assign_fault` recipient guards), #2096 (quick-xml XXE cargo-deny hard ban), #2097 (un-quarantine #1771 soft-delete unread invariant), #2098 (initial currency/country enum-sync guard), #2099 (`SlovakAccountingExport` enforce-by-construction).

## Watch
- No stalled non-draft PRs > 7 days. Two draft PRs (#1797 13d, #1812 12d) — pm-security flags #1797 as likely-superseded (content in tree via later PRs); recommends close-as-superseded.
- No revert PRs in window.
- Churn hotspots this run (top-3 non-excluded raw):
  - `backend/crates/db/src/models/multi_currency.rs` (3 touches — #2098/#2113/#2117, runs_seen=1)
  - `backend/crates/db/src/models/regional_compliance.rs` (3 touches — #2099/#2117, runs_seen=1)
  - `backend/servers/api-server/src/routes/regional_compliance.rs` (3 touches — #2099/#2117, runs_seen=1; also the source of all three code-review findings this run)
- Cumulative re-emit: `backend/crates/db/src/repositories/rental.rs` — planned split completed by #2100; file deleted from tree, backlog row moved to `done`.
- `repeated-churn-...-rental-rs` fired (runs_seen=2) but score suppressed (+0) — the second run's churn was the planned split, not organic instability.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: `api-handlers` (reason: churn-aligned — two of top-3 hotspots this run were `backend/servers/api-server/src/routes/regional_compliance.rs` and its DB-model sibling)
- Experts: rust + security
- Findings: 3 (2 vector=bug high-confidence, 1 vector=security high-confidence — see backlog deltas)
- Next segment: `ppt-web-core` (oldest unreviewed after this run — 2026-06-05)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 148 (`ppt/` 101 + `reality/` 47; `mobile/` not yet seeded → 0; there is a separate `reality-mobile/` bucket with 15 KMP-mobile screens that is orthogonal to the RN mobile drift-detection scope).
- Drift signals this run: 0 (no PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`; mobile RN routes changed via #2118 but mobile is excluded until `docs/screens/mobile/` is seeded).
- Orphan screens this run: 0 (no routes touched → no orphan check needed).
- Last `screen-map.yml` CI run on `main`: success at 2026-06-04 (run #1111). Workflow has not fired against `main` in 32 days — no route/doc changes on `main` have triggered it. Not a regression, but worth noting.

## Backlog deltas
- **New:** [3] `code-review-api-handlers-compliance-minutes-stubbed` (bug) — Slovak + Czech minutes/usneseni handlers ship hardcoded meeting data.
- **New:** [3] `code-review-api-handlers-gdpr-fake-dpo` (security) — GDPR consent status falls back to a fabricated `Jan Novak / dpo@example.sk` DPO.
- **New:** [2] `code-review-api-handlers-vote-validator-fake-defaults` (bug) — vote validators invent 75%/80% defaults on eligibles=0 or unparseable results.
- **New:** [1] `refactor-churn-hotspot-backend-crates-db-src-models-multi-currency-rs` (refactor) — 3 touches, enum-sync scaffolding.
- **New:** [1] `refactor-churn-hotspot-backend-crates-db-src-models-regional-compliance-rs` (refactor) — 3 touches, `SlovakAccountingExport` iteration.
- **New:** [1] `refactor-churn-hotspot-backend-servers-api-server-src-routes-regional-compliance-rs` (refactor) — 3 touches + 3 quality findings.
- **Bumped:** [none]
- **Decayed:** [none] (all open items were touched within 14 days; nothing crossed the decay threshold).
- **Resolved:** `refactor-churn-hotspot-backend-crates-db-src-repositories-rental-rs` → `done` — PR #2100 landed the split, file deleted from tree (evidence-gone applies).
- **Partial:** `code-review-mobile-rn-screens-mock-data` — PR #2118 title matches the item id verbatim (PR-body match) but `NotificationsScreen.tsx` + `PersonMonthsScreen.tsx` still hold `MOCK_` arrays; item stays `open` with a `[partial]` evidence line.

## Plans promoted
- `plans/code-review-api-handlers-compliance-minutes-stubbed.md` — Fix stubbed `SlovakVoteMinutes` / `CzechSvjUsneseni` handlers: parse real vote results, compute participation from DB, source meeting date/location from the poll row, add manager gate on the read. · adversarial pass: passed
- `plans/code-review-api-handlers-gdpr-fake-dpo.md` — Kill the `"Jan Novak / dpo@example.sk"` fallback in `get_gdpr_consent_status`: widen `dpo_contact` to `Option`, short-circuit when config missing, add source-level regression guard. · adversarial pass: passed

## Auto-fix
- no candidate — none of this run's signals matched the allowlist (screen-map-drift / screen-map-orphan / fixme-in-merged-code / dep-update-noise lockfile-drift / stalled-review). The two dep-update-noise signals (#2014, #2020) are Cargo bumps, not lockfile-drift on a consumer manifest — Phase 5 rightly stays out.

## Self-review findings
- open=8 (high=2, medium=4, low=2); new this run=0; acknowledged=19
- fp-mcp-push-cannot-inline-large-archive [recurrence=5] — Phase 6 archive-move produced a 198 KB `assignments-archive.json`; `mcp__github__push_files` inline limit + proxy-403 on plain `git push` mean the archive can't be pushed via the current path. fix: adopt the PR + API-merge path (push a NEW branch + `mcp__github__create_pull_request` + `mcp__github__merge_pull_request`) as the primary push mechanism for the dispatcher state commit — landing PR #1163 (sha 08ab915) via this path proved it out.
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer reported claimable=54 but real claimable=0; open action-list items already terminal in archive. fix: extend `gc1-reconcile.sh` to auto-close open action-list items whose exact task_id is terminal in active+archive (for any status, including failed); fix Phase 2.6 buffer-guard + goal-gate GC3 formulas to subtract archive task_ids too; extend self-test T15 with a guard that asserts `open_claimable` excludes archive-present ids.

## Open questions
- Are the compliance-minutes hardcoded stubs a known "spec-in-progress" state (i.e. Epic 72 is still mid-rollout and the stubs are placeholders the team knew about), or shipped past review by mistake? The Root-cause trace in the promoted plan defers to `git log -L` to identify the introducing commit; a quick answer from the pm-tech-lead or product owner would prune scope on the fix.
- pm-security flagged a **34-site** JWT-role vs DB-role anti-pattern spanning Epic 6 / 7A / 8A mutations (PR #2120 is one instance of the correct fix). Should this be one sweeping plan under `pm-backend`, or 34 focused per-route plans? The `role_focus: pm-security` handoff to `pm-backend` is in `.research/management/roles/pm-security.md`; leaving this open for the next PM rotation.

## Quiet day?
- No — 3 new score-≥2 items (2 promoted), 6 lower-priority new items, 1 resolution, 1 partial. Telegram digest will send.
