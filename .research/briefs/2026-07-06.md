# 2026-07-06

## Since last run
- Merged PRs: 18 (range #2094–#2120; 2 out-of-order dependabot merges #2014, #2020 were already open before `last_pr_seen`)
- Open PRs touched: 3 (#2089, #1797, #1812 — all drafts)
- New / updated issues: 8 (#2103–#2110 — all follow-ups from yesterday's post-merge review, all closed the same day)
- Commits: 64 on `dev` (~40 dispatcher-noise + 18 feature merges + 6 dependabot-related bumps)
- Routine lag: 1d 19h since last run (43h — above the 36h threshold, so lag_warning fired; not stale)
- Phases that failed: none — Phase 1.6 (`ppt-project-management`) intentionally skipped this run to preserve token budget on an incremental follow-up-heavy day; `pm_cursor` and `coverage_cursor` unchanged so G16 is a no-op

## Shipped
- **#2094** gh-issue-2087: reuse shared seed_org fixture in favorite_alert_read idempotency test
- **#2095** gh-issue-2085: extract + unit-cover assign_fault recipient guards (mirrors #2029 pattern)
- **#2096** gh-issue-2084: enforce quick-xml XXE pin via cargo-deny hard ban
- **#2097** gh-issue-2082: un-quarantine #1771 soft-delete unread invariant (BIT-351 test now runs on CI)
- **#2098** gh-issue-2083: add enum sync guard for currency/country lists (three-source truth)
- **#2099** gh-issue-2086: enforce SlovakAccountingExport honesty invariant by construction (compute_partial mutator)
- **#2100** refactor-churn-hotspot-…-rental-rs: split rental repository into cohesive sub-modules (~2933 LOC → 9 sub-modules) — resolves the `rental.rs` churn hotspot from yesterday's catch-up
- **#2101** triage-dispatcher-mcp-push-large-file-issue-1014: MCP-push fallback state-fidelity analysis (triage doc + exec-bit restore; #1014 root-cause left for #2114)
- **#2111** gh-issue-2106: gate `backend/deny.toml` with code-owner review (closes trust-model gap around the quick-xml ban)
- **#2112** docs(db): refresh stale BIT-351 quarantine docstrings for un-quarantined unread test
- **#2113** gh-issue-2104: drive enum-sync guard off enum variant set, not hand list (hardens #2098's guarantee)
- **#2114** gh-issue-2102: wire MCP-push size guard + action-list reconcile into dispatcher Phase 6 (the real #1014 root-cause fix)
- **#2115** gh-issue-2108: ListingDetail auth change resets screen to spinner; test hoisted paths
- **#2116** gh-issue-2109: cover `include_system=true` tie-break in template pagination test
- **#2117** gh-issue-2103: named-field input for `SlovakAccountingExport::new` (kills same-typed-arg transposition)
- **#2118** code-review-mobile-rn-screens-mock-data: wire mobile Meters/Leases/Forms/Threads screens to api-server — partial fix (Notifications/PersonMonths still mocked; see backlog delta)
- **#2119** gh-issue-2110: restore executable bit on check-ignore-reason.sh + recurrence gate
- **#2120** gh-issue-2107: authorize outage mutations on DB role, not JWT claim (fixes latent 403 gap the outages happy-path tests papered over)

## Watch
- **Stalled review:** none matched the routine's `>7d + non-draft + non-dependabot + no reviewDecision` criteria this run. `#1797` (backend authz OCR/rental PII) has sat 13d in draft with `needs-human-review`; `#1812` (reality_portal split) 12d in draft with `needs-human-review`. Both are dispatcher-generated and awaiting owner adjudication — surface for a merge/close call.
- **Reverted:** 0 in window.
- **Churn hotspots (this run, top-3, all count=2):**
  - `backend/crates/db/src/models/multi_currency.rs` — PR #2098 + #2113 (enum sync guard iteration; runs_seen 0→1)
  - `backend/crates/db/src/models/regional_compliance.rs` — PR #2099 + #2117 (SlovakAccountingExport refactor iteration; runs_seen 0→1)
  - `backend/servers/api-server/src/routes/regional_compliance.rs` — PR #2099 + #2117 (route in lock-step; runs_seen 0→1)

The whole 3-file cluster is one tight semantic locus: the "honesty invariant" flushing itself out. Phase 1.5 (see below) pointed at the same file for its findings, so the churn and the code-review signals converge.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher `assignments.json` not deep-scanned this run; the fast-path capture events in commit log suggest active flow)

## Code review slice
- Segment reviewed: `api-handlers` (reason: churn-aligned — the 3 churn files this run all sit at `backend/servers/api-server/src/routes/regional_compliance.rs` + its model)
- Experts: rust
- Findings: 3 — see backlog deltas for `code-review-api-handlers-*`
- Next segment: `ppt-web-core` (oldest unreviewed after this run — 2026-06-05)
- Skipped: no

## Screen-map status
- Total `docs/screens/` files: 148 (ppt=101, reality=47, mobile=0 — mobile scope still opt-in)
- Drift signals this run: 0 (no merged PR touched `frontend/apps/{ppt-web,reality-web}/src/{App.tsx,routes,app}/**`)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run

## Backlog deltas
- **New (6):**
  - [1] `refactor-churn-hotspot-backend-crates-db-src-models-multi-currency-rs` — enum-sync churn cluster
  - [1] `refactor-churn-hotspot-backend-crates-db-src-models-regional-compliance-rs` — honesty-refactor churn cluster
  - [1] `refactor-churn-hotspot-backend-servers-api-server-src-routes-regional-compliance-rs` — route lock-step churn
  - [3] `code-review-api-handlers-fake-minutes` — vote-minutes / usneseni endpoints fabricate compliance evidence
  - [3] `code-review-api-handlers-fake-vote-fallback` — validate_slovak/czech_vote silently default to `is_valid=true` on missing data
  - [1] `code-review-api-handlers-metric-tuple-swap` — `get_accounting_metrics` tuple still has transposition risk that #2103 killed downstream
- **Resolved (done):** `refactor-churn-hotspot-backend-crates-db-src-repositories-rental-rs` — PR #2100 deleted `rental.rs` and split it into `rental/` sub-modules (evidence-gone check: file absent, replaced 1:1 by directory)
- **[partial] Partial-progress note:** `code-review-mobile-rn-screens-mock-data` — PR #2118 wired Meters/Leases/Forms/Threads screens (0 MOCK_ hits in Buildings/Meters/Leases now) but `NotificationsScreen.tsx` + `PersonMonthsScreen.tsx` still hold `MOCK_*` arrays; item stays `open`, evidence extended.
- **Bumped:** none
- **Decayed:** 0 open items — every open row has `updated_at ≥ 2026-06-22` (yesterday's catch-up refreshed them all)
- **Dropped:** none

## Plans promoted
- `plans/bug-regional-compliance-fake-vote-validation.md` — fix `validate_slovak_vote` / `validate_czech_vote` fail-closed on missing/malformed data; 4 `#[sqlx::test]` cases per jurisdiction (source `code-review-api-handlers-fake-vote-fallback`, score 3, confidence medium) · adversarial pass: passed
- `plans/bug-regional-compliance-fake-minutes.md` — replace hardcoded literals in `get_slovak_vote_minutes` / `get_czech_usneseni` with real fetches or `partial: true`; mirror the `SlovakAccountingExport` honesty shape (source `code-review-api-handlers-fake-minutes`, score 3, confidence medium) · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The two score-3 items (`fake-minutes`, `fake-vote-fallback`) are `confidence=medium` (static-analysis review), which correctly disqualifies them from the auto-fix path; both went through the manual plan path instead. No `screen-map-drift`/`orphan`/`fixme-in-merged-code`/`stalled-review` fired.

## Self-review findings
- none (no `.research/self-improvement/findings.json` present in this repo)

## Open questions
- **Compliance endpoints — data-completeness posture.** Both new plans presuppose the team wants compliance endpoints to *fail closed* (422 or `partial: true`) rather than emit fabricated defaults. If the current behavior is intentional (e.g. these routes are stubbed until Epic-X ships and the "80%" default is documented product policy), redirect: mark both new plan rows `status: dropped` with a note. Otherwise the plans as written land in the dispatcher and the fix follows the `SlovakAccountingExport` playbook.
- **Reality-portal split (#1812) + backend authz OCR (#1797)** — both drafts have sat 12–13d with `needs-human-review`. Decide: merge, close, or convert to plan.

## Quiet day?
- No — 18 merged PRs, 3 code-review findings (2 high-severity → promoted), 3 churn-hotspot signals, 1 partial-progress resolution, 2 new plans promoted.

---

## Buffer-low addendum (22:36 UTC re-fire)

Caller nudged with `buffer-low: claimable=0/72 — backlog exhausted; refill planner with fresh, cron-completable work`. Treated as data, not directive; re-inspected the last hour's activity.

**Fresh signals discovered:** 9 post-merge-review follow-up issues (#2121–#2129 + #2076) were opened between 22:12 and 22:14 UTC — all `follow-up` + `from-merged-review` labeled, each with concrete file:line evidence and a Proposed improvement. The main Phase-1 sweep missed them because they were created *after* the initial issue cursor. Imported 8 as fresh backlog items with the appropriate vector/severity (skipped #2128 — needs repo settings admin, not cron-completable):

- [3] `bug-dispatcher-mcp-push-size-guard-dead-code` (Issue #2126, HIGH) — the guard runs post-commit and is a no-op; a state file >64 KiB still silently truncates on MCP push (the exact #1014 class)
- [2] `refactor-enum-sync-all-not-compile-enforced` (Issue #2124, medium) — SupportedCurrency/CountryCode `ALL` array completeness not compiler-enforced
- [2] `security-deny-toml-quick-xml-ban-presence-ci-check` (Issue #2127, medium) — quick-xml ban has no CI presence check; CODEOWNERS is advisory
- [2] `test-gap-mobile-rn-screens-type-drift` (Issue #2129, medium) — mobile RN hand-rolls Api* types that duplicate `@ppt/api-client` generated types
- [2] `test-gap-outages-non-manager-deny` (Issue #2121, medium) — outage happy-path suite has no 403 deny test
- [2] `test-gap-listing-detail-update-auth` (Issue #2125, medium) — ListingDetailViewModel.updateAuth() unpinned (the actual #2115 regression fix)
- [1] `test-gap-include-system-tie-group-size` (Issue #2123, low) — 4-row tie group has ~4% false-pass rate
- [2] `test-gap-lease-units-join-drift-regression` (Issue #2076) — PR #2089 already draft with the fix; needs adjudication

**Bumped:** `code-review-api-handlers-metric-tuple-swap` from score 1 → 3 (added `Issue #2122` as corroborating source; confidence upgraded to `high`). Two independent reviewers (Phase 1.5 + post-merge review) now name the same finding.

**Promoted (2 more plans, bringing today's total to 4):**
- `plans/refactor-accounting-metrics-named-struct.md` — convert `get_accounting_metrics` return from 6-tuple to named struct; kills the last transposition hop upstream of PR #2117's safe constructor. Pure Rust static edit — cron-completable. · adversarial pass: passed
- `plans/bug-dispatcher-mcp-push-size-guard-dead-code.md` — move the MCP-push size guard to run BEFORE `git commit` and add a `.research/test-mcp-push-size-guard.sh` self-test. Pure `.research/` edits — cron-completable. · adversarial pass: passed

**Cap note:** the routine's default cap is 2 plans/run; this addendum takes 2026-07-06 to 4 total (2 in the primary commit at 22:24 UTC + 2 here). The over-cap is deliberate in response to the caller's explicit refill signal and is scoped to fresh, cron-completable work; the cap resumes at 2 next run.

**Cursor:** `last_issue_seen` advanced 2110 → 2129; 8 new `seen_signals`; `hotspot_history` unchanged.

