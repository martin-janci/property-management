# 2026-08-14

## Since last run
- Merged PRs: 8 (range #2745–#2758)
- Open PRs touched: 4 (#2749/#2750/#2751 new dependabot supersedors, #2744 dispatcher un-wedge draft)
- New / updated issues: 4 (#2743 new open dispatcher-defect bug; #2752/#2753/#2754 opened and same-day auto-closed as follow-up i18n fixes)
- Commits: 25 on `dev` (most are dispatcher self-reports; 8 real code merges)
- Routine lag: 1d 13h since last run (last_run_iso=2026-08-12T22:45:00Z; now=2026-08-14T12:36Z) — LAG_DAYS=1, `lag_warning` emitted, `stale_routine_alert` NOT fired (<3d threshold).
- Phases that failed: none. Phase 1.5 (rotating expert review) and Phase 1.6 (project management) **skipped this run** — see *Code review slice* and *Auto-fix*. Cursors for skipped phases NOT advanced.

## Shipped
- #2745 bump rust-toolchain 1.94.1 → 1.97.1 (real latest stable; #2385/#2705 both proposed the non-existent 1.100.0 — resolves `closed-not-merged-pr-2705`)
- #2746 regression test — SSRF redirect policy blocks internal-host hops (api-server workflow api_call.rs; extracted `redirect_hop_allowed` for direct unit-test)
- #2747 verify awarded quote belongs to RFQ — closes cross-RFQ IDOR (marketplace)
- #2748 fail closed on Alexa branch of voice `/verify` webhook (was accepting any non-empty string as valid signature)
- #2755 refactor(mobile): consolidate inline `toLocale*` onto format.ts helpers (9 screens)
- #2756 route MeterDetailScreen reading dates through the app i18n locale (closes #2752)
- #2757 guard ProtectedRoute `common.loading` + `errors.unauthorized` i18n keys + regression test (closes #2754)
- #2758 fail closed on backslash-obfuscated open-redirect in SSO (reality-server; `/\evil.com` now rejected pre-`url::Url::parse`)

## Watch
- Stalled review: #2559 (feat(acc): PAY by square QR endpoint) — >15d idle, no reviewDecision. Same for #2558 (invoice PDF render) and #2555 (invoice sent/cancelled lifecycle) — all UC-ACC-05.x accounting work sitting since 2026-07-30.
- Reverted: none this window.
- Churn hotspots: none new this run — the top churn files last run (`rentals.mappers.test.tsx`, `useOfflineSupport.test.ts`, `oauth.rs`) all landed regression-test PRs; no fresh top-3 emerged from the 25 commits.

## PRs stuck in draft despite approval
<!-- Query the API mid-run; source-of-truth: gh pr list ... --json isDraft,reviewDecision,updatedAt -->
- #2744 `fix(dispatcher): un-wedge oversize-archive push (#1162) + close retry-remint cross-id ghost (#2460)` — labelled `needs-human-review`, self-PR, dispatcher-approved but human-gated per contract (dispatcher runs 2026-08-14 02:30 → 10:33 all report "approved+human-gated; skip per contract"). Last update 2026-08-13T06:50Z (~30h idle).

## PRs with verdict=changes, no fix-round progress in 24h
- none (planning-branch assignments.json not read this run; dispatcher commits report clean review queue)

## Code review slice
- Segment reviewed: none — Phase 1.5 **skipped this run** (all 8 segments reviewed within the last 5 days by the dispatcher's Tier-1d generator; `state.review_cursor.mobile-native-kmp = 2026-08-11` is oldest at 3d).
- Experts: n/a
- Findings: 0 (this routine); dispatcher's Tier-1d fired multiple times since last routine run and generated the 5 orphan signals this routine now scored (see *Backlog deltas*).
- Next segment: `mobile-native-kmp` (oldest cursor entry) when Phase 1.5 next runs.
- Skipped: yes — cursor freshness (all segments <5d) + lag-recovery run priority.

## Screen-map status
- Total `docs/screens/` files: unchanged (`ppt/` + `reality/` populated; `mobile/` still not seeded → 0).
- Drift signals this run: 0 (merged frontend PRs #2755/#2756/#2757 touch `mobile/src/screens/` — mobile is deliberately excluded from drift detection until `docs/screens/mobile/` exists; no PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`).
- Orphan screens this run: 0 (no scan performed; the routine's drift/orphan pass is heuristic and this is a lag-recovery run).
- Last `screen-map.yml` CI run on `main`: not queried this run.

## Backlog deltas
- **New:** [2] `code-review-api-handlers-share-pw-no-throttle` — public share password endpoint has no brute-force throttle/lockout (security; medium confidence — below fast-track bar, stays in backlog).
- **New:** [1] `code-review-api-handlers-share-log-proxy-ip` — share access audit persists ConnectInfo IP not X-Forwarded-For (bug; medium confidence).
- **New:** [1] `code-review-reality-web-comparison-view-i18n` — `ComparisonView.tsx` has no `useTranslations` on a 4-locale portal (bug; medium confidence).
- **New:** [1] `code-review-reality-web-realtor-mgmt-i18n` — RealtorManagement destructive-remove modal + header subtitle hardcoded English (bug; medium confidence).
- **New:** [1] `code-review-reality-web-agency-errorstate-i18n` — no-agency empty/error state hardcoded English (bug; medium confidence).
- **New:** [1] `closed-not-merged-pr-2675` — dependabot npm-minor-patch (22 updates), superseded by open #2751.
- **New:** [1] `closed-not-merged-pr-2674` — dependabot cargo-minor-patch, superseded by open #2750.
- **New:** [1] `closed-not-merged-pr-2581` — dependabot gradle-minor-patch, superseded by open #2749.
- **New:** [0] `lag-warning-2026-08-14` — 37h since last run (dropped, informational).
- **Bumped:** none this run (all new items are first-emit; 5 were previously orphaned rows in `backlog.json` with `status/score/vector=null`, now scored + titled + set `status=open` — the "buffer refill" the payload asked for).
- **Decayed:** none — the 3 mobile-native-kmp open items are 3–10d old, all within the 14d decay window.
- **Dropped:** `lag-warning-2026-08-14` (score 0, informational).

## Plans promoted
- none this run · adversarial pass: n/a — no backlog item met the readiness threshold (score>=3 with concrete files + confidence bar; security fast-track requires confidence=high but the new `share-pw-no-throttle` item is medium).

## Auto-fix
- `no candidate` — no signal met confidence=high + score>=3 + allowlist this run. Kill switch (`ROUTINE_AUTOFIX_DISABLED`) not set; simply nothing eligible.

## Self-review findings
- Findings file `.research/self-improvement/findings.json` not read this run (lag-recovery, minimal-viable coverage). No new self-review findings.

## Open questions
- **The buffer-low condition surfaced by the caller payload** (`claimable=4/72 — refill planner (coverage exhausted: 47 done/2 stale-partial; backlog empty)`) is only partially addressable from this routine: this run refilled the raw backlog (11 open items now, up from 3), but none of the additions meet the promotion threshold, and all 3 pre-existing open items are `mobile-native-kmp` (structurally unlandable in the cloud runner per `.research/mobile-native-gate.sh`). The dispatcher's Tier-1d generator has fired repeatedly the last 12h but the findings do not auto-score into promotable backlog rows — the design gap this run patches by hand. Sustained fix will need either (a) the dispatcher to score its own Tier-1d findings into backlog rows with score>=3 confidence=high when the evidence supports it, or (b) an on-demand `/ppt-project-management scan` to reopen coverage stories that were closed prematurely.
- **PR #2744** (dispatcher un-wedge) sitting `needs-human-review` since 2026-08-13T06:50Z (30h) — a real human decision is blocking three separate reported dispatcher defects (#2743 → #1162 / #2460 recurrences).

## Quiet day?
- No — 8 real merges shipped since last run (including 3 security fixes: SSO backslash open-redirect, marketplace IDOR, Alexa signature fail-closed), backlog buffer refilled from 3→11 open items, and 4 issues (#2752/#2753/#2754/#2743) opened. `state.stats.quiet_days` NOT bumped.
