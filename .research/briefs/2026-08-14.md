# 2026-08-14

## Since last run
- Merged PRs: not surveyed (buffer-refill scope — observation phase skipped)
- Open PRs touched: not surveyed
- New / updated issues: not surveyed
- Commits: not surveyed
- Routine lag: last full routine run was `last_run_iso = 2026-08-12T22:45Z` (~34h). A full daily brief is still owed and should be produced by the next full-run trigger.
- Phases that failed: none (Phase 1 / 1.5 / 1.6 / 3 / 5 deliberately skipped for this scope)

## Run type
- **buffer-refill only.** Fired externally with payload `buffer-starved: claimable=0/72 — all scored backlog is mobile-native (cloud-ungated). 6+ LANDABLE backend findings sit as unscored signals from the last 4h.` The dispatcher's Tier 1b (`backlog-refill.sh`) was blocked because every open backlog row was a mobile-native item the cloud runner cannot land, while sixteen `code-review-finding` signals sat under `.research/signals/` unscored. This run scores those signals into `backlog.json` so the next dispatcher tick has landable work to promote.

## Signals scored → new open backlog items (14)

**reality-server** (4)
- `[3, bug, high]` `code-review-reality-server-sso-introspect-negcache-poison` — SSO introspect_pm_token() poisons the 60s negative cache on transient PM 5xx/429 (sso.rs:743-762). Next request within TTL sees a bogus `{active:false}` and sync_session tears down the live portal session — spurious force-logouts up to 60s per transient upstream blip.
- `[3, security, medium]` `code-review-reality-server-imports-feed-url-ssrf` — `/api/v1/imports/feeds` create/update/sync accept unvalidated `feed_url`; the sibling `agency_imports.rs` route explicitly calls `util::url_validator::validate_fetch_url()` on the same field. Any portal agency user can register `feed_url=http://169.254.169.254/…` and have the sync worker fetch it (cloud-metadata SSRF).
- `[2, bug, high]` `code-review-reality-server-sso-reqwest-no-timeout` — three SSO helpers (`exchange_code_for_token` / `get_user_info` / `introspect_pm_token`) each `reqwest::Client::new()` per invocation with NO timeout and no connection reuse; a hung PM upstream stalls the auth path indefinitely. A correctly-timed shared client already lives on `AppState.pm_api_client`.
- `[1, refactor, medium]` `code-review-reality-server-dup-url-validators` — `reports.rs:35` and `agency_branding.rs:28` carry byte-duplicated local `validate_image_or_link_url` with a standing TODO; the shared `util::url_validator` now exists but neither call site adopts it.

**api-handlers** (7)
- `[2, bug, medium]` `code-review-api-handlers-financial-f64-money` — income-statement / balance-sheet exports (XLSX + PDF) round-trip `Decimal` money through `f64` (`.parse::<f64>().unwrap_or(0.0)` — five sites in `financial.rs`). Two defects: floating-point drift on exact-precision money, and a silent `0.00` on parse failure that lands as authoritative on the customer report.
- `[2, bug, medium]` `code-review-api-handlers-ai-pagination-no-cap` — AI list endpoints (`sessions.rs`, `equipment.rs`, `voice.rs`, `llm.rs`) forward caller `?limit` straight to `LIMIT $N` with no upper clamp. Contrast: `admin/*` and `listing.rs:136` all cap at `.min(100)`. Merged with the older signal id `code-review-api-handlers-ai-list-unclamped-limit` (same defect, no score double-count).
- `[2, bug, medium]` `code-review-api-handlers-marketplace-limit-no-cap` — `marketplace.rs` search/verification-queue/reviews format `LIMIT {}` from a caller-supplied `i32` with no clamp; `?limit=2147483647` forces a full scan on the public/authenticated-reachable provider search.
- `[2, security, medium]` `code-review-api-handlers-share-pw-no-throttle` — public unauthenticated `access_protected_share` verifies the share password with zero brute-force protection; per-tenant rate limiter cannot key anonymous requests (handler comment: "This public endpoint has no caller org context").
- `[1, bug, medium]` `code-review-api-handlers-auth-inactive-revoke-swallowed` — deactivated-account revoke uses `let _ =` (silent, no log); sibling revoke 9 lines below logs on error.
- `code-review-api-handlers-share-log-proxy-ip` — **DEFERRED this run**: pre-existing bare-signal evidence includes a private hostname string; the G9 grep-in-diff would catch even the removal/context lines. Left as bare-signal for the next full run to normalize with sanitized evidence; ID NOT added to seen_signals so it re-processes fresh.

**api-core** (2)
- `[2, bug, medium]` `code-review-api-core-oauth-refresh-race-drops-revoke-guard` — `refresh_tokens` reads-then-writes with an in-memory `is_revoked()` check and ignores the atomic `revoke_refresh_token` bool. Two concurrent refresh requests both pass the gate and both issue new pairs — defeats family-reuse detection. The atomic guard already exists in the repo layer.
- `[1, bug, medium]` `code-review-api-core-oauth-refresh-revoke-before-issue-lockout` — revoke fires BEFORE issue with no wrapping transaction; a DB failure between the two silently breaks the refresh chain.

**ppt-web-core** (3)
- `[2, bug, medium]` `code-review-ppt-web-core-rentals-mutation-swallow-err` — all four `rentals.tsx` mutations only handle auth errors; `handleRentalsAuthError` returns `false` for everything else and callers ignore the return → failed check-in/out/create-connection/sync-platforms produce zero UI feedback. `financial.tsx:179-210` shows the correct pattern.
- `[2, bug, medium]` `code-review-ppt-web-core-rentals-noop-actions` — `BookingsPageRoute` / `PlatformConnections` / `BookingDetailPageRoute` wire onCheckIn/onCheckOut/onCancel/onConnect/onEdit/onDeleteGuest to `() => {}` while the same actions are real mutations one screen deeper (`rentals.tsx:409-428`). Dead buttons that render as functional.
- `[1, bug, medium]` `code-review-ppt-web-core-person-months-toast-i18n` — `person-months.tsx:373-379` save toasts use raw English literals; the rest of the app routes toast copy through `t()` (see `financial.tsx:175,182,207`). Non-EN users see English on save.

## Watch
- Buffer starvation was real: before this run, 3 open backlog rows, all `mobile-native` (cloud-ungated). Dispatcher `avail=0` for two days. Now 17 open (3 mobile + 14 landable), and dispatcher `backlog-refill.sh` should promote in the next Tier 1b tick.
- **One bare-signal row normalized in place** (`code-review-api-handlers-share-pw-no-throttle`) — it had been ingested with only signal-shape keys (no `title` / `vector` / `score` / `status`), so the last render silently showed it as a blank row. Now proper backlog schema. Four more rows remain in this shape (`code-review-api-handlers-share-log-proxy-ip`, `code-review-reality-web-{comparison-view,realtor-mgmt,agency-errorstate}-i18n`); the next full routine run should normalize them with sanitized evidence.
- **`backlog.md` was slightly stale vs `backlog.json`** before this run — four `2026-08-12` rows showed `status: open` in the rendered view while the JSON had already flipped to `done`. That drift is fixed as a side effect of this regeneration.

## PRs stuck in draft despite approval
- not surveyed (observation phase skipped)

## PRs with verdict=changes, no fix-round progress in 24h
- not surveyed (observation phase skipped)

## Code review slice
- Segment reviewed: none this run (Phase 1.5 skipped). The signals scored this run came from earlier Tier-1d dispatcher passes (2026-08-13/14) that had already produced findings; the routine just hadn't scored them into the backlog yet.
- Experts: n/a
- Findings: 14 new open items scored (see the "Signals scored → new open backlog items" section)
- Skipped: yes — buffer-refill scope, not a full routine run

## Screen-map status
- not surveyed (observation phase skipped)

## Backlog deltas
- **New (14):** see the "Signals scored → new open backlog items" section above.
- **Normalized (1):** `code-review-api-handlers-share-pw-no-throttle` — bare-signal row given proper schema; scored as if freshly appended.
- **Deferred (1):** `code-review-api-handlers-share-log-proxy-ip` — see the same section for the G9-diff-context rationale.
- **Bumped:** none
- **Decayed:** none (all 3 pre-existing open items are <14 days old)
- **Dropped:** none

## Plans promoted
- none (buffer-refill scope; promotion left to next full routine run — the 15 new rows are all promotion candidates on merit: 5 have `score >= 3` including the security fast-track candidate `code-review-reality-server-imports-feed-url-ssrf` at `security`+`score=3`+`confidence=medium`, and `code-review-reality-server-sso-introspect-negcache-poison` at `bug`+`score=3`+`confidence=high`).

## Auto-fix
- `no candidate` — Phase 5 skipped entirely (buffer-refill scope). No `auto_fix/*` branch pushed.

## Self-review findings
- none

## Open questions
- Two rows (`code-review-api-handlers-share-pw-no-throttle`, `code-review-reality-server-imports-feed-url-ssrf`) are landable but need a design touch: the share-throttle fix needs a place to keep per-token/per-IP counters (Redis? in-process LRU? tenant_rate_limiters keyed differently?); the SSRF fix should also fold in the "belt-and-suspenders" TOCTOU re-validate at fetch time. Both are answerable in the implementer plan — flagged here so the next full-run Phase 3 doesn't glaze over them.

## Quiet day?
- No. Fired specifically because the dispatcher buffer was starved and 16 unscored landable signals were sitting on disk. Next full-run trigger should still produce a normal daily brief; this one is a targeted intervention, not a substitute.
