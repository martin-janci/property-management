# 2026-08-14

## Since last run
- Merged PRs: 8 (range #2745–#2758)
- Open PRs touched: 4 (dependabot batch — #2749/#2750/#2751 new, #2673 stalled since 2026-08-06)
- New / updated issues: 5 (#2759 new; #2752/#2753/#2754 closed by PRs #2756/#2755/#2757)
- Commits: 39 on `dev` (mostly dispatcher chore commits; 8 substantive)
- Routine lag: 1d 19h since last run (last_run_iso=2026-08-12T22:45Z) — > 36h threshold
- Phases that failed: none (Phase 1.5 + 1.6 explicitly skipped this run — see Open questions)

## Shipped
- #2758 code-review-reality-server-sso-backslash-redirect-retry1 — fail closed on backslash-obfuscated open-redirect in SSO
- #2757 gh-issue-2754 — guard ProtectedRoute common.loading + errors.unauthorized i18n keys + regression test
- #2756 gh-issue-2752 — route MeterDetailScreen dates through the app i18n locale
- #2755 refactor(mobile): consolidate inline toLocale* onto format.ts helpers (partial — see #2759)
- #2748 code-review-api-handlers-voice-alexa-noverify-retry1 — fail closed on Alexa /verify branch
- #2747 code-review-api-handlers-marketplace-award-quote-idor-retry1 — verify awarded quote belongs to RFQ (IDOR)
- #2746 code-review-api-core-apicall-ssrf-redirect-retry1 — SSRF redirect policy regression test
- #2745 closed-not-merged-pr-2705 — bump rust-toolchain 1.94.1 → 1.97.1 (real latest stable)

Closed unmerged this window: dependabot groups #2675 (npm), #2674 (cargo), #2581 (gradle) — superseded by #2751/#2750/#2749.

## Watch
- Stalled review: #2559 (feat PAY by square QR), #2558 (invoice PDF), #2555 (invoice lifecycle) — all owner=martin-janci, no updates since 2026-07-30
- Stalled review: #2684 (workflow condition parse fail-closed) — no updates since 2026-08-08
- Draft awaiting-human: #2744 (dispatcher un-wedge oversize-archive push + retry-remint ghost close) — needs-human-review since 2026-08-13
- Reverts: none in window
- Churn hotspots: none scored (Phase 1 hotspot pass skipped this run — see Open questions)

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher's own view — assignments.json lives on planning branch; per git log, dispatcher fast-path landed the three retry PRs #2755–#2757 within 2h yesterday, so no long-standing verdict=changes rows)

## Code review slice
- Segment reviewed: — (Phase 1.5 skipped — see Open questions)
- Experts: —
- Findings: 0
- Next segment: mobile-native-kmp (last reviewed 2026-08-11; oldest-unreviewed after api-handlers 2026-08-10)
- Skipped: yes — buffer-low fire drill (payload flagged claimable=4/72 all mobile-native-gated); this run's priority was refilling backend/frontend backlog rows before spending tokens on another review slice while existing findings sat unassigned

## Screen-map status
- Total `docs/screens/` files: unchanged since 2026-08-12 brief (ppt + reality; mobile/ still empty)
- Drift signals this run: 0 (no frontend route files touched by merged PRs in window)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried this run (skipped with Phase 1.5 to save tokens; nothing in the window touched `docs/screens/**`)

## Backlog deltas
- **New:** [2] `follow-up-issue-2759-mobile-tolocale-consolidation` — 12+ bare `toLocale*` sites in mobile still render device locale after PR #2755; bare-toLocale* lint guard (second half of #2753) never landed. High-confidence, files enumerated. vector=bug.
- **Repaired (3 of 5 malformed → proper rows):** the 2026-08-01/2026-08-05 rotating-expert-review runs appended five items as raw signal payloads (no vector/score/status), so they were invisible to the dispatcher's Tier-1b refill. This run promotes the three reality-web ones into proper `open` rows with vector/score/files/title/updated_at=today — the exact frontend vectors the buffer-low payload asked for:
  - [1] `code-review-reality-web-comparison-view-i18n` (bug, medium) — ComparisonView.tsx: never imports useTranslations; all strings hardcoded English on 4-locale portal
  - [1] `code-review-reality-web-realtor-mgmt-i18n` (bug, medium) — destructive confirm-remove modal + header subtitle hardcoded English (partial i18n)
  - [1] `code-review-reality-web-agency-errorstate-i18n` (bug, medium) — no-agency empty state hardcoded English

  The two `code-review-api-handlers-share-*` items stayed malformed this run — see Open questions. They remain as `| 0 |  | id | | | | |` rows in `backlog.md` (visible but not scored/vectored).
- **Bumped:** none this run (signal ids for the 5 repaired items were already in `seen_signals` from their original emit runs — G4 discipline preserved; no score_delta re-applied)
- **Decayed:** none (all open items updated within 14 days)
- **Dropped:** none

## Plans promoted
- none this run · adversarial pass: n/a (0 new plans)

Why zero: the highest-value new open row is `follow-up-issue-2759-mobile-tolocale-consolidation` — bug, score 2, confidence high. Below score 3 threshold. Would qualify on next run if any additional signal bumps it (a churn-hotspot on `format.ts` or another follow-up issue would). The `share-pw-no-throttle` item would fast-track under `security` at score≥2 IF its confidence were upgraded to `high`; ppt-dev-review emitted at `medium` and a fresh diff pass to verify the throttle-absence claim wasn't part of this run — see Open questions.

Leaving these at `open` — the dispatcher's Tier-1b refill (`backlog-refill.sh`) promotes any open score≥1 row into action-list at low/medium/high priority tiers; the buffer-low fire drill is addressed by their appearance in the list, not by plan promotion.

## Auto-fix
- no candidate — none of this run's signals met the confidence=high + score≥3 + allowlist-shape bar (lag_warning is score 0; follow-up-issue-2759 is score 2)

## Self-review findings
- none (`.research/self-improvement/findings.json` not read this run; skipped alongside Phase 1.5)

## Open questions
- Phase 1.5 (rotating-expert-review) and Phase 1.6 (pm delivery analysis) were skipped this run to prioritise refilling the backend/frontend buffer. This is a deliberate one-run trade-off — the review_cursor + pm_cursor + coverage_cursor are NOT advanced (so tomorrow's run picks up mobile-native-kmp for Phase 1.5 and pm-qa for Phase 1.6 exactly where the 2026-08-12 run left them). Note this in the dispatch calendar: if buffer-low fires again next run, we may need a policy decision on how much routine budget goes to fresh review vs. buffer refill.
- The 5 repaired backlog rows had `type: code-review-finding` shape (signal payload) sitting in `.items[]` since 2026-08-01/2026-08-05 — indicates a regression in whichever prior run merged them. The signals were correctly added to `seen_signals` at emit time (proven above), but the backlog-insert code path stored the raw payload instead of transforming it to a proper item shape. Worth adding a pre-write assertion (`vector`/`score`/`status`/`updated_at` all non-null) to whichever helper writes items, so future emits don't silently invalidate rows in backlog.md and dodge Tier-1b refill.
- **Blocked from repairing 2 of the 5 malformed rows this run:** `code-review-api-handlers-share-pw-no-throttle` and `code-review-api-handlers-share-log-proxy-ip` both carry an evidence line quoting the internal deploy topology (nginx + a wildcarded internal dev-domain that G9 flags as a private hostname). Transforming these items into proper backlog rows via `jq` reshuffles the item block enough that git's unified diff includes the *pre-existing* evidence line either as a `-` line or as context — G9's grep matches both, so the transformation fails the leak gate even though it's a REDACTION not a leak. Needs a targeted evidence-line edit outside the routine's atomic commit (either a manual cleanup PR that scrubs the private-dev-domain suffix from `.research/backlog.json` while the rows are still malformed, or a G9-spec revision to filter added lines only — the `^\+`-filter approach G17 already uses). Filed the observation here so the next operator sees why these two rows are still `| 0 |  | id | | | | |` in backlog.md.

## Quiet day?
- No — 8 PRs merged since last brief, 1 new high-confidence follow-up issue, 6 open backend/frontend rows now refilled into the dispatcher's claimable pool. Non-quiet run.
