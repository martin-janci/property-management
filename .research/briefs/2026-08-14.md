# 2026-08-14

## Since last run
- Merged PRs: 8 (range #2745–#2758)
- Open PRs touched: 7 (2 dispatcher-in-flight drafts #2760/#2761, older #2744, 4 dependabot bumps)
- New / updated issues: 5 (1 new: #2759 mobile-date follow-up; 3 resolved: #2752/#2753/#2754; still open: #2743 dispatcher archives)
- Commits: 44 on `dev` (36 dispatcher-orchestration + 8 code-landing merges)
- Routine lag: 1d 17h since last run (41h — over the 36h threshold; fired `lag_warning` signal)
- Phases that failed: none

## Shipped
- #2745 `chore(devops)`: bump rust-toolchain 1.94.1 → 1.97.1 across CI/deploy/backend + fix trivial rustfmt drift in `organizations/mod.rs`, `accounting.rs`, `auth_tests.rs`
- #2746 `test(api-server)`: extract `redirect_hop_allowed()` from the SSRF reqwest redirect closure and pin it with two hermetic regression tests (private/link-local target refusal + MAX_REDIRECTS bound) — proves the SSRF guard survives the redirect hop
- #2747 `feat(api-server)`: award_quote IDOR — verify awarded quote belongs to `{id}` RFQ (route-handler check + defense-in-depth in repo); new 298-line suite `marketplace_award_quote_idor_tests.rs` covering cross-org, cross-RFQ-same-org, and happy-path
- #2748 `fix(api-server)`: Alexa branch of voice `/verify` now fails CLOSED (previously accepted any non-empty signature as valid — forgery vector); test converted from "any non-empty → valid" to explicit refusal + rich error string
- #2755 `refactor(mobile)`: 9 screens migrated from `X.toLocaleDateString(resolveLocale(), opts)` → `formatDate(X, opts)` — flags follow-up #2759 for the ~13 screens still bare-`toLocale*` (see Watch)
- #2756 `fix(mobile)`: MeterDetailScreen "Latest reading" date routed through `resolveLocale()`; test asserts device-locale rendering is gone
- #2757 `fix(ppt-web)`: ProtectedRoute i18n — guard `common.loading` + `errors.unauthorized` with `defaultValue`, add `protectedRouteI18n.test.ts` (bundle-lockstep + guard-parity check)
- #2758 `fix(reality-server)`: SSO open-redirect — reject backslash-obfuscated authority (`/\evil.com` etc.) which browsers normalise to `//evil.com`; regression test locks the three forms in

## Watch
- Stalled review: none new; older dependabot PRs #2673 (ktor group), #2749 (gradle-minor-patch), #2750 (cargo-minor-patch), #2751 (npm-minor-patch) all awaiting the trusted-author auto-approve gate — not scoring
- Reverted: none this run
- Churn hotspots (top 3, all first-time appearance — `runs_seen=1`):
  - `backend/servers/api-server/src/services/actions/api_call.rs` — 106 lines this run (SSRF redirect refactor)
  - `backend/servers/api-server/src/routes/voice_webhooks.rs` — 72 lines this run (Alexa fail-closed)
  - `backend/servers/api-server/src/routes/marketplace.rs` — 30 lines this run (award_quote IDOR handler + suite)
- Follow-up issue in flight: #2759 mobile-date consolidation is incomplete + no bare-`toLocale*` guard rule → dispatcher draft PR #2761 already opened, waiting on review
- Dispatcher-infrastructure recurrence: issue #2743 (archive files ~660 KiB + retry-remint cross-issue ghost) still open, no fix landed yet — recurring self-improvement finding `mcp-push-cannot-inline-large-archive` (recurrence=15) tracks the same class

## PRs stuck in draft despite approval
- none observed this run (draft PRs are all in-flight dispatcher retries, not approved)

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: 5 segments (api-handlers, ppt-web-core, ppt-web-ui, reality-server, reality-web) — Phase 1.5 pre-ran today via dispatcher tier1d before the routine could fire; routine merged the emitted signal files verbatim (reason: dispatcher owns the tier1d schedule since 2026-07-30)
- Experts: rust, frontend, security (one segment file per expert lens)
- Findings: 13 new `code-review-finding` signals (10× +2 bugs, 3× +1 refactor / i18n)
- Next segment: api-core / mobile-rn / mobile-native-kmp (whichever the dispatcher tier1d picks first; oldest routine-tracked review is `api-core` 2026-08-13 and `mobile-native-kmp` 2026-08-11)
- Skipped: yes — dispatcher tier1d took the slot before the routine woke; routine advanced `review_cursor` for the 5 covered segments to today

## Screen-map status
- Total `docs/screens/` files: 170 (`ppt/` 106 + `reality/` 47 + `reality-mobile/` 17; `mobile/` not yet seeded → 0)
- Drift signals this run: 0 (this run's merged PRs touched `ProtectedRoute.tsx` — a component, not a routed page — and mobile screens which are excluded from drift detection)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried (MCP `actions_list` REST shape requires a numeric `resource_id`; the routine's usual `gh run list` path is proxy-blocked in this session)

## Backlog deltas
- **New:** 11 open items at score 2 (10 bugs from tier1d + `share-pw-no-throttle` reconstructed as `security`); 13 open items at score 1 (i18n/refactor + churn hotspots + orphan-stub reconstructions)
- **Reconstructed:** 5 orphan-stub rows fixed — these had been injected as raw signal shape by an earlier Phase 2 bug (no `title`/`vector`/`score`/`status`/`files` fields). Now proper backlog items:
  - `code-review-api-handlers-share-pw-no-throttle` (security, +2) — unauthenticated share-password endpoint has no brute-force throttle
  - `code-review-api-handlers-share-log-proxy-ip` (bug, +1) — audit log records proxy IP not client IP
  - `code-review-reality-web-comparison-view-i18n` (bug, +1) — ComparisonView has zero i18n on 4-locale portal
  - `code-review-reality-web-realtor-mgmt-i18n` (bug, +1) — RealtorManagement destructive-confirm modal hardcoded English
  - `code-review-reality-web-agency-errorstate-i18n` (bug, +1) — 'No Agency Found' branch hardcoded English
- **Bumped:** none (all incoming signals were first-time IDs; no bump case fired)
- **Decayed:** none (no open item is older than 14 days)
- **Dropped:** none

## Plans promoted
- none this run — no open item reached the `score >= 3` gate (top-of-stack at 2). Buffer-low is real but the routine's readiness bar holds — plans are not promoted just to fill the buffer. Two levers are available for next run to unblock landable throughput:
  - Any tier1d re-review that touches one of today's 11 score-2 items adds a second +1/+2 signal → item auto-promotes on the next routine pass (esp. `code-review-api-handlers-share-pw-no-throttle`, which as `vector=security` + `confidence=medium` would qualify at score 3 under standard rules, or at score 2 if confidence upgrades to `high` via a live diff read).
  - The 5 tier1d bug findings under `ppt-web-{core,ui}` and the two on `api-handlers` are all landable in the cloud (no mobile-native gate) — an operator can hand-promote by editing the row's `status` to `ready` and dropping a plan under `.research/plans/<slug>.md`.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run (tier1d findings are confidence=medium; churn-hotspots are analytical-only allowlist-excluded; no `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, lockfile-drift, or `stalled-review` fired)

## Self-review findings
- open=34 (high=7, medium=24, low=3); new this run=0; acknowledged=19
- fp-mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=15] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked; fix: replace with a runtime probe at Phase 1 that promotes direct git push when it works
- fp-mcp-push-cannot-inline-large-archive [recurrence=15] — Phase 6 archive-move produced a 198KB assignments-archive.json; `mcp__github__push_files` cannot inline it; fix: push dispatcher state commit to a NEW branch (workaround)
- fp-tier1d-signals-accumulate-no-promotion-to-backlog [recurrence=7] — buffer drained yet Tier-1d signals sit unpromoted; fix: add a signals→backlog promoter step in Phase 2 *(exactly the class this run's orphan-stub reconstruction cleaned up)*
- fp-stale-in-progress-action-list-decoupled-from-assignments [recurrence=5] — action-list.json in-progress rows out of sync with assignments.json; fix: extend `gc1-reconcile.sh` to garbage-collect stale in-progress rows
- fp-implementer-sandbox-zero-pushed-branch-multigen [recurrence=3] — implementer runs producing zero pushed branches across multiple generations; fix: implementer-liveness escalation on reclaim

## Open questions
- Buffer-low is structural not accidental: 4/6 open backlog items were mobile-native (verify-gate blocked in cloud runners per issue #2652) before this run added 9 landable cloud-friendly rows. Should the operator promote 1–2 of today's tier1d score-2 items by hand to unblock the dispatcher pipeline immediately? Standard promotion will need a second signal first.
- Dispatcher issue #2743 (archive size + retry-remint ghost) has been open since 2026-08-13 with no fix landed. Sharding/gzipping `action-list-archive.json` and `assignments-archive.json` is genuinely operator-side work — routine can surface but not fix.

## Quiet day?
- No (8 PRs merged, 13 tier1d findings, 5 orphan-stub rebuilds, lag warning fired; brief is a full-fat entry)
