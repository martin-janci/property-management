# 2026-08-14

## Since last run
- Merged PRs: 8 (range #2745–#2758)
- Open PRs touched: 6 (#2751 dependabot npm, #2750 dependabot cargo, #2749 dependabot gradle, #2673 dependabot ktor; #2744 dispatcher un-wedge draft — `needs-human-review`; #2684 code-review-api-core-workflow-cond-parse-failopen quarantined)
- New / updated issues: 4 (#2743 new/open — dispatcher regression; #2752, #2753, #2754 closed by their fix PRs)
- Commits: 32 on `dev` (mixed real code + heavy dispatcher `chore(research)` telemetry)
- Routine lag: 1d 6h since last run (last_run_iso=2026-08-12T22:45:00Z; now=2026-08-14T04:37:00Z) — on schedule (<36h)
- Phases that failed: none. Phase 1.5 (rotating expert review) and Phase 1.6 (project management) explicitly **skipped** this run — cursors not advanced (see Code review slice / Auto-fix). Trigger payload was `buffer-low: refill planner with landable backend/frontend vectors` — the run's whole focus was Phase 2 backlog scoring + Phase 3 promotions.

## Shipped
- #2745 closed-not-merged-pr-2705: bump rust-toolchain 1.94.1 → 1.97.1 (real latest stable; 1.100.0 doesn't exist, was the wedge). Fixed the entire backend CI's Rust job which had been failing at toolchain install for ~2 weeks.
- #2746 SSRF-redirect: prove redirect policy blocks internal-host hops (regression test suite — closed the residual gap after #2710).
- #2747 marketplace-award-quote IDOR: verify awarded quote belongs to RFQ (`load_rfq_for_org` was authorizing only the parent RFQ; body-supplied quote_id was awarded without cross-check).
- #2748 [security] Alexa /verify webhook: fail closed on Alexa branch — previously set `valid: !signature.is_empty()`, i.e. any non-empty string was a "valid" Alexa signature (forgery vector); now always false since JSON endpoint lacks the certificate-chain material.
- #2755 refactor(mobile): consolidate inline `toLocale*` onto `format.ts` helpers — 9 screens now route through the tested (but previously dead) `formatDate`/`formatTime` wrappers.
- #2756 gh-issue-2752: MeterDetailScreen reading dates through app i18n locale (bare `toLocaleDateString()` was following device locale, missed by #2740's sweep).
- #2757 gh-issue-2754: guard ProtectedRoute `common.loading` + `errors.unauthorized` i18n keys (two of four `t()` calls were missing the `defaultValue` guard the others use — dropped bundle key would leak raw dotted key).
- #2758 [security] SSO backslash open-redirect: fail closed on `\evil.com` / `\\evil.com` in `check_redirect_uri_allowed` (browsers normalise `\` to `/`, so `/\evil.com` → `//evil.com` → attacker origin; the existing `starts_with("//")` guard didn't catch it).

## Watch
- **PR #2684** — `code-review-api-core-workflow-cond-parse-failopen` open since 2026-08-06 (8d), quarantined per dispatcher (age 5d < 7d quarantine-exit SLA). Auto-fail + `from-stalled-pr` issue in ~1.4d if it doesn't unstick.
- **PR #2744** — dispatcher un-wedge draft, `needs-human-review` label; reviewer-bot approved but blocked on human-gated + self-PR contract. Awaiting operator merge.
- **Issue #2743** OPEN — dispatcher regressions recurred: archive-again exceeds MCP push ceiling (was #1162), retry-remint ghost cross-id (was #2460). Multi-label `bug, follow-up, infra, dispatcher`. Dispatcher itself has not filed a plan for it; this belongs to the dispatcher lane, not the routine.
- **Reverted:** none this window.
- **Churn hotspots (this run):** none. 30h window since last routine; the only production commits (#2745–#2758) each land small, focused fixes. Dispatcher `chore(research)` commits dominate the log but are `.research/`-only, filtered from churn per exclusion list. No `repeated-churn` firings; `hotspot_history` unchanged.

## PRs stuck in draft despite approval
<!-- PR #2744 is currently draft + `needs-human-review` + approved-by-bot but it's the dispatcher's own PR (self-PR) so the bot can't formally approve it — this is by design of the dispatcher contract, not a merge-gate bug. Left off the list. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Assignments.json lives on planning branch; last dispatcher commit at 04:35 UTC reports 0 review transitions this cycle, no verdict=changes rows detected. -->
- none

## Code review slice
- Segment reviewed: **skipped** (`state.review_cursor` recently advanced — reality-server 2026-08-13, api-core 2026-08-13, api-handlers 2026-08-10, ppt-web-core 2026-08-12, mobile-rn 2026-08-12; dispatcher's tier-1d generator has fired 4 times in the last 30h and produced 5 fresh code-review-finding signals already scored this run)
- Experts: n/a
- Findings: 0 (this run) · 14 fresh + 5 previously-unscored tier-1d findings ingested into backlog (see Backlog deltas)
- Next segment: `mobile-native-kmp` (last review 2026-08-11 — oldest unreviewed; skip continues while pipeline is buffer-starved with 3 open mobile-native items already backlogged)
- Skipped: yes — tier-1d generator has already saturated the finding stream; another synthetic review would produce net-zero new signal at real token cost

## Screen-map status
- Total `docs/screens/` files: 169 (`ppt/` 106 + `reality/` 47 + `reality-mobile/` 15 + template/README + 3 diagrams) — mobile RN app still not seeded
- Drift signals this run: 0 (no merged PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`; merges were backend + mobile-rn i18n only)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: unknown (workflow list restricted this session — MCP has `actions_get`/`actions_list` but tool-call was not made this run; no failure implied)

## Backlog deltas
- **New:** [3] `code-review-reality-server-imports-feed-url-ssrf` — SSRF: `/api/v1/imports/feeds*` accepts caller `feed_url` unvalidated (sibling `agency_imports.rs` has the guard) → **promoted to plan**
- **New:** [3] `code-review-reality-web-report-page-fake-submit` — abuse-report form silently drops every report (DSA notice-and-action compliance risk) → **promoted to plan**
- **New:** [3] `code-review-reality-web-sell-page-fake-submit` — sell wizard fakes success, business-lead data loss
- **New:** [2] `code-review-reality-web-realtor-profile-mock-noid` — `/realtor/[id]` never reads params.id; every URL shows same MOCK_AGENT (SEO risk)
- **New:** [2] `code-review-reality-web-realtor-contact-fake-submit` — agent-profile contact modal fakes send
- **New:** [2] `code-review-api-handlers-ai-list-unclamped-limit` — 9 AI list endpoints forward caller `?limit=` to SQL LIMIT unclamped (self-scoped DoS)
- **New:** [2] `code-review-api-handlers-financial-f64-money` — financial XLSX/PDF exports round Decimal through f64 with silent `unwrap_or(0.0)`
- **New:** [2] `code-review-ppt-web-core-rentals-mutation-swallow-err` — rentals mutations onError only handles auth codes; every other failure is silent
- **New:** [2] `code-review-ppt-web-core-rentals-noop-actions` — list-page check-in/out/cancel wired to `() => {}` (endpoints exist, wiring gap)
- **New:** [1] 6 lower-severity items (`realtor-account-i18n-gap`, `profile-page-mock-data`, `auth-inactive-revoke-swallowed`, `reality-server-dup-url-validators`, `ppt-web-core-person-months-toast-i18n`, `realtor-mgmt-i18n` details unified)
- **Filled-in (were unscored `status=null`):** 5 rows now have title/vector/score/status/files
  - [2] `code-review-api-handlers-share-pw-no-throttle` (security)
  - [1] `code-review-api-handlers-share-log-proxy-ip` (bug)
  - [1] `code-review-reality-web-comparison-view-i18n` (bug)
  - [1] `code-review-reality-web-realtor-mgmt-i18n` (bug)
  - [1] `code-review-reality-web-agency-errorstate-i18n` (bug)
- **Bumped:** none this run (no re-emit of previously-seen non-cumulative signals)
- **Decayed:** none (3 open mobile-native items all updated within 14d: portfolio-analytics-caps-100=10d, cancellation-swallowed=3d, ssoservice-untested=3d)
- **Dropped:** none

## Plans promoted
- `plans/code-review-reality-server-imports-feed-url-ssrf.md` — SSRF: guard `imports.rs` create/update/sync with `validate_fetch_url` (mirrors sibling `agency_imports.rs`, uses existing shared `util/url_validator.rs:116`). Cloud-landable backend Rust. · adversarial pass: passed
- `plans/code-review-reality-web-report-page-fake-submit.md` — DSA notice-and-action: wire `report/page.tsx` `handleSubmit` to a real reality-server endpoint (adding it via TypeSpec if missing) with `useMutation` + loading/error state; msw regression covers the fake-submit-is-impossible invariant. Cloud-landable frontend Next.js. · adversarial pass: passed

## Auto-fix
- `no candidate` — Phase 5's allowlist is `screen-map-drift | screen-map-orphan | fixme-in-merged-code | dep-update-noise (lockfile-drift) | stalled-review`. This run's signals are all `code-review-finding`, not in the allowlist. Kill switch `ROUTINE_AUTOFIX_DISABLED` not set.

## Self-review findings
- Findings file `.research/self-improvement/findings.json`: not present or not read this run — `none`
- (No `severity==high, status==open` entries to enumerate)

## Open questions
- **Buffer-starved root cause** — trigger payload named it: the dispatcher's active claimable set is 4 mobile-native items, all AGP-egress-blocked in the cloud runner (per `.research/mobile-native-gate.sh`). This run adds 3 new **landable** score-3 vectors (2 promoted, 1 open) plus 11 more open items, so the planner has real work again — but the underlying mobile-native gate needs an operator-side fix (issue #2652 per prior dispatcher notes) before those 4 items become claimable. Non-blocking for this routine, but the "buffer-low" trigger will re-fire until it lands.
- **Should the routine actively de-quarantine `code-review-api-core-workflow-cond-parse-failopen` (PR #2684)?** It has been open 8d with no reviewer engagement. The dispatcher's 7d SLA fires in ~1.4d and will auto-fail it. No action from the routine unless the operator asks.

## Quiet day?
- No — 14 new backlog vectors, 5 previously-unscored rows resolved, 2 plans promoted, 8 PRs merged since last run.
