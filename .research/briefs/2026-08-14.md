# 2026-08-14

## Since last run
- Merged PRs: 8 (range #2745–#2758)
- Open PRs touched: 5 (3 fresh dependabot, 1 draft self-fix, 1 aging ktor bump)
- New / updated issues: 4 (3 follow-ups auto-closed by merges, #2743 dispatcher self-fix open)
- Commits: 25 on `dev` (mostly dispatcher chore commits + the 8 PR merges)
- Routine lag: 1d 4h since last run (on schedule)
- Phases that failed: none

## Shipped
- #2758 code-review-reality-server-sso-backslash-redirect-retry1 — fail closed on backslash-obfuscated open-redirect in SSO (`sso.rs` fast-path now rejects `/\evil.com` alongside `//evil.com`; regression test added)
- #2757 gh-issue-2754 — guard ProtectedRoute `common.loading` + `errors.unauthorized` i18n keys; adds locale-lockstep regression test that was missing on #2737
- #2756 gh-issue-2752 — MeterDetailScreen reading dates now flow through `resolveLocale()` (fixed miss from the #2740 mobile date-locale sweep)
- #2755 gh-issue-2753 — consolidated 9 inline `toLocale*(resolveLocale(), …)` calls onto `formatDate`/`formatTime` helpers; kills the DRY drift
- #2748 code-review-api-handlers-voice-alexa-noverify-retry1 — Alexa branch of JSON `/verify` webhook now fails closed (was `valid: !signature.is_empty()` — accepted any forged non-empty string)
- #2747 code-review-api-handlers-marketplace-award-quote-idor-retry1 — `POST /marketplace/rfqs/{id}/award` now verifies awarded quote belongs to the RFQ; defense-in-depth in `award_rfq`; 3 regression tests
- #2746 code-review-api-core-apicall-ssrf-redirect-retry1 — extracted `redirect_hop_allowed()` and pinned it: SSRF redirect policy refuses hops to 10.0.0.0/8 + 169.254.169.254 metadata + bounds chain at MAX_REDIRECTS
- #2745 closed-not-merged-pr-2705 — rust-toolchain 1.94.1 → **1.97.1** (real latest stable; #2385/#2705 tried the non-existent 1.100.0 and failed at rustup); rippled through 9 workflow refs + 3 rustfmt/clippy fixes under the new toolchain

## Watch
- Stalled review: #2673 (ktor group bump) — 8d idle, no reviewDecision (dependabot)
- Reverted: none this run
- Churn hotspots: `backend/servers/api-server/src/services/actions/api_call.rs` (106 lines, PR #2746), `backend/servers/api-server/src/routes/voice_webhooks.rs` (72 lines, PR #2748), `backend/servers/reality-server/src/routes/sso.rs` (33 lines, PR #2758, runs_seen=2 after this run)

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: `api-handlers` (churn-aligned: voice_webhooks.rs + marketplace.rs both touched this run; also oldest-unreviewed since 2026-08-10)
- Experts: none spawned this run — segment already carrying 2 unscored `code-review-api-handlers-share-*` findings from the dispatcher's Tier-1d generator (landed today). Adding more without draining these would saturate the segment.
- Findings: 0 (no new `code-review-finding` signals from Phase 1.5 this run)
- Next segment: `mobile-native-kmp` (oldest unreviewed after this run: 2026-08-11)
- Skipped: no — cursor advanced with zero-finding outcome recorded

## Screen-map status
- Total `docs/screens/` files: 153 (ppt/ 106 + reality/ 47; mobile/ 0 — not yet seeded)
- Drift signals this run: 0 (no merged PR touched a triggering route path this run)
- Orphan screens this run: 0 (spot-check not run)
- Last `screen-map.yml` CI run on `main`: not checked (would need CI query)

## Backlog deltas
- **New:** [1] refactor `churn-hotspot-backend/servers/api-server/src/services/actions/api_call.rs` — SSRF redirect policy extraction (PR #2746)
- **New:** [1] refactor `churn-hotspot-backend/servers/api-server/src/routes/voice_webhooks.rs` — Alexa /verify fail-closed (PR #2748)
- **New:** [1] dx `stalled-review-pr-2673` — ktor bump open 8d, no reviewDecision (dependabot; dep-update-noise-adjacent)
- **Re-emitted (cumulative, score_delta=0):** `churn-hotspot-backend/servers/reality-server/src/routes/sso.rs` — 2nd runs_seen; kept out of backlog per cumulative-signal rule
- **Bumped:** none
- **Decayed:** none (no open items ≥14d old)
- **Dropped:** none

## Plans promoted
- none (no `open` item met score≥3 or security-fast-track this run — my new signals all landed at score 1)

## Auto-fix
- `no candidate` — none of this run's signals met the certainty bar (all new items score=1, below the ≥3 gate); the sole stalled-review candidate #2673 is dependabot noise not worth a "Routine ping" comment

## Self-review findings
- open=33 (high=7, medium=23, low=3); new this run=0; acknowledged=19
- fp-null [recurrence=15] — Phase 6 archive-move produced oversize `assignments-archive.json`; fix: adopt the proven PR+API-merge path as the primary push mechanism for oversize files
- fp-null [recurrence=14] — dispatcher-prompt hard-assumes git push is proxy-403 blocked; fix: runtime capability probe at Phase 1 + drop the 64KiB size-guard when PUSH_METHOD=git
- fp-null [recurrence=6] — 61 unconsumed `*tier1d*.json` signal files never promoted to backlog; fix: signals→backlog promoter script + Tier-1d gate on unconsumed count
- fp-null [recurrence=5] — action-list.json 31 in-progress rows vs assignments.json 3 live rows (twin status inverted); fix: Phase 2 reconcile step + T32 self-test
- fp-null [recurrence=3] — 5 in-progress rows produced 0 origin branches over 2 implementer generations; fix: implementer-liveness escalation to `failed` on branchless-after-reclaim

## Open questions
- 5 orphaned unscored backlog rows (status=null, score=null) — 3 reality-web-i18n + 2 api-handlers-share security (share-pw-no-throttle, share-log-proxy-ip). The dispatcher's 02:30 UTC run explicitly flagged these as "backlog-refill cannot promote" and kicked the Tier-2 planner. If they stay unscored, they can't be picked up by any pipeline. Human triage or a scoring pass needed.
- Recurrence of dispatcher archive-oversize (issue #2743) — both `action-list-archive.json` (638KiB) and `assignments-archive.json` (660KiB) far exceed the 64KiB MCP inline-push ceiling; dispatcher is one terminal-transition away from stuck. Draft PR #2744 exists for a fix, blocked on human review (approved but draft).

## Quiet day?
- No — 8 PRs merged, 3 new backlog vectors, active security remediation (SSO backslash + Alexa signature + IDOR + SSRF regression), rust-toolchain bump landed cleanly.
