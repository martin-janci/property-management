# 2026-06-17

## Since last run
- Merged PRs: **66** (range #1440–#1546) — 38h window since 2026-06-16T03:25Z
- Open PRs touched: **0** (everything in the window either merged or closed)
- Closed-but-not-merged PRs: **16** (all PR-rewrites superseded by a later merged PR in the same run — routine churn, no signals emitted)
- New / updated issues: **35** (1 new OPEN — #1538 CI gate gap; 16 new follow-up issues #1519–#1535 from the dispatcher's merged-PR review system; 18 follow-ups closed in the same run)
- Commits: **111** on `dev` (real-code + dispatcher state mixed)
- Routine lag: **1d 14h** (38h) — over the 36h soft threshold, `lag_warning` emitted (logged only; under 3d, no `stale_routine_alert`)
- Phases that failed: **none**

## Trigger payload
Dispatcher fired this run with a `buffer-low` advisory: *claimable=22/72 below floor=36; coverage.json stale (2026-06-16) and exhausted — all current gaps already in backlog/done; please run a fresh coverage scan + refill the research backlog with new stories.*

The routine **cannot** run the `scan` mode (per the prompt's Phase 1.6 rule: "Do NOT run the skill's `scan` mode here — the authoritative full rebuild is the on-demand local `/ppt-project-management scan`"). This run did the cheap upkeep path and surfaced 8 new vectors anyway (3 from a rotating expert review, 2 screen-map drifts, 3 churn/repeated-churn observations). The action list went 41 → 50 open after pm-security + scrum-master synthesis — above the 36 floor — but the dispatcher's `claimable` metric measures rows with no pending `depends_on`, so a raw count above floor doesn't fix the claimable gap. **Operator action required:** run `/ppt-project-management scan` locally to refill candidates.

## Shipped
- **#1454** `feat: native accounting MVP (N1-N4) + @hey-api migration` — 17,983 LOC. Adds accounting + rentals route groups to ppt-web (note: no `docs/screens/ppt/` update → drift signal emitted).
- **#1453** `N5 — Bank-statement upload + payment-matching + match-review (PAP-210)`.
- **#1450** `feat-notification-pref-sync-mobile-push: preference-aware push dispatch-target selection` — Coverage 8A-3 advance.
- **RLS hardening batch:** **#1471** BIT-74 RLS-routing test coverage (ESG + webhook + public plans, 643 LOC), **#1460** BIT-76 FORCE RLS on developer_oauth_apps/grants/api_key_usage_logs, **#1467** BIT-78 mfa.verify_recovery_code RLS-enforce + cross-user IDOR test, **#1473** BIT-85 cross-tenant OAuth upsert hazard + manager gate.
- **Booking.com security:** **#1472** BIT-98 encrypt credentials at rest, **#1485** BIT-110 client timeout + Retry-After + RateLimited, **#1502** BIT-98 IG3 contract test.
- **DevOps gates:** **#1441** pre-push cargo fmt+clippy hook, **#1452** dev-push compile gate, **#1455** CI fmt+clippy mirror.
- **Test coverage:** **#1456** forms RLS write/create coverage; **#1470** DSA report download regression; **#1501** reality-server handler-level IDOR regression for imports/feeds; **#1517** IDOR test seed_membership migration (#1373 closed); **#1477** mobile App.tsx auth-gate wiring; **#1542** iOS SearchView stale-response guard pagination; **#1544** dispute draft auto-save tests; **#1543** AnnouncementsScreen TZ-pinned test.
- **Reality-mobile / KMP polish:** #1447 (AC-4 infinite scroll), #1465 (iOS stale-response guard + universal-link + plist + xcconfig drift), #1514 (82.1 SwiftUI project-setup screen-map), #1515 (82-vs-implementation mapping), #1516 (82-2 deep-link confirm), #1536 (post-#1447 spotless fix), #1541 (FavoritesService session-gating).
- **Schema / enum cast cleanup:** #1486 (BIT-113 platform::text), #1487 (rustfmt budget), #1489 (#1422 nullable rental_bookings), #1490 (CI strict-fail on stale RLS baseline, closes #1340), #1491 (#1301 marketplace idempotent), #1492 (BIT-118 GUEST/CALENDAR_BLOCK/ICAL_FEED), #1495 (form RLS grant + rental enum decode), #1499 (#1331/#1332 form_submission_status cast), #1537 (#1008 enum write paths), #1545 (reality-web /accounting prerender + favorites pl/hu i18n).
- **OAuth / IDOR fixes:** #1457 (BIT-75 org-scope appeal/evidence/comment, #1342/#1339), #1463 (GH #1413 building/unit scope download), #1468 (GH #1364/#1404 dispute draft auto-save), #1480 (GH #1413 docs list + unify preview gate), #1539 (oauth reject public client w/ secret).
- **Research / dispatcher:** #1444 (T21 epic_prefix), #1445 (G14 projection sufficiency doc), #1448 (#583 depends_on populate + T25), #1466 (extracted merge-path "section above" fix), #1483 (#1151 Tier-2 kick payload + anthropic-version), #1508/#1509/#1513/#1518/#1546 (dispatcher state PRs).
- **Devops follow-ups:** #1526 (EAS workflow pins healthy), #1529 (ADR pre-push gate scope), #1530 (required-check verification doc), #1540 (App.tsx merge-queue confirm).
- **Other:** #1481 (retire broken budget reserve-fund duplicate surface), #1484 (BIT-114 tracker-id fix), #1507 (user_memberships seed + platform principal), #1510 (canonical tenant-aware RLS helper, #1370), #1512 (post-#1454 clippy fix in accounting routes).

## Watch
- **🚨 NEW — #1538 OPEN (labels=ci):** *Backend `test` job is not a required check on `dev` — red-test PRs merge freely and re-red the merge pipeline.* The 36+ tests added this run (BIT-74/76/78/85/98/110 RLS + IDOR coverage) are advisory only. Top scrum-master action item.
- **Risky-churn surface:** `backend/crates/integrations/src/booking.rs` (3714 lines, runs_seen=2), `backend/crates/db/src/repositories/rental.rs` (3191 lines, NEW hotspot), `backend/crates/db/src/repositories/document.rs` (3093 lines, runs_seen=2). All three repeatedly patched without structural review.
- **Churn hotspots (top 3 real-code):** booking.rs (3714), rental.rs (3191), document.rs (3093).
- **Stalled review:** none — all open PRs got attention within 24h this window.
- **Reverted:** none.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- (planning branch state inaccessible from this cloud session; not queried.)

## Code review slice
- Segment reviewed: **api-handlers** (reason: churn-aligned — booking.rs / rental.rs / document.rs / aml_dsa.rs / auth.rs / buildings.rs all sit under backend/servers/api-server/src/routes/)
- Experts: Rust (primary) + Security (secondary)
- Findings: **3** (all `+3`, vector=security, confidence=medium — manually verified by reading source)
  - `code-review-api-handlers-news-cross-tenant-idor` — 7 handlers in `news_articles.rs` (`get_article`, `delete_article`, `pin_article`, `add_media`, `delete_media`, `list_media`, `record_view`) destructure `TenantExtractor(_tenant)` then discard it; repo SQL keyed on `article_id` alone. Promoted → `plans/security-news-articles-cross-tenant-idor.md` (adversarial pass: passed).
  - `code-review-api-handlers-voice-hmac-timing` — `voice_webhooks.rs:959` HMAC compared with raw `==` (timing leak; sibling webhooks use `verify_slice`). Promoted → `plans/security-voice-webhooks-hmac-bypass.md` (adversarial pass: passed, covers both voice findings).
  - `code-review-api-handlers-voice-default-secret` — `voice_webhooks.rs:950` `VOICE_WEBHOOK_SECRET` falls back to `"default_secret"`; fails open if env var unset. Folded into the voice-webhooks-hmac-bypass plan.
- Next segment: **mobile-native-kmp** (last reviewed 2026-06-06 — now the oldest unreviewed)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: ppt + reality dirs populated; mobile/ still 0
- Drift signals this run: **2** (PR #1454 ppt-web accounting/rentals routes; PR #1545 reality-web /accounting layout — neither touched the matching `docs/screens/<product>/`)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: (not queried this run)

## Backlog deltas
- **New:**
  - `[3] code-review-api-handlers-news-cross-tenant-idor` (security) — promoted
  - `[3] code-review-api-handlers-voice-hmac-timing` (security) — promoted
  - `[3] code-review-api-handlers-voice-default-secret` (security) — folded into voice plan, status=ready
  - `[2] screen-map-drift-pr-1454-ppt` (test-gap) — drift on ppt-web accounting+rentals routes
  - `[2] screen-map-drift-pr-1545-reality` (test-gap) — drift on reality-web /accounting layout
  - `[1] churn-hotspot-backend-crates-db-src-repositories-rental-rs` (refactor) — new hotspot
  - `[1] repeated-churn-backend-crates-integrations-src-booking-rs` (refactor) — booking.rs hotspot for 2nd time (instability proxy)
  - `[1] repeated-churn-backend-crates-db-src-repositories-document-rs` (refactor) — document.rs hotspot for 2nd time
- **Bumped:** none (no signal stacked onto an existing non-cumulative item this run)
- **Decayed:** none (no open items aged past 14 days without new evidence; earliest open `updated_at` is well within 14d)
- **Dropped:** none

## Plans promoted
- `plans/security-news-articles-cross-tenant-idor.md` — 7-handler cross-tenant IDOR fix; add `organization_id` to repo SQL + rebind handler `tenant`; new cross-org IDOR integration test mirroring `form_cross_org_idor_tests.rs` · adversarial pass: passed
- `plans/security-voice-webhooks-hmac-bypass.md` — replace raw `==` with `Mac::verify_slice` (matches `portal_webhooks.rs:112`); fail-closed on missing `VOICE_WEBHOOK_SECRET` (no more `default_secret` fallback); add unit test + grep regression guard · adversarial pass: passed

## Auto-fix
- `no candidate` — Phase 1.5's three +3 findings are `confidence=medium` (not high) and `code-review-finding` is not on the Phase-5 allowlist; the two `screen-map-drift` signals are `confidence=medium` so the high-confidence bar isn't met. Nothing eligible to auto-PR this run.

## Self-review findings
- none (no `.research/self-improvement/findings.json`)

## Open questions
- Buffer health: action-list open count rose to 50 (above the 36 floor), but the dispatcher's `claimable` is 22/72 — most rows are blocked on `depends_on`. The on-demand local `/ppt-project-management scan` is needed to refill the candidate pool with stories the dispatcher can claim. **Operator decision:** is "claimable < floor" tolerable for one more day while the security plans (news-articles IDOR + voice HMAC bypass) consume implementer capacity, or should the scan run now?
- The two screen-map drift signals are heuristic (basename match); promote only if a human marks them real. Phase 3 deferred — they sit at score=2.

## Quiet day?
- **No.** 66 merged PRs, 8 new backlog vectors, 2 plans promoted, 3 high-impact security findings.
