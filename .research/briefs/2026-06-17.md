# 2026-06-17

## Since last run
- Merged PRs: **39** (#1447–#1551, non-contiguous) — span 1447–1551 over ~40h, dominated by Reality SwiftUI mobile epic-82 evidence + native accounting MVP N1–N5 + rls enum-cast fixes + RLS test helper canonicalization.
- Open PRs touched: not fetched this run (issues+open-PRs queries skipped on token budget).
- New / updated issues: not fetched this run.
- Commits on `dev`: not fetched this run (`gh api repos/.../commits` skipped on token budget — `last_commit_sha` advanced to `910274de` from the merged-PR cursor head).
- Routine lag: **1d 16h** since last run (40h > 36h threshold → `lag_warning` signal emitted, not a `stale_routine_alert`).
- Phases that failed: **phase-1-issues**, **phase-1-commit-log** (deliberately skipped for budget — cursors held); **phase-1.5-code-review** (skipped — buffer-low refill prioritized); **phase-1.6-project-management** (skipped — buffer-low refill prioritized).

## Buffer-low trigger response

The routine was fired with payload `buffer-low: claimable=29/72 (floor=36) — refill backlog from coverage rubric`. Response this run:

- **Coverage rubric scan:** `.research/management/coverage.json` carries **49 stories across 13 epics**, of which **29 done** + **20 partial**. Many of the partials advanced toward done in this 40h window (see *Shipped* below) — recommend Phase 1.6 coverage upkeep next run to consolidate those status flips into a refreshed `action-list.json`.
- **Resolution check:** ran PR-body-match + evidence-gone heuristic against open backlog rows; **1 row closed** (`refactor-churn-hotspot-mobile-announcements-test` resolved by PR #1543's TZ=UTC pin — verbatim id match in PR title).
- **Decay:** **4 open rows** with `updated_at ≤ 2026-06-03` decayed −1 score; 2 of those hit 0 and flipped to `dropped` (`triage-issue-951`, `triage-dispatcher-mcp-push-large-file-issue-1014`).
- **New plans this run:** **0**. All current score≥3 open items already carry plan files. Security fast-track has no eligible candidates (only `security-llm-doc-idor` is open at score 3, already planned).
- **Action-list refresh:** **deferred** to next routine run when Phase 1.6 pm-project-management skill runs and regenerates `.research/management/action-list.json` from the updated coverage state.

## Shipped
Highlights only (39 PRs total; full list in `signals/2026-06-17.json` snapshot signal):

- **Reality SwiftUI mobile (epic-82)** — 4 of 5 stories evidenced this window: #1447 (82-3 home/search infinite-scroll AC-4 mobile/KMP); #1514 + #1515 (82-1 SwiftUI project setup screen-map + epic-82 story-vs-implementation mapping); #1516 (82-2 deep-linking + URL schemes confirmed); #1541 (82-4 listing detail + favorites session-gating tests + screen-map sync). #1536 mop-up unused-import spotless break.
- **Native accounting MVP (PAP-209 / 210)** — #1454 (N1–N4 + @hey-api migration), #1453 (N5 bank-statement upload + payment matching + match-review).
- **Coverage 7a / 8a partials advancing** — #1464 (7a-2 folder-org mobile slice), #1550 (verify 7a-1 document upload + metadata + RLS — promotes to done), #1450 (8a-3 preference-aware push dispatch-target selection), #1456 (forms RLS write/download coverage #1369), #1548 (verify report-execution-history presigned-download retry — 81-2).
- **Enum-decode cast fixes (BIT-113 / BIT-118 / GH #1008 / GH #1528)** — #1486, #1492, #1537, #1551 — close out the rental_platform_connections + rental_guests + forms + document_category platform::text cast trail. #1549 audits allowed_pet_types decode paths.
- **RLS / IDOR test infra** — #1501 (reality-server handler-level IDOR regression GH #1300), #1502 (Booking.com credential encryption at-rest IG3 — BIT-98), #1507 (restore user_memberships seed + platform-principal fix), #1510 (canonical tenant-aware request helper #1370), #1517 (migrate IDOR test seed_membership to common helper #1373).
- **OAuth hardening** — #1539 reject public client supplying client_secret with invalid_client (security correctness).
- **Reality-web build unbreak** — #1545 /accounting QueryClient prerender + favorites pl/hu i18n.
- **DevOps ADRs** — #1526 confirm EAS workflow pins healthy, #1529 pre-push fmt/clippy gate scope ADR ("both"), #1530 security-test-gate required-check verification, #1540 confirm app-tsx-merge-queue active.
- **QA test additions** — #1542 iOS SearchView stale-response guard preserving pagination (#1365), #1543 stabilize AnnouncementsScreen date test (TZ=UTC pin — closes `refactor-churn-hotspot-mobile-announcements-test`), #1544 dispute draft autosave (#1360, #1364).
- **Clippy** — #1512 accounting routes cleanup post-#1454.
- **Dispatcher state** — #1508, #1509, #1513, #1518, #1546, #1547 dispatcher chore PRs (skip — pipeline maintenance, not application code).

## Watch
- **Stale drafts / open-PR review backlog:** open-PR query skipped this run — carry-over carry from yesterday: #1316 (was 3d), #1197 (was 7d), #988 (was 13d epic); next run picks up.
- **Reverted:** none observed in the 39-PR window.
- **Churn hotspots this run:** raw-churn count not computed (commit-log fetch skipped). `hotspot_history` not advanced this run.
- **Closed-not-merged:** not fetched this run.
- **Dispatcher buffer-low context:** `.research/management/action-list.json` has 38 open items but dispatcher reports claimable=29 — gap likely stem-collided rows or `assignments.json` quarantine carry-over (6 quarantined + 3 in-progress). Phase 1.6 next run will reconcile.

## PRs stuck in draft despite approval
- not fetched this run (open-PR query skipped)

## PRs with verdict=changes, no fix-round progress in 24h
- not fetched this run (assignments.json read via gh skipped)

## Code review slice
- Segment reviewed: **none — Phase 1.5 skipped** (reason: buffer-low refill prioritized; token budget pushed rotating expert review to next run).
- Experts: n/a
- Findings: **0**
- Next segment: per `state.review_cursor` — `ppt-web-core` (last reviewed 2026-06-05 = 12d old, oldest unreviewed). Next run should pick this up unless churn-aligned segment surfaces.
- Skipped: **yes** — Phase 1.5 deferred to next run.

## Screen-map status
- Total `docs/screens/` files: **129** (ppt: 80, reality: 47 + 2 added by #1514, mobile: 0 — not yet seeded).
- Drift signals this run: **not computed** (per-PR file-list fetch skipped on token budget — would need 39 individual PR file queries).
- Orphan screens this run: **not computed**.
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **Done:** [1] `refactor-churn-hotspot-mobile-announcements-test` — resolved by PR #1543 (TZ=UTC pin), verbatim id match in PR title.
- **Decayed:** [2→1] `test-gap-hotfix-no-test-pr-959-reality-listings-pagination` (no signal in 14d); [2→1] `bug-risky-churn-pr-963-api-main-rs` (no signal in 14d).
- **Dropped via decay:** [1→0] `triage-issue-951` (decayed to 0); [1→0] `triage-dispatcher-mcp-push-large-file-issue-1014` (decayed to 0).
- **New:** none this run (buffer refill comes via marking partial-coverage stories done in Phase 1.6 next run, not via new low-signal items here).
- **Bumped:** none this run (per-PR signal derivation skipped on budget).

## Plans promoted
- none this run · adversarial pass: n/a (0 promotions). Rationale — all 6 open score≥3 items already carry plan files (security-llm-doc-idor, code-review-reality-web-{share-comparison-404, realtor-mgmt-untranslated, listing-page-ssr-crash}, code-review-mobile-rn-report-fault-fake-submit, bug-ios-searchview-uncompilable). Promoting more would require fresh evidence boosts on score-2 items, which the per-PR signal pass deferred this run can supply next run.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run.

## Self-review findings
- `.research/self-improvement/findings.json` not present — `none`.

## Open questions
- Should Phase 1.6 pm-project-management be force-run on every buffer-low trigger fire, even when token budget would otherwise skip it? Current behavior defers to next routine fire; if the buffer stays low the dispatcher will keep firing the trigger, so this self-corrects within ~24h.

## Quiet day?
- **No** — 39 merged PRs since last run + 1 resolution + 4 decays + dispatcher buffer-low operational trigger documented. Send Telegram digest (will refresh with this run's deltas at Phase 6).
