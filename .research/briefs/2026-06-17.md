# 2026-06-17

## Since last run
- Merged PRs: **47** (range #1218–#1499; #1218/#1276–#1313/#1419–#1499 cluster — a single-day backend stabilization sweep + the long-overdue Epic #988 E2E framework + UC-06 announcement pinning + 6 dependabot bumps).
- Open PRs touched: **10** newly active — incl. the #1473 BIT-85 Booking.com cross-tenant token-binding hotfix (`pm-security` flagged it `P-high` for tomorrow), #1462/#1491 alternate idempotency fixes for the BIT-90 invitation-viewed bug (#1301), #1470 DSA-report-download regression-guard, #1489 nullable-decimal RentalBooking decode (#1422), #1460 FORCE-RLS on three developer_oauth tables (#1303), #1471 four ESG/webhook/esignature/public-plans RLS test suites (BIT-74), #1476 RLS-tooling cleanup (#1340/#1341/#1366), #1498 enum-cast for form_submissions.status (superseded same-day by merged #1499).
- New / updated issues: **0 new**, 37 updated (31 closed, 6 still open). The persistent CI-test-job-red issue **#1332** is now mostly addressed — #1469, #1495, #1499 landed the form/document/enum-decode cascade; remaining tail is the rental.rs PAP-158 cluster.
- Commits on `dev`: **50** since `4829015b` → `eb238737` (44 application-code commits + 5 dependabot + 1 routine).
- Routine lag: ~24h since last run — on schedule (no `lag_warning`).
- Phases that failed: **none**.

## Shipped

**Backend test-job unblock cascade (#1332):**
- **#1469** `fix(db): correct schema-drift SQL in document/form/user repos (backend test job red)` — the umbrella schema-drift fix; touched `document.rs` (top churn this run), `form.rs`, `user.rs`. _Note: shipped without a regression test → `risky-churn-pr-1469-document-rs` (+2)._
- **#1495** `fix(backend): unblock dev test job — form RLS-role grant + rental_platform_connections enum decode` — RLS grant + enum decode on the form RLS test.
- **#1499** `fix(db): cast form_submission_status enum in submission-detail queries (#1331/#1332)` — final layer of the form_submission_status cascade.
- **#1487** `style(db): rustfmt budget use-lists after reserve-fund retirement` — rustfmt follow-up.
- **#1481** `fix(backend): retire broken budget.rs reserve-fund duplicate surface (schema drift)` — drove `budgets.rs` to top-3 churn (#258 LOC) this run → new `churn-hotspot-…-budgets-rs` (+1).

**Security / OTA hardening:**
- **#1472** `fix(api-server): encrypt Booking.com connect credentials at rest (#1362)` — closes credential-at-rest gap on the connect endpoint.
- **#1485** `fix(integrations): Booking.com client timeout + Retry-After + RateLimited (BIT-110, GH #1338)` — outbound-client resilience.
- **#1457** `fix(api-server): org-scope create_appeal/add_evidence/add_comment writes (BIT-75, #1342/#1339)` — closes 3 cross-org write surfaces on appeals.
- **#1463** `fix(api-server): resolve building/unit scope in document download/preview gate (GH #1413)` — building/unit-scope tightening on document downloads. (Handler-level IDOR test still owed — pm-security flagged #1300 as high-priority follow-up.)

**Dispatcher self-healing:**
- **#1483** `fix(research): correct dispatcher Tier-2 kick payload + anthropic-version header (closes #1151)` — partial resolve of triage-issue-1151.
- **#1448** `fix(research): populate depends_on in pm-analysis/gap-scan emit + clear leaked role-deps (T25/#583)`.
- **#1444** `fix(research): broaden dispatcher self-test T21 to accept full-task_id epic_prefix`.

**CI hardening cluster:**
- **#1490** `ci(rls): fail --strict on stale RLS baseline entries (#1340)`.
- **#1455** CI fmt+clippy gate scope (post-merge).
- **#1452** dev-push compile gate.
- **#1441** pre-push cargo fmt + clippy gate (`#1375`).

**Feat / Epic landings:**
- **#988** — `Epic: reusable Playwright E2E framework @ppt/e2e + sitemap FlowRunner` (13d open — long carry, now landed).
- **#1446** UC-06 pinned-first announcement ordering in ppt-web.
- **#1392** `feat-home-and-search-screens-debounced-search-evidenced-mobile` — AC-2 evidence for reality search debounce.
- **#1179** docs(epics) catalog backfill for 37 modules (PAP-27).

**Test gaps closed:**
- **#1443** test(reports) cron-validator drift guard (#1368).
- **#1218** test(api-server) vendor-portal unmounted-route 404-vs-401 (PAP-66).
- **#1442** refactor(tests) replace duplicate `seed_membership` with canonical `common::` import (#1373).
- **#1451** test(forms) submit non-empty data (BIT-60).
- **#1477** test(mobile) App.tsx auth-gate wiring test (#1411).
- **#1427/#1428/#1429/#1433/#1434/#1438/#1440/#1465/#1468/#1419** — broader fix sweep across forms/RLS/work-orders/IDOR/iOS-deeplink.

## Watch
- **Top churn this run (excluding test files + Claude tooling):** `backend/crates/db/src/repositories/document.rs` (4 commits, 114 LOC, runs_seen=1→2 → `repeated-churn` fired +1), `backend/servers/api-server/src/routes/budgets.rs` (2 commits, 258 LOC — first appearance).
- **Risky-churn flagged:** PR #1469 (schema-drift fix in document/form/user repos) touched the `document.rs` hotspot without a `_test.rs` paired in diff → new `bug-risky-churn-pr-1469-document-rs` (+2).
- **Reverts:** none.
- **Stalled review:** none over the dispatcher's 7d threshold this run (carry-over PRs are now mostly merged; #988 landed after 13d open).
- **Open PRs touched (live):** #1473 (BIT-85 cross-tenant Booking.com token-binding + manager gate — pm-security high), #1462 + #1491 (two alternate idempotency fixes for #1301 — needs decision), #1470 (DSA-report-download regression test), #1489 (nullable-decimal RentalBooking decode), #1460 (developer_oauth FORCE RLS #1303), #1471 (4 RLS test suites — BIT-74), #1476 (RLS dev-tooling cleanup), #1464 (mobile folder-org — verdict=changes pending fix, 5h ago), #1498 (form_submissions enum cast — superseded by merged #1499).
- **Closed-not-merged:** #1497 (registry INSERT-branch NULL-boolean test) — closed 15 min after open, superseded by parent PR; no signal (self-cleanup).

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (closest is `feat-folder-organization-mobile-implementation` / PR #1464 — last_updated 5h ago, biome organizeImports lint error on `useFolderTree.ts:14`. Within the 24h tolerance.)

## Code review slice
- Segment reviewed: **ppt-web-core** (reason: oldest-unreviewed at 12d stale; backend churn — `document.rs`, `budgets.rs` — sits outside the 8-segment map, no clean churn alignment to a fresh segment).
- Experts: frontend.
- Findings: **3** — see Backlog deltas for `code-review-ppt-web-core-mfa-verify-hardcoded-url` (high, +3), `code-review-ppt-web-core-api-base-url-env-drift` (high, +2), `code-review-ppt-web-core-mfa-wrapper-untested` (high, +2). All three concentrate on the MfaWrapper + auth-URL surface in `App.tsx` / `AuthContext.tsx` / `main.tsx` / `lib/api.ts`.
- Next segment: **mobile-native-kmp** (last reviewed 2026-06-06 — oldest after this run).
- Skipped: no — reviewed.

## Screen-map status
- Total `docs/screens/` files: **127** (ppt: 80, reality: 47, mobile: 0 — not yet seeded).
- Drift signals this run: **0** (none of the 47 merged PRs touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**` without also touching `docs/screens/*`; #1477 touched `frontend/apps/mobile/src/App.tsx` but mobile remains excluded from the trigger set until `docs/screens/mobile/` exists).
- Orphan screens this run: **0**.
- Last `screen-map.yml` CI run on `main`: not fetched this run (no `docs/screens/**` edits to trigger).

## Backlog deltas
- **New:**
  - [3] `code-review-ppt-web-core-mfa-verify-hardcoded-url` — App.tsx:69 hardcoded `/api/v1/auth/mfa/verify` ignoring VITE_API_URL → cross-origin deploy MFA 404.
  - [2] `code-review-ppt-web-core-api-base-url-env-drift` — VITE_API_URL vs VITE_API_BASE_URL split-brain (3 modules, 2 names).
  - [2] `code-review-ppt-web-core-mfa-wrapper-untested` — MfaWrapper.verify swallows non-2xx into `false`; no test exercises it.
  - [2] `bug-risky-churn-pr-1469-document-rs` — schema-drift fix on the top churn file, no test in diff.
  - [1] `churn-hotspot-backend-servers-api-server-src-routes-budgets-rs` — 258 LOC across #1481, #1487 (first appearance).
- **Bumped:** [1→2] `churn-hotspot-backend-crates-db-src-repositories-document-rs` — runs_seen=1→2 across 2026-06-10 / 2026-06-17 (`repeated-churn` signal stacked +1; original `churn-hotspot` re-emitted with +0).
- **Decayed:** none (no open item with `updated_at < 2026-06-03`).
- **Dropped:** none.
- **Resolved:** `bug-ios-deeplink-info-plist-missing` → `done` (PR #1465 merged 2026-06-16; `mobile-native/iosApp/Info.plist` no longer tracked → evidence-gone).
- **Dep-update noise (logged, score 0, NOT in backlog):** #1276, #1277, #1278, #1279, #1313, #1449.

## Plans promoted
- none (cap=2, ready queue already at 6 — 5 of them score 3 with the implementer; promotion of `code-review-ppt-web-core-mfa-verify-hardcoded-url` deferred to next run so the queue can drain. The MFA bug stays at `open` / score 3, ready to promote tomorrow if nothing picks it up.)

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The 3 high-confidence Phase 1.5 findings are bug/refactor/test-gap, none of which match the auto-fix allowlist (screen-map-drift, screen-map-orphan, fixme-in-merged-code, dep-update-noise lockfile-drift sub-shape, stalled-review). `risky-churn-pr-1469-document-rs` is medium-confidence and outside the allowlist. No lockfile-drift detected from the 6 dependabot PRs (caret-spec / lockfile-pinned version alignment held).

## Self-review findings
- `.research/self-improvement/findings.json` absent — none.

## Open questions
- **Decision on #1462 vs #1491:** two alternate idempotency fixes for the BIT-90 `mark_invitation_viewed` 404-on-repeat-view bug (#1301). Both target the same root cause; pick one and close the other.
- **#1473 BIT-85 cross-tenant Booking.com token-binding:** open, pm-security flagged it `P-high` — Booking.com OAuth `ON CONFLICT (unit_id, platform)` for nil-UUID org-level rows could silently rebind tokens across orgs. Needs a manual review pass before merge (high-blast-radius RLS-adjacent migration adding partial unique index `WHERE unit_id = nil-UUID`).
- **#1300 handler-level IDOR test:** still open after #1463 closed the document-download/preview SQL gate. pm-security flagged this as the next IDOR test-gap to close.
- **Phase 1.6 PM rotation advanced** — pm-security ran this run (last 2026-05-27 → 21d stale; next: pm-data at index 6). Coverage cursor advanced 12 → 0 (epic-9 re-checked; epic-10a next).

## Quiet day?
- **No.** 47 application-code PRs merged (single-day record), 5 new backlog vectors + 1 bumped + 1 resolution + 0 plans promoted, 3 Phase 1.5 findings (ppt-web-core MfaWrapper / VITE_API_URL drift cluster), Phase 1.6 pm-security rotation after 21d stale (5 new risks, 5 new actions, 1 epic re-checked). `stats.runs` 40 → 41, `vectors_created` 173 → 178, `plans_created` 12 → 12, `auto_fix_count` 0 → 0.
