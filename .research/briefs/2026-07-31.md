# 2026-07-31

## Since last run
- Merged PRs: 21 (range #2577–#2609)
- Open PRs touched: 14
- New / updated issues: 4 (all closed same-window)
- Commits: 44 on `dev` (mostly dispatcher bookkeeping; 21 landed PR squashes + tier-1d review signal folds)
- Routine lag: 1d 2h since last run (on schedule)
- Phases that failed: none

## Shipped
- **Voice-webhooks tests, no fix** — #2604 test-gap-voice-webhooks-zero-coverage added 33 unit tests, but the tests **pin the current broken behavior** (Alexa signature never verified, auth bypass returns any tenant's device). Surfaced 3 P0 findings in Phase 1.5 — see Backlog deltas.
- **Layout admin/tenant error handling** — #2606 stopped silently swallowing serde failures into `Value::Null` (200 OK with `null` body); routes now log + return 500. Companion of #2609 (layout/resolved.rs raw-error leak).
- **Scheduler notification-target visibility** — #2608 surfaced DB errors on scheduled announcement/vote dispatch (previously coerced Err → empty target set with no log).
- **Reality-web tenant-JSON XSS** — #2600 escaped `<script>`/U+2028/U+2029 in the per-request tenant bootstrap and JSON-LD payloads via new `serializeForScript` helper.
- **Reality-web viewsource cardinality** — #2603 validated `?source=` against the known `ViewSource` set; unbounded value now maps to `unknown`.
- **AML review-decision validation** — #2596 stopped casting raw `window.prompt` text into the approve|reject|escalate union.
- **Android SSO initiation leg** — #2593 (closes #2574) wired the mint-CSRF-nonce hop that PR #2568 shipped only half of; every `reality://sso` callback was being default-rejected on Android.
- **Documents by-file-key guard** — #2597 (closes #2573) blocks DELETE from reaping S3 objects still referenced by another org's document row.
- **PPT-web WebSocket token rotation** — #2578 re-auths socket on token rotation (previously left a stale socket bound to the rotated-out token).
- **PPT-web WebSocket dev-only diagnostics** — #2592 gated 10 console.warn/error calls in websocket.ts behind `import.meta.env.DEV`.
- **Support-tooling retention regression** — #2594 added DB-backed test for the scheduler prune wired by #2547 (previous test-gap-hotfix carrier).
- **Reports.rs cadence extraction** — #2599 moved schedule cadence/cron/next-run helpers into new `reports/schedule_cadence.rs` (889-line new file — top-1 churn this window).
- **Admin-web honest-Save refactor** — #2601 dx: platform-settings + mobile-config Save buttons no longer fire non-existent PATCH endpoints (365-line refactor to display-only surface).
- **Frontend verify-gate unblocked** — #2602 (closes #2598) fixed pre-existing admin-web + reality-web test failures blocking the PR verify gate.
- **Reality-api-client drift gate** — #2607 (closes #2556) mirrors the @ppt/api-client generated-SDK drift gate for @ppt/reality-api-client.
- **Refactor: org-property authz test dedup** — #2579 collapsed triple-duplicated fixture helpers.
- **Refactor: platform-admin authz batch2 dedup** — #2580 collapsed anon/authed request builders.
- **Screen-map reality reconciliation** — #2577 updated listing-detail write-side note for layout-revalidate hardening.
- **Mobile-native portfolio-analytics pin** — #2595 reverted a phantom "fix" and retained the correct regression test.
- **Stale TODO(security) removal** — #2605 dropped obsolete comment headers in faults.rs / critical_notifications.rs (roles gate is real now).

## Watch
- **Stalled review:** none — accounting trio (#2555/#2558/#2559) at 3d, under threshold. Dependabot swarm (#2581–#2591) all fresh (~1d).
- **Reverted:** none this window.
- **Churn hotspots:**
  - `backend/servers/api-server/src/routes/reports/schedule_cadence.rs` (889 lines this run, first appearance — PR #2599 extraction)
  - `backend/servers/api-server/src/routes/voice_webhooks.rs` (472 lines this run, first appearance — PR #2604 test additions; **same file has 3 P0 security findings this run**)
  - `frontend/apps/admin-web/src/pages/MobileConfigPage.tsx` (365 lines this run, first appearance — PR #2601 no-op cleanup)
- **Secondary hotspots** (not top-3 but repeated appearance vs `hotspot_history`):
  - `backend/servers/api-server/src/services/scheduler.rs` — runs_seen 1→2 (PRs #2594, #2608)
  - `backend/servers/api-server/src/routes/layout/{admin,tenant}.rs` — runs_seen 1→2 (PR #2606)

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: `api-handlers` (reason: churn-aligned — 3 hotspots + 4 secondary hits in `routes/`)
- Experts: rust (with security lens)
- Findings: 3 (all in `voice_webhooks.rs`, all high-severity security, all confirmed by direct file read):
  - Cross-tenant auth bypass in `authenticate_voice_user` — impersonation on every Alexa/Google voice webhook
  - HMAC default-secret fallback (`"default_secret"`) + non-constant-time compare
  - `verify_alexa_signature` never checks the signature (`_signature` bound-and-ignored)
- Next segment: `reality-server` (oldest unreviewed after this run — 2026-07-18)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 153 (ppt: 106, reality: 47, mobile: 0 — not seeded)
- Drift signals this run: 1 (PR #2600 touched reality-web route files without updating `docs/screens/reality/*.md`)
- Orphan screens this run: 0 (spot-check only per routine guidance)
- Last `screen-map.yml` CI run on `main`: success (2026-06-04 — no route-touching commit has landed on main since)

## Backlog deltas
- **New:** [3] `code-review-api-handlers-voice-webhooks-auth-bypass` — SECURITY-P0 cross-tenant impersonation on Alexa/Google voice webhooks
- **New:** [3] `code-review-api-handlers-voice-webhooks-hmac-default` — SECURITY-P0 verify_hmac_signature fails open to hardcoded `default_secret`
- **New:** [2] `screen-map-drift-pr-2600-reality` — PR #2600 touched reality-web routes with no docs/screens/reality/*.md update
- **New:** [1] `churn-hotspot-...reports-schedule_cadence-rs` — 889 lines this run (PR #2599 extraction)
- **New:** [1] `churn-hotspot-...voice_webhooks-rs` — 472 lines this run (PR #2604 tests)
- **New:** [1] `churn-hotspot-...mobileconfigpage-tsx` — 365 lines this run (PR #2601 no-op cleanup)
- **Bumped:** [5→7] `security-voice-webhook-alexa-signature-not-verified` — re-opened; Phase 1.5 re-confirmed the bypass persists after PR #2604's test-only additions
- **Done:** `code-review-api-core-resolved-rs-leaks-db-error` — PR #2609 merged 2026-07-31 (verbatim id match)
- **Decayed:** none (no open item over 14 days without new evidence)
- **Dropped:** none this run

## Plans promoted
- `plans/security-voice-webhooks-auth-bypass.md` — wire real OAuth token validation into `authenticate_voice_user` (fixes the cross-tenant impersonation) · adversarial pass: passed
- `plans/security-voice-webhooks-signature-hardening.md` — fail-closed HMAC (no default secret) + constant-time compare + real Alexa cert-chain signature verification (covers both `code-review-api-handlers-voice-webhooks-hmac-default` and the re-opened `security-voice-webhook-alexa-signature-not-verified`) · adversarial pass: passed

## Auto-fix
- `no candidate` — the one non-code-review signal (`screen-map-drift-pr-2600-reality`, score 2) is confidence=medium, so it fails the Phase 5 certainty bar (requires `high`). The 3 voice_webhooks code-review findings are high-confidence but `code-review-finding` is not on the Phase 5 allowlist (goes through the manual plan path — 2 plans promoted this run cover them).

## Self-review findings
- open=21 (high=4, medium=13, low=4); new this run=0; acknowledged=19
- fp-1 [recurrence=11] — Phase 6 archive-move produces >64KiB assignments-archive.json that MCP `push_files` cannot inline (and direct `git push` is proxy-403'd here); fix: adopt the PR+API-merge path (push to a new branch → open PR → `mcp__github__merge_pull_request` squash) — proven workaround already used.
- fp-2 [recurrence=7] — dispatcher-prompt hard-assumes `git push` is proxy-403-blocked; in this env with `@users.noreply.github.com` commit-email `git push origin dev` actually succeeds, so the 64KiB size-guard false-positives; fix: add a runtime push-capability probe in Phase 1 + bypass the size-guard when `PUSH_METHOD=git`.
- fp-3 [recurrence=3] — Phase 2.6 buffer metric reports claimable=54 while REAL claimable=0 (all 54 open action-list items already terminal in archive by exact id); fix: extend gc1-reconcile to auto-close open items whose task_id is terminal in active+archive INCLUDING failed status, and subtract archive task_ids in the GC3 formula.
- fp-4 [recurrence=2] — Tier-2 planner kick returned http=401 (DISPATCHER_TOKEN rejected); the buffer-refill safety net is broken by an operator secret/config drift; fix: operator rotates DISPATCHER_TOKEN, dispatcher branches on 401/403 to escalate loudly instead of folding into generic "kick failed".

## Open questions
- Should the voice-webhook endpoints be sunset entirely (return 501) until real OAuth token linking + Alexa cert-chain verification ship, or should the two promoted plans land in parallel and gate real deploy on operator setting `VOICE_WEBHOOK_SECRET`? Deferred to security review on the two plans.

## Quiet day?
- No — 21 merged PRs, 6 new backlog items, 2 plans promoted (both high-severity security in voice_webhooks.rs), 3 code-review-finding signals from Phase 1.5, and 1 re-opened score-7 security item.
