# 2026-08-02

## Since last run
- Merged PRs: 1 (#2626)
- Open PRs touched: 1 (#2627 — dispatcher-opened, in review)
- New / updated issues: 0 past cursor (last_issue_seen=#2612)
- Commits: 1 code merge (#2626 code-review-api-core-idempotency-client-tenant) + 4 `chore(research)` dispatcher commits since the 04:25Z routine cursor
- Routine lag: 7h55m since last run — on schedule (this is a mid-day `buffer-low: claimable=1/72` refill fire, not the daily cadence; morning routine already ran at 04:25Z)
- Phases that failed: none

## Shipped
- #2626 code-review-api-core-idempotency-client-tenant — tenant_scope() now derives idempotency cache-scope solely from server-side `ResolvedTenant` (X-Tenant-ID header read removed); +2 sqlx tests prove two different authenticated tenants sharing a key do not collide AND a spoofed header cannot change scope. Closes the security fast-track vector promoted this morning; the dispatcher shipped it via fast-path.

## Watch
- Stalled review: none (no open PR has crossed the 7-day threshold — accounting-MVP trio at 5–6 days idle noted in the 2026-07-30 project-state)
- Reverted: none this run
- Churn hotspots: none — no application-code churn this run (only dispatcher `chore(research)` commits + 1 real merge)
- Dispatcher active work: PR #2627 (`code-review-api-core-ssrf-validator-drift`) opened 10:20Z, `scope_drift=true`, in review (`reviewer_summary=null`); `code-review-reality-server-agency-members-unauth-idor` claimed 04:10Z, no PR yet (`reclaim_attempts=1`). Morning-promoted `sso-session-invalidate-swallowed` and `db-error-leak-to-client` were reconciled to `dropped` by the 06:16Z dispatcher (`assignment_status=failed`) — that's why the claimable buffer drained to 1/72 and triggered this run.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: `ppt-web-ui` (reason: oldest-unreviewed — cursor was 2026-07-21, 12d old; churn-aligned N/A since no application code churned this run)
- Experts: frontend + completeness (secondary per skill's ppt-web-ui assignment)
- Findings: 1 (see Backlog deltas — `code-review-ppt-web-ui-offline-indicator-no-i18n` Δ+2 high; whole-component i18n gap on globally-mounted `<OfflineIndicator />`, hardcoded English at OfflineIndicator.tsx:59 + :89, mounted at App.tsx:222)
- Next segment: `mobile-rn` (oldest unreviewed after this run: 2026-07-22, 11d old)
- Skipped: no — reviewed (2nd Phase 1.5 slice this day; api-core was reviewed at the 04:25Z morning run)

## Screen-map status
- Total `docs/screens/` files: unchanged (ppt/ + reality/; mobile/ not yet seeded → 0)
- Drift signals this run: 0 (no route files touched this run)
- Orphan screens this run: 0 (no route files touched this run)
- Last `screen-map.yml` CI run on `main`: not checked this run (no drift trigger)

## Backlog deltas
- **New:** [2] `code-review-ppt-web-ui-offline-indicator-no-i18n` — Phase 1.5 ppt-web-ui review; MEDIUM severity (Δ+2), HIGH confidence; `frontend/apps/ppt-web/src/components/OfflineIndicator.tsx` shows hardcoded English to sk/cs/de users on every offline event
- **Resolved (open → done):** [2] `code-review-api-core-idempotency-client-tenant` — PR #2626 merged 10:15Z (item id appears verbatim in PR title; `X-Tenant-ID` read pattern is gone from `tenant_scope()` — only the safety docstring/comment mentions remain)
- **Bumped:** none
- **Decayed:** none (no open items older than 14 days)
- **Dropped this run:** none (the two morning-promoted reality-server plans were dropped by the 06:16Z dispatcher, not by this routine; recorded above under Watch for context)

## Plans promoted
- (none)

Rationale: post–Phase 1.5, exactly one candidate has any promotable shape (`offline-indicator-no-i18n`), and it sits at score 2 with `vector=bug` — below the standard `score >= 3` threshold and outside the security fast-track (`vector != "security"`). The two other open items with `score = 3` are already claimed by the dispatcher (`agency-members-unauth-idor` in-progress, `ssrf-validator-drift` in review via PR #2627) — the readiness gate's slug-stem uniqueness rule against `assignments.json` status ∈ {in-progress, review, quarantined} blocks their promotion. Buffer stays at 1 claimable until (a) another PR merges freeing an in-flight slot, (b) a second signal stacks on offline-indicator to reach score 3, or (c) the next daily Phase 1.5 rotation surfaces new score ≥3 items.

## Auto-fix
- `no candidate` — the only new signal is a `code-review-finding` (not on the auto-fix allowlist). No `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, or `stalled-review` fired.

## Self-review findings
- open=28 (high=6, medium=19, low=3); new this run=3; acknowledged=19
- fp-mcp-push-cannot-inline-large-archive [recurrence=13] — dispatcher archive files (>64KB) can't go through MCP `push_files` inline cap; fix: land archive commits via PR + API merge (bypasses both proxy-403 direct-push AND the inline size limit)
- fp-mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=9] — dispatcher-prompt hard-assumes `git push` is proxy-403 blocked, but direct `git push` actually works in this env; fix: runtime probe at Phase 1 rather than a hard-coded assumption
- fp-stale-in-progress-action-list-decoupled-from-assignments [recurrence=3] — `action-list.json` carries in-progress rows without matching `assignments.json` entries; fix: extend `gc1-reconcile.sh` to close orphaned action rows
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — dispatcher's Phase 2.6 buffer metric reports high claimable but real claimable = 0 because it doesn't filter items whose plan file is archived; fix: two-part reconcile close-step
- fp-implementer-sandbox-zero-pushed-branch-multigen [recurrence=2] — implementer rows produce zero branches on origin across multiple generations; fix: escalation when `reclaim_attempts >= 1` and status still `in-progress`

## Open questions
- Phase 1.6 (project management) skipped this run — the 06:16Z / 08:19Z / 10:20Z dispatcher commits already refreshed `.research/management/assignments.json` and `action-list.json`, and no code merged since Phase 1.6 last had signal. `pm_cursor` intentionally not advanced; the morning brief's open question about the pm-frontend rotation (last-run 2026-06-10, 53d stale) still stands and is on my radar for the next daily fire.

## Quiet day?
- No — 1 new backlog vector (offline-indicator-no-i18n), 1 resolution (idempotency-client-tenant → done), and Phase 1.5 review of `ppt-web-ui` all landed this run. Buffer-low refill trigger acknowledged; the second Phase 1.5 slice did produce a new signal, but its score/vector combination doesn't clear the promotion threshold this run.
