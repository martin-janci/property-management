# 2026-08-02

## Since last run
- Merged PRs: 0 (window 2026-08-01T18:19Z → now; last merge was #2625 already covered by yesterday's brief)
- Open PRs touched: 0 (14 open PRs — all created 2026-07-30 or 2026-07-28; none touched this window)
- New / updated issues: 2, both closed same-day (#2621 layout-revalidate pre-auth target_tenant — resolved by PR #2624; #2617 duplicate of #2575 — closed as dup). last_issue_seen advanced 2612 → 2621. 3 open follow-up issues remain (#2612, #2575, #2366) — no state change vs last run.
- Commits: 4 on `dev` since last routine (3 dispatcher chore commits `.research/management/` + 1 previous routine commit). Zero application-code churn.
- Routine lag: 6h since last run (on schedule)
- Phases that failed: none

## Shipped
- none (nothing merged in this 6h window)

## Watch
- Stalled review: none (14 open PRs — oldest is #2555 at 5 days, still below the 7-day threshold; all 11 dependabot PRs from 2026-07-30 have no reviewDecision but are also <5 days old)
- Reverted: none
- Churn hotspots: **no churn this run** (only `.research/` chore commits on `dev` since last routine — no application-code diffs in the window; hotspot_history not bumped, no cumulative signal emitted)
- Dispatcher buffer: **starved** — trigger payload flagged `claimable=0/72 — backlog exhausted, 5 rows sandbox-stalled`. All 4 top open backlog items are already `in-progress` in `assignments.json` (see *Open questions* below); stem-uniqueness blocks the routine from re-promoting them.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher assignments.json shows 0 `review` rows — the 5 active rows are all `in-progress`, none in `review`)

## Code review slice
- Segment reviewed: `api-core` (reason: **saturation-skip** — oldest `reality-server` at 2026-07-18 already has 3 open `code-review-reality-server-*` items in backlog; next-oldest `api-core` at 2026-07-20 has zero open findings, picked)
- Experts: rust
- Findings: 2 (see Backlog deltas — one high-severity SSRF-via-redirect promoted straight to plan, one medium-severity raw-DB-error-leak surfaced at score 2)
- Next segment: `api-core` again is now most recent, so next-oldest becomes `ppt-web-core` (2026-07-24) — unless reality-server's saturation clears in the meantime
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 153 (ppt: 106, reality: 47; mobile: 0 — not yet seeded)
- Drift signals this run: 0 (no merged PRs to derive drift from; nothing touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**` since last run)
- Orphan screens this run: 0 (heuristic scan skipped — no drift context this run)
- Last `screen-map.yml` CI run on `main`: not queried this run

## Backlog deltas
- **New:** [3] `code-review-api-core-api-call-ssrf-redirect` — Workflow API-call action's reqwest client silently follows redirects, `validate_external_url` only checks initial URL → SSRF to `169.254.169.254` / `127.0.0.1` via 302 (rotating-expert-review api-core)
- **New:** [2] `code-review-api-core-agencies-rs-raw-db-err-leak` — `routes/agencies.rs` returns raw `sqlx::Error` text to authenticated clients across ~25 map_err sites; mirrors already-fixed `resolved.rs` pattern (rotating-expert-review api-core)
- **Bumped:** none
- **Decayed:** none (0 open items updated_at ≤ 2026-07-19 — none in the 14-day decay window)
- **Dropped:** none

## Plans promoted
- `plans/code-review-api-core-api-call-ssrf-redirect.md` — one-line fix (`Client::builder().redirect(reqwest::redirect::Policy::none())` on `ApiCallExecutor::new()`) + fail-closed on 3xx + regression test using local 302→internal target · security, cloud-ok mode · adversarial pass: passed

## Auto-fix
- `no candidate` — the one promotable score-3 vector this run (`code-review-api-core-api-call-ssrf-redirect`) is a source code change (add `.redirect(Policy::none())` on `reqwest::Client::builder()`), not on the Phase 5 allowlist (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile drift, `stalled-review`). Kill switch `ROUTINE_AUTOFIX_DISABLED` not observed set.

## Self-review findings
- none (`.research/self-improvement/findings.json` not present in this repo scope)

## Open questions
- **Buffer refill vs. stem collision.** All 4 top open backlog items (`code-review-reality-server-sso-session-invalidate-swallowed`, `code-review-reality-server-db-error-leak-to-client`, `test-gap-disputes-kpis-window-validation`, `code-review-reality-server-password-reset-no-transport`) are already `in-progress` in `.research/management/assignments.json` — the dispatcher has claimed them but their implementers stalled per the trigger's `5 rows sandbox-stalled` note. The routine cannot re-promote them (stem-uniqueness blocks) and cannot force-drop the dispatcher's claim (out of scope). Realistic next step for the operator: hand-inspect why the 5 implementers stalled (likely the same cloud egress `github.com` block that PR #2620/#2623/#2625 all called out — `utoipa-swagger-ui` build script downloads Swagger-UI zip from github.com; sandbox proxy returns 403). The routine's contribution this run was one *fresh* actionable plan (SSRF) that gives the dispatcher's Tier-2 planner something to promote once the buffer clears.
- **Phase 1.6 skipped for the 3rd consecutive run** — the buffer-low trigger keeps taking priority over PM rotation cadence. `pm_cursor.next_index=2` (pm-frontend) is untouched; `role_last_run.pm-frontend=2026-06-10` is now 54 days stale. `pm_cursor` unchanged this run, so G16 (with its GC7 coverage-cursor clause) is a no-op per its own scope. Recommend a dedicated `pm-full` or `pm:frontend` trigger next opportunity to break the streak — deferring it a 4th time risks the PM signal going effectively cold.

## Phase skips (routine time budget)
- **Phase 1.6 (project-management delivery):** skipped (3rd consecutive). `pm_cursor.next_index=2` untouched; `coverage_cursor.next_index=4` untouched; management artifacts unchanged.

## Quiet day?
- No (0 merged PRs, but 2 new fresh signals from Phase 1.5, 2 new backlog vectors, 1 plan promoted — meaningful refill toward the buffer-starvation trigger)
