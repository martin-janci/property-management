# 2026-08-02

## Since last run
- Merged PRs: 0 (last cursor #2627; no new merges since 12:11Z — 6h+ quiet on `dev`)
- Open PRs touched: 0 since 14:23Z rerun
- New / updated issues: 0 (cursor #2628 unchanged since 12:16Z)
- Commits: 3 on `dev` since cursor `21a036ee` — all housekeeping (dispatcher chore ×2 + this morning's research rerun `d739d010`); zero app-code churn
- Routine lag: 3h since last run — on schedule (3rd routine firing today: 04:25Z / 14:23Z / 17:14Z)
- Phases that failed: none

## Shipped
- none (last two shipped were PRs #2626/#2627, already recorded in the 14:23Z rerun brief)

## Watch
- Stalled review: none crossed the 7-day threshold yet (same-day rerun; state unchanged from 14:23Z)
- Reverted: none
- Churn hotspots: none — zero app-code merges since last cursor

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: `ppt-web-ui` (reason: oldest-unreviewed; last touched 2026-07-21 = 12 days; zero churn hotspots this run so tie-break falls to age)
- Experts: frontend
- Findings: 2 (both high-confidence, score_delta=2, none reached ready threshold ≥3)
  - `code-review-ppt-web-ui-untranslated-shared-components` — refactor, shared components render hardcoded English (OfflineIndicator, ProtectedRoute role-denial screen, ConfirmationDialog defaults). Mirrors the `code-review-reality-web-realtor-mgmt-untranslated` pattern that already shipped.
  - `code-review-ppt-web-ui-auth-callback-double-decode` — bug, `AuthCallbackPage.tsx:100` calls `decodeURIComponent(errorDescription)` on a value URLSearchParams already decoded, so any provider error whose `error_description` contains a literal `%` throws URIError and drops the callback route to the ErrorBoundary instead of showing the friendly "Sign-in failed" screen.
- Next segment: `mobile-rn` (oldest unreviewed after this run — cursor 2026-07-22)
- Skipped: no — this segment slot was untouched by the earlier reruns today (they consumed `api-core` at 04:25Z and skipped 1.5 entirely at 14:23Z citing per-run cap; the spec has no per-day cap, so refilling here is the correct response to the buffer-low trigger)

## Screen-map status
- Total `docs/screens/` files: 153 (ppt=106, reality=47, mobile=0 — not yet seeded)
- Drift signals this run: 0 (no route files touched this run)
- Orphan screens this run: 0 (no route files touched this run)
- Last `screen-map.yml` CI run on `main`: not checked this run (no drift trigger)

## Backlog deltas
- **New:**
  - [2] `code-review-ppt-web-ui-untranslated-shared-components` (refactor, open, confidence=high) — see backlog.md
  - [2] `code-review-ppt-web-ui-auth-callback-double-decode` (bug, open, confidence=high) — see backlog.md
- **Bumped:** none
- **Decayed:** none (no open items older than 14 days at run start; the 2 new items are same-day fresh)
- **Dropped:** none

## Plans promoted
- none this run — both new items are score=2, non-security, so they miss the ready threshold (≥3 for non-security, ≥2 only under the security fast-track). Backlog now has 2 active items (both open); they will compound if the same shared-component or SSO-callback pattern re-surfaces, or land as plans once a manual +1 nudges them past 3.

## Auto-fix
- `no candidate` — neither `code-review-finding` matches the Phase 5 allowlist (screen-map-drift, screen-map-orphan, fixme-in-merged-code, dep-update-noise-lockfile, stalled-review). Auto-fix is not the right shape for code-review findings; those go via the manual implementer path (dispatcher).

## Self-review findings
- open=28 (high=6, medium=19, low=3); new this run=0; acknowledged=19
- fp-null [recurrence=13] — assignments-archive.json too large (198KB) for MCP push_files inline; fix: use PR+API-merge path as primary push mechanism
- fp-null [recurrence=10] — dispatcher hard-assumes proxy-403 for git push; env actually permits it if commit email is @users.noreply.github.com; fix: runtime push-capability probe at Phase 1
- fp-null [recurrence=4] — action-list.json in-progress twin diverges from assignments.json live rows; fix: Phase-2 reconcile step to reset action-list on missing assignment
- fp-null [recurrence=3] — in-progress rows produce ZERO branches on origin across two implementer generations; fix: escalate branchless-after-reclaim to status=failed
- fp-null [recurrence=3] — Phase 2.6 claimable metric ignores archive-terminal ids; fix: extend gc1-reconcile to close open items terminal-in-archive for any status incl. failed
- ... and 1 more (see findings.json)

## Open questions
- **Buffer refill under way — but not immediately claimable.** The trigger payload flagged `claimable=0/72 — refill planner`. This run added 2 open backlog items (`code-review-ppt-web-ui-*`) via Phase 1.5, but both are score=2 and neither will be promoted to a plan until score ≥3. The dispatcher's action-list won't pick them up either (its refill draws from ready/promoted items, not raw score=2 open). Practical implications:
  - If buffer-low keeps firing across the day, either (a) let tomorrow's routine run its next-segment review (mobile-rn) and hopefully find a score-3 finding, or (b) manually bump one of these two items to score 3 in `backlog.json` so it promotes on the next routine run.
  - The auth-callback double-decode bug is user-visible (breaks SSO error UI on any `%` in `error_description`) and worth an early hand-promotion if the operator wants a dispatcher pick-up before the next natural score bump.
- Phase 1.6 skipped this rerun — dispatcher refreshed `management/*` at 12:16Z and 14:09Z; pm_cursor stays at 2 (pm-frontend), advanced by yesterday's routine. Zero delivery flux since 14:23Z does not warrant re-running the full PM synthesis on the 3rd rerun of the day.

## Quiet day?
- No — Phase 1.5 rotating review of `ppt-web-ui` surfaced 2 new backlog items (both high-confidence, score 2). Signals derived, state advanced, backlog & triage regenerated. Buffer refill partial (items added but below ready threshold).
