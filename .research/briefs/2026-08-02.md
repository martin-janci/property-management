# 2026-08-02

## Since last run
- Merged PRs: 1 (#2629 — gh-issue-2628, OAuth token-usage analytics wired end-to-end)
- Open PRs touched: 0 (14 open PRs, oldest updated 07-30 — status unchanged since 14:23Z rerun)
- New / updated issues: 1 (#2628 — closed by PR #2629 merge)
- Commits: 5 on `dev` since cursor `21a036ee` (1 code merge + 4 dispatcher chore commits)
- Routine lag: 6h since 14:23Z rerun — on schedule
- Phases that failed: none

## Shipped
- #2629 gh-issue-2628 (OAuth token-usage analytics wired end-to-end) — producer added to `OAuthService` (best-effort issued/refreshed/revoked events on the token lifecycle path), read route `GET /api/v1/platform-admin/oauth/token-usage` gated by `Capability::AuditRead` + super-admin, and daily scheduler prune of `oauth_token_events` (default 90d retention). Adds end-to-end integration tests + 3 unit tests for the daily-cadence gate. No new TODO/FIXME markers; test-file diff present — no `hotfix-no-test` signal.

## Watch
- Stalled review: 3 PRs cross the 7-day threshold (accounting-MVP trio #2555/#2558/#2559 at 6d idle — no state change since 14:23Z, human owner). No routine ping this run — Phase 5 comment-only cap holds them for the operator's attention rather than auto-pinging.
- Reverted: none this run
- Churn hotspots: none — 1 code PR (~560 lines net across backend/servers/api-server/src/) is well below hotspot threshold. `services/oauth.rs` + `services/scheduler/retention.rs` continue to churn on adjacent lifecycle features but not enough to shift ranking.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: none this rerun (routine's daily slot already consumed at 04:25Z on `api-core`)
- Experts: n/a (this rerun)
- Findings: 0 (this rerun) — however 9 dispatcher-emitted **Tier 1d** code-review findings from earlier today (`api-handlers`, `mobile-native-kmp`, `mobile-rn` ×3, `ppt-web-core`, `ppt-web-ui` ×3) were never ingested by the routine. This rerun absorbs them (see *Backlog deltas* below).
- Next segment for the routine's own Phase 1.5: `ppt-web-ui` (last reviewed 2026-07-21 — 12d) — deferred to tomorrow.
- Skipped: yes — one-segment-per-run cap; earlier same-day routine run consumed the slot.

## Screen-map status
- Total `docs/screens/` files: 153 (ppt=106, reality=47, mobile=0 — not yet seeded)
- Drift signals this run: 0 (no frontend route files touched by PR #2629; backend-only diff)
- Orphan screens this run: 0 (no route files touched this run)
- Last `screen-map.yml` CI run on `main`: not checked this run (no drift trigger)

## Backlog deltas
- **New (9 — Tier 1d refill absorbing dispatcher-emitted code-review findings):**
  - [2] `code-review-regional-compliance-vote-validation-fabricated-pass` — regional_compliance vote validators fabricate 75% turnout on NULL eligible_count (bug, medium)
  - [2] `code-review-mobile-native-kmp-create-listing-notimpl` — Reality Portal Android CreateListing route returns NotImplementedError (bug, high)
  - [2] `code-review-mobile-rn-useapi-no-refresh-retry` — mobile useApi bearer token cached at import; no refresh/retry on 401 (bug, medium)
  - [2] `code-review-mobile-rn-nfc-device-clock-expiry` — NFCAccessController trusts device clock for credential expiry + access windows (bug, medium)
  - [1] `code-review-mobile-rn-nfc-modules-no-tests` — NFC access + credential modules (~800 LOC) ship with zero test files (test-gap, medium)
  - [2] `code-review-ppt-web-core-perf-listener-leak` — usePerformanceMetrics leaks window visibilitychange/load listeners on unmount (bug, medium)
  - [2] `code-review-ppt-web-ui-portfolio-dash-mock` — PortfolioDashboardPage ships hardcoded mock KPIs + no-op alert/property handlers (bug, medium)
  - [2] `code-review-ppt-web-ui-moderation-prompt-cast` — Moderation/AML compliance pages cast window.prompt() output to typed enum unchecked (bug, medium)
  - [1] `code-review-ppt-web-ui-dialog-i18n-gap` — PPT-web dialog/prompt/alert copy hardcoded English — bypasses react-i18next (refactor, medium)
- **Bumped:** none
- **Decayed:** none (no open items older than 14 days — the 9 additions are today's)
- **Resolved (to done):** none (this rerun; the earlier 14:23Z rerun already marked the two api-core security items done via PRs #2626/#2627)
- **Dropped:** none

## Plans promoted
- none this run — none of the 9 new items clears the readiness bar. Scores are 1 or 2 (below the +3 baseline for non-security vectors); no security-vector fast-track applies (all 9 are `bug`, `test-gap`, or `refactor`). One or two more signals bumping any of these would put them within promotion range.

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist this run. The one high-confidence signal (`code-review-mobile-native-kmp-create-listing-notimpl`) is a `bug` requiring real KMP work, not an allowlist-shape action (doc-stub / orphan-remove / lockfile-bump / fixme-issue / stalled-review comment).

## Self-review findings
- open=28 (high=6, medium=19, low=3); new this run=0; acknowledged=19
- fp-null [recurrence=13] — assignments-archive.json too large (198KB) for MCP push_files inline; fix: use PR+API-merge path as primary push mechanism
- fp-null [recurrence=10] — dispatcher hard-assumes proxy-403 for git push; env actually permits it if commit email is @users.noreply.github.com; fix: runtime push-capability probe at Phase 1
- fp-null [recurrence=4] — action-list.json in-progress twin diverges from assignments.json live rows; fix: Phase-2 reconcile step to reset action-list on missing assignment
- fp-null [recurrence=3] — in-progress rows produce ZERO branches on origin across two implementer generations; fix: escalate branchless-after-reclaim to status=failed
- fp-null [recurrence=3] — Phase 2.6 claimable metric ignores archive-terminal ids; fix: extend gc1-reconcile to close open items terminal-in-archive for any status incl. failed
- ... and 1 more (see findings.json)

## Open questions
- **Buffer-low trigger (`claimable=0/72 — planned coverage exhausted`) partially answered.** This rerun refills the **backlog** side: 9 open vectors are now available for the dispatcher to consider once they cross score≥3 (which requires additional stacking signals or explicit human bumps — none is at that threshold yet). The **coverage** side (`coverage.json 47 done/2 partial`) is NOT touched by the routine — the prompt hard-forbids running the coverage `scan` mode in the cloud ("the authoritative full rebuild is the on-demand local `/ppt-project-management scan`"). Coverage refill therefore still needs a human running `/ppt-project-management scan` locally against latest `dev`; the routine cannot manufacture epics/stories.
- The 9 absorbed findings sit at score=1 or 2. They will decay (−1 every 14d without new evidence) unless the dispatcher's next Tier 1d review round re-emits the same IDs with new churn or the daily routine's own Phase 1.5 discovers the same segments and bumps them. Consider human triage: `code-review-mobile-native-kmp-create-listing-notimpl` is a **high**-confidence dead-end in the Reality Portal CreateListing flow (UC-51.4) and could be manually bumped to score=3 to unblock promotion.

## Quiet day?
- No — 1 code PR merged (#2629 closes gh-issue-2628), 9 new backlog vectors added, backlog + IDEAS_TRIAGE regenerated, state advanced (cursors + seen_signals + stats). This second same-day rerun exists to fulfil the `buffer-low` refill request on the backlog side.
