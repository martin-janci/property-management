# 2026-08-02

## Since last run
- Merged PRs: 0 (highest still #2625; no new merges to `dev` since prior brief 2026-08-01 18:19 UTC)
- Open PRs touched: 0 (all 14 open — 11 dependabot + 3 acc feature PRs — last updated ≤ 2026-07-30; nothing under 7-day idle threshold either)
- New / updated issues: 0 (nothing since `last_issue_seen=2612`; #2617/#2621 opened+closed on 2026-08-01 were captured by yesterday's brief)
- Commits: 6 on `dev` since `last_commit_sha=4fde35c5` — **all** dispatcher/routine `chore(research):` commits touching only `.research/`; **zero application-code commits** this window
- Routine lag: 0d 9h since last run (below 36h warning threshold)
- Phases that failed: none

## Shipped
- nothing (no merged PRs since last routine run)

## Watch
- Stalled review: none (0 open PRs > 7 days idle since last update)
- Reverted: none
- Churn hotspots: **none** — zero application-code commits since last cursor; hotspot_history not bumped, no `churn-hotspot`/`repeated-churn` signals emitted this run
- Dispatcher activity (context, not signals): 5 in-progress rows in sandbox grace — self-review 2026-08-02T02:12Z filed 1 new finding `implementer-sandbox-zero-pushed-branch-multigen` (5 rows produced 0 origin branches across 2 implementer generations, ~6h stall). GC3 buffer-bounds red across every run since 2026-08-01 02:20 (record-only). None of this is a routine signal — surfacing as *watch* only.

## PRs stuck in draft despite approval
- none (0 PRs matched `isDraft=true AND reviewDecision==APPROVED AND updatedAt < now-24h` across the 14 open PRs)

## PRs with verdict=changes, no fix-round progress in 24h
- none (dispatcher `assignments.json` snapshot on session branch: 5 in-progress rows, 0 in `review` status, so no `verdict=changes` candidates to nudge)

## Code review slice
- Segment reviewed: **skipped this run** (3rd consecutive routine skip — pragmatic budget)
- Experts: —
- Findings: 0
- Next segment: `api-core` (last reviewed 2026-07-20 — oldest in `state.review_cursor`; reality-server was refreshed by the dispatcher's Tier-1d generator at 2026-08-02 02:11, so cursor now reads `reality-server: 2026-08-02`)
- Skipped: yes — reason: (1) dispatcher's Tier-1d dev-review generator is emitting `code-review-*` signals directly into backlog on its own cadence, so segment coverage is not stalling; (2) the quietest observation window in weeks (0 PRs, 0 issues, 0 code churn) argues for not spending token budget on a review that would surface findings against an unchanged tree.

## Screen-map status
- Total `docs/screens/` files: 153 (ppt: 106, reality: 47; mobile: 0 — not yet seeded)
- Drift signals this run: 0 (no PR touched route trigger set since last cursor)
- Orphan screens this run: 0 (heuristic scan skipped per routine guidance — no drift baseline changed this window)
- Last `screen-map.yml` CI run on `main`: not queried this run (zero merged PRs = no CI to check)

## Backlog deltas
- **New:** none (no new signals emitted this run)
- **Bumped:** none
- **Decayed:** none (all 5 open items updated ≤ 2026-08-01 → 1-2 days old, well within the 14-day decay window)
- **Dropped:** none
- **Status change:** `code-review-reality-server-agency-members-unauth-idor` — `open` → `needs-human-judgement` (confidence downgraded high → medium) after in-place adversarial check surfaced `backend/servers/reality-server/tests/suites/agencies_authz_tests.rs:87`, which explicitly codifies `list_members` as a **public** endpoint alongside `list_agencies`/`get_agency`. The Reality Portal is a public directory; agency member roster visibility on a public agency profile may be intentional. The finding needs a design call — either drop it (public is correct) or flip both the handler AND the authz-test contract. Not promoted; excluded from Phase 3 by the `needs-human-judgement` gate.

## Plans promoted
- none this run — cap 2, ready candidates 0. The two open score-≥ security items on the reality-server segment (`code-review-reality-server-sso-session-invalidate-swallowed` score 3, `code-review-reality-server-db-error-leak-to-client` score 2 + security fast-track) both **fail stem-uniqueness** — both slugs are already live in `.research/management/assignments.json` as dispatcher `in-progress` rows (assignments snapshot on session branch). Promotion would create duplicate work; the correct path is for the dispatcher's manual/implementer flow to close them out on the auto-impl branches. `bug-scheduler-notifications-fire-once` remains the one active `ready` plan (from 2026-08-01, adversarial pass: passed — carried forward untouched).

## Auto-fix
- `no candidate` — no signal met the certainty bar this run (there were 0 new signals; the one backlog delta was a status downgrade, not an eligible allowlist type). Kill switch `ROUTINE_AUTOFIX_DISABLED` not observed set.

## Self-review findings
- open=26 (high=6, medium=17, low=3); new this run=0 (dispatcher's 1 new finding at 02:12 UTC predates this run and is recorded under dispatcher self-review, not routine self-review); acknowledged=19
- fp-mcp-push-cannot-inline-large-archive [recurrence=13] — 198KB archive files exceed `mcp__github__push_files` 64KiB inline limit; fix: adopt new-branch push + PR-API-merge as the primary push mechanism (documented, not yet landed in dispatcher-prompt).
- fp-mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=9] — dispatcher-prompt hard-codes proxy-403 assumption; fix: runtime probe at Phase 1 with commit-email guard, drop the 64KiB guard when git push works.
- fp-buffer-metric-blind-to-archived-open-items [recurrence=3] — Phase 2.6 buffer reports claimable=54 when reality is 0 (all terminal in archive); fix: extend gc1-reconcile.sh to auto-close archived open action-list rows, subtract archive ids from the GC3 claimable formula.
- fp-tier2-planner-kick-auth-401 [recurrence=2] — Tier-2 planner kick 401s; fix: operator rotate DISPATCHER_TOKEN + dispatcher-side branch on HTTP status to escalate auth failures loudly.
- fp-stale-in-progress-action-list-decoupled-from-assignments [recurrence=2] — 31 action-list `in-progress` rows vs 3 live assignments; fix: add Phase-2 reconcile bijection + self-test T32.

## Open questions
- **`code-review-reality-server-agency-members-unauth-idor` — public directory or private roster?** The dispatcher's Tier-1d reviewer flagged the endpoint as an IDOR; the existing authz test suite (`agencies_authz_tests.rs:87`) codifies it as a public directory endpoint. Reality Portal precedent (`list_agencies`/`get_agency_by_slug` are public) supports the roster being public too — but a realtor's public agency profile may reasonably show a *sanitised* member list (names/roles only) rather than the full `RealityAgencyMember` record (email/joined_at). Decision: (a) keep public, drop the finding + close the review signal; (b) treat as private, flip the handler AND the authz-test contract in the same PR; (c) publish a sanitised subset. Filed as `needs-human-judgement`; will not promote until adjudicated.
- **3-in-a-row Phase 1.5 skip.** The dispatcher's Tier-1d generator is covering segment reviews on its own cadence, so the routine's separate Phase 1.5 slot is arguably redundant. Consider either (a) folding Phase 1.5 into "run only if Tier-1d has not touched a segment in >7 days" or (b) explicitly deleting Phase 1.5 from the routine prompt and pointing at the dispatcher as the single source. Would eliminate the recurring "skipped, budget" noise in briefs.

## Quiet day?
- **Yes** — 0 merged PRs, 0 new issues, 0 code churn, 0 new signals, 0 plans promoted. The only backlog delta was an in-place adversarial-pass demotion (`code-review-reality-server-agency-members-unauth-idor` → needs-human-judgement). `state.stats.quiet_days` incremented.
