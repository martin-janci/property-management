# 2026-07-21

## Since last run
- Merged PRs: 5 (range #2428–#2432 — all Layout & Content Manager slices 4–8)
- Open PRs touched: 1 (#2433 iOS listing detail through shared resolved layout — opened 2026-07-20T20:27Z, still open <8h — not stalled)
- New / updated issues: 0 new (#2422 still open — last-seen 2026-07-18)
- Commits: 5 on `dev` (one commit per merged PR)
- Routine lag: 0d 23h since last run — on schedule
- Phases that failed: none

## Shipped

Fifth day of the **Layout & Content Manager** cascade — slices 4–8 all landed this window, closing the desktop web + mobile legs of spec §6/§7/§4:

- **#2428** (`admin-web/layout-editor`) — Layout editor MVP for superadmins: SectionTreeEditor + RailsEditor + LayoutManifestsPage + publish/rollback/kill-switch + version history at `/platform/layout` (spec §6 minus §6.1 preview bridge). `layout_editor_*` capability registration explicitly tracked as follow-up (backend still gates on super-admin JWT).
- **#2429** (`ppt-web/tenant-editor`) — Org-admin `/dashboard/customize` with `TenantSectionEditor` where controls exist ONLY where rails grant them (eye toggle iff hideable, ↑/↓ iff reorderable, mode select iff mode-editable ∩ manifest modes, per-prop inputs iff whitelisted). Ships verbatim 422 rails-violation error carry-through; role-gated via `ProtectedRoute requiredRoles=['org_admin','super_admin']`.
- **#2430** (`layout/preview-bridge`) — Iframe live-preview: origin-validated postMessage protocol (`@ppt/shared/layout-preview`, 42 protocol tests, exact `targetOrigin` never `'*'`), stateless `POST /platform-admin/layout/preview-resolve` on api-server, ppt-web + reality-web child sides. **Ships a reality-web framing carve-out** — `LAYOUT_PREVIEW_FRAME_ANCESTORS` env-gated CSP header for `?layoutPreview=1` requests (baseline stays `frame-ancestors 'none'`; unset env keeps DENY). **Follow-up in body:** ppt-web has no frame-protection at its serving layer — this feature makes that gap load-bearing (see *Watch*).
- **#2431** (`layout/publish-webhook`) — Signed ISR revalidation from api-server to reality-web via `POST /api/layout-revalidate`: `X-Webhook-Signature: sha256=base64(HMAC-SHA256(secret, body))`, constant-time compare, env-gated (unset → debug no-op), fire-and-forget with 5s reqwest timeout. Layout fetch now tags with the global `layout:listing-detail` tag alongside host-scoped — a single webhook invalidates every host.
- **#2432** (`layout/mobile-registries`) — RN dashboard `LayoutSections` + KMP Android listing-detail `ListingSectionRegistry` (spec §4.6 activation semantics: state seeds from compiled default, activates ONCE from mount-time cache read, background fetch writes cache only — fresh layouts appear next launch). Canonical `mobile-manifest.json` in backend `RegistryManifest` shape, drift-guard test. **iOS explicitly out of this slice** (no macOS builder in fleet) — that slice landed as open PR #2433 on 2026-07-20T20:27Z.

Not shipped this window: iOS SwiftUI dispatch (#2433 opened, still open), KMP layout persistence, `layout_editor_*` capability, per-tenant preview URLs, ppt-web frame-protection (see below).

## Watch
- Stalled reviews: none — #2433 is <8h old, expected pattern.
- Reverts: none in window.
- **Security follow-up flagged in-body by PR #2430 (referenced again by #2431 + #2432):** ppt-web has **no frame-protection at its serving layer** (pre-existing gap). The preview-bridge feature makes that gap load-bearing — an attacker who frames ppt-web with their own origin can layout-spoof inside a frame they could already create. Judged low-severity/self-contained by the PR-time security review (no data exfil), but explicitly called out as needing `X-Frame-Options: SAMEORIGIN` / `frame-ancestors` allowlist at ppt-web's edge config. NOT a routine-emitted signal type — surfaced here as an operator-visible watch item.
- Churn hotspots (top 3, all inside the layout slice cascade — expected iteration, NOT organic instability):
  - `docs/repo-map.md` — 5 touches (per-PR route-map refresh, doc-signal only; runs_seen: 1 → 2, crossing repeated-churn threshold — the next run that scores it will emit +1 repeated-churn)
  - `docs/screens/ppt/dashboard.md` — 3 touches (Layout & Content Manager pilot + preview + mobile screen-map log)
  - `frontend/apps/admin-web/src/features/layout-editor/LayoutEditorPage.tsx` — 2 touches, 900 lines (created #2428, extended #2430 for iframe preview wiring)

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: **ppt-web-ui** (reason: churn-aligned + oldest-unreviewed — layout PRs #2428–#2432 all landed in ppt-web features/ AND the segment's last review was 2026-07-13, oldest of the 8)
- Experts: frontend (primary) + completeness (secondary)
- Findings: 3 (all vector=bug, confidence=medium — static analysis only)
  - `code-review-ppt-web-ui-actionqueue-mock-shipped` (+2, bug) — **most operationally significant.** `frontend/apps/ppt-web/src/features/dashboard/hooks/useActionQueue.ts:240-297` — `queryFn` returns `generateMockData()` after `setTimeout(300)` with `// TODO: Replace with actual API call when backend is ready`; executeAction mutation resolves `{ success: true }` without hitting any endpoint. Backs `action-queue.v1` section (registered in dashboard registry `LayoutRenderer → registry.tsx:22-26`), used directly by ResidentDashboardPage AND ManagerDashboardPage — **production users see fabricated action items and every approve/reject/dismiss click is a silent no-op.** Needs a real backend endpoint or the section should be gated behind a feature flag / kill-switched via the layout kill-switch until the API lands.
  - `code-review-ppt-web-ui-propinput-json-coerce` (+2, bug) — `TenantSectionEditor.tsx:59-84` PropInput displays string prop values raw (no quotes) then commit() calls `JSON.parse(val)` first. A string value like `"true"`, `"123"`, `"null"`, `"[]"` is displayed as-is, and any onBlur (even without a user edit) parses and stores the coerced boolean/number/null/array — silently corrupting the override payload sent to `useSaveTenantLayoutOverride`.
  - `code-review-ppt-web-ui-savedirty-stale-closure` (+2, bug) — `DashboardCustomizePage.tsx:58-69` "changed since sent" check compares two snapshots of the same closure-captured `localOverride` (tautologically true). Concurrent edits during in-flight `mutateAsync` (TenantSectionEditor is not gated by `mutation.isPending`) are not detected; `isDirty` is cleared on success and user may navigate away thinking newest keystrokes were saved.
- Next segment: `mobile-rn` (oldest unreviewed after this run — tied 2026-07-14 with `api-handlers`; alphabetical tiebreak → mobile-rn wins next)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` 106 + `reality/` 47; `mobile/` still not seeded → 0)
- Drift signals this run: 1 (`screen-map-drift-pr-2431-reality`) — heuristic false-positive: PR #2431 touched `frontend/apps/reality-web/src/app/api/layout-revalidate/route.ts` which matches the `src/app/**` trigger set, but this is an internal API webhook, NOT a user-facing screen. No screen doc under `docs/screens/reality/` would be appropriate. Backlog row created at score 2, confidence=medium, with the caveat in evidence; operator can dismiss.
- Orphan screens this run: 0 (no route→doc filename mismatch this window)
- Last `screen-map.yml` CI run on `main`: 2026-06-04 22:19 UTC (`id=26982999099`, conclusion=success). No push-to-main in window (all merges to `dev`).

## Backlog deltas
- **New:** [+2] `code-review-ppt-web-ui-actionqueue-mock-shipped` (bug) — Dashboard `useActionQueue` returns mock data; approve/reject/dismiss are no-ops.
- **New:** [+2] `code-review-ppt-web-ui-propinput-json-coerce` (bug) — TenantSectionEditor silently JSON.parse-coerces string props on blur.
- **New:** [+2] `code-review-ppt-web-ui-savedirty-stale-closure` (bug) — DashboardCustomizePage tautological dirty-check drops concurrent edits.
- **New:** [+2] `screen-map-drift-pr-2431-reality` (test-gap, heuristic caveat) — API webhook /api/ path triggered false-positive drift.
- **New:** [+1] `refactor-churn-hotspot-ppt-dashboard-md-2026-07-21` (refactor) — screen-map integration churn.
- **New:** [+1] `refactor-churn-hotspot-layouteditorpage-tsx-2026-07-21` (refactor) — feature-series iteration.
- **Bumped:** none — the one signal ID re-fired (`churn-hotspot-docs/repo-map.md`) is on the cumulative allowlist; evidence appended, score NOT re-applied per Phase 1 dedup rule. `refactor-churn-hotspot-repo-map-md-2026-07-20` `updated_at` moved 2026-07-20 → 2026-07-21.
- **Decayed:** none — all 7 open items pre-run are ≤1 day old (all `updated_at=2026-07-20`); no decay window reached.
- **Dropped:** none this run.

## Plans promoted
- none — no item hit the readiness bar (score ≥ 3 general, or security fast-track score ≥ 2 with `confidence=high`). The three new bug findings are `confidence=medium` (static analysis only, no runtime confirmation), so a single signal +2 keeps them at score 2 — below the +3 threshold for `bug` vector. They stay `open`; next run will decide whether to promote if corroborating evidence arrives (e.g., an issue filed against the useActionQueue mock, or a user-report).

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist type (screen-map-drift / screen-map-orphan / fixme-in-merged-code / dep-update-noise lockfile-drift / stalled-review) this run. The one screen-map-drift signal was medium-confidence heuristic and the action shape (write a stub screen doc for an API webhook path) would have been wrong.

## Self-review findings
- open=21 (high=4, medium=13, low=4); new this run=0; acknowledged=19.
- fp-null [recurrence=10] — dispatcher Phase 6 archive-move produces 198KB+ assignments-archive.json; mcp push_files 64KB inline limit + git-push-proxy-403 combine to drop the archive push; fix: adopt PR+API-merge path as primary push mechanism.
- fp-null [recurrence=6] — dispatcher-prompt hard-codes proxy-403 assumption; in this env `git push origin dev` succeeded once GH007 email was fixed; fix: runtime-probe push capability at Phase 1 and bypass 64KiB size-guard when git push works.
- fp-null [recurrence=3] — Phase 2.6 buffer reported claimable=54/open=54 but real claimable=0; all open action-list items already terminal in archive; fix: extend gc1-reconcile to auto-close ANY id shape (including status=failed) and adjust GC3 formula to subtract archive ids.
- fp-null [recurrence=2] — Tier-2 planner kick returned http=401; DISPATCHER_TOKEN/DISPATCHER_URL rejected; fix: operator rotates the secret + dispatcher branches on 401/403 as OPERATOR-CONFIG escalation.

## Open questions
- Is `useActionQueue`'s mock data still shipping *because* the backend endpoint hasn't been designed, or because a real endpoint exists and the wiring was forgotten? A quick grep of `backend/servers/api-server/src/routes/` for anything action-queue-shaped would answer it — but the routine's cloud budget deferred it. Next run's api-handlers segment review (or a manual pass) should confirm.
- The `layout_editor_*` capability referenced in #2428/#2429/#2430/#2431/#2432 bodies is a repeated known follow-up. It's not tracked as a coverage story or backlog vector today — should the routine surface a triage row for it, or is the deferred capability wiring intentional until the editor stabilises?

## Quiet day?
- No — 5 PRs merged, 6 new backlog vectors, 3 code-review findings (one of them a real production bug: dashboard ActionQueue mock shipped to production users), and 1 open PR to watch (#2433 iOS layout dispatch).
