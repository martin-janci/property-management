# 2026-09-07

_First routine fire in 5d 5h — cloud routine loop was quiet while the dispatcher (separate loop, its own branch) kept ticking. Trigger payload: `buffer-low: claimable=8/72 — all backlog cloud-gated (api-server+mobile-native egress-blocked); refill planner`._

## Since last run
- Merged PRs: 12 (range #2922–#2943, plus late-lands of #2558/#2583/#2585/#2586/#2673/#2935 all older than the cursor and already accounted for in prior stats)
- Open PRs touched: 2 net-new (#2933 gradle-minor-patch, #2939 cargo-minor-patch); rest are older dependabot rows updated by CI touch
- New / updated issues: 3 new open + 6 closed follow-ups. **New opens are all security IDOR:** #2946, #2945, #2944 — cross-tenant IDOR in portfolio_analytics / portfolio_properties / violations handlers. All user-filed with `bug`+`security` labels.
- Commits: ~100 on `dev` — 12 code PR merges + ~90 dispatcher/tier1d chore commits. HEAD `c2bd5baff699` (2026-09-06 22:34Z).
- Routine lag: 5d 5h since last run (last_run_iso 2026-09-01T18:38:58Z). `lag_warning` + `stale_routine_alert` both fired — cloud routine cron likely paused.
- Phases that failed: **phase-1.5-skipped-deliberately, phase-1.6-skipped-deliberately** (see rationale in *Code review slice* + *Open questions*). `review_cursor`, `pm_cursor`, `coverage_cursor` NOT advanced this run — consistent with G11.

## Shipped
- #2943 code-review-ppt-web-core-logout-purge-allowlist — logout cache-purge allowlist coverage enforcement
- #2942 code-review-ppt-web-core-api-401-no-request-replay — single-flight refresh + request replay on 401
- #2941 code-review-ppt-web-core-authctx-no-boot-expiry-check — validate JWT `exp` at cold boot
- #2940 code-review-ppt-web-ui-confirm-dialog-i18n — externalize ConfirmationDialog loading label via i18n
- #2938 gh-issue-2937 — invoice PDF renderer + handler test coverage (post-merge follow-up on #2558)
- #2932 gh-issue-2930 — e2e 403 test for agent-review self-review guard
- #2931 gh-issue-2929 — make report_summary snapshot-consistency test fail on pre-fix code
- #2928 gh-issue-2924 — snapshot-consistent report_summary counts + entries
- #2927 gh-issue-2923 — cover update-error i18n path in EmergencyContactDirectoryPage tests
- #2926 code-review-api-core-fcm-legacy-dead-endpoint — remove decommissioned FCM legacy send path
- #2925 code-review-api-handlers-compliance-raw-db-leak-regression — route compliance audit-log count error through `db_error`
- #2922 code-review-reality-server-saved-search-error-string-status — derive HTTP status from a typed error enum

## Watch
- **Three OPEN cross-tenant IDOR issues opened today (#2944 / #2945 / #2946)** — portfolio & violations handlers computing but discarding org_id. All labelled `bug`+`security`. These are dispatcher-managed via GH issue flow; not currently signals here, but worth eyes.
- Stalled review: #2559 (UC-ACC-05.8 PAY-by-square QR, ~40d idle) — signal id already in `seen_signals`, dedup'd. #2555 same. #2558 resolved this run (merged 2026-09-04).
- Reverted: none.
- Churn hotspots: none of code interest — the 5-day window is dominated by ~90 dispatcher chore commits inside `.research/` (excluded per rule). Real code touches from the 12 PRs were small (avg ~2 files each, tightly scoped fixes).

## PRs stuck in draft despite approval
- #2902 screen-map-drift-pr-2894-reality — unchanged from 2026-09-01 brief; branch contaminated; still draft.
- #2744 fix(dispatcher): un-wedge oversize-archive push — unchanged (self-approval blocker).
- none new this window.

## PRs with verdict=changes, no fix-round progress in 24h
- #2902 — unchanged from 2026-09-01 brief.
- otherwise none.

## Code review slice
- Segment reviewed: **none this run — Phase 1.5 skipped**.
- Rationale: the dispatcher's own Tier-1d generator (visible in commit log) has run rotating expert reviews across 7 segments in this 5-day window — `ppt-web-core` (×4), `ppt-web-ui`, `reality-web` (×2), `mobile-rn`, `mobile-native-kmp`, `reality-server` (×3), `api-handlers` (×3), `api-core`. Running the routine's own Phase 1.5 on top would generate duplicate signals against saturated segments.
- Experts: n/a.
- Findings: 0 (routine-native). Dispatcher-native findings continue to land as tier1d signal files (`signals/*-tier1d.json`).
- Next segment: `mobile-native-kmp` (review_cursor 2026-08-25 remains oldest) — cloud-unlandable via #2652 → #2655 gating, so its findings can only be picked up by a local implementer.
- Skipped: yes — see rationale above. `state.review_cursor` NOT advanced.

## Screen-map status
- Total `docs/screens/` files: unchanged (mobile/ not yet seeded → 0).
- Drift signals this run: 0. None of the 12 merged PRs touched `App.tsx` / `routes/**` / `reality-web/src/app/**`.
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not re-fetched this run (cheap check deferred; can inspect via `gh run list --workflow=screen-map.yml --branch=main --limit=1` if needed).

## Backlog deltas
- **New:** none (only lag_warning signal fired this run, score=0).
- **Bumped:** none.
- **Decayed:** `code-review-mobile-native-kmp-portfolio-analytics-caps-100` — score 1 → 0 → **dropped**. Last update 2026-08-19 (19d ago, past 14d threshold). Evidence line appended.
- **Dropped:** as above.
- **Resolved (from merged PRs):** `stalled-review-pr-2558` — PR #2558 merged 2026-09-04 (was `needs-human-judgement`, now `done`). Other resolutions already recorded in the 2026-09-01 brief; the 12 merged PRs this window either matched already-done rows (5 IDs found) or are dispatcher-native rows without a routine-side backlog entry.

## Plans promoted
- **0 new plans this run.** Same rationale as 2026-09-01: only `score >= 3 && confidence == high && status == open` candidate remains `code-review-mobile-native-kmp-create-listing-not-wired` (score 4, high, mobile-native/KMP → cloud-unlandable per #2652 → #2655 gating). Its companion `code-review-mobile-native-kmp-inquiries-response-contract` plan is still `ready` and unclaimed on the same parked pile after 13 days — evidence that adding a second local-only plan does not refill the cloud claimable buffer.

## Auto-fix
- `no candidate` — the one signal this run (`lag-warning-2026-09-07`, score_delta 0) is not in the allowlist (allowlist accepts only screen-map-drift, screen-map-orphan, fixme-in-merged-code, lockfile-bump, stalled-review). Score-delta 0 also fails the certainty bar (score >= 3).

## Self-review findings
- none (`.research/self-improvement/findings.json` absent).

## Open questions
- **Cloud routine cron paused?** 5d 5h gap on the routine while the dispatcher (separate loop, separate branch) kept ticking suggests the daily research routine's cron is broken or paused. The dispatcher state lives on the `planning` branch's `assignments.json`; check there separately. Operator action needed: confirm cron health, restart if needed.
- **Cloud-buffer chronic floor (repeat from 2026-09-01):** buffer stays at 8/72 not because signals are missing — the dispatcher's Tier-1d generator is producing them daily — but because the mobile-native/KMP pile (structurally cloud-unlandable per #2652) accumulates without draining. Options: (a) allow `dl.google.com:443` egress for cloud runner, (b) connect ppt-bridge MCP in dispatcher sessions, (c) file follow-up "structural-block" issue to formalise the gap. The 6-day dispatcher `GC3-buffer-bounds=FAIL` streak from 2026-09-01 has now grown; still deferred.
- **Should Phase 1.5 / 1.6 be skippable when the dispatcher is running Tier-1d reviews?** This run treats them as skippable (rationale in *Code review slice*). Prompt currently says "Skip Phase 1.5 when: state.json has paused: true, Phase 1 completely failed" — the dispatcher-overlap case isn't in the rule. Consider adding.

## Quiet day?
- No. 12 merges since last run, 3 new security issues, 1 decay, 1 resolution, 1 lag_warning. Full brief written and Telegram digest queued (best-effort).
