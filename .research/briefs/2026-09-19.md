# 2026-09-19

_Recovery run after 18-day cloud-routine silence — buffer-low payload from dispatcher (`honest-landable-claimable=0/72`); the dispatcher has kept ticking on the planning branch, but this routine (research/backlog on `dev`) stopped after the 2026-09-01 18:38Z brief. This run re-ingests the 45 tier-1d code-review-finding signals the dispatcher has been queuing daily, decays the stale mobile-native pile that couldn't move, and promotes 2 landable frontend/ppt-web-core plans that fit the payload's directive._

## Since last run
- Merged PRs: **17** (range #2922–#2956). Most (13 of 17) resolve code-review items already in backlog. Merge cadence stopped after #2956 (2026-09-08); nothing lands after that because everything left in the dispatcher's active pile is either mobile-native (AGP-egress-gated by #2652/#2951) or api-server crate (utoipa-swagger-ui egress-blocked by #2966/#2949).
- Open PRs touched: 12 dependabot noise (#2957, #2960-#2965, #2584, #2588-#2590) + 1 draft screen-map-drift (#2902, unchanged from prior brief). Dependabot noise is filtered per routine `dep-update-noise` rule (score 0).
- New / updated issues: 3 open — **#2946** (bug/security: cross-tenant IDOR portfolio_analytics; auto-opened 2026-09-06 by the review pipeline), **#2951** (blocked: mobile jest cloud runner egress-blocked), **#2966** (blocked: api-server verify gate cloud-build blocked, filed 2026-09-18). Plus 14 closed follow-ups from the merge wave.
- Commits: 51 on `dev` since 2026-09-01T18:38Z — the majority are `chore(research): dispatcher …` metadata commits on the planning branch replayed to dev; ~17 actual code commits.
- **Routine lag: 18d 21h since last routine commit — stale routine alert active.** state.last_run_iso=2026-09-01T18:38:58Z. Cloud routine cron appears to have paused between 2026-09-01 and today. Dispatcher (separate loop, .research/management/assignments.json on the planning branch) has continued to run, hence the backlog of unconsumed tier-1d signals under `.research/signals/2026-09-11..2026-09-19-*-tier1d*.json`.
- Phases that failed: none (Phase 1 via mcp__github__ tools after `gh` GraphQL 403; Phase 1.5 and Phase 1.6 both deferred this run — see below).

## Shipped
- #2922 code-review-reality-server-saved-search-error-string-status — derive saved-search HTTP status from error variant (was string-typed).
- #2925 code-review-api-handlers-compliance-raw-db-leak-regression — route compliance audit-log count error through `db_error` (regression left by #2915).
- #2926 code-review-api-core-fcm-legacy-dead-endpoint — remove decommissioned FCM legacy send path.
- #2927 gh-issue-2923 — cover update-error i18n path in EmergencyContactDirectoryPage tests.
- #2928 gh-issue-2924 — snapshot-consistent `report_summary` counts + entries.
- #2931 gh-issue-2929 — make `report_summary` snapshot-consistency test fail on pre-fix code (was passing on both).
- #2932 gh-issue-2930 — add e2e 403 test for agent-review self-review guard.
- #2935 dependabot npm-minor-patch group (40 updates) — noise.
- #2938 gh-issue-2937 — add invoice PDF renderer + handler test coverage.
- #2940 code-review-ppt-web-ui-confirm-dialog-i18n — externalize ConfirmationDialog loading label via i18n.
- #2941 code-review-ppt-web-core-authctx-no-boot-expiry-check — validate JWT exp at cold boot.
- #2942 code-review-ppt-web-core-api-401-no-request-replay — single-flight refresh + replay on 401.
- #2943 code-review-ppt-web-core-logout-purge-allowlist-fragile — enforce logout purge allowlist coverage (adjacent to today's promoted `logout-purge-disputes-leases-iot` plan — this landed the coverage test; today's plan adds the six roots that test would now catch).
- #2950 gh-issue-2947 — purge NFC access log, QR scan history & offline feedback on session change (mobile-rn).
- #2952 gh-issue-2948 — auto-discover feature-local `*Keys` factories in logout-purge coverage test.
- #2955 gh-issue-2953 — purge NFC credential material from SecureStore on session change (mobile-rn).
- #2956 gh-issue-2954 — harden queryKeys.test.ts auto-discovery (suite-crash risk + glob-depth gap).

## Watch
- **Stalled infrastructure blockers.** #2966 (api-server cloud-build gate blocked, since 2026-09-18) and #2951 (mobile jest cloud runner blocked, since 2026-09-07) are the two structural blockers gating the dispatcher's landable-buffer. #2652 (AGP egress) is the older mobile-native equivalent. Until one of the three lands, the dispatcher's active pile stays at 12/12 blocked and each routine cycle refills by adding LANDABLE frontend/reality-web work, not by unblocking the blocked pile.
- **Stalled review carryover.** #2559 / #2558 / #2555 UC-ACC-05.x — `needs-human-judgement`, 30+ days (from 2026-09-01 brief; unchanged this run — no new activity).
- Reverted: none this window.
- Churn hotspots (top-3 non-.research, this window):
  - `frontend/apps/ppt-web/messages/en.json` — 4469 lines added/removed across the merge wave (i18n keys landing with each PR); expected for the touched features, not a code-quality signal.
  - `frontend/packages/sitemap/src/json/sitemap.json` — 4207 lines; also expected as new routes landed.
  - `backend/servers/api-server/tests/suites/oauth_integration_tests.rs` — 3427 lines; test surface expansion, not instability.
- Repeated churn: none of the top-3 above are `runs_seen >= 2` yet in `hotspot_history` — first time seeing them since 2026-09-01 (last-run gap has flattened the "runs_seen" signal; will fire on the next normal-cadence run if they re-appear).

## PRs stuck in draft despite approval
- #2902 screen-map-drift-pr-2894-reality — draft since 2026-09-18 (branch contaminated per prior brief; unchanged, not blocked on approval).
- none additional (open dependabot PRs are not approved).

## PRs with verdict=changes, no fix-round progress in 24h
- Not fetched this run — planning-branch `assignments.json` read deferred (dispatcher had already run 5× today per commit log; state is fresh). Nothing new for this brief to surface here.
- none

## Code review slice
- Segment reviewed: **none this run — Phase 1.5 skipped.** Reason: the dispatcher has been running its own Tier-1d rotating expert review 8× since 2026-09-11 (see `.research/signals/2026-09-1[1-9]-*-tier1d*.json`), producing 45 fresh code-review-finding signals across ppt-web-core, ppt-web-ui, reality-web, mobile-rn, reality-server, and api-handlers. This routine's job for the recovery run is to *consume* that queue, not spawn another.
- Findings: **45 ingested** — see Backlog deltas below. Distribution: 22 ppt-web-core/ppt-web-ui, 11 reality-web, 4 mobile-rn, 4 reality-server, 2 api-handlers, plus scattered.
- Next segment (from `state.review_cursor`, oldest first): `ppt-web-core` (2026-09-06) and `reality-web` (2026-08-29) — both freshly covered by the dispatcher's tier-1d output above, so a fresh Phase 1.5 pass here would duplicate. Next non-tier1d-covered oldest is `reality-server` (2026-08-31) and `api-handlers` (2026-08-31) — earmark for the next normal-cadence run.
- Skipped: yes — dispatcher already produced this run's expert-review payload; ingest, don't duplicate.

## Screen-map status
- Total `docs/screens/` files: unchanged (ppt/ + reality/ populated; mobile/ still not seeded).
- Drift signals this run: 0 (none of the 17 merged PRs touch `App.tsx` / `routes/**` / `reality-web/src/app/**` — all merges were features-inside or backend).
- Orphan screens this run: 0 (not fresh-scanned this run; no new route files landed to invalidate any).
- Last `screen-map.yml` CI run on `main`: not fetched this run.

## Backlog deltas
- **New (45 rows, all landable except mobile-rn subset which is blocked by #2951 jest cloud):**
  - **[3][high][bug]** `code-review-ppt-web-core-community-route-fully-stubbed` — promoted (see Plans).
  - **[3][high][security]** `code-review-ppt-web-core-logout-purge-disputes-leases-iot` — promoted (see Plans).
  - **[3][medium][bug]** `code-review-ppt-web-core-ocr-fetch-no-auth`.
  - **[2][high][bug]** `code-review-mobile-rn-dashboard-layout-fetch-never-applied`, `code-review-mobile-rn-widget-bridge-auth-token-never-set` — blocked by mobile-rn cloud jest gate (#2951); await.
  - **[2][medium][security]** 4 items: `route-groups-no-auth-guard` (ppt-web-core), `org-member-role-mutation-guards` (api-handlers), `errorhandler-raw-backend-message-passthrough` (ppt-web-core), `unvalidated-href-url-scheme` (reality-web). All landable; will stack toward promotion on next run if evidence holds.
  - **[2][medium][bug]** 16 items across ppt-web-core (7 — dashboard/manager route guards, financial-query silent-error, needing-maint key, api default export, reports route stub, etc.), ppt-web-ui (4 — facilities/documents/reading-form), reality-web (3 — price map/agency chart/listing-header en-US format), mobile-rn (2 — image compress width-only, wallet passtype namespace), reality-server (1 — portal-listing no price validation).
  - **[1][medium]** 22 remaining rows — reality-web i18n gaps, reality-server hardcoded labels, ppt-web-core toasts i18n, faults URL param cast, iot ws no backoff, etc.
- **Bumped:** none this run (nothing existed to be bumped — everything new).
- **Decayed (14d+ open with no new evidence):**
  - `code-review-mobile-native-kmp-create-listing-not-wired` — score 4→3 (last updated 2026-09-01, 18d old); still open, still cloud-unlandable (AGP-egress).
- **Dropped (decayed to 0):**
  - `code-review-mobile-native-kmp-portfolio-analytics-caps-100` (was 1, unlandable, 31d stale).
  - `code-review-mobile-native-kmp-cancellation-swallowed` (was 1, unlandable, 25d stale).
  - `code-review-mobile-native-kmp-httpclient-no-timeout` (was 1, unlandable, 20d stale).
  - All three are real findings; dropping releases backlog pressure while the mobile-native cloud unblock (#2652) is pending. They'll re-emerge next time a mobile-native tier-1d review runs and stacks fresh evidence.
- **Resolved:** none newly this run — the 13 code-review items resolved by PRs #2922-#2956 were already marked `done` in the backlog before this run (dispatcher had been keeping the resolution ledger current; the routine's role is signal → backlog, not the other way).

## Plans promoted
- `plans/code-review-ppt-web-core-logout-purge-disputes-leases-iot.md` — add 6 missing roots to `AUTHED_QUERY_KEY_ROOTS` (disputes, leases, iot, my-units, templates, violations); extend `queryKeys.test.ts` auto-discovery to `@ppt/api-client` `all: [<literal>]` factories; defensive `queryClient.clear()` fallback behind flag. Vector security fast-track (confidence=high, score=3, ≥2). Landable (frontend/ppt-web only). · adversarial pass: passed
- `plans/code-review-ppt-web-core-community-route-fully-stubbed.md` — wire `FeedPageRoute` (and sibling routes in `community.tsx`) to `@ppt/api-client` community hooks; every action handler currently a no-op arrow. Score 3, confidence high. Landable (frontend/ppt-web only). · adversarial pass: passed

## Auto-fix
- `no candidate` — none of the 45 ingested signals fit the Phase 5 allowlist (`screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift sub-shape, `stalled-review`). All are `code-review-finding`, which is not eligible for auto-PR by design.

## Self-review findings
- none (`.research/self-improvement/findings.json` absent).

## Open questions
- **Cloud routine cron.** The 18-day gap suggests the cloud routine's scheduled trigger stopped firing between 2026-09-01 and today. The dispatcher (separate cron on the planning branch) kept running. Worth checking the schedule config for this routine (`.research/routine-prompt.md` is triggered by … which cron?). Not blocking this run's output — this brief is now landed and the routine advances state — but the operator should verify the schedule is re-enabled.
- **#2946 cross-tenant IDOR portfolio_analytics.** Filed 2026-09-06, labeled `bug` + `security`, still OPEN. Not yet promoted here because the issue body wasn't fetched this run (out of Phase 1 budget). Earmark for next run: read #2946, verify code cite, add as a `security-*` backlog row with vector=security. Suspected high-priority landable if the fix path stays in `backend/servers/api-server/src/routes/portfolio_*` (no swagger-ui egress needed for the crate's own tests, but see #2966 for the cloud-build blocker on the api-server crate as a whole).
- **When does #2652 / #2951 / #2966 land?** Structural cloud-runner blockers gating the dispatcher's landable buffer. Every routine run currently refills the buffer with fresh frontend/reality-web work but the underlying constraint stays. A single "unblock cloud runner" PR (per issue) would resolve much larger blocks of the mobile-native + api-server backlog than the daily tier-1d churn does.

## Quiet day?
- **No.** 17 merged PRs since last run (16 substantive + 1 dep noise); 45 backlog rows added; 2 plans promoted; 3 backlog rows decayed to dropped; 1 decayed one point. Loud recovery run.

## Goal violations
- **G2 timing note:** this run's state.json advances `last_pr_seen` 2921→2965, `last_issue_seen` 2847→2966, `last_commit_sha` eea70673→646fd91b, `last_run_iso` 2026-09-01→2026-09-19, `seen_signals` +48, `stats.runs` +1, `stats.vectors_created` +45. All cursor motion is real.
- Phase 1.6 (project management skill / role rotation) **skipped this run.** Reason: recovery run budget prioritized ingesting the 45 signal backlog and promoting 2 landable plans. `pm_cursor.next_index` (currently 5 = pm-security) and `coverage_cursor.next_index` (7) are NOT advanced this run — next routine run will pick up pm-security exactly where the schedule stopped. G16 is a no-op this run per its own "if Phase 1.6 was skipped, gate is a no-op" clause.
- Phase 1.5 (rotating expert review) **skipped this run.** Reason above — 45 fresh dispatcher tier-1d signals ingested instead. `review_cursor` unchanged. This is the routine's documented Phase 1.5 skip pattern (skill's oldest-unreviewed segment already covered by dispatcher tier-1d ≥8 times since 2026-09-11).
