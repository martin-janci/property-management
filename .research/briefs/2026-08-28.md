# 2026-08-28

## Refire — buffer-low (02:37 UTC)

Routine fired by the dispatcher with payload
`buffer-low: claimable=6/72 — refill planner (all remaining backlog is mobile-native/KMP, unlandable in cloud runner)`.
The 6 remaining action-list rows are all `code-review-mobile-native-kmp-*`
items — real bugs, but Gradle/JDK/Android SDK are absent in the cloud runner
so the dispatcher can't burn through them. The routine's job today is to
land **cloud-landable** work into `plans/` so the dispatcher has non-KMP
tasks to claim before the queue starves.

**Refill outcome — 2 landable-in-cloud plans promoted:**
- `plans/code-review-ppt-web-ui-registry-page-window-confirm-hardcoded-copy.md`
  (bug, score 3, confidence high, Mode: cloud-ok) — swap 4× `window.confirm`
  + hardcoded English on the pet/vehicle registry to the shared
  `ConfirmationDialog` + i18n, mirroring the compliance-page migration
  (#2829/#2855/#2849).
- `plans/code-review-ppt-web-ui-privacy-deletion-window-prompt-gdpr.md`
  (bug, score 3, confidence high, Mode: cloud-ok) — replace `window.prompt`
  gating GDPR account-deletion with a typed dialog; closes the
  Firefox/Safari dialog-suppression silent-drop on a right-to-erasure flow.

Both promotions come from this run's Phase 1.5 review of the `ppt-web-ui`
segment (churn-aligned; the top-3 churn files all live under that
segment). Both are non-`triage`, non-KMP, and pass the readiness gate at
score=3 + confidence=high on first surfacing — no waiting for score
compounding. Adversarial pass: passed for both.

**Structural note carried over from 08-25:** the KMP unlandable-in-cloud
gap is a DevOps concern. Phase 1.6 today rotated in `pm-devops` (last
run 2026-06-16 — 2+ months overdue) and raised two decisions on
`decisions.md`: (a) provision a self-hosted Kotlin/Gradle runner in the
dispatcher runner pool, or (b) split the mobile-native queue off to a
local-only lane the user drains manually. Adds 4 pm-devops action-list
items on top of the 6 mobile-native-kmp holdovers.

## Since last run
- Merged PRs: 11 (real merges in range #2848–#2864; 40 additional dispatcher chore commits on `dev`)
- Open PRs touched: 6 (all dependabot bumps, updated 2026-08-27 — no human review pending)
- Closed unmerged: 2 (dependabot #2809/#2810 superseded by regroup PRs #2865/#2867)
- New / updated issues: 6 (all CLOSED follow-ups: #2853, #2854, #2857, #2859, #2861, #2862 — all labeled, no untriaged intake)
- Commits: 40 on `dev` (11 real merge commits + 29 dispatcher chore commits touching only `.research/management/{assignments,action-list}.json`)
- Routine lag: 2d 17h since last run — **lag_warning fired** (>36h threshold; below 72h so no stale_routine_alert)
- Phases that failed: none — Phase 1 ran (subagent, 15 GitHub MCP calls / 20 budget), Phase 1.5 ran (`ppt-web-ui` segment, 3 findings), Phase 1.6 ran (upkeep mode, `pm-devops` rotated, coverage epic idx 6 → epic-81 no-op refresh), Phase 3 promoted 2 plans (both passed adversarial pass), Phase 5 skipped (no allowlisted signal met the certainty bar)

## Shipped
- #2864 `gh-issue-2861 fix membership ordering + remove dead capability-grant path` — root-cause follow-up to #2858; hardens `membership.rs` + prunes dead `capabilities.rs`/`auth_policy.rs` code (+131/-27, 5 files, includes `membership_test.rs`)
- #2863 `gh-issue-2862 suppress overdue truncation notice at exact 200 boundary` — off-by-one UX fix on the moderation list (+65/-9, 2 files)
- #2860 `gh-issue-2859 DB test overdue moderation filter + list truncation notice` — retro-fits the missing regression coverage for #2856 + #2858 as an IDOR-suite entry `aml_dsa_cross_org_idor_tests.rs` (+172/-6, 9 files)
- #2858 `fix(api-server) enforce strictest email-verification policy across all grantee gates` — closes #2857 (SECURITY: fail-open non-deterministic email-verification in `check_capability_grant_for_user`); +160/-8, 1 file (auth_policy.rs). **No test in the same PR** — regression test landed via follow-up #2860 (see `hotfix-no-test-pr-2858` signal in Backlog deltas, immediately resolved by the follow-up)
- #2856 `gh-issue-2853 server-side moderation overdue filter` — moves the overdue filter from client to server; +93/-81 across 7 files
- #2855 `gh-issue-2854 DsaReportsPage window.alert → toast` — matches the `window.alert → showToast` pattern from #2829/#2849 (+188/-23, 8 files, 6 locale JSONs)
- #2852 `churn-hotspot AmlDashboardPage.test.tsx dedupe helpers` — extracted shared decision-flow test helpers (+34/-26, 1 file)
- #2851 `pm-qa voice OAuth token encryption round-trip tests` — new test suite `voice_oauth_token_encryption_roundtrip_tests.rs` (+317/-0, 3 files)
- #2850 `pm-qa VerificationBadge expiry i18n snapshot` — initial regression snapshot across 6 locales (+251/-0, 1 file — top-3 churn this window as a result)
- #2849 `code-review-ppt-web-ui-content-mod-overdue-noop moderation overdue affordance filters queue` — links overdue-filter chip to the queried list (+175/-11, 8 files, 6 locale JSONs)
- #2848 `churn-hotspot AmlDashboardPage extract sections + data hook` — decomposed the AmlDashboard into sub-components (`AmlCountryRisksTable`, `AmlFiltersPanel`, `AmlThresholdsSection`) + `useAmlDashboard` data hook (+490/-277, 6 files — top-2 churn this window)

## Watch
- Stalled review: none new — all 6 open PRs are fresh dependabot bumps from 2026-08-27
- Reverted: none
- Churn hotspots this run:
  - `frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.test.tsx` — 332 lines across 4 PRs (#2849/#2856/#2860/#2863), first-seen (runs_seen=1)
  - `frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.tsx` — 326 lines from #2848 (runs_seen=2 — `repeated-churn` fired)
  - `frontend/apps/ppt-web/src/features/marketplace/components/VerificationBadge.i18n.snapshot.test.tsx` — 251 lines from #2850 (first-seen, initial suite)
- Compliance / moderation surface is the clear churn cluster this window — four merged PRs touched `ContentModerationPage.test.tsx` in 3 days. Consider consolidating remaining decision-flow logic and freezing the component surface after this batch lands.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none (the single `assignments.json` review row for `gh-issue-2743` reads `verdict=approve`, not `verdict=changes`)

## Code review slice
- Segment reviewed: ppt-web-ui (reason: churn-aligned — top-3 hotspots all live under this segment)
- Experts: Frontend (+ Completeness secondary)
- Findings: 3 (2× high-severity → immediately promoted to `plans/`; 1× low-severity → sits at score=1 in backlog waiting to stack)
- Next segment: reality-server (oldest unreviewed: 2026-08-19)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: unchanged (no PR touched frontend route trigger files this run)
- Drift signals this run: 0 (no PR touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: skipped this run (workflow only fires on route changes, and none occurred; save the GitHub call)

## Backlog deltas
- **New:** [3] `code-review-ppt-web-ui-registry-page-window-confirm-hardcoded-copy` — window.confirm + hardcoded English on 4 pet/vehicle registry sites; promoted to plan this run
- **New:** [3] `code-review-ppt-web-ui-privacy-deletion-window-prompt-gdpr` — window.prompt gates GDPR deletion; browser dialog-suppression silently drops right-to-erasure; promoted to plan this run
- **New:** [2] `hotfix-no-test-pr-2858` — merged as `done` immediately (resolved by follow-up test PR #2860)
- **New:** [1] `code-review-ppt-web-ui-compliance-console-error-in-onerror-handlers` — 8× console.error alongside localized toasts; low-effort sweep, waits to stack
- **New:** [1] `churn-hotspot-frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.test.tsx` — top-1 churn (332 lines / 4 PRs)
- **New:** [1] `repeated-churn-frontend/apps/ppt-web/src/features/compliance/pages/AmlDashboardPage.tsx` — runs_seen=2
- **New:** [1] `churn-hotspot-frontend/apps/ppt-web/src/features/marketplace/components/VerificationBadge.i18n.snapshot.test.tsx` — top-3 churn (251 lines)
- **Bumped:** none
- **Decayed:** none (no open item aged past 14 days this run)
- **Dropped:** none

## Plans promoted
- `plans/code-review-ppt-web-ui-registry-page-window-confirm-hardcoded-copy.md` — 4 window.confirm sites → shared ConfirmationDialog + `t(...)`; RegistryPage.i18n.snapshot.test.tsx regression · adversarial pass: passed
- `plans/code-review-ppt-web-ui-privacy-deletion-window-prompt-gdpr.md` — window.prompt → typed AccountDeletionDialog with email-retype; full-page i18n; regression asserts prompt-suppression can't silently drop · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met confidence=high + score≥3 + allowlist (screen-map-drift / screen-map-orphan / fixme-in-merged-code / dep-update-noise:lockfile-drift / stalled-review) this run

## Self-review findings
- open=35 (high=8, medium=24, low=3); new this run=0; acknowledged=19
- fp-mcp-push-large-archive-blocked [recurrence=18] — Phase 6 archive-move produces >64KiB assignments-archive.json, mcp__github__push_files can't inline it; fix: dispatcher-prompt Phase 1 runtime push probe + drop 64KiB size-guard when `PUSH_METHOD=git` succeeds
- fp-mcp-push-hard-assumed-proxy403 [recurrence=18] — dispatcher assumes `git push` proxy-403; empirically it succeeds once commit-email is `@users.noreply.github.com`; fix: same as above
- fp-tier1d-signals-not-promoted-to-backlog [recurrence=11] — 61+ `*tier1d*.json` signal files sit unpromoted while buffer drains; fix: add `.research/signals-promote.sh` or gate Tier-1d generation when unconsumed signals exceed threshold
- fp-action-list-in-progress-inverted [recurrence=6] — action-list.json has 31 `in-progress` twins for only 3 live assignments (twin-status inverted); fix: extend `gc1-reconcile.sh` with bijection check + T32 self-test
- fp-pr-2744-batches-unrelated-fixes [recurrence=3] — self-fix PR #2744 batches size-guard + retry-remint fixes, blocks 11d on the human-gate; fix: one-fix-per-PR + provision a second reviewer identity
- fp-implementer-branchless-after-reclaim [recurrence=3] — in-progress rows without an `origin` branch after reclaim occupy slots for 6h+; fix: escalate to `failed` after N reclaims + T33 self-test
- fp-phase-2-6-buffer-blind-to-archive [recurrence=3] — buffer-guard reports claimable=54/open=54 while archive holds terminal ids; fix: subtract archive task_ids in Phase 2.6 open_count AND GC3 formula
- … and 1 more (see `.research/self-improvement/findings.json`)

## Open questions
- Decision 2026-08-28 (pm-devops): (a) provision self-hosted Kotlin/Gradle runner in dispatcher runner pool, or (b) split mobile-native queue off to a local-only lane the user drains manually. See `.research/management/decisions.md` under today's date.
- Decision 2026-08-28 (pm-devops): should `plan-frontmatter` gain a `runner_requires:` field so the dispatcher can pre-filter plans by capability (e.g., `runner_requires: [jvm, android-sdk]`) instead of the current pattern-match on paths?

## Quiet day?
- No — 11 real merged PRs, 7 new backlog vectors, 2 plans promoted, 3 code-review findings, 4 new pm-devops action-list items. Telegram digest fires.
