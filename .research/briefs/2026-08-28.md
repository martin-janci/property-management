# 2026-08-28

## Fire context (dispatcher payload)

`buffer-low: claimable=0-landable/72 (6 open all mobile-native-gated) — refill planner needs non-KMP work`

This run's Phase 1.5 was steered accordingly: the rotating expert review picked `reality-server` (oldest-unreviewed at 2026-08-19, also non-KMP by construction). Two of the three findings promoted to plans this run — both `cloud-ok` — should fully refill the dispatcher's landable buffer.

## Since last run
- Merged PRs: 7 non-research application PRs on `dev` (real code): #2852, #2855, #2856, #2858, #2860, #2863, #2864 — all in the compliance / capability-grant / moderation clusters (all closed unmerged flag in the GitHub list is a false alarm — merge SHAs exist on `dev`).
- Open PRs touched: 3 dependabot PRs opened/updated this window (#2865/#2866/#2867 — chore stalls, `dep-update-noise`).
- Closed-not-merged PRs: 2 (#2809, #2810 — both dependabot, superseded by the newer group PRs).
- New / updated issues: 6 (all closed follow-ups: #2853, #2854, #2857, #2859, #2861, #2862; #2857 was a labeled security issue closed by #2858).
- Commits: 42 on `dev` (7 real merges + 35 dispatcher chore commits touching only `.research/management/…`).
- Routine lag: 2d 15h since last run — **lag_warning fired** (>36h threshold; <72h so `stale_routine_alert` does NOT fire).
- Phases that failed: none (Phase 1 fully ran; Phase 1.5 ran via an Explore subagent — 3 findings; Phase 1.6 **skipped this run — see Open questions**; Phase 3 promoted 2 plans; Phase 5 no candidate).

## Shipped
- #2852 `test(ppt-web)` — de-duplicate AmlDashboardPage decision-flow test helpers (churn-hotspot follow-up from 2026-08-25).
- #2855 `gh-issue-2854` — replace `DsaReportsPage` window.alert with localized toast feedback (resolves the 2026-08-26 follow-up for the i18n pass in #2847).
- #2856 `gh-issue-2853` — make moderation overdue filter server-side (resolves the "silent no-op" backlog item promoted in the 2026-08-25 brief).
- #2858 `fix(api-server)` — enforce strictest email-verification policy across all grantee orgs in capability-grant gate (closes security issue #2857).
- #2860 `gh-issue-2859` — DB test for overdue moderation filter + explicit list truncation notice (test-gap follow-up on #2856).
- #2863 `gh-issue-2862` — suppress overdue truncation notice at the exact 200 boundary (edge-case follow-up on #2860).
- #2864 `gh-issue-2861` — fix root-cause membership ordering non-determinism + remove dead capability-grant path + refresh stale doc (follow-up on #2858 removing the pattern rather than just the symptom).

## Watch
- Stalled review: no NEW human stalled-review this run. 3 `feat(acc)` PRs (#2555 / #2558 / #2559) remain in `needs-human-judgement` (all at 30 d idle now, one-shot deduped). #2744 dispatcher un-wedge PR still open as draft (15 d idle, `needs-human-review` label).
- Reverted: none.
- Churn hotspots (top-3 this run, filtered to real merged PRs only — dispatcher chore commits excluded):
  - `frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.test.tsx` — 218 lines, 3 touches (runs_seen=1)
  - `backend/servers/api-server/src/services/auth_policy.rs` — 191 lines, 2 touches (runs_seen=1)
  - `frontend/apps/ppt-web/src/features/compliance/pages/DsaReportsPage.test.tsx` — 133 lines, 1 touch (runs_seen=1)
  All three are new hotspots; no `repeated-churn` this run.

## PRs stuck in draft despite approval
- none — #2744 is draft but not approved (`reviewDecision != APPROVED`), so it doesn't hit the merge-gate class this section catches.

## PRs with verdict=changes, no fix-round progress in 24h
- none — dispatcher chore commits show only `verdict=approve` on the merged batch; no active `verdict=changes` row in `.research/management/assignments.json` on the planning branch (would need a cross-branch check to fully confirm; no chore commits reference such a row).

## Code review slice
- Segment reviewed: `reality-server` (reason: oldest-unreviewed at 2026-08-19; also aligned with the fire-payload directive to produce non-KMP buffer)
- Experts: rust + security (via a single-agent Explore pass — compact-routine mode; ppt-dev-review skill not spawned this run)
- Findings: 3 (`code-review-finding` signals — see *Backlog deltas*)
  - HIGH `sso-reqwest-missing-timeout` — indefinite hang / DoS on public SSO callback (score 3, `bug`, promoted)
  - HIGH `sso-pending-sessions-unbounded` — unbounded HashMap + async-Mutex contention on public `sso_login` (score 3, `bug`, promoted)
  - LOW `favorite-ids-fetches-full-listing-payload` — SSR hot-path over-fetch (score 1, `refactor`, backlog only)
- Next segment: `api-core` (oldest-unreviewed after this run: 2026-08-21; tied with `ppt-web-core` on that date, alphabetical break to `api-core`)
- Skipped: no — reviewed

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` + `reality/`; `mobile/` still not seeded → 0)
- Drift signals this run: 0 — none of the 7 merged PRs touched `frontend/apps/ppt-web/src/{App.tsx,routes/**}` or `frontend/apps/reality-web/src/app/**`
- Orphan screens this run: 0 — no route basenames changed under the trigger set
- Last `screen-map.yml` CI run on `main`: unchanged from 2026-08-25 brief (workflow only fires on relevant docs/frontend paths; no re-run this window)

## Backlog deltas
- **New:** [3] `code-review-reality-server-sso-reqwest-missing-timeout` — 3 SSO reqwest clients with no `.timeout(...)` on public `/api/v1/sso/callback` path
- **New:** [3] `code-review-reality-server-sso-pending-sessions-unbounded` — `sso_login` unbounded HashMap insert + no background sweeper
- **New:** [1] `code-review-reality-server-favorite-ids-fetches-full-listing-payload` — SSR favorites-ids hot-path over-fetches full listing rows + photo subquery
- **New:** [1] churn-hotspot `frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.test.tsx`
- **New:** [1] churn-hotspot `backend/servers/api-server/src/services/auth_policy.rs`
- **New:** [1] churn-hotspot `frontend/apps/ppt-web/src/features/compliance/pages/DsaReportsPage.test.tsx`
- **Bumped:** none
- **Decayed:** none (no `open`/`deferred` items with `updated_at <= 2026-08-14` at run start)
- **Dropped:** none

## Plans promoted
- `plans/code-review-reality-server-sso-reqwest-missing-timeout.md` — add a shared timeout-configured `reqwest::Client` on `AppState`, mirror the existing `PmApiClient` pattern, and thread it through all three SSO call sites · adversarial pass: passed
- `plans/code-review-reality-server-sso-pending-sessions-unbounded.md` — cap `sso_sessions` at 10 k entries + spawn a periodic sweeper; return 429 when the cap is hit · adversarial pass: passed

## Auto-fix
- `no candidate` — no signal met the allowlist (`screen-map-drift` / `screen-map-orphan` / `fixme-in-merged-code` / `dep-update-noise` lockfile-drift / `stalled-review`) × confidence=high × score≥3 × idempotency bar this run. The two new SSO findings meet confidence + score but are `code-review-finding` — not on the auto-fix allowlist (they require a full plan + human/implementer, not a mechanical safe-shape).

## Self-review findings
- none — `.research/self-improvement/findings.json` not present in the repo

## Open questions
- **Phase 1.6 skipped this run.** The `ppt-project-management` skill was NOT invoked this run — the fire payload framed this as a mid-day buffer-refill run rather than the full daily PM sweep. `pm_cursor.next_index` remains 4 (pm-devops, last run 2026-06-16 — 73 d idle). Next full daily run should either invoke pm-devops (rotation) or a `pm-full` catch-up given the accumulated staleness across roles.
- Cloud daily routine has now missed 3 firings in a row (last full brief 2026-08-25 03:20; two intra-day refires since). Same open question as prior brief: worth checking the deployment-side scheduler; the dispatcher on the planning branch keeps ticking, so the outage is scoped to the daily routine specifically.
- The 3 stalled `feat(acc)` PRs (#2555 / #2558 / #2559) are now 30 d idle. Blocked on the accounting spec resolution.

## Quiet day?
- **No** — 7 real PRs shipped, 6 new backlog vectors added, 2 plans promoted (both cloud-ok, both refill the non-KMP buffer that triggered this run), 0 auto-fixes. `stats.runs` 71 → 72; `stats.vectors_created` 324 → 330; `stats.plans_created` 28 → 30.
