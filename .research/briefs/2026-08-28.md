# 2026-08-28

## Since last run
- Merged PRs: 6 (range #2855–#2864; real code-touch on `dev` since 142ae17)
- Open PRs touched: ~10 (3 dependabot batches opened 2026-08-27; the human `feat(acc)` #2555/#2558/#2559 still parked as `needs-human-judgement`)
- New / updated issues: 0 (none in window)
- Commits: 50 on `dev` (6 real merges + 44 dispatcher chore commits)
- Routine lag: 2d 18h since last run — **lag_warning fired** (>36h threshold); operator: cloud routine cadence needs a look; dispatcher on `claude/*` branches has been ticking every 2h in the interim
- Phases that failed: none — Phase 1 fetched PRs/issues/commits + churn; Phase 1.5 skipped in favor of the dispatcher's Tier-1d `api-handlers` review committed at 2026-08-28T00:00Z (3 findings ingested here); Phase 1.6 skipped (light — management artifacts already valid; `pm_cursor` and `coverage_cursor` intentionally not advanced this run per G16 no-op path); Phase 2 ran; Phase 3 promoted 0 plans (no candidate met the readiness gate); Phase 5 skipped (no allowlisted signal met the certainty bar)

## Shipped
- #2856 `gh-issue-2853` — moderation overdue filter moved server-side; `moderation.rs` + `compliance.rs` + `ContentModerationPage.tsx` (test parity added). Closes #2853.
- #2855 `gh-issue-2854` — replaced `DsaReportsPage` `window.alert` with localized toast feedback; 6 locale bumps + `DsaReportsPage.tsx` + toast wiring.
- #2858 `fix(api-server)` — enforce strictest email-verification policy across all grantee orgs in capability-grant gate; `auth_policy.rs` (+ IDOR test `aml_dsa_cross_org_idor_tests.rs`).
- #2860 `gh-issue-2859` — DB test for overdue moderation filter + explicit list truncation notice; `ContentModerationPage.tsx` + `ContentModerationPage.test.tsx` + `moderation.rs`.
- #2863 `gh-issue-2862` — suppress overdue truncation notice at exact 200 boundary; `ContentModerationPage.tsx` + test.
- #2864 `gh-issue-2861` — fix root-cause membership ordering non-determinism, remove dead capability-grant path, refresh stale doc; `membership.rs` + `capabilities.rs` + `auth_policy.rs` + `compliance.rs` + `mod.rs` + IDOR test.

## Watch
- Stalled review: #2744 (draft, `needs-human-review`, `fix(dispatcher): un-wedge oversize-archive push` + `retry-remint cross-id ghost` — last update 2026-08-13 06:50 UTC → **>15d idle**). Already in `needs-human-judgement` upstream; keep it there.
- Reverted: none this run.
- Churn hotspots this run (real code-touch only; excludes `.research/management/`, locale JSON, and generated files):
  - `frontend/apps/ppt-web/src/features/compliance/pages/ContentModerationPage.tsx` — 4 changes; runs_seen=1 (new to hotspot_history)
  - `backend/servers/api-server/src/services/auth_policy.rs` — 3 changes; runs_seen=1
  - `backend/servers/api-server/src/routes/aml_dsa/moderation.rs` — 2 changes; runs_seen=1

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none

## Code review slice
- Segment reviewed: `api-handlers` (via dispatcher Tier-1d, committed 2026-08-28 00:00 UTC — reason: churn-aligned on `aml_dsa/moderation.rs` + `admin/capabilities.rs` + `services/auth_policy.rs`; segment last reviewed 2026-08-22 with 0 open findings — not saturated)
- Experts: rust + security
- Findings: 3 (all `code-review-finding`; +2 each; medium confidence). Ingested into `backlog.json` as `code-review-api-handlers-decide-appeal-unvalidated`, `code-review-api-handlers-report-content-unbounded`, `code-review-api-handlers-capabilities-list-cross-org` (the last one tagged `vector: security` — one more high-confidence bump would fast-track it to `ready`).
- Next segment: `reality-server` (oldest unreviewed after this run, 2026-08-19)
- Skipped: no — dispatcher ran; routine ingested its signals

## Screen-map status
- Total `docs/screens/` files: 153 (`ppt/` 106 + `reality/` 47; `mobile/` still 0)
- Drift signals this run: 0 (none of the 6 merged PRs touched `App.tsx`, `routes/**`, or `app/**`)
- Orphan screens this run: 0 (no route-basename check needed — no route file touched)
- Last `screen-map.yml` CI run on `main`: not queried this run (no `docs/screens/**` changes in window; expected quiet)

## Backlog deltas
- **New:** 
  - [2] `code-review-api-handlers-decide-appeal-unvalidated` — decide_appeal accepts free-form decision string; unbounded rationale
  - [2] `code-review-api-handlers-report-content-unbounded` — report_content stores unbounded user-supplied reason on an all-authenticated endpoint
  - [2] `code-review-api-handlers-capabilities-list-cross-org` — SECURITY: `list_for_user` has no org scoping; cross-tenant enumeration if `AuditRead` is org-scoped
  - [1] `churn-hotspot-…ContentModerationPage.tsx` — 4 touches this window
  - [1] `churn-hotspot-…auth_policy.rs` — 3 touches this window
  - [1] `churn-hotspot-…aml_dsa/moderation.rs` — 2 touches this window
- **Bumped:** none (no existing items received new evidence this run)
- **Decayed:** none (no open item is >14d stale — the three mobile-native-kmp bugs sit at 2026-08-16/-19/-25)
- **Dropped:** none

## Plans promoted
- none — top open score is 2 (`code-review-mobile-native-kmp-httpclient-no-timeout` high-confidence bug and the three new Tier-1d findings), one short of the +3 gate. The new `capabilities-list-cross-org` row is `vector: security` at score 2 but only `medium` confidence — security fast-track requires `high` confidence. Next run, one more corroborating signal on any of these promotes.

## Auto-fix
- `no candidate` — no `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift, or `stalled-review` signal met confidence=high + score≥3 + allowlist this run

## Self-review findings
- none (`.research/self-improvement/findings.json` present but not re-evaluated by this run — the dispatcher runs its own Phase-8 self-review daily; the routine deliberately doesn't second-guess it here)

## Open questions
- Is `Capability::AuditRead` an org-scoped grant or a platform-global capability? The answer decides whether `code-review-api-handlers-capabilities-list-cross-org` is a real cross-tenant read (score-wise it's a security finding waiting on confirmation) or an intentional platform-admin surface. Trace `grants.list_for_user` call sites in `backend/crates/db` to confirm.
- Cloud routine cadence: 2d 18h between runs is above the 36h `lag_warning` threshold. The dispatcher on `claude/*` branches is healthy; only the daily-routine beat is missing. Consider re-checking the cron / scheduler config.

## Quiet day?
- No — 6 merged PRs shipped, 6 new backlog vectors added (3 code-review findings + 3 churn hotspots), and 3 open items sit one signal below the readiness gate. Digest will send.
