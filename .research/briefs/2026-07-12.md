# 2026-07-12

## Since last run
- Merged PRs: 41 (range #2186–#2261)
- Open PRs touched: 2 (#2260, #2262 — both same-day follow-ups)
- New / updated issues: 9 (all `follow-up` + `from-merged-review` — none untriaged)
- Commits: 100 on `dev` (~50 by dispatcher / research automation, ~50 real code/PR merges)
- Routine lag: 3d 13h since last run — **stale-routine alert** (>= 3 days). Cloud routine has been paused between 2026-07-09T02:37Z and today's ~16:39Z; dispatcher (planning branch) continued ticking normally throughout.
- Phases that failed: none

## Shipped
- **Security (this batch is heavy on hardening).**
  - **#2254 (fix reality-server SSO):** exchange no longer trusts client-supplied `request.roles` — closes portal privilege-escalation surface (closes #2249).
  - **#2257 (fix reality-server portal analytics):** SECURITY DEFINER migration wires `track_listing_view` write path around FORCE RLS block.
  - **#2218 (fix reality-server portal analytics reads):** SECURITY DEFINER `portal_get_listing_analytics` scoped to portal owners.
  - **#2234 (fix api-server auth):** rate-limit `/forgot-password` + `/resend-verification` — mailbomb / token-clobber.
  - **#2215 (fix api-server documents):** enforce share↔document scoping at the repo layer (closes #2197, defense-in-depth on top of #2181's handler guard).
  - **#2205 (fix api-server auth):** empty `refresh_token=;` cookie no longer shadows a valid body token (closes #2179 class).
  - **#2261 (fix api-server auth):** `resolve_current_session_id` now user-scoped (defense-in-depth follow-up to #2190).
  - **#2226 (KMP path-param encoding + pgvector clamp-panic fix):** supersedes #2213.
  - **#2219 (test api-server oauth):** OAuth CSRF single-use rejection covered at handler level (closes #2203).
- **CI / infra.**
  - **#2223 (CI shard):** backend `test` job now sharded — ended the `-p common --lib` timeout wedge (closes #2222). Follow-ups #2260 / #2262 rebalance shards + harden sitemap test.
  - **#2225 (fix common):** stale sitemap `test_metadata` count 13→19 — unblocked the merge pipeline.
  - **Dependabot flood:** #2232 (cargo-minor-patch × 3), #2228 (npm-minor-patch × 17), #2212 (netinfo), #2211 (metro-config), #2210 (expo-localization), #2209 (expo-location) — no code impact; noise, tracked in signals.
- **Test-coverage remediation series (7 PRs).** #2252/#2253/#2255 (RLS regression tests locking down PR #1287 / #1289 / #1292 hotfixes), #2189 (Airbnb direct-connect + availability-sync handlers), #2229 (KMP NotificationRepository test), #2216 (ProtectedRoute return-URL guard), #2227 (getAppEnv fallback + dedup).
- **Frontend / UX.** #2217 (integrations settings discoverability + i18n), #2233 (mirror integrations i18n to sk/cs/de/hu/pl), #2194 (Booking property-mapping wizard wired).

## Watch
- **Stalled review:** none — both open PRs (#2260, #2262) are same-day and moving.
- **Reverted:** none in-window.
- **Churn hotspots:**
  - `backend/servers/api-server/src/routes/auth.rs` — 309 lines (add+del, excl. dispatcher) across 3 real commits (#2234 / #2250 / #2261); `runs_seen`=3 (repeated-churn now bumps to score 2 for this hotspot).
  - `backend/crates/integrations/src/booking/mod.rs` — 239 lines across 2 commits (#2230 / #2231); `runs_seen`=2 (first-time repeated territory).
  - `backend/crates/db/src/repositories/llm_document.rs` — 66 lines across 2 commits (#2226 / #2256); first-run hotspot, `runs_seen`=1.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h.
     Example:
       gh pr list --repo martin-janci/property-management --state open --draft \
         --json number,title,updatedAt,reviewDecision,isDraft \
         --jq '.[] | select(.reviewDecision=="APPROVED" and .isDraft==true)'
     Compute age-in-hours from updatedAt. If none: emit a single line "- none".
     This catches the merge-gate bug class (#539-style): PR approved, CI green,
     but stuck in draft because a human gate wasn't cleared. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json (planning branch) where
     reviewer_summary starts with "verdict=changes" AND
     (now - last_updated) > 24h AND status == "review".
     Example (read planning's assignments via gh):
       gh api repos/martin-janci/property-management/contents/.research/management/assignments.json?ref=planning \
         --jq '.content' | base64 -d | jq '.assignments[]
         | select(.status=="review" and (.reviewer_summary // "" | startswith("verdict=changes")))
         | {task_id, pr_number, last_updated, reviewer_summary}'
     If none: emit "- none". -->
- none (dispatcher's own tracking is on the planning branch; not surveyed here to keep routine budget-bounded)

## Code review slice
- Segments reviewed this run (four ran opportunistically before this brief): **api-handlers, mobile-native-kmp, ppt-web-ui, reality-server** (reason: pre-computed segment signal files present under `.research/signals/2026-07-12-<segment>.json` from earlier same-day activity).
- Experts: rust-backend (api-handlers, reality-server), react-frontend (ppt-web-ui), kotlin (mobile-native-kmp).
- Findings: 6 (see Backlog deltas for `code-review-finding` signals) — 2 high-severity security (suspended-enum-oracle, SSRF ipv4-mapped-ipv6), both promoted to plans this run.
- Next segment: **reality-web** (oldest unreviewed at 2026-06-14 after this run).
- Skipped: fresh Phase 1.5 invocation this run — the 4 segment signal files from earlier today already saturate the review budget; a 5th segment would exceed the token cap.

## Screen-map status
- Total `docs/screens/` files: existing docs unchanged this run (no frontend route file was touched by any merged PR in-window, so no drift check fired).
- Drift signals this run: 0
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not queried (routine budget); the workflow only fires on `docs/screens/**` touches, none this window.

## Backlog deltas
- **New (open):**
  - [2] `refactor-churn-hotspot-backend-servers-api-server-src-routes-auth-rs` — auth.rs churn (churn +1, repeated +1)
  - [1] `refactor-churn-hotspot-backend-crates-integrations-src-booking-mod-rs` — booking OTA client
  - [1] `refactor-churn-hotspot-backend-crates-db-src-repositories-llm-document-rs` — llm_document
  - [1] `bug-code-review-mobile-native-kmp-no-http-timeout` — KMP HttpClient missing HttpTimeout
  - [1] `refactor-code-review-ppt-web-ui-sessionspage-unsafe-cast` — SessionsPage.tsx double cast
  - [1] `bug-code-review-ppt-web-ui-moderation-prompt-unvalidated` — ContentModerationPage prompt() coerce
  - [2] `security-code-review-api-handlers-raw-db-error-leak` — 437 arms leak sqlx `e.to_string()` (info disclosure)
- **New (ready — promoted this run):**
  - [3] `security-code-review-api-handlers-suspended-enum-oracle` — see Plans promoted
  - [3] `security-code-review-reality-server-ssrf-ipv4-mapped-ipv6` — see Plans promoted
- **Bumped:** none (all previous rows are already terminal — no active items pre-existed)
- **Decayed:** none (all previous rows are already terminal)
- **Dropped:** none

## Plans promoted
- `plans/security-code-review-api-handlers-suspended-enum-oracle.md` — reorder login so password verify runs BEFORE the suspended-status branch; add regression tests. · adversarial pass: passed
- `plans/security-code-review-reality-server-ssrf-ipv4-mapped-ipv6.md` — route `::ffff:a.b.c.d` IPv4-mapped IPv6 through the existing IPv4 blocklist; add unit tests including `[::ffff:169.254.169.254]` cloud-metadata bypass. · adversarial pass: passed

## Auto-fix
- `no candidate` — the 2 high-scored high-confidence signals promoted this run (`suspended-enum-oracle`, `ssrf-ipv4-mapped-ipv6`) are code-fix signals, NOT in the Phase 5 allowlist (allowlist covers `screen-map-drift`, `screen-map-orphan`, `fixme-in-merged-code`, `dep-update-noise` lockfile-drift sub-shape, `stalled-review`); code fixes go to the manual implementer path per policy.

## Self-review findings
- self-improvement/findings.json: 34 total open (per dispatcher's most-recent `chore(research): dispatcher self-review 2026-07-12 — 3 new findings (findings_total=34)` on `dev`); no new-this-run action-taken.
- (No high-severity open findings surfaced this run — the routine's self-improvement synth runs off the dispatcher loop, not the daily routine loop.)

## Open questions
- Routine cadence: last commit 2026-07-09; lag 85h. Should the operator review why the cloud routine trigger stalled? (Dispatcher on planning branch is unaffected — same-repo, different loop.)
- pm-full mode: 15 of the 17 role-audit slots since 2026-06-16 are stale — worth a one-off `text: "pm-full"` trigger to refresh the whole `.research/management/` panel? Not required for correctness (G16 is a no-op when Phase 1.6 is upkeep), just currency.

## Quiet day?
- No — 41 merged PRs, 6 code-review findings, 2 plans promoted, 9 new backlog rows.
