# 2026-07-12

## Since last run
- Merged PRs: 35 (range #2186–#2250)
- Open PRs touched: 5 (#2251, #2252, #2253, #2254, #2255 — all opened today, all draft, none stalled)
- New / updated issues: 26 (new since #2087: 15 follow-up review items, 1 CI-timeout bug, 1 SSO privilege-escalation security)
- Commits: 97 on `dev` (majority dispatcher chore commits — real code churn concentrated in ~30)
- Routine lag: 3d 8h since last run (last_run_iso=2026-07-09T02:37:00Z) — `lag_warning` + `stale_routine_alert` both fired (≥3d threshold)
- Phases that failed: none
- Trigger payload: `buffer-low: claimable=17/72 — refill planner` — noted; 1 solid plan promoted this run

## Shipped
- **#2250** refactor(api-server): extract shared refresh-token resolve + hash helpers in `routes/auth` — deduplicates cookie-first/body-fallback + SHA-256 hashing across `refresh_token`, `logout`, `resolve_current_session_id`. Adds 4 unit tests. Resolves the highest-churn logic in the top-churn hotspot.
- **#2234** fix(api-server): rate-limit `/forgot-password` and `/resend-verification` — closes `security-forgot-password-no-rate-limit` (previously score 2/ready). Adds `rate_limit.rs` middleware crate.
- **#2233** gh-issue-2224: mirror integrations i18n keys into sk/cs/de/hu/pl locales — closes the 5-locale gap from #2217.
- **#2229** code-review test: NotificationRepository repository test — from the review backlog.
- **#2226** gh-issue-2195: KMP path-param encoding + pgvector clamp-panic fix (supersedes #2213).
- **#2225** fix(common): unblock CI — update stale sitemap `test_metadata` route count (13→19).
- **#2223** gh-issue-2222: shard CI backend test job to end the `-p common --lib` timeout wedge (closed issue #2222).
- **#2221** gh-issue-2201: harden RAG embedding-write path — connection-holding, provider provenance, FK error shape.
- **#2220** gh-issue-2198: harden create-schedule (report_id/timezone validation + next_run_at).
- **#2219** gh-issue-2203: cover OAuth CSRF replay/single-use rejection at handler level.
- **#2218** gh-issue-2199: scope per-listing analytics reads to portal owners (SECURITY DEFINER).
- **#2217** gh-issue-2202: make integrations settings discoverable, i18n it, reconcile Airbnb OAuth redirect — **screen-map drift signal fired** (route change without `docs/screens/ppt/` update).
- **#2216** gh-issue-2200: exercise ProtectedRoute /login return-URL guard.
- **#2215** gh-issue-2197: enforce share↔document scoping at the repo layer.
- **#2214** gh-issue-2196: provision PORTAL_WEBHOOK_SECRET + webhook-secret env docs.
- **#2205** bug-refresh-empty-cookie-shadows-body-token — closes prior backlog item (was score 1/ready, now `done`).
- **#2190** bug-revoke-all-sessions-cookie-blindness — closes prior backlog item (was score 3/ready, now `done`).
- **#2186–#2194** hotspot / gap tasks: booking-oauth CSRF + routes coverage, Airbnb direct-connect coverage, UC↔screen-map links (UC-10, 29, 33, 40), Booking.com property-mapping wizard wired into settings, plus dependabot minor bumps (#2209–#2212, #2228, #2232).

## Watch
- Stalled review: none — all open PRs are ≤ 6 h old and drafts.
- Reverted: none merged this window.
- Closed unmerged: #2213 (KMP path-param, superseded by #2226 — new backlog vector `closed-not-merged-pr-2213`); #2208 npm-minor-patch, #2207 cargo-minor-patch, #2206/#2016 dtolnay/rust-toolchain (dependabot noise); #1797 old OCR-auth PR closed after being long stale.
- Churn hotspots (top 3, code-only):
  - `backend/servers/api-server/src/routes/auth.rs` — 471 lines across 6 events (runs_seen=3, `refactor-churn-hotspot-api-server-routes-auth` was fixed by #2250 mid-window; residual churn from associated bug fixes).
  - `backend/crates/integrations/src/booking/mod.rs` — 333 lines across 6 events (runs_seen=2 — Booking OTA retry + XML envelope coverage).
  - `backend/servers/api-server/src/routes/rate_limit.rs` — 253 lines across 3 events (net-new middleware from #2234; expected to stabilise post-landing).
- Note: `.github/workflows/backend.yml` (1470 lines) and `backend/scripts/ci-test-shard.sh` (732 lines) also churned heavily this window but are CI infrastructure, not application code — skipped for hotspot signals.

## PRs stuck in draft despite approval
<!-- Query: PRs where isDraft==true AND reviewDecision==APPROVED AND updatedAt < now-24h. Result: none. -->
- none

## PRs with verdict=changes, no fix-round progress in 24h
<!-- Query: rows in .research/management/assignments.json where reviewer_summary starts with "verdict=changes" AND status=="review" AND age>24h. Not queried this run (Phase 1.6 skipped for token discipline); dispatcher tracks. -->
- none surfaced (Phase 1.6 skipped this run — dispatcher owns this alert)

## Code review slice
- Segment reviewed: `api-handlers` (churn-aligned — routes/auth.rs was top-churn) AND `mobile-native-kmp` (dispatcher-tier1d demand-driven — see per-slice signal files)
- Experts: rust + security (api-handlers), kotlin (mobile-native-kmp)
- Findings: 3 total (2 high-conf on api-handlers, 1 medium-conf on KMP)
  - `code-review-api-handlers-suspended-enum-oracle` (+3, high, → new `security` vector row, ready, promoted)
  - `code-review-api-handlers-raw-db-error-leak` (+2, high, bug — ~437 handler arms leak `e.to_string()`; open at score 2)
  - `code-review-mobile-native-kmp-no-http-timeout` (+1, medium, bug — HttpClient has no HttpTimeout plugin; open at score 1)
- Next segment: `reality-server` (oldest-unreviewed at 2026-06-11, ~31 days stale)
- Skipped: no — two segments landed this run because pre-existing 2026-07-12 slice files (`signals/2026-07-12-api-handlers.json`, `signals/2026-07-12-mobile-native-kmp.json`) had been committed by earlier dispatcher passes; the routine absorbed them and advanced both cursors to 2026-07-12.

## Screen-map status
- Total `docs/screens/` files: unchanged this run (product dirs `ppt/` + `reality/`; `mobile/` still unseeded, product excluded from drift detection).
- Drift signals this run: 1 (`screen-map-drift-pr-2217-ppt` — new discoverable integrations settings route added without `docs/screens/ppt/` update).
- Orphan screens this run: 0.
- Last `screen-map.yml` CI run on `main`: not queried (green on last check — no recent doc-tree changes triggering it).

## Backlog deltas
- **New (score 3):** `code-review-api-handlers-suspended-enum-oracle` (security) — reclassified from reviewer's `bug` to `security` (it IS an enumeration oracle); ready and promoted.
- **New (score 2):** `code-review-api-handlers-raw-db-error-leak` (bug) — systemic information disclosure across ~21 route files.
- **New (score 2):** `screen-map-drift-pr-2217-ppt` (test-gap) — PR #2217 route change without `docs/screens/ppt/` update.
- **New (score 1):** `code-review-mobile-native-kmp-no-http-timeout` (bug) — HttpClient timeout plugin missing.
- **New (score 1):** `repeated-churn-backend-servers-api-server-src-routes-auth-rs` (refactor) — post-#2250 residual instability watch.
- **New (score 1):** `repeated-churn-backend-crates-integrations-src-booking-mod-rs` (refactor).
- **New (score 1):** `churn-hotspot-backend-servers-api-server-src-routes-rate-limit-rs` (refactor) — new middleware from #2234.
- **New (score 1):** `closed-not-merged-pr-2213` (dx) — superseded by #2226.
- **Re-emit (score 0, cumulative-type):** `churn-hotspot-…-auth-rs` and `churn-hotspot-…-booking-mod-rs` — both re-fire with new churn evidence but no `score_delta` applied per G4 carve-out.
- **Bumped:** none (no signal updated an already-open item this run — the previously-open items were all flipped to `done` by the dispatcher before this run).
- **Decayed:** none applied (no `open` items with `updated_at ≤ 2026-06-28`; all previously-open items had been resolved by dispatcher between runs).
- **Dropped:** none this run.

## Plans promoted
- `plans/security-suspended-account-enum-oracle.md` — move the pre-password suspended-account branch to post-password (mirroring the #956 email-verification fix) + 3 regression tests in `auth_enumeration_tests.rs` · adversarial pass: passed — hypothesis maps directly to file:line evidence; suggested approach is a single code move + additive tests; IG3 test (`login_suspended_wrong_password_returns_generic_invalid_credentials`) will fail on `main` and pass after the move.

## Auto-fix
- `no candidate` — the highest-confidence auto-fix candidate this run was `screen-map-drift-pr-2217-ppt` (allowlisted, doc-stub shape), but its confidence is `medium` (heuristic), which fails the Phase 5 certainty bar (`high`-only). `screen-map-orphan`, `fixme-in-merged-code`, `stalled-review`, and lockfile-drift `dep-update-noise` all had zero eligible signals this run.

## Self-review findings
- none

## Open questions
- Should `code-review-api-handlers-raw-db-error-leak` be reclassified `security` (would fast-track at score 2)? Left as `bug` for now — the systemic scope (~437 arms across 21 files) makes it feel more like a hardening refactor than a discrete vulnerability, and the reviewer classified it that way. Revisit if a specific arm turns out to leak a schema-level secret.

## Quiet day?
- No — 35 merged PRs, 8 new backlog vectors, 1 plan promoted, 3 code-review findings. High activity.
