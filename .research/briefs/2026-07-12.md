# 2026-07-12

## Since last run
- Merged PRs: 48 (range #2189–#2271)
- Open PRs touched: 0 (dev queue drained — everything merged or closed)
- New / updated issues: 27 (all closed same day — dispatcher follow-up auto-issues from post-merge reviews)
- Commits: ~90 on `dev` (48 feature merges + ~42 dispatcher chore commits)
- Routine lag: 3d 19h since last run (last: 2026-07-09T02:37Z) — dispatcher kept ticking on the same branch while the daily routine was quiet
- Phases that failed: none

## Shipped

Very high-velocity delivery cycle. Themes:

**Security hardening (5 hot-path merges)**
- #2254 `gh-issue-2249`: reality-server SSO exchange no longer trusts client-supplied roles — closes portal privilege-escalation (high-severity, found by dispatcher Tier-1d static review on `reality-server` segment)
- #2267 `gh-issue-2263`: treat empty `<PORTAL>_WEBHOOK_SECRET` as unconfigured (fail closed) — closes fail-open follow-up on #2259
- #2261 `gh-issue-2246`: harden `resolve_current_session_id` user-scoping + tighten session tests
- #2271 `gh-issue-2241`: OAuth state consume atomic on Redis path (single-use replay-safe)
- #2234 `security-forgot-password-no-rate-limit`: rate-limit /forgot-password + /resend-verification (mailbomb/token-clobber gap)

**IDOR/RLS cleanups**
- #2218 `gh-issue-2199`: scope per-listing analytics reads to portal owners (SECURITY DEFINER)
- #2257 `gh-issue-2244`: record portal listing views via SECURITY DEFINER (fix analytics write path)
- #2266 `gh-issue-2243`: scope `get_share_access_log_rls` to owning document
- #2215 `gh-issue-2197`: enforce share↔document scoping at the repo layer

**Test-gap coverage**
- #2216 exercise ProtectedRoute /login return-URL guard
- #2252/#2253/#2255 `test-gap-hotfix-no-test-*`: lock RLS on sentiment_trends, public-connection routing, MFA regenerate_backup_codes
- #2265 test RAG provenance `model_filter`; #2264 test DST transitions in report-schedule
- #2270 handler-level regression for empty-cookie-shadows-body-token
- #2229 NotificationRepository KMP repo test

**CI unblock / infra**
- #2223 shard backend `test` job to end `-p common --lib` timeout wedge (fixes issue #2222)
- #2260 rebalance test shards + lock bundled common-lib fix
- #2225 fix stale sitemap `test_metadata` route count (13→19)

**Refactor churn**
- #2250 `refactor-churn-hotspot-api-server-routes-auth`: extract shared refresh-token resolve + hash helpers
- #2251 finish env-resolver de-dup — stale `getAppEnv()` comment + duplicated `AppEnvironment` type

**Deps** (6 dependabot merges — expo-location, expo-localization, @expo/metro-config, netinfo, npm-minor-patch bundle, cargo-minor-patch bundle)

## Watch
- Stalled review: none — every open PR either merged or was closed unmerged (#2262/#2258 duplicate of #2247, #2213 superseded by #2226, plus 4 dependabot supersessions)
- Reverted: 0 reverts this cycle
- Churn hotspots: `backend/servers/api-server/src/routes/auth.rs` (4 modifications; runs_seen=**3**, recent_churn=4), `backend/servers/api-server/tests/auth_tests.rs` (3; runs_seen=1), `backend/servers/reality-server/src/routes/sso.rs` (2; runs_seen=1). The post-#2250 helper-extract refactor did NOT calm auth.rs — iteration is on legitimate security/correctness branches (#2205, #2261, #2270, #2271), not stylistic churn.

## PRs stuck in draft despite approval
- none

## PRs with verdict=changes, no fix-round progress in 24h
- none — dispatcher fast-path merged verdict=approve PRs within the same day; verdict=changes cases (e.g. #2266, #2263) were re-spawned and re-merged inside 24h

## Code review slice
- Segment reviewed: **skipped by the routine**
- Reason: the dispatcher's Tier-1d rotating static-review pass covered **5 segments today** — findings persisted to `.research/signals/2026-07-12-<segment>.json`: `api-handlers` (2 findings), `mobile-native-kmp` (1), `ppt-web-ui` (2), `reality-server` (1 — became security issue #2249), `reality-web` (3). Running an additional routine slice this run would be redundant.
- Findings this cycle (dispatcher segments): 9 — all already converted into merged PRs (#2229 KMP NotificationRepo, #2254 reality-server SSO role-trust, and 7 others through the dispatcher pipeline)
- Next segment (routine cursor): `reality-server` (2026-06-11) or `ppt-web-ui` (2026-06-11) are the routine's oldest, but dispatcher advanced both today — routine will pick `api-core` (2026-06-15) or `reality-web` (2026-06-14) next
- Skipped: yes — reason above

## Screen-map status
- Total `docs/screens/` files: 150 (`ppt/`=103, `reality/`=47, `mobile/`=0 — not yet seeded)
- Drift signals this run: 0 (checked 48 merged PRs against ppt-web + reality-web route trigger sets)
- Orphan screens this run: 0
- Last `screen-map.yml` CI run on `main`: not checked (skipped — no drift signals to correlate)

## Backlog deltas
- **New:** [2] `refactor-churn-hotspots-api-server-auth-2026-07-12` — Churn hotspot cluster: api-server routes/auth.rs (runs_seen=3, +4 modifications) + auth_tests.rs (+3) + reality-server routes/sso.rs (+2). Score builds from repeated-churn (+1) + auth_tests hotspot (+1) + sso hotspot (+1, but consolidated: net score 2 after churn-hotspot cumulative re-emit on auth.rs contributes 0). Vector=refactor, confidence=medium. Kept below the +3 promotion bar because the recent PRs are legitimate security/correctness work, not stylistic thrash.
- **Bumped:** none — no existing item matched the new signals
- **Decayed:** none — backlog has no `open` items with `updated_at` >14d old (every prior item is `done`/`dropped`/`closed`)
- **Dropped:** none

## Plans promoted
- none — the only new backlog item (`refactor-churn-hotspots-api-server-auth-2026-07-12`) scores 2, below the promotion bar of 3 (no security fast-track applies to `refactor` vector). Adversarial pass N/A.

## Auto-fix
- `no candidate` — no signal matched the certainty bar (`confidence=high` + `score≥3` + allowlist). Allowlist categories not triggered this run: no `screen-map-drift`, no `screen-map-orphan`, no `fixme-in-merged-code`, no `dep-update-noise` lockfile-drift sub-shape, no `stalled-review` (no open PRs meet the >7-day criterion).

## Self-review findings
- open=13 (high=2, medium=8, low=3); new this run=0; acknowledged=19
- fp-null [recurrence=7] — Phase 6 archive-move produced a 198KB `assignments-archive.json` that could not be pushed via mcp push_files; fix: adopt the PR+API-merge path landed as workaround for PR #1163
- fp-null [recurrence=3] — Phase 2.6 buffer reported `claimable=54/open=54` but real claimable=0 (all terminal in archive); fix: (1) extend `gc1-reconcile.sh` to auto-close open action-list items whose exact `task_id` is terminal (any status incl. failed) in archive, (2) fix Phase 2.6 buffer-guard AND GC3 formulas to subtract archive task_ids, add T15 self-test guard

## Open questions
- none this run — the delivery pipeline drained cleanly; every follow-up issue opened by post-merge review this cycle was resolved same-day

## Quiet day?
- No — 48 PRs merged, 1 new backlog vector, 0 plans promoted, 0 auto-fix. Send Telegram digest (`PM_DIGEST_QUIET=0`).
