# Project state — 2026-07-18

<sub>Regenerated by routine Phase 1.6 (pm-scrum-master + pm-security)</sub>

## Executive summary

27 PRs merged since 2026-07-16T03:04Z with zero open blockers and a clean CI trail, but the implementer buffer is **fully starved** (`claimable=0/72`) — its one queued item is a thrice-failed, quarantined PR whose underlying GitHub issue is already closed. This run administratively marks that action `done`, ingests two **HIGH-severity** cross-agency invitation-escalation findings (reality-server, live in prod), the Phase 1.5 pagination bug, and reopens the two remaining mvp-phase `partial` coverage stories (84-1, 84-2) as fresh actionable buffer items — 7 new open action-list rows total, unblocking the buffer.

## Sprint progress

Sprint: **Epic 6, 7A, 8A & 10A** (Announcements, Documents, Notifications & OAuth).

- Coverage.json (deep scan 2026-07-15): **47/49 stories `done`**, 2 `partial` (both epic-84 mvp).
- Epic rollups in `sprint-status.yaml` are stale relative to story-level detail — epic-6 shows 3/6 despite all 6 marked done individually; epic-10b shown `in-progress` despite 7/7 done; epic-80 shown `partial` despite all 3 done. Low-priority reconciliation queued.

## Shipped since last run (27 PRs)

**Security / RLS / IDOR:** #2416 (gov_portal cross-tenant IDOR), #2421 (rag/migrate NOBYPASSRLS), #2378 & #2408 (`/sign` token hardening), #2419 (scheduler-RLS comment fix), #2420 (scheduler mutex-poison recovery).

**Mobile:** #2372 (login clears full query cache), #2383 (upload allow-list + MIME), #2410 (iOS HEIC/HEIF transcode), #2412 (AsyncStorage cache purge on session change).

**Reality-web / data integrity:** #2373 (`parseListingDetail` consolidation), #2379 (Decimal-as-string guards), #2409 (favorites DTO reconciliation), #2380 (candidate id IdSchema validation), #2414 (screen-map init IdSchema).

**Backend / data correctness:** #2375 (`/agencies/me` active-only), #2376 (portal_webhook_events dedup net), #2398 (Booking.com push sync state).

**i18n:** #2384, #2411 (create-schedule localization); #2395, #2415 (orphan namespace cleanup).

**UI/UX:** #2381 (category badge sync), #2382 (AC-5 price-alert toggle test coverage).

**DX/CI:** #2396 (@ppt/sitemap CJS default export), #2397 (deterministic sitemap.json), #2407 (src/data ↔ sitemap drift guard).

## What's next (top 5 — replaces sprint-only view)

1. **[HIGH]** Add agency-membership check to `create_invitation` on reality-server — hotfix-shaped, ready to claim (`security-reality-server-agency-invitation-escalation` plan promoted this run). Owner: pm-security / rust-backend.
2. **[HIGH]** Bind `accept_invitation` to invited email (blocks on #1 landing to avoid churn on the same file). Owner: pm-security.
3. **[HIGH]** Audit `reality_agency_invitations` / `reality_agency_members` for anomalous grants once the fix ships. Owner: pm-security / db-owner.
4. **[MEDIUM]** Fix pagination `total = rows.len()` bug across `reality-server` routes (reports.rs, imports.rs, agency_imports.rs). Owner: pm-tech-lead.
5. **[MEDIUM]** Wire ppt-web direct-to-S3 upload flow (story 84-1) to the already-shipped backend endpoint. Owner: pm-frontend.

## Blockers

- **Implementer buffer starvation** (`claimable=0/72`) — this run adds 7 open action-list items and 1 promoted plan; expect the next dispatcher run to be able to claim.

## Role focus today

- pm-scrum-master (always-on)
- pm-security (rotation slot 5)

### pm-security summary
Cross-agency invitation escalation confirmed live in production reality-server code today; recent RLS/IDOR fixes (#2416, #2421) and mobile session-cache fixes (#2412, #2372) narrowed the api-server / mobile attack surface meaningfully this sprint. Recommends the invitation fix be treated as a hotfix candidate (branch from `main`), paired with a production audit query and token rotation. Also flags `list_members` as a possible second IDOR to investigate.
