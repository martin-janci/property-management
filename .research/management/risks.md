# PPT Risks

_Generated: 2026-07-05T10:30:27Z · 52 items · 47 open_

| Prob | Impact | Owner | Status | Risk | Mitigation |
|---|---|---|---|---|---|
| high | high | pm-backend | open | Cross-tenant IDOR cluster in ai.rs equipment endpoints: update_equipment (ai.rs:1113), delete_equipment (ai.rs:1131), and updat... | Thread the principal's tenant_id into equipment_repo.update/delete/update_maintenance and add `AN... |
| high | high | pm-devops | open | PR #1426 merged despite breaking `dev` backend compile (issue #1437) - ALL backend CI gates now broken on `dev` until #1435/#14... | Land #1435 or #1436 immediately to restore green. Add `cargo check --workspace --tests` as a fail... |
| high | high | pm-qa | open | Issue #480 (JWT access token in WebSocket query-param access logs) is high-severity and open; story 8a-3 has a WS dependency — ... | Block 8a-3 promotion; strip token from log line and re-validate JWT on session before any WS stor... |
| high | high | pm-qa | open | Issue #481 (OAuth refresh-token revocation bypass) is high-severity and open, yet stories 10a-1/10a-3 target this sprint; mergi... | Block 10a-1/10a-3 promotion until #481 closed; escalate to tech lead. |
| high | high | pm-scrum-master | open | Story 7a-1 not promotable: POST /api/v1/documents/upload backend handler absent; mobile upload UI (PR #447) calls a missing rou... | Immediate backend task; critical-path blocker for Epic 7A completion. |
| high | high | pm-security | open | Epic 10a (OAuth) ships with revoked-token reuse (#481) and no MFA brute-force coverage (#487) still open | block 10a story promotion on these two closing |
| high | high | pm-security | open | Raw fetch() calls without Authorization headers across ai-chat, person-months, and news feature (create/edit/publish/archive/pi... | centralize all API calls through the shared authenticated fetch helper; add a lint rule banning b... |
| high | high | pm-security | open | update_schedule (report_schedule.rs) missing RBAC (#614) and missing tenant/org scope (#624): an authenticated user can mutate ... | Add RequireCapability extractor + thread principal tenant/org into the WHERE clause; cross-tenant... |
| high | medium | pm-backend | open | 10B handler stubs (10b-4/5/6/7) are mounted routes returning success with no business logic — callers get 200/204 for operation... | Return 501 Not Implemented with a documented timeline until real handlers ship, so callers fail l... |
| high | medium | pm-devops | open | Mobile release pipeline has no merged path: EAS Android/iOS build workflows exist only in draft PRs (#566 cluster) with broken ... | Land gap-85-2-android-ci-fix + gap-85-2-ios-ci-fix together; downgrade pins to @v4, add eas-cli d... |
| medium | high | pm-devops | open | security-test-gate.yml may be advisory only (not a required status check): PR #497 shipped a security IDOR fix with zero tests ... | Make the gate a required status check on dev branch protection; re-run against a synthetic test-l... |
| medium | high | pm-devops | open | `security-test-gate.yml` workflow file is present but enforcement-vs-advisory on `dev` branch protection remains unconfirmed si... | Verify required-status-check listing via `gh api repos/.../branches/dev/protection`. Promote to r... |
| high | medium | pm-frontend | open | Post-merge review opened follow-up issues #480-#487 covering test gaps + minor security/UX follow-ups on the heavy frontend del... | pm-scrum-master to slot #480-#487 into the sprint as a single test-hardening batch; do not promot... |
| high | medium | pm-backend | open | Airbnb at-least-once webhook (webhook.rs:1028) enqueues duplicate SYNC_EXTERNAL jobs on event bursts — redundant sync load and ... | Add an event_id dedup table (ON CONFLICT DO NOTHING) + already-queued-job check before CreateBack... |
| high | medium | pm-backend | open | Redis push-fanout queue (push_fanout.rs:621) is never drained (BLPOP deferred) — jobs enqueued to push_fanout_queue are silentl... | Implement the BLPOP drain before any production traffic relies on the fanout path; add a queue-de... |
| medium | high | pm-backend | open | sqlx 0.8→0.9 major bump (Dependabot PR #666) affects every workspace query; merging without a coordinated upgrade pass may brea... | Freeze PR #666 from auto-merge; backend owner audits the 0.9 changelog and runs cargo check --wor... |
| medium | high | pm-security | open | Booking.com OAuth/credential connect flow lacks secure replacement on re-connect (#1362, #1374) — old credentials can linger an... | Implement atomic credential swap + add OAuth handler/CSRF test coverage. |
| high | medium | pm-devops | open | Dispatcher meta-issue #1380: stale gap-scan buffer feeds no-op claims + Tier-2 escalation endpoint misconfigured — wastes imple... | pm-devops or dispatcher owner refreshes gap-scan buffer; verify Tier-2 endpoint config in dispatc... |
| high | medium | pm-scrum-master | open | 18 follow-up issues (#1360–#1377) from the post-merge review of 2026-06-14 merges remain untriaged; backlog grows faster than b... | pm-scrum-master triages the batch this run — assign owner or close as won't-fix; bind to action p... |
| medium | high | pm-qa | open | PR #497 inquiry IDOR fix shipped with zero regression tests; mark_inquiry_read_for_realtor does an ownership EXISTS check then ... | Add reality-server/tests/inquiry_idor_tests.rs immediately; require a test file on security-fix PRs. |
| medium | high | pm-backend | open | record_payment handler does a check-then-insert without serializable isolation or unique-constraint guard (#1361); concurrent r... | Wrap in serializable tx OR add (idempotency_key, payment_id) unique constraint; add concurrency r... |
| medium | high | pm-security | open | PR #725 (ai-maintenance IDOR fix) sits at verdict=changes (B1-session-IDOR, B2-sentiment-IDOR, B3-missing-test); the maintenanc... | pm-security addresses the three change requests within a day; reviewer re-approves and merges bef... |
| high | medium | pm-scrum-master | open | Action-list buffer at 16/72 claimable (target 36) risks the dispatcher idling or re-picking low-value churn items instead of re... | Seed the 6 gap-driven next_actions above directly into the backlog this cycle |
| high | medium | pm-scrum-master | open | App.tsx is the top churn hotspot; concurrent edits across #503, #500, and 80-3 mediation wiring risk triple-conflict rebases. | Sequence App.tsx PRs: merge 80-3 wiring first, then #503; pm-frontend coordinates. |
| high | medium | pm-frontend | open | 10 of 22 remaining partial/not-started stories are mobile (7a-2/7a-4, 8a-3 push, epic-82 SwiftUI); unreviewed gap-82 drafts let... | Force gap-82 drafts (#639/#641/#705) to non-draft review this window; assign pm-frontend as expli... |
| medium | high | pm-scrum-master | open | PR #1797 (backend-authz-ocr-and-rental-pii) has sat in draft 12 days — it touches authz and PII handling, both high-sensitivity... | Assign pm-security as explicit owner with a hard review deadline |
| medium | high | pm-security | open | PR #435 merged with deferred security findings (#438/#439): P1-05 SSRF, P0-12 cookie scope, P1-04 Debug-format audit-hash domai... | Assign pm-security sole owner of the residual #438/#439 items; treat remaining P0/P1 as pre-featu... |
| high | medium | pm-scrum-master | open | sprint-status.yaml is materially stale (10b-3/10b-4/10b-6 still ready-for-dev despite done evidence); risks duplicated work or ... | Write the sprint-status update as a tracked action (pm-scrum-master-sync-sprint-status-10b); conf... |
| high | medium | pm-scrum-master | open | sprint-status.yaml epic-level status rollups are stale (epic-10b shows in-progress despite 7/7 done; epic-10a gated on possibly... | Run a reconciliation pass syncing epics: block to development_status + coverage.json |
| medium | high | pm-scrum-master | open | Test-hardening issue #481 (OAuth refresh-token revocation bypass, high severity) is recorded open in sprint-status and gates 10... | pm-security verifies #481's actual GitHub state this cycle before trusting coverage.json's 'done'... |
| medium | high | pm-security | open | OAuth provider stories 10a-1/10a-2/10a-3 shipped with no introspection/refresh-rotation/PKCE security tests; a refactor could s... | Add the OAuth security test suite (revoked-token, family-reuse, PKCE S256) before any further OAu... |
| medium | high | pm-security | open | security-llm-doc-idor (cross-tenant write/read on AI listing data) sits unclaimed despite high-confidence, score-3 rating | assign owner this sprint, use existing plan file |
| high | medium | pm-security | open | Test-hardening batch gate rule is being violated in practice (8a-2/8a-3 done while #480/#484 open) — undermines the whole gatin... | formal close/defer decision + audit of any other stories promoted past open gates |
| medium | high | pm-tech-lead | open | Three route monoliths on hot paths this run: ai.rs (3142 lines), platform_admin.rs (2762), announcements.rs (2722). ai.rs alrea... | Add ai.rs/platform_admin.rs/announcements.rs to the module-split backlog; add a CI grep/clippy li... |
| medium | medium | pm-backend | open | Epic 81 frontend calls backend report-schedule endpoints (/schedules/{id}/pause\|resume, /executions) that do not exist — 404 i... | Implement the missing endpoints or feature-flag the frontend calls until they exist; Epic 81 sche... |
| medium | medium | pm-data | open | FaultStatusCount / FaultByStatusTable surfaced by the new Support Data admin page (#635) defines a fault-by-status metric for s... | Factor the fault-status bucket + window definition into one shared query/helper reused by the sup... |
| medium | medium | pm-data | open | The Support Data page (#635) lets support/admin staff read cross-tenant user diagnostics (memberships, sessions with IP, activi... | Emit a support_data_viewed / support_sessions_revoked audit event (admin_user_id + target tenant/... |
| medium | medium | pm-devops | open | App.tsx router-file churn + 6 concurrent dispatcher drafts (#563-#568) risk repeated triple-conflict rebases, stalling the merg... | Serialize App.tsx-touching PRs via a merge queue / auto-rebase ordering so concurrent route-wirin... |
| medium | medium | pm-devops | open | `eas-build-android.yml` and `eas-build-ios.yml` now exist in `.github/workflows/` (cleared since 2026-05-27 from draft PRs), bu... | Trigger a no-op push or workflow_dispatch run; confirm both Android and iOS jobs reach the build ... |
| medium | medium | pm-devops | open | Pre-push fmt/clippy gate (#1431) merged but is local hook only - does not enforce on CI side and does not run cargo check. Cann... | Mirror the gate as a fast CI status check (`cargo fmt --check && cargo clippy -q && cargo check`)... |
| medium | medium | pm-frontend | open | Epic 6 announcement web UI (viewing/ack 6-2, comments 6-3, pin 6-4) is split across three draft PRs (#474/#475/#479) that have ... | Sequence the three drafts (#474 viewing → #475 comments → #479 pin) so they merge in dependency o... |
| medium | medium | pm-backend | open | e-signature webhook status writes have no idempotency guard; a provider re-delivering a completed/voided event can overwrite a ... | Skip the workflow update when current_status is terminal; coordinate with PR #719 (gap-84-2 signe... |
| medium | medium | pm-qa | open | Cron validator drift (#1368) could silently reintroduce regression #616 (Epic 81 promotion blocker) — current tests don't pin t... | Pin a fixture-based test for the cron validator; gate Epic 81 promotion on it. |
| medium | medium | pm-scrum-master | open | 7a-2-folder-organization has zero action-list coverage despite being CI-red since PR #1316 — epic-7a completion is silently sta... | Top-ranked in next_actions this cycle; assign pm-backend now |
| medium | medium | pm-security | open | P1-04 residual (PR #435): internal type internals may reach audit-trail log lines via Debug ({:?}) formatting. | Replace {:?} with Display/structured fields on internal types in audit records. |
| low | medium | pm-security | open | quick-xml XXE mitigation is a dependency pin, not a code-level test — a future alternate parser or vendored XML lib could reint... | add the runtime regression test noted above |
| low | medium | pm-tech-lead | open | OAuth provider (10A) backend complete; admin client-management UI + user-grants UI now shipped (PRs #468/#469/#471). Residual g... | Add OAuth integration test suite (gap-10a-1-oauth-tests); UI delivery risk now closed. |
| medium | high | pm-security | resolved | Cookie Path breaking change (#617) from PR #565 session-cookie scope hardening: if mis-scoped it either silently logs users out... | Verify cookie set/clear across / and the API prefix and the SSO callback flow before merging the ... |
| low | high | pm-backend | resolved | Two outbound-request sinks (signatures.rs:628 signed_url fetch, integrations.rs:2743 webhook-test POST) issued requests to unva... | validate_external_url extracted to shared module and applied at the outbound sinks (PR #450). |
| low | high | pm-scrum-master | resolved | Epic 6/8A blocked on un-built Epic 2B notification infra; deferred dispatch + WS sync (8A.2/8A.3) | Epic 2B notification pipeline (PR #463) + WebSocket realtime sync (PR #472) merged — the blocking... |
| low | high | pm-security | resolved | Voice device endpoints permitted cross-tenant device access (IDOR) — a principal could address another org's voice device by id. | Tenant scoping added to voice device lookups (PR #461). |
| low | medium | pm-frontend | resolved | ProtectedRoute.tsx:117 role gate was fail-open: when user.role was falsy the role check was skipped and access granted — would ... | Now denies on missing role; user.role populated in AuthContext (PR #459). |
