# Risks register

_Generated: 2026-06-25T03:30:00Z_

Total: **52** · open: **46** · resolved: **6**

| ID | Status | P | I | Owner | Risk | Mitigation |
|---|---|---|---|---|---|---|
| `pm-backend-ai-equipment-cross-tenant-idor` | open | high | high | pm-backend | Cross-tenant IDOR cluster in ai.rs equipment endpoints: update_equipment (ai.rs:1113), delete_equipment (ai.rs:1131), and update_maintenance (ai.rs:1192) all bind `_principal: RequestPrincipal` and di | Thread the principal's tenant_id into equipment_repo.update/delete/update_maintenance and add `AND tenant_id = $tenant` to the WHERE clauses (or use t |
| `pm-qa-jwt-token-in-ws-logs-open` | open | high | high | pm-qa | Issue #480 (JWT access token in WebSocket query-param access logs) is high-severity and open; story 8a-3 has a WS dependency — shipping it leaks tokens via logs. | Block 8a-3 promotion; strip token from log line and re-validate JWT on session before any WS story ships. |
| `pm-qa-oauth-revoked-token-bypass-open` | open | high | high | pm-qa | Issue #481 (OAuth refresh-token revocation bypass) is high-severity and open, yet stories 10a-1/10a-3 target this sprint; merging OAuth stories ships a broken security contract. | Block 10a-1/10a-3 promotion until #481 closed; escalate to tech lead. |
| `pm-scrum-master-documents-upload-endpoint-missing` | open | high | high | pm-scrum-master | Story 7a-1 not promotable: POST /api/v1/documents/upload backend handler absent; mobile upload UI (PR #447) calls a missing route in production. | Immediate backend task; critical-path blocker for Epic 7A completion. |
| `pm-scrum-master-test-hardening-batch-480-jwt-in-ws-logs-and-48` | open | high | high | pm-scrum-master | Test-hardening batch #480 (JWT in WS logs) and #481 (OAuth revoked-token reuse / RFC 9700 break) open since 2026-05-25 — 31 days — without resolution; gate 8a-3, 10a-1, 10a-3 | Escalate to pm-security as today's rotating role; treat as P0 before next sprint |
| `pm-security-oauth-refresh-token-revocation-bypass-issue-481` | open | high | high | pm-security | OAuth refresh-token revocation bypass (issue #481) — revoked_at IS NULL guard removed from oauth refresh path; replayed revoked refresh tokens accepted, violating RFC 9700 and enabling session hijacki | Restore predicate in oauth.rs refresh handler; gate stories 10a-1/10a-3 on fix; add sqlx::test regression |
| `pm-security-update-schedule-cross-tenant-idor` | open | high | high | pm-security | update_schedule (report_schedule.rs) missing RBAC (#614) and missing tenant/org scope (#624): an authenticated user can mutate another tenant's report schedule and/or mutate schedules without the requ | Add RequireCapability extractor + thread principal tenant/org into the WHERE clause; cross-tenant regression test; block Epic 81 promotion until both  |
| `pm-devops-security-test-gate-advisory-only` | open | medium | high | pm-devops | security-test-gate.yml may be advisory only (not a required status check): PR #497 shipped a security IDOR fix with zero tests despite the gate existing, suggesting it does not block merges. | Make the gate a required status check on dev branch protection; re-run against a synthetic test-less security PR to confirm it blocks. |
| `pm-devops-security-test-gate-still-advisory` | open | medium | high | pm-devops | `security-test-gate.yml` workflow file is present but enforcement-vs-advisory on `dev` branch protection remains unconfirmed since 2026-05-27. If still advisory, security-labelled PRs can merge with n | Verify required-status-check listing via `gh api repos/.../branches/dev/protection`. Promote to required check if currently advisory. |
| `pm-integration-sqlx-0-9-major-bump` | open | medium | high | pm-backend | sqlx 0.8→0.9 major bump (Dependabot PR #666) affects every workspace query; merging without a coordinated upgrade pass may break compile-time query! checks or the migrate API and block all backend CI. | Freeze PR #666 from auto-merge; backend owner audits the 0.9 changelog and runs cargo check --workspace on a branch before landing. |
| `pm-qa-booking-oauth-no-secure-replacement` | open | medium | high | pm-security | Booking.com OAuth/credential connect flow lacks secure replacement on re-connect (#1362, #1374) — old credentials can linger and be used post-rotation. | Implement atomic credential swap + add OAuth handler/CSRF test coverage. |
| `pm-qa-inquiry-idor-no-regression-test` | open | medium | high | pm-qa | PR #497 inquiry IDOR fix shipped with zero regression tests; mark_inquiry_read_for_realtor does an ownership EXISTS check then a separate UPDATE — a future refactor could silently reintroduce the IDOR | Add reality-server/tests/inquiry_idor_tests.rs immediately; require a test file on security-fix PRs. |
| `pm-qa-record-payment-double-write` | open | medium | high | pm-backend | record_payment handler does a check-then-insert without serializable isolation or unique-constraint guard (#1361); concurrent retries can double-write a payment. | Wrap in serializable tx OR add (idempotency_key, payment_id) unique constraint; add concurrency regression test. |
| `pm-scrum-master-30-open-follow-up-issues-from-merged-prs-1663` | open | medium | high | pm-scrum-master | 30+ open follow-up issues from merged PRs (#1663–#1828) including attachment IDOR (#1791), unauthenticated OCR (#1772), guest PII reads (#1766) — shipped-but-unpatched security regressions if not clos | pm-security to triage and rank by severity; security-class items must have fix PRs merged before next dev->main release |
| `pm-scrum-master-725-ai-idor-stalled` | open | medium | high | pm-security | PR #725 (ai-maintenance IDOR fix) sits at verdict=changes (B1-session-IDOR, B2-sentiment-IDOR, B3-missing-test); the maintenance/chat-session/sentiment IDOR vector stays live until merged. | pm-security addresses the three change requests within a day; reviewer re-approves and merges before next run. |
| `pm-scrum-master-pr-435-deferred-security` | open | medium | high | pm-security | PR #435 merged with deferred security findings (#438/#439): P1-05 SSRF, P0-12 cookie scope, P1-04 Debug-format audit-hash domain. SSRF (#450) and ProtectedRoute fail-open (#459) have since landed; rem | Assign pm-security sole owner of the residual #438/#439 items; treat remaining P0/P1 as pre-feature-work. SSRF and ProtectedRoute legs now resolved. |
| `pm-security-guest-id-document-pii-name-dob-passport-from-ocr` | open | medium | high | pm-security | Guest ID-document PII (name/DOB/passport) from OCR extraction in rentals path may surface in tracing spans — issue #1783, no PII audit-logging confirmed for extract_guest_id_document handler | Audit tracing::debug calls in id_ocr.rs and rentals.rs; redact PII fields; add append-only audit log entry per extraction per GDPR |
| `pm-security-jwt-access-token-exposed-in-websocket-query-param` | open | medium | high | pm-security | JWT access token exposed in WebSocket query param and written to access logs (issue #480, severity high) — token harvestable from log aggregation (CloudWatch/Loki/Datadog) | Move token to Sec-WebSocket-Protocol or first WS message; scrub query-param from TraceLayer; gate 8a-3 on closure |
| `pm-security-oauth-10a-untested-security-contract` | open | medium | high | pm-security | OAuth provider stories 10a-1/10a-2/10a-3 shipped with no introspection/refresh-rotation/PKCE security tests; a refactor could silently reintroduce revoked-token acceptance or replay. | Add the OAuth security test suite (revoked-token, family-reuse, PKCE S256) before any further OAuth-provider change merges. |
| `pm-security-third-jwtservice-token-verifier-copy-issue-1782` | open | medium | high | pm-security | Third JwtService/token-verifier copy (issue #1782) not migrated after #1744 unified the verification path — divergent verifier may use different validation parameters allowing forged or mistyped token | Grep DecodingKey::from_secret outside auth.rs and JwtService; consolidate before any Epic 10A story ships |
| `pm-tech-lead-ai-platform-announcements-route-monoliths` | open | medium | high | pm-tech-lead | Three route monoliths on hot paths this run: ai.rs (3142 lines), platform_admin.rs (2762), announcements.rs (2722). ai.rs already harbors the cross-tenant IDOR cluster above — exactly the failure mode | Add ai.rs/platform_admin.rs/announcements.rs to the module-split backlog; add a CI grep/clippy lint that flags handlers binding `_principal` (discarde |
| `pm-backend-10b-silent-stub-handlers` | open | high | medium | pm-backend | 10B handler stubs (10b-4/5/6/7) are mounted routes returning success with no business logic — callers get 200/204 for operations that never happened (silent data loss / incorrect UI state) | Return 501 Not Implemented with a documented timeline until real handlers ship, so callers fail loudly |
| `pm-devops-mobile-eas-no-merged-pipeline` | open | high | medium | pm-devops | Mobile release pipeline has no merged path: EAS Android/iOS build workflows exist only in draft PRs (#566 cluster) with broken @v6 action pins (checkout/setup-node/pnpm-action-setup @v6 do not exist); | Land gap-85-2-android-ci-fix + gap-85-2-ios-ci-fix together; downgrade pins to @v4, add eas-cli devDependency + missing Android npm scripts; verify gr |
| `pm-frontend-followup-test-gaps-480-487` | open | high | medium | pm-frontend | Post-merge review opened follow-up issues #480-#487 covering test gaps + minor security/UX follow-ups on the heavy frontend delivery (messaging realtime, document share-flow, OAuth UI, MFA). If these  | pm-scrum-master to slot #480-#487 into the sprint as a single test-hardening batch; do not promote dependent stories to done until their follow-up iss |
| `pm-integration-airbnb-webhook-dup-jobs` | open | high | medium | pm-backend | Airbnb at-least-once webhook (webhook.rs:1028) enqueues duplicate SYNC_EXTERNAL jobs on event bursts — redundant sync load and possible reservation-state races. | Add an event_id dedup table (ON CONFLICT DO NOTHING) + already-queued-job check before CreateBackgroundJob; mirrors backlog item bug-webhook-airbnb-du |
| `pm-integration-push-fanout-silent-drop` | open | high | medium | pm-backend | Redis push-fanout queue (push_fanout.rs:621) is never drained (BLPOP deferred) — jobs enqueued to push_fanout_queue are silently dropped while Epic 8A notification stories are marked done. | Implement the BLPOP drain before any production traffic relies on the fanout path; add a queue-depth monitor. |
| `pm-qa-dispatcher-stale-gap-scan` | open | high | medium | pm-devops | Dispatcher meta-issue #1380: stale gap-scan buffer feeds no-op claims + Tier-2 escalation endpoint misconfigured — wastes implementer cycles claiming gap stories already shipped. | pm-devops or dispatcher owner refreshes gap-scan buffer; verify Tier-2 endpoint config in dispatcher settings. |
| `pm-qa-followup-issue-flood-2026-06-14` | open | high | medium | pm-scrum-master | 18 follow-up issues (#1360–#1377) from the post-merge review of 2026-06-14 merges remain untriaged; backlog grows faster than burn-down without owner assignment. | pm-scrum-master triages the batch this run — assign owner or close as won't-fix; bind to action pm-qa-triage-followup-issues-2026-06-14. |
| `pm-scrum-master-app-tsx-router-merge-conflict` | open | high | medium | pm-scrum-master | App.tsx is the top churn hotspot; concurrent edits across #503, #500, and 80-3 mediation wiring risk triple-conflict rebases. | Sequence App.tsx PRs: merge 80-3 wiring first, then #503; pm-frontend coordinates. |
| `pm-scrum-master-mobile-coverage-lag` | open | high | medium | pm-frontend | 10 of 22 remaining partial/not-started stories are mobile (7a-2/7a-4, 8a-3 push, epic-82 SwiftUI); unreviewed gap-82 drafts let the mobile slice fall further behind. | Force gap-82 drafts (#639/#641/#705) to non-draft review this window; assign pm-frontend as explicit mobile-lag owner. |
| `pm-scrum-master-new-acc-accounting-workstream-bootstrapped-with` | open | high | medium | pm-scrum-master | New ACC (Accounting) workstream bootstrapped with 6+ draft PRs without sprint plan, story estimates, or owner assignments — context collision and resource fragmentation risk | Conduct ACC sprint-0 scoping; assign story owners from ACC spec before any draft PRs merge |
| `pm-scrum-master-sprint-status-stale` | open | high | medium | pm-scrum-master | sprint-status.yaml is materially stale (10b-3/10b-4/10b-6 still ready-for-dev despite done evidence); risks duplicated work or mis-reporting progress. | Write the sprint-status update as a tracked action (pm-scrum-master-sync-sprint-status-10b); confirm sync next run. |
| `pm-scrum-master-sprint-status-yaml-significantly-out-of-sync-with` | open | high | medium | pm-scrum-master | Sprint-status.yaml significantly out of sync with coverage.json (multiple done stories labeled ready-for-dev; epic-10a labeled in-progress when all 3 stories done) — produces false blockers in automat | Prioritize sprint-status reconciliation as first next_action; establish post-merge hook to sync coverage.json status back into sprint-status.yaml |
| `pm-security-unauthenticated-meter-ocr-endpoints-api-v1-ai-oc` | open | high | medium | pm-security | Unauthenticated meter-OCR endpoints (/api/v1/ai/ocr/meter-reading, /api/v1/ai/ocr/correction) — confirmed in backend/servers/api-server/src/routes/ai/ocr.rs; handlers accept multipart/JSON with no aut | Add RequestPrincipal extractor to both handlers before next staging deploy |
| `pm-backend-81-missing-endpoints` | open | medium | medium | pm-backend | Epic 81 frontend calls backend report-schedule endpoints (/schedules/{id}/pause/resume, /executions) that do not exist — 404 in production | Implement the missing endpoints or feature-flag the frontend calls until they exist; Epic 81 schedule/execution verify PRs (#488/#489) in flight. |
| `pm-data-fault-status-metric-divergence` | open | medium | medium | pm-data | FaultStatusCount / FaultByStatusTable surfaced by the new Support Data admin page (#635) defines a fault-by-status metric for support staff. The same fault counting also exists in owner_analytics.rs a | Factor the fault-status bucket + window definition into one shared query/helper reused by the support-data, owner-analytics, and portfolio routes; add |
| `pm-data-support-data-pii-no-usage-tracking` | open | medium | medium | pm-data | The Support Data page (#635) lets support/admin staff read cross-tenant user diagnostics (memberships, sessions with IP, activity log) gated only by the audit_read capability. There is no analytics/au | Emit a support_data_viewed / support_sessions_revoked audit event (admin_user_id + target tenant/user) on each support-data read and session-revoke; b |
| `pm-devops-app-tsx-churn-concurrent-drafts` | open | medium | medium | pm-devops | App.tsx router-file churn + 6 concurrent dispatcher drafts (#563-#568) risk repeated triple-conflict rebases, stalling the merge queue. | Serialize App.tsx-touching PRs via a merge queue / auto-rebase ordering so concurrent route-wiring drafts do not collide. |
| `pm-devops-mobile-eas-pipeline-unverified` | open | medium | medium | pm-devops | `eas-build-android.yml` and `eas-build-ios.yml` now exist in `.github/workflows/` (cleared since 2026-05-27 from draft PRs), but green status on `dev` is unverified. If action pins / eas-cli installat | Trigger a no-op push or workflow_dispatch run; confirm both Android and iOS jobs reach the build step. Audit required secrets (EXPO_TOKEN, EXPO_PROJEC |
| `pm-devops-pre-push-gate-local-only` | open | medium | medium | pm-devops | Pre-push fmt/clippy gate (#1431) merged but is local hook only - does not enforce on CI side and does not run cargo check. Cannot prevent a contributor with hooks disabled from re-landing a #1426-clas | Mirror the gate as a fast CI status check (`cargo fmt --check && cargo clippy -q && cargo check`) gated on `dev`-push so local-disabled hooks still ge |
| `pm-frontend-epic6-announcement-web-ui-in-draft` | open | medium | medium | pm-frontend | Epic 6 announcement web UI (viewing/ack 6-2, comments 6-3, pin 6-4) is split across three draft PRs (#474/#475/#479) that have not merged. Backend + notification pipeline (PR #463) and WebSocket sync  | Sequence the three drafts (#474 viewing → #475 comments → #479 pin) so they merge in dependency order without conflicting on the shared AnnouncementsP |
| `pm-integration-esignature-webhook-no-idempotency` | open | medium | medium | pm-backend | e-signature webhook status writes have no idempotency guard; a provider re-delivering a completed/voided event can overwrite a terminal state. | Skip the workflow update when current_status is terminal; coordinate with PR #719 (gap-84-2 signerParties) on the same surface. |
| `pm-qa-cron-validator-drift-regression` | open | medium | medium | pm-qa | Cron validator drift (#1368) could silently reintroduce regression #616 (Epic 81 promotion blocker) — current tests don't pin the validator surface. | Pin a fixture-based test for the cron validator; gate Epic 81 promotion on it. |
| `pm-scrum-master-epic-6-announcement-screen-ppt-announcements-sti` | open | medium | medium | pm-scrum-master | Epic 6 announcement screen (ppt/announcements) still buildStatus=in-progress blocking 6-2/6-3/6-4 promotion; also redesignStatus=in-progress, dual dependency on impl completion and redesign sign-off | Decouple redesign from story completion: ship implementation at current fidelity and flip buildStatus=shipped, track redesign separately |
| `pm-security-audit-hash-debug-format-p1-04` | open | medium | medium | pm-security | P1-04 residual (PR #435): internal type internals may reach audit-trail log lines via Debug ({:?}) formatting. | Replace {:?} with Display/structured fields on internal types in audit records. |
| `pm-tech-lead-oauth-10a-no-token-design` | open | low | medium | pm-tech-lead | OAuth provider (10A) backend complete; admin client-management UI + user-grants UI now shipped (PRs #468/#469/#471). Residual gap is end-to-end integration/security test coverage (PKCE flow, refresh r | Add OAuth integration test suite (gap-10a-1-oauth-tests); UI delivery risk now closed. |
| `pm-devops-dev-ci-discipline-1437` | resolved | high | high | pm-devops | PR #1426 merged despite breaking `dev` backend compile (issue #1437) - ALL backend CI gates now broken on `dev` until #1435/#1436 lands. Indicates the PR-merge gate didn't run (or was bypassed) and th | Land #1435 or #1436 immediately to restore green. Add `cargo check --workspace --tests` as a fail-fast `dev`-push smoke (backend.yml run on push, not  |
| `pm-security-cookie-path-breaking-565` | resolved | medium | high | pm-security | Cookie Path breaking change (#617) from PR #565 session-cookie scope hardening: if mis-scoped it either silently logs users out or leaks the session cookie to unintended paths / breaks the SSO auth-ca | Verify cookie set/clear across / and the API prefix and the SSO callback flow before merging the change to dev; document the migration. |
| `pm-backend-webhook-signature-ssrf` | resolved | low | high | pm-backend | Two outbound-request sinks (signatures.rs:628 signed_url fetch, integrations.rs:2743 webhook-test POST) issued requests to unvalidated user/provider URLs — SSRF to 169.254.169.254 / internal services. | validate_external_url extracted to shared module and applied at the outbound sinks (PR #450). |
| `pm-scrum-master-epic-2b-foundation-missing` | resolved | low | high | pm-scrum-master | Epic 6/8A blocked on un-built Epic 2B notification infra; deferred dispatch + WS sync (8A.2/8A.3) | Epic 2B notification pipeline (PR #463) + WebSocket realtime sync (PR #472) merged — the blocking infra is now live; 6-2/6-3/6-4 web UI and 8a-3 WS ha |
| `pm-security-voice-device-idor` | resolved | low | high | pm-security | Voice device endpoints permitted cross-tenant device access (IDOR) — a principal could address another org's voice device by id. | Tenant scoping added to voice device lookups (PR #461). |
| `pm-frontend-protectedroute-fail-open` | resolved | low | medium | pm-frontend | ProtectedRoute.tsx:117 role gate was fail-open: when user.role was falsy the role check was skipped and access granted — would silently no-op the first role-gated route. | Now denies on missing role; user.role populated in AuthContext (PR #459). |