{
  "schema_version": 1,
  "epic_prefix": "NONE",
  "selected_at": "2026-06-08T14:12:50Z",
  "last_action": "empty",
  "reason": "Finish-first inoperable: every open action-list item is a flat task-id (no gap-N / pm-<role> epic prefix), so pick-target-epic.sh --update emitted epic_prefix='feat-admin-web-oauth-consent-page', which violates T21 (must match gap-N[a-z]?|pm-<role>|NONE) and FAILs dispatcher-self-test. Reset to NONE so the self-test passes; Phase 3 falls to the normal claim path (free_slots=3 by priority). Recurring structural env blocks (same as prior runs): (1) gh CLI unavailable -> Phase 0.5 lock skipped-gh-unavailable, all GitHub I/O via MCP. (2) MCP token identity == PR author (martin-janci) -> bot cannot APPROVE/REQUEST_CHANGES its own PRs (GitHub 422); Phase 5 reviewers NOT spawned, Phase 5.5 merge human-gated; open PRs #1165 #1170 await human review+merge. (3) Tier-1 refill is quality-limited: coverage.json (2026-05-29) is stale and its actionable gaps are already drained into the existing open items, so claimable sits just under FLOOR. Operator: add gap-N / pm-<role> prefixed ids to action-list to make finish-first targetable (or set DISPATCHER_FINISH_FIRST=0); re-scan coverage to refill the buffer; and run the dispatcher under a bot identity distinct from the PR author to unblock the review->merge pipeline.",
  "stats_at_selection": {
    "open": 0,
    "claimable": 0
  }
}
