{
  "schema_version": 1,
  "epic_prefix": "NONE",
  "selected_at": "2026-06-08T08:12:20Z",
  "last_action": "empty",
  "reason": "Finish-first inoperable this run: every open action-list item is a flat task-id (no gap-N/pm-<role> epic prefix), so pick-target-epic.sh --update wrote epic_prefix='test-oauth-authorization-server-integration', which violates the T21 objective schema (must be gap-N[a-z]?|pm-<role>|NONE) and FAILs dispatcher-self-test. Reset to NONE so the self-test passes; the high-priority closest-to-done task the picker identified (test-oauth-authorization-server-integration) is still claimed this run (1 claim, honoring depth-first intent). Structural env blocks persist from prior runs: (1) gh CLI unavailable -> Phase 0.5 lock skipped-gh-unavailable, all GitHub I/O via MCP. (2) MCP token identity == PR author (martin-janci) -> bot cannot approve/request-changes its own PRs (GitHub 422), so Phase 5 review -> Phase 5.5 merge pipeline is HUMAN-GATED; reviewers not spawned this run; PRs #1165 #1167 await human review+merge. (3) MCP push_files cannot carry the ~300KB assignments-archive.json as a literal param; no archive move needed this run (no merges/fails). Operator: either add epic-prefixed (gap-N / pm-<role>) ids to action-list so finish-first can target an epic, or set DISPATCHER_FINISH_FIRST=0.",
  "stats_at_selection": {
    "open": 0,
    "claimable": 0
  }
}
