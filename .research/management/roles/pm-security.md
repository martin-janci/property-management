# pm-security — 2026-07-16

_Rotating role focus for the daily PPT delivery routine. Generated by Phase 1.6._

## Summary

The Phase-1.5 finding is confirmed and worse than reported: `POST /api/v1/agencies/{id}/invitations` (`backend/servers/reality-server/src/routes/agencies.rs:373`) has no `check_agency_membership()` call, and the repo-layer `create_invitation` (`backend/crates/db/src/repositories/reality_portal/agencies.rs:223-248`) does a bare INSERT with zero authz or role validation — any authenticated portal user can mint a live 7-day invitation token (any email, any role) for any agency, a release-blocking cross-tenant IDOR. This sits inside a 3-day run of 4 near-identical IDOR fixes (#2289, #2316, #2335, #2356), indicating the missing-guard pattern is systemic, not a one-off, and two related authz threads (#2357 untested role-gate, #2361 incomplete cache eviction on org switch) remain open.

## Next actions

| Priority | Owner | Action |
|---|---|---|
| high | rust-backend | Add `check_agency_membership(&mut conn, agency_id, principal.user_id)` to `create_invitation` in `agencies.rs:373`, mirroring `update_agency` (line 268) / `update_branding` (line 322). Add deny-path regression test in `agencies_authz_tests.rs`. |
| high | rust-backend | Validate `CreateAgencyInvitation.role` server-side against an allow-list enum before insert (repo currently COALESCEs to `'realtor'` with no bound check on supplied value). |
| high | rust-backend | Close #2357 by adding the missing deny-path regression test for `GET /ai/chat/escalated` role gate before treating PR #2356 as fully resolved. |
| medium | product-owner | Audit `list_members` (`agencies.rs` ~345) — no `RequestPrincipal`/auth extractor at all; confirm whether unauthenticated exposure of agency member list (incl. emails) is intended. |
| medium | rust-backend | Introduce a shared Axum extractor (e.g. `AgencyMember`) that enforces membership at the type level for all agency-scoped mutating routes, replacing per-handler manual `check_agency_membership` calls. |

## Risks (added this run)

- **pm-security-agency-invite-idor-2026-07-16** (P=high, I=high) — Cross-tenant invitation-minting IDOR at `POST /api/v1/agencies/{id}/invitations`.
- **pm-security-idor-pattern-systemic-2026-07-16** (P=high, I=high) — Recurring missing-guard pattern across sibling routes (5 in 72h). Consider type-level extractor + PR checklist.
- **pm-security-agency-invite-role-priv-esc-2026-07-16** (P=medium, I=high) — Invitation `role` accepted without server-side enum validation; potential privilege escalation on acceptance.

## Open questions

- Is `list_members` intentionally unauthenticated, or missing an auth extractor by omission like `create_invitation` was?
- Is `CreateAgencyInvitation.role` validated anywhere upstream (e.g. request DTO deserialization) against a fixed enum, or does the DB accept any string?
- How long has `POST /api/v1/agencies/{id}/invitations` been live/mounted without the membership check — what is the exposure window for incident/backfill review?
- Should the `agencies.rs:373` fix go out as a `hotfix/` branch per `git-workflow.md` given it is a live cross-tenant vulnerability, or land via the normal `dev` -> `main` release train?

## Decisions needed

- **Hotfix vs release train** for the `create_invitation` IDOR — owner: release-manager / pm-security.
- **`list_members` membership gate** — owner: product-owner (PII exposure vs public directory).
