# pm-qa — 2026-08-11

**Sprint window observed:** 2026-08-07 → 2026-08-11 (5 days, 30 merged PRs).

## Test-strategy read

The 30-PR wave is dominated by code-review fixes and refactors surfaced by the daily rotating expert review. Test coverage discipline is holding:

- **Every `fix(...)` PR in the window ships inline regression tests.** #2730, #2728, #2707, #2706, #2708 each add a `#[test]` in the same source file naming the exact behaviour changed (quiet-gate fail-closed, voice fabricated-success, response-body cap, RAG partial batch, non-finite compare). No `hotfix-no-test` signals fired after the test-added heuristic ran.
- **Announcement-fanout metric PR (#2723)** landed observability + sqlx integration tests for delivered/read/ack counters — the pattern the pm-data rotation had asked for.
- **Rentals mappers regression (#2735)** added the API↔UI mapper test suite — good coverage for a route the offline-queue work churns.

## Gaps observed

1. **`code-review-api-core-workflow-cond-parse-failopen` is quarantined at fix_round=3** (PR #2684). CI red on test-shard 1-4 after clippy fixes. The task hit the retry ceiling — needs manual triage or a fresh implementer approach.
2. **Screen-map drift (#2735 ppt, #2718 reality)** — route changes landed without touching the corresponding `docs/screens/<product>/*.md`. #2718 is likely a false-positive (API route under `app/api/…/route.test.ts`), but #2735 rentals is a real drift; screen-map for rentals hasn't been reconciled with the mapper split.
3. **Stalled review — accounting MVP-loop trio** (#2555, #2558, #2559) sat 12 days with zero reviewer engagement, unchanged since the previous state. Still the top delivery-flow blocker after auto-fix follow-ups. QA cannot mark 84-1/84-2 further along without these landing.

## Recommendation

Focus this week's QA/test capacity on:
- Draining the mobile-native-kmp backlog (3 open items — all `bug`/`test-gap` vector; blocking cloud implementer scale per buffer-low signal). Especially `code-review-mobile-native-kmp-cancellation-swallowed` — the `catch(e: Exception)` swallowing `CancellationException` pattern needs coroutine-scope tests.
- Unquarantine or re-plan `workflow-cond-parse-failopen` — its 3 CI failures on the fmt/clippy path are worth a human-eye before another auto-fix respawn.
