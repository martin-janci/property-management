# Dispatcher self-review — 2026-07-12T06-16-49Z

Commit: f093812 — chore(research): dispatcher 2026-07-12 06:10 — 1 claimed, 0 reviewed, 0 merge-attempts, 0 merged-now, 1 failed, 1 active

## Run shape
Claimed=1 Reviewed=0 Merged-attempts=0 Merged-now=0
Failed=1 Active=1 Buffer=24/24 dep-blocked=0
Hang alerts: WARN=0 ALERT=0  Self-test: pass (T28 advisory WARN: 1 open action-list item terminal-in-archive)

## New findings this run
- dev-truth-blind-to-non-id-prefixed-merge [medium] — ghost retry: out-of-band fix landed on dev under a non-`<id>:` squash subject, invisible to dev-reconcile.sh:73 + retry-remint.sh:167, so Tier-1c re-minted a well-formed retry that burned a full implementer claim confirming already-done work (also hit prior run on a different task); fix: broaden dev-truth signal beyond `<id>:` prefix (fuzzy title/stem match) OR add a pre-claim verify-first probe for retry_of rows.
- tier1d-devreview-generator-stalls-no-artifacts [medium] — the sole non-empty refill tier fired but stalled on its nested Rust sub-subagent and returned zero artifacts before Phase 6; fix: bound the generator with a deadline + partial-return, treat a stall as avail=0 and escalate.
- reconciled-done-annotation-contradicts-archive [low] — action-list row carried `[RECONCILED-DONE: … already merged]` while the archive showed the same task `failed`; fix: audit the annotation emitter + self-test that the note can't contradict archive terminal status.

## Recurring findings (≥2 runs, still open)
- mcp-push-cannot-inline-large-archive [recurrence=7, high] — archive 227KB > 64KiB MCP inline ceiling; landed via direct git push this run.
- buffer-starved-below-floor-no-refill-path [recurrence=5, medium] — GC3 FAIL claimable 24<floor 36; Tier-1b/1c avail=0, Tier-1d fired but folded nothing.

## Anomalies this run (single occurrence, watching)
- git-push-blocked-by-proxy (acknowledged, rec 6): CONTRADICTED this run — direct `git push` to dev WORKED, only the MCP inline size-guard tripped. Left the finding untouched (did not bump recurrence — the opposite of its symptom occurred). Operator may consider whether the proxy-403 assumption is now stale for this environment and downgrade/resolve.
- Ghost retry re-mint: the retry (search-stale-response-race-retry1) was a well-formed Tier-1c row whose fix had already landed on dev; verify-first inside the implementer caught it (pr=none, no bad PR), but a full claim+spawn was spent. Rolled into dev-truth-blind-to-non-id-prefixed-merge.
- Self-test T28 WARN: `code-review-mobile-native-kmp-search-stale-response-race-retry1` open but terminal-in-archive — clears on next `gc1-reconcile.sh --apply`.

## Self-test tail
```
  ok    prompt wires backlog-honesty pass
  ok    prompt wires Tier 1c retry re-mint
  ok    prompt encodes merge-confirm sweep
  ok    prompt encodes escalation SLA
  ok    all retry_of rows well-formed
  ok    no open retry row targets landed work
==> dispatcher-self-test: PASS
```
