# Dispatcher Self-Review — 2026-06-04T02:22:28Z

Reviewed HEAD `b79cde7` + the last 8 `chore(research): dispatcher` commits (02:04 cycle back through 18:08).

## Run shape
- Last run (2026-06-04 02:04): 3 claimed, 0 reviewed, 0 merge-attempts, 4 merged-now, 0 failed, 7 active (3 in-progress / 4 review), buffer 36/36 dep-blocked=0, 0 rebased, 0 dup-skipped.
- Phase 2 archived 4 merged rows (PR #1035 #1034 #1033 #1039).
- Phase 5.5: PR #1004 #1011 #1022 #1026 approved but human-gated → skipped (the recurring approved-but-unmergeable drain stall).
- goal-check on the run: GC1=FAIL (referential-integrity); GC2/GC3 ok. Self-test PASS at review time.
- Archive 186 rows; combined HEAD=193=current (active=7). Active pool clean (T18/T18b/T19/T20 green).
- Oldest review row: gap-83-1-airbnb-integration-ui, age ~14h.

## New findings this run
- None created this invocation. Both qualifying signals from the 02:04 cycle were already upserted earlier this run (last_seen 02:19:55Z): the bare-task-id picker conflict and the large-archive push collision (below). No further qualifier crossed threshold.

## Recurring findings (upserted)
- **pick-target-epic-emits-bare-task-id-epic-prefix** (medium, planner/self-test; recurrence 1, severe-single-event). Finish-first picker returned epic_prefix `security-pkce-oauth-authcode-pr-908-closed` (a single-task backlog row). Verified `.research/pick-target-epic.sh:84` (`else . end;`) falls back to the full task_id for non-gap/non-pm ids, and `.research/dispatcher-self-test.sh:498-502` T21 accepts only `gap-N[a-z]?` / `pm-*` / `NONE` — so persisting that objective.json would have hard-failed the Phase 6 gate. Dispatcher dodged it by resetting objective→NONE. Fix: collapse bare-task-id candidates to NONE in the picker (preferred), OR relax T21 to accept anything `epic_prefix()` can emit.
- **mcp-push-cannot-inline-large-archive** (high; recurrence 2). 211KB / 186-row archive again impractical to inline for `mcp__github__push_files`; Phase 6 state landed via branch+PR+rebase-merge (PR #1042). Root cause stands: monotonically growing single-file archive × content-inline-only push primitive. Fix: shard by month, or delta-append + out-of-band compaction.
- **git-push-blocked-by-proxy** (medium; recurrence 4). Direct `git push` to dev is HTTP-403 again; MCP/PR path used. Already acknowledged (PUSH_METHOD=mcp default drafted).

## Anomalies
- `gh` CLI unavailable → Phase 0.5 lock sanctioned-skipped (expected in this env; consistent across recent runs).
- The bare-task-id issue did NOT trip T21 only because the dispatcher manually reset objective before the gate — latent, not surfaced by the self-test. The picker/T21 disagreement remains live.
- Findings array re-sorted to (severity desc, recurrence desc, last_seen desc) — fixed two mis-ordered ties within the high/medium tiers (06-04 r=2/r=4 rows had sunk below older 05-28 peers).

## Acknowledge pass
- 0 acknowledged. No open finding's proposed-fix file:line changed in the last 8 commits — only `.research/management/*.json` moved; `pick-target-epic.sh`, `dispatcher-self-test.sh`, `dispatcher-prompt.md`, `archive-reconcile.sh` untouched. No auto-acknowledge fires; never auto-resolve.

## Self-test tail
`==> dispatcher-self-test: PASS` — T15 open=33 claimable=33 dep_blocked=0; T17 HEAD=193 current=193 (active=7, archive=186); T18/T18b/T19/T20 clean; T21 objective=NONE; T22 WIP=7≤8; T23/T24/T25 ok.
