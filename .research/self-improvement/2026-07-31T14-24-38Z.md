# Dispatcher self-review — 2026-07-31T14-24-38Z

Commit: d08a8d0 — dispatcher 2026-07-31 14:20 — 1 claimed, 0 reviewed, 1 failed, 3 active

## Run shape
Claimed=1 Reviewed=0 Merged-attempts=0 Merged-now=0
Failed=1 Active=3 Buffer=2/2 (action-list open=6, most dep/assigned) dep-blocked=0
Hang alerts: WARN=0 ALERT=0  Self-test: pass (T28 WARN advisory)

## New findings this run
- stale-in-progress-action-list-decoupled-from-assignments [high] — action-list has 31 in-progress rows vs 3 live assignments, and the 3 truly-active assignments are marked `open` (not in-progress) in action-list; the 31 phantom rows are excluded from claimable, driving chronic GC3 buffer FAIL; fix: reconcile action-list.status<->assignments lifecycle (extend gc1-reconcile) + self-test T32 asserting in-progress bijection.
- churn-hotspot-refactor-stale-premise-no-preflight [medium] — refactor-churn-hotspot-* spawned against files already cohesive / DTOs extracted / removed by an intervening merge; fix: preflight target-file existence + size/complexity threshold at claim time, auto-fail reason=stale-premise before sandbox spawn.
- finish-first-singleton-epic-throttles-retry-ids [medium] — finish-first claimed only 1 with free_slots=6 because retry/bare ids lack a gap-/pm-/epic- prefix and collapse into one synthetic singleton epic; fix: distinct epic key per unprefixed/retry stem (or exempt retries from singleton cap) + self-test.

## Recurring findings (>=2 runs, still open)
- buffer-starved-below-floor-no-refill-path [recurrence=17, medium] — GC3 buffer-bounds FAIL again (claimable ~2 << floor 36); note: root cause now traced to the stale-in-progress decoupling finding above, not genuine backlog exhaustion.
- gc3-buffer-bounds-fails-on-legitimate-backlog-exhaustion [recurrence=2, medium] — GC3 FAIL, but this run shows the exhaustion is NOT legitimate (31 phantom in-progress rows); premise partly superseded by the new decoupling finding — operator please reconcile the two.

## Anomalies this run (single occurrence, watching)
- scheduler churn-hotspot task left in wait (115m/120m grace); target backend/servers/api-server/src/services/scheduler.rs confirmed NOT on dev (removed by merged #2610) — will fail next run absent a preflight (captured under churn-hotspot-refactor-stale-premise-no-preflight).
- T28 self-test WARN: layout-tenant terminal in archive yet status=open in action-list — same decoupling class as the new high finding; run `bash .research/gc1-reconcile.sh --apply`.
- ~37/31 action-list in-progress vs 3 live assignments: large mismatch corroborated directly in state files.

## Self-test tail
```
  ok    prompt wires backlog-honesty pass
  ok    prompt wires Tier 1c retry re-mint
  ok    prompt encodes merge-confirm sweep
  ok    prompt encodes escalation SLA
  ok    all retry_of rows well-formed
  ok    no open retry row targets landed work
==> dispatcher-self-test: PASS
```
