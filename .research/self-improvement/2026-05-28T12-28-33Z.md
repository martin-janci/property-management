# Dispatcher self-review — 2026-05-28T12-28-33Z

Commit: 613ea35 — chore(research): dispatcher 2026-05-28 12:26 — C2 R4 M2 X0 F0 A12 B64/64

## Run shape
Claimed=2 Reviewed=4 Merge-attempts=2 Merged-now=0 Failed=0 Active=12 (2 in-progress, 10 review)
Buffer=64/64 dep-blocked=0. Oldest review 17h (fix-569 PR633). Hang alerts: WARN=0 ALERT=0.
Self-test PASS (all T10–T19 ok, combined row count HEAD=149=current).

## Systemic issues this run
1. **Suffix-variant duplicates dodge the dedup gate.** `gap-82-4-swiftui-listing-detail`
   sits in review as TWO rows: PR#641 (base id) and PR#644 (`-impl` suffix). The Phase 7
   dup-skip log keys on exact task_id, so a re-spawn under a `-impl`/`-fix`/`-v2` suffix
   opens a second PR for the same gap. Two PRs, one feature → wasted review + merge-conflict risk.
2. **Orphaned PRs keep appearing in Phase 2 reconciliation.** This run found 4 (#685/#686/#687/#641)
   with no assignment row. Fire-and-forget implementer spawns are losing the row↔PR link, forcing
   the dispatcher to back-fill review rows. Second consecutive run with reconciliation noise.
3. **Stale local base wastes a cycle.** Local dev was 50 commits behind origin/dev; the run had to
   reset + re-run. Fresh-clone resets keep biting timing-sensitive gates.
4. **Post-merge review gate fooled by clone-reset mtime.** Post-merge skipped ("mtime < 24h — fresh
   clone reset mtime"). A clone resets file mtimes to now, so the mtime-based cadence gate never fires
   on a fresh clone — the cadence should key on a persisted timestamp, not file mtime.
5. **Review→merge conversion stalled.** Both merge attempts failed (#633 ci-failed, #639
   needs-human-review); 0 merged vs 10 in review. Last run merged 11, this run 0 — bursty drain.

## Trend vs. prior runs (3 visible in fresh-clone history)
- Merged-now 4 → 11 → 0. Highly bursty; in-review pool oscillates 14→11→12.
- Claimed steady 3→2→2. Reviewed 3→0→4. Failed 0→1→0.
- Reconciliation/orphan-PR noise present in 2 of 3 runs — trending into a recurring class.

## Improvement ideas (concrete, scoped)
- Dedup gate: normalize task_id by stripping trailing `-impl|-fix|-v\d+` before the open-PR/same-epic
  conflict check, OR key dedup on the gap-id prefix (`gap-82-4-*`) so suffix variants collide.
- Persist `last_post_merge_review_at` in management state and gate Phase 6 on that, not file mtime —
  immune to clone resets.
- On implementer spawn, write the review row (status=in-progress, branch) BEFORE firing, so a PR can
  never be orphaned; Phase 2 reconciliation becomes a safety net, not the primary path.
- Auto `git fetch && reset --hard origin/dev` at run start when N commits behind, before any gate.

## Self-test
T17 combined row count OK: HEAD=149 current=149 (active=12, archive=137)
T18 all archive rows terminal; T19 no terminal rows in active. ==> PASS
