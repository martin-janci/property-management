# Dispatcher self-review — 2026-08-12T00-18-15Z

Commit: 4699a32 — dispatcher 2026-08-12 Tier 1d generator signals (ppt-web-ui, 2 findings)

## Run shape
Claimed=0 Reviewed=0 Merged-attempts=0 Merged-now=0
Failed=0 Active=1 (quarantined PR #2684, SLA ~4/7d) Buffer=4/5 dep-blocked=0
Hang alerts: WARN=0 ALERT=0  Self-test: pass

## New findings this run
None (this run's dominant issue is an existing finding, re-armed with hard evidence).

## Recurring findings (≥2 runs, still open)
- tier1d-signals-accumulate-no-promotion-to-backlog [recurrence=5, HIGH ↑from medium] — 52 of 59 Tier-1d signal ids emitted since 2026-08-04 never reached backlog.json; the buffer starves while landable high-value work (prod-401 OCR/AI-chat, api auth-bypass, tenant-isolation) sits unpromoted under `.research/signals/`.
- buffer-starved-below-floor-no-refill-path [recurrence=25, medium] — claimable=4 < floor=36; refill produced only 2 score-1 i18n nits.
- mobile-native-kmp-items-starve-claim-pool-in-cloud [recurrence=4, medium] — all 3 open non-i18n rows are AGP-gated KMP; claimable pool has zero landable claim.
- mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=13, high] — direct git-push to dev succeeded again; MCP push not used.
- mcp-push-cannot-inline-large-archive [recurrence=15, high] — backlog.json ~365 KiB far over the 64 KiB MCP inline ceiling; direct transport carried it.
- temp-phase-8-past-documented-removal-deadline [recurrence=11, medium] — Phase 8 still spawned past its 2026-06-30 removal marker.

## Anomalies this run (single occurrence, watching)
- ROOT-CAUSE PINPOINTED: staged Tier-1d signals are never promoted. Only 7/59 signal ids since 08-04 exist in backlog.json. This makes GC3-buffer-bounds=FAIL this run a TRUE positive (real starvation), not the false-positive-on-legitimate-exhaustion premise of `gc3-...-exhaustion` — several prior GC3 FAILs may likewise have masked this promotion gap. Not softening GC3; the fix is a signals→backlog promoter.
- Tier-1d generator behaved gate-aware this run (deliberately excluded mobile-native-kmp, chose landable ppt-web-ui), which would satisfy `tier1d-generator-not-gate-aware`. But dispatcher-prompt.md and the ppt-dev-review skill were NOT edited in the last 8 commits — this was discretionary model behavior, not a landed fix. Per constraint #4 the finding stays OPEN (no acknowledge); watch for a codified selection guard.
- ppt-web-ui re-reviewed near-daily (18+ signal files, incl. 08-09/10/11×2/12), re-emitting the same i18n signals with drifted ids (`-i18n-gap` → `-i18n`) — a direct consequence of the promotion gap: unpromoted signals leave no backlog dedup fingerprint, so the generator keeps rediscovering them.
- Quarantine PR #2684 (code-review-api-core-workflow-cond-parse-failopen) at ~4/7d, no exit; correctly excluded from claimable — no twin-block evidence this run.

## Self-test tail
```
  ok    prompt excludes gated ids from candidates
  ok    prompt surfaces Phase 7 Mobile-native gated line
  ok    predicate gates a canonical mobile-native id
  ok    predicate does NOT gate mobile-rn (React Native) ids

==> dispatcher-self-test: PASS
```
