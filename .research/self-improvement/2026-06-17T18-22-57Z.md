# Dispatcher self-review — 2026-06-17T18-22-57Z

Commit: ce9a2cb — chore(research): dispatcher 2026-06-17 18:14 — C2/R0/M0/X5/F0, A3

## Run shape
Claimed=2 Reviewed=0 Merged-attempts=0 Merged-now=5 (PRs #1540–1544)
Failed=0 Active=3 (3 in-progress, 6 quarantined → 9 rows) Buffer=29/29 (reported) dep-blocked=0
True claimable=2 (29 of 31 open action-list items are archive zombies, mostly status=failed)
Hang alerts: WARN=0 ALERT=0  Self-test: PASS

## New findings this run
None — this run's KEY FINDING (failed-zombie open action-list items inflating the
buffer/GC3 claimable metric) is already captured as `buffer-metric-blind-to-archived-open-items`.

## Recurring findings (≥2 runs, still open/ack)
- buffer-metric-blind-to-archived-open-items [recurrence=3, high] — buffer-guard reported claimable=22 / GC3 reported claimable=29 while true claimable=2; 29/31 open action-list items are exact-id-present in archive (mostly status=failed). First claim attempt picked 6 zombie ids; only the self-test active∩archive duplicate-task_id gate caught it. GC3 buffer-bounds FAILed (below floor).
- git-push-blocked-by-proxy [recurrence=6, medium, ack] — direct push to dev = 403 (protected); landed via branch-PR #1546.
- no-run-level-lock-parallel-overlap [recurrence=6, high, ack] — Phase 0.5 run-lock sanctioned-skipped (gh CLI unavailable); no run-level mutex held.

## Smallest fix for the failed-zombie leak (proposed_fix refined on the existing finding)
Two unlanded parts, both narrow:
1. CLOSE step — extend `gc1-reconcile.sh` (or add an action-list-reconcile pass) to
   auto-close ANY open action-list item whose EXACT task_id is terminal in active+archive
   for ANY id shape **including status=failed**. Today it auto-closes only items whose
   archived status is merged/done, so failed-zombie open items accumulate run over run.
2. METRIC step — fix the Phase 2.6 buffer-guard `open_count` AND the goal-gate GC3
   claimable formula to subtract archive task_ids (both currently subtract only ACTIVE
   assignment ids). Self-test T15 (`dispatcher-self-test.sh:294`) encodes the same blind
   formula — add a guard asserting `open_claimable` excludes archive-present ids.

## Anomalies this run (single occurrence, watching)
- Tier 2 upstream planner kick fired with **http=200** (success), NOT the recurring HTTP-400
  payload rejection (`tier2-kick-payload-rejected-400`) — but coverage.json is stale
  (2026-06-16) and exhausted, so the kick produced no new claimable rows. The 400-payload
  finding was NOT bumped; watching whether the 200 path actually refills next run.
- pm-qa-allowed-pet-types-enum-decode-audit: sandbox-timeout reclaim #1 (235m, no branch).
- Phase 0.5 lock + ensure-labels both sanctioned-skipped because gh CLI was unavailable —
  same gh-unavailable degradation that forced the branch-PR push path.

## Self-test tail
  ok    pr-reviewer-prompt.md intact + referenced by dispatcher-prompt.md
  ok    premerge-autofix-prompt.md intact + referenced by dispatcher-prompt.md
  ok    rebaser-prompt.md intact + referenced by dispatcher-prompt.md
  ok    pr-followup-prompt.md intact + referenced by dispatcher-prompt.md
==> dispatcher-self-test: PASS
