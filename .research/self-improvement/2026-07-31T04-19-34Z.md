# Dispatcher self-review — 2026-07-31T04-19-34Z

Commit: df1cfd7 — dispatcher 2026-07-31 04:18 (1 claimed, 1 reviewed, 1 merged-now)

## Run shape
Claimed=1 Reviewed=1 Merged-attempts=1 Merged-now=1
Failed=0 Active=1 Buffer=6/7 dep-blocked=0
Hang alerts: WARN=0 ALERT=0  Self-test: pass

## New findings this run
None (ref-delete-unsupported folds into git-push-blocked-by-proxy —
08ab915 already scoped ref-DELETEs as the blocked proxy op).

## Recurring findings (≥2 runs, still open)
- buffer-starved-below-floor-no-refill-path [recurrence=15, medium] — GC3=FAIL 6 of last 8 runs; this run claimable 6/7 « floor 36, backlog-refill avail=0, retry-remint none-eligible. Open pool collapsed 72→7: backlog is now largely terminal/tracked, so refill paths structurally cannot recover. Structural, not transient.
- mcp-push-cannot-inline-large-archive [recurrence=11, high] — 340bc98 archive 476KB and bd5b4e0 backlog 254KB both exceed the 64KiB MCP inline ceiling → ARCHIVE MOVE / backlog-reconcile DEFERRED. Fix: stop routing large-state writes through MCP push_files inline; use direct git push (works this env) for state commits.
- no-run-level-lock-parallel-overlap [recurrence=10, ack] — lock=skipped-gh-unavailable again; the Phase 0.5 lock CAS is gh-CLI-dependent and gh is exactly what's down, so the lock is inert precisely when needed. Fix already drafted in evidence: CAS via mcp__github__* or a git-ref push (both available when gh is not).
- mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=7, open] — env note confirms direct git push works; MCP-primary default remains obsolete.
- git-push-blocked-by-proxy [recurrence=7, ack] — git push WORKS but ref-DELETE disconnects (merged/probe branches undeletable). Note 340bc98 reported push/fetch 403'd — proxy push behaviour is flaky run-to-run; the reliable invariant is "new-branch push + state push OK, ref-delete blocked".

## Anomalies this run (single occurrence, watching)
- github_io_fallback=ref-delete-unsupported: merged/probe branches accumulate undeletable via proxy. Branch-hygiene only, not throughput; folded into git-push-blocked-by-proxy evidence.
- Positive signals (NOT auto-resolved, operator owns status): Tier-2 planner kick http=200 (success) — tier2-planner-kick-auth-401 / tier2-kick-payload-rejected-400 not evidenced this run; Tier-1d generator produced an artifact (1 finding, api-core) — tier1d-devreview-generator-stalls-no-artifacts not evidenced; retry-remint minted nothing invalid (none-eligible). None flipped — no proposed-fix file changed in the last 8 commits (all were state/backlog/signal artifacts only).
- Open pool shrank 72→7: buffer *floor* (36) now exceeds the entire open backlog, so GC3 can never pass until the floor adapts to a drained backlog. Candidate refinement to the buffer-starved finding's fix: cap the floor at open-count, or exempt "backlog structurally drained" from GC3 FAIL.

## Self-test tail
  ok    prompt wires Tier 1c retry re-mint
  ok    prompt encodes merge-confirm sweep
  ok    prompt encodes escalation SLA
  ok    all retry_of rows well-formed
  ok    no open retry row targets landed work
==> dispatcher-self-test: PASS
