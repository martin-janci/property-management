# Dispatcher self-review — 2026-06-14T10:23:44Z

Run mode: normal. Lock: skipped (gh unavailable). Skip-gate: none (assignments.generated age 214m).

## What this run did (reliable, MCP/local-only)
- Phase 2: reconciled review PRs #1318 (approve, CONFLICTING) and #1319 (changes, unstable) against GitHub via MCP — both head SHAs unchanged vs last_reviewed_oid, no transitions, no orphans.
- Archive sweep: 12 leaked terminal rows (8 merged + 4 failed) → assignments-archive (active 16→4); action-list reconcile: 47 done/dropped → archive; GC1 clean. (Local only — large archive files not pushed this run.)
- Phase 3: finish-first picker chose a bare-task-id epic (T21-invalid) → reset objective to NONE; claimed 1 general-pool task.
- Phase 4: implementer confirmed the claimed task (airbnb OAuth token exchange, story 83-1) is already implemented+tested on dev (PR #1177 + #1318) → push-free no-op drain → row failed, action-list item marked done.
- Phase 6: minimal-safe MCP push (marker + objective + triage + this report); large state files deferred (byte-exact transfer risk vs. benign cleanup).

## Recurring findings bumped (≥3 = pattern)
- **pick-target-epic-emits-bare-task-id-epic-prefix** (recurrence 3, now a confirmed pattern): with DISPATCHER_FINISH_FIRST=1, the picker emits a feat-* (bare task_id) epic_prefix that dispatcher-self-test T21 rejects. The current action-list has zero gap-/pm- epics, so finish-first is structurally inert here and churns objective.json every run. **Proposed fix:** either (a) teach pick-target-epic.sh to emit only T21-valid prefixes (gap-N/pm-<role>/NONE) and return NONE when no such epic exists, or (b) relax T21 to accept the picker's slug-prefix grammar, or (c) disable DISPATCHER_FINISH_FIRST until the backlog is re-keyed to gap-/pm- epics.
- **git-push-blocked-by-proxy** (recurrence 6): git push 403 + gh missing. Beyond the dispatcher's own MCP push, this blocks Phase 5.6 rebaser and Phase 5.7 followup (force-push) and any ppt-implement run that produces a real diff. Only push-free paths work (no-op drains, MCP PR create/merge/issue). **Proposed fix:** port ppt-implement/ppt-pr-create/rebaser/followup to an MCP push path (create_branch + push_files) so branch-mutating work can land in this environment.

## Stuck (env-blocked this run)
- PR #1318 (approve, CONFLICTING): needs a rebase to clear the conflict; force-push blocked → could not run Phase 5.6. The 'test' job is red but inherited from dev #1332 and appears non-required (4 sibling PRs merged today despite it).
- PR #1319 (verdict=changes, draft, fix_rounds=2): the 'changes' is solely the inherited #1332 test failure ("new airbnb tests look correct"); Phase 5.7 followup needs push → blocked.
