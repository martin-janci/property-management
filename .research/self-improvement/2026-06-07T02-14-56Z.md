# Dispatcher self-review — 2026-06-07T02-14-56Z

Commit: 89b7b23 (landed d1bbc54 on dev via PR #1152 rebase) — dispatcher 2026-06-07 02:11

## Run shape
Claimed=0 Reviewed=0 Merged-attempts=0 Merged-now=4
Failed=0 Active=0 Buffer (spec metric)=52/54 dep-blocked=0; **REAL claimable=0**
Hang alerts: WARN=0 ALERT=0  Self-test: PASS  Run lock: skipped (gh unavailable)

## What happened
- Phase 2 reconciled 4 rows to merged from GH truth: PRs #1004, #1011, #1123 (review→merged) and #1126 (quarantined→merged — a human merged the quarantined PR; reconciled rather than left stale). Swept all 4 to archive (active 4→0, archive 260→264, combined unchanged).
- Phase 3 claimed 0. The entire 54-item open buffer is exact-id duplicates of archived tasks (45 merged/done, 9 failed). HARD RULE + T4 forbid re-claiming an archived id, so genuine claimable = 0.
- Tier 2 upstream kick fired (claimable=0) → planner returned HTTP 400 ("claimable: Extra inputs are not permitted").
- Phase 6 pushed via the proven large-archive workaround (session-branch push + PR #1152 + rebase-merge); md5-equivalent state verified on origin/dev.

## Recurring findings (bumped this run)
- mcp-push-cannot-inline-large-archive [rec=4, high] — used the proven session-branch+rebase-merge workaround again; 291KB archive landed cleanly. Bake into Phase 6 as the primary path.
- buffer-metric-blind-to-archived-open-items [rec=2, high] — spec buffer metric reported 52 claimable but REAL=0 (all open items terminal-in-archive). gc1-reconcile only closes gap-N-M stems, so test-gap-*/refactor-*/triage-*/bug-* dups never drain. Fix: subtract archived ids (any shape/status) from open_count, and/or auto-close open items whose exact id is terminal in archive.
- tier2-kick-payload-rejected-400 [rec=2, medium] — same HTTP 400 schema-drift on the planner kick endpoint; payload "claimable" field rejected. DISPATCHER_URL appears to point at an Anthropic-API-shaped endpoint. Fix: align payload with the fire endpoint / correct DISPATCHER_URL.

## Anomalies this run (watching)
- Quarantined PR #1126 was merged by a human without flipping status back to "review"; the spec has no Phase-2 path out of quarantined. Dispatcher reconciled it to merged from GH truth (merged is terminal). Consider adding a Phase-2 quarantined-row GH-state probe to the spec so this is no longer a manual judgement call.

## Self-test tail
PASS (T1–T26 green; GC1 record-only FAIL = buffer pollution above).
