# Dispatcher self-review — 2026-07-16T16-20-35Z

Commit: cff7ea1 — chore(research): dispatcher 2026-07-16 16:17 — 1 claimed, 1 reviewed, 0 merge-attempts, 0 merged-now, 1 failed, 1 active

## Run shape
Claimed=1 Reviewed=1 Merged-attempts=0 Merged-now=0
Failed=1 Active=1 (quarantined) Buffer=6/72 dep-blocked=0
Hang alerts: WARN=0 ALERT=0  Self-test: PASS

## New findings this run
- fp-tier2-planner-kick-auth-401 [high] — Tier-2 planner-kick returned http=401 authentication_error (DISPATCHER_TOKEN/DISPATCHER_URL rejected), so the only upstream buffer-refill path is dead while claimable=6/72 is below floor=36; fix: operator rotate/verify DISPATCHER_TOKEN + DISPATCHER_URL, and branch the Phase 2.6 curl so 401/403 escalates as an operator-config outage instead of a generic record-only "kick failed".

## Recurring findings (≥2 runs, still open)
- fp-buffer-starved-below-floor-no-refill-path [recurrence=6, medium] — GC3-buffer-bounds=FAIL claimable=6/72 vs floor=36; Tier-1b avail=0 AND Tier-2 kick 401 → both refill paths dead.
- fp-retry-remint-mints-invalid-rows [recurrence=4, medium] — retry-remint re-minted gap-10a-2-...-retry1, already status=done in action-list-archive; it dedups vs the ASSIGNMENTS archive but NOT the action-list-archive.
- fp-dev-truth-blind-to-non-id-prefixed-merge [recurrence=3, medium] — the #482 landing (descriptive, non-id-prefixed squash subject) is invisible to retry-remint's `<id>:`-subject dev-truth guard; coverage-done exclusion missed story 10a-2 → ghost retry no-op'd on claim.
- fp-no-run-level-lock-parallel-overlap [recurrence=7, high(ack)] — Phase 0.5 run-lock skipped-gh-unavailable; the lock is gh-REST-dependent so it goes inert exactly when gh is down.

## Anomalies this run (single occurrence, watching)
- PR #2377 / gh-issue-2360 QUARANTINED at fix_rounds=3 (reviewer verdict=changes): gate inert (Ok(None) dup collapses to Recorded), clippy red, 00219 migration collision. This is the 3-round quarantine cap working as designed on a PR-local bug, not a dispatcher systemic defect — watching, no finding.
- Tier-1d dev-review generator PRODUCED 2 findings this run (scheduler notify-swallow + reconfirmed high-sev SSRF redirect) — the opposite of the `tier1d-devreview-generator-stalls-no-artifacts` stall symptom; left that finding untouched (no auto-resolve), but generator health looks recovered this run.
- Tier-1b backlog-refill avail=0 (upstream drained) — the supply side of the buffer-starvation finding, no separate row.

## Self-test tail
  ok    prompt wires Tier 1c retry re-mint
  ok    prompt encodes merge-confirm sweep
  ok    prompt encodes escalation SLA
  ok    all retry_of rows well-formed
  ok    no open retry row targets landed work
==> dispatcher-self-test: PASS
