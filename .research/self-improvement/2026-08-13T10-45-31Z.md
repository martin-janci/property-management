# Dispatcher self-review — 2026-08-13T10-45-31Z

Commit: 30450605 — chore(research): dispatcher 2026-08-13 10:42 — 1 claimed, 4 reviewed, 4 merged-now, 1 failed

## Run shape
Claimed=1 Reviewed=4 Merged-attempts=4 Merged-now=4
Failed=1 Active=2 Buffer=9/9 dep-blocked=0
Hang alerts: WARN=0 ALERT=0  Self-test: PASS (T28 advisory WARN — 5 open action-list items terminal in archive)

## New findings this run
None — every observed symptom mapped to an existing root-cause finding_id.

## Recurring findings (≥2 runs, still open) — bumped this run
- mcp-primary-push-default-obsolete-direct-git-push-works [recurrence=14, high] — direct `git push` SUCCEEDED again (proxy did NOT block it); gh CLI unavailable so all GitHub I/O via mcp__github__*. Directly contradicts finding git-push-blocked-by-proxy.
- tier1d-signals-accumulate-no-promotion-to-backlog [recurrence=6, high] — Tier-1d review-merged opened issues #2752/#2753/#2754 but claimable stayed 9/9 (all gated); generated issues did not become claimable rows, relieving zero starvation.
- buffer-starved-below-floor-no-refill-path [recurrence=26, medium] — claimable=9 < floor=36, residual all mobile-native/KMP (gated) → effective 0; Tier-1b/1c avail=0.
- gc3-buffer-bounds-fails-on-legitimate-backlog-exhaustion [recurrence=11, medium] — GC3 FAIL (record-only) while backlog legitimately exhausted (residual all gated); same hard FAIL as a genuine refill defect.
- retry-remint-mints-invalid-rows [recurrence=6, medium] — GHOST-RETRY: voice-webhook-timing-cmp-retry1 minted for a constant-time-compare fix ALREADY on dev (ct_eq_str/subtle); returned pr=none, no-op empty diff, FAILED. Wasted a full claim+spawn cycle.
- dev-truth-blind-to-non-id-prefixed-merge [recurrence=5, medium] — the HMAC fix landed on dev under a non-dispatcher commit, invisible to retry-remint dev-truth; self-test T31 PASSED (checks archive, not dev) — false green while the ghost-retry shipped.
- mobile-native-kmp-items-starve-claim-pool-in-cloud [recurrence=5, medium] — remaining open action-list items ALL mobile-native/KMP, AGP egress-blocked in cloud; sole driver of the starved buffer + GC3 FAIL (operator-side #2652).
- self-pr-approve-blocked-shared-bot-review-account [recurrence=2, medium] — all 4 approve-verdict reviews posted as plain COMMENTS (bot is repo owner, cannot self-approve); approvals cannot gate merge, forcing reliance on CI-green alone.

## Anomalies this run (single occurrence, watching)
- The retry-remint ghost + dev-truth blindness converged this run: PR #2744 (human-gated, NOT merged) carries the intrinsic already-landed-summary guard that would suppress exactly this ghost-retry class. Landing #2744 resolves both retry-remint-mints-invalid-rows and the dev-truth blindness for id-prefixed cases.
- Environment shift worth confirming: direct `git push` now works (proxy did NOT block it), while gh CLI was unavailable (all GitHub I/O via MCP). The push-path premise the MCP-primary contract is built on is disproved again — see mcp-primary-push finding; the acknowledged git-push-blocked-by-proxy finding is contradicted (status left untouched — operator owns).
- Self-test T28 WARN: 5 open action-list items terminal in archive (this run's 4 merged + 1 failed). Expected post-merge reconcile lag, not new drift — next run should run `bash .research/gc1-reconcile.sh --apply`.
- Tier-1d generator fired review-merged (not devreview) on the starved buffer and produced 3 issues — gate-aware enough to avoid the gated stack, so tier1d-generator-not-gate-aware was NOT bumped this run.

## Self-test tail
```
  ok    prompt excludes gated ids from candidates
  ok    prompt surfaces Phase 7 Mobile-native gated line
  ok    predicate gates a canonical mobile-native id
  ok    predicate does NOT gate mobile-rn (React Native) ids

==> dispatcher-self-test: PASS
```
