# Dispatcher Self-Review — 2026-05-28T14:42:00Z

**Run commit:** 561874d5bae75acb0ec6078ed8cbef27f2462612
**Self-test:** FAIL — T4 (task_id unique across active+archive): 4 task_ids duplicated WITHIN assignments-archive.json (active=0, archive=2 each): gap-10b-5-support-data-ui, gap-82-4-swiftui-listing-detail-impl, gap-82-5-swiftui-inquiries-account, pm-data-support-data-tracking-events. (Earlier in the run T11 also FAILed — 5 new rows missing hardening fields — then backfilled.)

## Recurring issues (>=3 runs)
- **no-run-level-lock-parallel-overlap** (rec 3): concurrent dispatcher cycles with no lock; verdicts captured by a parallel run, archive re-merged already-archived rows. Fix: Phase 1 advisory lock, abort if held.
- **stale-local-base-wastes-cycle** (rec 3): local dev drifts behind origin/dev, mid-run reset + re-run. Fix: fetch + reset --hard origin/dev at Phase 1 before any gate.
- **review-merge-conversion-bursty** (rec 3): merged-now swings 0..11 run-to-run; dispatcher M=0 on the big-merge runs (human/concurrent merges drained the pool). Fix: per-blocker routing + auto-respawn on ci-fail.

## New issues (first seen)
- **archive-append-not-idempotent-duplicate-rows** (high): Phase 6 archive append re-appends rows already in the archive when a concurrent/prior run archived the same task_id → in-archive dupes (T4 FAIL, not the active-leak T18/T19 guards). Fix: upsert-by-task_id (group_by | map(last)) before writing archive; add T-check for in-archive dupes.
- **self-test-fail-recurs-each-run** (high): self-test runs AFTER state is written, so T11/T4 violations are repaired reactively ("-> now backfilled") instead of prevented. Fix: stamp hardening fields in the Phase 3 claim template, dedup in Phase 6 write, and gate the Phase 6 commit on a passing self-test.

## Counts (last 8 runs)
| Counter | Runs with non-zero |
|---------|-------------------|
| Self-test FAIL | 2 (T11 backfill; T4 archive dupes) |
| Dup-skip / suffix-variant collision | 2 (gap-82-4) |
| Same-epic skipped | 1 (pm-security-p1-04-audit-debug-format) |
| Dropped / stale-already-merged spawn | 1 |
| Concurrent-run capture (R0 new) | 2 |
| CI-unknown / 0-checks stuck (PR#643) | 2 |
| Approved-but-unmergeable strand | 2 |
| Failed PR (F>=1) | 2 (both gap-82-5-ios-push-keychain closed) |
| Local-base reset + re-run | 3 |

## Verdict
Throughput is healthy (X=11 merged this run, buffer 142/159 claimable) but reliability is gated by concurrency: a single missing run-level lock is the upstream cause of three recurring symptoms (parallel-overlap, stale base, non-idempotent archive). The two new high findings both point at the write path — enforce hardening fields and archive-dedup at write time and gate the Phase 6 commit on the self-test, instead of backfilling after a FAIL. Recommend prioritizing the Phase 1 lock + Phase 6 upsert next.
