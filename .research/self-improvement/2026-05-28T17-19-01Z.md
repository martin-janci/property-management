# Dispatcher self-review — 2026-05-28T17:19:01Z
Commit: 1d441b07 — chore(research): dispatcher — Phase 5+6 capture: PR#708 approved, 2 merged rows archived

## Run shape
Claimed=3 (prior 17:13 cmt) Reviewed=1 (PR#708 approve) Merge-attempts=2 (PR#706,#695) Merged-now=2 (archived)
Failed=0 Active=7 (4 review, 3 in-progress) Buffer=claimable=117 dep-blocked=17 Archive=153 (combined=160)
Hang alerts: WARN=0 ALERT=0  Self-test: FAIL T4  goal-check: GC1 FAIL, GC2 ok, GC3 FAIL (record-only)

## New findings this run
- **fp-task-id-reclaim-archive-bypass** (high): gap-10b-3-admin-health-ui lives in BOTH active (review)
  and archive (merged). Phase 3 in an earlier run re-claimed a task_id already terminal in the archive; this
  run's Phase 6 archived the terminal copy without superseding the live active twin. No write-path guard
  enforces active∩archive=∅. Fix: Phase 3 must reject candidates whose task_id/stem is archive-terminal;
  Phase 6 must delete active rows that became terminal; add a Phase 6 pre-commit self-test gate.

## Recurring findings (≥2 runs, still open)
- no-run-level-lock-parallel-overlap (rec 5): this run saw 6 foreign commits (2 version bumps 0.2.998/0.2.999,
  2 PR-mark chores, 2 implementer commits) interleave between the 17:13 and 17:18 dispatcher commits. Root of the
  stale-base / reclaim / archive-dup cascade. Needs a Phase 1 advisory lock.
- self-test-fail-recurs-each-run (rec 3): third consecutive run ending on a T4 FAIL (T11→T4→T4→T4).
  Self-test runs AFTER state is written, so it is a backfill ritual not a gate.
- archive-append-not-idempotent-duplicate-rows (rec 2): gap-10b-6-onboarding-api-retry now appears TWICE
  inside assignments-archive.json (verified n=2) after Phase 6 re-appended an already-archived row.
- goal-check-gc1-orphan-action-list-items (rec 2): orphans=105 done_with_open_gap=10, identical to prior run.
- goal-check-gc3-buffer-overshoot (rec 2): claimable=117 vs ceiling 60, identical to prior run. Refill has no cap.
- stale-local-base-wastes-cycle (rec 5): same 6-commit interleave drifted the base mid-run.
- approved-but-unmergeable-pool-grows (rec 3); dup-suffix-variant-dodges-dedup (rec 2);
  orphaned-prs-reconciliation-noise (rec 2); review-merge-conversion-bursty (rec 4) — carried, unchanged.

## Anomalies this run
- T4 FAIL has TWO distinct sub-causes simultaneously: an active↔archive leak (gap-10b-3) AND an in-archive
  duplicate (gap-10b-6). They map to two separate findings with two separate fixes.
- goal-check GC1/GC3 numbers are byte-identical to the prior run — no remediation has landed; both remain
  record-only (enforce=0), so the 105-orphan / 117-claimable backlog is accumulating unmeasured-against.
- Dispatcher contributed 0 of its own merges again (X via archive only); review→merge pool still drained
  mostly by concurrent/CI actors.

## Self-test tail
```
T19 active assignments contains NO terminal rows (issue #9)
  ok    no terminal rows leaked into active
T20 stem-uniqueness among active (in-progress|review) rows
  ok    no two active rows share a stem
==> dispatcher-self-test: FAIL
```
