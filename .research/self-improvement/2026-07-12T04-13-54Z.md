# Dispatcher self-review — 2026-07-12T04-13-54Z

Commit: 9185040d97977bf65217a478e26d32f8ea5e72b0 — chore(research): dispatcher 2026-07-12 04:11 — 1 claimed, 0 reviewed, 0 merge-attempts, 0 merged-now, 1 failed, 1 active

## Run shape
Claimed=1 Reviewed=0 Merged-attempts=0 Merged-now=0
Failed=1 Active=1 Buffer=24/72 dep-blocked=0
Hang alerts: WARN=0 ALERT=0  Self-test: PASS

## New findings this run
- retry-remint-mints-invalid-rows [medium] — retry-remint.sh minted gh-issue-1680-retry1 with no issue_ref (T30 trip) + prior-run ghost retries on closed work; fix: backfill issue_ref from source row and skip remint of landed/closed ids in .research/retry-remint.sh.
- buffer-starved-below-floor-no-refill-path [medium] — claimable 24<floor 36 (GC3 FAIL) but Tier-1b found 0 upstream candidates and Tier-2 gated at floor/2=18, so nothing refills; fix: raise Tier-2 trigger to floor / escalate Tier-1b to Tier-2 when avail=0.

## Recurring findings (≥2 runs, still open)
- mcp-push-cannot-inline-large-archive [recurrence=6, high] — archive now 226KB/207 rows > 64KiB MCP inline ceiling; mcp-push-size-guard aborted MCP push, run landed via PUSH_METHOD=git.

## Anomalies this run (single occurrence, watching)
- git push WORKED this run (proxy not blocking), landing the full 226KB archive via PUSH_METHOD=git. This directly contradicts the acknowledged finding `git-push-blocked-by-proxy` (dev-push=403). Not bumped (opposite outcome); operator may want to re-confirm whether the 403 is environment-transient. It is also the ONLY working path for the oversized archive (see mcp-push finding), so the two findings are now coupled.
- Failed task `test-gap-repeated-churn-oauth-integration-tests` (reason=sandbox-failure-after-reclaim, reclaim_attempts at cap=1). This oauth test-gap has churned across multiple runs (wait/wait → fail); reclaim cap=1 permanently fails it after a single post-reclaim sandbox hiccup. Watching — if it recurs on a fresh reclaim, promote to a finding (reclaim cap too low for flaky sandboxes).
- Tier 1d generator (dev-review, segment api-handlers) emitted 2 product-code findings (suspended-account enumeration oracle HIGH; systemic raw DB-error leak MEDIUM) into the research backlog — healthy, not a dispatcher finding.
- GC3-buffer-bounds=FAIL is record-only/non-blocking; GC1 + GC2 green; issue-ingest +2 (gh-issue-2248/2249).

## Self-test tail
```
  ok    prompt encodes merge-confirm sweep
  ok    prompt encodes escalation SLA
  ok    all retry_of rows well-formed
  ok    no open retry row targets landed work

==> dispatcher-self-test: PASS
```
