# Dispatcher self-review — 2026-05-28T15:22:00Z

Scope: HEAD `b8bf30ad` Phase 7 + prior dispatcher run `6bf8e883` (only 2 true
dispatcher cycles in the grep window; rest are reconcile/mark commits). Self-test
and goal-check run live.

## Headline

- **self-test: FAIL (T4)** — `gap-10a-1-oauth-tests` and `gap-10b-3-admin-health-ui`
  exist in BOTH active and archive. Both were **re-claimed in-progress this run
  at 15:17**, yet are already **merged** in archive (PR#470 / PR#471, merged
  2026-05-25 ~12h earlier). The dispatcher scheduled already-shipped work.
- **goal-check: FAIL (GC1 + GC3)** — first run where `goal-check.sh` actually
  executes (prior run: SKIP/not-found). It came up red on two of three checks.

## New findings (3)

1. **reclaim-of-already-merged-task-id** (critical) — Phase 3 claim selection
   does not subtract task_ids already terminal in `assignments-archive.json`, so
   merged gaps re-enter the claimable pool. Root of the T4 FAIL and of goal-check
   `done_with_open_gap=10`. Fix: exclude archive-terminal task_ids (and stems)
   from claim selection; reconcile coverage done-state vs archive merges in 2.6.
2. **goal-check-gc1-orphan-action-list-items** (high) — GC1 FAIL `orphans=105`
   (action-list items referencing nonexistent coverage entries) `+ done_with_open_gap=10`.
   The newly-live gate exposes a backlog of referential drift never enforced
   before. Fix: Phase 2.6 cascade prune/relink + one-time triage of the 105.
3. **goal-check-gc3-buffer-overshoot** (high) — GC3 FAIL `claimable=117` vs the
   `18..60` bound. The +30 Phase 2.6 refill (6bf8e883) has no upper cap. Fix:
   make refill bound-aware (stop at 60); share GC3 bounds between refill and
   goal-check.

## Recurrences bumped (5)

- **no-run-level-lock-parallel-overlap** -> 4. The re-claim of already-merged
  rows points again at concurrent/stale-state cycles with no advisory lock.
- **stale-local-base-wastes-cycle** -> 4. Claim pool read from a base not
  reflecting the archive's merged state.
- **self-test-fail-recurs-each-run** -> 2. FAIL now 2+ consecutive runs
  (T11 -> T4 -> T4); still repaired reactively, not prevented.
- **approved-but-unmergeable-pool-grows** -> 3. M0/X0 this run; PR#639, PR#641
  still stranded on needs-human-review with no escalation routing.
- **review-merge-conversion-bursty** -> 4. X0 merged-now (prior run X11);
  dispatcher again contributed 0 merges.

## Theme

Three of this run's signals (T4 dup, GC1 done_with_open_gap, claim of merged
work) share ONE root cause: **claim selection ignores terminal archive/coverage
state.** That, plus the still-absent run lock and uncapped buffer refill, is the
highest-leverage fix cluster — it would clear the T4 FAIL, half of GC1, and stop
re-doing shipped work in a single change.

## Backlog

18 findings total (3 new, 5 bumped, all `open`; none auto-resolved).
See `.research/self-improvement/findings.json`.
