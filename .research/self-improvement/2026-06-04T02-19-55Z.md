# Dispatcher Self-Review — 2026-06-04T02:19:55Z

Reviewed HEAD `b79cde7` + the last 8 `chore(research): dispatcher` commits.

## Run shape
- Last run (2026-06-04 02:04): 3 claimed, 0 reviewed, 0 merge-attempts, 4 merged-now, 0 failed, 7 active (3 in-progress / 4 review), buffer 36/36 dep-blocked=0.
- Phase 2 archived 4 merged rows (PR #1035 #1034 #1033 #1039).
- Phase 5.5: PR #1004 #1011 #1022 #1026 approved but human-gated → skipped (recurring drain stall).
- goal-check: GC1=FAIL (referential-integrity) on the run; self-test now PASS at review time (T21 ok=NONE, T17 row-count balanced 193=193).
- Archive: 186 rows (211KB). Active pool clean (T18/T19/T20 green).

## New findings this run
- **pick-target-epic-emits-bare-task-id-epic-prefix** (medium, planner/self-test). Finish-first picker returned epic_prefix `security-pkce-oauth-authcode-pr-908-closed` — a bare single-task id. `.research/pick-target-epic.sh:84` (`else . end;`) falls back to the full task_id for non-gap/non-pm ids; `.research/dispatcher-self-test.sh:498-502` T21 only accepts `gap-N[a-z]?` / `pm-*` / `NONE`, so persisting that objective.json would have hard-failed the Phase 6 gate and aborted the run. The dispatcher dodged it by resetting objective→NONE. Severe-single-event: qualifies. Fix: make the picker collapse bare-task-id candidates to NONE (preferred), OR relax T21 to accept anything `epic_prefix()` can emit.

## Recurring findings (upserted)
- **mcp-push-cannot-inline-large-archive** → recurrence 2. 211KB archive again impractical to inline for `push_files`; Phase 6 landed via branch+PR+rebase-merge (PR #1042). Root cause stands: monotonically growing single-file archive × content-inline-only push primitive. Shard or delta-append.
- **git-push-blocked-by-proxy** → recurrence 4. Direct `git push` to dev is HTTP-403 again; MCP/PR path used. Already acknowledged (PUSH_METHOD=mcp default drafted).

## Anomalies
- `gh` CLI unavailable → Phase 0.5 lock sanctioned-skipped (expected in this env; matches prior runs).
- No self-test FAIL recurrence (T21 currently NONE). No ALERT-hang recurrence. The bare-task-id issue did NOT trip T21 only because the dispatcher manually reset objective before the gate — latent, not surfaced.
- Acknowledge pass: 0. No open finding's proposed-fix file:line changed in the last 8 commits — only `.research/management/*.json` moved; `pick-target-epic.sh`, `dispatcher-self-test.sh`, `dispatcher-prompt.md` untouched. So no auto-acknowledge.

## Self-test tail
`==> dispatcher-self-test: PASS` — T15 open=33 claimable=33; T17 HEAD=193 current=193 (active=7, archive=186); T18/T18b/T19/T20 clean; T21 objective=NONE; T22 WIP=7≤8; T23/T24/T25 ok.
