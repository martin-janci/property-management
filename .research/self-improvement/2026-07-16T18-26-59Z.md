# Dispatcher self-review — 2026-07-16T18-26-59Z

Commit: 6ea41b3b — chore(research): dispatcher 2026-07-16 18:18 — 6 claimed, 0 reviewed, 1 failed

## Run shape
Claimed=6 Reviewed=0 Merged-attempts=0 Merged-now=0
Failed=1 Active=6 Buffer=1/1 dep-blocked=0
Hang alerts: WARN=0 ALERT=0  Self-test: PASS (T28 WARN, not FAIL)

## New findings this run
- fp-mcp-primary-push-default-obsolete-direct-git-push-works [high] — dispatcher hard-assumes git push is proxy-403-blocked; in THIS env `git push origin dev` WORKED (only GH007 needed noreply email), so the MCP-primary contract + 64KiB size-guard false-positive on the 333782-byte archive; fix: Phase 1 runtime push-capability probe → PUSH_METHOD=git + bypass size-guard, MCP only on real 403.

## Recurring findings (≥2 runs, still open)
- no-run-level-lock-parallel-overlap [recurrence=8, high] — lock=skipped-gh-unavailable; the drafted `gh api` CAS can't acquire in exactly the gh-absent condition while MCP+git push both work — fix should CAS via mcp__github__* / git-ref.
- buffer-starved-below-floor-no-refill-path [recurrence=7, medium] — GC3-buffer-bounds=FAIL; claimable=1/open=1 vs floor=36 (coverage 47/49 done); Tier-1b/1c avail=0, only Tier-1d fired (2 findings) but that doesn't lift the claimable buffer.
- dev-truth-blind-to-non-id-prefixed-merge [recurrence=4, medium] — failed=1 ghost (test-gap-screen-map-drift-pr-1100-ppt-retry2) already reconciled on dev but invisible to the `<id>:`-anchored dev-truth guard; a claim slot spent on done work.
- reconciled-done-annotation-contradicts-archive [recurrence=2, low] — same ghost carried a stale RECONCILED-DONE marker while being terminal-in-archive/reconciled-on-dev; two dispatcher records disagree again.

## Anomalies this run (single occurrence, watching)
- Tier-1d dev-review generator fired on the reality-server segment and RETURNED 2 findings this run — the opposite of tier1d-devreview-generator-stalls-no-artifacts (rec=1); positive counter-evidence, not resolving that finding (operator owns status).
- gh CLI not installed this whole run, yet GitHub MCP + direct git push both worked — the only capability actually lost was the gh-dependent Phase 0.5 lock. Reinforces routing the lock through MCP.
- No review/merge activity (reviewed=0, merge-attempts=0) — expected given buffer starvation + all-in-progress/quarantined active pool; not a finding.

## Self-test tail
```
  ok    prompt encodes merge-confirm sweep
  ok    prompt encodes escalation SLA
  ok    all retry_of rows well-formed
  ok    no open retry row targets landed work

==> dispatcher-self-test: PASS
```
