{
  "generated": "2026-09-24T00:00:00Z",
  "source_run": "dispatcher Phase 1.5 Tier-1d demand-driven static code review (claim buffer starved; cloud-landable frontend segment preferred) 2026-09-24",
  "segment_reviewed": "reality-web",
  "selection_reason": "oldest-unreviewed (tie). review_cursor had ppt-web-core and reality-web both at 2026-08-29 (tied oldest). ppt-web-core was ALREADY mined earlier this same run cycle (see .research/signals/2026-09-24-ppt-web-core-tier1d.json, 3 findings; its strict infra re-confirmed clean) — reviewing it again would duplicate. That earlier run had SKIPPED reality-web for saturation (3 OPEN import/ silent-mutation findings dated 2026-09-21); those 3 are now status=done in backlog.json (0 open), so reality-web's saturation has cleared. reality-web is therefore the correct non-duplicate pick: tied-oldest, unsaturated, landable frontend, no same-day file. The already-mined-and-fixed import/ mutation files (FeedImport/SyncSchedule/CrmConnection) were deliberately excluded from this pass.",
  "experts_used": ["frontend"],
  "landable_in_cloud": true,
  "landability_note": "reality-web is a Next.js frontend app; findings here ARE landable in the cloud runner (unlike backend api-server, blocked by utoipa-swagger-ui build-script egress-403 #2966, and mobile-native KMP, AGP egress-gated).",
  "findings_count": 0,
  "static_only": true,
  "note": "ZERO landable net-new findings. reality-web is an exceptionally hardened, mature segment (consistent with the prior-run assessment that these frontend segments have been reviewed repeatedly 2026-09-16..-19 and are hardened). The frontend-expert static sweep (read + grep only, no build/test per skill contract) covered every high-risk surface and found each class already correctly handled: (1) SSR data fetch app/[locale]/listings/[slug]/page.tsx — !response.ok -> null, malformed-body normalization via parseListingDetail, try/catch -> null, notFound() emits real 404 (no soft-404), host-inference guards SSR against misconfigured NEXT_PUBLIC_API_URL. (2) Webhook receiver app/api/layout-revalidate/route.ts — HMAC over {timestamp}.{body} with timingSafeEqual + length guard, replay protection (±300s), 16KiB body cap enforced pre-auth, '*' cardinality sentinel on pre-auth emit paths (#2621). (3) XSS: lib/serialize-script.ts escapes <,>,&,U+2028,U+2029; verified ALL dangerouslySetInnerHTML script sinks route through it (ListingDetailContent JSON-LD, layout.tsx tenantBootstrap + ORGANIZATION_LD, env.js route) or are static string constants (color-scheme bootstrap reads only validated localStorage). (4) Client auth lib/auth-token.ts — try/catch around all localStorage, client expiry check with server as authority. (5) SSO callback app/[locale]/auth/callback/page.tsx — CSRF state check is documented defense-in-depth over a server-authoritative cookie flow; open-redirect guard rejects //, /\\, and non-path redirect_uri. (6) TenantProvider.tsx context value correctly memoized (useMemo, ref-stable deps, review R7 note). No `as any` casts, no stray console.log (only legitimate error-boundary/diagnostic console.error/warn), no useMutation lacking error handling outside the already-mined import/ files. Cursor should advance reality-web to 2026-09-24 so the rotation moves on.",
  "signals": []
}
