{
  "generated": "2026-09-24T00:00:00Z",
  "source_run": "dispatcher Phase 2.6 Tier-1d demand-driven (claim buffer starved; cloud-landable segment preferred) 2026-09-24",
  "segment_reviewed": "reality-server",
  "selection_reason": "CHURN-ALIGNED + oldest-landable. Phase-1 churn hotspots by most-recent last_seen (2026-09-20) were all in backend/servers/api-server/ (push_fanout.rs, integrations/webhook.rs, voice_webhooks.rs) -> segments api-handlers/api-core, which are STRUCTURALLY UNBUILDABLE in this cloud runner (utoipa-swagger-ui build-script egress-403, issue #2966), so findings there are unclaimable backlog -> skipped per task's landable-preference override. Next churn-aligned LANDABLE segment: reality-server (churn files backend/servers/reality-server/src/routes/sso.rs and services/search_alert_drainer.rs, both last_seen 2026-08-31; reality-server is a non-api-server backend crate and builds in cloud). reality-server also ties for oldest review_cursor among landable segments (2026-08-31). SATURATION CHECK PASSED: backlog.json holds 0 OPEN code-review findings for reality-server (2 historical, both status=done), so the >=3-open saturation rule does not fire. Landable frontend alternatives were less suitable: reality-web is saturated (3 open findings dated 2026-09-21 per today's earlier ppt-web-core signal), ppt-web-ui was mined 2026-09-23, ppt-web-core was mined earlier today 2026-09-24; mobile-native-kmp is egress-blocked (AGP/Gradle). Static analysis only (read + grep, no build/test) per skill contract.",
  "experts_used": ["rust"],
  "landable_in_cloud": true,
  "landability_note": "reality-server is a non-api-server backend crate and is buildable in the cloud runner (not affected by the utoipa-swagger-ui #2966 egress block that makes api-server unbuildable, nor by the mobile-native AGP block). Had any findings been emitted they would be verifiable via `cargo check -p reality-server` + targeted `cargo test -p reality-server`.",
  "findings_count": 0,
  "static_only": true,
  "note": "ZERO landable net-new findings. reality-server is an exceptionally hardened, mature segment; the Rust-expert sweep confirmed every high-risk class is already correctly handled, with tests and explanatory comments. This is an honest empty result -- no nits manufactured to pad the buffer. Verified CLEAN this pass (so future rotations and the claim buffer are not re-mined speculatively): (1) OPEN-REDIRECT: routes/sso.rs check_redirect_uri_allowed (:161-192) explicitly blocks protocol-relative `//evil.com` and backslash-obfuscated `/\\evil.com`, requires http(s) scheme, and allowlists origin scheme://host:port -- hardened per issue #820 with unit tests. (2) SQL INJECTION: the two `format!`-built SQL statements interpolate only trusted values -- routes/listings.rs:700 interpolates a static const (RICH_LISTING_SELECT); routes/price_map.rs:158 interpolates `interval`, which is whitelist-matched to static literals ('1 month'/'3 months'/'12 months'/'5 years') at :109-123 with a 400 on any other value -- all user values ($1/$2/$3) are bound, city via ILIKE bound param. (3) INFO-DISCLOSURE: util/errors.rs db_error() scrubs raw sqlx/anyhow error text to a generic 500 with three dedicated tests asserting no constraint/host/dbname/sslmode leak; grep found no route bypassing it into a response body (health.rs:151 verbose DB error is on the /health struct; agency_imports.rs:239/291 err.to_string() are BAD_REQUEST validation messages, appropriate). (4) UNWRAP/EXPECT PANICS: all non-test hits are safe -- handlers/inquiries/mod.rs:274 `.chars().next().unwrap()` is guarded by an is_empty() early-return at :269; extractors/auth.rs:63 `.strip_prefix(\"portal_session=\").unwrap()` runs only after `.find(|c| c.trim().starts_with(\"portal_session=\"))` on the same trimmed string; state.rs:760 / handlers/users:558 / sso.rs:708 are startup/OS-rng expects. No unsafe blocks, no todo!()/unimplemented!() in production paths. (5) IDOR/PAGINATION: listings clamp_pagination (:28-33, DEFAULT 20 / MAX 100) and agent_reviews limit.clamp(1,100) bound result sets; drainer fans push tokens strictly by row-owner user_id (documented cross-tenant invariant). (6) BACKGROUND DRAINER: services/search_alert_drainer.rs run_once (:358-434) logs-and-continues per-row failures, uses at-least-once semantics (mark_notified only after any channel succeeds, else record_failure). (7) PUBLIC WRITE ABUSE SURFACES: routes/agent_reviews.rs validates rating 1..=5 (:225); routes/reports.rs enforces enforce_public_report_rate_limit (:143-150,191) and validate_image_or_link_url (:38,220) with attachment caps.",
  "signals": []
}
