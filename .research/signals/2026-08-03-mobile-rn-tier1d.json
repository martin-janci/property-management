{
  "generated": "2026-08-03T00:00:00Z",
  "segment": "mobile-rn",
  "source": "dispatcher-tier1d",
  "selection_reason": "oldest-unreviewed",
  "experts_used": ["frontend", "tester"],
  "findings_count": 5,
  "note": "Demand-driven Tier 1d generation (claim buffer starved: claimable=4 < floor=36). review_cursor.mobile-rn was the oldest unreviewed segment (2026-07-22). TWO passes are merged in this artifact: pass A (first 3 signals) — offline-sync false-complete, ThreadDetailScreen send silent-fail, hardcoded empty/error/loading i18n. pass B (last 2 signals, appended by a second same-day Tier 1d generator) — offline-queue transient-drop data loss and usePushNotifications debug console.log; both verified NET-NEW and non-overlapping vs pass A and vs the un-folded 2026-08-02-mobile-rn-tier1d.json (token-refresh, NFC device-clock, NFC test-gap). Pass B re-audited the segment and confirmed it is otherwise well-defended: VotingScreen parseVoteSummaries validates response shape, useApi.ts/DocumentUploadScreen fetch paths check !response.ok, AnnouncementDetailScreen surfaces acknowledge.isError, no skipped/only tests, all Alert.alert copy is i18n-wrapped. Saturation check: 0 open code-review-finding items for mobile-rn in backlog.json. Root path frontend/apps/mobile/src (skill segment-map lists the stale frontend/mobile/src). Dispatcher should advance review_cursor.mobile-rn to 2026-08-03.",
  "signals": [
    {
      "id": "code-review-mobile-rn-offline-sync-false-complete",
      "type": "code-review-finding",
      "source": "rotating-expert-review",
      "segment": "mobile-rn",
      "score_delta": 2,
      "confidence": "medium",
      "candidate_vector": "bug",
      "title": "useOfflineSupport.processQueue reports isComplete:true after a head-of-line-blocked (halted) sync cycle",
      "files": [
        "frontend/apps/mobile/src/hooks/useOfflineSupport.ts"
      ],
      "evidence": [
        "frontend/apps/mobile/src/hooks/useOfflineSupport.ts:360-375 — on a *retryable* failure the FIFO drain intentionally halts: it sets haltedProgress {current: i+1, failed, isComplete: false} (:367-372), calls onProgress, and `break`s out of the for-loop. The comment (:361-366, issue #1767) documents that the failed action and all successors are carried forward for the next reconnect.",
        "frontend/apps/mobile/src/hooks/useOfflineSupport.ts:424-426 — but after the loop there is NO early return on the halted path: control falls straight through to `const finalProgress: SyncProgress = { total, current: total, failed, isComplete: true }` which is set + emitted via setSyncProgress/onProgress UNCONDITIONALLY, immediately overwriting the halted `isComplete:false` signal. So a partially-drained cycle reports to the UI as fully complete (current === total, isComplete: true).",
        "frontend/apps/mobile/src/hooks/useOfflineSupport.ts:399-416 — the `failed` counter reported in finalProgress excludes the carried-forward retryable items (they are neither in succeededIds nor terminalFailureIds and are re-persisted to QUEUE_KEY at :413-415). Net effect: the sync banner shows 'complete, N failed' while additional actions silently remain queued, so the user believes the offline queue fully flushed when it did not. Fix: `return { success, failed }` on the halt branch (or guard the finalProgress block so it is skipped when the loop broke early). Confidence medium — bug is unambiguous from source; exact UI copy impact not runtime-confirmed."
      ],
      "expert": "frontend"
    },
    {
      "id": "code-review-mobile-rn-thread-send-silent-fail",
      "type": "code-review-finding",
      "source": "rotating-expert-review",
      "segment": "mobile-rn",
      "score_delta": 2,
      "confidence": "medium",
      "candidate_vector": "bug",
      "title": "ThreadDetailScreen.handleSend: send-message mutation has onSuccess only, no onError — failed sends are silent",
      "files": [
        "frontend/apps/mobile/src/screens/messages/ThreadDetailScreen.tsx"
      ],
      "evidence": [
        "frontend/apps/mobile/src/screens/messages/ThreadDetailScreen.tsx:139-142,152-163 — `sendMessage` (useApiMutation POST /api/v1/messages/threads/{id}/messages) is invoked via sendMessage.mutate({content}, { onSuccess: () => { setDraft(''); refetch(); } }). There is NO onError callback and the component never reads sendMessage.isError anywhere. On a 5xx/network failure the message just fails to post with zero user feedback (no Alert/toast/inline error).",
        "Because the draft is cleared only in onSuccess, on failure the draft text is retained but the user gets no signal it didn't send — they may assume it was delivered or re-tap send repeatedly. This is a chat send path, where silent drop is directly user-visible correctness.",
        "Contrast with the segment's other write paths which DO surface errors: LeaseSignatureScreen.tsx:162-165 (onError -> Alert.alert(t(errorKey), err.message)) and VoteDetailScreen.tsx:233-234 (onError -> Alert.alert). ThreadDetailScreen is the outlier. Fix: add an onError that surfaces err.message (Alert or inline banner) and/or render a retry affordance keyed off sendMessage.isError."
      ],
      "expert": "frontend"
    },
    {
      "id": "code-review-mobile-rn-hardcoded-empty-error-i18n",
      "type": "code-review-finding",
      "source": "rotating-expert-review",
      "segment": "mobile-rn",
      "score_delta": 1,
      "confidence": "medium",
      "candidate_vector": "bug",
      "title": "mobile: empty/error/loading state strings hardcoded in English across ~18 screens despite react-i18next being wired",
      "files": [
        "frontend/apps/mobile/src/screens"
      ],
      "evidence": [
        "react-i18next IS configured and used app-wide (50 files call t()/useTranslation; many screens such as LeaseSignatureScreen and VoteDetailScreen are fully translated), but only 23 of 41 screen .tsx files import useTranslation — the remaining ~18 render hardcoded English, concentrated in list/empty/error/loading fallbacks that sk/cs/de users will see untranslated.",
        "Representative hardcoded user-facing strings (not wrapped in t()): FaultsListScreen.tsx:185,195 ('Loading…','No faults found'); BuildingsScreen.tsx:126,136,137 ('Loading…','No buildings','No properties are linked to your account yet.'); MetersScreen.tsx:156,171,172 ('No unit linked','No meters', body copy); MessagesScreen.tsx:134,141,147 ('Loading…','Try again','No conversations'); ThreadDetailScreen.tsx:174,183 ('← Messages','Loading…'); AnnouncementsScreen.tsx:266 ('No announcements'); WidgetSettingsScreen.tsx:90,163.",
        "Impact: low — i18n/consistency gap, no functional break. The primary/happy-path copy on these screens is often translated, so users get a jarring mix of translated content with English empty/error states. Fix: wrap the empty/error/loading Text nodes in t(...) and add the keys to the mobile locale bundles; add a lint guard (biome/custom) for bare visible <Text> literals in screens/."
      ],
      "expert": "tester"
    },
    {
      "id": "code-review-mobile-rn-offline-terminal-drop-dataloss",
      "type": "code-review-finding",
      "source": "rotating-expert-review",
      "segment": "mobile-rn",
      "score_delta": 2,
      "confidence": "medium",
      "candidate_vector": "bug",
      "title": "useOfflineSupport: transient (5xx/network) queued actions are permanently DROPPED after 3 retries with only console.error — silent loss of offline-created content",
      "files": [
        "frontend/apps/mobile/src/hooks/useOfflineSupport.ts",
        "frontend/apps/mobile/src/App.tsx"
      ],
      "evidence": [
        "frontend/apps/mobile/src/hooks/useOfflineSupport.ts:353-357 — the catch branch increments action.retries (:353) then `if (isPermanent || action.retries >= 3)` (:354) adds the action to terminalFailureIds and logs `console.error('Action failed after max retries:', action)` (:357). Dropping the 4xx-`permanent` case is correct, but bundling *transient* failures (5xx / network — the comment at :350-352 explicitly routes '5xx, network' through the retry budget) into the SAME terminal drop means a queued mutation that hit 3 consecutive server-5xx / connectivity blips is discarded, not carried forward.",
        "frontend/apps/mobile/src/hooks/useOfflineSupport.ts:399-401 — carriedForward = queue.filter(a => !succeededIds.has(a.id) && !terminalFailureIds.has(a.id)); anything in terminalFailureIds is excluded from `merged` and thus deleted from QUEUE_KEY on the subsequent AsyncStorage.setItem. The action is gone from local storage; nothing re-enqueues it.",
        "frontend/apps/mobile/src/App.tsx:81-83,337 — the sync toast's ONLY recovery affordance is onRetry={handleRetrySync}, and handleRetrySync (:81-83) just calls processQueue() again. Because the terminally-dropped action is no longer in the queue, tapping Retry cannot recover it. For offline-created user content (a queued fault report, meter reading, vote, or acknowledgement made while offline) this is silent data loss after transient server unavailability, with no user-facing record of what was lost (only a console.error a phone user never sees).",
        "Distinct from the pass-A 'offline-sync-false-complete' finding (about the isComplete:true flag on the halted path, useOfflineSupport.ts:424-426). This is a separate failure mode: permanent discard of transient-failed actions at :354-357. Suggested fix: treat only isPermanent (4xx) as terminal; give transient failures a larger bounded budget or move them to a dead-letter store surfaced in the UI rather than deleting them. Confidence medium — data-loss path is unambiguous from source; exact field impact depends on which action types reach retries>=3."
      ],
      "expert": "frontend"
    },
    {
      "id": "code-review-mobile-rn-push-debug-console-log",
      "type": "code-review-finding",
      "source": "rotating-expert-review",
      "segment": "mobile-rn",
      "score_delta": 1,
      "confidence": "medium",
      "candidate_vector": "bug",
      "title": "usePushNotifications: debug console.log left on the device-token register/unregister production path",
      "files": [
        "frontend/apps/mobile/src/hooks/usePushNotifications.ts"
      ],
      "evidence": [
        "frontend/apps/mobile/src/hooks/usePushNotifications.ts:240 — console.log('[push] registering native device token with backend') and :267 — console.log('[push] unregistering native device token with backend') are plain debug logs on the always-run push-registration path, not gated behind __DEV__ or a logger abstraction.",
        "Contrast with the rest of the segment, whose logging is exclusively console.warn/console.error inside catch blocks (useOfflineSupport.ts, NFCCredentialManager.ts, backgroundNotifications.ts, AuthContext.tsx). These two console.log calls are the only informational debug logs left on a hot production path.",
        "Impact: low — no functional break; release-log noise and a lint/hygiene smell. Fix: remove them or route through the app logger gated on __DEV__. Grouped here only as the one net-new hygiene item not covered by prior mobile-rn passes."
      ],
      "expert": "frontend"
    }
  ]
}
