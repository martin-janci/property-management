{
  "date": "2026-09-18",
  "generated_at": "2026-09-18T03:10:45Z",
  "signals": [
    {
      "id": "code-review-ppt-web-core-ocr-fetch-no-auth",
      "type": "code-review-finding",
      "source": "Phase 1.5 Tier-1d rotating expert review (ppt-web-core segment)",
      "score_delta": 3,
      "evidence": [
        "frontend/apps/ppt-web/src/features/meters/hooks/useOcrMeterReading.ts:26-51,54-72 — the OCR hooks call the app's OWN api-server endpoints (`POST /api/v1/ai/ocr/meter-reading` :45 and `POST /api/v1/ai/ocr/correction` :59) through a bare `ocrFetch()` (:26 `const response = await fetch(url, options)`) that never attaches an Authorization header. The image request (:44-49) passes only a FormData body with no `headers` at all; the correction request (:59-71) sets only `Content-Type: application/json`. ppt-web authenticates with a localStorage Bearer JWT injected by the axios interceptor / setTokenProvider (contexts/AuthContext.tsx:361-390, lib/api.ts:217-229) — there is no cookie session and no global fetch monkey-patch (confirmed: no window.fetch/globalThis.fetch override in src). So both OCR calls go out UNAUTHENTICATED and will 401, and they also omit the `X-Tenant-ID` tenant header, so even if a request landed it would not be org-scoped. Net effect: Epic-128 OCR meter-reading preview and its correction-feedback training loop are broken against a real backend.",
        "This is inconsistent with every sibling raw-fetch-to-own-API caller in this app, which all attach the Bearer token (and tenant header where relevant): features/notification-analytics/hooks/useNotificationAnalytics.ts:35-41 (`headers.set('Authorization' header with 'Bearer '+token)`) and features/accounting/api/paymentMatching.ts:64-73 (`headers.Authorization = 'Bearer '+token; headers['X-Tenant-ID'] = org`). Landable frontend-only remedy: route ocrFetch through getApiClient() (lib/api.ts — gains Bearer injection, 401 refresh-and-replay, X-Tenant-ID, and ErrorResponse→ApiError transform for free), or, if the raw multipart fetch must stay, attach `Authorization header (Bearer scheme) ${getToken()}` and `X-Tenant-ID` exactly as paymentMatching.uploadStatement does. Verifiable via `pnpm --filter @ppt/web typecheck` + a hook test asserting the outbound request carries the auth header.",
        "The sentiment (features/sentiment/hooks/useSentiment.ts:17-21) and predictive-maintenance (features/predictive-maintenance/hooks/usePredictiveMaintenance.ts:8-11) hooks carry explicit doc comments stating that routing through getApiClient() is what applies Bearer injection + 401 handling + retry — useOcrMeterReading is the lone hook that ignores that convention and uses a bare fetch."
      ],
      "confidence": "medium",
      "candidate_vector": "bug",
      "files": [
        "frontend/apps/ppt-web/src/features/meters/hooks/useOcrMeterReading.ts"
      ]
    },
    {
      "id": "code-review-ppt-web-core-needing-maint-key",
      "type": "code-review-finding",
      "source": "Phase 1.5 Tier-1d rotating expert review (ppt-web-core segment)",
      "score_delta": 2,
      "evidence": [
        "frontend/apps/ppt-web/src/features/predictive-maintenance/hooks/usePredictiveMaintenance.ts:59-70 — `useNeedingMaintenance(daysAhead = 30)` feeds `daysAhead` into the request URL (:64 `...?days_ahead=${daysAhead}`) but its TanStack Query key is the constant `predictiveKeys.needingMaintenance()` (:61 → :18 `[...all, 'needing-maintenance']`), which does NOT include `daysAhead`. So two callers requesting different horizons (e.g. 30 vs 90 days) collide on one cache entry: the second caller is served the first caller's cached data, and changing the `daysAhead` prop on a mounted component will not trigger a refetch. This contradicts the other two hooks in the same file, which correctly thread their query object into the key: useEquipmentList (:30-31 `predictiveKeys.equipment(query)`) and useMaintenancePredictions (:47-48 `predictiveKeys.predictions(query)`).",
        "Landable frontend-only remedy: parameterize the key — change predictiveKeys.needingMaintenance to `(daysAhead: number) => [...all, 'needing-maintenance', daysAhead]` and pass `daysAhead` at the call site (:61). Verifiable via `pnpm --filter @ppt/web typecheck` and a Vitest asserting distinct cache entries per daysAhead."
      ],
      "confidence": "medium",
      "candidate_vector": "bug",
      "files": [
        "frontend/apps/ppt-web/src/features/predictive-maintenance/hooks/usePredictiveMaintenance.ts"
      ]
    },
    {
      "id": "code-review-ppt-web-core-iot-ws-no-backoff",
      "type": "code-review-finding",
      "source": "Phase 1.5 Tier-1d rotating expert review (ppt-web-core segment)",
      "score_delta": 1,
      "evidence": [
        "frontend/apps/ppt-web/src/features/iot/hooks/useIotWebSocket.ts:26,84-95 — the IoT sensor WebSocket reconnect loop uses a FIXED 5s interval (`WS_RECONNECT_MS = 5_000` :26) with NO exponential backoff, NO jitter, and NO maximum-attempt cap: `ws.onerror` closes the socket (:84-86), `ws.onclose` unconditionally re-arms `setTimeout(connect, WS_RECONNECT_MS)` (:88-95), and connect() re-reads getToken() each cycle. On a persistent outage — server down, Redis unconfigured, or an expired/revoked token repeatedly rejected at the WS upgrade before AuthContext refreshes — the client hammers `/api/v1/iot/sensors/ws` forever every 5s across every mounted telemetry chart (thundering herd, server-log spam, needless battery/CPU/socket churn).",
        "The app already has the correct pattern in lib/websocket.ts:600-637 (WebSocketService.scheduleReconnect): exponential backoff `min(minDelay * 2**attempts, maxDelay)` with `[0.5,1.0)` jitter and a hard `maxReconnectAttempts` cap that flips shouldReconnect=false and emits `connection:max-retries-exceeded`. Landable frontend-only remedy: give useIotWebSocket the same capped backoff+jitter (track an attempt counter in a ref, compute delay like scheduleReconnect, stop after N attempts). Verifiable via `pnpm --filter @ppt/web typecheck` + a fake-timer Vitest asserting increasing delays and a cap."
      ],
      "confidence": "medium",
      "candidate_vector": "bug",
      "files": [
        "frontend/apps/ppt-web/src/features/iot/hooks/useIotWebSocket.ts"
      ]
    },
    {
      "id": "lag-warning-2026-09-18",
      "type": "lag_warning",
      "source": "routine self-check",
      "score_delta": 0,
      "evidence": [
        "Routine last ran 2026-09-01T18:38:58Z; today 2026-09-18. Lag: 16d ~= 400h > 36h threshold.",
        "Routine cron on cloud has drifted — dispatcher (separate branch loop) still ticking daily, but this routine has not written a brief since 2026-09-01."
      ],
      "confidence": "high",
      "candidate_vector": "dx"
    },
    {
      "id": "stale-routine-alert-2026-09-18",
      "type": "stale_routine_alert",
      "source": "routine self-check",
      "score_delta": 0,
      "evidence": [
        "state.last_run_iso=2026-09-01 is 16d old; scheduled routine likely paused.",
        "Dispatcher state on planning branch is fresh (0dd3ef75 2026-09-18 00:38); routine cron is what stopped."
      ],
      "confidence": "high",
      "candidate_vector": "dx"
    }
  ],
  "goal_checks": [
    {
      "goal": "G1",
      "passed": true,
      "command": "test -f briefs/2026-09-18.md && head -1 == '# 2026-09-18'",
      "observed": "exit 0",
      "expected": "exit 0"
    },
    {
      "goal": "G2",
      "passed": true,
      "command": "jq state advanced vs HEAD",
      "observed": "true",
      "expected": "true"
    },
    {
      "goal": "G3",
      "passed": true,
      "command": "jq unique ids",
      "observed": "true",
      "expected": "true"
    },
    {
      "goal": "G4",
      "passed": true,
      "command": "jq cumulative-type score-delta gate",
      "observed": "no re-emit",
      "expected": "true"
    },
    {
      "goal": "G5",
      "passed": true,
      "command": "score cap 8 + decay ≥1 for open 14d+",
      "observed": "3 decayed→dropped, 1 decayed only",
      "expected": "all applied"
    },
    {
      "goal": "G6",
      "passed": true,
      "command": "no new plans this run — vacuous",
      "observed": "0 promotions",
      "expected": "n/a"
    },
    {
      "goal": "G7",
      "passed": true,
      "command": "count new plans",
      "observed": "0",
      "expected": "≤2"
    },
    {
      "goal": "G8",
      "passed": true,
      "command": "staged files under .research/",
      "observed": "0 outside",
      "expected": "0"
    },
    {
      "goal": "G9",
      "passed": true,
      "command": "grep secret patterns in cached diff",
      "observed": "0",
      "expected": "0"
    },
    {
      "goal": "G10",
      "passed": true,
      "command": "diff backlog.md vs regen",
      "observed": "MATCH",
      "expected": "MATCH"
    },
    {
      "goal": "G11",
      "passed": true,
      "command": "grep placeholder rot in brief",
      "observed": "no hits",
      "expected": "no hits"
    },
    {
      "goal": "G12",
      "passed": true,
      "command": "prior-run plans present or archived",
      "observed": "all present",
      "expected": "all present"
    },
    {
      "goal": "G13",
      "passed": true,
      "command": "archive count ≥ HEAD count",
      "observed": "same 8",
      "expected": "≥"
    },
    {
      "goal": "G14",
      "passed": true,
      "command": "diff IDEAS_TRIAGE.md vs regen",
      "observed": "MATCH",
      "expected": "MATCH"
    },
    {
      "goal": "G15",
      "passed": true,
      "command": "auto_fix_actions length ≤ 3 with per-type caps",
      "observed": "0 actions (no candidate)",
      "expected": "true"
    },
    {
      "goal": "G16",
      "passed": true,
      "command": "management artifacts valid + GC7 coverage_cursor liveness",
      "observed": "action-list/risks/project-state OK; cc in range; PM cursor unchanged (Phase 1.6 skipped)",
      "expected": "PASS"
    },
    {
      "goal": "G17",
      "passed": true,
      "command": "grep telegram token patterns in added lines",
      "observed": "0",
      "expected": "0"
    }
  ],
  "auto_fix_actions": [],
  "auto_fix_skipped_reason": "no candidate — no allowlist signal matched confidence=high + score>=3 this run"
}
