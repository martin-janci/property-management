{"generator":"dev-review","generated_at":"2026-08-28T00:00:00Z","segment_reviewed":"ppt-web-core","selection_reason":"oldest-unreviewed","selection_detail":"No CHURN_FILES passed (standalone Tier-1d demand run, backlog starved). REVIEW_CURSOR oldest entries are api-core (2026-08-21) tied with ppt-web-core (2026-08-21). api-core was ALREADY reviewed today by a sibling Tier-1d run (signals/2026-08-28-api-core-tier1d.json exists and emitted 2 findings), and today's other tier1d runs cover api-handlers, ppt-web-ui, and reality-server. ppt-web-core has NOT been reviewed today, is co-oldest, and is cloud-landable (Frontend TS) — so it is the correct non-duplicative pick under the tie. Mobile-native (KMP) deliberately avoided per the dispatcher directive (structurally unlandable in this cloud runner; backlog already holds un-actionable KMP items). backlog.json has 0 open code-review-finding rows for ppt-web-core, so the segment is not saturated. Scope mapped to this repo's actual layout: the skill's ppt-web-core (hooks/, api/, stores/, router) maps to frontend/apps/ppt-web/src/{hooks,lib,contexts,routes} (19 core files; pages/ and components/ belong to the ppt-web-ui segment and were excluded). Static-only Frontend review: grep sweep for console.* (all DEV-gated), `as any`/`as unknown as` (none), unguarded JSON.parse (all wrapped in try/catch), useQuery/useMutation error handling, and effect cleanup. The segment is mature: lib/api.ts retry is idempotency-aware and DEV-gates its warn; lib/websocket.ts is token-rotation-aware with bounded exponential backoff and a reconnect budget; hooks (useWebSocket, useFocusTrap) have correct listener/subscription cleanup; tokenStorage getters swallow only storage-access exceptions and parse guards are present. The two residual findings are auth-token-at-rest / in-transit exposure choices the code itself flags as interim.","experts_used":["frontend"],"findings_count":2,"note":"ppt-web-core (Frontend/TS) — both findings land in frontend/apps/ppt-web/src, fully landable in the cloud runner. No mobile-native/KMP content.","signals":[{"id":"code-review-ppt-web-core-refresh-token-localstorage","type":"code-review-finding","source":"rotating-expert-review","segment":"ppt-web-core","score_delta":2,"confidence":"medium","candidate_vector":"bug","expert":"frontend","evidence":["frontend/apps/ppt-web/src/contexts/AuthContext.tsx:116,136-150 — the auth REFRESH token (and access token) is persisted in localStorage via tokenStorage.setRefreshToken/setAccessToken (REFRESH_TOKEN_KEY / ACCESS_TOKEN_KEY). localStorage is readable by any JavaScript running on the origin, so a single XSS foothold (a compromised dependency, an unescaped injection anywhere in the SPA) can exfiltrate the refresh token — a long-lived credential that grants persistent re-authentication and silent access-token minting even after the session tab closes. This is materially worse than access-token exposure because the refresh token outlives the page and cannot be invalidated by simply expiring the short-lived access token.","The code's own comment on line 116 (`// Token Storage (localStorage for MVP, httpOnly cookies later)`) marks this as acknowledged interim debt rather than a settled design — i.e. it is planned, actionable work, not a documented accepted tradeoff. Fix direction: move the refresh token to a Secure, HttpOnly, SameSite cookie issued by api-server (so JS cannot read it), keeping only the short-lived access token in memory (not localStorage); the api.ts getToken hook (lib/api.ts:291) and the refresh flow would read the access token from an in-memory holder and rely on the cookie for refresh. At minimum, isolate the refresh token from access to scripts.","Confidence medium: the localStorage persistence of the refresh token is confirmed by static read of AuthContext.tsx:136-150; the XSS-exfiltration impact is the well-established consequence of the storage choice, not a runtime-confirmed exploit in this app."]},{"id":"code-review-ppt-web-core-ws-jwt-in-url","type":"code-review-finding","source":"rotating-expert-review","segment":"ppt-web-core","score_delta":1,"confidence":"medium","candidate_vector":"bug","expert":"frontend","evidence":["frontend/apps/ppt-web/src/lib/websocket.ts:94-100 — buildWebSocketUrl appends the JWT as a URL query parameter (`url.searchParams.set('token', token)`) because the api-server reads WS auth from `?token=<jwt>` (comment at :86-92). JWTs in URLs are a recognized exposure: the full ws(s):// URL with the token can land in server/proxy access logs, browser history, and any diagnostics that capture request lines — leaking a bearer credential to systems that should never see it. Unlike an Authorization header, a query-string token is logged by default in most infrastructure.","This is a known browser constraint (the WebSocket handshake cannot carry custom headers), so query-param auth is a common compromise — hence low severity — but the safer, actionable pattern is a short-lived single-use connection ticket: the client fetches a one-time ticket over normal HTTPS (Authorization header), passes THAT (not the durable JWT) as `?ticket=`, and the server redeems+invalidates it on connect. Alternatively pass the JWT via the Sec-WebSocket-Protocol subprotocol header, which is not written to the URL. Either bounds the blast radius if a URL leaks.","Confidence medium: the token-in-URL construction is confirmed by static read of websocket.ts:94-100; the leakage channels (logs/history) are the standard consequence, and browsers do log request URLs, but no concrete leak was observed at runtime in this review."]}]}
