{
  "run_iso": "2026-09-11T14:25:00Z",
  "trigger_payload": "Tier-1d demand-driven code-review generation: dispatcher claimable buffer STARVED (0 landable open items; remaining backlog structurally cloud-gated on mobile-native/KMP AGP-egress + api-server utoipa-egress). Fresh LANDABLE review signals must be PRODUCED (not promoted). ONE rotating static-analysis segment, mode: cloud-ok (file reading + grep only), no compilation, no test execution. DISPATCHER_OWNED_COMMIT=1: this generator writes the signal artifact only under .research/signals/ and returns; the dispatcher orchestrator owns the single Phase 6 commit (no git add/commit/push here, no PR, no assignment/state mutation).",
  "segment_reviewed": "mobile-rn",
  "selection_reason": "Four Tier-1d slices already ran TODAY (2026-09-11): api-handlers, ppt-web-core, ppt-web-ui, reality-web (their signal files exist). Task directive: prefer a segment NOT already covered today, and cloud-landable FRONTEND segments are most valuable (backend + mobile-native/KMP explicitly named as currently unlandable in the cloud runner). Of the four uncovered segments (mobile-rn, api-core, reality-server, mobile-native-kmp), only mobile-rn is a cloud-landable frontend TS segment: api-core + reality-server are Rust (backend, not cloud-buildable), mobile-native-kmp is Kotlin (AGP-egress cloud-gated). All three web-frontend segments (ppt-web-ui/-core, reality-web) are already covered today. Note the segment map lists root frontend/mobile/src but the actual path is frontend/apps/mobile/src (@ppt/mobile, React Native + Expo); a pure-TS one-line fix here is verified by biome/typecheck/jest-expo in CI without any native prebuild, so it is cloud-landable (the finding below is a single string-constant change). Saturation check PASSED: 0 OPEN code-review-finding rows for mobile-rn in backlog.json (skill >=3-open skip rule not triggered). HONEST LOW YIELD: this segment is exceptionally well-hardened. The following were read and found clean/well-defended (each carries explicit issue-referenced reasoning, so re-flagging would be noise): useOfflineSupport.ts offline queue (401/408/429 correctly classified retryable via RETRYABLE_HTTP_STATUSES; strict-FIFO head-of-line blocking; transient 5xx never dropped — issues #1767/#2637/#2361), AuthContext.tsx (token-refresh-then-restore on biometric unlock, resetLocalData on org switch — #2399), resetLocalData.ts + localCacheKeys.ts (sweeps AsyncStorage cache/layout prefixes + exact tenant-scoped keys + SecureStore NFC purge — #2947/#2953), qrcode/universalLinks.ts + DeepLinkHandler.ts (HTTPS host allowlist + custom-scheme allowlist + fixed DEEP_LINK_ROUTES route table, so resolveDeepLinkTarget's `as Screen` cast is only ever reached with allowlisted screens — not a finding), utils/jwt.ts (decode-only, never verifies client-side by design, narrow isJwtExpired semantics), utils/idempotencyKey.ts (Math.random UUID documented-acceptable for dedup, not a security token), the documents folder sheets (NewFolderSheet/RenameFolderSheet/MoveDocumentSheet/MoveFolderSheet all wrap mutateAsync in try/catch + Alert), usePushNotifications.ts (token-registration console.log statements are __DEV__-guarded and explicitly never log the raw token). Hygiene grep clean: 0 `as any`, 0 skipped tests (xit/.skip/@Ignore), all console.* calls are legit catch-block logging or __DEV__-guarded. Only ONE genuine net-new defect surfaced.",
  "experts_used": ["frontend", "tester"],
  "findings_count": 1,
  "static_only": true,
  "signals": [
    {
      "id": "code-review-mobile-rn-wallet-passtype-namespace",
      "type": "code-review-finding",
      "source": "rotating-expert-review (dispatcher Tier-1d 2026-09-11 mobile-rn NFCAccessController.addToWallet builds the Apple Wallet pass with a SCRAMBLED reverse-DNS passTypeIdentifier — 'pass.bit.two.three.ppt.access' — that does not match the project namespace three.two.bit.ppt, so it cannot match the Apple Developer Portal pass-type registration / provisioning profile the code's own comment requires)",
      "segment": "mobile-rn",
      "expert": "frontend",
      "score_delta": 2,
      "confidence": "medium",
      "candidate_vector": "bug",
      "files": [
        "frontend/apps/mobile/src/nfc/NFCAccessController.ts",
        "frontend/apps/mobile/app.config.ts"
      ],
      "evidence": [
        "NFCAccessController.ts:383 sets `passTypeIdentifier: 'pass.bit.two.three.ppt.access'` when constructing the WalletPass for `addToWallet(credentialId)` (the iOS Apple-Wallet building-access credential path, method at :358-391). The reverse-DNS body is SCRAMBLED: it reads `bit.two.three` where the project's canonical namespace is `three.two.bit`. Per root CLAUDE.md the package namespace is `three.two.bit.ppt` and the iOS bundle is `three.two.bit.ppt.management`; app.config.ts confirms this at :250 (`bundleIdentifier: 'three.two.bit.ppt.management'`) and :318 (`package: 'three.two.bit.ppt.management'`). A correct Wallet pass type identifier would be `pass.three.two.bit.ppt.access` (or `pass.three.two.bit.ppt.management.access`), NOT `pass.bit.two.three.ppt.access`.",
        "Impact: Apple requires the passTypeIdentifier to be pre-registered in the Apple Developer Portal and to match an entitlement in the app's provisioning profile — the code's own comment at :382 says exactly this ('passTypeIdentifier must be configured in Apple Developer Portal and match the provisioning profile. See docs/ios-wallet-setup.md'). Because the string embeds a namespace that does not exist anywhere else in the project, `NativeWallet.addPass(...)` (:390) will fail for every iOS user attempting Add-to-Wallet on a building-access credential — the whole feature is dead on arrival unless someone also mis-registered the same typo in the portal (which would itself violate the reverse-DNS convention). This is a security-adjacent access-control feature (physical building entry via NFC-in-Wallet).",
        "Grep-confirmed uniqueness of the typo: `grep -rn 'bit.two.three|two.three.bit|three.two.bit'` across frontend/apps/mobile shows the scrambled `bit.two.three` ordering appears ONLY at NFCAccessController.ts:383 — every other namespace literal in the app uses the correct `three.two.bit` order (app.config.ts:250/318, usePushNotifications.test.ts:61/67/95 appId 'three.two.bit.ppt.management'). This is an isolated transposition, not a project-wide alternate convention.",
        "Landable frontend-only fix (single string constant, no native prebuild — verified by biome + tsc + jest-expo in CI): change NFCAccessController.ts:383 to `passTypeIdentifier: 'pass.three.two.bit.ppt.access'` (align the reverse-DNS body with app.config.ts bundle namespace), and confirm the same identifier is what docs/ios-wallet-setup.md instructs registering in the Apple Developer Portal. Confidence medium: the namespace mismatch vs the project convention is code-verified and unambiguous statically; the runtime add-to-wallet failure is inferred (the portal/provisioning config is not in the repo and cannot be executed in the cloud runner)."
      ]
    }
  ]
}
