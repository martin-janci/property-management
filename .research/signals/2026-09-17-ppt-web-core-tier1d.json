{"generated":"2026-09-17T00:00:00Z","source_run":"dispatcher Phase 2.6 Tier-1d demand-driven (claim buffer starved) 2026-09-17","segment_reviewed":"ppt-web-core","selection_reason":"OLDEST-UNREVIEWED (buildable-preferred). By the signals-corpus cursor (state.json review_cursor is stale relative to the tier1d signal files), the least-recently-reviewed segments are api-core (2026-09-09), mobile-native-kmp (2026-09-10), reality-server (2026-09-11). All three are structurally unbuildable/less-preferred in this cloud runner (backend api-server = swagger-ui build-script egress 403; reality-server = same Rust workspace; mobile-native KMP = AGP egress 403), so per the dispatcher's buildable-stack preference I picked the oldest FRONTEND segment not already reviewed today. reality-web AND ppt-web-ui AND mobile-rn already have 2026-09-17 tier1d files; the only frontend segment NOT yet covered today is ppt-web-core (last reviewed 2026-09-16). SATURATION CHECK PASSED: backlog.json holds 0 open code-review-finding rows for ppt-web-core (prior ones all done/dropped), so the <3-open saturation rule does not skip it. Static-only (no build/test): the ppt-web SPA (Vite) is buildable in principle in the cloud runner, but this pass is pure static read/grep per the skill contract; findings are backlog signals. NOTE (bonus, out-of-segment): while confirming the reality-web cursor I incidentally observed two net-new i18n gaps in the Epic-51 comparison feature (frontend/apps/reality-web/src/components/comparison/ComparisonTray.tsx and CompareButton.tsx are fully hardcoded English on the 4-locale portal, siblings of the already-fixed ComparisonView.tsx) — NOT emitted here because reality-web is a different segment already reviewed today; recorded in the return note for the dispatcher to route if desired.","experts_used":["frontend","security"],"findings_count":2,"static_only":true,"signals":[{"id":"code-review-ppt-web-core-route-groups-no-auth-guard","type":"code-review-finding","source":"rotating-expert-review (dispatcher Tier-1d 2026-09-17 ppt-web-core routes/groups)","segment":"ppt-web-core","score_delta":2,"confidence":"medium","candidate_vector":"security","expert":"frontend","files":["frontend/apps/ppt-web/src/routes/groups/financial.tsx","frontend/apps/ppt-web/src/routes/groups/accounting.tsx","frontend/apps/ppt-web/src/routes/AppRoutes.tsx","frontend/apps/ppt-web/src/components/ProtectedRoute.tsx"],"evidence":["ppt-web has NO global auth boundary: App.tsx mounts <AppRoutes /> directly (App.tsx:274) and authentication is enforced purely per-route by wrapping a page in <ProtectedRoute> (components/ProtectedRoute.tsx:121 redirects unauthenticated users to /login via <Navigate replace/>, and its requiredRoles gate at :128-143 is the only RBAC check). There is no top-level RequireAuth/layout route around the table.","11 feature route-groups register authenticated pages as BARE <Route element={<PageRoute/>}/> with ZERO <ProtectedRoute> wrapper, so an unauthenticated visitor to those paths is NOT redirected to /login (the page component mounts for anonymous users). Confirmed by grep across routes/groups/: financial.tsx (5 routes: /financial, /financial/invoices, /financial/payments, /financial/budgets, /financial/reports — financial.tsx:466-476, imports only `Route`), accounting.tsx (/accounting/invoices, /accounting/payment-matching — accounting.tsx:12-13, imports only `Route`), faults.tsx (5 routes :567-571), disputes.tsx (4 routes :481-484), outages.tsx (4 routes :367-370), community.tsx (6), messaging.tsx (3), neighbors.tsx (3), iot.tsx (6), ai-dashboards.tsx (2), announcements.tsx (4). None of these files contain the string `ProtectedRoute`.","This is INCONSISTENT with the ~10 groups that DO wrap every page in <ProtectedRoute>: buildings.tsx (19), leases.tsx (21), rentals.tsx (15), meters.tsx (13), documents.tsx (16), person-months.tsx (9), reports.tsx (5), voting.tsx (7), notifications.tsx (3), and core.tsx's protected subset (30). The split is arbitrary per-feature, which is exactly how a guard silently gets dropped when a new group file is authored by copying a bare template.","Impact: broken-access-control-in-depth + inconsistent UX. Underlying data is not directly leaked because the api-client calls are org-scoped and the server authenticates every request (the bare wrappers default `orgId = user?.organizationId ?? ''` and gate queries with `enabled: !!orgId`, so anon queries no-op), but anonymous users see the app shell / empty financial+accounting screens instead of a login redirect, and any client-only logic in those pages runs unauthenticated. This is a superset of the already-filed 2026-09-16 core.tsx dashboard-unguarded finding (which covered only /dashboard/*); these 11 feature groups are net-new and un-flagged. Fix: introduce a single auth boundary (wrap <AppRoutes/> children — except the public auth routes — in one <ProtectedRoute>, or a parent layout <Route element={<ProtectedRoute><Outlet/></ProtectedRoute>}>), rather than relying on every group file to remember the wrapper. Confidence medium: the missing guard + redirect semantics are confirmed by static read of ProtectedRoute + the group files, but exact anonymous-render behavior of each page is not runtime-confirmed."]},{"id":"code-review-ppt-web-core-financial-query-silent-error","type":"code-review-finding","source":"rotating-expert-review (dispatcher Tier-1d 2026-09-17 ppt-web-core financial route wrappers)","segment":"ppt-web-core","score_delta":2,"confidence":"medium","candidate_vector":"bug","expert":"frontend","files":["frontend/apps/ppt-web/src/routes/groups/financial.tsx"],"evidence":["The financial route wrappers issue TanStack useQuery calls but destructure only `{ data, isLoading }` — they never read `isError`/`error` and never surface a fetch failure to the user. FinancialDashboardPageRoute (financial.tsx:70-84) runs getARAgingReport, getOverdueInvoices and listInvoices; on any of these failing it falls back to zeros (`totals` default :88-94, `invoices = invoicesData?.invoices ?? []` :95) and renders the dashboard with totalOutstanding/totalOverdue = 0 and empty invoice counts — i.e. a FAILED load is indistinguishable from a legitimately-zero financial position. Same pattern in InvoiceManagementPageRoute (:157-167, `data?.invoices ?? []`) and PaymentManagementPageRoute (:265-284, payments/unallocated/invoices all `?? []`).","Contrast the correct pattern in the SAME file: FinancialReportsPageRoute (:401-462) threads `activeQuery.error` into an `error` prop (:458) so the reports page can render an error state. The dashboard/invoice/payment wrappers omit this, so the FinancialDashboardPage / InvoiceManagementPage / PaymentManagementPage components receive no error signal and have no way to show a retry/error affordance.","Impact: on a money-facing dashboard, a transient API failure (or an auth/permission error) renders as EUR 0 balances and 'no invoices/payments' rather than an error — a misleading correctness bug that could lead a manager to believe outstanding/overdue balances are zero. Fix: destructure `isError`/`error` from each useQuery and pass an `error` prop (as FinancialReportsPageRoute already does) so the pages can render an error+retry state instead of silently coercing failures to empty/zero data. Confidence medium: the missing error handling is confirmed by static read; whether the downstream page components have an error-state slot to fill is inferred from FinancialReportsPage's existing `error` prop."]}]}